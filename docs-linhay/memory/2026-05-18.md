# 2026-05-18

## Dedicated iOS simulator verification

- 按要求创建并使用专用模拟器 `TritonKit Dedicated iPhone 17`，UDID `0333546D-2AC6-4C22-AF01-293E2F4BA5BC`，runtime 为 iOS 26.5。
- XcodeBuildMCP defaults 固定到 `Examples/TritonKitDemo/TritonKitDemo.xcodeproj`、scheme `TritonKitDemo`、专用 simulator、DerivedData `/tmp/TritonKit-XcodeBuildMCP-DerivedData`。
- `triton serve --host 127.0.0.1 --port 19421` 启动后，`build_run_sim` 在专用模拟器上构建并启动 `TritonKitDemo` 成功。
- `triton status --json` 返回 `connected=true`、`latestHierarchyAvailable=true`、`runtime=embedded`、`targetCount=1`。
- 复杂 harness 复验通过：`TRITON_VERIFY_OUT_DIR=/tmp/triton-dedicated-complex-harness docs-linhay/scripts/verify-complex-harness.sh` 输出 `complex harness verification passed`，生成 `ax.json`、`input.jsonl`、`screenshot.png`、`archive.triton`。
- 复验最终状态为 `mode=Edit progress=60 count=3 switch=on text=complex-script`，截图已归档到 `docs-linhay/spaces/20260516-hybrid-transport-smoke/screenshots/20260518/ios/20260518-ios-complex-harness-dedicated-after-v01.png`。
- XcodeBuildMCP `snapshot_ui` 在该轮返回 AX translation 错误，因此 UI 层断言以 Triton 自身 `ax/input/screenshot/archive` 机器契约和截图为准。

## Local triton install error polish

- 用户本地运行裸 `triton` 时，在未启动 server 的情况下看到 Foundation `NSURLErrorDomain` 与 `LocalDataTask` 原始错误。
- 根因是 `TritonKitCLI` 默认子命令为 `List`，`List.run` 只在 JSON 模式下调用统一错误 envelope，text 模式直接抛出 `/targets` 的 `URLError`。
- 已补回归断言到 `docs-linhay/scripts/verify-cli-bootstrap.sh`：裸 `triton` 未连接 server 时不得泄漏 `NSURLErrorDomain/LocalDataTask`，必须包含 `server_unavailable` 与 `triton serve --host` 下一步提示。
- 已修复 CLI text 模式错误输出：裸 `triton`、`triton status` 以及通过 `resolveTarget` 的 text 模式 runtime 命令会输出稳定人读错误并退出 1；JSON 模式继续输出 `{ok:false,error:{...}}`。
- 验证通过：`swift build --product triton`、`docs-linhay/scripts/verify-cli-bootstrap.sh`、`swift test`、`swift build -c release --product triton`。已覆盖安装 `/Users/linhey/.local/bin/triton`，安装后二进制 SHA-256 为 `f260defece909e35d1c6ee63fb97a0a0a85c23b8eede1ed16a49a09a934ff590`。

## CLI localization switch

- 新增 `triton` 人读输出语言切换：`--language en|zh`、`--lang en|zh`、`TRITON_LANGUAGE`、`TRITON_LANG`。
- JSON 机器契约保持英文稳定；`triton version --language zh --json` 只新增发现字段 `language=zh` 与 `supportedLanguages=["en","zh"]`。
- 当前覆盖范围：裸 `triton` / `list` 无 server 错误、`status` text 输出、`doctor/capabilities` text 输出、`plan --format text` 标签与错误提示、`schema --format text` 常用标签、`version` text/JSON。
- 回归脚本 `docs-linhay/scripts/verify-cli-bootstrap.sh` 已新增中文语言断言：`triton --language zh` 与 `TRITON_LANGUAGE=zh triton` 在无 server 时必须输出 `服务器不可用` 和 `triton serve --host` 下一步提示。
- 验证通过：`swift test`、`docs-linhay/scripts/verify-cli-bootstrap.sh`、`triton version --language zh --json`、`triton plan --format text --language zh`。
- 用户指出 `triton -h` 仍是英文。已新增中文 help 拦截：当 `--language zh`、`--lang zh`、`TRITON_LANGUAGE=zh` 或 `TRITON_LANG=zh` 与 `-h/--help/help` 同时出现时，打印 Triton 自己的中文帮助文本；默认英文 help 保持 ArgumentParser 原生输出。
- 已验证：`triton --language zh -h`、`TRITON_LANGUAGE=zh triton -h`、`triton --language zh help input` 输出中文帮助；`docs-linhay/scripts/verify-cli-bootstrap.sh` 和 `swift test` 通过。

## CLI single-target default

- 用户希望 `triton hierarchy --target <target-id> --format tree` 在当前只有一个 target 时可以省略 `--target`。
- 决策：默认 target（`triton:local` 或 `local` alias）在 `/targets` 返回唯一目标时自动解析为该目标；显式其他 id 仍严格匹配；多个目标且未显式指定时返回 ambiguous，提示传入 `--target <id>`。
- 已补共享层纯函数 `TKResolveTargetSummary` 与 Swift Testing 覆盖，避免只依赖真实 server 进程测试。
- 验证通过：`swift test`、`swift build -c release --product triton`、`docs-linhay/scripts/check-docs.sh`；连接态安装后二进制执行 `triton hierarchy --format tree --no-refresh` 可在唯一 target `Overloaded` 上直接输出视图树。
- 已覆盖安装 `/Users/linhey/.local/bin/triton` 并重新 ad-hoc codesign；安装后二进制 SHA-256 为 `27e787256ea096bbade0fad706990a041b9c72b7affcc7b9e5de94388a5a5fc2`。

## Overloaded hierarchy depth investigation

- 用户在 Overloaded 上运行 `triton hierarchy --format tree` 只看到 `Overloaded.DebugShakeWindow -> UITransitionView -> UIDropShadowView -> UILayoutContainerView`。
- 根因 1：embedded runtime 的 `TKHierarchyBuilder.buildHierarchy` 默认 `maxDepth=5`，成熟 App 的真实业务内容位于 UINavigationController/容器更深层，采集阶段被截断。
- 已将 TritonKit runtime 默认采集上限调整为 `defaultMaxDepth=64`、`defaultMaxChildrenPerNode=200`，并同步到 Overloaded 本地包 `/Users/linhey/Desktop/FlowUp-Libs/Overloaded-org/components/TritonKit`。
- 根因 2：修深度后 Overloaded 会通过 WebSocket 发送约 270KB hierarchy payload，而 `triton serve` 的 Hummingbird WebSocket 默认 `maxFrameSize=16KB`，server 收到大帧后断开连接。
- 决策：host CLI server 显式设置 `tritonWebSocketMaxFrameSize=16_777_216`，为成熟 App 的完整层级、截图 metadata 和后续 archive 留足帧空间。
- 验证：重新构建安装 `/Users/linhey/.local/bin/triton`、重启 `triton serve --host 127.0.0.1 --port 19421` 后，Overloaded 在专用模拟器上重连成功；`triton hierarchy --format tree --no-refresh` 已输出 `SectionUI.SKCollectionView`、`RemoteFilesKit.LocalAlbumCardCell`、`RemoteFilesKit.RemoteAlbumCardCell`、`RemoteFilesKit.WallDisplayCardCell`、`UINavigationBar` 等真实页面子树。
- 机器统计：`/tmp/overloaded-hierarchy-after-depth.json` 节点数 `276`，最大 `indentLevel=28`，view class 数 `58`；截图写入 Overloaded 仓 `docs-linhay/screenshots/20260518/triton/20260518-triton-hierarchy-depth-after-ios-v01.png`。

## CLI hierarchy tree noise filtering

- 用户希望 `UITransitionView`、`UIDropShadowView` 这类基本无用的系统包装视图能在 CLI 配置项中隐藏。
- 决策：只在 `hierarchy --format tree` 的人读渲染层默认折叠低信号 UIKit 包装视图；`--json` 继续输出完整原始 hierarchy，避免破坏机器契约。
- 新增默认隐藏类：`UITransitionView`、`UIDropShadowView`。折叠策略保留子树，只隐藏该包装节点本身。
- 新增 CLI 开关：`--hide-noise/--no-hide-noise`，默认 `--hide-noise`；需要排查系统容器时可运行 `triton hierarchy --format tree --no-hide-noise`。
- 验证：`swift test` 通过 26 个 Swift Testing 用例；`swift build -c release --product triton` 与 `docs-linhay/scripts/check-docs.sh` 通过。
- Overloaded 实测：默认 `triton hierarchy --format tree --no-refresh` 输出不包含 `UITransitionView` / `UIDropShadowView`，但仍包含 `UILayoutContainerView`、`SectionUI.SKCollectionView` 等业务子树；`--no-hide-noise` 输出仍包含 `UITransitionView` / `UIDropShadowView`。
- 已覆盖安装 `/Users/linhey/.local/bin/triton` 并重新 ad-hoc codesign；安装后二进制 SHA-256 为 `7810225e3686686248d1a6688d0a93af1582296f657e56abfa4442426f35befd`。

## AX to hierarchy mapping and AX tap

- 用户确认 `triton hierarchy` 与 `triton ax` 是否能存在映射关系，并希望用于点击命中。
- 决策：新增 `triton ax --with-hierarchy`，保持原始 `triton ax --json` 输出兼容；新开关按 `AX.viewOID/targetOID -> hierarchy.viewObject.oid` 生成 `TKAXHierarchyMapResponse`，包含 `layerOID`、class、frame、path、mapped/unmatched 统计。
- 新增共享 helper `TKBuildAXHierarchyMap` 与单元测试，避免 CLI 里散落 ad-hoc jq join 逻辑。
- 新增 `triton tap --ax-oid <oid>` 与 `triton tap --ax-label <label>`；实现上优先把 AX 节点的 `targetOID/viewOID` 传给 embedded runtime 执行 in-app tap，仅当节点没有 oid 时才回退到 frame center 坐标。
- Overloaded 实测：`triton ax --with-hierarchy --json` 输出 `axNodeCount=16`、`hierarchyNodeCount=276`、`mappedCount=16`、`unmatchedCount=0`；`添加` 按钮映射到 `UIButton viewOID=295 layerOID=296`。
- Overloaded 实测点击：`triton tap --ax-oid 295 --json` 返回 `ok=true`、`targetClassName=UIButton`、`targetOID=295`；随后当前页面 `triton tap --ax-label 验证连接 --json` 返回 `ok=true`、`targetClassName=UIButton`、`targetOID=608`。
- 验证通过：`swift test` 通过 27 个 Swift Testing 用例，`swift build -c release --product triton` 通过；已覆盖安装 `/Users/linhey/.local/bin/triton` 并重新 ad-hoc codesign，安装后二进制 SHA-256 为 `4cc882951be472a6de32efd8df3b08e2353ec2f71f71ba25eee35d1a12f4a8ee`。
- 用户随后尝试 `triton tap --ax-label HTTP --json` 失败。定位原因：当前 AX 树没有 label 精确等于 `HTTP` 的节点；`HTTP` 是 `UISegmentedControl` 内部 segment 文本，当前 AX 节点暴露为 `role=segmentedControl,label=协议,value=HTTPS,targetOID=588`。
- 顺手修复两个 CLI 侧问题：`ax --with-hierarchy` 的 refresh 现在直接使用 `request(type:"hierarchy")` 返回的新 payload，避免拿旧 `/hierarchy/latest` 快照；`tap --ax-label` 精确匹配多个同名 label 时优先选择可操作 control/segmentedControl，而不是同名 `UILabel`。
- 修复后 Overloaded 当前页验证：`triton ax --with-hierarchy --json` 输出 `axNodeCount=54`、`hierarchyNodeCount=290`、`mappedCount=54`、`unmatchedCount=0`，`协议` 的 `UISegmentedControl` 映射到 `viewOID=588/layerOID=748`。
- 已重新构建安装 `/Users/linhey/.local/bin/triton` 并 ad-hoc codesign，安装后二进制 SHA-256 为 `c05faf20e46c5cdc33aed1f3b0d3388e6dbb027e7eaf038831f967bfedc4a2e7`。后续补齐 segmented option 级 AX 节点后，才应支持按 `HTTP` 这种 segment title 精确点击。
- 用户明确第一版不需要兼容旧 CLI 形态，期望主入口是 `triton tap "HTTP"`，由 Triton 自动区分 AX、hierarchy、oid、segmented option 等内部细节。
- 已新增 `tap` 位置参数 `<query>`：`triton tap "HTTP" --json` 会按用户意图解析文本、label、identifier、value 或可见选项标题；解析顺序为 AX 可操作节点、AX label/value、hierarchy 文本节点，命中文本或 segment label 时用其 frame center 点击。
- `tap` schema 已更新，示例首位为 `triton tap "HTTP" --json`；中文 help 使用 `triton tap [文本] [选项]`。
- 验证：`swift test` 通过 27 个测试，`swift build -c release --product triton` 通过，`triton schema --command tap --json` 暴露 `<query>` 与 `triton tap "HTTP" --json` 示例。因当前 Overloaded target 已断开（`targetCount=0`），未能在本轮继续做真实点击验证。
- 已覆盖安装 `/Users/linhey/.local/bin/triton` 并 ad-hoc codesign，安装后二进制 SHA-256 为 `3b5183d87ea213babdad66a92af45b96a12c5916f0e25141471a886364793da3`。

## Intent-first CLI command redesign smoke

- 用户继续要求“重新设计命令”，第一版确认以“意图优先”为主，不要求调用者区分 AX、hierarchy、oid、坐标或 segmented option。
- 当前主入口是 `triton tap "HTTP"`；定位入口是 `triton find "HTTP"`，两者共用解析器。解析顺序：AX 可操作节点、AX label/value/identifier/title、hierarchy 文本节点（包括 `UISegmentLabel`），最终生成 `TKInputRequest`。
- TritonKit runtime 调整 `UISegmentedControl` 触发：先设置 `selectedSegmentIndex`，再异步按已注册 target/action 使用 `UIApplication.sendAction` 派发 `.valueChanged`，避免直接调用 `sendActions(.valueChanged)` 的 UIControl 内部重入。
- 冒烟时发现 Overloaded 的 `DocumentWebdavConnectionSection` 存在独立 Swift 独占访问问题：`WebDAVEditor.scheme` setter 的 `SKPublished` 同步通知触发 sink，sink 立刻 `updateModels()` 又读取 `store.scheme`，导致 `Fatal access conflict detected`。已在 Overloaded 实际构建路径 `components/RemoteFilesKit/Sources/RemoteFilesKit/Editor/DocumentWebdavConnectionSection.swift` 将刷新延后一拍；误改的 `products/Overloaded/components/Document/...` 副本也保留同样修复。
- 专用模拟器 `TritonKit Dedicated iPhone 17` 复测通过：重建安装 Overloaded 后，`triton tap "添加" --json` 进入添加连接页，`triton find "HTTP" --json` 命中 `source=hierarchy-text`、`className=UISegmentLabel`，`triton tap "HTTP" --json` 返回 `ok=true`、`targetClassName=UISegmentedControl`；随后 `triton status --json` 仍为 `connected=true`，`triton ax --json` 读回 `UISegmentedControl.value="HTTP"` 且端口字段变为 `80`。
- 本轮验证：TritonKit `swift test` 通过 27 个 Swift Testing；`swift build -c release` 通过；已覆盖安装 `/Users/linhey/.local/bin/triton`；Overloaded Debug simulator build 通过。

## Overloaded intent-first spread smoke

- 用户要求继续扩散实验各种场景，并要求开 subagent 快速行动；主控 agent 负责集成、验证、文档和收口。
- 使用专用模拟器 `TritonKit Dedicated iPhone 17`（`0333546D-2AC6-4C22-AF01-293E2F4BA5BC`）和 Overloaded 仓库 `/Users/linhey/Desktop/FlowUp-Libs/Overloaded-org` 做真实 App smoke。
- 扩散覆盖：`triton tap "添加"` 进入添加连接页；`triton find/tap "HTTP"` 与 `HTTPS` 覆盖 hierarchy 文本 fallback 和 `UISegmentedControl` 派发，端口在 `80/443` 间联动；`triton tap "名称"` + `triton type --text "Triton Smoke"` 可写入 `UITextField`；disabled `验证连接` 返回 `ok=false,message=Target UIControl is disabled`；静态标题 `连接信息` 返回不可操作错误，没有误触业务动作；重复文本 `导入连接` 优先命中 `OUIComponents.SwissArmyRowButton` control。
- 发现并修复 runtime 控制通道阻塞问题：未知 `UIControl` 的 `.touchUpInside` 改为下一轮 main queue 异步派发，避免 App 业务 action 读取剪贴板弹出系统权限弹窗时让 `triton tap` 自身 HTTP 408。
- 新增 HTTP timeout 分类：`input/accessibility/geometry/hit/screenshot/hierarchy` 等 UI runtime 请求超时时返回 `error.code=runtime_ui_interrupted`，hint 说明 embedded runtime 不能 inspect 或点击 SpringBoard/CoreSimulatorBridge 系统弹窗；非 UI 请求仍保留 `request_timeout`。
- 新增脚本 `docs-linhay/scripts/verify-overloaded-triton-smoke.sh`。默认跑安全 smoke；设置 `TRITON_OVERLOADED_INCLUDE_SYSTEM_PROMPT=1` 才触发剪贴板系统弹窗并断言 `runtime_ui_interrupted`。
- 验证通过：`swift test` 通过 28 个 Swift Testing；`swift build -c release` 通过；Overloaded Debug simulator build 通过；安装 `/Users/linhey/.local/bin/triton` 后重新 ad-hoc codesign；`TRITON_BIN=/Users/linhey/.local/bin/triton TRITON_VERIFY_OUT_DIR=/tmp/triton-overloaded-smoke-safe docs-linhay/scripts/verify-overloaded-triton-smoke.sh` 输出 `overloaded triton smoke passed`。

## Subagent JSON envelope hardening

- 用户明确要求开 subagent 快速行动；主控启动 Pauli 做只读风险复核、Noether 做测试/脚本补强，并由主控集成修复。
- Pauli 发现高风险缺口：`ax/find/tap/geometry` 等命令在 `--json` 下遇到 runtime HTTP 408 时可能由 ArgumentParser 输出文本错误，而不是稳定 `{ok:false,error:{code:"runtime_ui_interrupted"}}`。
- 已修复 CLI HTTP 非 2xx 处理：若 server 返回 Triton error envelope，CLI 在 JSON 模式下原样透传 envelope 并退出非 0；text 模式继续输出人读错误。
- Noether 新增 `docs-linhay/scripts/verify-intent-cli-smoke.sh`，用 mock HTTP server 覆盖唯一 target 省略 `--target`、`triton tap "HTTP"`、找不到 label 的错误、以及 `geometry --json` 对 HTTP 408 `runtime_ui_interrupted` 的 envelope 透传。
- `docs-linhay/scripts/verify-overloaded-triton-smoke.sh` 的系统弹窗分支从 `grep runtime_ui_interrupted` 收紧为 `jq '.ok == false and .error.code == "runtime_ui_interrupted"'`，避免文本错误误判通过。
- 验证通过：`swift test` 通过 29 个 Swift Testing；`swift build -c release --product triton` 通过；安装 `/Users/linhey/.local/bin/triton` 后重新 ad-hoc codesign；`docs-linhay/scripts/verify-intent-cli-smoke.sh` 和安装版 mock smoke 通过；专用模拟器 Overloaded 安全 smoke `/tmp/triton-overloaded-smoke-final` 通过。

## Continued Overloaded validation hardening

- 用户要求继续验证。扩散验证时发现 `triton tap "名称"` 在 Overloaded cell 重建后可能拿到已失效 AX oid，runtime 返回 `Target oid is not a UIView`；决策：`textField/textView` query tap 改用 frame center 坐标策略，避免弱引用 oid 失效。
- 继续发现 `triton tap "HTTPS"` 可能先匹配 AX `value == HTTPS` 或隐藏旧 cell 的 `UISegmentLabel`，导致点到文本框；决策：query 解析顺序调整为 `AX label/identifier/title -> hierarchy 可见文本 -> AX value`，并让 hierarchy 文本候选继承祖先 hidden/alpha 可见性。
- Overloaded smoke 脚本新增全新安装前置处理：如果看到 App 内 `稍后再说` 权限引导，先点击进入主流程。
- 当前专用模拟器即使重装 Overloaded 也不再出现 pasteboard 系统权限弹窗；`TRITON_OVERLOADED_INCLUDE_SYSTEM_PROMPT=1` 改为记录 `system-prompt-skipped.txt` 并通过，只有额外设置 `TRITON_OVERLOADED_REQUIRE_SYSTEM_PROMPT=1` 时才强制要求弹窗中断。
- 验证通过：`swift test` 通过 29 个 Swift Testing；`swift build -c release --product triton` 通过；安装版 `/Users/linhey/.local/bin/triton` 重新签名，SHA-256 为 `471729faff274fb10f7c8f5ed12ec172ea8a89838f28690caa651674b183f30e`；`verify-intent-cli-smoke.sh` 通过；干净安装 Overloaded 后 `verify-overloaded-triton-smoke.sh` 安全分支通过；`TRITON_OVERLOADED_INCLUDE_SYSTEM_PROMPT=1` 分支通过并记录系统弹窗未出现。
- 继续将上述解析边界固化进 mock smoke：`verify-intent-cli-smoke.sh` 现在同时覆盖 `名称` textField 坐标策略、`HTTPS` 可见 hierarchy label 优先于 AX value、隐藏祖先内同名 `UISegmentLabel` 不参与候选、HTTP targetOID tap、找不到 label 和 runtime 408 envelope。安装版扩展 smoke、`verify-cli-bootstrap.sh`、`check-docs.sh` 均通过。

## Overloaded experience mining and no-op UIControl guard

- 用户要求继续体验挖掘新的场景和能力；主控继续使用专用模拟器 `TritonKit Dedicated iPhone 17` 与 Overloaded 真实 App。
- 新场景结论：`input --summary --strict` 只负责失败时非 0，不会自动阻止后续动作；真实批量动作若前一步 tap 坐标失败，后续 `type` 可能继续写入旧 first responder。推荐 agent 默认使用 `input --summary --strict --fail-fast`。
- 新场景结论：`tap "地址"` 后串行 `type --text ...` 能稳定写入地址字段；但并发发出依赖 first responder 的命令会产生竞态，出现 `No target responder`。agent 对观察/动作依赖链必须串行。
- 新场景结论：`ax --with-hierarchy` 在 Overloaded 添加连接页能做到 AX 与 hierarchy 全映射，但复杂表单会暴露多份同名 `地址`、`协议`、`验证连接` 候选；后续需要候选去重、可见性/current 标记或 ambiguity 解释。
- 新场景结论：`hit` 对自定义 `OUIComponents.SwissArmyRowButton` 可提升到可操作父控件；但缺少 raw hit、actionable target、提升路径、reason 和 suggested input，仍不足以让 agent 自动修正坐标。
- 新场景结论：`attrs` 对 `UITextField`/`UISegmentedControl` 仍只有 class/layout/layer 样式，缺少 text、placeholder、selectedSegmentIndex、segments、firstResponder、targetActions 等对 agent 断言和解释更有价值的字段。
- 修复：未知 `UIControl` fallback 不再无条件返回 `ok=true` 并派发 `.touchUpInside`；现在只在发现 `.primaryActionTriggered` 或 `.touchUpInside` target-action 时异步派发，否则返回 `ok=false,message=Target UIControl has no primary or touchUpInside action`。这修掉 `triton tap "添加连接"` 命中 `_UINavigationBarTitleControl` 后假成功的问题，同时保留 `triton tap "导入连接"` 对 `OUIComponents.SwissArmyRowButton` 的真实 action。
- 已同步同一 runtime 修复到 Overloaded 本地包 `/Users/linhey/Desktop/FlowUp-Libs/Overloaded-org/components/TritonKit`，并使用专用模拟器重建安装 Overloaded。
- 验证通过：`swift build -c release --product triton`、`swift test`、安装版 `docs-linhay/scripts/verify-intent-cli-smoke.sh`、Overloaded Debug simulator build、干净安装 Overloaded 后 `TRITON_BIN=/Users/linhey/.local/bin/triton TRITON_VERIFY_OUT_DIR=/tmp/triton-overloaded-smoke-nav-title-2 docs-linhay/scripts/verify-overloaded-triton-smoke.sh` 均通过。安装版 CLI SHA-256 仍为 `471729faff274fb10f7c8f5ed12ec172ea8a89838f28690caa651674b183f30e`。
