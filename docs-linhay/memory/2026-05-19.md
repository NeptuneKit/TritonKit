# 2026-05-19

## TritonKit real-device smoke revalidation

- 使用 `TritonKit Dedicated iPhone 17`（UDID `0333546D-2AC6-4C22-AF01-293E2F4BA5BC`，iOS 26.5）复验当前本地 `triton` release build。
- `swift build -c release --product triton`、`swift test`（29 个 Swift Testing 用例）、`docs-linhay/scripts/check-docs.sh`、`docs-linhay/scripts/verify-cli-bootstrap.sh`、`docs-linhay/scripts/verify-intent-cli-smoke.sh` 均通过。
- 启动 `.build/release/triton serve --host 127.0.0.1 --port 19421` 后，`TritonKitDemo` 在专用模拟器 build/run 成功，`triton status --json` 返回 `connected=true`、`runtime=embedded`、`targetCount=1`。
- Demo 复杂 harness 真实复验通过：`TRITON_BIN=.build/release/triton TRITON_VERIFY_OUT_DIR=/tmp/triton-dedicated-complex-harness-20260519 TRITON_COMPLEX_TEXT=complex-20260519 docs-linhay/scripts/verify-complex-harness.sh` 输出 `complex harness verification passed`，生成 `ax.json`、`input.jsonl`、`screenshot.png`、`archive.triton`。
- Overloaded 使用同一专用模拟器构建运行成功，bundle id 为 `overloaded.cn`；`triton list --json` 确认 target 切换为 `Overloaded`。
- Overloaded safe smoke 串行复验通过：`TRITON_BIN=.build/release/triton TRITON_VERIFY_OUT_DIR=/tmp/triton-overloaded-smoke-20260519-safe-serial TRITON_OVERLOADED_NAME='Triton Smoke Safe Serial 20260519' docs-linhay/scripts/verify-overloaded-triton-smoke.sh`。
- Overloaded system prompt 分支复验通过：`TRITON_OVERLOADED_INCLUDE_SYSTEM_PROMPT=1` 后，`tap-import.json` 返回 `Dispatched UIControl.touchUpInside`，随后 `ax-after-import.json` 稳定返回 `ok=false,error.code=runtime_ui_interrupted`，说明 embedded runtime 对系统弹窗阻塞的错误 envelope 符合预期。
- 调整 `docs-linhay/scripts/verify-overloaded-triton-smoke.sh`：名称输入框断言从精确等于本轮文本改为包含本轮文本。原因是 `triton type` 语义是插入文本，重复执行 smoke 时字段可能保留上轮内容；新断言仍验证本轮写入成功，同时让脚本可重复执行。

## 风险与后续

- 当前仓库仍有大量未提交改动，覆盖 CLI、runtime、shared DTO、测试、文档、脚本和 Baguette 参考资料；后续应按主题拆分提交。
- Overloaded 仓库本身也有大量既有未提交改动，本轮只作为真实 App 验收环境使用，不应把这些外部仓变更混入 TritonKit 提交。

## TritonKit dev feedback skill

- 新增项目级 skill：`.agents/skills/tritonkit-dev-feedback/SKILL.md`。
- 目的：TritonKit 处于开发阶段，外部使用者试用、接入或评估时提出的任何需求、bug、文档缺口或兼容性问题，都可以转成 `NeptuneKit/TritonKit` GitHub issue。
- 关键约束：AI agent 应负责复现/澄清、收集证据并直接提交 issue；不要默认让用户自己去仓库提 issue。
- 验证：使用 `$skill-creator` 初始化，临时 venv 安装 `PyYAML` 后运行 `quick_validate.py .agents/skills/tritonkit-dev-feedback`，结果为 `Skill is valid!`。

## GitHub CI and release artifacts

- 新增 `.github/workflows/ci.yml`，在 `push main`、`pull_request main`、`workflow_dispatch` 和 `v*` tag 上运行。
- CI 执行 `swift test` 与 `swift build -c release --product triton`。
- Workflow artifact 与 tag release asset 均包含 CLI 包：`triton-macos-<arch>.tar.gz` / `.zip`。
- Workflow artifact 与 tag release asset 均包含 skill 包：`tritonkit-dev-feedback.tar.gz` / `.zip`。
- 同步更新 `AGENTS.md` 与 `.agents/skills/tritonkit-ops-governance/SKILL.md`：GitHub CI / Release 产物必须包含 `triton` CLI 和项目级 skill 包。

## Package Manager Debug-only runtime

- 明确 PM 集成约束：embedded TritonKit runtime 只在 `DEBUG` 编译配置下生效，不按端类型或 UIKit 可导入性决定是否启用。
- Release 下 package 保持可编译，但 runtime 不连接、不采集、不上传、不响应控制。
- 新增 `TritonKit.isRuntimeEnabled` 与 `TritonKitRuntimeError.disabledOutsideDebug`，并在 `connect` / `send` / reconnect / ping、request handler、hierarchy 采集、data upload 路径上落地。
- 同步更新 `AGENTS.md`、`.agents/skills/tritonkit-ops-governance/SKILL.md` 与 `docs-linhay/dev/20260519-debug-only-pm-runtime.md`。
- 验证通过：`swift test`、`swift build -c release --target TritonKit`、`swift build -c release --product triton`、`swift test -c release`。

## Session distillation

- 按用户“整理会话”要求完成本轮沉淀，新增 `docs-linhay/dev/20260519-session-distillation.md`。
- 本轮保留三条可复用模式：开发阶段反馈由 AI 直接提 GitHub issue；CI/release 同时产出 CLI 包和项目级 skill 包；PM 集成只按 `DEBUG` / Release 编译配置启停 embedded runtime。
- 明确不沉淀“按端类型拆分 PM 集成”的误判；后续遇到 PM/环境相关表述，先区分编译配置、平台、包管理器和发布产物。

## CocoaPods support

- 新增 CocoaPods 分发入口：`TritonKitShared.podspec` 与 `TritonKit.podspec`。
- 保持 SwiftPM 相同 module 边界：`TritonKit` 依赖同版本 `TritonKitShared`，CocoaPods 不打包 `Sources/TritonKitCLI`。
- CI 新增 podspec lint：`pod lib lint TritonKitShared.podspec --allow-warnings --skip-tests` 与 `pod lib lint TritonKit.podspec --include-podspecs=TritonKitShared.podspec --allow-warnings --skip-tests`。
- 同步更新 `AGENTS.md`、`.agents/skills/tritonkit-ops-governance/SKILL.md` 与 `docs-linhay/dev/20260519-cocoapods-support.md`。

## iOS integration guide

- 新增仓库入口 `README.md`，覆盖 SwiftPM、CocoaPods、Debug-only App bootstrap、CLI server 启动与 `status/list/hierarchy/ax` 验证命令。
- 更新已发布的项目级 skill `.agents/skills/tritonkit-dev-feedback/SKILL.md`，让 AI agent 帮用户试用或接入 TritonKit 时能直接提供 iOS 侧接入指南。
- 新增 `docs-linhay/dev/20260519-ios-integration-guide.md`，记录 README 与 skill 的接入口径：PM 安装、强引用 `TritonKitRequestHandler`、设置 `dataURL`、模拟器/真机 host 差异、本地网络与 ATS 注意事项、Release no-op 边界。

## Remove Objective-C TritonKitCore

- 确认 `Sources/TritonKitCore` 是旧 Lookin Objective-C core 残留，当前 `Package.swift`、`TritonKit.podspec`、`TritonKitShared.podspec` 均不引用该目录。
- 决策：直接删除 `Sources/TritonKitCore`，不通过 `.gitattributes linguist-vendored` 隐藏语言占比；如需参考旧实现，通过 git 历史回看。
- 目标：让仓库语言统计和当前正式交付面保持一致，即 Swift embedded runtime、Swift shared DTO 与 macOS `triton` CLI。

## Unused Swift runtime pruning

- 继续清理 Swift runtime 残留：删除未被 runtime/CLI/tests/docs 引用的 `TKAutoLayoutConstraint`、`TKConstraintItemType`、`TKAttributesSectionStyle`。
- 删除未使用 helper：`CALayer.tk_topmostHostView`、`UIView.tk_memoryAddress`、`TritonKitDataUploader.captureScreenshot(view:)`、`captureAndUpload(view:)`。
- 收窄 `TKHierarchyBuilder.buildHierarchy` 参数，移除未生效的 `includeScreenshots` / `uploader` 传递；截图正式路径保留在 `TritonKitRequestHandler.handleScreenshot`。
- 保留 `TKDisplayItem` wire 字段、`TKDisplayItemDetail`、`TKEventHandler`、`TKStringTwoTuple`，因为它们仍属于 hierarchy/raw request JSON 契约面。
