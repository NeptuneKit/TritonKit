# 2026-05-20

## Homebrew install guidance in skills

- 同步 Homebrew 二进制安装与更新信息到项目级 skills。
- `tritonkit-real-project-regression`：真实项目回归优先使用 released Homebrew `triton` CLI；只有验证未发布本地源码时才使用 `swift build -c release --product triton`。
- `tritonkit-dev-feedback`：iOS 接入 checklist 增加 `brew install NeptuneKit/tap/triton`、`brew update && brew upgrade triton`、GitHub Release 资产和 fallback 说明。
- `tritonkit-ops-governance`：记录默认 tap `NeptuneKit/homebrew-tap`、`TAP_GITHUB_TOKEN` 要求，以及对外接入文档/skill 的 Homebrew 优先口径。

## CI-written CLI and skill versions

- 决策：CLI 与对外发布的 skill 包必须携带同一个 CI 解析出的版本号。
- `v*` tag 构建解析为正式版本，例如 `v1.2.3` -> `1.2.3`；非 tag 构建解析为 `0.1.0-dev+<short-sha>`。
- 新增 `TritonKitBuildInfo.cliVersion`，CI 构建前用 `docs-linhay/scripts/write-cli-version.sh` 写入 `Sources/TritonKitCLI/main.swift`，`triton version --json` 读取同一个常量。
- 新增 `docs-linhay/scripts/stamp-skill-version.sh`，CI 打包 skill 前向 `SKILL.md` front matter 写入 `metadata.version` 字段，保持 skill validator 兼容。
- 新增 `docs-linhay/scripts/verify-version-stamping.sh` 覆盖版本解析、Swift 版本常量写入和 skill front matter 更新。

## Issue #2 Homebrew pre-release fallback

- 线上确认：`NeptuneKit/TritonKit` 暂无 GitHub Release，`NeptuneKit/homebrew-tap` 仓库暂不存在。
- 决策：首个 `v*` release 与 tap 仓库可用前，README 与项目级 skill 的 CLI 接入指南必须先给 `swift build -c release --product triton` 本地 release CLI fallback，Homebrew 只作为发布后路径。

## Issue #1 target cache state

- 根因：server 的 `TargetState` 同时保存当前连接 identity 与 latest hierarchy cache；新 WebSocket 连接建立时未清空当前 identity，可能在新 App 的 app info / hierarchy 返回前让 `/targets` 暴露上一轮 App metadata。
- 修复方向：新连接和断开时清空当前 identity、active hierarchy 标记和 pending responses，但保留 latest hierarchy cache；WebSocket 断开按 connection generation 清理，避免旧连接迟到退出覆盖新连接；`status` / `doctor` / `list` JSON 增加 `activeHierarchyAvailable`、`hierarchyCacheState`、`targetConnectionState` / `identityState` 等字段，明确 active target 与 stale cache 的区别。

## Issue #4 visible UIKit text in AX

- 根因：`TritonKitRequestHandler.collectAXLeafNodes` 以安全叶子节点为主，`UIScrollView` / `UICollectionView` 容器导出时不携带子节点；同时 AX 构建深度和节点预算偏保守，复杂 collection-view 页面容易在深层 cell 文本前停止。
- 修复：AX 构建改为 scroll 容器嵌套子节点，默认 `maxDepth` 提升到 32、`maxNodes` 提升到 800；空 `UIImageView` / 空 label 不再进入 AX 输出；文本兜底覆盖 `UILabel.attributedText`、`UIButton.currentAttributedTitle`、`UITextField.placeholder` 等公开 UIKit 属性。
- 验证：新增 iOS-only UIKit 回归测试描述 collection view 深层 label；macOS `swift test` 通过 32 个可执行测试，`xcodebuild build -scheme TritonKit -destination 'platform=iOS Simulator,name=iPhone 17'` 通过。iOS package test 目前被 `triton` 可执行目标依赖的 iOS 15/17 minimum deployment 阻断，后续可独立整理测试 scheme。

## Issue #3 exact text input

- 决策：精确文本输入先使用 embedded runtime 内公开 UIKit `UIKeyInput` API，不依赖 Simulator pasteboard、AppleScript 或 host-side HID；`paste` 语义是 exact insertion，而不是系统剪贴板粘贴。
- 新增 `TKInputType.paste` / `clear`、`triton paste <text>`、`triton clear`，均支持当前 first responder、`--oid` 和 `--x/--y` 坐标聚焦路径；`type --exact` 保留为显式精确输入口径。
- 敏感字段通过 `--secure` 标记，结果只回显 `secure/redacted/insertedLength`，不在 `TKInputResult` 中回显原始文本。

## Issue #5 wait/assertion commands

- 新增 `triton wait`，支持 `--text`、`--gone`、`--exists --role`、`--idle`、`--hierarchy-change --since latest` 与安全 `--predicate`。
- JSON 默认返回 `TKWaitResult`，包含 `condition/query/predicate/elapsedMs/pollCount/timedOut/targetConnectionState/hierarchyCacheState/lastObservedTextSample/match`；超时输出 `ok=false,timedOut=true` 并以非 0 退出。
- `--predicate` 只支持内置安全函数与布尔组合：`text.exists`、`text.gone`、`exists`、`gone`、`&&`、`||`、`!`，不执行脚本代码。
- `--idle` 使用 AX 可见文本签名连续稳定作为可交互近似信号，避免完整 hierarchy 内部字段波动导致误判。

## Issue #6 evidence bundle export

- 新增 `triton evidence`，用于真实项目回归和 GitHub issue 附件证据包导出；首期证据包是 `.tritonevidence` 目录，固定包含 `manifest.json`。
- 默认采集 `status/list/version/hierarchy/ax/screenshot`；可通过 `--include` 选择 `status,list,version,hierarchy,ax,screenshot,geometry,archive,logs`，当前 embedded runtime 不支持的 `logs` 会进入 `manifest.skipped`。
- `manifest` 记录 `formatVersion`、`artifacts[]`、`skipped[]`、target identity、connection/cache state、CLI version 与 artifact freshness；`triton evidence inspect <dir> --json` 可离线读取 manifest。
- README、`ai-cli-readable-control`、真实项目回归 handoff 与项目级 skills 已同步改为优先使用 `triton evidence --name <case> --output <dir.tritonevidence> --json`。

## Issue #7 record/replay smoke flows

- 新增 `.tritonplan` schema version 1 和共享模型 `TKReplayPlan` / `TKReplayPlanStep` / `TKReplayResult`。
- 新增 `triton record --output <file.tritonplan> --json`，首期只生成可编辑模板并明确 `templateOnly=true`，不伪装成交互式真实录制。
- 新增 `triton plan inspect <file.tritonplan> --json`，可离线查看 plan 摘要。
- 新增 `triton replay <file.tritonplan> --json` 与 `--dry-run`；支持 `tap/paste/type/clear/wait/screenshot/evidence`、`${variable}`、`--var key=value`、`--var key-env=ENV_NAME` 和 secure 值脱敏。
- 对外文档与项目级 skills 已同步要求复用真实项目 smoke 时优先沉淀 `.tritonplan`，并把密码等敏感值保持在环境变量中。

## Issue #8 agent regression capture/assertions

- 新增 `triton capture --case <case> --output <dir.tritonevidence> --json`，作为回归友好的一站式 evidence 包入口，默认包含 `status/list/version/hierarchy/ax/screenshot/geometry/archive`。
- 新增 `triton assert text-exists|text-not-exists <text> --json`，返回 `ok/count/matches/sample/targetConnectionState/hierarchyCacheState/message`，断言失败以非 0 退出。
- `assert` 支持 `--role`、`--count`、`--min-count`、`--max-count`、`--within x,y,width,height`，用于重复文本与局部列表 stale 文本证明。
- deep-link launch 暂未进入首期实现，后续应作为 host-side simulator/app adapter 单独设计。

## Delivery flow optimization

- 决策：普通 `main` push / PR 的 CI 只阻塞 validate；双架构 CLI build、skill 包、checksum、release asset 打包只在 `v*` tag 或手动 `workflow_dispatch` 执行。
- 新增 `docs-linhay/scripts/verify.sh`，本地默认门禁为 `--local`，CI validate 门禁为 `--ci-validate`。
- 新增 `docs-linhay/scripts/gh-issue-comment-file.sh`，所有包含 Markdown 命令片段的 issue 评论默认走 `gh --body-file`，避免反引号被 shell 执行。
- 新增 `docs-linhay/scripts/gh-run-summary.sh`，低噪音观察 GitHub Actions run；失败后再看完整日志。
- 新增 `docs-linhay/scripts/qmd-sync.sh` 固定项目 qmd 同步入口；当前 qmd 本体仍不支持 scoped `update/embed`，因此脚本会明确提示并执行全量维护。

## CLI action shorthand

- 决策：面向 agent 的输入动作命令默认输出 JSON，减少 `--json` 高频样板；`--format text` 继续保留给人读输出。
- `triton tap "取消"` 等价于原先用于机器解析的 `triton tap --json "取消"`；同类动作 `find/tap/swipe/type/paste/clear/press` 默认 JSON。
- `triton type <text>` 新增位置参数入口，旧的 `triton type --text <text>` 保持兼容；两者同时出现时返回 validation error。
- 已同步项目级 skills：治理 skill 记录 action 默认 JSON 规则，真实项目回归与开发反馈 skill 改用简写示例。
- 已用 `docs-linhay/scripts/verify-intent-cli-smoke.sh` 覆盖无 `--json` 的 tap、`type "hello"`、schema 默认值；`swift test` 通过。

## CLI duplicate text target disambiguation

- 决策：同页面出现多个相同文案时，保留 `triton tap "<text>"` 选择第一个候选的兼容行为，同时新增机器可读消歧入口。
- `triton find "<text>" --all` 返回 `matchCount` 与 `candidates[]`，每个候选包含 `index/source/strategy/frame/targetOID/viewOID/request`。
- `triton tap "<text>" --index <n>` 按 `find --all` 的 1 起始序号点击；`triton tap "<text>" --within x,y,width,height` 先按区域过滤再选择候选。
- `docs-linhay/scripts/verify-intent-cli-smoke.sh` 已覆盖两个 `hello` 目标的 `find --all`、`tap --index 2` 与 `tap --within`。

## CLI point shorthand sweep

- 交互命令扫描结论：`--within x,y,width,height` 保持区域过滤语义；只需要指定一个点位时新增 `--at x,y`，避免把点位伪装成零尺寸区域。
- `triton find "<text>" --at x,y` 与 `triton tap "<text>" --at x,y` 会筛选 frame 包含该点的同文案候选；`triton tap --at x,y`、`triton hit --at x,y`、`triton paste "<text>" --at x,y`、`triton clear --at x,y` 作为坐标简写，旧的 `--x/--y` 保持兼容。
- `triton press <button>` 新增位置参数入口，`triton press --button <button>` 保持兼容；两者同时提供时返回 validation error。
- 已同步 README、`ai-cli-readable-control`、device control 文档、真实项目回归 handoff 与项目级 skills；`verify-intent-cli-smoke.sh` 覆盖 `find/tap --at`、坐标类 `--at`、`press home` 和 schema 暴露。

## Issue #9 atomic CLI replacement docs

- 风险结论：当 `triton serve` 正从 `~/.local/bin/triton` 等路径运行时，直接 `cp .build/release/triton <active-path>` 覆盖旧文件可能导致后续新进程被 macOS kill，表现为无 stdout/stderr 的 exit 137。
- 决策：所有本地 fallback、手动 release asset 和 skill 中的现有 CLI 路径更新说明，必须推荐先停止 `triton serve`，或使用 `cp .build/release/triton ~/.local/bin/triton.new && mv ~/.local/bin/triton.new ~/.local/bin/triton` 原子替换。
- 已同步 README、Homebrew 发布设计文档、真实项目回归 handoff、`tritonkit-ops-governance`、`tritonkit-dev-feedback` 与 `tritonkit-real-project-regression`。

## Release v0.1.0

- 决策：首个公开二进制版本发布为 `v0.1.0`，原因是仓库此前没有 tag / release，CI 版本脚本默认基础版本也是 `0.1.0`。
- 发布结果：`https://github.com/NeptuneKit/TritonKit/releases/tag/v0.1.0` 已发布，包含 macOS arm64 / x86_64 CLI 包、checksum manifest，以及 `tritonkit-dev-feedback` / `tritonkit-real-project-regression` skill 包。
- Homebrew：初次 release workflow 的 tap 更新因缺少 `TAP_GITHUB_TOKEN` 且 `NeptuneKit/homebrew-tap` 不存在而失败；已手动创建 `NeptuneKit/homebrew-tap` 并提交 `Formula/triton.rb`，`brew info NeptuneKit/tap/triton` 可解析到 `0.1.0`。
- 验证：release run 的 Validate、双架构 CLI build、release asset package 成功；本地 `brew fetch --formula NeptuneKit/tap/triton` 成功；下载 arm64 release 包后执行 `triton version --json` 返回 `version=0.1.0`。
- 后续：未来自动更新 Homebrew tap 仍需在 `NeptuneKit/TritonKit` 配置专用 `TAP_GITHUB_TOKEN`，避免把本地个人 `gh` token 直接写入仓库 secret。

## Release automation

- 决策：维护者发布新版本默认走 `docs-linhay/scripts/release.sh <version>`，脚本负责检查 clean worktree、`main` 同步、tap 仓库、`TAP_GITHUB_TOKEN`、本地门禁、annotated tag 推送、CI 观察、Release checksum/formula 验证和 Homebrew fetch。
- 修复：CI 的 release asset 验证不再硬编码 `v0.1.0`，改为从当前 tag 或 CI 解析版本动态生成 `formula_tag`。
- Secret：已在 `NeptuneKit/TritonKit` 配置 `TAP_GITHUB_TOKEN`，并手动触发 `update-homebrew-tap.yml` 对 `v0.1.0` 重跑成功，证明 tap 自动更新权限可用。
- 门禁：新增 `docs-linhay/scripts/verify-release-automation.sh` 并接入 `verify.sh --ci-validate`，防止发布脚本、动态 formula tag、tap workflow 手动重跑能力退化。

## xcrun host-side adapter research

- 新建 `docs-linhay/spaces/20260520-xcrun-host-adapter-research/`，调研 Xcode 26.5 / `xcrun version 72` 下 TritonKit 可复用的 `simctl`、`devicectl`、`xcdevice`、`xctrace`、`xcresulttool` 能力。
- 关键结论：其他 AI agent 在真实项目回归中直接使用 `xcrun simctl openurl/get_app_container`，不是偏好裸命令，而是 Triton CLI 还没有覆盖 host-side simulator/device adapter；应补 `triton app open-url`、`app container`、`app prefs get/dump`、`sim privacy/location/ui`、`logs stream` 等 JSON 契约。
- `xcrun` 应作为 CLI 内部实现资源，不应继续成为 agent 默认面对的产品接口；裸 `xcrun` 示例后续应在 skills 和真实项目文档中替换为 `triton` 命令，只作为 unsupported fallback。
- `SKProcessRunner` 可作为候选进程执行底座，优势是 SwiftPM、cwd/env、stdout/stderr 捕获、流式输出、timeout、截断、PTY 与长生命周期 pipe session；依赖边界必须限制在 macOS CLI / host adapter，不进入 embedded runtime。

## Simulator takeover design

- 新建 `docs-linhay/spaces/20260520-simulator-takeover/`，将“全面接管模拟器”拆为需求、BDD、技术设计和 GitHub issue 草案。
- 正式把 `getsentry/XcodeBuildMCP` 列入参考项目，归档到 `docs-linhay/references/xcodebuildmcp.md`；采纳其 workflow 分组、共享 handler、结构化输出、JSONL progress、workspace daemon 和 session defaults 思路，但不直接依赖其 Node/MCP 工具名作为 TritonKit 产品契约。
- 推荐方案：Triton 原生 host adapter 封装 Apple 官方 CLI，P0 先做 `sim list/use/boot/shutdown/screenshot` 与 `app list/install/launch/terminate/open-url/container/prefs`，再扩展 `.tritonplan`、evidence、logs、host UI、xctrace 和 Xcode build/test。
- 已创建 GitHub feature issue：`https://github.com/NeptuneKit/TritonKit/issues/12`。
- 联网与本机 `simctl help` 补充调研后，追加漏列能力：media/contact 注入、keychain 证书、`.xcappdata` 容器替换、iCloud sync、pasteboard sync、env/getenv、runtime disk image 管理、watch pair、personalization、coverage、SPM、project scaffolding。按 P1-P4 分层，避免把破坏性 runtime 维护命令混进首期真实回归。
- 整理后定位：`xcrun-host-adapter-research` 只保留前置调研；主需求、验收、分期、target 模型和技术契约以 `docs-linhay/spaces/20260520-simulator-takeover/README.md` 与 `technical-design.md` 为准。新增项目级架构摘要 `docs-linhay/dev/20260520-simulator-takeover-architecture.md`。

## Simulator takeover P0 implementation start

- 已开始实现 P0 host adapter，新增 `Sources/TritonKitShared/TKHostAdapterModels.swift` 与 `Tests/TritonKitSharedTests/TKHostAdapterModelsTests.swift`。
- 首批 CLI 已接入：`triton sim list/boot/shutdown/screenshot`、`triton app open-url/container/prefs get/prefs dump`；`schema --command sim/app` 已可发现。
- 本机验证：`swift test` 通过 50 个测试；`swift build --product triton` 通过；`.build/debug/triton sim list --json` 能返回 Xcode 26.5 simulator 列表；缺失 bundle 的 `app container` 返回稳定 `app_container_not_found`。
- 已同步 README、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-real-project-regression` 与 `tritonkit-dev-feedback` skills，真实项目回归应优先用 `triton sim/app` 替代裸 `xcrun simctl openurl/get_app_container/plutil`。

## Simulator takeover P0 app lifecycle

- 继续补齐 P0 App lifecycle CLI：`triton app list/info/install/launch/terminate`，其中 `list/info` 会把 `simctl listapps/appinfo` 的 OpenStep plist 转成结构化 JSON。
- 真实 smoke 发现 `simctl appinfo` 对缺失 bundle 可能 exit 0 且只回显 `CFBundleIdentifier`，已归一为稳定 `app_info_not_available` error code，避免 agent 误判成功。
- 本轮验证：`swift test` 通过 58 个测试，`swift build --product triton` 通过；`schema --command app` 可发现 lifecycle 命令；本机 `app list/info/launch/terminate` smoke 通过，缺失 bundle 的 `app info` 返回 `app_info_not_available`。
- 已同步 README、AI CLI 契约文档、真实项目回归 skill、开发反馈 skill 和 P0 实现记录；后续真实项目回归应继续优先走 `triton app list/info/install/launch/terminate/open-url/container/prefs`，只有 schema 未覆盖时才 fallback 到裸 `xcrun simctl`。

## Simulator takeover P0 simulator defaults

- 新增 `triton sim use <udid> --json`，把 workspace 默认 simulator 写入 `.triton/host-defaults.json`；smoke 在 `/tmp/triton-sim-use-smoke` 执行，避免污染 TritonKit 仓库。
- 新增 `triton sim boot <udid> --wait --jsonl`，对 boot 后状态进行 `simctl list devices available --json` 轮询并输出 JSONL ready event；已 booted simulator 会直接返回 ready。
- 本轮验证：`swift test --filter TKHostAdapterModelsTests` 通过 14 个测试，`swift build --product triton` 通过；`schema --command sim` 已包含 `use`、`--wait`、`--jsonl`；本机 `sim use` 和 `boot --wait --jsonl` smoke 通过。

## Simulator takeover skill and docs sync

- 新增项目级 skill `.agents/skills/tritonkit-host-simulator-takeover/SKILL.md`，用于后续设计、实现、扩展或验证 host-side Apple Simulator 接管能力；该 skill 明确 Triton CLI/HTTP schema 是产品契约，裸 `xcrun simctl` 只作为未覆盖能力的 fallback。
- AGENTS 项目级 skills 列表新增 `tritonkit-host-simulator-takeover` 触发规则；架构摘要与 P0 实现记录已链接该 skill。
- 当前分工：`tritonkit-host-simulator-takeover` 面向实现/扩展 host adapter，`tritonkit-real-project-regression` 面向真实业务 App 回归使用，`tritonkit-dev-feedback` 面向 issue 证据与反馈。

## Xcode workflow takeover space

- 新建 `docs-linhay/spaces/20260520-xcode-workflow-takeover/`，目标是把 XcodeBuildMCP 适合 TritonKit 的能力吃进 `triton`，不照搬 MCP tool name、Node runtime、workflow gating 或 Xcode IDE Bridge。
- 采纳矩阵：吃 project/workspace discovery、session defaults、build/test/run、xcresult、coverage、logs、SwiftPM、真机/macOS workflow、LLDB 和必要 host UI；不吃 XcodeBuildMCP 对外 API、Node 必依赖、IDE Bridge 首期依赖，以及会替代 embedded runtime 语义的 UI 自动化。
- 新增项目级 skill `.agents/skills/tritonkit-xcode-workflow-takeover/SKILL.md`，并在 `AGENTS.md` 项目级 skills 中加入触发规则。后续涉及 Xcode workflow takeover 时优先使用该 skill。
- 更新 `docs-linhay/references/xcodebuildmcp.md`：记录官方文档截至 2026-05-20 显示 v2.5.2、79 tools、15 workflows，并链接新 space、技术设计和 skill。

## Xcode workflow takeover deep research

- 深挖 XcodeBuildMCP 源码后确认：最值得 TritonKit 吸收的是 manifest-like capability registry、domain fragment / final result 分层、workspace-scoped daemon、xcodebuild parser、xcresult/xccov/log capture/debug session 等架构模式，而不是 MCP tool name 或 Node runtime。
- 新增 `docs-linhay/spaces/20260520-xcode-workflow-takeover/deep-technical-research-20260520.md`，系统记录 project discovery、schemes/settings、DerivedData/app path、build/test/run pipeline、test preflight、xcresult、coverage、logs、SwiftPM、LLDB、device/macOS/host UI/scaffolding 的可吸收边界。
- 技术设计更新：新增 Swift-native capability descriptor、JSONL domain event 名、artifact layout、parser strategy、P0 error codes 和 P0 implementation tasks。
- 分期调整：P0 只做 discovery/use/schemes/settings/build/test/run/xcresult；P1 做 coverage/logs/evidence include xcode；P2 做 SwiftPM/device；P3 做 debug/macOS/host UI；P4 做 scaffolding/runtime maintenance/bridge。
- 关键风险：Xcode 输出和 `xcresulttool` 版本变化要求 raw log artifact 与 best-effort parser；多 workspace/scheme 必须返回 ambiguous 而不是猜测；`xcode run` 只证明 build/install/launch，不证明业务 ready。

## Harmony emulator alignment

- 新建 `docs-linhay/spaces/20260520-harmony-emulator-alignment/`，评估 TritonKit 对齐 HarmonyOS NEXT / DevEco Emulator 的能力边界。
- 参考 `.agents/skills/harmony-next` 的 Emulator playbook，结论是 Harmony 可以作为第二个 host-side platform adapter 接入统一 CLI/HTTP 契约：device list/use/wait-ready、app inspect/launch、ax/find/tap/swipe/type/press、screenshot、logs、capture/evidence、`.tritonplan` 均可对齐。
- 风险边界更新：上游 `harmony-next` v1.3.9 release 包已移除人工确认/授权 gate。用户默认拥有完整执行权限；policy 只表达执行模式、产物目录、timeout 和脱敏契约；`blocked` 只用于缺少 target、artifactDir、脱敏策略、timeout、审计命令记录等客观运行配置。
- 后续若进入实现，优先补 schema/capabilities 与 adapter-level 测试，再接 HDC/DevEco 底层命令，不把私有 DevEco trace pipe 或 IDE 内部协议沉淀为稳定产品契约。
- 上游反馈：`https://github.com/linhay/harmony-next.skills/issues/10` 已关闭，提交 `68a3f3e docs: make emulator automation policy non-interactive`；TritonKit 本地 `.agents/skills/harmony-next` 已从 GitHub Release asset `harmony-next.skill` 同步到 v1.3.9，asset sha256 为 `009e58c25b06f639cbb2b6e80a9646f0a525a040d00d4ceb0a2bb03d8f277c67`。
- 仓库治理更新：`.agents/skills/harmony-next/` 解包后约 54MB，不进入 TritonKit git；已加入 `.gitignore` 并删除本地解包目录。后续只保留 release URL、版本号和 sha256，使用时按需下载或安装。
- 重新技术评估：新增 `docs-linhay/spaces/20260520-harmony-emulator-alignment/plans/20260520-technical-implementation-assessment.md`。关键结论是先抽平台中立 Host Adapter Core，再接 Harmony HDC/DevEco adapter；现有 `TKHostCommand.requiresConfirmation` 和 `runHostCommand` 硬编码 `/usr/bin/xcrun` 必须先改为 riskLevel/policy/requiredConfig 与可配置 executable/timeout runner，否则会把旧确认模型和 Apple-only runner 带入 Harmony。
- P0 实现完成：`TKHostCommand` 下线 `requiresConfirmation`，改为 `riskLevel/requiredConfig/defaultTimeout/capturesArtifacts/sensitiveOutput`；新增 `TKHostExecutionPolicy`、可配置 executable/timeout/truncated/sourceCommand 的 host runner、Harmony HDC command builder、target parser 与 boot ready parser。
- 新增 `triton device doctor/list/use/wait-ready --platform harmony`；`schema --command device` 和 `capabilities` 暴露 `host-device` 与 Harmony P0 能力；多 Connected HDC target 返回 `ambiguous_target`，`wait-ready` 只接受 `bootevent.boot.completed=true`。
- `TKEvidenceArtifact` 新增可选 `platform/riskLevel/policy/redactionStatus/sourceCommand/target`，保持旧 manifest 兼容，为后续 Harmony evidence/capture 审计字段预留。
- 验证：`swift test` 通过 58 个测试；fake HDC smoke 覆盖 `device list`、`device use` 的 `ambiguous_target`、`device wait-ready`、`schema --command device` 和 `capabilities`。未做真实 DevEco / Harmony Emulator 验证。

## Harmony DEBUG-only collector contract

- 决策：Harmony P0/P1 不以内置采集器为前提；host-side HDC / DevEco adapter 继续优先。内置采集器只作为后续可选增强，提供 App 内 ArkUI 状态、路由、geometry、accessibility、截图元数据和 redaction status。
- 本轮只在 `TritonKitShared` 新增共享契约模型 `TKHarmonyCollectorManifest`、`TKHarmonyCollectorConfiguration`、`TKHarmonyCollectorSnapshot`、App/Page 状态、screenshot metadata 和 redaction status，不新增不可编译 Harmony 工程。
- DEBUG 默认 `enabled=true`、`platform=harmony`、`transport=embedded-websocket`；Release 必须 `enabled=false`、capabilities 为空，并保持不采集、不上传、不响应控制。
- 验证：先运行 `swift test --filter TKHarmonyCollectorModelsTests` 确认类型不存在红灯，再实现模型；新增 4 个 Swift Testing 用例覆盖 DEBUG manifest、Release no-op、snapshot round-trip 和 screenshot metadata 不内联 base64。
- 上游反馈：当日确认 `harmony-next.skills` 只有 DevEco 新工程向导、`module.json5` 参考和 Emulator/HDC playbook，没有可直接复制的 HarmonyOS NEXT 最小测试工程 scaffold；已提交 `https://github.com/linhay/harmony-next.skills/issues/11`，请求补充 Empty Ability 模板或逐文件 scaffold 指南。后续状态：2026-05-21 已解决，`harmony-next` v1.3.7 起提供 `references/quickStart/ets/minimal-project-scaffold.md` 和 `references/templates/empty-ability-app/`。
