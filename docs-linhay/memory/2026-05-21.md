# 2026-05-21

## Harness reference research

- 将 `https://github.com/awizemann/harness` 列为参考项目，新增 `docs-linhay/references/harness.md`。
- 结论：Harness 对 TritonKit 的价值不在 GUI 产品形态，而在 UX run / agent loop / evidence 体系：append-only JSONL run log、friction taxonomy、persona + goal 分离、每步 fresh screenshot、observation/intent/action/result、cycle detector、credential redaction、clean screenshot vs agent-only overlay、PlatformAdapter/UXDriving 抽象。
- Simulator takeover 更新：Harness 的 WebDriverAgent 经验作为 P2/P3 黑盒 App input fallback 评估，不进入 P0/P1 默认依赖；若引入，必须按 iOS runtime + WDA SHA 缓存 build，并将 WDA build/test runner log 纳入 evidence。
- Xcode workflow takeover 更新：吸收 Harness 的 ad-hoc signing 经验，评估 `CODE_SIGN_IDENTITY=-`、`CODE_SIGNING_ALLOWED=YES` 等 simulator build 默认策略，避免 `CODE_SIGNING_ALLOWED=NO` 导致 entitlements 失真。
- Evidence 路线补充：后续 `.tritonevidence` 可增加 Harness 风格 run event 子流：`run_started`、`step_started`、`tool_call`、`tool_result`、`friction`、`step_completed`、`run_completed`，并保持 screenshot-before-event、relative path、partial tail tolerant、schema versioned parser 等不变量。

## Harness UX run evidence space

- 新建 `docs-linhay/spaces/20260521-harness-ux-run-evidence/`，把 Harness 调研从参考资料收敛成独立需求空间。
- 需求边界：要做 UX run evidence schema、append-only JSONL run log、friction taxonomy、clean screenshot invariant、credential redaction；能做外部 agent 写入、`.tritonplan replay` 生成基础 run events、`capture/evidence` run-aware；适合先做 filesystem portable evidence，不适合首期做 GUI、SwiftData、内置 LLM provider、WDA 默认依赖或 human approval UI。
- 技术设计：定义 `run/events.jsonl`、`meta.json`、step screenshots、common event envelope、`run_started/step_started/tool_call/tool_result/friction/step_completed/run_completed` payload、writer/parser、CLI/HTTP 候选入口、redaction 与测试策略。

## ai-phone reference research

- 将 `https://github.com/dongxinsuperman/ai-phone` 列为参考项目，新增 `docs-linhay/references/ai-phone.md`。
- 结论：ai-phone 不是 TritonKit 应复制的中台技术栈；按最新边界，它主要作为本机三端模拟器/仿真器 CLI 的 command ledger、执行安全层、AI 可消费 case、batch 和 evidence 参考。
- License 风险：ai-phone 使用 GNU GPLv3，TritonKit 只能借鉴架构和契约，不能直接复制代码。
- 原 `docs-linhay/spaces/20260521-ai-phone-device-cloud/` 经两轮重评估后收敛为 `docs-linhay/spaces/20260521-ai-phone-emulator-cli/`：只做 CLI、只考虑本机 iOS Simulator / Android Emulator / Harmony Emulator，不做 Web、不做真机、不做远端 agent、不做设备云。P0/P1 优先做 filesystem portable command ledger、三端本机 target readiness、execution safety metadata 和 AI case lint。
- 与 Harness 分工：Harness 更强在单次 UX run 的可解释证据；ai-phone 更强在生产化调度、设备池、命令证据链和执行安全层。两者都要落到 TritonKit 的 CLI/HTTP JSON 契约与 `.tritonevidence`，不能反向变成 GUI-first 或中台-first。

## CLI capability admission decision

- 用户明确授权由 Codex 决策“哪些能力进 CLI，哪些不进”，因为这是后续 AI agent 自己要用的工具。
- 决策原则：agent 在一次回归中需要直接观察、准备、执行、验证、归档，或需要稳定 JSON/JSONL、evidence、command ledger、无 UI 复跑的能力，必须进入 `triton` CLI。
- 明确进入 CLI：schema/doctor/capabilities/plan、device list/use/wait-ready/lock/unlock、sim/app lifecycle、runtime ax/find/tap/wait/assert、capture/evidence、command ledger inspect、record/replay、case lint、run submit/status/cancel、agent connect/status/disconnect。
- 只读或非 P0 CLI：本机 device pool list/resolve、run events、checkpoint replay、本地报告生成、bounded logs collect。
- 不进入当前方向：远端 agent、中心 Server、对外 HTTP API、Postgres migration、Kafka/Webhook、Web/运维大盘、多租户权限、全局设备池写入、内置 VLM loop、V2/V3 轨迹自动重定位、系统安全绕过。

## Emulator CLI implementation slice

- 按用户进一步校准，“模拟器方案”覆盖 iOS Simulator、Android Emulator、Harmony Emulator，但本轮实现 Android 可以后续接入。
- 本轮实施切片：新增 `triton app uninstall --bundle-id <id> --simulator <udid|booted> --confirm --json`，补齐 simulator App lifecycle 的安装/卸载/启动/终止闭环；未传 `--confirm` 时返回 `error.code=destructive_action_requires_policy`，不执行底层卸载。
- 同步新增 Harmony App CLI 面：`triton app inspect --platform harmony --bundle <bundle> --json` 走 HDC `bm dump -n`；`triton app launch --platform harmony --bundle <bundle> --ability <ability> --json` 走 HDC `aa start`。Android adapter 仍后续接入。
- 同步更新 CLI schema、README、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-host-simulator-takeover` skill；`app uninstall` 不再列为 P0 remaining。
- 冒烟验证：`swift test` 通过 64 个 Swift Testing 测试；`.build/debug/triton schema --command app --json` 已暴露 uninstall 与 Harmony inspect/launch；`.build/debug/triton app uninstall --bundle-id com.example.missing --simulator booted --json` 无 `--confirm` 返回机器可读错误；带 `--confirm` 的缺失 bundle在当前 booted simulator 上由 `simctl` 幂等返回成功；`.build/debug/triton app launch --bundle-id com.example.missing --simulator booted --json` 返回 `app_launch_failed`；`.build/debug/triton app launch --json` 返回 `validation_failed`；`.build/debug/triton app launch --platform harmony --json` 返回 Harmony 参数校验错误；`.build/debug/triton app inspect --platform harmony --bundle com.example.missing --target 127.0.0.1:10100 --json` 成功走到 `host-harmony` envelope；`.build/debug/triton sim list --json` 成功列出本机 iOS Simulator。

## Emulator CLI skill and release packaging

- 新增项目级 skill `.agents/skills/tritonkit-emulator-cli-takeover/SKILL.md`，把三端本机模拟器/仿真器 CLI 接管沉淀为长期执行入口：iOS Simulator、Android Emulator、HarmonyOS / DevEco Emulator 均属于本机 CLI 方向；默认不做真机、远端 agent、设备云、Web/Wails UI、对外 HTTP 产品面或中台服务。
- 同步更新 `AGENTS.md`：emulator 接管方向上升为 repo-wide 产品边界；发布产物必须覆盖 `tritonkit-dev-feedback`、`tritonkit-real-project-regression` 与 `tritonkit-emulator-cli-takeover` 三个项目级 skill，后续打包形态以 `tritonkit-skills.tar.gz` 合并包为准。
- 同步更新 release packaging：先将 `tritonkit-emulator-cli-takeover` 纳入 release 校验；后续已在本文件 “Release asset consolidation” 收敛为 `tritonkit-skills.tar.gz`。
- 同步更新 `tritonkit-ops-governance`、`tritonkit-host-simulator-takeover`、`tritonkit-dev-feedback` 与 `tritonkit-real-project-regression`，加入本机 emulator 边界、release skill 包契约、`app uninstall --confirm`、Harmony `app inspect/launch` 示例，避免外部 agent 继续退回裸 `xcrun` / `hdc`。

## Release run id parsing fix

- v0.1.1 发布时，本地 `docs-linhay/scripts/release.sh` 已成功完成 preflight、推送 tag、CI 与 Homebrew 也最终全部成功，但脚本在观察 GitHub Actions run 时通过 `databaseId` + `gh --template` 输出 run id，GitHub CLI 将大整数格式化为科学计数法，导致 `gh-run-summary` 对 `2.6182928685e+10` 查询 404。
- 修复：`release.sh` 改为从 `gh run list --json headBranch,url` 的 URL 字符串解析最后一段 run id，并把 `--repo "${repo}"` 显式传给 `gh-run-summary.sh`；`verify-release-automation.sh` 新增检查，禁止回退到 `databaseId` 模板渲染路径。

## Release asset consolidation

- 用户反馈 v0.1.1 Release 资产过多：双架构 CLI 同时生成 `.tar.gz` / `.zip`，3 个 skill 又分别生成 `.tar.gz` / `.zip`，加上 checksum 和 GitHub 自动源码包后页面噪音过高。
- 决策：后续 release 只保留 `triton-macos-arm64.tar.gz`、`triton-macos-x86_64.tar.gz`、`tritonkit-skills.tar.gz` 和 `tritonkit_checksums.txt`；GitHub 自动源码包保留。`tritonkit-skills.tar.gz` 内含 `tritonkit-dev-feedback`、`tritonkit-real-project-regression`、`tritonkit-emulator-cli-takeover` 三个目录，并统一 stamp 版本号。
- CI 已移除所有 zip 资产和单独 skill tarball；`verify-release-automation.sh` 增加检查，禁止重新生成 `.zip` 或单独 skill 包。
- 修复 workflow_dispatch 云端演练失败：非 tag 版本会解析为 `0.1.0-dev+<short-sha>`，不能拼成 Homebrew release tag。后续手动打包只验证 release asset 集合并上传 workflow artifact；只有真实 `v*` tag 构建才渲染 Homebrew formula。
- 云端手动 CI run `26201878796` 在 commit `25468de` 上通过，`Package release assets` 成功；下载 `tritonkit-release-assets` artifact 后确认只有 `triton-macos-arm64.tar.gz`、`triton-macos-x86_64.tar.gz`、`tritonkit-skills.tar.gz`、`tritonkit_checksums.txt` 四个文件，checksum manifest 只覆盖三个 `.tar.gz`。
- 现有 GitHub Release `v0.1.1` 已手动收敛为同一资产集合：保留两个 CLI `.tar.gz`，从 `v0.1.1` tag 重新生成并上传 `tritonkit-skills.tar.gz`，替换 `tritonkit_checksums.txt`，删除 CLI zip 与三个单独 skill 的 tar/zip。

## Issue triage and Homebrew install fix

- GitHub issues 当前处理策略：#9 atomic CLI replacement docs 已由 README、项目级 skills 与 handoff 文档覆盖，可关闭；#10 Homebrew install block 需要修复 template 与 tap；#11/#12 host simulator takeover 已完成 P0 子集但仍保留 P1/P2 能力缺口，继续保持 open 并评论状态。
- 修复 #10：`.github/homebrew/triton.rb.template` 的 install block 同时查找 `triton-macos-*/triton` 和 Homebrew 可能提供的扁平 `triton`；`verify-homebrew-formula.sh` 增加回归检查，防止再次只支持一种 staging 布局。
- 真实 `brew install NeptuneKit/tap/triton` 已验证通过；随后 `brew test` 暴露 formula test 不能用紧凑 JSON 字符串匹配 pretty JSON，需改为 `JSON.parse` 后断言 `ok` 与 `version`。
- 收尾：tap 已通过 `update-homebrew-tap.yml` 更新到修复后的 `v0.1.1` formula；`brew install NeptuneKit/tap/triton`、`/opt/homebrew/bin/triton version --json`、`brew test NeptuneKit/tap/triton` 均通过。GitHub #9、#10 已关闭，#11 标记 duplicate 并关闭，#12 保持 open 作为综合 simulator takeover 跟踪。

## TritonKit skill layout split

- 用户指出当前 `.agents/skills` 同时承载对外 skill 与内部治理 skill，不利于打包且边界混乱；决策：TritonKit-owned skill 源码迁移到独立根目录 `.agents/tritonkit-skills/`。
- `public/` 只放进入 release asset `tritonkit-skills.tar.gz` 的对外 skill：`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover`、`tritonkit-real-project-regression`。
- `internal/` 只放 repo 内部维护、治理、监督、实现和规划 skill：`tritonkit-ops-governance`、`tritonkit-host-simulator-takeover`、`tritonkit-xcode-workflow-takeover`、`tritonkit-session-skill-distill`、`tritonkit-subagent-supervision`。
- `.agents/skills/` 只保留 symlink 作为本地 agent discovery shim；CI release packaging 改为从 `.agents/tritonkit-skills/public` 读取，并由 `verify-release-automation.sh` 防止再次从 `.agents/skills` 打包或把 internal skill 混入对外包。

## Harmony scaffold skill update

- 上游 `linhay/harmony-next.skills#11` 已关闭，缺失的 HarmonyOS NEXT 最小测试工程 scaffold 已补齐。
- 本地 `/Users/linhey/Desktop/linhay-open-sources/harmony-next.skills/harmony-next/SKILL.md` 显示版本 `v1.3.7`，已包含 `references/quickStart/ets/minimal-project-scaffold.md` 和 `references/templates/empty-ability-app/`。
- `harmony-next` 新增默认 fixture 信息：bundleName `com.example.emptyability`、module `entry`、ability `EntryAbility`、Compatible SDK `5.0.0(12)`，并提供 `ohpm install`、`hvigorw --mode module -p module=entry@default assembleHap`、HDC install/start、`uitest dumpLayout`、`uitest screenCap` 验证路径。
- 已更新 TritonKit 项目级 skills：`tritonkit-dev-feedback` 与 `tritonkit-real-project-regression` 后续需要 Harmony fixture 时，优先使用 `harmony-next` 的 Empty Ability scaffold，不再手写或从本机其他 Harmony 工程拼凑配置。

## Harmony collector smoke fixture

- 基于 `harmony-next` Empty Ability scaffold 新增 `docs-linhay/spaces/20260520-harmony-emulator-alignment/fixtures/harmony-collector-smoke/`。
- fixture bundleName 为 `com.neptunekit.tritonkit.collectorsmoke`，Ability 为 `EntryAbility`，页面暴露 collector 契约信号：`Triton Collector Ready`、`collector-debug-manifest`、`collector-capabilities`、`collector-release-noop`、`collector-snapshot-metadata` 和 `smoke-increment`。
- 新增 ArkTS 契约文件 `entry/src/main/ets/triton/TritonCollectorContract.ets`，覆盖 DEBUG manifest、capabilities、Release no-op configuration 和 snapshot metadata，与 Swift `TKHarmonyCollector*` 模型保持同一字段语义。
- 本地构建验证通过：`fixtures/harmony-collector-smoke/verify-local.sh` 执行 `ohpm install` 与 `hvigorw --mode module -p module=entry@default assembleHap` 成功，输出 `entry/build/default/outputs/default/entry-default-unsigned.hap`；`No signingConfig found` 为模板预期边界。
- 启动 `Codex Test Phone` HVD 后，HDC target `127.0.0.1:10100` 进入 `TCP/Connected` 且 `bootevent.boot.completed=true`；安装 unsigned HAP、`aa start`、`uitest dumpLayout`、`screenCap` 和点击 smoke 均通过。
- 验收信号：before layout 包含 `Triton Collector Ready`、`harmony|embedded-websocket|enabled=true`、capabilities、Release no-op 和 snapshot metadata；点击 `smoke-increment` 后 after layout 包含 `Triton Collector Tapped` 与 `tapCount=1`。
- 产物归档：`docs-linhay/spaces/20260520-harmony-emulator-alignment/screenshots/20260521/harmony/20260521-harmony-collector-layout-before-v01.json`、`screen-before-v01.png`、`layout-after-v01.json`、`screen-after-v01.png`。
- 修复：真实 HDC smoke 暴露 `TKHdcTargetListParser` 对 `hdc list targets -v` verbose 输出解析错误；`target TCP Connected localhost` 旧逻辑会把 `TCP` 当作 state，导致 `triton device wait-ready` 误报 `target_offline`。已新增 verbose parser 测试并修复为 transport=`TCP`、state=`Connected`。

## Harmony collector smoke retest

- 继续在 `Codex Test Phone` / `127.0.0.1:10100` 上复测 Harmony collector fixture：`verify-local.sh`、HAP 安装、`aa start`、before/after `uitest dumpLayout`、`screenCap`、按 bounds 中心点击 `smoke-increment` 均通过。
- 第二轮验收信号：before layout 包含 `Triton Collector Ready` 与 `tapCount=0`；after layout 包含 `Triton Collector Tapped` 与 `tapCount=1`；DEBUG manifest、capabilities、Release no-op、snapshot metadata 全程存在。
- `triton device wait-ready --platform harmony --target 127.0.0.1:10100 --timeout 10 --json` 在真实 HDC verbose target 输出下返回 `ready=true`，确认 parser 修复后 CLI 路径可用。
- 第二轮证据归档为 `docs-linhay/spaces/20260520-harmony-emulator-alignment/screenshots/20260521/harmony/*-v02.*`。

## Harmony comprehensive test gate

- 针对“全面测试”补跑 `docs-linhay/scripts/verify.sh --ci-validate`，覆盖 Swift tests、`TritonKitShared.podspec` lint、`TritonKit.podspec` lint、Homebrew formula template、version stamping scripts、release automation contract，结果通过。
- 已同步更新 `docs-linhay/spaces/20260520-harmony-emulator-alignment/plans/20260520-formal-verification.md`：旧结论中“没有 Connected target、未做真实 wait-ready/UI/screenshot”的限制已由 2026-05-21 真实 smoke 覆盖；P2/P3 的 capture/evidence、logs、HVD lifecycle、Harmony `.tritonplan` 回放仍明确列为后续范围。

## iOS integration DEBUG guard correction

- 用户纠正：业务 App 侧 iOS 接入文件不能只依赖 TritonKit 内部 Release no-op，所有 `import TritonKit`、`TritonKitRequestHandler`、`delegate`、`dataURL` 与 `connect(host:port:)` 都必须显式包在 `#if DEBUG` 内。
- 决策：README、public skills 和真实项目回归指南统一推荐独立 `TritonKitDebugBootstrap.swift`，整个文件使用文件级 `#if DEBUG`；AppDelegate、SceneDelegate 或 SwiftUI 入口只保留 `#if DEBUG` 调用点。`Examples/TritonKitDemo` 已拆出文件级 DEBUG bootstrap。
- CocoaPods 对外示例同步加 `:configurations => ['Debug']`，避免 Release 配置安装 embedded runtime；AGENTS 与 `tritonkit-ops-governance` 已上升为长期治理规则。

## iOS integration guide public entrypoints

- 用户补充要求：iOS 接入指南需要直接写进 public skills 与 README，而不是只在内部 dev 文档中存在。
- README 已把入口收敛为 `iOS App Integration Guide`，覆盖安装 iOS runtime、文件级 `#if DEBUG` bootstrap、AppDelegate/SwiftUI 调用点、CLI 安装与运行。
- `tritonkit-dev-feedback` 与 `tritonkit-real-project-regression` public skills 均写入可复制的接入指南和 Debug-only CocoaPods 示例，确保外部 agent 试接入和真实项目回归时不用再回查内部文档。

## SwiftPM integration clarification

- 用户指出上一轮接入指南主要强调 CocoaPods Debug-only 配置，SwiftPM 口径不够明确。
- 决策：SwiftPM / Xcode package product dependency 没有 CocoaPods-style `:configurations => ['Debug']` 开关；默认策略是源码级 `#if DEBUG` bootstrap + TritonKit Release no-op runtime。
- 若业务要求生产 Release target 完全不链接 TritonKit，对外指南必须推荐独立 Debug-only app target / scheme，并只在该 target 上挂 `TritonKit` product。README、public skills、AGENTS、ops skill 与 dev 文档已同步该口径。

## Convenient iOS start API

- 用户指出接入 API 仍过重，期望类似 `TritonKit.shared.start(<payload>)` 的便捷入口。
- 决策：新增 `TritonKitStartPayload` 与 `TritonKit.shared.start(_:)`，默认 `TritonKit.shared.start(.environment())` 从 `TRITON_HOST` / `TRITON_PORT` 读取配置，内部强持有默认 `TritonKitRequestHandler`，业务 App 不再需要保存 handler、设置 `delegate`、`dataURL` 或手动 `connect`。
- 保留高级路径：需要自定义 delegate 时使用 `TritonKit.shared.start(payload, delegate:)` 或继续使用低层 `delegate` / `connect(host:port:)`。README、public skills、dev 文档和 demo bootstrap 已同步为优先展示便捷 API。

## iOS embedded SDK expansion planning

- 用户要求规划 iOS 内置 SDK 后续更多采集内容和操作命令。
- 新增 `docs-linhay/spaces/20260521-ios-embedded-sdk-expansion/`，明确下一阶段仍坚持 DEBUG-only、App 内、机器可读、可审计；系统弹窗、Home/App Switcher、SpringBoard、host framebuffer、安装卸载、deep link 提交等设备级能力继续归 host-side adapter。
- 规划 P0 采集内容：runtime manifest、App/scene/window state、route/navigation state、first responder、control attrs v2、unified snapshot、runtime ledger。
- 规划 P0 语义操作命令：`focus`、`set-text`、`submit`、`select-segment`、`set-switch`、`set-slider`、`stepper`、`scroll`、`scroll-to-visible`、`wait-idle`。
- P2 高风险能力如 UserDefaults allowlist、network breadcrumbs、app logs、business debug state 必须 opt-in provider，不做默认全量采集或 swizzling。
- 补充执行表：实现前必须完成技术调研，R0 覆盖 runtime routing、UIKit 公开 API、redaction/DEBUG 边界；R1 覆盖 snapshot payload、semantic selector、UIKit actions、runtime ledger；R2 覆盖 harness/真实项目回归与 opt-in provider API。
- 用户明确北极星目标：让 AI 通过 CLI 直接和 App embedded SDK 沟通，优化 AI 操控 App 的能力边界上限。规划已调整为围绕“观察、解释、执行、验证、复盘”闭环排序，不以增加采集项数量为目标。
- 新计划按 S0-S6 执行：S0 能力边界，S1 状态理解，S2 快照聚合，S3 语义执行，S4 验证复盘，S5 真实项目上限验证，S6 opt-in 业务语义。第一轮只落 S0-S3 最小闭环：manifest、capabilities/schema、state、attrs v2、snapshot、focus、set-text、select-segment、set-switch、ledger。
- 按“设想场景 -> 设计命令和数据 -> 调研方案 -> 可行则测试，不可行进头脑风暴池”补齐 50 轮执行表：`docs-linhay/spaces/20260521-ios-embedded-sdk-expansion/plans/20260521-50-round-ai-cli-sdk-loop.md`。其中 B01/B02/B03 分别记录 snapshot diff、debug state provider、network breadcrumbs opt-in。
- 本轮基线验证：`verify-intent-cli-smoke.sh` 通过；`swift test --filter TritonKitSharedTests` 通过 60 个 Swift Testing 测试；`verify-cli-bootstrap.sh` 未通过，原因是本机默认 `127.0.0.1:19421` 已有 PID 62859 的 `triton` server 监听，裸 `triton` 直接连接到当前 App，不满足脚本的 server unavailable 前提。
- 新增 R0 调研产物 `technical-research-ai-cli-sdk-boundary-v01.md`：结论是 S0 首个可执行切片应先做 `runtime manifest`，用于让 AI 通过 CLI 获得 embedded SDK 能力边界、限制、redaction 和 next action，再进入 state/attrs/snapshot/semantic actions。

## S0 runtime manifest implementation

- 已实现 `triton runtime manifest --json`：CLI 通过 `/request type=runtimeManifest` 直连 iOS embedded SDK，返回 platform/runtime/transport/enabled/sdkVersion/buildConfiguration/capabilities/limits/redaction。
- Shared DTO 新增 `TKRuntimeManifestResponse`、`TKRuntimeCapabilityDetail`、`TKRuntimeLimits`、`TKRuntimeRedactionPolicy`；`TKRequestType` 新增 `runtimeManifest`，`TKCLICommandRequest` 支持 `runtimeManifest` 与 `manifest` 映射。
- Embedded handler DEBUG 返回 `debugDefault` manifest；Release disabled 路径保留 no-op manifest shape，用于明确 runtime disabled 边界。
- CLI schema/capabilities 已暴露 `runtime-manifest`；README、`docs-linhay/dev/ai-cli-readable-control.md`、public skills 已同步新入口。
- `verify-intent-cli-smoke.sh` mock server 已新增 runtime manifest 响应与 `schema --command runtime` 断言。

## S1 runtime state implementation

- 已完成 `technical-research-uikit-public-api-v01.md`，确认 `state app|scene|route|responder` 只依赖公开 UIKit/Foundation API：`UIApplication.connectedScenes`、`UIScene.activationState`、`UIWindow`、`UIViewController` container 关系、`UIResponder` 与 `UITextInputTraits`。
- 新增 Shared DTO：`TKRuntimeAppStateResponse`、`TKRuntimeSceneStateResponse`、`TKRuntimeRouteStateResponse`、`TKRuntimeResponderStateResponse`，统一包含 `ok/capturedAt/runtime/targetConnectionState/warnings/unsupported`。
- `TKRequestType` 新增 `stateApp/stateScene/stateRoute/stateResponder`，CLI 新增 `triton state app|scene|route|responder --json`，通过 `/request` 直连 embedded SDK。
- Manifest、capabilities、schema 和 `verify-intent-cli-smoke.sh` 已暴露 `state-app/state-scene/state-route/state-responder`；README、AI CLI dev 文档与 public skills 已同步。
- 隐私边界：responder state 只返回 first responder identity、frame、text input traits 与 redaction，不回显文本内容；SwiftUI 私有 tree 只报告 `UIHostingController` 边界。

## iOS SDK facade API P0

- 用户明确：不能只设计 `TritonKit.shared.start(payload)` 一个便捷接口，需要一系列便捷接入 API。
- 决策：P0 facade 保留 `TritonKitStartPayload` 兼容，同时新增 `TritonKit.Endpoint` alias、`TritonKit.Configuration` builder、`Feature`、`RedactionPolicy`、`AppIdentity`、`stop()`、`onStateChange` 与 `onError` token。
- README、public skills、AGENTS、ops skill 与 dev 文档统一改为默认展示 `TritonKit.shared.start()` / `start { config in ... }`；只有自定义 delegate 或消息路由时才展示低层 `delegate` / `connect(host:port:)`。
- 本切片不实现 `exposeState` / `registerAction` / UserDefaults allowlist / network breadcrumbs，这些留到后续 opt-in provider 切片。

## iOS facade docs finalization

- 用户要求把一系列便捷接入 API 明确更新进 skills 和 README，而不是只靠示例暗示。
- README、`tritonkit-dev-feedback` 与 `tritonkit-real-project-regression` public skills 新增 Preferred facade APIs 清单，覆盖 `start()`、`.local(port:)`、`.environment()`、`.device(_:port:)`、`start { config in ... }`、`stop()`、`onStateChange`、`onError`。
- 观察示例同步保留 `stateToken` 与 `errorToken`，并注明 token 需要强持有，取消观察时调用 `cancel()`；默认仍强调只有自定义 delegate 或消息路由时才使用低层 `delegate` / `connect(host:port:)`。
