# 2026-05-23

## Simulator Advanced Controls 继续推进

- 已把 `triton sim` 扩展为可用的 host-side simulator maintenance 面：`status-bar list|clear|override`、`privacy grant|revoke|reset`、`location list|clear|set|run|start`、`ui appearance|increase-contrast|content-size`、`pasteboard set|get|sync`、`push`，并继续推进到 `diagnose`、`logverbose`、`runtime list|verify`。
- `TKHostCommand` 新增 `stdinData`，`runHostCommand` 已支持向 child process 写入 stdin，因此 `simctl pbcopy` 可以通过 CLI 直接转发文本输入，而不是回退 shell pipe。
- `CLISchemaRuntime` 已同步暴露新的 `sim` 子命令与能力点，`sim` schema 仍保持 JSON / JSONL 机器可读 envelope。
- 本地验证已通过：`swift test --package-path CLI --filter SimulatorAdvancedControlsTests --skip-update`、`swift test --package-path CLI --skip-update`、`swift test`、`swift build --package-path CLI --scratch-path .build/cli --product triton`。
- 这轮已把 #23 的 Phase 1 再向前推进，并开始接入 diagnostics/runtime maintenance slice；`video` / `xctrace` / `coverage` / `pair` / `unpair` / clone / erase / personalization 仍在后续范围内，issue 还不能在这里误关。

## Real Project Smoke 会话沉淀

- 本轮整理确认 `main` / `origin/main` 最新稳定提交为 `be30ba4 feat: add open-url readiness snapshot`，前置关键提交包括 `4bbd2a5 feat: add iOS smoke orchestration`、`b0612bf docs: expand webview bridge guidance` 和 `78b9efa docs: sync webview bridge skill docs`。
- 已将真实项目 smoke issue 收口规则沉淀到 `.agents/tritonkit-skills/public/tritonkit-real-project-regression/SKILL.md`：关闭前必须确认主线 commit、schema 暴露、相关测试、真实或结构化验证、证据脱敏、space 文档、memory 和 qmd 同步。
- 关键边界继续保持：`app open-url` / HDC `aa start` / `xcode run` / tap / launch / install 的 host ack 只证明命令提交成功，不能等价于业务成功；业务成功必须由 `wait`、`assert`、snapshot、screenshot 或 evidence 证明。
- #17 已满足一命令 iOS smoke 本地关闭条件并关闭；#18 仅完成 priority 1/2 的一部分，#15 和 #12 仍未达到关闭条件，后续不能在整理里误写为完成。
- 本次整理发现工作区存在 3 个未跟踪源码/测试文件：`CLI/Tests/TritonKitCLITests/HostPreferencesSetTests.swift`、`CLI/Tests/TritonKitCLITests/WebViewRouteTests.swift`、`Sources/TritonKitCLI/CLIWebViewModels.swift`；它们视为并行 WIP，不纳入本次 docs/skill/memory 提交。

## 2026-05-23 #12/#15/#18 继续推进

- `triton webview current-url --platform ios --json` 已接入并在真实 iOS Simulator 上正向复跑通过，当前 Demo provider URL 为 `https://tritonkit.local/smoke`，对应 `route assert-current-url` 也已通过 `exact` 与 `--ignore-query` 两种路径。
- `triton app prefs set <key> <json-value> --bundle-id <id> --simulator booted --json` 已接入并验证自然 JSON 编码，支持 string/bool/int/double/array/object；真实 simulator 里 `prefs get/dump` 可读回 `false` 与对象型配置。
- `TKHostPreferenceValue` 从 Swift 默认 enum 编码改为自然 JSON 编码，避免 `{"bool":{"_0":true}}` 这类对 agent 不友好的输出。
- `docs-linhay/scripts/verify-harmony-host-smoke.sh` 已作为 #15 的本地验证脚本跑通，覆盖 fake HDC 下的 `device/app/ax/wait/tap/screenshot/observe/node` 契约；真实 `triton device list --platform harmony --json` 仅发现 `127.0.0.1:10100` 为 `Offline`，本轮未做真实 HDC 写入或截图。
- 真实 iOS simulator 复跑前曾遇到旧 `triton serve` 二进制仍在监听 19421、导致 `/request` 上 `webViewList` 回退为 `invalid_payload`；重启 serve 后 provider 正向恢复。后续若再遇到同类现象，优先怀疑 serve 进程未随源码更新重启。
- #12 继续保持 epic open；#15 目前完成 host-side adapter 命令与 fake HDC smoke 验证，但尚未补 `smoke harmony` 聚合入口；#18 已覆盖 open-url readiness snapshot、webview current-url/assert 与 prefs set，剩余 failure diagnostics / evidence summary-redact 仍待推进。

## 2026-05-23 #15/#18 收口与 #12 拆分策略

- 本轮补齐 `triton smoke harmony` 聚合入口，schema 暴露 `smoke-harmony`，测试覆盖 device wait-ready、app inspect/open-url、layout wait、tap、post-tap wait、layout/screenshot artifact 和 evidence manifest。`TRITON_BIN=.build/cli/debug/triton docs-linhay/scripts/verify-harmony-host-smoke.sh` 通过；真实 HDC target `127.0.0.1:10100` 仍为 `Offline`，`device wait-ready --timeout 1` 返回 `target_offline`，所以未做真实 HDC 写入或截图。
- 本轮补齐 `triton evidence summary` / `triton evidence redact`，redacted manifest 保留非敏感 artifact 为 `included`，敏感 screenshot/AX/hierarchy/geometry/archive/logs 写入 redacted placeholder；CLI fixture 完成 `summary -> redact -> inspect` 正向复跑。
- 本轮补齐 `tap` / `assert` 失败诊断增强，JSON error/result 暴露 nearest candidates / nearestText、candidateCount 和 suggestedCommands。
- 本地验证通过：focused tests（EvidenceBundleTests、SmokeHarmonyRuntimeTests、FailureDiagnosticsTests）、完整 CLI tests、root `swift test`、CLI build、schema evidence/smoke、evidence fixture、Harmony fake host smoke、sim list、Harmony offline wait-ready。
- #15 与 #18 已满足关闭条件；#12 的 P0/P1 真实 smoke 主闭环已满足，但原 epic 中 privacy/location/status bar/push/logs/video/diagnostics/xctrace/watch/runtime maintenance 等高级能力未在本轮实现，关闭 #12 前需把剩余范围拆入 follow-up 或在 closure comment 中明确 deferred。

## #22 参考项目归档

- 已把 `karminski/eyes-on-my-phone` 补成本地参考归档 `docs-linhay/references/eyes-on-my-phone.md`。
- 该项目更适合作为“移动端本地推理 + MCP 工具暴露”的参考，而不是 TritonKit 当前本机 simulator / emulator host adapter 的直接实现模板。
- 这类 reference-only issue 后续可以优先落地为 `docs-linhay/references/` 条目，再按需要决定是否保留在 issue 中追踪。

## Simulator video recording slice

- 已把 `triton sim record --simulator booted --output <path.mov> --duration <seconds> --json` 接进 `sim` namespace，底层封装 `simctl io recordVideo`，并在 duration 到点后通过 `interrupt` 结束录制。
- 新增了 `sim-video` schema capability、命令帮助、文档入口与公开 skill 示例，保持视频录制仍然是 JSON/JSONL 友好的 host-side 证据能力。
- 本地验证已通过：`swift test`、`docs-linhay/scripts/check-docs.sh`、`git diff --check`、`docs-linhay/scripts/qmd-sync.sh`。

## Simulator bounded logs slice

- 已把 `triton sim logs --simulator booted --output <path.ndjson> --duration <seconds> --json` 接进 `sim` namespace，底层封装 `simctl spawn <udid> log stream --timeout <seconds>`，默认输出 `ndjson` artifact。
- JSON 输出只返回 artifact path、stdout/stderr bytes 和 truncation 状态，不把完整私有日志塞进 stdout envelope。
- 本地验证已通过：`swift test`、`swift test --package-path CLI --skip-update`、`swift build --package-path CLI --scratch-path .build/cli --product triton`、`triton schema --command sim --json`、真实 booted simulator 上 `triton sim logs --duration 1` 正向采集 `/tmp/triton-sim-logs.ndjson`。
