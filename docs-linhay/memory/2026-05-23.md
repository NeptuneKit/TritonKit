# 2026-05-23

- GitHub issue `#23` 的 simulator advanced controls 已推进到 Phase 3 收口，`pair` / `unpair` / `clone` / `erase` / `upgrade` / runtime maintenance / personalization 都已进入 `triton sim`。
- 破坏性维护命令统一保持机器可读门禁：`erase`、`runtime delete` 非 dry-run、`runtime dyld-cache remove`、`personalization remove-*` 需要 `--confirm`；`runtime delete` 支持 `--dry-run` 先复跑输出。
- 本地验证已覆盖共享层 builder、CLI schema、`triton sim runtime list`、`triton sim runtime match list`、`triton sim runtime delete --dry-run`，以及 `triton sim erase booted --json` 返回 `confirmation_required`。
- `CLIHostCommands.swift` 的 runtime / personalization 旧定义已拆到 `CLIHostSimulatorMaintenanceCommands.swift`，并将 `requireConfirmation` / `requireExactlyOneSelector` 调整为模块内可见以支持新文件复用。
