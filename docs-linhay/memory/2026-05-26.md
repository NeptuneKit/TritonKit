# 2026-05-26

## Issue 20 Tap Activation

- 已补齐 tap 激活路径的 exact / smart / ancestor 透传：`tapRequest(for:)` 现在会为 exact 低层 selector 保留 matched 元数据和 `activationStrategy`，`performTap` 的结果也会显式返回 `matched` / `activation` / `strategy`。
- CLI 文本输出 `printInputResult` 已补齐 `matchedOID`、`matchedClassName`、`activationOID`、`activationClassName`、`strategy`，避免 text 模式看不到实际激活目标。
- 验证通过：`swift test --package-path CLI --filter InputOutputTests`，`xcodebuild -scheme TritonKit-Package -destination 'platform=iOS Simulator,id=60667794-96F8-40E6-8664-85538EC4663E' test`。
