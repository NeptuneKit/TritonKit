# 2026-05-27

## Revyl CLI 参考源码归档

- 按用户要求将 `RevylAI/revyl-cli` 克隆到 `docs-linhay/references/revyl-cli/`，作为 TritonKit 后续 CLI、agent skill、workflow 和 evidence 设计参考。
- 克隆来源：`https://github.com/RevylAI/revyl-cli.git`，当前 HEAD：`9931a77`。
- 同步更新 `docs-linhay/references/revylai.md`，记录本地源码路径、来源仓库、clone HEAD 和克隆日期。

## Revyl CLI agent entrypoint 深度研究

- 新建 `docs-linhay/spaces/20260527-revyl-cli-agent-entrypoint-research/`，沉淀 Revyl CLI 对 TritonKit 的可借鉴方向、BDD 验收和执行计划。
- 关键结论：TritonKit 应借鉴 Revyl 的 agent entrypoint 工程化模式，而不是照搬云设备平台路线；优先推进 `triton skill list/install/export`、`triton evidence summary --json`、共享 execution status model、`triton schema` workflows/error codes 和 CI 示例。
- 新增 `docs-linhay/dev/agent-entrypoint-engineering.md`，作为后续实现的技术设计入口。
- 当前决策：本方向暂不进入实现，仅作为参考研究和 backlog 归档。

## Agent-facing CLI 兼容策略修正

- 用户澄清：TritonKit 不是需要兼容某个固定版本 agent 的通用 CLI，而是 `triton` CLI 直接绑定随包发布的 skills；agent 使用时面对的是同一套 CLI + skills 契约。
- 关键决策：后续 agent-facing CLI 信息架构可以进行破坏性更新，不需要保留 legacy / compatibility 层；命令、schema、README 与 public skills 必须同步迁移，确保随包 skills 永远描述当前 CLI 契约。
- 规划影响：长期巡航队列中移除“兼容层与 legacy 标注”，改为“破坏性命令契约重排 + skills 同步更新 + docs/memory 闭环”。

## Command surface optimization 长期计划落地

- 新建 `docs-linhay/spaces/20260527-command-surface-optimization/`，作为新一期 CLI 命令面与 agent-facing 信息架构优化的正式 space。
- 新增 `README.md`，明确本期目标、非目标、BDD 验收场景和“CLI 直接绑定随包 skills，因此可破坏性演进”的关键决策。
- 新增 `plans/20260527-long-term-cruise-plan-v01.md`，将长期巡航拆为 21 轮：先收口 WIP、冻结命令基线、补齐 schema、统一错误 envelope、拆分 Host 命令，再进入方案 C 的 target / capabilities / doctor / plan / action / observe / assert / evidence / replay 信息架构重排。

## Command surface optimization 计划继续完善

- 用户纠偏：当前任务是继续制定长期计划，不是推进第 1 轮 WIP 收口或代码实现。
- 已补充 `plans/20260527-long-term-cruise-plan-v01.md` 的里程碑视图、巡航执行协议、checkpoint 模板、优先级规则、破坏性更新规则、暂停条件和报告格式。
- 关键执行口径：长期运行时按最靠前未完成轮次推进，但每轮都必须先补 BDD / 测试或证据，再最小实现；破坏性 CLI 更新必须同轮同步 schema、public skills、README / dev 文档和测试。

## 方案 C 完整定义并入长期计划

- 用户要求确认“方案 C：把 Triton CLI 改造成 agent 可自发现、可规划、可执行、可诊断的控制界面”是否已进入当前计划；已补入 `plans/20260527-long-term-cruise-plan-v01.md`。
- 新增内容包括：方案 C 定义、核心问题、五层信息架构、目标命令分层、目标 agent 流程、BDD 场景、四期实施切片、主要风险和刹车线。
- 按用户澄清修正：不保留 legacy / compatibility 层；旧入口若不符合 agent-facing 信息架构，可以破坏性删除或重命名，但必须同轮同步 CLI schema、随包 public skills、README / dev 文档和测试。

## Command surface optimization Round 01 checkpoint

- 开始执行长期计划第 1 轮，新增 `docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260527-round-01-wip-triage.md`。
- 当前 WIP 已归为三组：Issue 20 tap activation 收尾、WebView wait 扩展、Command surface optimization 规划。
- 验证通过：`swift test --package-path CLI --filter WebViewRouteTests`、`swift test --package-path CLI --filter InputOutputTests`、`xcodebuild -scheme TritonKit-Package -destination 'platform=iOS Simulator,id=60667794-96F8-40E6-8664-85538EC4663E' test`。
- 风险结论：工作区仍未清洁；进入破坏性 CLI 重排前应先拆分提交或明确暂存策略。未 push、未 tag、未 release。

## Command surface optimization Round 03 baseline

- 新增 `docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260527-round-03-command-baseline.md`，冻结当前 CLI 命令面基线。
- 当前 `triton --help` 和 `triton schema --json` 均暴露 52 个命令，schema version 为 1。
- 当前 schema 覆盖结论：`requiresServer` 32 个、`requiresTarget` 33 个、`requiresHierarchy` 1 个；但 `nextCommands` 仅 2 个命令、`failureCodes` / `outputContracts` / `artifacts` 均仅 4 个命令，作为 agent 事实源仍明显不足。
- 差距结论：当前 root 命令仍偏扁平，没有 `target` / `project` / `action` 分层；`plan` 仍不是任务型 workflow planner；`capabilities` 还需要升级为环境能力矩阵。
- 执行注意：后续 SwiftPM 命令串行运行，避免多个 `swift run --package-path CLI` 进程争抢同一个 `.build` 锁。未 push、未 tag、未 release。

## Command surface optimization Round 04 schema bootstrap facts

- 新增 `CLI/Tests/TritonKitCLITests/SchemaFactSourceTests.swift`，先用红灯确认 `status` / `doctor` / `capabilities` / `plan` / `schema` 缺少 `failureCodes`、`nextCommands` 和 `outputContracts`。
- 新增 `Sources/TritonKitCLI/CLISchemaContracts.swift`，集中定义 bootstrap output contracts，避免继续把重复 contract helper 堆进 `CLISchemaRuntime.swift`。
- 更新 `Sources/TritonKitCLI/CLISchemaRuntime.swift`，补齐 bootstrap 命令的恢复建议、错误码和 output contract；本轮不改变 CLI 执行行为。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`；`swift test --package-path CLI` 通过，68 个 Swift Testing 用例通过。
- schema 覆盖提升：`nextCommands` 覆盖命令数 2 -> 7，`failureCodes` 4 -> 9，`outputContracts` 4 -> 9。下一批应覆盖 `find` / `wait` / `tap` / `input` / `assert` / `evidence` / `capture` / `replay`。

## Command surface optimization Round 04 execution/evidence schema facts

- 继续扩展 `SchemaFactSourceTests`，用红灯确认 `find` / `wait` / `tap` / `input` / `assert` / `evidence` / `capture` / `replay` 的恢复建议、失败码和 output contract 为空。
- 更新 `CLISchemaContracts.swift`，新增 `target.resolution`、`wait.result`、`input.result`、`input.summary`、`assert.result`、`evidence.manifest`、`replay.result` 等 output contract helper。
- 更新 `CLISchemaRuntime.swift`，补齐执行、验收、证据和复跑链路命令的 `nextCommands`、`failureCodes`、`outputContracts` 与 evidence artifact 标注；本轮仍不改变 CLI 执行行为。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`；`swift test --package-path CLI` 通过，69 个 Swift Testing 用例通过。
- schema 覆盖继续提升：`nextCommands` 覆盖命令数 7 -> 15，`failureCodes` 9 -> 17，`outputContracts` 9 -> 17，`artifacts` 4 -> 6。下一批应覆盖 observe / runtime / webview / route / screenshot 等观察与诊断入口。

## Command surface optimization Round 04 observation/runtime schema facts

- 继续扩展 `SchemaFactSourceTests`，用红灯确认 `runtime` / `state` / `snapshot` / `observe` / `webview` / `route` / `screenshot` 缺少诊断恢复建议、失败码和 output contract。
- 更新 `CLISchemaContracts.swift`，新增 `runtime.manifest`、`runtime.state`、`runtime.snapshot`、`observe.surface`、`webview.snapshot`、`webview.wait`、`route.current-url-assert`、`screenshot.metadata`、`host.artifact` 等 output contract helper。
- 更新 `CLISchemaRuntime.swift`，补齐 agent 观察、WebView、route、runtime snapshot、screenshot 入口的 `nextCommands`、`failureCodes`、`outputContracts` 与 artifact 标注；本轮仍不改变 CLI 执行行为。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`；`swift test --package-path CLI` 通过，70 个 Swift Testing 用例通过。
- schema 覆盖继续提升：`nextCommands` 覆盖命令数 15 -> 22，`failureCodes` 17 -> 24，`outputContracts` 17 -> 24，`artifacts` 6 -> 10。下一批应覆盖 host-side `sim` / `device` / `app` / `smoke` / `record` 等命令。

## Mirroir host adapter 下期计划

- 按用户要求将 `jfarcand/mirroir-mcp` 克隆到 `docs-linhay/references/mirroir-mcp/`，作为 host-side iPhone Mirroring / macOS window automation 参考项目；当前 HEAD 为 `7e97f0c`。
- 关键决策：TritonKit 不接 MCP、不引入 mirroir 的 stdio JSON-RPC server、不新增 MCP 工具面；只吸收 window discovery、screenshot、Vision OCR、CGEvent input、target registry 和 fail-closed permission policy 等底层能力。
- 新建 `docs-linhay/spaces/20260527-mirroir-host-adapter/`，下期第一刀建议只做只读能力：host target discovery、host screenshot metadata、schema/capabilities/evidence 集成；tap/swipe/type/press 输入动作推迟到确认策略和 error envelope 稳定后再做。

## Command surface optimization Round 04 host workflow schema facts

- 继续扩展 `SchemaFactSourceTests`，用红灯确认 `device` / `sim` / `app` / `smoke` / `record` 缺少 host target、artifact、smoke result 和 plan template output contract。
- 更新 `CLISchemaContracts.swift`，新增 `host.device-list`、`host.device-selection`、`host.device-ready`、`host.simulator-list`、`host.simulator-action`、`host.app-action`、`host.app-open-url`、`smoke.result`、`record.plan` 等 output contract helper。
- 更新 `CLISchemaRuntime.swift`，补齐 host-side target/app/smoke/record 命令的 `nextCommands`、`failureCodes`、`outputContracts` 与 artifact 标注；本轮仍不改变 CLI 执行行为。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/hostWorkflowSchemasExposeTargetAndArtifactContracts`；`swift test --package-path CLI` 通过，71 个 Swift Testing 用例通过。
- schema 覆盖继续提升：`nextCommands` 覆盖命令数 22 -> 27，`failureCodes` 24 -> 29，`outputContracts` 24 -> 29，`artifacts` 10 -> 14。下一批可覆盖低层 runtime inspection 与 semantic action 命令，然后进入 Round 05 错误 envelope 一致性核对。

## Command surface optimization Round 04 low-level runtime schema facts

- 继续扩展 `SchemaFactSourceTests`，用红灯确认 `list` / `inspect` / `hierarchy` / `nodes` / `node` / `attrs` / `object` / `export` / `geometry` / `hit` / `focus` / `set-text` / `select-segment` / `set-switch` / `ledger` 缺少低层 inspection、semantic action 与 ledger 契约。
- 更新 `CLISchemaContracts.swift`，新增 `targets.list`、`target.summary`、`hierarchy.info`、`hierarchy.nodes`、`hierarchy.node`、`node.resolve`、`node.attributes`、`node.object`、`export.archive`、`geometry.current`、`hit.result`、`semantic.action`、`runtime.ledger`、`runtime.ledger-entry` 等 output contract helper。
- 更新 `CLISchemaRuntime.swift`，补齐低层 runtime inspection、export、geometry/hit、semantic action、ledger 命令的 `nextCommands`、`failureCodes`、`outputContracts` 与 artifact 标注；本轮仍不改变 CLI 执行行为。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/lowLevelRuntimeSchemasExposeInspectionAndActionContracts`；`swift test --package-path CLI --filter SchemaFactSourceTests`；`swift test --package-path CLI` 通过，72 个 Swift Testing 用例通过。
- schema 覆盖继续提升：`nextCommands` 覆盖命令数 27 -> 42，`failureCodes` 29 -> 44，`outputContracts` 29 -> 44，`artifacts` 14 -> 17。下一轮进入 Round 05，系统核对 runtime error envelope 与 schema `failureCodes` 一致性。

## Command surface optimization Round 05 error envelope consistency

- 新增 `docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260527-round-05-error-envelope-consistency.md`，记录错误 envelope 一致性第一刀。
- 扩展 `FailureDiagnosticsTests`，锁定 runtime HTTP 已返回的 `TKCLIErrorResponse` 必须原样保留，不得被 CLI 重包成 `request_failed`；同时锁定 runtime-facing schema 必须列出 `runtime_ui_interrupted`、`request_timeout`、`invalid_payload`、`action_not_supported`、`unsupported_runtime_scope` 等真实 runtime 错误码。
- 更新 `CLIRuntimeTransport.swift`，新增 `cliErrorResponse(for:endpoint:host:port:)`，并让 `printCLIError` / `failCommand` JSON 分支统一使用该 helper；`CLIHTTPError.response` 存在时直接透传。
- 更新 `CLISchemaRuntime.swift`，补齐 runtime/state/snapshot/geometry/hit/ledger/semantic action 的 runtime envelope failure codes。
- 验证通过：`swift test --package-path CLI --filter FailureDiagnosticsTests`；`swift test --package-path CLI --filter SchemaFactSourceTests`；`swift test --package-path CLI` 通过，74 个 Swift Testing 用例通过。
- 风险记录：本轮操作中误触发过并发 SwiftPM / 文档记录，最终均完成并通过；后续必须严格串行 SwiftPM 与 文档记录。下一批建议核对 host-side `failHostCommand` 与 `device` / `sim` / `app` / `xcode` / `xcresult` / `xctrace` / `coverage` schema 的 failure code 一致性。

## Command surface optimization Round 05 host-side failure code coverage

- 继续扩展 `FailureDiagnosticsTests`，新增集合级测试，要求 host-facing schema 的 `failureCodes` 并集覆盖 `failHostCommand` 可能输出的 host error code。
- 红灯确认 schema 缺少 20 个 host-side code：Harmony layout path/text/layout/artifact recv、sim status-bar / privacy / location / ui / pasteboard / push、pair/unpair/clone/erase/upgrade、record/logs/diagnose/logverbose、plist not found。
- 更新 `CLISchemaRuntime.swift`，把缺失 code 补入对应 domain schema：`sim`、`app`、`observe`。
- 验证通过：`swift test --package-path CLI --filter FailureDiagnosticsTests`；`swift test --package-path CLI --filter SchemaFactSourceTests`；`swift test --package-path CLI` 通过，75 个 Swift Testing 用例通过。
- 下一批建议核对 WebView 专用失败、smoke step failure 与 command-local validation failure，重点检查 `failWebViewCommand`、`failEvidenceValidation`、`failReplayValidation`、`failHostValidation` 等入口。

## Command surface optimization Round 05 command-local failure code coverage

- 继续扩展 `FailureDiagnosticsTests`，新增 command-local validation / workflow failure code 覆盖测试，要求 `webview`、`route`、`smoke`、`sim`、`app`、输入动作命令以及 evidence/replay/record/assert 的 schema `failureCodes` 覆盖实际专用错误码。
- 红灯确认 schema 缺少 route server/target/request 透传码，smoke 的 `runtime_not_connected` / `smoke_step_failed` / `app_info_not_available`，sim 的 `confirmation_required` / `invalid_duration` / `invalid_location_value` / runtime selector 失败码，app 的 `destructive_action_requires_policy`，以及 `swipe` / `type` / `paste` / `clear` / `press` 的输入动作失败码。
- 更新 `CLISchemaRuntime.swift`，补齐上述 failure code，并为 `swipe`、`type`、`paste`、`clear`、`press` 增加 machine-readable failure shape。
- 验证通过：`swift test --package-path CLI --filter FailureDiagnosticsTests`；`swift test --package-path CLI --filter SchemaFactSourceTests`；`swift test --package-path CLI` 通过，76 个 Swift Testing 用例通过。
- 关键决策：schema `failureCodes` 必须覆盖 runtime/server 透传码、host adapter 码、command-local validation 码和 workflow summary 专用码；agent-facing CLI 的事实源不能只描述“主要失败路径”。

## Command surface optimization Round 05 schema command diagnostics

- 继续扩展 `SchemaFactSourceTests`，新增 `schemaCommandFilteringAndUnknownCommandDiagnosticsAreMachineReadable`，锁定 `triton schema --command <name>` 的单命令过滤和未知命令诊断。
- 更新 `CLICoreCommands.swift`，抽出 `buildSchemaResponse(command:)`，新增 `SchemaCommandLookupError` 与 `schemaUnknownCommandErrorResponse(_:)`。
- 关键行为：`triton schema --command tap --json` 仍返回一个 `TKCLISchemaResponse`，仅包含 `tap`；`triton schema --command not-a-command --json` 返回 exit 1，并输出单个 JSON `unknown_command_schema` envelope，带 `nextAction: schema --json`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`；`swift test --package-path CLI` 通过，77 个 Swift Testing 用例通过；真实 CLI 验证 `swift run --package-path CLI triton schema --command not-a-command --json` 输出符合预期。
- 风险记录：本轮仍出现过 SwiftPM 等待另一个实例的提示，说明必须继续严格单命令串行执行 SwiftPM / 文档记录，避免 `.build` 锁竞争。

## Command surface optimization Round 06 schema runtime split

- 新增 `SchemaFactSourceTests.schemaCommandInventoryRemainsStableForAgentDiscovery`，锁定 schema command 数量 52、名称唯一性和当前顺序，作为后续拆分 `CLISchemaRuntime.swift` 的 agent-facing 契约护栏。
- 新增 `Sources/TritonKitCLI/CLISchemaShared.swift`，迁出 schema 共享 option、failure code 集合、`inputActionSchemas()` / `inputField(...)` 和 `renderSchema(...)`。
- 更新 `Sources/TritonKitCLI/CLISchemaRuntime.swift`，顶部本地重复定义改为引用 shared facts，命令事实数组顺序保持不变；文件行数从约 2452 行降到约 2267 行。
- 新增 `Sources/TritonKitCLI/CLISchemaBootstrapCommands.swift`，迁出 `version`、`serve`、`status`、`doctor`、`plan`、`capabilities`、`schema` 这 7 个 bootstrap command facts；`commandSchemas()` 改为 `bootstrapCommandSchemas() + [...]`，文件行数进一步降到约 2124 行。
- 新增 `Sources/TritonKitCLI/CLISchemaXcodeCommands.swift`，机械迁出 `xcode`、`xcresult`、`xctrace`、`coverage` 这 4 个 host Xcode command facts；`commandSchemas()` 改为 `bootstrapCommandSchemas() + xcodeCommandSchemas() + [...]`，文件行数进一步降到约 1639 行。
- 新增 `Sources/TritonKitCLI/CLISchemaRuntimeCommands.swift`，机械迁出 `runtime`、`state`、`snapshot`、`focus`、`set-text`、`select-segment`、`set-switch`、`ledger` 这 8 个 embedded runtime command facts；`commandSchemas()` 改为 `bootstrapCommandSchemas() + xcodeCommandSchemas() + runtimeCommandSchemas() + [...]`，文件行数进一步降到约 1377 行，低于 1500 行治理线。
- 新增 `Sources/TritonKitCLI/CLISchemaHostCommands.swift`，机械迁出 `device`、`sim`、`app` 这 3 个 host command facts；`commandSchemas()` 改为 `bootstrapCommandSchemas() + xcodeCommandSchemas() + runtimeCommandSchemas() + hostCommandSchemas() + [...]`，文件行数进一步降到约 1056 行。
- 新增 `Sources/TritonKitCLI/CLISchemaObservationCommands.swift`，机械迁出 `list`、`inspect`、`observe`、`webview`、`route`、`hierarchy`、`nodes`、`node`、`attrs`、`object`、`export`、`geometry`、`hit`、`screenshot` 这 14 个 observation command facts；`commandSchemas()` 改为 `bootstrapCommandSchemas() + xcodeCommandSchemas() + runtimeCommandSchemas() + hostCommandSchemas() + observationCommandSchemas() + [...]`，文件行数进一步降到约 243 行，回到纯组合层。
- 新增 `Sources/TritonKitCLI/CLISchemaActionCommands.swift`，机械迁出 `tap`、`swipe`、`type`、`paste`、`clear`、`press`、`input` 这 7 个 action command facts；`commandSchemas()` 改为 `bootstrapCommandSchemas() + xcodeCommandSchemas() + runtimeCommandSchemas() + hostCommandSchemas() + observationCommandSchemas() + actionCommandSchemas()`，文件行数进一步降到约 13 行。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260527-round-06-schema-runtime-split.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`；`swift test --package-path CLI` 通过，78 个 Swift Testing 用例通过；`git diff --check -- Sources/TritonKitCLI/CLISchemaRuntime.swift Sources/TritonKitCLI/CLISchemaShared.swift CLI/Tests/TritonKitCLITests/SchemaFactSourceTests.swift`；`docs-linhay/scripts/check-docs.sh`。
- bootstrap 拆分后再次验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`；`swift test --package-path CLI` 通过，78 个 Swift Testing 用例通过。
- Xcode group 拆分后验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`；中断恢复后重新跑 `swift test --package-path CLI`，78 个 Swift Testing 用例通过。
- runtime group 拆分后验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`；`swift test --package-path CLI` 通过，78 个 Swift Testing 用例通过。
- host group 拆分后验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`；`swift test --package-path CLI` 通过，78 个 Swift Testing 用例通过。
- observation group 拆分后验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`；`swift test --package-path CLI` 通过，78 个 Swift Testing 用例通过。
- action group 拆分后验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`；`swift test --package-path CLI` 通过，78 个 Swift Testing 用例通过。
- 风险记录：本轮执行中仍误用过并行 wrapper 触发 SwiftPM、文档记录、check 命令；其中一个 历史检索 session 因 SQLite primary key constraint 失败，另一个 历史检索 session 完整成功并完成 embed。后续必须严格单命令串行执行 SwiftPM、文档记录 和 check-docs。`CLISchemaRuntime.swift` 已收敛到组合层，后续重点转向最终验收与报告收敛。
- 收尾验证：`git diff --check` 通过；`docs-linhay/scripts/check-docs.sh` 完成并成功 embed 40 chunks from 3 documents；`swift test --package-path CLI` 通过，78 个 Swift Testing 用例通过。
- 结论：Round 06 已完成最终验收，`CLISchemaRuntime.swift` 保持纯组合层，下一步转向 Round 07 / 方案 C 的后续切片，不再继续治理 root 文件大小。
