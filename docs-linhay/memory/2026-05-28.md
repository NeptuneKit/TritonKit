# 2026-05-28

## Command surface optimization Round 07 agent-facing CLI information architecture

- 启动长期计划执行续航，当前切片进入第 7 轮：agent-facing CLI 信息架构设计落文档。
- 新增 `docs-linhay/dev/agent-facing-cli-information-architecture.md`，将 `triton` 定义为 agent 本机控制面，并按 Bootstrap、Capabilities、Doctor、Target、Project/Xcode、Runtime、Observe、Action、Assert、Evidence/Replay 10 个信息域组织。
- 文档定义 agent 标准启动流程：`version -> schema -> doctor -> capabilities -> plan`，以及 target 选择流程：`target list -> resolve -> use -> current -> wait-ready`。
- 文档定义 command schema、capabilities、doctor、plan、error envelope、evidence / replay 的机器可读契约，并明确破坏性更新必须同步命令实现、schema、测试、README、public skills、dev 文档、checkpoint、memory 和 qmd。
- 更新 `docs-linhay/spaces/20260527-command-surface-optimization/README.md`，把新架构文档加入当前 space 关联文档。
- 更新 `docs-linhay/dev/ai-cli-readable-control.md`，标注长期信息架构以新文档为准，原文继续记录当前 CLI / HTTP / runtime 已实现契约。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260528-round-07-agent-facing-cli-information-architecture.md`。
- 下一步进入 Round 08：建立 `target` 一等入口，优先测试锁定 `target list/use/current/resolve/wait-ready` 的 JSON shape，再迁移现有 `device` / `sim` 目标选择能力。

## Command surface optimization Round 08 target entry

- 新增 `Sources/TritonKitCLI/CLITargetCommands.swift`，把 `target` 建成 agent-facing 一等入口，子命令覆盖 `list`、`use`、`current`、`resolve`、`wait-ready`。
- 更新 `Sources/TritonKitCLI/TritonKitCLI.swift`，将 `Target.self` 注册进 root subcommands，放在 bootstrap 之后、Xcode 之前。
- 新增 `Sources/TritonKitCLI/CLISchemaTargetCommands.swift`，将 `target` schema 落成命令事实源，复用 host device list / selection / ready output contracts。
- 更新 `Sources/TritonKitCLI/CLISchemaRuntime.swift`，让 `targetCommandSchemas()` 进入 schema 事实源顺序。
- 新增 `CLI/Tests/TritonKitCLITests/SchemaFactSourceTests.targetSchemasExposeDiscoveryAndReadinessContracts`，锁定 `target` 的 failure codes、nextCommands 和输出契约字段。
- 更新 `docs-linhay/dev/ai-cli-readable-control.md`，补入 `triton target list|use|current|resolve|wait-ready` 入口说明。
- 更新 `README.md`，把 `target` 标成首选目标选择入口，并把 iOS / Harmony 示例改为优先使用 `target` 做发现、解析和 ready 等待。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260528-round-08-target-entry.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`；`swift test --package-path CLI` 通过，79 个 Swift Testing 用例通过；`git diff --check`；`docs-linhay/scripts/check-docs.sh`。
- 下一步进入 Round 09：升级 `capabilities` 为环境能力矩阵，并继续把 target、device、runtime、app 和 xcode 的能力输出统一到同一套可解释契约。

## Command surface optimization Round 09 capabilities matrix

- 扩展 `TKRuntimeCapability`，新增 `group`、`requiredBy`、`nextAction`、`evidence`，将 `triton capabilities --json` 从能力名清单升级为 agent-facing 环境能力矩阵。
- 更新 `runtimeCapabilities` enrichment：target、runtime、host、xcode、observe、action、assert、evidence、replay 等能力现在带分组、依赖 workflow、恢复/执行建议与证据面。
- server 不可达且能力需要 server 时，`nextAction` 返回 `serve --host 127.0.0.1 --port 19421` 并标记 `requiresLongRunningProcess=true`；runtime 未连接且能力需要 embedded target 时，`nextAction` 返回 `status --json`。
- 补齐 capabilities schema output contract，显式暴露 `capabilities[].name/supported/reason/group/requiredBy/nextAction/evidence`。
- 更新 `docs-linhay/dev/ai-cli-readable-control.md`、`docs-linhay/dev/agent-facing-cli-information-architecture.md`、public skills，并新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260528-round-09-capabilities-matrix.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`，9 个 Swift Testing 用例通过；`swift test --package-path CLI`，80 个 Swift Testing 用例通过；`swift test`，125 个 Swift Testing 用例通过。
- 下一步进入 Round 10：升级 `triton plan` 的任务型 planning 能力，优先覆盖 iOS smoke、open-url 和 webview-check 高频目标。

## Command surface optimization Round 10 task workflow plans

- `TKWorkflowPlanResponse` 新增 `goal` 字段，`triton plan` 开始支持任务型 planning。
- 新增任务目标：`ios-smoke`、`open-url`、`webview-check`；新增参数：`--device`、`--bundle-id`、`--url`、`--text`、`--expected-url`、`--evidence`。
- `ios-smoke` 输出 target list / resolve / use / wait-ready、`triton smoke ios ... --json` 与 evidence summary；`open-url` 输出 app open-url、wait、assert、evidence；`webview-check` 输出 WebView current、route assert、WebView wait、evidence。
- 修正 `plan` 解析分支：`inspect` 继续走 `.tritonplan` 离线摘要，任务目标走 workflow planner，不再被误判为 unsupported inspect action。
- 更新 README、`docs-linhay/dev/ai-cli-readable-control.md`、`docs-linhay/dev/agent-facing-cli-information-architecture.md` 与 public skills，明确 task plan 只给建议，不替代真实命令执行。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260528-round-10-task-plans.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`，10 个 Swift Testing 用例通过；`swift test --package-path CLI`，81 个 Swift Testing 用例通过；`swift test`，125 个 Swift Testing 用例通过；`swift run --package-path CLI triton plan ios-smoke ... --json` 在无 server 环境下返回合法 JSON、`goal=ios-smoke` 和 `nextStep=start-server`。
- 下一步进入 Round 11：升级 `doctor` 恢复路径建议，基于 capabilities 的 group/nextAction 让 agent 更快定位缺 server、缺 target、缺 runtime、缺 host tool 或缺 artifact 配置。

## Command surface optimization Round 11 doctor recovery checks

- 新增 `TKDoctorCheck` 与 `TKDoctorResponse`，`triton doctor --json` 不再复用 capabilities 输出，而是返回有序诊断检查。
- `doctor` 输出字段为 `ok/serverReachable/connected/runtime/nextStep/checks[]/error`；每个 check 包含 `id/status/code/message/hint/nextAction/relatedCapabilities`。
- `buildDoctorResponse` 基于 capabilities 生成恢复路径：server 不可达给 `start-server + serve`，runtime 未连接给 `connect-target + target list`，runtime 已连接时给 server/target/runtime pass，action surface 受限时给 warn 并指向 `capabilities --json`。
- 更新 doctor schema output contract、`docs-linhay/dev/ai-cli-readable-control.md` 与 public skills，明确 doctor 负责 ordered recovery，capabilities 负责 capability matrix。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260528-round-11-doctor-recovery-checks.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`，11 个 Swift Testing 用例通过；`swift test --package-path CLI`，82 个 Swift Testing 用例通过；`swift test`，126 个 Swift Testing 用例通过；`swift run --package-path CLI triton doctor --json` 在无 server 环境下返回合法 JSON、`nextStep=start-server`、`checks[]` 和 `nextAction.command=serve`。
- 下一步进入 Round 12：整理 action 层，优先让 action schema、capabilities、doctor 的 unsupported 与恢复建议保持一致。

## Command surface optimization Round 12 action contract alignment

- 修正 action capabilities 的 `nextAction` 与当前 schema 不一致问题：`swipe` 改为 `--start-x/--start-y/--end-x/--end-y`，`clear` 改为 `--at <x,y>`，`input` 改为 `--json --summary --strict`。
- embedded `press` 继续明确 unsupported，capabilities 的 `nextAction` 指向 `schema --command press --json`，避免 agent 把 host-side Harmony keyEvent 或未来 HID 能力误认为 embedded runtime 可用。
- 为 `tap/swipe/type/paste/clear/input` 补齐 `input.result` 与 `runtime-ledger` evidence；为 `press` 补齐 `unsupported-envelope` 与 `command-schema` evidence。
- 修正 `triton schema --command input --json` 的 `providedCapabilities`，加入 `input`，移除 unsupported `press`。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-emulator-cli-takeover` 与 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260528-round-12-action-contract-alignment.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`，11 个 Swift Testing 用例通过；`swift test --package-path CLI`，82 个 Swift Testing 用例通过。
- 下一步进入 Round 13：整理 observe / webview / route 层，优先检查 hybrid/provider 边界在 schema、capabilities、doctor 和 plan 中是否一致。

## Command surface optimization Round 13 observe/webview/route boundaries

- 补齐 `triton capabilities --json` 中缺失的 WebView / route 细粒度能力：`webview-list/current/current-url/snapshot/bridge-call/events/wait` 和 `route-current-url-assert`。
- 将 `webview-list/current` 定义为候选发现能力，证据来自 `webview-candidates`、`host-layout`、`runtime-ax`；将 `webview-current-url/snapshot/bridge-call/events/wait` 与 `route-current-url-assert` 定义为 provider 级能力，runtime 未连接时给出 `status --json` 或 `serve` 恢复动作。
- `route-current-url-assert` 独立归入 `route` group，明确它依赖 WebView provider URL，不等同于 native `state route` 或 AX/layout URL 文本。
- `SchemaFactSourceTests` 新增断言，要求 `triton schema --command webview|route --json` 的 `providedCapabilities` 能在 capabilities matrix 中找到，避免 schema 与 capabilities 事实源分裂。
- 更新 README、`docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md` 与 public skills，统一说明 WebView 候选发现和 provider 级操作的边界。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260528-round-13-observe-webview-route-boundaries.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`，11 个 Swift Testing 用例通过；`swift test --package-path CLI`，82 个 Swift Testing 用例通过。
- 下一步进入 Round 14：整理 `assert` / `evidence` / `replay` 闭环，重点检查失败后的 evidence 指向、assert 输出与 replay dry-run 是否足够让 agent 自恢复。

## Command surface optimization Round 14 assert/evidence/replay closure

- 补齐 `triton capabilities --json` 中缺失的 `evidence`、`smoke-ios`、`smoke-harmony` 能力，避免 schema `providedCapabilities` 与 capabilities matrix 分裂。
- 将 `evidence-summary` 与 `evidence-redact` 的 `nextAction` 改成对应子命令，而不是泛化的 `evidence --output <dir.tritonevidence> --json`。
- `smoke-ios` / `smoke-harmony` 独立归入 `smoke` group，`evidence` 记录 `smoke-summary` 与 `evidence-bundle`；同时明确 smoke 是编排能力，不替代 wait/assert/evidence proof。
- `replay` schema 的 `providedCapabilities` 增加 `replay-dry-run`，明确 dry-run 是离线校验能力，不等同于真实 replay 执行。
- `SchemaFactSourceTests` 新增断言，要求 evidence / capture / smoke / record / replay 的 `providedCapabilities` 都能在 capabilities matrix 中找到。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md` 与 public skills。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260528-round-14-assert-evidence-replay-closure.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`，11 个 Swift Testing 用例通过。
- 下一步进入 Round 15：回查 `schema --command` 过滤、命令 inventory 和 README 示例，检查方案 C 的 agent 首选路径是否已经完整覆盖 `target -> plan -> execute -> assert -> evidence -> replay`。
