# 2026-05-29

## Command surface optimization Round 15 schema/capabilities global invariant

- 继续方案 C 长期计划，目标是把 schema 与 capabilities 的事实源对齐从人工点名升级为全局测试不变量。
- 新增 `SchemaFactSourceTests.schemaProvidedCapabilitiesAreDiscoverableInCapabilitiesMatrix`，要求所有 `commandSchemas().flatMap(\.providedCapabilities)` 都能在 `runtimeCapabilities(... connected:true)` 中找到。
- 补齐 capabilities matrix 中缺失的 host target、host simulator、host app、Harmony action、Xcode、screenshot 和 semantic action 能力项。
- 为新增能力补默认 `group`、`requiredBy`、`nextAction` 和 `evidence`，让 agent 不只知道能力名，也能得到下一步命令和证据面。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md` 和 `tritonkit-emulator-cli-takeover`，明确新增 schema capability 时必须同步 capabilities matrix。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-15-schema-capabilities-global-invariant.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaProvidedCapabilitiesAreDiscoverableInCapabilitiesMatrix`；`swift test --package-path CLI --filter SchemaFactSourceTests`，12 个 Swift Testing 用例通过。
- 下一步进入 Round 16：检查 `schema --command` 过滤输出与 README / public skills 的 agent 首选路径一致性。

## Command surface optimization Round 16 schema command filtering invariant

- 新增 `SchemaFactSourceTests.schemaCommandFilteringCoversTheFullCommandInventory`，遍历所有 `commandSchemas()` 命令名并验证 `buildSchemaResponse(command:)` 只返回该单个命令。
- 该测试保证 agent 可以先读取全量 schema inventory，再按需用 `schema --command <name> --json` 读取精确契约，不需要每次处理完整 schema。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md` 和 `tritonkit-dev-feedback`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-16-schema-command-filtering-invariant.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaCommandFilteringCoversTheFullCommandInventory`；`swift test --package-path CLI --filter SchemaFactSourceTests`，13 个 Swift Testing 用例通过。
- 下一步进入 Round 17：检查 `triton plan` 输出 command 字符串与 schema 参数形态是否一致。

## Command surface optimization Round 17 capability planning metadata invariant

- Round 17 先补强 capabilities 元数据不变量：所有 schema 暴露的 `providedCapabilities[]` 不只要能在 capabilities matrix 中发现，还必须有非 `misc` 分组、`nextAction` 和 evidence。
- 新增 `SchemaFactSourceTests.schemaProvidedCapabilitiesExposePlanningMetadata`，聚合输出缺失 `group`、`misc` 分组、缺失 `nextAction`、缺失 evidence 的 capability 清单。
- 补齐 bootstrap、runtime state、semantic action、observe、node、wait/assert、screenshot、Xcode 基础能力和 replay dry-run 的 `nextAction` / `evidence`。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-emulator-cli-takeover`、`tritonkit-dev-feedback` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-17-capability-planning-metadata-invariant.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaProvidedCapabilitiesExposePlanningMetadata`；`swift test --package-path CLI --filter SchemaFactSourceTests`，14 个 Swift Testing 用例通过；`swift test --package-path CLI`，85 个 Swift Testing 用例通过。
- 下一步进入 Round 18：检查 `triton plan` 输出 command 字符串与 schema 参数形态是否一致。

## Command surface optimization Round 18 plan schema alignment invariant

- 新增 `SchemaFactSourceTests.taskWorkflowPlanCommandsStayAlignedWithCommandSchemas`，覆盖 `ios-smoke`、`open-url`、`webview-check` 三个任务型 plan。
- 测试要求 plan 的每条 `steps[].command` 都能被 schema 解释：根命令存在、子命令存在、所有 `--flag` 已在根 schema 或 subcommand schema 声明。
- 红灯暴露了 `target --host/--port`、`smoke --assert-text` 和 `smoke --json` 未进入 schema 的问题。
- 修正 `target` schema 补齐 host/port；修正 `smoke` schema 补齐 `--assert-text`、`--evidence-name`、`--evidence-note`、`--host`、`--port`、`--interval`、`--format` 和 `--json`。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-emulator-cli-takeover`、`tritonkit-dev-feedback` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-18-plan-schema-alignment-invariant.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/taskWorkflowPlanCommandsStayAlignedWithCommandSchemas`；`swift test --package-path CLI --filter SchemaFactSourceTests`，15 个 Swift Testing 用例通过；`swift test --package-path CLI`，86 个 Swift Testing 用例通过。
- 下一步进入 Round 19：把 `capabilities[].nextAction` 推荐命令也纳入 schema 参数形态校验。

## Command surface optimization Round 19 capability nextAction schema alignment

- 新增 `SchemaFactSourceTests.capabilityNextActionsStayAlignedWithCommandSchemas`，遍历 connected capabilities matrix 中所有 `nextAction`。
- 测试要求 `nextAction.command` 存在于 schema；若该 command 声明了 subcommands，则 `nextAction.args.first` 子命令必须存在；所有 `--flag` 必须在根 schema 或 subcommand schema 中声明。
- 红灯暴露了 simulator artifact / push 参数、app prefs 参数和 Harmony app install nextAction 与 schema 不一致。
- 修正 `sim` schema，补齐 `--simulator`、`--output`、`--bundle-id`、`--payload`；修正 `app` schema，补齐 `--bundle-id`、`--app`、`--hap`；修正 Harmony install nextAction 为 `app install --platform harmony --hap <path.hap> --json`。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-emulator-cli-takeover`、`tritonkit-dev-feedback` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-19-capability-nextaction-schema-alignment.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/capabilityNextActionsStayAlignedWithCommandSchemas`；`swift test --package-path CLI --filter SchemaFactSourceTests`，16 个 Swift Testing 用例通过；`swift test --package-path CLI`，87 个 Swift Testing 用例通过。
- 下一步进入 Round 20：把 schema `nextCommands[]` 恢复建议也纳入 schema 参数形态校验。

## Command surface optimization Round 20 schema nextCommands alignment

- 新增 `SchemaFactSourceTests.schemaNextCommandsStayAlignedWithCommandSchemas`，遍历所有 command schema 的 `nextCommands[]`。
- 测试要求每条恢复建议都是 schema-backed command：根命令存在、子命令存在、所有 `--flag` 已声明。
- 当前新增不变量直接通过，说明现有 `nextCommands[]` 与 schema 参数事实源一致。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md` 和 `tritonkit-dev-feedback`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-20-schema-nextcommands-alignment.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaNextCommandsStayAlignedWithCommandSchemas`；`swift test --package-path CLI --filter SchemaFactSourceTests`，17 个 Swift Testing 用例通过；`swift test --package-path CLI`，88 个 Swift Testing 用例通过。
- 下一步进入 Round 21：抽取重复的 schema-backed command 校验逻辑，降低 plan / capabilities / schema 三个不变量的维护成本。

## Command surface optimization Round 21 schema command validation helper

- 抽取 `SchemaBackedCommandIssues`、`validateSchemaBackedCommandString(...)` 和 `validateSchemaBackedCommand(...)`，统一 plan command、capabilities nextAction、schema nextCommands 的 schema-backed command 校验逻辑。
- 保留 Round 18-20 的三个不变量，行为不变，只减少重复测试代码。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-21-schema-command-validation-helper.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`，17 个 Swift Testing 用例通过；`swift test --package-path CLI`，88 个 Swift Testing 用例通过。
- 下一步进入 Round 22：检查 `outputContracts[]` 与 schema success/failure shape 的最低字段覆盖是否能形成全局不变量。

## Command surface optimization Round 22 capability output contracts

- 新增 `SchemaFactSourceTests.commandsThatProvideCapabilitiesExposeOutputContracts`，要求所有声明 `providedCapabilities[]` 的命令都必须有 `outputContracts[]`。
- 红灯暴露 `ax`、`clear`、`paste`、`press`、`swipe`、`type` 缺少 outputContracts。
- 为 `swipe`、`type`、`paste`、`clear` 复用 `inputResultOutputContract()`；为 `press` 增加 embedded `input.result` 与 Harmony host `host.key-action`；新增 `axOutputContract()` 并让 `ax` 同时声明 host artifact contract。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md` 和 `tritonkit-dev-feedback`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-22-capability-output-contracts.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/commandsThatProvideCapabilitiesExposeOutputContracts`；`swift test --package-path CLI --filter SchemaFactSourceTests`，18 个 Swift Testing 用例通过；`swift test --package-path CLI`，89 个 Swift Testing 用例通过。
- 下一步进入 Round 23：检查 `outputContracts[].fields` 的最低字段质量，避免空字段或低信号 contract。

## Command surface optimization Round 23 output contract field quality

- 新增 `SchemaFactSourceTests.schemaOutputContractsExposeNonemptyFields`，要求所有 output contract 具备非空 `selector`、非空 `model`、非空 `fields[]`。
- 测试同时检查同一 contract 内字段名不能重复，且每个 field 的 `name`、`type`、`description` 都不能为空。
- 聚焦测试先发现测试实现需要按 optional `model` 处理；修正后现有 schema 直接满足该不变量。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`，明确 output contract 不能只是低信号占位。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-23-output-contract-field-quality.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaOutputContractsExposeNonemptyFields`；`swift test --package-path CLI --filter SchemaFactSourceTests`，19 个 Swift Testing 用例通过；`swift test --package-path CLI`，90 个 Swift Testing 用例通过。
- 下一步进入 Round 24：检查 failure code 覆盖，确保有失败退出或 failure shape 的 agent-facing command 都有稳定 `failureCodes[]`。

## Command surface optimization Round 24 failure code coverage

- 新增 `SchemaFactSourceTests.schemaFailureSurfacesExposeStableFailureCodes`，要求所有声明失败退出或非空 `failureShape` 的 command 都有非空 `failureCodes[]`。
- 红灯缺口为 `ax`、`serve`、`version`。
- `version` 是无失败面的 bootstrap 命令，显式设置 `failureShape=nil`，避免继承默认失败 envelope。
- `serve` 增加 `failureShape` 与 `failureCodes=["validation_failed","server_start_failed"]`。
- `ax` 增加 embedded / host 统一失败 shape，并补齐 server、target、runtime、request、host command、artifact write 和 validation 相关 failure codes。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`，明确有失败面就必须有稳定错误码集合。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-24-failure-code-coverage.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaFailureSurfacesExposeStableFailureCodes`；`swift test --package-path CLI --filter SchemaFactSourceTests`，20 个 Swift Testing 用例通过；`swift test --package-path CLI`，91 个 Swift Testing 用例通过。
- 下一步进入 Round 25：检查 subcommand failure codes 与父命令 failure codes 的包含关系。

## Command surface optimization Round 25 subcommand failure code coverage

- 新增 `SchemaFactSourceTests.subcommandFailureCodesStayCoveredByParentSchemas`，要求所有子命令 `failureCodes[]` 都必须被父命令 `failureCodes[]` 覆盖。
- 聚焦测试直接通过，说明现有 subcommand failure code 没有逃逸父命令恢复码全集。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`，明确 agent 可先读父命令建立错误恢复全集，再由子命令收窄范围。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-25-subcommand-failure-code-coverage.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/subcommandFailureCodesStayCoveredByParentSchemas`；`swift test --package-path CLI --filter SchemaFactSourceTests`，21 个 Swift Testing 用例通过；`swift test --package-path CLI`，92 个 Swift Testing 用例通过。
- 下一步进入 Round 26：检查 schema options / subcommands 的字段质量和重复项。

## Command surface optimization Round 26 schema metadata quality

- 新增 `SchemaFactSourceTests.schemaOptionsAndSubcommandsExposeNonemptyMetadata`，要求 command `options[]` 的 `name`、`type`、`description` 非空，同一 command 内 option name 不重复。
- 同一测试要求 command `subcommands[]` 的 `name`、`summary` 非空，同一 command 内 subcommand name 不重复。
- 聚焦测试直接通过，说明现有 schema 参数与子命令元数据没有空字段或重复项。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`，明确 schema 参数/子命令不能是低信号占位。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-26-schema-metadata-quality.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaOptionsAndSubcommandsExposeNonemptyMetadata`；`swift test --package-path CLI --filter SchemaFactSourceTests`，22 个 Swift Testing 用例通过；`swift test --package-path CLI`，93 个 Swift Testing 用例通过。
- 下一步进入 Round 27：检查 `outputFormats[]` 与 examples 的最低质量。

## Command surface optimization Round 27 schema examples quality

- 新增 `SchemaFactSourceTests.schemaExamplesAndOutputFormatsRemainAgentUsable`，要求每个 command 有非空 `outputFormats[]` 与至少一个 example。
- 测试要求 example 中的 `triton` command、subcommand 和 `--flag` 都能被 schema 解释。
- 红灯暴露 `input` pipeline 示例无法被旧 helper 识别，已调整 helper 支持从示例 token 中定位第一个 `triton` 调用。
- 红灯同时暴露 `sim` 示例使用了 schema 未声明的 `--duration`、`--style`、`--confirm`、`--dry-run`；已补齐。
- 红灯同时暴露 `app` 示例使用了 schema 未声明的 `--user-only`、`--bundle`、`--ability`、`--confirm`、`--kind`；已补齐。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`，明确 examples 是 agent-facing argv 样本。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-27-schema-examples-quality.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaExamplesAndOutputFormatsRemainAgentUsable`；`swift test --package-path CLI --filter SchemaFactSourceTests`，23 个 Swift Testing 用例通过；`swift test --package-path CLI`，94 个 Swift Testing 用例通过。
- 下一步进入 Round 28：检查 agent-facing 示例与恢复建议的 JSON 输出口径是否一致。

## Command surface optimization Round 28 plan step contract

- 新增 `SchemaFactSourceTests.workflowPlanStepsExposeExecutableMetadata`，覆盖 server 不可达、server 可达但无 target、已连接 target，以及 `ios-smoke/open-url/webview-check` 任务 plan。
- 测试要求每个 plan step 的 `id`、`title`、`command`、`when`、`expected` 非空，同一 plan 内 step id 不重复，并且 `command` 能被 schema 解释。
- 红灯暴露通用 plan 的 `connect-target` step 仍是自然语言：`open the iOS app or run the simulator build that embeds TritonKit`。
- 修复为 `triton xcode run --json`，让 agent 能通过 schema / failure code 执行和恢复。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`，明确 plan steps 不能输出自然语言动作。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-28-plan-step-contract.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/workflowPlanStepsExposeExecutableMetadata`；`swift test --package-path CLI --filter SchemaFactSourceTests`，24 个 Swift Testing 用例通过；`swift test --package-path CLI`，95 个 Swift Testing 用例通过。
- 下一步进入 Round 29：检查 plan `nextStep` 是否与 `steps[].id` 对齐。

## Command surface optimization Round 29 plan nextStep alignment

- 新增 `SchemaFactSourceTests.workflowPlanNextStepPointsToAnEmittedStep`，要求 `plan.nextStep` 必须能在同一响应的 `steps[].id` 中找到。
- 红灯暴露已连接 runtime 的通用 plan 返回 `nextStep=observe`，但 steps 中没有 `id=observe`。
- 修复为 `nextStep=geometry`，指向首个实际观察步骤。
- 测试覆盖 server 不可达、server 可达但无 target、已连接 target，以及 `ios-smoke/open-url/webview-check` 三类任务 plan。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`，明确 agent 可直接用 nextStep 定位下一条步骤。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-29-plan-nextstep-alignment.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/workflowPlanNextStepPointsToAnEmittedStep`；`swift test --package-path CLI --filter SchemaFactSourceTests`，25 个 Swift Testing 用例通过；`swift test --package-path CLI`，96 个 Swift Testing 用例通过。
- 下一步进入 Round 30：抽取 workflow plan fixtures，减少 Round 18/28/29 测试重复构造。

## Command surface optimization Round 30 capability name uniqueness

- 新增 `SchemaFactSourceTests.capabilityNamesRemainUniqueForAgentIndexing`，要求单个 command 的 `providedCapabilities[]` 内部不能重复。
- 同一测试要求 connected 状态下 `runtimeCapabilities(...)` 输出的 `capabilities[].name` 不能重复。
- 聚焦测试直接通过，说明当前 schema 与 capabilities matrix 没有重复能力名。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`，明确能力名是 agent 索引 key。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-30-capability-name-uniqueness.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/capabilityNamesRemainUniqueForAgentIndexing`；`swift test --package-path CLI --filter SchemaFactSourceTests`，26 个 Swift Testing 用例通过；`swift test --package-path CLI`，97 个 Swift Testing 用例通过。
- 下一步进入 Round 31：检查 `capabilities[].requiredBy` 与 `evidence` 的数组质量。

## Command surface optimization Round 31 capability array quality

- 新增 `SchemaFactSourceTests.capabilityPlanningArraysExposeNonemptyUniqueValues`，要求 connected capabilities matrix 中每个 capability 的 `requiredBy` 和 `evidence` 不包含空字符串。
- 同一测试要求 `requiredBy` 与 `evidence` 在单个 capability 内不能重复。
- 聚焦测试直接通过，说明当前 capabilities matrix 的 workflow 分类和证据面数组质量稳定。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-31-capability-array-quality.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/capabilityPlanningArraysExposeNonemptyUniqueValues`；`swift test --package-path CLI --filter SchemaFactSourceTests`，27 个 Swift Testing 用例通过；`swift test --package-path CLI`，98 个 Swift Testing 用例通过。
- 下一步进入 Round 32：检查 `capabilities[].group` 是否落在固定允许集合。

## Command surface optimization Round 32 capability group taxonomy

- 新增 `SchemaFactSourceTests.capabilityGroupsStayWithinTheAgentTaxonomy`，要求 connected capabilities matrix 中每个 `capabilities[].group` 必须落在固定 agent taxonomy。
- 固定 taxonomy 为 `action`、`assert`、`bootstrap`、`evidence`、`host`、`observe`、`replay`、`route`、`runtime`、`smoke`、`target`、`webview`、`xcode`。
- 聚焦测试直接通过，说明当前能力矩阵没有临时分组、近义分组或 `misc` 分组。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-32-capability-group-taxonomy.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/capabilityGroupsStayWithinTheAgentTaxonomy`；`swift test --package-path CLI --filter SchemaFactSourceTests`，28 个 Swift Testing 用例通过；`swift test --package-path CLI`，99 个 Swift Testing 用例通过。
- 下一步进入 Round 33：检查 `capabilities[].requiredBy` 是否落在固定 workflow taxonomy，继续收紧 agent workflow 分类入口。

## Command surface optimization Round 33 requiredBy workflow taxonomy

- 新增 `SchemaFactSourceTests.capabilityRequiredByValuesStayWithinTheWorkflowTaxonomy`，要求 connected capabilities matrix 中每个 `capabilities[].requiredBy` 必须落在固定 workflow taxonomy。
- 固定 taxonomy 为 `action`、`app`、`assert`、`evidence`、`observe`、`project`、`replay`、`route`、`runtime`、`smoke`、`target`、`webview-check`、`xcode`。
- 聚焦测试直接通过，说明当前能力矩阵没有临时 workflow 分类。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-33-requiredby-workflow-taxonomy.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/capabilityRequiredByValuesStayWithinTheWorkflowTaxonomy`；`swift test --package-path CLI --filter SchemaFactSourceTests`，29 个 Swift Testing 用例通过；`swift test --package-path CLI`，100 个 Swift Testing 用例通过。
- 下一步进入 Round 34：检查 `capabilities[].evidence` 是否落在固定 artifact taxonomy，继续收紧 agent 证据选择入口。

## Command surface optimization Round 34 evidence artifact taxonomy

- 新增 `SchemaFactSourceTests.capabilityEvidenceValuesStayWithinTheArtifactTaxonomy`，要求 connected capabilities matrix 中每个已声明的 `capabilities[].evidence` 必须落在固定 artifact taxonomy。
- taxonomy 覆盖 stdout/schema/status、host target/artifact、runtime manifest/snapshot/AX/ledger、WebView provider/route/assertion、input/action result、evidence bundle、smoke summary、tritonplan、Xcode/xcresult/trace/coverage 和 unsupported envelope。
- 聚焦测试直接通过，说明当前能力矩阵没有临时 artifact 标签。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-34-evidence-artifact-taxonomy.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/capabilityEvidenceValuesStayWithinTheArtifactTaxonomy`；`swift test --package-path CLI --filter SchemaFactSourceTests`，30 个 Swift Testing 用例通过；`swift test --package-path CLI`，101 个 Swift Testing 用例通过。
- 下一步进入 Round 35：检查 `capabilities[].evidence` 是否应对所有 capability 非空，并补齐 inspect/hierarchy/attrs/export 等 observe 能力的证据面。

## Command surface optimization Round 35 capability evidence nonempty

- 新增 `SchemaFactSourceTests.capabilitiesExposeAtLeastOneEvidenceSource`，要求 connected capabilities matrix 中每个 capability 都至少暴露一个 `evidence` source。
- 红灯暴露 `attrs`、`export-archive`、`export-json`、`geometry`、`hierarchy`、`hit`、`inspect`、`list`、`nodes`、`object` 缺少 evidence。
- 在 `runtimeCapabilityEvidence(for:)` 中补齐 observe 细分能力的证据面：`list` 指向 `status-json/runtime-manifest`，`inspect/hierarchy/nodes` 指向 `surface-tree/runtime-ax`，`attrs/object` 指向 `hierarchy-node/surface-tree`，`export-*` 指向 `host-artifact` 等，`geometry` 指向 `snapshot-json`，`hit` 指向 `target.resolution/surface-tree`。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-35-capability-evidence-nonempty.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/capabilitiesExposeAtLeastOneEvidenceSource`；`swift test --package-path CLI --filter SchemaFactSourceTests`，31 个 Swift Testing 用例通过；`swift test --package-path CLI`，102 个 Swift Testing 用例通过。
- 下一步进入 Round 36：检查 capabilities 的 `nextAction.requiresLongRunningProcess` 语义，只允许真正长驻或长耗时恢复动作使用。

## Command surface optimization Round 36 long-running nextAction semantics

- 新增 `SchemaFactSourceTests.capabilityLongRunningNextActionsStayExplicit`，检查 server 不可达、server 可达但 runtime 未连接、server 与 runtime 均可用三种 capabilities 状态。
- 当前唯一允许的 long-running nextAction 是 server bootstrap：`serve --host 127.0.0.1 --port 19421`。
- 聚焦测试直接通过，说明当前 `requiresLongRunningProcess` 没有误标到普通 target、observe、action、assert、evidence 或 schema discovery 命令上。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-36-long-running-next-action.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/capabilityLongRunningNextActionsStayExplicit`；`swift test --package-path CLI --filter SchemaFactSourceTests`，32 个 Swift Testing 用例通过；`swift test --package-path CLI`，103 个 Swift Testing 用例通过。
- 下一步进入 Round 37：检查 capability `nextAction.args` 中占位符与 schema option 类型的关系，避免 `<placeholder>` 形式漂移。

## Command surface optimization Round 37 nextAction placeholder tokens

- 新增 `SchemaFactSourceTests.capabilityNextActionPlaceholdersAreCompleteArgvTokens`，检查 server 不可达、server 可达但 runtime 未连接、server 与 runtime 均可用三种 capabilities 状态。
- 测试要求 `capabilities[].nextAction.args` 中凡是包含 `<` 或 `>` 的参数，都必须是完整 argv token，不能是半截字符串拼接、不闭合尖括号或多个占位符拼在同一 token。
- 聚焦测试直接通过，说明当前 capabilities nextAction 占位符可被 agent 作为槽位替换。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-37-nextaction-placeholder-tokens.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/capabilityNextActionPlaceholdersAreCompleteArgvTokens`；`swift test --package-path CLI --filter SchemaFactSourceTests`，33 个 Swift Testing 用例通过；`swift test --package-path CLI`，104 个 Swift Testing 用例通过。
- 下一步进入 Round 38：把 placeholder token 校验扩展到 `schema.nextCommands[]` 和 `plan.steps[].command`。

## Command surface optimization Round 38 schema and plan placeholder tokens

- 新增 `SchemaFactSourceTests.schemaAndPlanPlaceholdersAreCompleteArgvTokens`，将 placeholder token 校验扩展到 `schema.nextCommands[]`、`schema.examples[]` 与 `plan.steps[].command`。
- 红灯暴露两个问题：`sim:<udid>` 把 placeholder 混进 target-id 字符串；通用 plan 的 `input` step 使用 shell 重定向 `< gestures.ndjson`。
- 修复为 `triton device use <sim-target-id> --json`，并将通用 plan 的 `input` step 改成纯 Triton argv `triton input --host ... --format json --summary --strict`，stdin NDJSON 要求放进 `expected`。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-38-schema-plan-placeholder-tokens.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaAndPlanPlaceholdersAreCompleteArgvTokens`；`swift test --package-path CLI --filter SchemaFactSourceTests`，34 个 Swift Testing 用例通过；`swift test --package-path CLI`，105 个 Swift Testing 用例通过。
- 下一步进入 Round 39：检查 plan step `command` 不包含 shell control operators，确保 plan 始终输出单条 Triton argv。

## Command surface optimization Round 39 plan single invocation commands

- 新增 `SchemaFactSourceTests.workflowPlanCommandsStaySingleTritonInvocations`，覆盖通用 plan 的 server 不可达、target/runtime 缺失、connected 三种状态，以及 `ios-smoke`、`open-url`、`webview-check` 三种任务 plan。
- 测试要求每个 `plan.steps[].command` 以 `triton` 开头，且不包含 `|`、`&&`、`||`、`;`、`<`、`>`、`>>`、`2>`、`2>>`、命令替换或反引号。
- 聚焦测试直接通过，说明 Round 38 去掉 shell 重定向后，当前 plan 已保持单条 Triton invocation。
- 更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-39-plan-single-invocation.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/workflowPlanCommandsStaySingleTritonInvocations`；`swift test --package-path CLI --filter SchemaFactSourceTests`，35 个 Swift Testing 用例通过；`swift test --package-path CLI`，106 个 Swift Testing 用例通过。
- 下一步进入 Round 40：抽取 workflow plan fixture builder，减少 Round 18/28/29/37/38/39 对同一批 plan 状态的重复构造。

## Command surface optimization Round 40 workflow plan test fixtures

- 新增 `workflowPlanFixtures(includeTaskInputs:)` 测试 helper，统一生成 server 不可达、target/runtime 缺失、connected、`ios-smoke`、`open-url`、`webview-check` 六类 plan。
- `includeTaskInputs=true` 保留已有具体输入值场景，`includeTaskInputs=false` 覆盖默认 placeholder 场景。
- 替换多处重复 plan 构造，行为不变。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-40-workflow-plan-fixtures.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`，35 个 Swift Testing 用例通过；`swift test --package-path CLI`，106 个 Swift Testing 用例通过。
- 下一步进入 Round 41：抽取 capabilities 三态 fixtures，继续降低全局不变量测试重复。

## Command surface optimization Round 41 capability state fixtures

- 新增 `capabilityStateFixtures()` 测试 helper，统一生成 `server-unreachable`、`runtime-disconnected`、`runtime-connected` 三类 capabilities matrix。
- 替换 `capabilityLongRunningNextActionsStayExplicit` 与 `capabilityNextActionPlaceholdersAreCompleteArgvTokens` 中重复的三态构造。
- 失败上下文增加 fixture 名称，后续定位更直接。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-41-capability-state-fixtures.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`，35 个 Swift Testing 用例通过；`swift test --package-path CLI`，106 个 Swift Testing 用例通过。
- 下一步进入 Round 42：将 connected-only capabilities 检查也统一走 helper，继续减少 `runtimeCapabilities(host:port:serverReachable:connected:)` 直接散落。

## Command surface optimization Round 42 connected capability fixtures

- 新增 `connectedCapabilities()` 与 `connectedCapabilityMap()` 测试 helper，复用 `capabilityStateFixtures()` 中的 `runtime-connected` 状态。
- 将 connected-only schema / capability 不变量检查统一改为 helper，包括 capability discoverability、name uniqueness、planning arrays、group / requiredBy / evidence taxonomy、evidence nonempty、planning metadata、nextAction schema alignment，以及 execution / evidence / webview / route provided capability subset 检查。
- 保留 doctor 与三态 capabilities matrix 测试中的显式状态构造，因为这些测试本身验证连接状态差异。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-42-connected-capability-fixtures.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`，35 个 Swift Testing 用例通过；`swift test --package-path CLI`，106 个 Swift Testing 用例通过。
- 下一步进入 Round 43：继续抽取 schema-backed command 校验或 placeholder / single invocation 相关 helper，降低 agent-facing schema 不变量测试维护成本。

## Command surface optimization Round 43 schema-backed command test helpers

- 新增 `commandSchemaMap()`，统一生成 command name 到 `TKCommandSchema` 的索引。
- 新增 `expectNoSchemaBackedCommandIssues()`，统一断言 schema-backed command 校验没有 unknown command、unknown subcommand 或 unknown flag。
- 替换 task workflow plan、workflow plan metadata、schema nextCommands、schema examples、capability nextAction 等测试里的重复 issue 断言。
- 同步替换多个 schema contract 测试中的重复 schema map 构造，测试语义保持不变。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-43-schema-command-test-helpers.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`，35 个 Swift Testing 用例通过；`swift test --package-path CLI`，106 个 Swift Testing 用例通过。
- 下一步进入 Round 44：继续治理 placeholder / single invocation 的 command string 收集逻辑，或补一个新的 agent-facing schema 不变量。

## Command surface optimization Round 44 output contract selector uniqueness

- 新增 `SchemaFactSourceTests.schemaOutputContractSelectorsRemainUniqueForAgentLookup`，要求同一 command 内的 `outputContracts[].selector` 必须唯一。
- 该约束用于保证 agent 可以用 selector 明确定位一个输出模型，避免自动解析器或回归断言遇到同命令内 selector 歧义。
- 当前 schema 直接满足该不变量，没有发现重复 selector。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-44-output-contract-selector-uniqueness.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaOutputContractSelectorsRemainUniqueForAgentLookup`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，36 个 Swift Testing 用例通过；`swift test --package-path CLI`，107 个 Swift Testing 用例通过。
- 下一步进入 Round 45：继续补齐 output contract selector 与 capability evidence / artifacts 的交叉一致性，或治理 command string 收集 helper。

## Command surface optimization Round 45 subcommand output selector coverage

- 新增 `SchemaFactSourceTests.subcommandOutputSelectorsStayCoveredByParentOutputContracts`，要求 `subcommands[].outputSelectors[]` 必须能被父命令 `outputContracts[].selector` 覆盖。
- 该约束用于保证 agent 可以先从子命令找到应该消费的输出 selector，再回到父命令 output contract 获取字段模型。
- 当前 schema 直接满足该不变量，没有发现缺失 selector。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-45-subcommand-output-selector-coverage.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/subcommandOutputSelectorsStayCoveredByParentOutputContracts`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，37 个 Swift Testing 用例通过；`swift test --package-path CLI`，108 个 Swift Testing 用例通过。
- 下一步进入 Round 46：继续补齐 subcommand `nextCommands[]` 与 schema-backed command 校验，或治理 command string 收集 helper。

## Command surface optimization Round 46 subcommand nextCommands schema alignment

- 新增 `SchemaFactSourceTests.subcommandNextCommandsStayAlignedWithCommandSchemas`，要求 `subcommands[].nextCommands[]` 中的 root command、subcommand 和 `--flag` 必须能被同一份 command schema 解释。
- 该约束用于保证子命令级恢复建议也是可执行的 schema-backed Triton 命令，而不是只在父命令 `nextCommands[]` 层面保持自洽。
- 当前 schema 直接满足该不变量，没有发现未声明命令、子命令或 flag。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-46-subcommand-nextcommands-schema-alignment.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/subcommandNextCommandsStayAlignedWithCommandSchemas`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，38 个 Swift Testing 用例通过；`swift test --package-path CLI`，109 个 Swift Testing 用例通过。
- 下一步进入 Round 47：治理 command string 收集 helper，避免 schema / subcommand / plan 三处命令抽取逻辑继续分散。

## Command surface optimization Round 47 command string fixtures

- 新增 `CommandStringFixture`、`schemaNextCommandFixtures(includeSubcommands:)`、`schemaExampleCommandFixtures()` 与 `workflowPlanCommandFixtures(includeTaskInputs:)`。
- 替换 schema nextCommands、subcommand nextCommands、schema examples、schema/plan placeholder 和 plan single invocation 测试中的重复 command string 收集逻辑。
- 测试语义保持不变，后续继续增加 command string 层不变量时可以复用统一 fixture。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-47-command-string-fixtures.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`，38 个 Swift Testing 用例通过；`swift test --package-path CLI`，109 个 Swift Testing 用例通过。
- 下一步进入 Round 48：继续补一个新的 command string 层不变量，或将 schema / plan command 校验上下文进一步统一。

## Command surface optimization Round 48 nextCommands single invocation contract

- 新增 `SchemaFactSourceTests.schemaNextCommandsStaySingleTritonInvocations`，要求命令级与子命令级 `nextCommands[]` 必须是单条 `triton ...` invocation。
- 该约束用于防止恢复建议依赖 shell 管道、重定向、命令替换或多命令拼接；stdin、文件输入和上下文要求应由契约字段或说明表达。
- 当前 schema 直接满足该不变量，没有发现复合 shell command。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-48-nextcommands-single-invocation.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaNextCommandsStaySingleTritonInvocations`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，39 个 Swift Testing 用例通过；`swift test --package-path CLI`，110 个 Swift Testing 用例通过。
- 下一步进入 Round 49：将 command string fixture helper 继续用于 examples 的 shell 边界，明确哪些 example 可以包含 pipeline，哪些恢复/plan 命令不能包含。

## Command surface optimization Round 49 example single Triton invocation

- 新增 `SchemaFactSourceTests.schemaExamplesContainOneTritonInvocationForAgentReuse`，要求每条 schema `examples[]` 恰好包含一个可抽取的 `triton` invocation。
- 该约束允许 `printf ... | triton input ...` 这类 stdin 准备或 shell pipeline，但禁止一条 example 中包含多个 `triton` 调用，避免 agent 把多步流程当成单步 argv 样本。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-49-example-single-triton-invocation.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaExamplesContainOneTritonInvocationForAgentReuse`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，40 个 Swift Testing 用例通过；`swift test --package-path CLI`，111 个 Swift Testing 用例通过。
- 下一步进入 Round 50：继续补一个 schema contract invariant，优先检查 `outputContracts[].format` 是否能收敛到固定输出格式 taxonomy，或继续细化 examples 的 shell 上下文分类。

## Command surface optimization Round 50 output contract format taxonomy

- 新增 `SchemaFactSourceTests.schemaOutputContractFormatsStayWithinAgentTaxonomy`，要求 `outputContracts[].format` 只能是 `json`、`jsonl` 或 `archive`。
- 该约束把单响应、流式事件和归档产物三类输出明确化，避免 schema 中出现 agent 无法预先选择解析策略的自由格式字符串。
- 当前 schema 直接满足该不变量，没有发现越界格式。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-50-output-contract-format-taxonomy.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaOutputContractFormatsStayWithinAgentTaxonomy`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，41 个 Swift Testing 用例通过；`swift test --package-path CLI`，112 个 Swift Testing 用例通过。
- 下一步进入 Round 51：继续检查 `outputContracts[].kind` taxonomy，或收敛 command-level `outputFormats[]` taxonomy。

## Command surface optimization Round 51 output contract kind taxonomy

- 新增 `SchemaFactSourceTests.schemaOutputContractKindsStayWithinAgentTaxonomy`，要求 `outputContracts[].kind` 必须落在显式允许集合内。
- 该约束把输出模型语义分类固定下来，覆盖 response envelope、event stream、artifact envelope、runtime / observe / action / assert / evidence / host / Xcode 等已存在输出面。
- 当前 schema 直接满足该不变量，没有发现越界 kind。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-51-output-contract-kind-taxonomy.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaOutputContractKindsStayWithinAgentTaxonomy`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，42 个 Swift Testing 用例通过；`swift test --package-path CLI`，113 个 Swift Testing 用例通过。
- 下一步进入 Round 52：继续收敛 command-level `outputFormats[]` taxonomy，或检查 output contract field `type` taxonomy。

## Command surface optimization Round 52 command output formats taxonomy

- 新增 `SchemaFactSourceTests.schemaOutputFormatsStayWithinCommandTaxonomy`，要求命令级 `outputFormats[]` 只能使用 `text`、`json`、`jsonl`、`logs`、`tree`、`auto`、`archive`、`file`、`json-metadata`，且同一命令内不能重复。
- 该约束区分命令可选输出模式和 `outputContracts[].format` 解析模型，避免 agent 需要临时猜测输出模式语义。
- 当前 schema 直接满足该不变量，没有发现越界或重复输出格式。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-52-command-output-formats-taxonomy.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaOutputFormatsStayWithinCommandTaxonomy`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，43 个 Swift Testing 用例通过；`swift test --package-path CLI`，114 个 Swift Testing 用例通过。
- 下一步进入 Round 53：检查 output contract field `type` taxonomy，或抽取 schema taxonomy helper，降低测试维护重复。

## Command surface optimization Round 53 output field type grammar

- 新增 `SchemaFactSourceTests.schemaOutputContractFieldTypesStayMachineReadable`，要求 output contract field `type` 使用机器可读类型语法。
- 初次运行聚焦测试失败，暴露现有 `hierarchy.info.colorAlias` 使用 `[String:String]` 字典类型；修正 helper 后支持标量/DTO、optional `?`、数组 `[Type]`、字典 `[Key:Value]` 和 union `TypeA|TypeB`。
- 当前 schema 通过该不变量，没有字段类型仍是自然语言自由文本。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-53-output-field-type-grammar.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaOutputContractFieldTypesStayMachineReadable`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，44 个 Swift Testing 用例通过；`swift test --package-path CLI`，115 个 Swift Testing 用例通过。
- 下一步进入 Round 54：抽取 schema taxonomy helper，减少 format/kind/evidence/outputFormats 等固定集合在测试内的分散维护。

## Command surface optimization Round 54 schema taxonomy helpers

- 新增 `capabilityGroupTaxonomy()`、`capabilityWorkflowTaxonomy()`、`capabilityEvidenceTaxonomy()`、`outputContractFormatTaxonomy()`、`outputContractKindTaxonomy()` 和 `commandOutputFormatTaxonomy()` 测试 helper。
- 替换 `SchemaFactSourceTests` 中 capability group / workflow / evidence、output contract format / kind、command output format 的 inline `Set<String>`。
- 测试语义保持不变，后续新增 taxonomy 值时有单一修改入口。
- 本轮不改变 CLI runtime、schema 数据或 public skill 契约。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-54-schema-taxonomy-helpers.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`，44 个 Swift Testing 用例通过；`swift test --package-path CLI`，115 个 Swift Testing 用例通过。
- 下一步进入 Round 55：继续抽取 schema issue collection helpers，或新增新的 agent-facing schema contract invariant。

## Command surface optimization Round 55 output model type grammar

- 新增 `SchemaFactSourceTests.schemaOutputContractModelsStayMachineReadable`，要求 `outputContracts[].model` 使用机器可读类型语法。
- 首次聚焦测试红灯暴露三个问题：`Dictionary<String, [HierarchyNodeSummary]>`、`[TKAXNode]|TKAXHierarchyMapResponse` 和 `embedded screenshot metadata dictionary`。
- 修正类型语法 helper，支持 union 中包含数组类型；将截图元数据 model 收敛为 `ScreenshotMetadataOutput`，将 hierarchy nodes map model 收敛为 `HierarchyNodeSummaryMap`。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-55-output-model-type-grammar.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaOutputContractModelsStayMachineReadable`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，45 个 Swift Testing 用例通过；`swift test --package-path CLI`，116 个 Swift Testing 用例通过。
- 下一步进入 Round 56：继续检查 selector / kind / model 命名风格一致性，或抽取 schema type grammar helper 的专门测试。

## Command surface optimization Round 56 output selector and kind keys

- 新增 `SchemaFactSourceTests.schemaOutputContractSelectorsAndKindsUseStableAgentKeys`，要求 `outputContracts[].selector` 使用点分层级 lower-kebab key，`outputContracts[].kind` 使用单段 lower-kebab key。
- 新增 `isAgentSelectorKey(_:)` 与 `isKebabCaseKey(_:)` helper。
- 当前 schema 直接满足该不变量，没有发现非法 selector 或 kind。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-56-output-selector-kind-keys.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaOutputContractSelectorsAndKindsUseStableAgentKeys`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，46 个 Swift Testing 用例通过；`swift test --package-path CLI`，117 个 Swift Testing 用例通过。
- 下一步进入 Round 57：检查 command/subcommand/option 名称风格，或抽取 schema key grammar helper 的专门测试。

## Command surface optimization Round 57 failure code key style

- 新增 `SchemaFactSourceTests.schemaFailureCodesUseStableSnakeCase`，要求 command 与 subcommand 的 `failureCodes[]` 全部使用 lower_snake_case，且同一命令面内不能重复。
- 新增 `isSnakeCaseKey(_:)` helper，和 selector / kind 的 lower-kebab helper 分开表达不同机器 key 语法。
- 当前 schema 直接满足该不变量，没有发现非法失败码或重复失败码。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-57-failure-code-key-style.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaFailureCodesUseStableSnakeCase`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，47 个 Swift Testing 用例通过；`swift test --package-path CLI`，118 个 Swift Testing 用例通过。
- 下一步进入 Round 58：检查 command / subcommand / option 名称风格，或把 key grammar helper 进一步整理成更明确的测试工具层。

## Command surface optimization Round 58 command key style

- 新增 `SchemaFactSourceTests.schemaCommandSubcommandAndFlagNamesUseStableCLIKeys`，要求 root command 与 subcommand 使用 lower-kebab key，纯长 flag 或 slash alias 组也使用 lower-kebab。
- 首次测试把所有 `options[].name` 都视为纯 option key，红灯暴露历史混用：`options[].name` 里仍有 `inspect <path>`、`device:alias set <name> ...`、`sim:runtime list|verify|...` 等 usage / synopsis 字符串。
- 将本轮测试收敛为只锁定可安全判断的 root/subcommand key 和以 `--` 开头的长 flag alias 组，并把 `options[].name` 语义拆分记录为后续治理点。
- 新增 `isLongOptionKeyExpression(_:)` helper。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-58-command-key-style.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaCommandSubcommandAndFlagNamesUseStableCLIKeys`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，48 个 Swift Testing 用例通过；`swift test --package-path CLI`，119 个 Swift Testing 用例通过。
- 下一步进入 Round 59：优先治理 `options[].name` 语义拆分，或先补一个不改变 wire model 的 synopsis 分类不变量。

## Command surface optimization Round 59 usage forms

- 新增 `TKCommandUsageForm` 与 `TKCommandSchema.usageForms` wire 字段，包含 `form`、`kind`、`description`。
- `TKCommandSchema` 初始化时自动把历史 `TKCommandSchemaOption.type == "Subcommand"` 或 `"Task"` 条目从 `options[]` 分离到 `usageForms[]`，保留纯 flags 在 `options[]`。
- `renderSchema` 文本输出新增 usage forms 分组。
- 新增 `SchemaFactSourceTests.schemaUsageFormsStaySeparateFromOptions`，要求 Subcommand / Task 不再出现在 `options[]`，且有 subcommands 的命令必须暴露 usage forms。
- 更新 `plan`、`device`、`sim`、`webview`、`xctrace`、`coverage`、`xcresult` 相关测试，从 `options[]` 改为读取 `usageForms[]`。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-59-usage-forms.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaUsageFormsStaySeparateFromOptions`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，49 个 Swift Testing 用例通过；`swift test --package-path CLI`，120 个 Swift Testing 用例通过。
- 下一步进入 Round 60：继续拆 positional argument，将 `<query>`、`<path>`、`<text>` 等从 `options[]` 移入 `argumentForms[]` 或等价字段。

## Command surface optimization Round 60 argument forms

- 新增 `TKCommandArgumentForm` 与 `TKCommandSchema.argumentForms` wire 字段，包含 `name`、`type`、`required`、`description`。
- `TKCommandSchema` 初始化时自动把 `name` 形如 `<...>` 的历史 option 条目从 `options[]` 分离到 `argumentForms[]`。
- `renderSchema` 文本输出新增 argument forms 分组。
- 新增 `SchemaFactSourceTests.schemaArgumentFormsStaySeparateFromOptions`。
- 收紧 `SchemaFactSourceTests.schemaCommandSubcommandAndFlagNamesUseStableCLIKeys`：拆分后 `options[]` 全部都必须是 `--long-flag` 或 slash alias 组。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-60-argument-forms.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaArgumentFormsStaySeparateFromOptions`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，50 个 Swift Testing 用例通过；`swift test --package-path CLI`，121 个 Swift Testing 用例通过。
- 下一步进入 Round 61：检查 `requiredOptions[]`、`optionalOptions[]`、`oneOfRequiredOptions[]` 是否只引用 schema 中可发现的 option / argument key。

## Command surface optimization Round 61 subcommand parameter reference coverage

- 新增 `SchemaFactSourceTests.subcommandParameterReferencesStayCoveredByParentSchema`，要求 `subcommands[].requiredOptions[]`、`optionalOptions[]` 与 `oneOfRequiredOptions[]` 只能引用父命令 `options[]` 或 `argumentForms[]` 中可发现的参数 key。
- 首次红灯暴露 `xcode discover:--path`、`xctrace record:--template`、`xctrace record:--output`、`coverage report:--xcresult`、`coverage report:--output` 未在父 schema 参数集合中声明。
- 修正 `CLISchemaXcodeCommands.swift`，补齐 `xcode` 的 `--path`、`xctrace` 的 `--template` / `--output`、`coverage` 的 `--xcresult` / `--output`。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-61-subcommand-parameter-reference-coverage.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/subcommandParameterReferencesStayCoveredByParentSchema`，1 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，51 个 Swift Testing 用例通过；`swift test --package-path CLI`，122 个 Swift Testing 用例通过。
- 下一步进入 Round 62：治理命令级 requirement 中的人读摘要，优先把 `requiredOptions[]` 里无法由 `options[]` / `argumentForms[]` 直接解释的默认来源、二选一组合或复合条件迁移到更机器可读的字段。

## Command surface optimization Round 62 command requiredOptions scope

- 新增 `SchemaFactSourceTests.commandLevelRequiredOptionsStayDirectOrSubcommandScoped`，要求带 `subcommands[]` 的父命令不得再用命令级 `requiredOptions[]` 聚合子命令需求；无子命令命令的 `requiredOptions[]` 只能引用自身 `options[]` 或 `argumentForms[]`。
- 红灯暴露 `xcode`、`xcresult`、`xctrace`、`coverage` 父级仍含 `use:--scheme`、`summary/failures:--path`、`record:--template`、`report:--xcresult` 等人读摘要。
- 清空这些父命令的命令级 `requiredOptions[]`，保留具体 `subcommands[].requiredOptions[]`、`oneOfRequiredOptions[]` 与 `optionalOptions[]` 作为机器可读事实源。
- 同步更新 `SimulatorAdvancedControlsTests`、`docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-62-command-required-options-scope.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/commandLevelRequiredOptionsStayDirectOrSubcommandScoped`；`swift test --package-path CLI --filter SimulatorAdvancedControlsTests/schemaExposesXctraceAndCoverageCommands`；`swift test --package-path CLI --filter SchemaFactSourceTests`，52 个 Swift Testing 用例通过；`swift test --package-path CLI`，123 个 Swift Testing 用例通过。
- 下一步进入 Round 63：检查 `defaultProviders[]` / `inheritsDefaultsFrom[]` 是否都能回到 schema-backed Triton 命令。

## Command surface optimization Round 63 default provider references

- 新增 `SchemaFactSourceTests.schemaDefaultProviderReferencesStaySchemaBacked`，覆盖 command 级 `inheritsDefaultsFrom[]` 与 subcommand 级 `defaultProviders[]` / `inheritsDefaultsFrom[]`。
- 复用 schema-backed command 校验 helper，要求默认来源引用必须是可被 `commandSchemas()` 解释的 `triton ...` 命令。
- 当前 schema 直接满足该不变量；本轮不修改 schema 数据或 runtime 行为。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-63-default-provider-references.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaDefaultProviderReferencesStaySchemaBacked`；`swift test --package-path CLI --filter SchemaFactSourceTests`，53 个 Swift Testing 用例通过；`swift test --package-path CLI`，124 个 Swift Testing 用例通过。
- 下一步进入 Round 64：检查 schema `artifacts[]` 与 subcommand artifacts 是否落在固定 taxonomy。

## Command surface optimization Round 64 schema artifact taxonomy

- 新增 `SchemaFactSourceTests.schemaArtifactsStayWithinTheArtifactTaxonomy`，覆盖 command 级与 subcommand 级 `artifacts[]`。
- 新增 `schemaArtifactTaxonomy()` helper，锁定当前 schema artifact 名称集合，并要求同一层级内不重复。
- 当前 schema 直接满足该不变量；本轮不修改 schema 数据或 runtime 行为。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-64-schema-artifact-taxonomy.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaArtifactsStayWithinTheArtifactTaxonomy`；`swift test --package-path CLI --filter SchemaFactSourceTests`，54 个 Swift Testing 用例通过；`swift test --package-path CLI`，125 个 Swift Testing 用例通过。
- 下一步进入 Round 65：检查 `jsonlEvents[]` / `finalEventKind` 的命名和覆盖关系。

## Command surface optimization Round 65 JSONL event contract

- 新增 `SchemaFactSourceTests.schemaJSONLEventsExposeStableEventKeys`，覆盖 command 级与 subcommand 级 `jsonlEvents[]` / `finalEventKind`。
- 新增 `isAgentEventKey(_:allowingPlaceholders:)` helper。command 级 event key 允许完整 token 占位符，例如 `xcode.<action>.summary`；subcommand 级 event key 必须使用具体 action。
- 测试要求 `jsonlEvents[]` 不重复、event key 稳定、`finalEventKind` 出现在同层 `jsonlEvents[]` 中，并且声明 JSONL event 的 command 暴露 `jsonl` output format。
- 当前 schema 直接满足该不变量；本轮不修改 schema 数据或 runtime 行为。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-65-jsonl-event-contract.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaJSONLEventsExposeStableEventKeys`；`swift test --package-path CLI --filter SchemaFactSourceTests`，55 个 Swift Testing 用例通过；`swift test --package-path CLI`，126 个 Swift Testing 用例通过。
- 下一步进入 Round 66：检查 `retryable` 与 failure code / next command 的关系。

## Command surface optimization Round 66 retryable recovery commands

- 新增 `SchemaFactSourceTests.retryableSchemasExposeRecoveryCommands`，要求 command 级和 subcommand 级 `retryable=true` 时必须暴露非空 `nextCommands[]`。
- 首次红灯暴露 `xcode discover/use/schemes/status/wait-idle/settings`、`xctrace` / `xctrace record`、`coverage` / `coverage report` 缺少恢复建议。
- 补齐 Xcode 子命令 next commands：discover -> use，use -> schemes/settings，schemes -> use，status -> wait-idle，wait-idle -> status，settings -> build/run。
- 补齐 `xctrace` 与 `coverage` command/subcommand 的 evidence 归档建议。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-66-retryable-recovery-commands.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/retryableSchemasExposeRecoveryCommands`；`swift test --package-path CLI --filter SchemaFactSourceTests/subcommandNextCommandsStayAlignedWithCommandSchemas`；`swift test --package-path CLI --filter SchemaFactSourceTests/schemaNextCommandsStayAlignedWithCommandSchemas`；`swift test --package-path CLI --filter SchemaFactSourceTests`，56 个 Swift Testing 用例通过；`swift test --package-path CLI`，127 个 Swift Testing 用例通过。
- 下一步进入 Round 67：检查 failure code 与恢复建议的语义覆盖。

## Command surface optimization Round 67 failure code recovery path

- 新增 `SchemaFactSourceTests.failureCodesExposeARecoveryCommandPath`，要求有 `failureCodes[]` 的 command 必须有命令级 `nextCommands[]`，有 `failureCodes[]` 的 subcommand 若自身没有 `nextCommands[]`，父命令必须提供恢复路径。
- 首次红灯暴露 `serve`、`ax`、`swipe`、`type`、`paste`、`clear`、`press` 有失败码但缺少恢复建议。
- 补齐 `serve` 的 `status` / `doctor` / `capabilities` 恢复建议，补齐 `ax` 的 `observe current` / `screenshot` / `evidence` 恢复建议，补齐动作命令的 `status` / `ax` / `evidence` 或 `schema --command press` 恢复建议。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-67-failure-code-recovery-path.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/failureCodesExposeARecoveryCommandPath`；`swift test --package-path CLI --filter SchemaFactSourceTests/schemaNextCommandsStayAlignedWithCommandSchemas`；`swift test --package-path CLI --filter SchemaFactSourceTests`，57 个 Swift Testing 用例通过；`swift test --package-path CLI`，128 个 Swift Testing 用例通过。
- 下一步进入 Round 68：检查每个 failure code 与恢复建议之间的语义密度，或先抽取 recovery taxonomy/helper，降低后续测试复杂度。

## Command surface optimization Round 68 recovery command list quality

- 新增 `SchemaFactSourceTests.schemaRecoveryCommandListsStayClean`，覆盖 command 级与 subcommand 级 `nextCommands[]`。
- 测试要求同一层级的恢复建议不能包含空字符串，不能重复，避免 agent 在恢复候选动作上额外做过滤和去重。
- 当前 schema 直接满足该不变量；本轮不修改 schema 数据或 runtime 行为。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-68-recovery-command-list-quality.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaRecoveryCommandListsStayClean`；`swift test --package-path CLI --filter SchemaFactSourceTests`，58 个 Swift Testing 用例通过；`swift test --package-path CLI`，129 个 Swift Testing 用例通过。
- 下一步进入 Round 69：抽取 recovery command taxonomy，或建立 failure code 到 recovery category 的轻量映射规则。

## Command surface optimization Round 69 recovery command root taxonomy

- 新增 `SchemaFactSourceTests.schemaRecoveryCommandRootsStayWithinRecoveryTaxonomy`，覆盖 command 级与 subcommand 级 `nextCommands[]`。
- 新增 `recoveryCommandRootTaxonomy()`，固定可作为恢复建议根命令的集合，避免 schema-backed 但恢复角色未定义的 root command 被随手放入恢复建议。
- 新增 `tritonRootCommand(in:)` helper，从单条 `triton ...` invocation 中提取 root command。
- 当前 schema 直接满足该不变量；本轮不修改 schema 数据或 runtime 行为。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-69-recovery-command-root-taxonomy.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaRecoveryCommandRootsStayWithinRecoveryTaxonomy`；`swift test --package-path CLI --filter SchemaFactSourceTests`，59 个 Swift Testing 用例通过；`swift test --package-path CLI`，130 个 Swift Testing 用例通过。
- 下一步进入 Round 70：继续把 recovery taxonomy 拆成轻量 category，例如 `diagnose`、`discover`、`prepare-target`、`observe`、`verify`、`archive`，再逐步映射到 failure code 类型。

## Command surface optimization Round 70 recovery command categories

- 新增 `SchemaFactSourceTests.schemaRecoveryCommandRootsExposeStableCategories`，要求 recovery root taxonomy 和 category map 一一覆盖。
- 新增 `recoveryCommandCategoryTaxonomy()`，固定 `diagnose`、`discover`、`prepare-target`、`project`、`observe`、`act`、`verify`、`archive`、`replay`、`smoke`、`plan`。
- 新增 `recoveryCommandRootCategoryMap()`，为每个 recovery root command 绑定稳定恢复阶段。
- 当前 schema 直接满足该不变量；本轮不修改 schema 数据或 runtime 行为。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-70-recovery-command-categories.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaRecoveryCommandRootsExposeStableCategories`；`swift test --package-path CLI --filter SchemaFactSourceTests`，60 个 Swift Testing 用例通过；`swift test --package-path CLI`，131 个 Swift Testing 用例通过。
- 下一步进入 Round 71：评估是否把 recovery category 暴露到 schema wire model，或先建立 failure code family 到 recovery category 的测试映射。

## Command surface optimization Round 71 recovery commands wire model

- 新增 `TKCommandRecoveryCommand`，字段为 `command` 与 `category`。
- `TKCommandSchema` 与 `TKCommandSubcommandSchema` 新增 `recoveryCommands[]`，默认由 `nextCommands[]` 自动派生。
- 新增 `TKCommandRecoveryCommand.rootCommand(in:)`、`category(forRootCommand:)`、`rootCommandTaxonomy` 与 `categoryTaxonomy`，把 Round 70 的恢复分类推进到 wire model 可复用层。
- 新增 `SchemaFactSourceTests.schemaRecoveryCommandsMirrorNextCommandsAndExposeCategories`，要求 command 级和 subcommand 级 `recoveryCommands[].command` 完全镜像 `nextCommands[]`，且 category 与 root command taxonomy 对齐。
- 直接验证 `triton schema --command status --json` 已输出 `recoveryCommands[]`，包含 `diagnose` 与 `discover` category。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-71-recovery-commands-wire-model.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaRecoveryCommandsMirrorNextCommandsAndExposeCategories`；`swift test --package-path CLI --filter SchemaFactSourceTests`，61 个 Swift Testing 用例通过；`swift test --package-path CLI`，132 个 Swift Testing 用例通过；`swift run --package-path CLI triton schema --command status --json`。
- 下一步进入 Round 72：建立 failure code family 到 recovery category 的测试映射，让 agent 能从 `error.code` 自动选择优先恢复阶段。

## Command surface optimization Round 72 failure code recovery category families

- 新增 `SchemaFactSourceTests.schemaFailureCodesMapToRecoveryCategoryFamilies`，要求 command 与 subcommand 的 `failureCodes[]` 都能映射到至少一个稳定 recovery category family，且 category 必须属于 `TKCommandRecoveryCommand.categoryTaxonomy`。
- 首次红灯暴露大量未分类错误码，包括 `target_offline`、`host_command_timeout`、`app_path_unresolved`、`bundle_id_unresolved`、`runtime_not_connected`、`artifact_output_rejected`、`webview_wait_timeout`、`javascript_error` 等。
- 本轮将测试口径收敛为“failure code family 可分类”，不强制每个命令现有 `recoveryCommands[]` 立即覆盖对应 family 的所有 category，避免一次性扩大所有 schema `nextCommands[]`。
- 补齐 failure code family 映射规则：ambiguous / invalid / missing / not_found / unavailable / timeout / unresolved / failed / mismatch / unsupported / required / rejected / interrupted / changed 等命名族均可映射到诊断、发现、目标准备、工程、观察、动作、验证或归档阶段。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-72-failure-code-recovery-category-families.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaFailureCodesMapToRecoveryCategoryFamilies`；`swift test --package-path CLI --filter SchemaFactSourceTests`，62 个 Swift Testing 用例通过；`swift test --package-path CLI`，133 个 Swift Testing 用例通过。
- 下一步进入 Round 73：逐步检查具体命令的 `recoveryCommands[]` 是否覆盖其高优先级 failure family，而不是一次性要求所有 family 全覆盖。

## Command surface optimization Round 73 artifact failure archive recovery

- 新增 `SchemaFactSourceTests.artifactFailureCodesExposeArchiveRecoveryCategories`，要求 `artifact_output_rejected`、`artifact_write_failed`、`file_write_failed`、`overwrite_refused`、`xcresult_output_too_large` 这类 artifact / output 失败码必须暴露 `archive` category 的恢复入口。
- 初始更宽的高优先级 failure recovery 覆盖红灯过大，会牵动大量 target/server/runtime 命令恢复建议；本轮收窄到 artifact / output 失败族，先处理语义最明确、修复面最小的恢复类别。
- 红灯暴露 `sim:artifact_output_rejected` 只有 `prepare-target|smoke` 恢复类别，`record:file_write_failed` 只有 `diagnose|plan|replay` 恢复类别。
- 补齐 `sim` 与 `record` 的 `triton evidence --output <dir.tritonevidence> --json` 恢复建议，使对应 failure code 能导向 evidence/archive 路径。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-73-artifact-failure-archive-recovery.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/artifactFailureCodesExposeArchiveRecoveryCategories`；`swift test --package-path CLI --filter SchemaFactSourceTests`，63 个 Swift Testing 用例通过；`swift test --package-path CLI`，134 个 Swift Testing 用例通过。
- 下一步进入 Round 74：按单一 failure family 继续收紧，例如 assertion / route mismatch 必须导向 `verify`，或 server/runtime transport 失败必须导向 `diagnose`。

## Command surface optimization Round 74 assertion failure verify recovery

- 新增 `SchemaFactSourceTests.assertionFailureCodesExposeVerifyRecoveryCategories`，要求 `assertion_failed`、`route_mismatch`、`text_not_found` 必须暴露 `verify` category 的恢复入口。
- 红灯暴露 `focus`、`select-segment`、`set-switch`、`route`、`smoke`、`assert`、`find` 的 assertion / route / text-not-found 失败面缺少 verify 类恢复建议。
- 补齐 semantic action 命令的 `triton wait --text <selector> --json`；补齐 `route` 的 `triton route assert-current-url <expected-url> --json`；补齐 `smoke` 的 `triton assert text-exists <text> --json`；补齐 `assert` 和 `find` 的 wait 验证建议。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-74-assertion-failure-verify-recovery.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/assertionFailureCodesExposeVerifyRecoveryCategories`；`swift test --package-path CLI --filter SchemaFactSourceTests`，64 个 Swift Testing 用例通过；`swift test --package-path CLI`，135 个 Swift Testing 用例通过。
- 下一步进入 Round 75：按单一 failure family 收紧 server/runtime transport 失败必须导向 `diagnose`，或 target failure 必须导向 `prepare-target`。

## Command surface optimization Round 75 runtime transport diagnose recovery

- 新增 `SchemaFactSourceTests.runtimeTransportFailureCodesExposeDiagnoseRecoveryCategories`，要求 `server_unavailable`、`request_failed`、`request_timeout`、`runtime_unavailable`、`runtime_not_connected` 必须暴露 `diagnose` category 的恢复入口。
- 红灯集中在 runtime / observe / action / assert / wait / evidence 等命令面：这些命令有 server/runtime/request 失败码，但恢复建议多为 observe、archive、verify 或 action，缺少回到状态诊断的入口。
- 补齐相关命令的 `triton status --json` 恢复建议，使 agent 在 transport 不可靠时能先诊断 server、target、runtime、hierarchy/cache 状态，再继续观察、动作、验证或归档。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-75-runtime-transport-diagnose-recovery.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/runtimeTransportFailureCodesExposeDiagnoseRecoveryCategories`；`swift test --package-path CLI --filter SchemaFactSourceTests`，65 个 Swift Testing 用例通过；`swift test --package-path CLI`，136 个 Swift Testing 用例通过。
- 下一步进入 Round 76：按单一 failure family 收紧 target failure 必须导向 `prepare-target`。

## Command surface optimization Round 76 target failure prepare-target recovery

- 新增 `SchemaFactSourceTests.targetFailureCodesExposePrepareTargetRecoveryCategories`，要求 `ambiguous_target`、`device_not_ready`、`simulator_not_found`、`target_not_found`、`target_offline`、`target_unavailable` 必须暴露 `prepare-target` category 的恢复入口。
- 红灯暴露 bootstrap、Xcode、runtime、observe、action、assert、evidence、smoke、replay 等命令面已有 diagnose / observe / verify / archive 恢复建议，但 target failure 缺少回到 target resolver 的统一入口。
- 在 `commandSchemas()` fact source 层新增 target failure recovery enrichment：声明上述失败码的 command / subcommand 自动补齐 `triton target resolve <selector> --json`，已有同名建议时去重，并继续让 `recoveryCommands[]` 从最终 `nextCommands[]` 派生。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-76-target-failure-prepare-target-recovery.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/targetFailureCodesExposePrepareTargetRecoveryCategories`；`swift test --package-path CLI --filter SchemaFactSourceTests`，66 个 Swift Testing 用例通过；`swift test --package-path CLI`，137 个 Swift Testing 用例通过。
- 下一步进入 Round 77：继续按单一 failure family 收紧项目/Xcode failure 必须导向 `project`，或检查 recoveryCommands 与 capabilities/plan 的恢复分类一致性。

## Command surface optimization Round 77 Project / Xcode failure project recovery

- 新增 `SchemaFactSourceTests.projectFailureCodesExposeProjectRecoveryCategories`，要求 `ambiguous_workspace`、`invalid_workspace_path`、`scheme_not_found`、`workspace_not_found`、`xcode_not_idle` 必须暴露 `project` category 的恢复入口。
- 红灯集中在 `xcode` 根命令和 `xcode test/run` 子命令：已有 archive / diagnose / verify / target 恢复建议，但 workspace、scheme 和 Xcode idle 类失败缺少回到工程发现或默认值配置的入口。
- 将 Round 76 的集中式 schema enrichment 扩展为 failure family recovery enrichment：target failure 自动补 `triton target resolve <selector> --json`，Project / Xcode failure 自动补 `triton xcode discover --path . --json`，统一去重并继续派生 `recoveryCommands[]`。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-77-project-failure-project-recovery.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/projectFailureCodesExposeProjectRecoveryCategories`；`swift test --package-path CLI --filter SchemaFactSourceTests`，67 个 Swift Testing 用例通过；`swift test --package-path CLI`，138 个 Swift Testing 用例通过。
- 下一步进入 Round 78：检查 action / step failure 是否必须导向 `act`，或检查 recovery family enrichment 是否需要覆盖 capabilities / plan 的同类恢复分类。

## Command surface optimization Round 78 action failure act recovery

- 新增 `SchemaFactSourceTests.actionFailureCodesExposeActRecoveryCategories`，要求 `action_failed`、`step_failed` 必须暴露 `act` category 的恢复入口。
- 红灯集中在 `replay:step_failed` 与 `input:action_failed`：已有 archive / diagnose / plan / target / verify 恢复建议，但缺少回到可执行动作面的入口。
- 将 failure family recovery enrichment 扩展到 action / step failure：声明上述失败码的 command / subcommand 自动补齐 `triton input --json --summary --strict`，统一去重并继续派生 `recoveryCommands[]`。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-78-action-failure-act-recovery.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/actionFailureCodesExposeActRecoveryCategories`；`swift test --package-path CLI --filter SchemaFactSourceTests`，68 个 Swift Testing 用例通过；`swift test --package-path CLI`，139 个 Swift Testing 用例通过。
- 下一步进入 Round 79：检查 destructive / confirmation failure 是否必须导向 `plan` 或 `diagnose`，或检查 capabilities / plan 与 `recoveryCommands[]` 的分类一致性。

## Command surface optimization Round 79 destructive policy plan recovery

- 新增 `SchemaFactSourceTests.destructivePolicyFailureCodesExposePlanRecoveryCategories`，要求 `confirmation_required`、`destructive_action_requires_policy` 必须暴露 `plan` category 的恢复入口。
- 红灯集中在 `sim:confirmation_required` 与 `app:destructive_action_requires_policy`：已有 target / archive / smoke / diagnose / verify 恢复建议，但缺少回到规划与策略确认的入口。
- 将 failure family recovery enrichment 扩展到 destructive / confirmation failure：声明上述失败码的 command / subcommand 自动补齐 `triton plan --format json`，统一去重并继续派生 `recoveryCommands[]`。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-79-destructive-policy-plan-recovery.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/destructivePolicyFailureCodesExposePlanRecoveryCategories`；`swift test --package-path CLI --filter SchemaFactSourceTests`，69 个 Swift Testing 用例通过；`swift test --package-path CLI`，140 个 Swift Testing 用例通过。
- 下一步进入 Round 80：检查 JavaScript / WebView failure 是否必须导向 `observe` 或 `diagnose`，或检查 capabilities / plan 与 `recoveryCommands[]` 的分类一致性。

## Command surface optimization Round 80 unsupported failure plan recovery

- 新增 `SchemaFactSourceTests.unsupportedFailureCodesExposePlanRecoveryCategories`，要求 `action_not_supported`、`unsupported_capability`、`unsupported_runtime_scope`、`webview_method_not_allowed`、`webview_wait_unsupported` 必须暴露 `plan` category 的恢复入口。
- 红灯集中在 focus / set-text / select-segment / set-switch / webview / wait / tap / swipe / type / paste / clear / press 等命令面：这些命令能诊断、观察、验证或动作，但 unsupported 类失败缺少回到能力边界规划的统一入口。
- 将 failure family recovery enrichment 扩展到 unsupported failure：声明上述失败码的 command / subcommand 自动补齐 `triton plan --format json`，统一去重并继续派生 `recoveryCommands[]`。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-80-unsupported-failure-plan-recovery.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/unsupportedFailureCodesExposePlanRecoveryCategories`；`swift test --package-path CLI --filter SchemaFactSourceTests`，70 个 Swift Testing 用例通过；`swift test --package-path CLI`，141 个 Swift Testing 用例通过。
- 下一步进入 Round 81：检查 `recoveryCommands[]` 与 `capabilities[].nextAction` / `plan.steps[]` 的恢复分类一致性，或继续按剩余高优先级 failure family 收紧命令级 category 覆盖。

## Command surface optimization Round 81 plan step category

- 新增 `SchemaFactSourceTests.workflowPlanStepsExposeStableRecoveryCategories`，要求 `plan.steps[].category` 非空、落在 `TKCommandRecoveryCommand.categoryTaxonomy`，并且与 `steps[].command` 的 root command category 一致。
- 初始红灯为 `TKWorkflowPlanStep` 缺少 `category` 字段。
- `TKWorkflowPlanStep` 新增 `category` wire 字段，默认由 `TKCommandRecoveryCommand.rootCommand(in:)` 和 `category(forRootCommand:)` 自动派生；decoder 在旧 JSON 缺失 category 时也按 command 派生。
- `plan.next-steps` output contract 新增 `steps[].id`、`steps[].command` 与 `steps[].category` 字段说明，避免 wire model 新字段没有 schema contract。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-81-plan-step-category.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/workflowPlanStepsExposeStableRecoveryCategories`；`swift test --package-path CLI --filter SchemaFactSourceTests/agentBootstrapSchemasExposeRecoveryCommandsAndOutputContracts`；`swift test --package-path CLI --filter SchemaFactSourceTests`，71 个 Swift Testing 用例通过；`swift test --package-path CLI`，142 个 Swift Testing 用例通过。
- 下一步进入 Round 82：检查 `capabilities[].nextAction` 是否也需要 category，或继续按剩余高优先级 failure family 收紧命令级 recovery category 覆盖。

## Command surface optimization Round 82 capability nextAction category

- 新增 `SchemaFactSourceTests.capabilityNextActionsExposeStableRecoveryCategories`，要求 `capabilities[].nextAction.category` 非空、落在 `TKCommandRecoveryCommand.categoryTaxonomy`，并且与 `nextAction.command` 的 root command category 一致。
- 初始红灯为 `TKCLINextAction` 缺少 `category` 字段。
- `TKCLINextAction` 新增 `category` wire 字段，默认由 `TKCommandRecoveryCommand.category(forRootCommand:)` 从 `command` 自动派生；decoder 在旧 JSON 缺失 category 时也按 command 派生。
- `capabilities` output contract 新增 `capabilities[].nextAction.category` 字段说明，避免 capabilities matrix 新字段没有 schema contract。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-82-capability-nextaction-category.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/capabilityNextActionsExposeStableRecoveryCategories`；`swift test --package-path CLI --filter SchemaFactSourceTests/agentBootstrapSchemasExposeRecoveryCommandsAndOutputContracts`；`swift test --package-path CLI --filter SchemaFactSourceTests`，72 个 Swift Testing 用例通过；`swift test --package-path CLI`，143 个 Swift Testing 用例通过。
- 下一步进入 Round 83：检查 `doctor.checks[].nextAction` 是否也需要 output contract 中显式暴露 `category`，或继续按剩余高优先级 failure family 收紧命令级 recovery category 覆盖。

## Command surface optimization Round 83 doctor nextAction category contract

- 在 `SchemaFactSourceTests.agentBootstrapSchemasExposeRecoveryCommandsAndOutputContracts` 中新增 `checks[].nextAction.category` contract 断言，要求 doctor output contract 显式暴露 nextAction category。
- 在 `SchemaFactSourceTests.doctorResponseExposesOrderedRecoveryChecks` 中新增 server bootstrap check 的 `nextAction.category == diagnose` 断言，证明 doctor runtime 输出和 category 派生一致。
- 初始红灯为 `doctor` output contract 缺少 `checks[].nextAction.category` 字段；补齐后聚焦测试通过。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-83-doctor-nextaction-category-contract.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/agentBootstrapSchemasExposeRecoveryCommandsAndOutputContracts`；`swift test --package-path CLI --filter SchemaFactSourceTests`，72 个 Swift Testing 用例通过；`swift test --package-path CLI`，143 个 Swift Testing 用例通过。
- 下一步进入 Round 84：检查 `TKCLIErrorDetail.nextAction` 是否也需要在错误 output contract / failure shape 中显式说明 category，或继续按剩余高优先级 failure family 收紧命令级 recovery category 覆盖。

## Command surface optimization Round 84 error nextAction category contract

- 新增 `SchemaFactSourceTests.schemaFailureShapesDescribeNextActionCategory`，要求所有声明 `nextAction?` 的 command-level `failureShape` 同时说明 nextAction 的 category 结构。
- 新增 `SchemaFactSourceTests.errorOutputContractsExposeNextActionCategory`，要求所有声明 `error: TKCLIErrorDetail?` 的 output contract 同步暴露 `error.nextAction.category`。
- 初始红灯显示 `status/doctor/capabilities/plan` 以及 observe/action/runtime 等 schema failure shape 仍只写 `nextAction?`，且 doctor、plan、capabilities、target readiness、semantic action 等 `TKCLIErrorDetail?` output contract 缺少 `error.nextAction.category`。
- `TKCommandSchema` 默认 failure shape 更新为 `nextAction?{ command,args,category,requiresLongRunningProcess? }`，初始化/解码时会标准化旧式 `nextAction?` 文案。
- `schemaContractFields` 对 `TKCLIErrorDetail?` 字段自动补充 `error.nextAction` 与 `error.nextAction.category`，避免每个 output contract 重复维护通用错误 envelope 字段。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260529-round-84-error-nextaction-category-contract.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaFailureShapesDescribeNextActionCategory`；`swift test --package-path CLI --filter SchemaFactSourceTests/errorOutputContractsExposeNextActionCategory`；`swift test --package-path CLI --filter SchemaFactSourceTests`，74 个 Swift Testing 用例通过；`swift test --package-path CLI`，145 个 Swift Testing 用例通过。
- 下一步进入 Round 85：继续检查剩余 error family 的 recovery category 覆盖，或检查单命令 schema 输出是否需要额外暴露 error envelope field taxonomy。
