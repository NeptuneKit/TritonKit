# 2026-05-31

## Command surface optimization Round 85 plan step execution metadata

- 继续执行方案 C：把 Triton CLI 改造成 agent 可自发现、可规划、可执行、可诊断的机器可读控制界面。
- Round 85 聚焦 `triton plan --json`：`TKWorkflowPlanStep` 新增 `requires`、`expectedArtifacts` 与 `stopConditions` wire 字段，让计划步骤不再只依赖 `when/expected` 人读文本。
- 新字段默认派生规则：
  - `requires` 至少包含 `cli.available`，并按 `requiresServer/requiresTarget` 补充 `server.reachable`、`target.ready`、`runtime.connected`。
  - `expectedArtifacts` 至少包含 `stdout-json`，并按命令 root 补充 `target-resolution`、`wait-result`、`assertion-result`、`evidence-bundle`、`smoke-summary`、`xcode-log` 等。
  - `stopConditions` 至少包含 `command.failed`，并按 server/target 和命令 root 补充 `server.unavailable`、`target.unavailable`、`timeout`、`assertion.failed`、`artifact.write-failed` 或 `step.failed`。
- `plan.next-steps` output contract 新增 `steps[].requires`、`steps[].expectedArtifacts` 与 `steps[].stopConditions`；decoder 对旧 JSON 缺字段时自动派生默认值。
- 新增 `SchemaFactSourceTests.workflowPlanStepsExposeStructuredExecutionMetadata`，要求所有 plan fixture step 都暴露非空结构化执行元数据，且 key 使用稳定点分 / kebab 语法。
- 共享模型测试补充 plan JSON roundtrip 后的新字段断言。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-85-plan-step-execution-metadata.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/workflowPlanStepsExposeStructuredExecutionMetadata`；`swift test --package-path CLI --filter SchemaFactSourceTests/agentBootstrapSchemasExposeRecoveryCommandsAndOutputContracts`；`swift test --filter TKCLITransportModelsTests/workflowPlanShape`；`swift test --package-path CLI --filter SchemaFactSourceTests`，75 个 Swift Testing 用例通过；`swift test --package-path CLI`，146 个 Swift Testing 用例通过；`swift test`，126 个 Swift Testing 用例通过；`swift run --package-path CLI triton plan --json` 输出包含 `steps[].requires`、`steps[].expectedArtifacts`、`steps[].stopConditions`。
- 执行中误把两个 SwiftPM 聚焦测试并行启动；两者均完成并通过。后续继续遵守 SwiftPM / 文档记录 / check-docs 串行执行，避免 `.build` 锁竞争。
- 下一步进入 Round 86：继续细化 task plan metadata taxonomy，或推进 `plan inspect` / `.tritonplan` 与 `triton plan` 的字段口径统一。

## Command surface optimization Round 86 plan step argv

- Round 86 聚焦 `triton plan --json` 的可执行形态：`TKWorkflowPlanStep` 新增 `argv` wire 字段，让 agent 可直接执行 argv，而不是解析 `command` shell 字符串。
- `command` 保留为人读日志和复制入口；`argv` 是机器执行首选入口。
- 初始化和 decoder 在缺少 `argv` 时，会从既有 `command` 做保守 tokenization，支持当前 `shellEscaped` 生成的单引号形式。
- `plan.next-steps` output contract 新增 `steps[].argv` 字段说明。
- 新增 `SchemaFactSourceTests.workflowPlanStepsExposeExecutableArgv`，要求所有 plan fixture step 都暴露非空 argv，且首 token 为 `triton`。
- 共享模型测试补充 plan JSON roundtrip 后 `argv` 的断言。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover` 和 `tritonkit-real-project-regression`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-86-plan-step-argv.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/workflowPlanStepsExposeExecutableArgv`；`swift test --package-path CLI --filter SchemaFactSourceTests/agentBootstrapSchemasExposeRecoveryCommandsAndOutputContracts`；`swift test --filter TKCLITransportModelsTests/workflowPlanShape`；`swift test --package-path CLI --filter SchemaFactSourceTests`，76 个 Swift Testing 用例通过；`swift test --package-path CLI`，147 个 Swift Testing 用例通过；`swift test` 第二次全量 126 个 Swift Testing 用例通过；`swift run --package-path CLI triton plan --json` 输出包含 `steps[].argv`。
- 根包 `swift test` 第一次在 `TKPlatformFallbackTests.missingLocalServerDoesNotSurfaceConnectionNoise` 触发 runtime observer 清理相关 NSException；该用例单跑通过，第二次根包全量通过，暂记录为非本轮 plan schema 路径的间歇性风险。
- 下一步进入 Round 87：把 plan builder 改成先组装 argv 再派生 command，或推进 `.tritonplan` / `plan inspect` 与 `triton plan` 的字段口径统一。

## Command surface optimization Round 87 plan inspect step metadata

- Round 87 聚焦 `.tritonplan` 离线检查入口：`TKReplayPlanSummary` 新增 `steps: [TKReplayPlanStepSummary]`，让 `triton plan inspect <file.tritonplan> --json` 不再只返回 actions 列表。
- 每个 replay step summary 暴露 `index/id/name/action/command/argv/category/requires/expectedArtifacts/stopConditions`，与 `triton plan --json` 的执行元数据词汇保持一致。
- `argv[]` 保留 `${variable}` 占位；secure step 输出 `<redacted:length>`，避免 inspect 摘要泄露明文。`command` 保留为日志/复制入口，agent 执行仍优先使用 `argv[]` 或 `replay --dry-run`。
- `plan` schema 新增 `plan.inspect` output contract，model 为 `TKReplayPlanSummary`，并将 output contract kind taxonomy 扩展为 `replay-plan-summary`。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-real-project-regression` 和 `tritonkit-emulator-cli-takeover`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-87-plan-inspect-step-metadata.md`。
- 验证通过：`swift test --filter TKReplayPlanModelsTests/planInspectSummaryExposesStepExecutionMetadata`；`swift test --package-path CLI --filter SchemaFactSourceTests/agentBootstrapSchemasExposeRecoveryCommandsAndOutputContracts`；`swift test --filter TKReplayPlanModelsTests`，5 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，76 个 Swift Testing 用例通过；`swift run --package-path CLI triton record --output /tmp/round87-plan-inspect.tritonplan --json`；`swift run --package-path CLI triton plan inspect /tmp/round87-plan-inspect.tritonplan --json` 输出包含 step metadata；`swift test --package-path CLI`，147 个 Swift Testing 用例通过；`swift test` 第二次全量 127 个 Swift Testing 用例通过。
- 根包 `swift test` 第一次在 `TKPlatformFallbackTests.stateObserverToken` 出现 observer 重复通知间歇失败；该用例单跑通过，第二次根包全量通过，暂记录为非本轮 replay inspect 路径的间歇性风险。
- 下一步建议：继续收敛 `replay --dry-run` 的 step result metadata，使 dry-run 输出与 `plan inspect` 摘要能交叉验证。

## Command surface optimization Round 88 replay step result metadata

- Round 88 聚焦 `triton replay --dry-run --json`：`TKReplayStepResult` 新增 `category/requires/expectedArtifacts/stopConditions`，让 replay dry-run / execution step result 与 `plan inspect` summary 使用同一套执行诊断元数据。
- 新字段默认按 step `command` argv 派生；`category` 复用 recovery taxonomy，`expectedArtifacts` 覆盖 input-result、wait-result、screenshot、evidence-bundle，`stopConditions` 覆盖 timeout、artifact.write-failed 与 step.failed。
- `TKReplayStepResult` 增加自定义 decoder，旧 JSON 缺少这些字段时仍可解码并自动派生默认值。
- `replay.result` output contract 新增 step 字段声明，包括 `steps[].category`、`steps[].requires`、`steps[].expectedArtifacts`、`steps[].stopConditions`。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-real-project-regression` 和 `tritonkit-emulator-cli-takeover`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-88-replay-step-result-metadata.md`。
- 验证通过：`swift test --filter TKReplayPlanModelsTests/replayStepResultDerivesExecutionMetadata`；`swift test --package-path CLI --filter SchemaFactSourceTests/executionAndEvidenceSchemasExposeRecoveryCommandsAndOutputContracts`；`swift run --package-path CLI triton replay /tmp/round87-plan-inspect.tritonplan --dry-run --var username=alice --var password=secret --json` 输出包含 step metadata；`swift test --filter TKReplayPlanModelsTests`，6 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，76 个 Swift Testing 用例通过；`swift test --package-path CLI`，147 个 Swift Testing 用例通过；`swift test`，128 个 Swift Testing 用例通过。
- 风险：`TKReplayStepResult.command` 历史上是 argv token 数组，字段名与 task plan 的 string `command` 不完全一致；文档已注明用 dry-run `steps[].command` 与 inspect `steps[].argv` 对照。
- 下一步建议：抽取 replay step metadata 派生逻辑，减少 inspect summary 与 replay result 的重复映射。

## Command surface optimization Round 89 replay step result argv

- Round 89 聚焦 `triton replay --dry-run --json` / replay result 的首选执行字段：`TKReplayStepResult` 新增 `argv: [String]`，与 `plan inspect` 和任务型 `plan` 的 `steps[].argv` 口径对齐。
- `command` 保留为历史 argv token 数组和日志字段；agent 首选读取 `steps[].argv`，避免继续依赖 `command` 字段语义。
- decoder 支持旧 JSON：缺少 `argv` 时由 `command` 自动补齐；缺少 `command` 但存在 `argv` 时也可解码。
- replay step metadata 派生改为基于 `argv`，保证 category、expectedArtifacts 与 stopConditions 跟随首选执行字段。
- `replay.result` output contract 与 success shape 已声明 `steps[].argv`。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-real-project-regression` 和 `tritonkit-emulator-cli-takeover`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-89-replay-step-result-argv.md`。
- 验证通过：`swift test --filter TKReplayPlanModelsTests/replayStepResultDerivesExecutionMetadata`；`swift test --package-path CLI --filter SchemaFactSourceTests/executionAndEvidenceSchemasExposeRecoveryCommandsAndOutputContracts`；`swift test --filter TKReplayPlanModelsTests`，6 个 Swift Testing 用例通过；`swift test --package-path CLI --filter SchemaFactSourceTests`，76 个 Swift Testing 用例通过；`swift test --package-path CLI`，147 个 Swift Testing 用例通过；`swift test`，128 个 Swift Testing 用例通过；`swift run --package-path CLI triton replay /tmp/round87-plan-inspect.tritonplan --dry-run --var username=alice --var password=secret --json` 输出包含 `steps[].argv` 与 `steps[].command`。
- 下一步建议：抽取 replay step metadata / argv 派生 helper，减少 inspect summary 与 replay result 的重复映射。

## Command surface optimization Round 90 replay step execution helper

- Round 90 聚焦 replay step argv / metadata 的实现事实源收敛：新增 `TKReplayStepExecution` helper，统一 `plan inspect` summary、replay dry-run 和真实 replay result 的 argv / metadata 派生。
- 新增 `TKReplayStepExecutionDescriptor` 与 `TKReplayStepExecutionMetadata`，`TKReplayPlanStepSummary` 与 `TKReplayStepResult` 均改为消费共享 helper。
- CLI `replayCommand` 改为调用 `TKReplayStepExecution.argv(for:planName:index:variables:)`，不再在 CLI runtime 中维护一份独立 argv builder。
- 保持对外行为不变：inspect summary 的 `steps[].argv` 保留 `${variable}`，replay dry-run 的 `steps[].argv` 输出变量替换后的 argv，secure 值仍为 `<redacted:length>`，`steps[].command` 继续作为历史 argv alias / 日志字段存在。
- 新增测试 `TKReplayPlanModelsTests.replayExecutionHelperAlignsInspectAndResultMetadata`，先红灯确认 helper 缺失，再实现通过。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-90-replay-step-execution-helper.md`。
- 验证通过：`swift test --filter TKReplayPlanModelsTests/replayExecutionHelperAlignsInspectAndResultMetadata`；`swift test --filter TKReplayPlanModelsTests`，7 个 Swift Testing 用例通过；`swift test --package-path CLI`，147 个 Swift Testing 用例通过；`swift run --package-path CLI triton replay /tmp/round87-plan-inspect.tritonplan --dry-run --var username=alice --var password=secret --json` 输出保持 `steps[].argv`、`steps[].command` 与 metadata；`swift test`，129 个 Swift Testing 用例通过。
- 下一步建议：补充 invalid replay step 的 helper 单元测试，确保 dry-run 提前暴露缺失 selector / text / wait condition。

## Command surface optimization Round 91 replay step static validation

- Round 91 聚焦 replay dry-run 静态验证：`TKReplayStepExecution.argv(...)` 在 strict 路径中提前拒绝多 selector 的 `tap`、多 condition 的 `wait`、缺少 `value/text` 的 `paste/type`、缺少 condition 的 `wait`。
- 新增错误码模型：`TKReplayStepExecutionError.ambiguousTapSelector` 与 `TKReplayStepExecutionError.ambiguousWaitCondition`；错误描述与既有 runtime validation 文案对齐。
- 新增测试 `TKReplayPlanModelsTests.replayExecutionHelperRejectsAmbiguousDryRunSteps`，先红灯确认缺少 ambiguous 错误 case，再实现通过。
- 有效 replay dry-run 输出保持不变：`steps[].argv`、`steps[].command`、`category/requires/expectedArtifacts/stopConditions` 仍按 Round 89 / Round 90 口径输出。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-real-project-regression` 和 `tritonkit-emulator-cli-takeover`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-91-replay-step-static-validation.md`。
- 验证通过：`swift test --filter TKReplayPlanModelsTests/replayExecutionHelperRejectsAmbiguousDryRunSteps`；`swift test --filter TKReplayPlanModelsTests`，8 个 Swift Testing 用例通过；`swift test --package-path CLI`，147 个 Swift Testing 用例通过；`swift test`，130 个 Swift Testing 用例通过；`swift run --package-path CLI triton replay /tmp/round87-plan-inspect.tritonplan --dry-run --var username=alice --var password=secret --json` 输出保持有效 plan 的 argv / metadata。
- 下一步建议：在 CLI 层补 invalid `.tritonplan` dry-run 的 JSON 错误 envelope 测试，确保静态 validation error 不被二次包装。

## Command surface optimization Round 92 replay dry-run error envelope

- Round 92 使用 subagent 加速：Hilbert 负责进程级测试切片，Socrates 负责只读路线审计；主控负责集成、验证和写回。
- 新增 `CLI/Tests/TritonKitCLITests/ReplayCommandTests.swift`，构造 `tap` 同时包含 `text` 与 `oid` 的 invalid `.tritonplan`，执行真实 `triton replay <file> --dry-run --json`。
- 测试锁定静态 validation failure 的 CLI 行为：exit code 非 0、stdout/stderr 只有一个非空 JSON envelope、可解码为 `TKCLIErrorResponse`、`ok=false`、`error.code=validation_failed`、message 包含 tap selector 歧义说明。
- 当前 runtime 已满足该行为，Round 92 只新增测试；主控补充临时目录清理。
- Socrates 审计给出后续 3 个切片建议：`plan-inspect` 一等 capability、校验 `steps[].argv` 而非只校验 `command`、给 `.tritonplan` 静态 validation 增加 step-level 诊断。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-92-replay-dry-run-error-envelope.md`。
- 验证通过：`swift test --package-path CLI --filter ReplayCommandTests/invalidDryRunJSONReplayEmitsOneErrorEnvelope`；`swift test --package-path CLI`，148 个 Swift Testing 用例通过。
- 下一步建议：采用低风险的 `plan-inspect` 一等 capability 作为 Round 93。

## Command surface optimization Round 93 plan inspect capability

- Round 93 继续使用 subagent 加速：Curie 负责 `plan-inspect` capability 实现，Chandrasekhar 负责下一轮 `steps[].argv` schema-backed 校验只读审计；主控负责复核、全量 CLI 验证、docs / skills / memory 写回。
- `runtimeCapabilities(...)` 新增 `plan-inspect`，在 server down、disconnected、connected 三态下均 `supported=true`。
- `plan-inspect` 规划元数据固定为 `group=replay`、`requiredBy=["replay"]`、`nextAction=triton plan inspect <file.tritonplan> --json`、`evidence=["tritonplan","stdout-json"]`。
- `plan` schema 的 `providedCapabilities[]` 新增 `plan-inspect`，使 `triton schema --command plan --json` 与 `triton capabilities --json` 能暴露同一事实口径。
- 新增 `SchemaFactSourceTests.capabilitiesMatrixExposesPlanInspectCapability`，并在 bootstrap schema 测试中断言 `plan.providedCapabilities` 同时包含 `plan` 与 `plan-inspect`。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md`、README 与三个 public skills，把 `plan-inspect` 与 `replay-dry-run` 分离表达：前者是离线 `.tritonplan` 摘要检查，后者是离线 validation，二者都不证明真实执行。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-93-plan-inspect-capability.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/capabilitiesMatrixExposesPlanInspectCapability`；`swift test --package-path CLI --filter SchemaFactSourceTests`，77 个 Swift Testing 用例通过；`swift test --package-path CLI`，149 个 Swift Testing 用例通过；`swift run --package-path CLI triton capabilities --json` 输出包含 `plan-inspect` 的 replay 分组和 `plan inspect` nextAction。
- Round 94 推荐：新增 `validateSchemaBackedArgv(...)` 测试 helper，把 task workflow plan 的 schema-backed 校验从 `step.command` shell 字符串迁到 `step.argv` 数组；replay / plan inspect 的 argv schema-backed 校验可先作为 CLI 测试补强。

## Command surface optimization Round 96 compatibility wording cleanup

- Round 96 处理 agent-facing 契约治理尾巴：清掉 README、CLI help、schema 和三个随包 public skills 里的 `compatibility` / `legacy` 使用口径，避免继续暗示 TritonKit 需要维护旧入口兼容层。
- 现有 `--simulator`、Harmony `--target`、`type --text`、`press --button` 暂时不删行为，但统一改成“显式 selector / alternate flag form”语义；`--device` 仍是默认 agent-facing selector。
- `observe` / `screenshot` / `hit --x --y` 等 help/schema 文案同步改为当前执行语义；host selector 冲突错误 hint 也改为“只选一种显式 selector 路径”。
- `steps[].command` 在 public skills 中统一改称 human-readable/logging form，不再称 legacy alias，继续把 `argv` 作为机器执行事实源。
- CLI 测试命名同步收敛为 explicit selector / mixed selector conflicts，减少治理文案与产品决策相互打架。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-96-compatibility-wording-cleanup.md`。
- 验证通过：`swift test --package-path CLI --filter DeviceCrossPlatformTests`；`swift test --package-path CLI --filter SchemaFactSourceTests`；`rg -n 'compatibility path|kept for compatibility|legacy/logging alias|compatibility with older scripts|legacy selector' README.md Sources CLI .agents/tritonkit-skills/public docs-linhay/spaces/20260527-command-surface-optimization -g '!**/.build/**'` 无命中。
- 下一步回到未完成的 Round 94：补 schema-backed `steps[].argv` 校验 helper，把 plan / replay 的测试事实源彻底从 shell 字符串迁到 argv 数组。

## Command surface optimization Round 94 schema-backed plan argv validation

- 回补 Round 94：在 `SchemaFactSourceTests` 新增 `validateSchemaBackedArgv(...)` helper，把 task workflow plan 的 schema-backed 校验正式迁到 `steps[].argv`。
- `task workflow plan ... aligned with command schemas`、`workflow plan steps expose executable metadata`、`workflow plan steps expose executable argv` 和 `workflow plan steps expose stable recovery categories` 现在都直接基于 `argv` 做 root command / flag / category 校验，不再依赖 `command` shell 字符串。
- 这轮不改外部 CLI 行为；`steps[].command` 继续保留为人读与复制入口，`steps[].argv` 是测试和 agent 执行的事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-94-schema-backed-plan-argv-validation.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/taskWorkflowPlanArgvStayAlignedWithCommandSchemas`；`swift test --package-path CLI --filter SchemaFactSourceTests/workflowPlanStepsExposeExecutableArgv`；`swift test --package-path CLI --filter SchemaFactSourceTests/workflowPlanStepsExposeStableRecoveryCategories`；`swift test --package-path CLI --filter SchemaFactSourceTests`，77 个 Swift Testing 用例通过。
- 下一步建议：把 replay / `plan inspect` 的 `steps[].argv` 也接入同一套 schema-backed helper，继续减少对 `command` 字符串的依赖。

## Command surface optimization Round 97 schema-backed replay argv validation

- 继续沿 `argv` 事实源推进：在 `SchemaFactSourceTests` 新增两条校验，把 `plan inspect` summary 与 replay result 的 `steps[].argv` 也接入 `validateSchemaBackedArgv(...)`。
- `plan inspect steps expose schema-backed argv templates` 通过 `TKReplayPlanSummary` 校验 variable-preserving template argv；`replay step results expose schema-backed argv` 通过 `TKReplayStepExecution.argv(...)` + `TKReplayStepResult` 校验真实 dry-run/replay argv。
- 现在 workflow plan、`plan inspect`、replay result 三条 agent-facing 执行链都能用同一套 schema-backed argv helper 做 root command / subcommand / flag 校验。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-97-schema-backed-replay-argv-validation.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/planInspectStepsExposeSchemaBackedArgvTemplates`；`swift test --package-path CLI --filter SchemaFactSourceTests/replayStepResultsExposeSchemaBackedArgv`；`swift test --package-path CLI --filter SchemaFactSourceTests`，79 个 Swift Testing 用例通过。
- 下一步建议：继续清理 schema examples / nextCommands 等仍主要依赖字符串 fixture 的测试路径，逐步形成更统一的 argv-first 校验基座。

## Command surface optimization Round 98 schema fixture argv extraction

- 继续推进 argv-first 测试基座：给 `CommandStringFixture` 增加 `argv`，并新增 `extractSingleTritonInvocationArgv(from:)`，从 `nextCommands[]` / `examples[]` 的字符串里抽出单个 Triton 调用 token。
- `schema next commands`、`subcommand next commands`、`schema examples and output formats remain agent usable` 现在都直接把抽出的 `argv` 喂给 `validateSchemaBackedArgv(...)`，不再让这几条路径单独走字符串拆词。
- `schema and plan placeholders are complete argv tokens` 对 schema nextCommands / examples 也改为检查抽出的 argv，避免 pipeline 前面的 shell 片段污染 placeholder 质量判断。
- `workflowPlanCommandFixtures(...)` 也同步补了 `argv` 字段，保持 fixture 结构统一。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-98-schema-fixture-argv-extraction.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaNextCommandsExposeSchemaBackedArgv`；`swift test --package-path CLI --filter SchemaFactSourceTests/subcommandNextCommandsExposeSchemaBackedArgv`；`swift test --package-path CLI --filter SchemaFactSourceTests/schemaExamplesAndOutputFormatsRemainAgentUsable`；`swift test --package-path CLI --filter SchemaFactSourceTests/schemaAndPlanPlaceholdersAreCompleteArgvTokens`；`swift test --package-path CLI --filter SchemaFactSourceTests`，79 个 Swift Testing 用例通过。
- 下一步建议：继续看 `capability.nextAction`、`defaultProviders[]` / `inheritsDefaultsFrom[]` 这些 string 入口是否也值得往统一 argv 模型收。

## Command surface optimization Round 99 string entrypoint argv unification

- 继续收敛测试基座残余的 string 入口：新增 `validateSchemaBackedCommandExpression(...)`，让 `defaultProviders[]` / `inheritsDefaultsFrom[]` 通过 argv 提取后再走统一 helper。
- 同时新增 `validateSchemaBackedNextAction(...)`，把 capability `nextAction { command, args }` 也统一组装成 argv，再走 `validateSchemaBackedArgv(...)`，减少直接调用底层 `validateSchemaBackedCommand(...)` 的测试路径。
- 这轮不改产品行为或 wire model，只让测试消费路径更一致，继续强化 “argv 是机器执行事实源” 这个方向。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-99-string-entrypoint-argv-unification.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests/schemaDefaultProviderReferencesStaySchemaBacked`；`swift test --package-path CLI --filter SchemaFactSourceTests/capabilityNextActionsStayAlignedWithCommandSchemas`；`swift test --package-path CLI --filter SchemaFactSourceTests`，79 个 Swift Testing 用例通过。
- 下一步建议：继续评估哪些旧 string helper 已经只剩边缘场景，再决定是否删除或继续保留；或者转回更高层的 CLI schema / info architecture 收敛。

## Command surface optimization Round 100 argv primary wording

- Round 100 处理 schema / 文档 / public skills 的残留文案歧义：继续保持行为不变，但把 `argv` 明确写成 agent 主执行事实源，避免 `command` 被误读为主执行入口。
- `Sources/TritonKitCLI/CLISchemaContracts.swift` 已更新：
  - `plan.next-steps.steps[].command` 改为 human-readable Triton invocation；
  - `plan.next-steps.steps[].argv` 改为 agent 主执行 argv；
  - `plan.inspect.steps[].command` 改为 human-readable template；
  - `plan.inspect.steps[].argv` 改为 agent 主执行 template；
  - `replay.steps[].command` 改为历史 argv alias / 日志一致性字段；
  - `replay.steps[].argv` 保持 agent 主执行字段。
- `docs-linhay/dev/agent-facing-cli-information-architecture.md` 与 `docs-linhay/dev/ai-cli-readable-control.md` 已收敛：
  - task plan、plan inspect、replay dry-run 一律以 `argv[]` 为主执行字段；
  - `command` 一律定位为人读 / 日志 / copy-paste 字段；
  - 文档中的旧测试名 `taskWorkflowPlanCommandsStayAlignedWithCommandSchemas` 已改为当前实际测试名 `taskWorkflowPlanArgvStayAlignedWithCommandSchemas`。
- 三个 public skills 已同步调整：
  - `tritonkit-dev-feedback`
  - `tritonkit-emulator-cli-takeover`
  - `tritonkit-real-project-regression`
  重点是把 placeholder / 执行契约的检查目标收敛到 `plan.steps[].argv`，同时保留 `plan.steps[].command` 必须是单条 `triton ...` 人读调用的约束。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-100-argv-primary-wording.md`。
- 下一步建议：继续清点是否还存在只剩边缘用途的 string-first helper，或转入更高层的 `schema/capabilities/plan` 信息架构收敛。

## Command surface optimization Round 101 recovery root argv fixtures

- Round 101 继续清理测试基座残余的 string-first 入口：把 recovery taxonomy 相关的 schema fixture root command 提取，从 `fixture.command` 收口到 `fixture.argv`。
- `SchemaFactSourceTests.schemaRecoveryCommandRootsStayWithinRecoveryTaxonomy` 与 `SchemaFactSourceTests.schemaRecoveryCommandRootsExposeStableCategories` 现在都直接读取 `schemaNextCommandFixtures(...).argv` 推导 root command，不再解析 command 字符串。
- 删除已无引用的 `validateSchemaBackedCommandString(...)` helper，进一步减少 `SchemaFactSourceTests` 中的 string-first 校验入口。
- 这样一来，schema fixture 的字符串字段主要只剩两类职责：
  - `command`：单条 invocation / placeholder / shell-control-operator 质量约束；
  - `argv`：schema-backed 执行事实校验与 recovery root / category 推导。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-101-recovery-root-argv-fixtures.md`。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`，79 个 Swift Testing 用例通过。
- 下一步建议：继续盘点是否还有只保留历史用途的 string helper；如果测试基座已基本收口，可以开始进入更高层的 `schema/capabilities/plan` 入口整理。

## Command surface optimization Round 102 plan mode boundary

- Round 102 从 wording / test helper 收敛进入一个真实 wire contract 边界：给 `TKWorkflowPlanResponse` 增加 `mode`，把 bootstrap 恢复计划和 task workflow 计划正式区分开。
- `mode` 取值：
  - `bootstrap`：环境恢复 / 发现计划；
  - `task`：`ios-smoke`、`open-url`、`webview-check` 这类目标型 workflow 计划。
- `buildWorkflowPlan(...)` 的三类通用返回路径都显式输出 `mode=bootstrap`；`buildTaskWorkflowPlan(...)` 与 `taskWorkflowPlan(...)` 输出 `mode=task`。未知 goal 的 validation failure 仍归 `task`，因为它属于 task plan 的输入错误，而不是 bootstrap 恢复链路。
- `TKWorkflowPlanResponse` decoder 对旧 JSON 增加保守回推：`goal == general` 或 goal 缺省时推断为 `bootstrap`，其他 goal 推断为 `task`。
- `plan.next-steps` output contract 已新增 `mode` 字段；dev 文档与 public skills 也同步更新，要求 agent 用 `mode` 判断当前是恢复环境还是进入任务链路，不再依赖 `goal == general` 这样的隐式约定。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-102-plan-mode-boundary.md`。
- 验证通过：
  - `swift test --filter TKCLITransportModelsTests`，21 个 Swift Testing 用例通过；
  - `swift test --package-path CLI --filter SchemaFactSourceTests`，80 个 Swift Testing 用例通过。
- 下一步建议：继续检查 `status / doctor / capabilities / plan` 之间是否还缺少能显式表达职责边界的字段，而不是继续让 agent 从隐式行为推断。

## Command surface optimization Round 103 bootstrap surface contract

- Round 103 继续收紧 bootstrap 入口职责边界：给 `status`、`doctor`、`capabilities`、`plan` 四个 bootstrap response 增加顶层 `surface` 字段，避免 agent 只靠调用上下文判断当前 JSON 的入口职责。
- 新增 top-level `surface` 取值：
  - `TKCLIStatusEnvelope` => `status`
  - `TKDoctorResponse` => `doctor`
  - `TKCapabilitiesResponse` => `capabilities`
  - `TKWorkflowPlanResponse` => `plan`
- shared model decoder 对旧 JSON 保持兼容：缺少 `surface` 时自动补默认值；`plan` 继续沿用 Round 102 的 `mode` 回推逻辑，因此旧 payload 既能补 `surface`，也能补 `mode`。

## Command surface optimization Round 104 doctor workflow taxonomy bridge

- Round 104 继续收紧 bootstrap 诊断入口：给 `doctor` 增加 workflow taxonomy bridge，避免 agent 必须把 `doctor.checks[].relatedCapabilities` 与 `capabilities[].requiredBy` 手工 join 一次，才能知道当前阻塞的是哪条 workflow。
- `TKDoctorCheck` 新增 `workflowCategories`，由关联 capability 的 `requiredBy[]` 汇总去重得到。
- `TKDoctorResponse` 新增顶层 `nextWorkflows`，直接复用第一条 `fail/warn` check 的 `workflowCategories`，让 agent 可先按 workflow lane 决定恢复链路。
- `doctor` output contract 已同步声明 `nextWorkflows` 与 `checks[].workflowCategories`；shared decoder 对旧 JSON 缺少 `nextWorkflows` 时保持兼容回填 `[]`。
- `buildDoctorResponse(...)` 现在负责 capability -> workflow taxonomy 的桥接，`doctor` 成为真正可规划的 ordered recovery surface，而不只是有序诊断列表。
- 同步更新：
  - `docs-linhay/dev/ai-cli-readable-control.md`
  - `docs-linhay/dev/agent-facing-cli-information-architecture.md`
  - `tritonkit-dev-feedback`
  - `tritonkit-real-project-regression`
  - `tritonkit-emulator-cli-takeover`
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-104-doctor-workflow-taxonomy-bridge.md`。
- 验证通过：
  - `swift test --filter TKCLITransportModelsTests/doctorResponseShape`
  - `swift test --package-path CLI --filter SchemaFactSourceTests/doctorResponseExposesOrderedRecoveryChecks`
  - `swift test --package-path CLI --filter SchemaFactSourceTests/agentBootstrapSchemasExposeRecoveryCommandsAndOutputContracts`
  - `swift test --filter TKCLITransportModelsTests`，21 个 Swift Testing 用例通过
  - `swift test --package-path CLI --filter SchemaFactSourceTests`，80 个 Swift Testing 用例通过
- 本轮没有扩大 workflow taxonomy，只把现有 `capabilities.requiredBy` 映射直接带进 `doctor`，保持方案 C 的信息架构继续向“agent 先问 Triton 下一步”收口。

## Command surface optimization Round 105 plan next workflows routing

- Round 105 把同一套 workflow taxonomy 再推进一层到 `plan`：新增 `TKWorkflowPlanResponse.nextWorkflows`，让 agent 不需要只靠 `goal` 或 step 命令词推断当前推荐 lane。
- shared model 对旧 JSON 保持兼容：缺少 `nextWorkflows` 时，按 `goal + nextStep` 回填默认 workflow lane。
- `plan.next-steps` output contract 已新增 `nextWorkflows` 字段；text render 也同步输出，避免 text mode 丢失 routing 事实。
- `SchemaFactSourceTests` 已固定：
  - `plan.next-steps` output contract 包含 `nextWorkflows`
  - `ios-smoke` -> `["app","assert","evidence","smoke","target"]`
  - `open-url` -> `["app","assert","evidence","target"]`
  - `webview-check` -> `["assert","evidence","observe","route","webview-check"]`
  - `workflowPlanNextWorkflowsStayWithinTheWorkflowTaxonomy`
- shared tests已固定：
  - `workflowPlanShape`
  - `workflowPlanInfersModeForOlderPayloads`
- 同步更新：
  - `docs-linhay/dev/ai-cli-readable-control.md`
  - `docs-linhay/dev/agent-facing-cli-information-architecture.md`
  - `tritonkit-dev-feedback`
  - `tritonkit-real-project-regression`
  - `tritonkit-emulator-cli-takeover`
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-105-plan-next-workflows-routing.md`。
- 验证通过：
  - `swift test --filter TKCLITransportModelsTests/workflowPlanShape`
  - `swift test --filter TKCLITransportModelsTests/workflowPlanInfersModeForOlderPayloads`
  - `swift test --package-path CLI --filter SchemaFactSourceTests/taskWorkflowPlansExposeExecutableCommandSequences`
  - `swift test --package-path CLI --filter SchemaFactSourceTests/workflowPlanNextWorkflowsStayWithinTheWorkflowTaxonomy`
  - `swift test --filter TKCLITransportModelsTests`，21 个 Swift Testing 用例通过
  - `swift test --package-path CLI --filter SchemaFactSourceTests`，81 个 Swift Testing 用例通过
- 本轮聚焦测试时误并行起了多个 SwiftPM 命令，触发 `.build` 锁等待；最终结果全绿，后续继续回到串行执行，避免验证噪音。

## Command surface optimization Round 106 plan step workflow taxonomy

- Round 106 继续把 workflow taxonomy 从顶层 plan 推进到单步 step：新增 `TKWorkflowPlanStep.workflowCategories`，让 agent 进入具体 step 后不需要再从 root command 或顶层 goal 反推 lane。
- shared model 对旧 JSON 保持兼容：缺少 `workflowCategories` 时，按 step `command` 自动派生默认 workflow taxonomy。
- `plan.next-steps` output contract 已新增 `steps[].workflowCategories`。
- `SchemaFactSourceTests` 已固定：
  - `plan.next-steps` output contract 包含 `steps[].workflowCategories`
  - `target-list` step -> `["action","app","assert","evidence","observe","runtime","smoke","target"]`
  - `ios-smoke` step -> `["app","assert","evidence","smoke","target"]`
  - `app-open-url` step -> `["app","assert","evidence","target"]`
  - `webview-current` step -> `["assert","evidence","observe","route","webview-check"]`
  - `workflowPlanStepsExposeWorkflowTaxonomy`
- shared tests已固定：
  - `workflowPlanShape` 断言 bootstrap `start-server` step 带 workflow taxonomy
- 同步更新：
  - `docs-linhay/dev/ai-cli-readable-control.md`
  - `docs-linhay/dev/agent-facing-cli-information-architecture.md`
  - `tritonkit-dev-feedback`
  - `tritonkit-real-project-regression`
  - `tritonkit-emulator-cli-takeover`
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-106-plan-step-workflow-taxonomy.md`。
- 验证通过：
  - `swift test --filter TKCLITransportModelsTests/workflowPlanShape`
  - `swift test --package-path CLI --filter SchemaFactSourceTests/taskWorkflowPlansExposeExecutableCommandSequences`
  - `swift test --package-path CLI --filter SchemaFactSourceTests/workflowPlanStepsExposeWorkflowTaxonomy`
  - `swift test --filter TKCLITransportModelsTests`，21 个 Swift Testing 用例通过
  - `swift test --package-path CLI --filter SchemaFactSourceTests`，82 个 Swift Testing 用例通过

## Command surface optimization Round 107 replay step workflow taxonomy

- Round 107 把同口径 workflow taxonomy 再推进到 replay 侧：新增 `TKReplayPlanStepSummary.workflowCategories` 与 `TKReplayStepResult.workflowCategories`。
- `TKReplayStepExecutionDescriptor` / `Metadata` 同步扩展 `workflowCategories`，inspect summary 与 replay result 共用同一套默认派生逻辑。
- replay step workflow taxonomy 当前固定为：
  - `tap/paste/type/clear/input` -> `["action","assert","evidence"]`
  - `wait` -> `["assert","evidence","observe"]`
  - `screenshot` -> `["evidence","observe"]`
  - `evidence/capture` -> `["evidence","replay"]`
  - 其他 fallback -> `["replay"]`
- output contract 已同步声明：
  - `plan.inspect.steps[].workflowCategories`
  - `replay.result.steps[].workflowCategories`
- `SchemaFactSourceTests` 已固定：
  - `planInspectStepsExposeWorkflowTaxonomy`
  - `replayStepResultsExposeWorkflowTaxonomy`
- shared tests已固定：
  - `planInspectSummaryExposesStepExecutionMetadata`
  - `replayStepResultDerivesExecutionMetadata`
  - `replayExecutionHelperAlignsInspectAndResultMetadata`
- 同步更新：
  - `docs-linhay/dev/ai-cli-readable-control.md`
  - `docs-linhay/dev/agent-facing-cli-information-architecture.md`
  - `tritonkit-dev-feedback`
  - `tritonkit-real-project-regression`
  - `tritonkit-emulator-cli-takeover`
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-107-replay-step-workflow-taxonomy.md`。
- 验证通过：
  - `swift test --filter TKReplayPlanModelsTests/planInspectSummaryExposesStepExecutionMetadata`
  - `swift test --filter TKReplayPlanModelsTests/replayStepResultDerivesExecutionMetadata`
  - `swift test --package-path CLI --filter SchemaFactSourceTests/planInspectStepsExposeWorkflowTaxonomy`
  - `swift test --package-path CLI --filter SchemaFactSourceTests/replayStepResultsExposeWorkflowTaxonomy`
  - `swift test --filter TKReplayPlanModelsTests`，9 个 Swift Testing 用例通过
  - `swift test --package-path CLI --filter SchemaFactSourceTests`，84 个 Swift Testing 用例通过
- bootstrap output contracts 已同步更新：
  - `status`
  - `doctor`
  - `capabilities`
  - `plan.next-steps`
  现在都显式声明 `surface` 字段。
- `Tests/TritonKitSharedTests/TKCLITransportModelsTests.swift` 与 `CLI/Tests/TritonKitCLITests/SchemaFactSourceTests.swift` 已同步补断言，覆盖：
  - JSON roundtrip / old-payload decode 的 `surface` 回填；
  - schema contract 的 `surface` 暴露；
  - doctor / task plan builder 产物的 `surface` 一致性。
- dev 文档和 3 个 public skills 已更新，把 `surface` 定义为 bootstrap 入口职责边界字段，让 agent 能从 JSON 自身区分状态事实、诊断排序、能力矩阵和规划入口。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260531-round-103-bootstrap-surface-contract.md`。
- 验证通过：
  - `swift test --filter TKCLITransportModelsTests`，21 个 Swift Testing 用例通过；
  - `swift test --package-path CLI --filter SchemaFactSourceTests`，80 个 Swift Testing 用例通过。
- 下一步建议：继续检查 bootstrap 入口是否还缺少更直接的 workflow/task taxonomy，减少 agent 在 `doctor.nextStep`、`capabilities.requiredBy` 和 `plan` 之间做二次推断。
- evidence surfaces 已补 `primaryArtifacts[]` 与离线 output contracts：`TKEvidenceManifest`、`TKEvidenceSummaryResponse`、`TKEvidenceRedactionResponse` 现在都会直接给出 agent 首先该看的高信号 artifact 顺序，`evidence` schema 也新增 `evidence.summary` / `evidence.redact` 详细 output contracts，减少 agent 从全量 `artifacts[]` 和文本说明自行推断检查顺序。
- replay result 顶层已补 failure routing：`TKReplayResult` 现在直接暴露 `failureWorkflowCategories[]`、`failurePrimaryArtifacts[]`、`suggestedCommands[]`，让 agent 在 replay 失败后先拿到 workflow lane、优先诊断 artifact 和最小 follow-up 命令，而不是自己从 `failedStepIndex + steps[] + evidence/file` 再做一次聚合。
- replay result 顶层 failure routing 继续结构化：新增 `recoveryCommands[]`，把 replay follow-up 从字符串 `suggestedCommands[]` 提升成 `{command, category}`，与 schema `recoveryCommands[]` 和 recovery taxonomy 对齐，减少 agent 自己做命令字符串分类。
- replay 顶层 failure routing 已补稳定失败码桥接：`TKReplayResult` / `TKReplayStepResult` 现在直接暴露 `failureCode`，顶层再补 `failureRecoveryCategories[]`，让 agent 能先读“这次 replay 在 failure code 体系里属于哪类”，再决定恢复阶段和下一步命令。
- replay 真实执行的 catch 路径继续收紧：外层失败 step 不再一律落成 `step_failed`，而是优先保留底层 `TKCLIErrorDetail.code`；请求超时映射为 `timeout`，本地 screenshot/evidence 写盘异常映射为 `artifact_write_failed`，只有无法识别的异常才回落到 `step_failed`。
- replay step 级失败面继续结构化：`TKReplayStepResult` 现已补 `error: TKCLIErrorDetail?`，agent 在 replay 失败后除了顶层 `failureCode` 之外，还能直接读取失败 step 的 `hint/endpoint/nextAction` 等诊断字段，而不是只拿到 `message`。
- replay 的非抛错失败步骤也开始补 step 级 `error`：当前 `input.ok=false`、`wait.ok=false`、`evidence.ok=false` 会直接生成 `TKCLIErrorDetail`，避免 agent 在这些分支上丢掉 `hint` 和 `endpoint`。
- replay 顶层 failure surface 继续收紧：`TKReplayResult` 现已补 `failureError: TKCLIErrorDetail?`，agent 可以直接从 replay 顶层拿到失败详情，不再必须通过 `failedStepIndex -> steps[].error` 再做一次关联。
- replay error contract 继续细化：`failureError.*` 和 `steps[].error.*` 现在都在 output contract 中展开到稳定子字段，减少 agent 把 `TKCLIErrorDetail` 当黑盒对象处理。
- replay 恢复建议继续收紧：若 `failureError.nextAction` 存在，replay 现在会把它折进 `suggestedCommands[]` / `recoveryCommands[]`，避免错误详情和恢复命令给出两套不同入口。
- replay 恢复阶段也继续收紧：若 `failureError.nextAction` 存在，其 category 现在也会并入 `failureRecoveryCategories[]`，减少“命令出现了，但恢复阶段仍看不见它”的分裂。
- replay shared model 兜底已补齐：`TKReplayResult` 的默认构造与 JSON 解码现在都会自动把 `failureError.nextAction` 回填进 `suggestedCommands[]`、`recoveryCommands[]` 和 `failureRecoveryCategories[]`，避免只有 CLI runtime 当场运行时才满足恢复面一致性。
- replay recovery surface 已补首选路径语义：`failureError.nextAction` 一旦存在，shared model 与 CLI runtime 都会把它移动到 `suggestedCommands[0]`、`recoveryCommands[0]` 和 `failureRecoveryCategories[0]`，减少 agent 失败后自行排序恢复步骤。
- replay recovery conflict 策略已显式化：只有带 `nextAction` 的失败才会把 `recoveryCommands[]` 覆盖到的阶段提前到 `failureRecoveryCategories[]` 前部，并把未覆盖的 failure family 余项留在后面；没有 `nextAction` 的失败继续保持 family-first。
- replay 顶层 failure surface 已补单值首选 lane：`failurePrimaryWorkflowCategory` 和 `failurePrimaryRecoveryCategory` 现在分别回填失败 workflow lane 与首选恢复阶段，agent 不再必须每次扫描 `failureWorkflowCategories[]` / `failureRecoveryCategories[]` 数组才能拿到第一优先级。
- replay 顶层诊断提示已补单值字段：`failurePrimaryHint` 现在会回填 `failureError?.hint`，让 agent 不拆结构化错误对象也能直接拿到首选诊断提示。
- replay 顶层诊断 endpoint 已补单值字段：`failurePrimaryEndpoint` 现在会回填 `failureError?.endpoint`，让 agent 不拆结构化错误对象也能直接拿到首选 transport/runtime endpoint。
- replay 顶层结构化恢复动作已补单值字段：`failurePrimaryNextAction` 现在会回填 `failureError?.nextAction`，让 agent 不解包 `failureError` 也能直接拿到首选结构化恢复动作。
- replay 顶层 evidence surface 已补单值首选 artifact：`failurePrimaryArtifact` 现在会回填 `failurePrimaryArtifacts.first`，让 agent 在失败后可以直接拿到“先看哪一个证据对象”。
- replay 顶层恢复字符串入口已补单值字段：`failurePrimarySuggestedCommand` 现在会回填 `suggestedCommands.first`，兼容只消费字符串命令、不解析 `TKCommandRecoveryCommand` 的 agent。
- replay 顶层恢复命令也已补单值入口：`failurePrimaryRecoveryCommand` 现在会回填 `recoveryCommands.first`，让 agent 在失败后可以一跳拿到“下一条最优先执行的恢复命令”。
- replay error subfield contract 继续补全：`replay.result` schema 现在把 `failureError` 与 `steps[].error` 里的 `nearestCandidates`、`suggestedCommands`、`candidateCount` 也显式展开，减少 agent 只能靠 `TKCLIErrorDetail` DTO 猜测可读字段。
- 通用 error contract 也开始补齐：凡是 output contract 里声明 `error: TKCLIErrorDetail?`，schema helper 现在都会自动展开 `endpoint`、`hint`、`nearestCandidates`、`suggestedCommands`、`candidateCount` 和完整 `nextAction` 子字段，不再只自动补 `nextAction.category`。
- Round 131 继续把 nextAction 契约从“部分手写字段”收口成通用 schema 规则：凡是 output contract 里声明 `TKCLINextAction?`，现在都会自动展开 `command`、`args`、`category`、`requiresLongRunningProcess`，覆盖 `capabilities[].nextAction`、`doctor.checks[].nextAction`、`failurePrimaryNextAction`、`failureError.nextAction` 与 `steps[].error.nextAction`。
- `SchemaFactSourceTests` 新增 `nextAction output contracts expose stable next action subfields`，同时补强 bootstrap / replay contract 断言，锁定所有 nextAction 输出面不再退回 DTO 黑盒。
- 验证通过：`swift test --package-path CLI --filter SchemaFactSourceTests`，85 个 Swift Testing 用例通过。
- Round 132 继续把 bootstrap 首选入口提升到顶层：`TKDoctorResponse.primaryNextAction`、`TKCapabilitiesResponse.primaryCapability` / `primaryNextAction`、`TKWorkflowPlanResponse.primaryNextAction` 现在都能直接告诉 agent “下一条先跑哪条 Triton 命令”，不必再扫 `checks[]`、`capabilities[]` 或 `steps[]`。
- shared decode 也补了旧 payload 回填：旧 `plan` JSON 即使没有 `primaryNextAction` 和 `steps[]`，也会按 `nextStep` 保守回推出 `serve` / `target` / `webview` 一类顶层首选命令，避免兼容读取时退回 nil。
- 本轮验证通过：
  - `swift test --filter TKCLITransportModelsTests`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
- Round 133 继续把顶层首选入口的“为什么是它”结构化：`doctor.primaryNextActionSource`、`plan.primaryNextActionSource`、`capabilities.primaryNextActionSource` 现在会直接说明这条首选命令来自当前 step/check、fallback、默认 nextStep、error 还是显式覆盖，避免 agent 再倒推来源。
- `capabilities` 同步补了 `primaryWorkflowCategory`，让 agent 不扫 `primaryCapability.requiredBy[]` 也能直接拿到首选能力所属的顶层 workflow lane。
- 本轮验证通过：
  - `swift test --filter TKCLITransportModelsTests`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
- Round 134 继续减少 bootstrap 顶层数组扫描：`TKDoctorResponse` 新增 `primaryCapability`，把当前 recovery check 首先关联的 capability 直接提升到顶层，默认取 `nextStep` 对应 check 的首个 `relatedCapabilities[]`；若落入 fail/warn fallback 或 `error.nextAction` 回填，也沿同一路径保守派生。
- `doctor` output contract 已同步声明 `primaryCapability`，`SchemaFactSourceTests` 现在固定无服务场景返回 `status`、已连接但 action surface 受限场景返回 `press`，避免 agent 再扫 `checks[].relatedCapabilities[]` 才知道先看哪项 capability。
- 本轮验证通过：
  - `swift test --filter TKCLITransportModelsTests`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
- Round 135 继续把 `doctor` 的 workflow lane 收口到单值顶层：`TKDoctorResponse` 新增 `primaryWorkflowCategory`，让 agent 不再需要从 `nextWorkflows[]` 或 `checks[].workflowCategories[]` 自己挑“首条 lane”。
- 这次没有直接镜像数组首项，而是从当前 primary check 的 workflow 集合里按固定 canonical 优先级选出单值 lane，避免 capability 并集顺序把无服务场景的顶层 lane 错拉成 `action` 一类噪声；当前固定无服务场景为 `app`，action surface 受限场景为 `action`。
- `doctor` output contract、shared roundtrip 测试和 bootstrap fixture 测试都已同步补齐 `primaryWorkflowCategory`。
- 本轮验证通过：
  - `swift test --filter TKCLITransportModelsTests`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
- Round 136 继续把 `capabilities` 的 evidence 入口收口到顶层：`TKCapabilitiesResponse` 新增 `primaryEvidence`，让 agent 不再需要先取 `primaryCapability` 再扫描该 capability 的 `evidence[]` 才知道“先看哪类 artifact taxonomy”。
- 该字段当前只表达首选 artifact 类型，不表达真实路径；默认直接取 `primaryCapability` 对应 capability 的首个 `evidence[]`。当前固定场景里，`tap` 回填 `input.result`，runtime disconnected 的 `runtime-manifest` 回填 `runtime-manifest`。
- `capabilities` output contract、shared roundtrip 测试和 bootstrap fixture 测试都已同步补齐 `primaryEvidence`。
- 本轮验证通过：
  - `swift test --filter TKCLITransportModelsTests`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
- Round 137 继续把 `plan` 的 workflow lane 收口到单值顶层：`TKWorkflowPlanResponse` 新增 `primaryWorkflowCategory`，让 agent 不再需要从 `nextWorkflows[]` 或 `steps[].workflowCategories[]` 自己挑“首条 lane”。
- 该字段默认从 `nextStep` 对应 step 的 `workflowCategories[]` 里按固定 canonical 优先级挑选；若落入 default-next-step 兼容回填，则沿 `nextWorkflows[]` 走同一规则。当前固定场景里，`ios-smoke` / `open-url` / bootstrap `start-server` 都回填 `app`，`webview-check` 回填 `observe`。
- `plan` output contract、shared roundtrip 测试和 task/bootstrap fixture 测试都已同步补齐 `primaryWorkflowCategory`。
- 本轮验证通过：
  - `swift test --filter TKCLITransportModelsTests`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
- Round 138 继续把 `plan` 的 artifact 入口收口到顶层：`TKWorkflowPlanResponse` 新增 `primaryExpectedArtifact`，让 agent 不再需要从 `nextStep` 对应 step 的 `expectedArtifacts[]` 自己挑“首看哪类计划产物 taxonomy”。
- 该字段当前只表达首选 artifact 类型，不表达真实文件路径；默认直接取当前 primary step 的首个 `expectedArtifacts[]`。当前固定场景里，bootstrap `start-server`、`ios-smoke`、`open-url` 和 `webview-check` 都回填 `stdout-json`。
- `plan` output contract、shared roundtrip 测试和 task/bootstrap fixture 测试都已同步补齐 `primaryExpectedArtifact`。
- 本轮验证通过：
  - `swift test --filter TKCLITransportModelsTests`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
- Round 139 把 evidence surface 的“首看对象”也收口到单值顶层：`TKEvidenceManifest`、`TKEvidenceSummaryResponse`、`TKEvidenceRedactionResponse` 新增 `primaryArtifact`，让 agent 不再需要从 `primaryArtifacts[]` 自己取第一个高信号 artifact。
- 该字段当前只表达首选 artifact summary，不替代 `primaryArtifacts[]` 的完整排序；默认直接回填 `primaryArtifacts.first`。当前 evidence manifest / summary / redact 旧 JSON 如果缺这个字段，也会按现有排序规则保守回填。
- `evidence.manifest`、`evidence.summary`、`evidence.redact` output contract、shared evidence roundtrip 测试和 schema fixture 测试都已同步补齐 `primaryArtifact`。
- 本轮验证通过：
  - `swift test --filter TKEvidenceModelsTests`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
- Round 140 把 observe surface 的“首信任 source”也收口到单值顶层：`ObserveOutput` 新增 `primarySource`，让 agent 不再需要从 `sources[]` 自己判断当前应优先相信 `runtime-tree`、`host-layout` 还是 `webview-provider`。
- 当前 observe 语义的 canonical source 优先级固定为 `runtime-tree`、`host-layout`、`webview-provider`；只有三者都不可用时才回退到首个 source。这样 iOS `observe.current` 即使把不可用的 `host-layout` 放在数组前面，顶层仍会直接给出 `runtime-tree`。
- `observe.surface` output contract 和新的 `ObservationOutputTests` 已同步补齐 `primarySource` 与回填规则。
- 本轮验证通过：
  - `swift test --package-path CLI --filter ObservationOutputTests`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
- Round 141 把 WebView list/current 的事实来源也收口到单值顶层：`TKWebViewListResponse` 与 `TKWebViewCurrentResponse` 新增 `primarySource`，让 agent 不再需要从 `sources[]` 自己判断当前结果来自 provider、runtime tree 还是 host layout。
- WebView 语义的 canonical source 优先级固定为 `webview-provider`、`runtime-tree`、`host-layout`；这与 observe surface 不同，因为 WebView provider 才能证明 URL、DOM、bridge、events 和 wait 等 provider 级能力。旧 JSON 缺少 `primarySource` 时会按同一规则回填。
- `webview.list` 与 `webview.current` output contract 已补齐，并同步加入 schema kind taxonomy：`webview-candidates`、`webview-current`。
- 本轮验证通过：
  - `swift test --filter TKObservationModelsTests`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
- Round 142 继续补齐 WebView provider 级 schema 输出契约：`webview.current-url`、`webview.call`、`webview.events` 现在都有独立 output contract，不再只依赖 `successShape` 文本描述。
- 新增 schema kind taxonomy：`webview-provider-url`、`webview-bridge-call`、`webview-events`。`SchemaFactSourceTests` 已锁定 provider URL、allowlist bridge call 和 page events 的字段列表。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
- Round 143 补齐 Harmony host-side action 输出契约：`tap` 现在同时暴露 `input.result` 与 `host.harmony-tap`，`swipe` 暴露 `host.harmony-swipe`，`type/paste` 暴露 `host.harmony-text-input`。
- 这些 host-side action 不返回 embedded `TKInputResult`，schema 现在直接声明 `HostHarmonyTapOutput`、`HostHarmonySwipeOutput`、`HostHarmonyTextInputOutput` 字段，减少 agent 从 `successShape` prose 解析 HDC 结果。
- 三个 public skills 已同步说明：`tap/swipe/type/paste --platform harmony` 应读取 `host.harmony-*` output contract，不要套用 embedded `input.result` parser。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `git diff --check`
  - `docs-linhay/scripts/check-docs.sh`
  - `docs-linhay/scripts/check-docs.sh`
  - `历史检索 "Round 143 harmony action output contracts"`
