# 2026-06-01

## Command surface optimization Round 144 harmony wait output contract

- Round 144 补齐 `wait --platform harmony` 的 host-side schema 输出契约：新增 `host.harmony-wait`，model 为 `HostHarmonyWaitOutput`，kind 为 `host-action`。
- `wait` command schema 的 `outputContracts` 现在同时暴露 embedded `wait.result` 与 host-side `host.harmony-wait`，避免 agent 复用 embedded parser 误读 host wait 字段。
- 在 `SchemaFactSourceTests.executionAndEvidenceSchemasExposeRecoveryCommandsAndOutputContracts` 增加 red-first 断言：`wait` 必须声明 `host.harmony-wait` 的核心字段（`condition/query/matched/timedOut/elapsedMs/pollCount/match/sourceCommands`）。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md` 和三个 public skills，统一说明 Harmony host wait 输出解析入口。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-144-harmony-wait-output-contract.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/executionAndEvidenceSchemasExposeRecoveryCommandsAndOutputContracts`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `git diff --check`
  - `docs-linhay/scripts/check-docs.sh`
  - `docs-linhay/scripts/qmd-sync.sh`
  - `qmd query "Round 144 harmony wait output contract"`

## Command surface optimization Round 145 harmony artifact output contract

- Round 145 修正 Harmony host artifact 的 schema 语义：`ax/screenshot --platform harmony` 现在通过独立 `host.harmony-artifact` contract 对齐 `HostHarmonyArtifactOutput`，不再沿用 device-level `host.artifact`。
- 新增 `host.harmony-artifact` output contract，kind 为 `host-artifact`，字段覆盖 `ok/action/platform/target/artifact/sourceCommands/note`。
- `ax` / `screenshot` schema output contracts 已改为引用 `host.harmony-artifact`；`SchemaFactSourceTests` 的 screenshot contract 断言同步改为 `host.harmony-artifact`。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md` 和三个 public skills，统一 Harmony host artifact 解析入口。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-145-harmony-artifact-output-contract.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/observationAndRuntimeSchemasExposeDiagnosticContracts`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `git diff --check`
  - `docs-linhay/scripts/check-docs.sh`
  - `docs-linhay/scripts/qmd-sync.sh`
  - `qmd query "Round 145 harmony artifact output contract"`

## Command surface optimization Round 146 harmony press selector alignment

- Round 146 收敛 Harmony host press selector：`press` schema 的 host output selector 从 `host.key-action` 改为 `host.harmony-key-action`，与 `host.harmony-*` 命名体系对齐。
- 新增 red-first 断言：`SchemaFactSourceTests.executionAndEvidenceSchemasExposeRecoveryCommandsAndOutputContracts` 现在要求 `press` 暴露 `host.harmony-key-action`，并覆盖 `HostActionOutput` 全字段。
- 同步更新 `docs-linhay/dev/agent-facing-cli-information-architecture.md`、`docs-linhay/dev/ai-cli-readable-control.md` 与三个 public skills，统一 Harmony press 解析入口。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-146-harmony-press-selector-alignment.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/executionAndEvidenceSchemasExposeRecoveryCommandsAndOutputContracts`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `git diff --check`
  - `docs-linhay/scripts/check-docs.sh`
  - `docs-linhay/scripts/qmd-sync.sh`
  - `qmd query "Round 146 harmony press selector alignment"`

## Command surface optimization Round 147 ax artifact contract test coverage

- Round 147 补齐 `ax --platform harmony` 的 schema contract 测试保护，避免只覆盖 screenshot 的 `host.harmony-artifact` 而漏掉 `ax` 路径。
- `SchemaFactSourceTests.observationAndRuntimeSchemasExposeDiagnosticContracts` 新增 `ax` 断言：failure codes 覆盖 `server_unavailable` / `host_command_failed`，output contract 断言 `host.harmony-artifact` 字段集合。
- 本轮是测试加固，不改 runtime 或 schema 实现；用于防止后续 refactor 把 `ax` selector 回退到泛化 contract。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-147-ax-artifact-contract-test-coverage.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/observationAndRuntimeSchemasExposeDiagnosticContracts`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `git diff --check`
  - `docs-linhay/scripts/check-docs.sh`
  - `docs-linhay/scripts/qmd-sync.sh`
  - `qmd query "Round 147 ax artifact contract test coverage"`

## Command surface optimization Round 148 harmony clear capability boundary

- Round 148 在 capabilities matrix 中新增 `harmony-clear-text`，明确 `clear --platform harmony` 当前是显式 unsupported 边界，避免 agent 把通用 `clear` 误判为 Harmony 可执行动作。
- `harmony-clear-text` 的 nextAction 固定为 `triton clear --platform harmony --json`，evidence 固定为 `unsupported-envelope` 与 `command-schema`，用于稳定产出可诊断错误而非隐式失败。
- `clear` schema 的 `providedCapabilities` 同步补齐 `harmony-clear-text`，保持 schema fact source 与 capabilities matrix 一致。
- `SchemaFactSourceTests` 新增断言：`harmony-clear-text` 必须存在、`supported=false`、nextAction 与 evidence 固定；`clear` command 必须暴露 `harmony-clear-text` 能力并保持仅 `input.result` 输出契约。
- `doctor` 的 action-surface 检查显式排除 `harmony-clear-text`，保持 existing `press` unsupported 警告语义不回归为 fail。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `git diff --check`

## Command surface optimization Round 149 legacy selector regression guards

- Round 149 补充 action/observe contract 的 legacy selector 防回退断言，锁定 `host.harmony-*` 命名不会回退到泛化 selector。
- `SchemaFactSourceTests.executionAndEvidenceSchemasExposeRecoveryCommandsAndOutputContracts` 新增负向断言：
  - `wait` 不得出现 `host.wait`
  - `tap` 不得出现 `host.tap`
  - `swipe` 不得出现 `host.swipe`
  - `type/paste` 不得出现 `host.text-input`
- 该轮只做测试加固，不改 runtime 行为或 schema 事实源。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/executionAndEvidenceSchemasExposeRecoveryCommandsAndOutputContracts`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`

## Command surface optimization Round 150 doctor action-surface boundary filter

- Round 150 将 `doctor` 对 unsupported action 的判断从 capability 名硬编码改为规则化过滤：
  - 对 `harmony-*` 且 reason 包含 `not available in the current adapter` 的能力，视为信息性边界，不纳入 `action-surface` fail/warn 判断。
- 该改动保持了 `press` 的 existing warning 语义，同时避免后续新增 Harmony 边界能力时反复改 capability 名白名单。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/doctorResponseExposesOrderedRecoveryChecks`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`

## Command surface optimization Round 151 harmony clear nextAction server-independence

- Round 151 修复 `harmony-clear-text` 的 capabilities nextAction 误导：该能力是 host-side unsupported 边界，不应在 server 不可达时被覆盖成 `serve`。
- 在 `runtimeCapabilityRequiresServer` 中移除 `harmony-clear-text`，保持它在各状态下的 nextAction 均为 `triton clear --platform harmony --json`。
- `SchemaFactSourceTests.capabilitiesMatrixExposesGroupsDependenciesRecoveryAndEvidence` 新增断言：`unavailableServer["harmony-clear-text"]` 的 nextAction 仍是 `clear --platform harmony --json`，且不标记 long-running。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilitiesMatrixExposesGroupsDependenciesRecoveryAndEvidence`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`

## Command surface optimization Round 152 harmony host action nextAction batch guard

- Round 152 增加批量断言，锁定所有 Harmony host action capability 在 server 不可达时仍走各自命令，不退化成 `serve`：
  - `harmony-tap-text`
  - `harmony-wait-text`
  - `harmony-swipe`
  - `harmony-type-text`
  - `harmony-paste-text`
  - `harmony-clear-text`
  - `harmony-press-key`
- 新增测试 `SchemaFactSourceTests.harmonyHostActionCapabilitiesKeepServerIndependentNextActions`，同时约束 `requiresLongRunningProcess != true`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/harmonyHostActionCapabilitiesKeepServerIndependentNextActions`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`

## Command surface optimization Round 153 harmony host action capability taxonomy alignment

- Round 153 收敛 Harmony host action capability 的 taxonomy：
  - `harmony-tap-text`、`harmony-wait-text`、`harmony-swipe`、`harmony-type-text`、`harmony-paste-text`、`harmony-press-key`
  - 在 capabilities matrix 中统一归入 `group=action`，`requiredBy=action/assert/evidence`。
- 同步调整 evidence 映射：上述 capability 统一使用 `host-command-json` + `host-artifact`。
- 新增测试断言覆盖 connected + server-unreachable 两种状态下的分组、requiredBy 与 nextAction，防止后续回退到 `host target/app` 规划语义。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilitiesMatrixExposesGroupsDependenciesRecoveryAndEvidence`
  - `swift test --package-path CLI --filter SchemaFactSourceTests/harmonyHostActionCapabilitiesKeepServerIndependentNextActions`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`

## Command surface optimization Round 154 route nextAction server-independence

- Round 154 修复 `route-current-url-assert` capability 的恢复提示与 schema `requiresServer=false` 不一致问题：server 不可达时不再被 `runtimeCapabilityRequiresServer` 劫持为 `serve`。
- 在 `runtimeCapabilityRequiresServer` 与 disconnected fallback 列表中都移除 `route-current-url-assert`，让 route capability 在 connected/disconnected/server-unreachable 三态都返回同一 nextAction：
  - `triton route assert-current-url <expected-url> --json`
- `SchemaFactSourceTests.capabilitiesMatrixExposesGroupsDependenciesRecoveryAndEvidence` 新增断言，锁定 disconnected 与 server-unreachable 状态下 route capability 的 nextAction 不变，且 `requiresLongRunningProcess != true`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-154-route-nextaction-server-independence.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilitiesMatrixExposesGroupsDependenciesRecoveryAndEvidence`
  - `swift test --package-path CLI --filter SchemaFactSourceTests/harmonyHostActionCapabilitiesKeepServerIndependentNextActions`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 155 webview provider nextAction server-independence

- Round 155 收敛 WebView provider 能力的 recovery 语义：`webview-current-url`、`webview-snapshot`、`webview-bridge-call`、`webview-events`、`webview-wait` 在 server 不可达或 runtime 未连接时，不再被 capabilities fallback 改写为 `serve/status`。
- 在 `runtimeCapabilityRequiresServer` 与 disconnected fallback 列表中移除上述能力，保持 nextAction 为各自命令级入口（如 `triton webview current-url --json`、`triton webview snapshot --include metadata,text,forms --json`）。
- `SchemaFactSourceTests.capabilitiesMatrixExposesGroupsDependenciesRecoveryAndEvidence` 同步更新 `webview-current-url` 的 disconnected/server-unreachable 断言。
- 新增测试 `webviewProviderCapabilitiesKeepServerIndependentNextActions`，批量覆盖：
  - `webview-current-url`
  - `webview-snapshot`
  - `webview-bridge-call`
  - `webview-events`
  - `webview-wait`
  - `route-current-url-assert`
  并锁定 disconnected/server-unreachable 两态下 `requiresLongRunningProcess != true`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-155-webview-provider-nextaction-server-independence.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilitiesMatrixExposesGroupsDependenciesRecoveryAndEvidence`
  - `swift test --package-path CLI --filter SchemaFactSourceTests/harmonyHostActionCapabilitiesKeepServerIndependentNextActions`
  - `swift test --package-path CLI --filter SchemaFactSourceTests/webviewProviderCapabilitiesKeepServerIndependentNextActions`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 156 observe-ios nextAction server-independence

- Round 156 收敛 `observe-ios` capability 的恢复语义：当 runtime 未连接或 server 不可达时，不再退化到 `serve/status`，而是保持命令级 nextAction：`triton observe current --platform ios --json`。
- 在 `runtimeCapabilityRequiresServer` 与 disconnected fallback 列表中移除 `observe-ios`，和 `observe` 命令的 `--runtime-base-url` 执行边界保持一致。
- `SchemaFactSourceTests.capabilitiesMatrixExposesGroupsDependenciesRecoveryAndEvidence` 补齐 `observe-ios` 三态断言（connected / runtime-disconnected / server-unreachable），并锁定：
  - `group=observe`
  - `requiredBy` 包含 `action/assert/evidence`
  - server-unreachable 下 `requiresLongRunningProcess != true`
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-156-observe-ios-nextaction-server-independence.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilitiesMatrixExposesGroupsDependenciesRecoveryAndEvidence`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 157 discovery nextAction regression guard

- Round 157 新增 discovery 能力批量防回退测试，锁住 `runtime-disconnected` 与 `server-unreachable` 两态下的命令级 nextAction 行为，避免后续把 server-independent 能力回退成 `serve/status`。
- 新增测试 `SchemaFactSourceTests.discoveryCapabilitiesKeepServerIndependentNextActions`，覆盖：
  - `observe-ios`
  - `webview-list`
  - `webview-current`
  - `node-resolve`
- 测试断言：
  - `supported` 与现有语义一致（`observe-ios=false`，其余 `true`）；
  - `nextAction.command/args` 保持命令级入口；
  - `requiresLongRunningProcess != true`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-157-discovery-nextaction-regression-guard.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/discoveryCapabilitiesKeepServerIndependentNextActions`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 158 observe and node metadata consistency guard

- Round 158 对 discovery 批量门禁追加 capability 元数据一致性断言，避免“nextAction 没回退但 metadata 漂移”。
- `SchemaFactSourceTests.discoveryCapabilitiesKeepServerIndependentNextActions` 现覆盖：
  - `observe-ios`
  - `observe-harmony`
  - `webview-list`
  - `webview-current`
  - `node-resolve`
- 对 `runtime-disconnected/server-unreachable` 两态新增精确断言：
  - `group`
  - `requiredBy`
  - `evidence`
  - `nextAction.command/args`
  - `requiresLongRunningProcess != true`
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-158-observe-node-metadata-consistency-guard.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/discoveryCapabilitiesKeepServerIndependentNextActions`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 159 observe and node schema-capability cross-check

- Round 159 新增 `observe` / `node` 的 schema-capability 交叉一致性门禁，直接把 schema `providedCapabilities` 与 capabilities matrix 元数据绑定。
- 新增测试 `SchemaFactSourceTests.observeAndNodeProvidedCapabilitiesStaySchemaMatrixAligned`，断言：
  - `observe.providedCapabilities == ["observe", "observe-ios", "observe-harmony"]`
  - `node.providedCapabilities == ["node", "node-resolve"]`
- 对上述能力在 connected/disconnected 两态交叉校验：
  - `group`
  - `requiredBy`
  - `evidence`
  - `supported`
  - `nextAction.command/args`
- 该轮明确并锁定 `node` 与 `node-resolve` 的恢复边界：
  - `node` disconnected 时走 `status --json`
  - `node-resolve` disconnected 时保持 `node resolve --text <text> --json`
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-159-observe-node-schema-capability-cross-check.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/observeAndNodeProvidedCapabilitiesStaySchemaMatrixAligned`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 160 capability cross-check fixture refactor

- Round 160 对 `SchemaFactSourceTests` 做去重复重构，提取 capability 状态读取与断言 helper，降低后续门禁扩展成本。
- 新增 helper：
  - `capabilityMap(state:)`
  - `disconnectedCapabilityMap()`
  - `unavailableServerCapabilityMap()`
  - `assertCapability(...)`
- 以下测试已切换为共享 helper，断言语义保持不变：
  - `webviewProviderCapabilitiesKeepServerIndependentNextActions`
  - `discoveryCapabilitiesKeepServerIndependentNextActions`
  - `observeAndNodeProvidedCapabilitiesStaySchemaMatrixAligned`
- 本轮只改测试结构，不改 runtime 行为或 schema/capability 事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-160-capability-crosscheck-fixture-refactor.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 161 webview and route schema-capability cross-check

- Round 161 新增 `webview` / `route` 的 schema-capability 交叉门禁，防止 WebView 子能力新增或调整时只改 schema 或 matrix 的单侧。
- 新增测试 `SchemaFactSourceTests.webviewAndRouteProvidedCapabilitiesStaySchemaMatrixAligned`，固定断言：
  - `webview.providedCapabilities == ["webview-list", "webview-current", "webview-current-url", "webview-snapshot", "webview-bridge-call", "webview-events", "webview-wait"]`
  - `route.providedCapabilities == ["route-current-url-assert"]`
- 对上述能力在 connected/disconnected 两态交叉校验：
  - `group`
  - `requiredBy`
  - `evidence`
  - `supported`
  - `nextAction.command/args`
- 该轮显式锁定 WebView provider 和 route 断言能力在 disconnected 态仍保持命令级 nextAction，不回退到 `serve/status`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-161-webview-route-schema-capability-cross-check.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/webviewAndRouteProvidedCapabilitiesStaySchemaMatrixAligned`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 162 schema capability state metadata invariant

- Round 162 把 `schema.providedCapabilities` 的门禁从“存在性”升级为“跨状态元数据一致性”。
- 新增测试 `SchemaFactSourceTests.schemaProvidedCapabilitiesKeepStableMetadataAcrossCapabilityStates`，对全部 schema capability 在以下三态做覆盖：
  - `runtime-connected`
  - `runtime-disconnected`
  - `server-unreachable`
- 新增断言：
  - capability 在三态都必须存在；
  - `group` 三态一致；
  - `requiredBy` 三态一致；
  - `evidence` 三态一致。
- 该轮只加门禁，不改 runtime 行为或 schema 事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-162-schema-capability-state-metadata-invariant.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/schemaProvidedCapabilitiesKeepStableMetadataAcrossCapabilityStates`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 163 schema capability nextAction state invariant

- Round 163 在 Round 162 的三态 metadata 门禁基础上，新增三态 nextAction 可执行性门禁，防止 fallback 分支出现“字段齐全但命令不可执行”。
- 新增测试 `SchemaFactSourceTests.schemaProvidedCapabilitiesKeepSchemaBackedNextActionsAcrossCapabilityStates`。
- 对所有 `schema.providedCapabilities` 在 connected/disconnected/server-unreachable 三态统一校验：
  - capability 必须存在；
  - `group` 必须存在且不能是 `misc`；
  - `evidence` 不能为空；
  - `nextAction` 不能为空；
  - `nextAction.command/args` 必须通过 schema-backed argv 校验。
- 本轮只新增测试门禁，不改 runtime 行为或 schema 事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-163-schema-capability-nextaction-state-invariant.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/schemaProvidedCapabilitiesKeepSchemaBackedNextActionsAcrossCapabilityStates`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 164 capability supported/reason state machine

- Round 164 新增 capability `supported/reason` 的三态状态机门禁，防止 fallback 分支出现 unsupported 无原因或 reason 词汇漂移。
- 新增测试 `SchemaFactSourceTests.capabilitySupportedAndReasonKeepStableStateMachineInvariants`，覆盖 connected/disconnected/server-unreachable 三态：
  - `supported=true` 时 `reason` 必须为空；
  - `supported=false` 时 `reason` 必须非空；
  - unsupported reason 必须来自固定词汇表（runtime、webview provider、harmony-clear 边界、press 边界）。
- 额外锁定：
  - connected 态仅 `press` 和 `harmony-clear-text` 允许 unsupported；
  - `press` / `harmony-clear-text` 在三态都保持 unsupported 且 reason 不变；
  - 所有 schema capability 在 disconnected 与 server-unreachable 两态 reason 必须一致。
- 本轮只新增测试门禁，不改 runtime 行为或 schema 事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-164-capability-supported-reason-state-machine.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilitySupportedAndReasonKeepStableStateMachineInvariants`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 165 capability reason and nextAction state machine

- Round 165 把 capability `reason` 与 `nextAction` 的恢复路径耦合关系加入三态门禁，防止 runtime/webview provider 边界在 fallback 分支发生恢复动作漂移。
- 新增测试 `SchemaFactSourceTests.capabilityReasonAndNextActionKeepStableRecoveryTransitions`，覆盖 connected/disconnected/server-unreachable 三态。
- Runtime reason（`Requires connected embedded TritonKit runtime`）约束：
  - connected 态 reason 为空；
  - disconnected 若 nextAction 为 `status --json`，server-unreachable 必须升级为 `serve --host 127.0.0.1 --port 19421` 且 `requiresLongRunningProcess=true`；
  - disconnected 若为命令级 nextAction（非 status），则需与 connected 一致且非 long-running；server-unreachable 需与 disconnected 一致。
- WebView-provider reason（`Requires WebView provider metadata from embedded runtime or --runtime-base-url`）约束：
  - connected 态 reason 为空；
  - disconnected / server-unreachable 不得回退 `serve/status`；
  - disconnected 需与 connected 命令级 nextAction 一致；
  - server-unreachable 需与 disconnected 一致，且都非 long-running。
- 本轮只新增测试门禁，不改 runtime 行为或 schema 事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-165-capability-reason-nextaction-state-machine.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilityReasonAndNextActionKeepStableRecoveryTransitions`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 166 capability reason-family bidirectional guard

- Round 166 新增 capability reason 文本与 capability family 的双向门禁，防止 reason 语义变更后映射漂移。
- 新增测试 `SchemaFactSourceTests.capabilityReasonTextStaysBidirectionallyAlignedWithCapabilityFamilies`。
- `reason -> family` 约束：
  - runtime-reason 只能对应 runtime-family capability；
  - webview-provider-reason 只能对应 webview-provider family capability；
  - harmony-clear boundary reason 只能对应 `harmony-clear-text`；
  - press boundary reason 只能对应 `press`。
- `family -> reason` 反向约束（三态）：
  - runtime-family capability：connected 无 reason，disconnected/server-unreachable 必须是 runtime-reason；
  - webview-provider family capability：connected 无 reason，disconnected/server-unreachable 必须是 webview-provider-reason；
  - `press` / `harmony-clear-text` 三态 reason 必须保持边界文案不变。
- 本轮只新增测试门禁，不改 runtime 行为或 schema 事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-166-capability-reason-family-bidirectional-guard.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilityReasonTextStaysBidirectionallyAlignedWithCapabilityFamilies`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 167 reason-family taxonomy binding

- Round 167 把 reason family 与 `group/requiredBy/evidence` taxonomy 绑定为门禁，防止 reason 映射与规划/证据语义脱钩。
- 新增测试 `SchemaFactSourceTests.capabilityReasonFamiliesStayAlignedWithGroupWorkflowAndEvidenceTaxonomies`，覆盖 disconnected/server-unreachable 两态。
- Runtime reason family 约束：
  - `group` 必须在 `runtime|observe|assert|evidence|replay|action`；
  - `requiredBy` 不得包含 `webview-check`；
  - `evidence` 不能为空，且不得混入 webview/provider 证据键。
- WebView-provider reason family 约束：
  - `group` 必须在 `webview|route`；
  - `requiredBy` 必须包含 `webview-check`、`assert`、`evidence`；
  - `evidence` 必须包含 `webview-provider`。
- `press` / `harmony-clear-text` boundary 约束：
  - `group=action`；
  - `requiredBy` 包含 `action/assert/evidence`；
  - `evidence` 集合固定为 `unsupported-envelope + command-schema`。
- 本轮只新增测试门禁，不改 runtime 行为或 schema 事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-167-reason-family-taxonomy-binding.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilityReasonFamiliesStayAlignedWithGroupWorkflowAndEvidenceTaxonomies`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 168 requiredBy and nextAction.category alignment

- Round 168 新增 `requiredBy` workflow lane 与 `nextAction.category` 的一致性门禁。
- 新增测试 `SchemaFactSourceTests.capabilityRequiredByLanesStayAlignedWithNextActionCategories`。
- 新增约束：
  - `requiredBy` 含 `webview-check` => category 只能是 `observe|verify`；
  - `requiredBy` 含 `xcode|project` => category 只能是 `project|archive`；
  - `smoke-*` capability（且 lane 含 `smoke`）=> category 必须是 `smoke`；
  - `requiredBy` 含 `route` 或 `group=route` => category 只能是 `observe|verify`。
- 本轮只新增测试门禁，不改 runtime 行为或 schema 事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-168-requiredby-nextaction-category-alignment.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilityRequiredByLanesStayAlignedWithNextActionCategories`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 169 group and nextAction root alignment

- Round 169 新增 capability `group` 与 `nextAction.command` 根命令一致性门禁。
- 新增测试 `SchemaFactSourceTests.capabilityGroupStaysAlignedWithNextActionRootCommands`，对 capability 三态统一校验 `group -> allowed roots` 映射。
- 门禁初版命中了真实现状差异：`harmony-ax` 的 `group=host`，但 nextAction root 为 `ax`。测试规则已显式把 `ax` 纳入 host 允许集合，保留该 host-side 入口语义。
- 本轮只新增测试门禁，不改 runtime 行为或 schema 事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-169-group-nextaction-root-alignment.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilityGroupStaysAlignedWithNextActionRootCommands`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 170 lane-specific placeholder semantics

- Round 170 新增 `requiredBy` lane 与 `nextAction.args` 占位符语义门禁，避免 workflow lane 参数模板被过度泛化。
- 新增测试 `SchemaFactSourceTests.capabilityNextActionArgsKeepLaneSpecificPlaceholderSemantics`。
- 新增约束：
  - `route` lane（或 `group=route`）的 `route` 根命令必须包含 `<expected-url>`；
  - `smoke-ios` 必须包含 `ios/<device>/<bundle-id>/<url>/<text>`；
  - `smoke-harmony` 必须包含 `harmony/<device>/<bundle>/<ability>/<text>`。
- `webview-check` lane 收敛为 capability 级精确约束：
  - `route-current-url-assert`、`webview-wait` 必须带 URL/text 占位符；
  - `webview-bridge-call` 必须带 `<method>`；
  - `webview-current-url`、`webview-snapshot`、`webview-events` 不强制 URL/text 占位符。
- 本轮只修正测试门禁语义，不改 runtime 行为或 schema 事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-170-lane-placeholder-arg-semantics.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilityNextActionArgsKeepLaneSpecificPlaceholderSemantics`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 171 group machine-readable nextAction flags

- Round 171 新增 capability group 维度的 nextAction 输出模式门禁，防止恢复路径回退到非机器可读输出。
- 新增测试 `SchemaFactSourceTests.capabilityGroupsKeepMachineReadableNextActionOutputFlags`。
- 新增约束（覆盖 connected/disconnected/server-unreachable 三态）：
  - 除 `serve` 外，所有 capability 的 `nextAction.args` 必须至少包含一种机器可读出口：`--json`、`--jsonl`、`--format json` 或 `--metadata`。
  - `observe` 组的 `screenshot` nextAction 必须同时包含 `--metadata` 与 `<path.png>`，锁定结构化元数据与文件产物双轨语义。
- 本轮只新增测试门禁，不改 runtime 行为或 schema 事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-171-group-machine-readable-nextaction-flags.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilityGroupsKeepMachineReadableNextActionOutputFlags`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 172 output placeholder artifact contract

- Round 172 新增 capability `nextAction --output` 参数契约门禁，防止恢复命令输出路径语义漂移。
- 新增测试 `SchemaFactSourceTests.capabilityNextActionOutputPlaceholdersStayArtifactTypedAndExplicit`。
- 新增约束（三态统一）：
  - 任何带 `--output` 的 nextAction，后继值必须是 `<...>` 占位符 token；
  - 带 `--output` 的 capability 必须显式落在白名单中，并固定对应输出占位符类型（如 `<path.mov>`、`<path.ndjson>`、`<dir.tritonevidence>`、`<file.tritonplan>`、`<path.png>`）。
- 本轮测试开发中命中了真实差异：`ios-screenshot` 与 `harmony-screenshot` 也走 `--output <path>`。已补入契约白名单后回归通过。
- 本轮只新增测试门禁，不改 runtime 行为或 schema 事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-172-output-placeholder-artifact-contract.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilityNextActionOutputPlaceholdersStayArtifactTypedAndExplicit`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 173 target selector placeholder canonicalization

- Round 173 新增 capability `nextAction` 目标选择参数占位符门禁，锁定 `--device/--simulator/--bundle-id` 的 canonical token。
- 新增测试 `SchemaFactSourceTests.capabilityNextActionTargetSelectorPlaceholdersStayCanonical`。
- 新增约束（三态统一）：
  - `--device`：`smoke` 命令固定 `<device>`，其余命令固定 `<selector>`；
  - `--simulator`：固定 `<udid|booted>`；
  - `--bundle-id`：固定 `<bundle-id>`。
- 本轮只新增测试门禁，不改 runtime 行为或 schema 事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-173-target-selector-placeholder-canonicalization.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilityNextActionTargetSelectorPlaceholdersStayCanonical`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 174 platform flag canonicalization

- Round 174 新增 capability `nextAction --platform` 参数门禁，锁定跨平台能力的平台值语义。
- 新增测试 `SchemaFactSourceTests.capabilityNextActionPlatformFlagsStayCanonicalAndFamilyAligned`。
- 新增约束（三态统一）：
  - `--platform` 值只能是 `ios|harmony`；
  - `harmony-*` capability 若带 `--platform` 必须是 `harmony`；
  - `ios-*` capability 与 `observe-ios` 若带 `--platform` 必须是 `ios`。
- 本轮只新增测试门禁，不改 runtime 行为或 schema 事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-174-platform-flag-canonicalization.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilityNextActionPlatformFlagsStayCanonicalAndFamilyAligned`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Command surface optimization Round 175 text placeholder canonicalization

- Round 175 新增 capability `nextAction` 文本参数占位符门禁，统一 `--text` / `--wait-text` / `assert text-exists` 的 token 语义。
- 新增测试 `SchemaFactSourceTests.capabilityNextActionTextPlaceholdersStayCanonical`。
- 新增约束（三态统一）：
  - `--text` 后继值必须为 `<text>`；
  - `--wait-text` 后继值必须为 `<text>`；
  - `assert text-exists` 的文本参数必须为 `<text>`。
- 本轮只新增测试门禁，不改 runtime 行为或 schema 事实源。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-175-text-placeholder-canonicalization.md`。
- 本轮验证通过：
  - `swift test --package-path CLI --filter SchemaFactSourceTests/capabilityNextActionTextPlaceholdersStayCanonical`
  - `swift test --package-path CLI --filter SchemaFactSourceTests`
  - `docs-linhay/scripts/check-docs.sh`
  - `git diff --check`

## Simulator gate baseline（按用户要求指定）

- 新增模拟器测试门禁入口脚本：`docs-linhay/scripts/verify-simulator-gate.sh`。
- `quick` 模式（默认）：
  - `SimulatorAdvancedControlsTests`
  - `DeviceCrossPlatformTests`
  - `SchemaFactSourceTests` 中 selector/platform/text 三条 simulator 相关门禁
- `full` 模式：
  - 在 `quick` 基础上增加 `verify-ios-runtime-observe-smoke.sh` 与 `verify-ios-webview-harness.sh`
- 本轮已执行并通过：`docs-linhay/scripts/verify-simulator-gate.sh quick`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-176-simulator-test-gate.md`。

## Simulator gate port collision hardening

- `verify-simulator-gate.sh` 在调用 `verify-ios-runtime-observe-smoke.sh` 前改为自动注入随机 `TRITON_IOS_RUNTIME_SMOKE_PORT`，并输出端口值。
- 目标是降低连续轮询执行时固定端口冲突导致的噪声 traceback，不改变 quick/full 门禁结构。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-177-simulator-gate-port-collision-hardening.md`。

## Simulator gate port collision hardening

- Round 177 对 `verify-simulator-gate.sh` 增加 smoke 端口自避让：调用 `verify-ios-runtime-observe-smoke.sh` 前自动注入随机 `TRITON_IOS_RUNTIME_SMOKE_PORT`，并输出实际端口。
- 目标是避免连续轮询时固定端口冲突造成的 traceback 噪声，保证门禁日志稳定性。
- 本轮验证通过：`docs-linhay/scripts/verify-simulator-gate.sh quick`。
- 新增 checkpoint：`docs-linhay/spaces/20260527-command-surface-optimization/plans/checkpoints/20260601-round-177-simulator-gate-port-collision-hardening.md`。
