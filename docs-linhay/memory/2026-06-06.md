# 2026-06-06

## Subagent 完全访问权限

- 背景：用户要求 subagent 配置开启完全访问权限，避免 Android 支持并行执行时被 workspace-write sandbox 阻断本机 emulator、adb、worktree 或外部产物访问。
- 处理：`.codex/agents/tritonkit_android_{contract,fake_adb,device,app,observe_smoke}_agent.toml` 的 `sandbox_mode` 已统一从 `workspace-write` 改为 `danger-full-access`，并增加 `approval_policy = "never"`，与当前主控会话的完全访问执行口径一致。
- 沉淀：`.agents/skills/tritonkit-android-subagent-orchestration/SKILL.md` 已增加 Android subagent 配置必须使用完全访问权限的执行前置检查；该规则不升级到 `AGENTS.md`，因为它目前只适用于 Android Emulator subagent 编排。
- 边界：本次只调整 subagent 执行权限与编排 skill，不改变各 agent 的职责、写入面、Android Emulator 产品边界或主控 agent 的最终验收责任。
