# 2026-06-07

## Android smoke 真机闭环完成

- 在 AVD `Neptune_API_34`、target `emulator-5554`、alias `android-a` 上，真实 `smoke android` 已通过。
- 成功命令：
  - `./.build/cli/debug/triton smoke android --device android-a --package com.android.settings --wait-text 'Settings' --tap-text 'Network & internet' --post-tap-wait-text 'Internet' --screenshot docs-linhay/spaces/20260605-android-emulator-support/screenshots/20260607/android/20260607-android-smoke-network-after-v01.png --evidence /tmp/20260607-android-settings-smoke.tritonevidence --evidence-name android-settings-host-smoke --evidence-note 'settings launch and network navigation on Neptune_API_34' --json`
- 结果：
  - `status=pass`
  - `android.wait`、`android.tap`、`android.post-tap-wait`、`android.layout`、`android.screenshot`、`evidence.capture` 全部通过
  - assertions 为 `Settings` 与 `Internet`

## Android tap 错误映射修正

- 发现 Android host `tap --platform android <text>` 在文本未命中时，错误地落成 Harmony 专属 `harmony_layout_text_not_found`。
- 修正为统一返回 `text_not_found`，并给出 Android host observe 的恢复 hint。
- 新增定向测试：
  - `CLI/Tests/TritonKitCLITests/FailureDiagnosticsTests.swift`
- 回归：
  - `swift test --package-path CLI --scratch-path .build/cli --filter FailureDiagnosticsTests`
  - 通过。

## Android host observe 并发注意事项

- 同一台 emulator 上并发执行多条依赖 `uiautomator dump` + `adb shell cat` 的 host observe 命令时，曾复现 `Host command exited 137`。
- 串行执行 `launch -> wait -> tap -> post-wait -> screenshot -> smoke` 时，真实链路稳定通过。
- 后续 Android host-side 验证、smoke 和 evidence 收集应默认串行化 UIAutomator dump/read-back 操作，避免把同一 target 的 XML 临时文件互相覆盖或争用。

## 本地验证与环境备注

- `swift build --package-path CLI --scratch-path .build/cli --product triton` 已通过。
- CLI 定向测试偶发受 macOS system policy 影响时，需要先执行：
  - `codesign -s - .build/cli/arm64-apple-macosx/debug/TritonKitCLIPackageTests.xctest/Contents/MacOS/TritonKitCLIPackageTests`
- 2026-06-07 新增证据：
  - `docs-linhay/spaces/20260605-android-emulator-support/screenshots/20260607/android/20260607-android-network-after-v01.png`
  - `docs-linhay/spaces/20260605-android-emulator-support/screenshots/20260607/android/20260607-android-smoke-network-after-v01.png`
  - `/tmp/20260607-android-settings-smoke.tritonevidence`

## Android 提交前治理

- 为避免真实 emulator smoke 产生的 repo-local target 状态污染提交，`.gitignore` 已忽略：
  - `.triton/host-defaults.json`
  - `.triton/host-targets.json`
- Android 专属 shared host adapter 代码已从 `TKHostAdapterModels.swift` 拆入 `TKAndroidADBSupport.swift`；Android CLI action 辅助逻辑已拆入 `CLIAndroidActionRuntime.swift`。
- 拆分后行数回到治理线以下：
  - `Sources/TritonKitCLI/CLIActionCommands.swift`: 1454 行
  - `Sources/TritonKitShared/TKHostAdapterModels.swift`: 1311 行
- 拆分后回归：
  - `swift test --filter TKHostAdapterModelsTests` 通过。
  - `swift test --package-path CLI --scratch-path .build/cli --filter DeviceCrossPlatformTests` 通过。
  - `docs-linhay/scripts/verify.sh --local` 通过。

## 主仓合并后门禁修复

- Android feature fast-forward 合入主仓后，主仓 `docs-linhay/scripts/verify.sh --local` 暴露 `TKPlatformFallbackTests` 在 Swift Testing 并发执行下会偶发重复收到 `.disconnected` 状态，并在 suite 级重跑时触发 `stateObservers` 字典并发读写崩溃。
- 修复：
  - `TritonKit.state` 只有状态真实变化时才通知 delegate / state observers。
  - `stateObservers` 与 `errorObservers` 增加锁保护，注册、取消和通知时先拷贝 observer 列表再回调。
  - `stateObserverToken` 测试增加重复 `stop()` 不应重复广播当前状态的断言。
- 回归：
  - `swift test --filter TKPlatformFallbackTests` 通过。
  - 主仓 `docs-linhay/scripts/verify.sh --local` 通过。

## 会话整理：多 worktree 收尾

- 本轮用户要求检查其他 worktree，结束后合并到主分支并删除；实际处理了 issue 26、issue 27 与 Android Emulator support 三个 worktree。
- 稳定可复用模式已沉淀到 `.agents/skills/tritonkit-ops-governance/SKILL.md`：
  - 先枚举 registered worktree 和 clean status。
  - 用 `git log main..<branch>`、`git merge-base --is-ancestor`、`git merge-tree --write-tree main <branch>` 区分已包含、可合入和有冲突的分支。
  - 合入主仓并跑主仓门禁后，再 `git worktree remove` 删除已结束 worktree。
  - 只删除 registered worktree 与确认为空的包装目录；不默认删除本地或远端分支。
- 不升级 `AGENTS.md`：现有 AGENTS 已有 worktree 路径、合并后删除与 space 保留规则；本次沉淀属于执行顺序细化，放在内部治理 skill 更合适。

## v0.1.17 发布

- 用户要求推送并发版；`main` 已推送到 `origin/main`，随后执行：
  - `docs-linhay/scripts/release.sh v0.1.17 --yes`
- 本地 release preflight 已通过：
  - `docs-linhay/scripts/verify.sh --local`
  - Swift tests、release CLI build/smoke、Harmony host smoke、iOS runtime observe smoke、iOS Simulator build、docs structure、whitespace check
- GitHub Actions run:
  - `https://github.com/NeptuneKit/TritonKit/actions/runs/27091786360`
- GitHub Release:
  - `https://github.com/NeptuneKit/TritonKit/releases/tag/v0.1.17`
- 已验证发布闭环：
  - `triton-macos-arm64.tar.gz`
  - `triton-macos-x86_64.tar.gz`
  - `tritonkit-skills.tar.gz`
  - `tritonkit_checksums.txt`
  - Homebrew: `brew install NeptuneKit/tap/triton`
- `Build CLI (x86_64)` backfill job 已在同一 GitHub Actions run 中完成，Release checksum manifest 已后补刷新。
