# 2026-06-08

## Android 支持边界确认

- 用户追问“是否已经完整支持安卓”；结论需要按当前产品边界回答。
- 已完整闭环并发布的是本机 Android Emulator host-side CLI 接管能力：
  - `triton device --platform android` 的 doctor / list / alias / use / wait-ready / screenshot。
  - `triton app --platform android` 的 list / info / install / uninstall / launch / terminate / open-url。
  - Android host-side `observe tree`、`wait`、`tap` 和 `smoke android`。
  - 真实 AVD `Neptune_API_34`、adb serial `emulator-5554`、alias `android-a` 上的 Settings smoke / evidence / screenshot 验收。
  - `v0.1.17` 已发布并包含 macOS arm64 / x86_64 CLI 包、skills 包和 checksum manifest。
- 不应表述为“整个 Android 平台完全支持”：
  - Android 真机、USB 授权、设备农场、远端 adb server、远端 agent 不在当前范围。
  - Android embedded runtime SDK 不在当前范围。
  - Web / Wails UI、多 emulator 并发 UIAutomator 调度、内置智能重定位或 VLM loop 不在当前范围。
- 后续对外口径：说“Android Emulator 接管能力已完整支持并发版”，同时明确它是本机 emulator host-side 支持。

## iOS 真机接入规划

- 用户要求规划接入真机方案；按现有 AGENTS 边界，真机不能直接并入 emulator takeover，已新建独立 space：`docs-linhay/spaces/20260608-ios-real-device-takeover/`。
- 方案文档包括入口 README、`technical-design.md` 与 `plans/p0-p2-plan.md`，覆盖 BDD 场景、机器可读契约、P0-P2 分期和验收命令。
- 核心决策：
  - 范围限定为本机 Mac + 本机连接或配对的 iOS/iPadOS 真机，不做远端 agent、设备云、Web/Wails UI 或系统级 HID。
  - 首期复用 `triton device/app/xcode/logs/evidence`，新增 real-device scope/kind；不新增平行产品入口。
  - `devicectl` 只消费 `--json-output <path>` 的稳定 JSON 文件，stdout 不作为 agent 解析契约。
  - signing / provisioning / trust / Developer Mode / DDI 等阻塞要映射为稳定 JSON 错误码和 nextAction，Triton 不自动修改 Apple 账号、证书或 Xcode signing 资产。
  - 真机业务验证依赖 Debug embedded runtime；Release 仍保持 no-op，系统级截图/点按/隐私弹窗自动化不进入 P0-P2。

## 三端真机总方案

- 用户随后要求“三端一起规划”；已新建三端总入口 `docs-linhay/spaces/20260608-cross-platform-real-device-takeover/`，覆盖 iOS/iPadOS 真机、Android 真机、HarmonyOS 真机。
- 原 iOS space 保留为 Apple / `devicectl` 细化材料，并在 README 顶部标注后续边界以三端总方案为准。
- 三端共同决策：
  - 仍限定本机开发机可见真机，不做远端 agent、设备云、真机农场、Web/Wails UI 或多租户调度。
  - agent 面统一复用 `triton device/app/smoke/evidence`，新增 `--scope real` 与 `kind=real-device`；Android/Harmony emulator 继续走 `--scope emulator`，iOS Simulator 走 `--scope simulator`。
  - 三端 host tools 分别是 `devicectl` / `adb` / `hdc`，全部作为实现细节，输出必须归一为 JSON/JSONL envelope 和 artifact。
  - P0 只读发现和 ready 诊断；P1 install/launch/open-url + wait/assert/evidence；P2 才做 Xcode / Gradle / hvigor build-run 编排；P3 再做日志、截图、录屏、调试等显式 opt-in 能力。
  - launch/open-url 成功只证明 host action 已提交；业务通过必须由 runtime、host layout、wait/assert 或 evidence 证明。

## 三端真机 subagent 编排

- 用户要求“编写 subagent”；已新增 7 个项目级 Codex subagent 配置：
  - `.codex/agents/tritonkit_real_device_contract_agent.toml`
  - `.codex/agents/tritonkit_ios_real_device_agent.toml`
  - `.codex/agents/tritonkit_android_real_device_agent.toml`
  - `.codex/agents/tritonkit_harmony_real_device_agent.toml`
  - `.codex/agents/tritonkit_real_device_app_agent.toml`
  - `.codex/agents/tritonkit_real_device_smoke_evidence_agent.toml`
  - `.codex/agents/tritonkit_real_device_build_agent.toml`
- 新增内部 orchestration skill：`.agents/skills/tritonkit-real-device-subagent-orchestration/SKILL.md`，并更新 `.agents/skills/README.md`。
- 批次：P0 Contract / iOS / Android / Harmony device agents 可并行；P1 App + Smoke/Evidence 在 target resolver 稳定后启动；P2 Build agent 在 P0/P1 surface 稳定后启动。
- 已用 `tomli` 校验 TOML 可解析，且全部使用 `sandbox_mode="danger-full-access"` 与 `approval_policy="never"`。
- 用户指出“目前没见接入真机”，已补充更具体的 subagent 执行计划：`docs-linhay/spaces/20260608-cross-platform-real-device-takeover/plans/20260608-subagent-execution-plan-v01.md`。该计划明确 Batch 0-3、每个 subagent 的任务包、主要文件写入面、不得修改区域、红灯测试、交付物、Gate 1/2/3 集成命令、真实设备 smoke 跳过条件和派发 prompt。
- 随后按 `tritonkit-subagent-supervision` 正式启动 Batch 1 并发执行，已派发 4 个 subagent：RealContract `019ea333-4945-7471-9ba5-01b121ec2968`、iOSDevice `019ea333-7ca2-7863-a1b9-9dea8b1b588f`、ADBReal `019ea333-7e76-7582-897d-0bf2fb75856d`、HDCReal `019ea333-842c-7410-9c54-89ca2e1b3df8`。调度记录写入 `docs-linhay/spaces/20260608-cross-platform-real-device-takeover/batch-1-dispatch-20260608.md`。
- Batch 1 四个 subagent 已返回，主控完成 Gate 1 集成验证：
  - `TritonKitSharedTests.TKHostAdapterModelsTests|TritonKitSharedTests.TKAndroidADBFixturesTests`：39 tests 通过。
  - `DeviceCrossPlatformTests`：23 tests 通过。
  - `SchemaFactSourceTests`：106 tests 通过。
  - `FailureDiagnosticsTests`：7 tests 通过。
  - `EvidenceBundleTests`：6 tests 通过。
  - `swift build --package-path CLI --scratch-path .build/cli --product triton` 通过。
  - `triton schema --command device/app/smoke/evidence --json` 已抽样确认 real-device selector、`--scope real`、三端错误码和 evidence taxonomy 暴露。
- 当前准确状态：P0 target 发现、scope/kind 契约、ready 诊断和 schema 入口已集成；完整真机接入仍需 Batch 2 App Lifecycle / Smoke Evidence 与后续 Gate 2/真实设备 smoke。
- 已启动 Batch 2 并发执行：
  - App Lifecycle `019ea356-f7f0-78c0-b07b-b5b6c81ec050`，负责 P1 三端 real-device install / launch / open-url / terminate / info/list。
  - Smoke Evidence `019ea356-f8d8-7ea3-b72d-ad2da5a79d10`，负责 P1 三端 real-device smoke / wait / assert / evidence proof path。
- Batch 2 调度记录：`docs-linhay/spaces/20260608-cross-platform-real-device-takeover/batch-2-dispatch-20260608.md`。后续只有 Gate 2 通过，才能声明 P1 app/smoke/evidence 契约完成；真实设备 smoke 仍需本机设备与 Debug artifact。
- Batch 2 两个 subagent 已返回，主控完成 Gate 2 集成验证：
  - Shared focused：43 tests 通过。
  - CLI focused：8 suites / 152 tests 通过。
  - `SchemaFactSourceTests` schema recheck：106 tests 通过。
  - `swift build --package-path CLI --scratch-path .build/cli --product triton` 通过。
  - `triton schema --command device/app/smoke/evidence --json` 已抽样确认 `--scope real`、`hostAction.proofSource`、`hostAction.businessReady`、`steps[].proofSource`、`steps[].businessReady`、`real-device.diagnostics` 暴露。
- 主控修正了一个 Batch 2 集成漏口：`SmokeRunSummary` 模型已有 `steps[].proofSource` / `steps[].businessReady`，但 schema output contract 原先未显式暴露；已补入 `CLISchemaContracts.swift` 并用 `SchemaFactSourceTests` 覆盖。
- 本机真机探测结果：
  - iOS `devicectl` 可返回 real-device envelope，共 11 个记录，但均为 offline / DDI missing。
  - Android real-device 列表为空。
  - Harmony real-device 列表为空。
  - 因没有 ready 真机和 Debug artifact，本轮真实设备 install / launch / smoke 记为环境 skipped，不能宣称实机 smoke 已通过。
- 当前准确状态：P0 target 发现和 P1 app/smoke/evidence 机器可读契约已集成；完整三端真机接入仍需 Batch 3 build-run、ready 真机补验和 1500 行以上 Swift 文件拆分治理。
- 已启动 Batch 3 Build subagent：`019ea370-48f7-7193-af44-6e69a0087e81`，负责 P2 iOS Xcode real-device build-run、Android Gradle、Harmony hvigor build-run 机器可读契约。
- Batch 3 调度记录：`docs-linhay/spaces/20260608-cross-platform-real-device-takeover/batch-3-dispatch-20260608.md`。
- 启动前事实：`triton schema --command xcode --json` 可用但仍偏 simulator-oriented；`triton schema --command build --json` 当前返回 `unknown_command_schema`，属于 Batch 3 待补契约。
- Batch 3 Build subagent 长时间无响应，主控 interrupt 后仍未拿到可集成回报；主控已接管最小 P2 schema 契约切片。
- 已新增 `CLISchemaBuildCommands.swift` 并注册 `build` schema；`xcode` schema 已补 iOS real-device `--device`、`sdk=iphoneos` examples、签名/provisioning/DDI 失败码与 `xcode.final.device` 输出字段。
- 已验证：
  - `swift build --package-path CLI --scratch-path .build/cli --product triton` 通过。
  - `swift test --filter TKXcodeWorkflowModelsTests`：13 tests 通过。
  - `swift test --package-path CLI --scratch-path .build/cli --filter SchemaFactSourceTests`：107 tests 通过。
  - 三端真机 focused CLI gate：`DeviceCrossPlatformTests|AppOpenURLFlowTests|SmokeRuntimeTests|SmokeAndroidRuntimeTests|SmokeHarmonyRuntimeTests|EvidenceBundleTests|FailureDiagnosticsTests|SchemaFactSourceTests`，8 suites / 153 tests 通过。
  - `triton schema --command xcode --json` 抽样确认 `--device`、`iphoneos`、`xcode_signing_failed` 暴露。
  - `triton schema --command build --json` 抽样确认 `gradle_not_found`、`hvigor_not_found`、`build.progress`、`build.final` 暴露。
- 当前准确状态：P0 target 发现、P1 app/smoke/evidence、P2 build-run schema 契约已集成；但 `build` 仍是机器可读契约，不是实际 Gradle / hvigor / Xcode real-device runner。完整三端真机接入仍需 runner 实现、ready 真机与 Debug artifact 补验，以及 `CLIHostCommands.swift`、`CLIHostRuntime.swift`、`CLISmokeRuntime.swift`、`CLIEvidenceRuntime.swift` 的 1500 行治理。
- 收尾跑 `docs-linhay/scripts/verify.sh --local` 时发现 Harmony host smoke 负例回归：`triton app install --platform harmony` 缺少 `--hap` 时返回 `host_action_failed`，而 schema/脚本期望参数错误为 `validation_failed`。主控已在 `failHostCommand` 中补 `ValidationError -> validation_failed` 映射，并用 `FailureDiagnosticsTests` 覆盖。
- 最终门禁：
  - `swift test --package-path CLI --scratch-path .build/cli --filter FailureDiagnosticsTests`：8 tests 通过。
  - `env TRITON_BIN=.build/cli/debug/triton docs-linhay/scripts/verify-harmony-host-smoke.sh /tmp/triton-harmony-host-smoke-debug-2` 通过。
  - `docs-linhay/scripts/check-docs.sh` 通过。
  - `docs-linhay/scripts/check-docs.sh` 通过。
  - `docs-linhay/scripts/verify.sh --local` 通过，覆盖 SwiftPM dependency boundary、iOS DEBUG isolation、Swift tests 166 tests、release CLI build/smoke、Harmony host smoke、iOS runtime observe smoke、iOS Simulator build、docs structure 和 git diff whitespace check。
- 主控随后继续推进 P2 runner：再次派发 Build subagent `019ea50d-f0d7-7970-b3d8-07a66932c880`，但 6 分钟内未返回，关闭时仍为 running；该 agent 关闭前留下半截 `CLIBuildModels.swift` / `BuildRunnerTests.swift`，主控保留并修正后继续接管。
- 已新增实际 `triton build` runner：
  - `Sources/TritonKitCLI/CLIBuildModels.swift`
  - `Sources/TritonKitCLI/CLIBuildRuntime.swift`
  - `Sources/TritonKitCLI/CLIBuildCommands.swift`
  - `Sources/TritonKitCLI/TritonKitCLI.swift` 注册 `Build.self`
  - `CLI/Tests/TritonKitCLITests/BuildRuntimeTests.swift`
  - `CLI/Tests/TritonKitCLITests/BuildRunnerTests.swift`
- 当前 `triton build android --project <dir> --gradle <path> --variant debug --json` 可执行 Gradle / gradlew、记录 stdout/stderr log、发现 APK artifact，并给出 `triton app install ... --apk <path>` nextAction。
- 当前 `triton build harmony --project <dir> --hvigor <path> --module entry --mode debug --json` 可执行 hvigor / hvigorw、记录 stdout/stderr log、发现 HAP artifact，并给出 `triton app install ... --hap <path>` nextAction。
- 当时 `triton build ios` 尚未新增；iOS real-device build 仍由 `triton xcode build --device <ios-real-target> --sdk iphoneos --jsonl` 承担。
- P2 runner 验证：
  - `swift test --package-path CLI --scratch-path .build/cli --filter 'BuildRuntimeTests|BuildRunnerTests'`：2 suites / 9 tests 通过。
  - `swift build --package-path CLI --scratch-path .build/cli --product triton` 通过。
  - fake Gradle CLI smoke：`triton build android ... --json` 输出 `build.android`、`artifactKind=apk`、`nextAction.command=app`。
  - fake hvigor CLI smoke：`triton build harmony ... --json` 输出 `build.harmony`、`artifactKind=hap`、`nextAction.command=app`。
  - P2 runner focused gate：`BuildRuntimeTests|BuildRunnerTests|SchemaFactSourceTests|DeviceCrossPlatformTests|AppOpenURLFlowTests|SmokeRuntimeTests|SmokeAndroidRuntimeTests|SmokeHarmonyRuntimeTests|EvidenceBundleTests|FailureDiagnosticsTests`，10 suites / 163 tests 通过。
  - runner 落地后重新执行 `docs-linhay/scripts/verify.sh --local` 通过，覆盖 SwiftPM dependency boundary、iOS DEBUG isolation、Swift tests 166 tests、release CLI build/smoke、Harmony host smoke、iOS runtime observe smoke、iOS Simulator build、docs structure 和 git diff whitespace check。
- 当时准确状态：P0 target 发现、P1 app/smoke/evidence、P2 build schema、Android/Harmony build runner 已集成；完整三端真机接入仍需 ready 真机 + 真实项目 artifact 补验、`triton build ios` 聚合入口取舍，以及 1500 行以上 Swift 文件拆分治理。
- 主控继续收口并补上 `triton build ios` 聚合入口：
  - `BuildIOS` 复用既有 Xcode runner，支持 `--workspace` / `--project`、`--scheme`、`--device`、`--destination`、`--sdk`、`--derived-data-path`、`--timeout`。
  - `--device <udid>` 默认合成为 `destination=platform=iOS,id=<udid>`；有 `--device` 时默认 `sdk=iphoneos`。
  - 输出从 `TKXcodeActionSummary` 适配为统一 `TKBuildActionSummary`，`action=build.ios`、`platform=ios`、成功 artifact 为 `.app`，nextAction 指向 `triton app install --platform ios --app <path.app>`。
  - `build ios` schema 使用 `oneOfRequiredOptions=[["--workspace","--project"]]`，JSONL final event 为 `build.ios.summary`，outputSelectors 复用父级 `build.progress` / `build.final`。
  - `triton build ios --jsonl` 参数负例已抽样，缺 workspace/project 时返回 `invalid_workspace_path`，nextAction 为 `triton xcode discover --path . --json`。
- P2 收口验证：
  - `swift test --package-path CLI --scratch-path .build/cli --filter 'BuildRuntimeTests|BuildRunnerTests|SchemaFactSourceTests|TKXcodeWorkflowModelsTests'`：3 suites / 116 tests 通过。
  - 三端 focused gate：`BuildRuntimeTests|BuildRunnerTests|SchemaFactSourceTests|DeviceCrossPlatformTests|AppOpenURLFlowTests|SmokeRuntimeTests|SmokeAndroidRuntimeTests|SmokeHarmonyRuntimeTests|EvidenceBundleTests|FailureDiagnosticsTests`：10 suites / 163 tests 通过。
  - `.build/cli/debug/triton schema --command build --json` 抽样确认 `ios`、`android`、`harmony` 子命令、`build.ios.summary`、`build.progress`、`build.final`。
  - `swift build --package-path CLI --scratch-path .build/cli --product triton` 通过。
  - `docs-linhay/scripts/verify.sh --local` 通过，覆盖 SwiftPM dependency boundary、iOS DEBUG isolation、Swift tests 166 tests、release CLI build/smoke、Harmony host smoke、iOS runtime observe smoke、iOS Simulator build、docs structure 和 git diff whitespace check。
- 当前准确状态更新：P0 target 发现、P1 app/smoke/evidence、P2 build schema、iOS build 聚合入口、Android/Harmony build runner 已集成；完整三端真机接入仍需 ready 真机 + 真实项目 artifact 补验，以及 1500 行以上 Swift 文件拆分治理。
- Batch 4 行数治理已完成：
  - HostCommands / HostRuntime / SmokeRuntime / EvidenceRuntime subagent 已将原超线源文件拆分为职责文件；主控随后补充派发 SchemaFactSourceTests split subagent `019ea606-d7bf-76c0-a43a-3e9ff8edcc0e`，拆分超线 schema fact source 测试。
  - 拆分后原始超线文件行数：`CLIHostCommands.swift` 33、`CLIHostRuntime.swift` 535、`CLISmokeRuntime.swift` 160、`CLIEvidenceRuntime.swift` 505、`SchemaFactSourceTests.swift` 742。
  - 当前 Swift 文件最大行数为 `Sources/TritonKitCLI/CLIActionCommands.swift` 1454；无 Swift 文件超过 1500 行。
  - Batch 4 调度与完成记录已写入 `docs-linhay/spaces/20260608-cross-platform-real-device-takeover/batch-4-line-governance-dispatch-20260608.md`。
- 当前准确状态更新：P0 target 发现、P1 app/smoke/evidence、P2 build schema、iOS build 聚合入口、Android/Harmony build runner、1500 行以上 Swift 文件治理均已集成；完整三端真机接入仍需 ready iOS/Android/Harmony 真机和真实 Debug artifact 补验。因本机没有 ready 真机与 artifact，不能宣称真实设备 smoke 已通过。
- 新增 single-device Web preview：
  - `triton serve` 现在暴露 `/web/device`，返回自包含 HTML 页面并在启动日志打印 Web URL。
  - 页面读取 `/status`、`/targets`，展示单个 runtime target 的身份、cache state、geometry/screenshot preview，并通过 `/input?target=<id>` 支持 tap、swipe、type、paste、clear。
  - `/geometry`、`/accessibility`、`/hit`、`/screenshot`、`/input` 已接受 `target` query，避免多 target 时 Web 操作隐式走默认 target。
  - 本轮刻意只支持 exactly one connected target；0 target 显示 empty state，>1 target 只读并禁用操作。
  - 验证：`swift test --filter SingleDeviceWebPageTests` 2 tests 通过；浏览器 smoke 打开 `http://127.0.0.1:19421/web/device` 无 console error。当前本机 serve 连接了两个 app runtime target（同一 simulator UDID，`Overloaded` 与 `丁香园`），页面按设计显示 `2 targets` 并进入 read-only single-device-only state。
  - 单设备 smoke 补验：临时终止 `cn.dxy.iDxyer` 后 `/status` 为 `targetCount=1`，Web 详情页显示 `connected` / `1 target` / `Overloaded` / `overloaded.cn.debug`，截图预览 natural size 为 `402x874`，操作按钮均可用。
  - Web 操作补验：点击预览图空白导航区域会经 `/input?target=...` 写回 Action Result，命中 `UIView` 并返回非 UIControl 的预期失败；随后点击 `Swipe Down` 返回 `ok=true`，命中 `UITableView` 并写回 Action Result。浏览器 console error 为空。
  - 验收截图：`docs-linhay/spaces/20260608-single-device-web-preview/screenshots/20260608/20260608-web-device-single-target-after-v01.png`。

## 远端 main 合并补充

- 合并 iOS media playback helpers：runtime snapshot 新增 `media` section，用公开 UIKit / AVFoundation / AVKit 可见信号总结 media surfaces、AX playback controls、automation confidence、fallback advice 和 evidence commands。
- 合并 semantic provider capability contracts：runtime manifest 广告 `semanticDomains[]`，snapshot 可读取 app-owned semantic state 和 action catalog；该能力只表达 provider-backed business facts，不从 AX/layout/screenshot 推断业务 readiness。
- 合并 issue #34 prefs Data 契约：`triton app prefs get/dump/set` 对 plist `Data` 值输出/写入稳定 envelope，并暴露 stored plist type，避免 agent 无提示把二进制 preference 改成 array/dictionary。
- 合并 selector flag 兼容与 issue #39 capture target propagation：host/action/observe/evidence 入口继续接受统一 `--device` selector，evidence/capture 期间 runtime request 必须携带已解析 target，避免多 target 时 capture 错到默认 target。
