# 2026-06-09

## Issue 30 Android contract compatibility

- 为 GitHub issue #30 做字面契约收口：`triton app inspect --platform android --bundle <package> --json` 作为 Android `dumpsys package` metadata inspection 兼容入口，输出 `HostAppInfoOutput` / `host.android-app-inspect`。
- `triton ax --platform android --device <selector> --json` 作为 Android UIAutomator layout 兼容入口，复用 `observeAndroid` / `hostCaptureAndroidLayout`，输出 `HostAndroidArtifactOutput` / `host.android-ax`。
- schema 显式暴露 Android host platform-specific contracts：`host.android-device`、`host.android-app-inspect`、`host.android-app-install`、`host.android-app-launch`、`host.android-screenshot`、`host.android-ax`、`host.android-tap`、`host.android-wait`、`host.android-key-action`。
- `CLI/Package.swift` 使用显式 package identity `.package(name: "tritonkit", path: "..")`，避免 feature worktree 目录名不是 `TritonKit` 时 SwiftPM 解析本地依赖失败。

## Issue 31: Open URL Plan Recovery Workflow

- 在 `feat/20260609-issue-31-open-url-plan` 中为 `triton plan open-url ... --json` 增加 `afterRecoverySteps[]` 契约：当 server 不可达导致 `mode=bootstrap` 时，`steps[]` 仍保留恢复步骤，目标 open-url workflow 放入恢复后步骤，避免 agent 丢失 open-url / wait / evidence 链路。
- `plan open-url` 支持 Harmony 规划输入：`--platform harmony`、`--bundle`、`--ability`、可选 `--hap`。提供 `--hap` 时计划包含 `install-app`，随后是 `app-open-url`、`wait-text`、`capture-screenshot`、`evidence-summary`，所有步骤暴露 schema-backed `argv[]`。
- `CLI/Package.swift` 的本地 path dependency 显式命名为 `tritonkit`，避免 issue worktree 目录名不是 `TritonKit` 时 SwiftPM 无法解析 `TritonKitCLI` 对根 package product 的依赖。
- 纠偏记录：本轮最初误把 issue space README 写到了主仓 untracked 目录，已复制到 `/Users/linhey/Desktop/linhay-open-sources/TritonKit-worktrees/20260609-issue-31-open-url-plan` 并仅从主仓移除 `docs-linhay/spaces/20260609-issue-31-open-url-plan/`，未触碰主仓其他改动。
