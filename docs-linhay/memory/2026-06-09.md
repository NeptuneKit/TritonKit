# 2026-06-09

## GitHub #38 Semantic Provider Capabilities 收口

- 在 `feat/20260608-semantic-provider-capabilities` worktree 继续收口 #38 第一片，没有改主仓。
- BDD 场景补齐到 manifest domain/source 广告、CLI capability query/assert/action 发现元数据、snapshot semantic 输出和无 provider warning。
- Runtime manifest 新增 `semanticDomains[]`，只广告 provider domain、source/confidence、schema、action catalog、redaction 与 evidence commands，不携带当前 state 值；当前 state 仍通过 `triton snapshot --include semantic,app,scene --json` 获取。
- Snapshot semantic 输出保留 provider-backed typed facts、redaction metadata 和 action descriptors；`app-semantic-action` 仍只表示 action catalog discoverability，不表示 generic provider action execution 已实现。
- 已同步 README、`docs-linhay/dev/ai-cli-readable-control.md`、`tritonkit-dev-feedback`、`tritonkit-real-project-regression` 与 space README。
