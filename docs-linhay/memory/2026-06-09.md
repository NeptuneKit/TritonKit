# 2026-06-09

- iOS 网络接管边界决策：后续做 host-side Simulator proxy 接管，不和 App 内 runtime / SDK 做网络交易。
  - 目标入口是 `triton sim proxy start/status/export/stop --json`，输出 proxy endpoint、证书状态、simulator proxy 配置状态、capture/mock/block 模式、请求 artifact 和 limitations。
  - 明确不要求业务 App 集成 `URLProtocol`、method swizzling、SDK network interceptor 或 network breadcrumbs provider。
  - 证书 pinning、不走系统代理、自定义 socket、私有加密协议或不支持的 QUIC 路径必须返回 explicit limited / unsupported，不伪装为全量网络可见。
  - 已同步 `docs-linhay/spaces/20260520-simulator-takeover/README.md`、`technical-design.md`、`docs-linhay/dev/ai-cli-readable-control.md`、host simulator takeover skill 与 emulator CLI takeover skill。
- 按用户要求拉取 Rockxy 作为 Simulator proxy 参考项目：
  - 本地位置：`docs-linhay/references/rockxy/`。
  - 索引文档：`docs-linhay/references/rockxy.md`。
  - 快照 commit：`120a4428feb1a436677c7b8a9c7332f38ae6b0e0`。
  - 重点参考 `ProxyConfigurator.swift`、`RockxyHelperProtocol.swift`、`HelperService.swift`、MCP bridge、证书信任、proxy restore、bypass、HAR / capture / redaction 等 host-side 代理工程经验。
- 按用户要求拉取 Atlantis 作为应用内流量接管参考项目：
  - 本地位置：`docs-linhay/references/atlantis/`。
  - 索引文档：`docs-linhay/references/atlantis.md`。
  - 快照 commit：`6427600ec87d516a43754e4eaa7762f3271ac77f`。
  - 重点参考 `Atlantis.start(...)`、URLSession / WebSocket method swizzling、Bonjour / simulator direct TCP transport、manual traffic add API、gRPC interceptor 示例、Info.plist local network / Bonjour 要求。
  - 明确该参考属于 App 内 opt-in 方案，不改变当前 `triton sim proxy ...` host-side Simulator proxy 主线边界。
- 新建三端网络接管规划 space：`docs-linhay/spaces/20260609-three-platform-network-takeover/`。
  - 规划入口：`README.md`。
  - 实施计划：`plans/20260609-three-platform-network-takeover-plan-v01.md`。
  - 决策：三端统一入口使用 `triton device proxy ... --platform ios|android|harmony --json`，iOS 保留 `triton sim proxy ...` 别名。
  - 默认主线：host-side proxy takeover；App-internal traffic takeover 仅作为 explicit opt-in runtime lane，借鉴 Atlantis，不默认注入业务 App。
  - P0 要同时暴露 iOS / Android / Harmony capabilities / doctor / schema 骨架；实现按 iOS、Android、Harmony 真实 smoke 分期推进。
- 三端网络接管 P0 机器可读契约骨架已落地：
  - `device` schema 暴露 `proxy doctor/start/status/export/stop` usage forms、examples、`proxy_visibility_limited` / `proxy_platform_not_supported` failure codes 与 `host.device-proxy` output contract。
  - capabilities matrix 暴露 `device-proxy-ios`、`device-proxy-android`、`device-proxy-harmony` 与 `network-capture-export`，每项都有 `group/requiredBy/nextAction/evidence`；`network-capture-export` 作为 evidence 能力，执行入口保持 `device proxy export --platform <platform> --device <selector> --output <path.har|path.ndjson> --json`。
  - taxonomy 增补 `network-capture`、`proxy-restore`、`host-device-proxy` 与 `proxy_visibility_limited` 恢复分类。
  - 当前仅完成 schema / capability / output contract 发现层，不包含真实 `device proxy` runner、平台代理配置、证书安装、capture export 或 restore 行为。
  - 验证记录：`swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 24 tests；`swift test --package-path CLI --filter SchemaFactSource` 通过 107 tests；`swift test --package-path CLI` 通过 230 tests。
- 三端网络接管 P0 安全 CLI skeleton 已继续落地：
  - 新增 `CLIHostDeviceProxyRuntime.swift` 作为执行期 proxy DTO / safe runtime，新增 `device proxy doctor/status/start/export/stop` ArgumentParser 命令接线。
  - `triton device proxy doctor --platform ios|android|harmony --json` 可执行并返回 `ok=true`、`surface=host.device-proxy`、`lane=host-proxy`、limitations。
  - `triton device proxy status --platform ios|android|harmony --device <selector> --json` 可执行并返回未运行状态，不伪装真实代理已接管。
  - `triton device proxy start/export/stop ... --json` 当前稳定返回单个 JSON error envelope，`error.code=proxy_platform_not_supported`，并以非 0 退出，避免误报真实网络接管成功。
  - 验证记录：`swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 26 tests；`swift test --package-path CLI --filter SchemaFactSource` 通过 107 tests；`swift test --package-path CLI` 通过 234 tests。
  - CLI smoke：`doctor --platform android` exit 0；`status --platform harmony --device harmony-a` exit 0；`start --platform ios --device iphone15 --mode record --output /tmp/ios-network` 输出 JSON 并 exit 1。
- 三端网络接管 P0 补齐 iOS `sim proxy` alias：
  - `triton sim proxy doctor/start/status/export/stop --simulator <udid|booted> --json` 已接入 `sim` 命令树和 `sim` schema，复用 `host.device-proxy` output contract。
  - `sim proxy status --simulator booted --json` 返回 `ok=true`、`platform=ios`、`target.id=sim:booted`、`limitations=["proxy_session_not_running"]`。
  - `sim proxy start --simulator booted --mode record --output /tmp/ios-network --json` 返回 `ok=false`、`error.code=proxy_platform_not_supported` 并 exit 1，继续避免误报真实代理接管。
  - 验证记录：`swift test --package-path CLI --filter SimulatorAdvancedControlsTests` 通过 13 tests；`swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 26 tests；`swift test --package-path CLI --filter SchemaFactSource` 通过 107 tests；`swift test --package-path CLI` 通过 235 tests。
- 三端网络接管 P1 iOS fake adapter 与 `networksetup` command planner 已落地：
  - `FakeNetworkProxyHostAdapter` 支持内存态 `start/status/export/stop`，只写临时 restore snapshot 和 network capture artifact，不触碰本机系统代理。
  - `export` 未 start 时返回 `proxy_not_running`，并给出 `device proxy start --platform ios --device <target> --mode record --json` nextAction。
  - iOS `networkSetupProxyOverrideCommands` 纯函数生成 HTTP/HTTPS proxy override + SOCKS off 命令；`networkSetupProxyRestoreCommands` 纯函数生成按 snapshot restore 的 HTTP/HTTPS/SOCKS/bypass domains 命令。
  - 所有 `networksetup` 命令计划标记为 `break-glass` risk，并要求 audit/timeout；真实执行仍未接入 CLI，后续需要显式策略保护。
  - 验证记录：`swift test --package-path CLI --filter SimulatorAdvancedControlsTests` 通过 15 tests；`swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 28 tests；`swift test --package-path CLI --filter SchemaFactSource` 通过 107 tests；`swift test --package-path CLI` 通过 239 tests。
- Single-device Web preview 继续向 Baguette-like 设备镜像界面收敛：
  - `/web/device` 从左侧调试面板改为浮动胶囊工具栏 + codec segmented control + 中央手机外壳 + 底部圆形工具按钮。
  - 页面仍消费既有 `/status`、`/targets`、`/geometry?target=...`、`/screenshot?target=...`、`/input?target=...`，不新增前端工程或额外 Web 产品面。
  - 多 runtime target 时保留 active target 自动选择与 compact target strip，避免隐藏设备预览。
  - 浏览器截图验证通过：工具栏、`H.264` / `MJPEG`、phone shell、真实截图 image 均渲染；image natural size 为 `402x874`，console error 为空。
  - 验收截图：`docs-linhay/spaces/20260608-single-device-web-preview/screenshots/20260609/20260609-web-device-baguette-mirror-after-v01.png`。
- 验证记录：
  - `swift test --filter SingleDeviceWebPageTests` 通过，3 tests。
  - `swift build` 通过。
  - `docs-linhay/scripts/check-docs.sh` 当前因无关既有目录 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 README 失败；本次 space 文档和截图路径已落位。
- Single-device Web preview 开始补三端模拟器支持：
  - `TKTargetSummary` 新增机器可读 `platform` 字段，显式值优先；老 payload 会从 target id、transport、simulatorUDID、device / OS 描述推断 `ios` / `android` / `harmony`，默认兼容旧 iOS embedded runtime。
  - `TKAppInfo` 输出 `platform`，`triton serve` 的 target metadata 会把 platform 带入 `/targets`。
  - Web 单设备页已按 platform 显示 `iOS Simulator`、`Android Emulator`、`Harmony / DevEco Emulator`，target strip 和隐藏详情区域都可被自动化读取。
  - 当前 Web 操作仍针对 connected runtime target；host-only Android / Harmony emulator 直连控制需后续补 HTTP adapter endpoint，CLI 侧已有 `device` / `observe` / `tap` / `swipe` / `type` / `paste --platform ...` 能力。
  - 浏览器 smoke 验证根路径 `/`：toolbar 显示 `iOS Simulator | cn.dxy.iDxyer | iPhone | active`，target strip 显示 `iOS Simulator · 丁香园`，预览图渲染；console 仅有 favicon 404。
  - 验收截图：`docs-linhay/spaces/20260608-single-device-web-preview/screenshots/20260609/20260609-web-device-three-platform-label-after-v01.png`。
  - 验证记录：`swift test --filter TKCLITransportModelsTests` 通过，23 tests；`swift test --package-path CLI --filter SingleDeviceWebPageTests` 通过，3 tests；`swift build` 和 `swift build --package-path CLI` 通过。
- Single-device Web preview 完成 host-side Web adapter 首轮：
  - 新增 `/web/targets` 聚合 embedded runtime 与 ready host emulator target；host target id 使用 `host:ios:<udid>`、`host:android:<serial>`、`host:harmony:<target>`，iOS host target 可继承同 UDID runtime 的 App 名称与 bundle id。
  - 新增 `/web/screenshot`：host iOS 走 `simctl`，Android 走 `adb screencap`，Harmony 走 `hdc snapshot_display`；runtime target 继续 fallback 到原 screenshot 请求。
  - 新增 `/web/geometry` 与 `/web/input`：host Android / Harmony 支持基础 tap、swipe、type / paste、button；iOS host-side 仅承诺 framebuffer preview，App 输入仍要求 embedded runtime 或后续 host HID adapter。
  - 根路径 `/` 浏览器 smoke：页面不空白，toolbar 显示 `丁香园`，selected target 为 `host:ios:60667794-96F8-40E6-8664-85538EC4663E`，preview image natural size 为 `1206x2622`，console error 为空。
  - 直接 host 截图 smoke：`/web/screenshot?target=host%3Aios%3A60667794-96F8-40E6-8664-85538EC4663E` 返回 `image/png`，PNG 为 `1206x2622`。
  - 当前可见模拟器画面本身为白色 App 界面；这已由 `simctl io screenshot` 证实是模拟器当前 framebuffer，不是 Web 未渲染。
  - 验收截图：`docs-linhay/spaces/20260608-single-device-web-preview/screenshots/20260609/20260609-web-device-root-host-screenshot-after-v03.png`。
  - 验证记录：`swift test --package-path CLI --filter SingleDeviceWebPageTests` 通过，6 tests；`swift test --filter TKCLITransportModelsTests` 通过，23 tests；`swift build --package-path CLI` 通过。
- Single-device Web preview 补 Baguette-compatible 指定设备详情入口：
  - 新增 `/simulators/<id>`，复用单设备详情页，并把 `<id>` 注入为初始 target selector；同时支持 `/web/device?target=<id>`。
  - 前端 selector 匹配 exact id、`simulatorUDID` 和 `host:<platform>:<selector>` 后缀，因此同一逻辑覆盖 iOS Simulator UDID、Android emulator serial、Harmony / DevEco target id。
  - 浏览器 smoke 对齐 Baguette 参考 URL：打开 `http://127.0.0.1:19421/simulators/F4E55B8E-0141-4C46-9965-263CCE782B5F` 后直接选中 `host:ios:F4E55B8E-0141-4C46-9965-263CCE782B5F`，toolbar 显示 `Overloaded`，preview image natural size 为 `1206x2622`，console error 为空。
  - Harmony 浏览器 smoke：打开 `http://127.0.0.1:19421/simulators/127.0.0.1%3A5555` 后直接选中 `host:harmony:127.0.0.1:5555`，toolbar 显示 `Harmony / DevEco Emulator`，preview image natural size 为 `1308x2880`，console error 为空。
  - Android 当前本机 `adb devices` 为空，未做真实浏览器截图；Android Web target / selector / host action 路径由 `SingleDeviceWebPageTests` 的 Android emulator fixture 覆盖。
  - 验收截图：`docs-linhay/spaces/20260608-single-device-web-preview/screenshots/20260609/20260609-web-device-simulators-route-overloaded-after-v04.png`。
  - Harmony 验收截图：`docs-linhay/spaces/20260608-single-device-web-preview/screenshots/20260609/20260609-web-device-simulators-route-harmony-after-v05.png`。
  - 验证记录：`swift test --package-path CLI --filter SingleDeviceWebPageTests` 通过，7 tests；`swift build --package-path CLI` 通过。
- Single-device Web preview 按 Baguette 详情页做 UI / 能力对比后收敛遮挡问题：
  - 参考页：`http://localhost:8421/simulators/F4E55B8E-0141-4C46-9965-263CCE782B5F`，Baguette 已有真实 stream / FPS、Live logs、AX inspector、rotate、Home、Screenshot、App Switcher 等控制入口。
  - TritonKit 当前已对齐：`/simulators/<id>` 指定设备详情、浮动 toolbar、H.264 / MJPEG 分段 chrome、中央 phone shell、host framebuffer screenshot preview、iOS / Harmony 指定目标选择。
  - 明确差距：TritonKit 的 H.264 / MJPEG 仍是 UI chrome，不是 Baguette 的真实 stream / FPS；Live logs、AX inspector、rotate、iOS host HID / Home / App Switcher 仍未实现。
  - 本轮修正：target strip 默认收起并显式 `display:none`，screen HUD 不再压在 App 画面上，action log 没有动作前隐藏；最终浏览器 smoke 显示 `Overloaded`，preview image natural size 为 `1206x2622`，target strip / action log 均为 `display:none`，console error 为空。
  - Android 当前本机 `adb devices` 仍为空，因此真实 Android Web 截图 smoke 未执行；Android 能力仍停留在 tests / contract fixture 覆盖。
- Single-device Web preview 完成三端真实页面 smoke 与截图轮询稳定性修复：
  - 修复用户指出的“只显示方块/没有 APP 界面”：Web 单设备页现在优先用 `/web/targets` 的 ready host target，三端详情 URL 都能直接选中目标并渲染真实 framebuffer。
  - 前端新增 `refreshInFlight` / `screenshotInFlight` 防重入，截图 fetch 使用 `AbortController` 8 秒超时，并接受 `image/png,image/jpeg`，避免页面轮询堆积 pending `/web/screenshot` 后停在“已选目标但 no screenshot”状态。
  - Android 真实 smoke：`/simulators/emulator-5556` 选中 `host:android:emulator-5556`，toolbar 显示 `sdk_gphone64_arm64`，preview image natural size `1080x2400`，action log 初始隐藏。
  - Harmony 真实 smoke：`/simulators/127.0.0.1%3A5555` 选中 `host:harmony:127.0.0.1:5555`，preview image natural size `1308x2880`，action log 初始隐藏。
  - iOS 复测：`/simulators/60667794-96F8-40E6-8664-85538EC4663E` 选中 `host:ios:60667794-96F8-40E6-8664-85538EC4663E`，toolbar 显示 `丁香园`，preview image natural size `1206x2622`，action log 初始隐藏。
  - 截图证据：`docs-linhay/spaces/20260608-single-device-web-preview/screenshots/20260609-web-ios-detail-after-v03.png`、`20260609-web-android-detail-after-v01.png`、`20260609-web-harmony-detail-after-v01.png`。
  - 验证记录：`swift test --package-path CLI --filter SingleDeviceWebPageTests` 通过，12 tests；`swift build --package-path CLI` 通过；HTTP smoke 确认 `/web/screenshot` Android 返回 `200 image/png`，Harmony 返回 `200 image/jpeg`，Harmony geometry 为 `1308x2880`。
