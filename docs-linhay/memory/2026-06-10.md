# 2026-06-10

- 三端网络接管继续推进到 Android / Harmony planning slice：
  - `FakeNetworkProxyHostAdapter` 从 iOS-only 测试 adapter 扩展为 iOS / Android / Harmony 通用内存态 adapter；restore snapshot 与 export artifact 动态写入 request 的 `platform` 和 `target`，不再硬编码 iOS。
  - Android 新增 ADB proxy command planner：`adb -s <serial> shell settings put global http_proxy <host>:<port>` 与 `adb -s <serial> shell settings delete global http_proxy`。命令计划标记为 `break-glass`，要求 target / timeout / auditRecord；当前未接入 CLI 真实执行器。
  - Harmony 新增 probe-only command planner：HDC boot completed + shell probe；在真实 DevEco Emulator 系统代理命令完成验证前，不声明 `start` 支持，不伪装代理已配置。
  - 文档同步：`docs-linhay/spaces/20260609-three-platform-network-takeover/README.md`、对应 plan、`docs-linhay/dev/ai-cli-readable-control.md`、public `tritonkit-emulator-cli-takeover` skill。
  - 验证记录：`swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 30 tests。
- 三端网络接管新增 `device proxy start --plan-only` CLI 可执行面：
  - `NetworkProxySession` 增加 `sourceCommands[]`，`host.device-proxy` output contract 同步声明该字段。
  - `triton device proxy start --platform ios|android|harmony --device <selector> --mode record --output <dir> --plan-only --json` 返回 `ok=true`、`configured=false`、`proxyEndpoint`、平台 limitations、`proxy_plan_only:not_executed` 与底层 host command ledger。
  - iOS plan-only 输出 `networksetup` HTTP / HTTPS proxy override + SOCKS off 命令；Android 输出 ADB `settings put global http_proxy`；Harmony 输出 HDC readiness / shell probe，不伪装代理配置。
  - 验证记录：`swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 31 tests；`swift test --package-path CLI --filter SchemaFactSource` 通过 107 tests；CLI smoke 覆盖 iOS、Android、Harmony 三端 `--plan-only` JSON 输出。
- 三端网络接管新增 `device proxy stop --restore --plan-only` restore ledger：
  - `triton device proxy stop --platform ios|android|harmony --device <selector> --restore --plan-only --json` 返回 `ok=true`、`configured=false`、`restore.restored=false`、`sourceCommands[]` 与 `proxy_plan_only:not_executed`。
  - iOS restore plan 输出关闭 HTTP / HTTPS / SOCKS 代理状态的 `networksetup` 命令；Android 输出 `adb -s <serial> shell settings delete global http_proxy`；Harmony 继续 probe-only，并带 `proxy_restore_probe_only:no_verified_harmony_proxy_mutation`，不编造未验证代理恢复命令。
  - 验证记录：`swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 32 tests；`swift test --package-path CLI --filter SchemaFactSource` 通过 107 tests；CLI smoke 覆盖 iOS、Android、Harmony 三端 `stop --restore --plan-only` JSON 输出。
- 三端网络接管新增 `device proxy export --plan-only` evidence artifact plan：
  - `triton device proxy export --platform ios|android|harmony --device <selector> --output <path.har|path.ndjson> --plan-only --json` 返回 `ok=true`、`configured=false`、`visibility=unknown`、`artifacts[].kind=network-capture`、`bytes=null`、`proxy_plan_only:not_executed` 与 `proxy_export_plan_only:artifact_not_written`。
  - 该路径只声明后续 network evidence artifact 目标，不读取 session capture、不写 HAR / NDJSON、不执行 host / emulator 代理命令；`sourceCommands[]` 保持空数组。
  - 验证记录：先补红灯测试，`DeviceCrossPlatformTests` 编译失败于缺少 `makeNetworkProxyExportPlanSession`；实现后 `swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 33 tests，`swift test --package-path CLI --filter SchemaFactSource` 通过 107 tests，`swift test --package-path CLI` 通过 244 tests。
  - CLI smoke 覆盖 iOS / Android / Harmony 三端 `export --plan-only` JSON 输出；三端均返回 `network-capture` artifact path 和 `proxy_export_plan_only:artifact_not_written`。
  - 文档同步：`docs-linhay/spaces/20260609-three-platform-network-takeover/README.md`、对应 plan、`docs-linhay/dev/ai-cli-readable-control.md`、public `tritonkit-emulator-cli-takeover` skill。
  - qmd 维护：`qmd update` 成功；`qmd embed` 复现本机 `node-llama-cpp` / Metal `GGML_ASSERT([rsets->data count] == 0)` 崩溃并被终止。`docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。
- 三端网络接管普通 `start/stop` 从泛化 unsupported 收敛为 break-glass policy envelope：
  - 新增 `--confirm` 与 `--audit-record <id>` CLI / schema 入口；缺少任一项时返回 `ok=false`、`error.code=destructive_action_requires_policy`，`error.nextAction` 指向带 policy 的 `device proxy start/stop` 命令。
  - 上一阶段 iOS / Android 在 policy 满足后返回 `ok=false`、`error.code=proxy_runner_not_configured`、`proxy_execution_policy_accepted:auditRecord=<id>`、`proxy_runner_not_configured:not_executed` 与底层 `sourceCommands[]`，明确当时真实 runner 尚未接入，没有执行 `networksetup` 或 ADB 代理修改；本日后续已推进到 `--execute-runner` 显式执行路径。
  - Harmony 在 policy 满足后返回 `ok=false`、`error.code=proxy_unverified_platform_proxy`、HDC probe ledger 和 `proxy_harmony_probe_only:no_verified_proxy_mutation`，继续禁止未验证 DevEco / Harmony 代理设置命令。
  - schema taxonomy 已把 `proxy_runner_not_configured`、`proxy_unverified_platform_proxy` 纳入 diagnose / plan 恢复分类；`device` schema failure codes 同步暴露这两个错误码和 `destructive_action_requires_policy`。
  - 验证记录：`swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 35 tests；`swift test --package-path CLI --filter SchemaFactSource` 通过 107 tests；`swift test --package-path CLI` 通过 246 tests。
  - CLI smoke 覆盖 iOS 缺 policy、iOS policy accepted / runner not configured、Android stop policy accepted / runner not configured、Harmony policy accepted / platform proxy unverified。
  - 文档同步：`docs-linhay/spaces/20260609-three-platform-network-takeover/README.md`、对应 plan、`docs-linhay/dev/ai-cli-readable-control.md`、public `tritonkit-emulator-cli-takeover` skill。
- 三端网络接管补齐 iOS / Android 可注入 command runner 测试面：
  - 新增 `NetworkProxyCommandRunner`、`makeNetworkProxyStartExecutedSession`、`makeNetworkProxyStopExecutedSession` 和内部顺序执行 helper；该层可消费 iOS `networksetup` / Android ADB planner 产出的 `TKHostCommand` ledger。
  - fake runner 成功时返回 `ok=true`、`configured=true/false`、`sourceCommands[]`、`proxy_execution_policy_accepted:auditRecord=<id>`、`proxy_runner_executed:break_glass` 与 restore 状态；这只证明 runner 抽象可用，不代表 CLI 默认路径已启用真实代理修改。
  - fake runner 失败时稳定返回 `proxy_start_failed` 或 `proxy_restore_failed`，保留完整 `sourceCommands[]`，`error.nextAction.category=diagnose`；schema failure codes 与 taxonomy 已同步到 `device` / `sim`。
  - Harmony 仍保持 `proxy_unverified_platform_proxy` probe-only，不接入任何未验证 DevEco / Harmony 代理设置命令。
  - 验证记录：先补红灯测试，失败于缺少 `NetworkProxyCommandRunner` / executed session 构造函数；实现后 `swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 37 tests，`swift test --package-path CLI --filter SchemaFactSource` 通过 107 tests。
- 三端网络接管推进到显式 CLI runner opt-in 与 iOS alias 对齐：
  - `device proxy start/stop` 新增 `--execute-runner`，现在必须同时具备 `--confirm`、`--audit-record <id>`、`--execute-runner` 才会调用真实 runner；缺少任一项返回 `destructive_action_requires_policy`，并在 `error.nextAction.args` 中给出完整 policy argv。
  - iOS / Android 三项策略满足后会调用 `runHostCommand` 执行 planner 产出的 `networksetup` / ADB ledger；成功返回 `proxy_runner_executed:break_glass`，失败返回 `proxy_start_failed` 或 `proxy_restore_failed`。
  - `sim proxy start/export/stop` 对齐 iOS alias：`start/export/stop --plan-only` 可执行；`start/stop --confirm --audit-record <id> --execute-runner` 才会调用 iOS `networksetup` runner。
  - Harmony 即使带 `--execute-runner` 仍返回 `proxy_unverified_platform_proxy` 和 HDC probe ledger，不执行未验证 DevEco / Harmony 代理设置。
  - 验证记录：`swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 38 tests；`swift test --package-path CLI --filter SimulatorAdvancedControlsTests` 通过 15 tests；`swift test --package-path CLI --filter SchemaFactSource` 通过 107 tests。CLI smoke 覆盖 `sim proxy start --plan-only`、iOS 缺 `--execute-runner` policy envelope、Harmony 带 `--execute-runner` 仍 probe-only。
- 三端网络接管补清 Android Emulator loopback 映射契约：
  - `--proxy` 继续表示 Mac host 上代理服务监听地址，默认 `127.0.0.1:19431`；`NetworkProxySession.proxyEndpoint` 保留该 host endpoint。
  - Android Emulator 的 ADB ledger 必须把 `127.0.0.1:<port>`、`localhost:<port>`、`::1:<port>` 改写为 `10.0.2.2:<port>`，因为 emulator 内的 loopback 指向 emulator 自身，不是 Mac host。
  - 非 loopback host 例如 `192.168.1.10:<port>` 保持原样；agent 审计真实 mutation 时以 `sourceCommands[]` 为准。
- 三端网络接管完成 proxy endpoint preflight 安全门：
  - iOS / Android `--execute-runner` 现在会先检查 `--proxy <host:port>` 可由 Mac host 建连，失败时返回 `proxy_endpoint_unreachable`，不得执行 `networksetup` 或 ADB mutation。
  - Android preflight 检查 host endpoint；ADB ledger 继续使用 emulator 可达地址，例如 host `127.0.0.1:19431` 对应 `10.0.2.2:19431`。
  - 验证记录：`swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 40 tests；`swift test --package-path CLI --filter SchemaFactSource` 通过 107 tests；`swift test --package-path CLI --filter SimulatorAdvancedControlsTests` 通过 15 tests。
  - CLI smoke：`device proxy start --platform android --proxy 127.0.0.1:1 --confirm --audit-record smoke-preflight --execute-runner --json` 按预期返回 `ok=false`、`error.code=proxy_endpoint_unreachable`、`configured=false`、`proxy_endpoint_unreachable:not_mutated`，并保留 Android `10.0.2.2:<port>` ADB ledger，没有进入真实 ADB mutation。
- 三端网络接管补上 iOS / Android command-ledger restore snapshot：
  - `device proxy start --platform ios|android ... --execute-runner --output <dir>` 与 `sim proxy start ... --output <dir>` 会在 mutation 前写出 `<dir>/restore-state.json`，schema 为 `triton.proxy.restore.v1`，记录平台、target、proxyEndpoint、auditRecord、start/restore `TKHostCommand` 和人读 `sourceCommands[]` / `restoreSourceCommands[]`。
  - `device proxy stop --platform ios|android --restore-snapshot <path> --confirm --audit-record <id> --execute-runner --json` 与 `sim proxy stop --restore-snapshot <path> ...` 优先执行 snapshot 中的 restore ledger，成功 limitation 包含 `proxy_restore_snapshot_used`。
  - 当前 snapshot 是 command-ledger restore snapshot，不是读取真实系统原始代理状态；真实原值 snapshot、证书 trust、capture export 仍是后续切片。
- 三端网络接管继续补齐 iOS / Android 原值 restore snapshot：
  - `start --execute-runner --output <dir>` 现在在 mutation 前先执行只读 snapshot ledger；iOS 使用 `networksetup -getwebproxy/-getsecurewebproxy/-getsocksfirewallproxy/-getproxybypassdomains` 解析 HTTP / HTTPS / SOCKS / bypass domains，Android 使用 `adb -s <serial> shell settings get global http_proxy` 读取原始 global proxy。
  - `restore-state.json` schema `triton.proxy.restore.v1` 新增 `snapshotCommands`、`snapshotSourceCommands[]`、`serviceSnapshots[]` 与 `androidOriginalHTTPProxy`；`restoreCommands` 不再只是清空模板，而是按 iOS / Android 原值生成。Android 原值为 `null` 或空时 delete，原值为 `<host>:<port>` 时 stop 恢复为原值。
  - snapshot query 失败会进入 `proxy_start_failed` envelope，仍不得执行后续 `networksetup` / ADB mutation；endpoint preflight 仍在 snapshot query 前阻断不可达代理。
  - Harmony 仍保持 HDC probe-only，不新增或类推 DevEco / Harmony proxy mutation。
  - 验证记录：先补红灯测试，失败于缺少 `networkSetupProxySnapshotCommands`、`parseNetworkSetupServiceSnapshot`、`adbProxySnapshotCommands`、`parseADBHTTPProxySetting`、snapshot 新字段；实现后窄测试通过，`swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 42 tests，`swift test --package-path CLI --filter SimulatorAdvancedControlsTests` 通过 16 tests，`swift test --package-path CLI --filter SchemaFactSource` 通过 107 tests。
  - 收尾验证：`swift test --package-path CLI` 通过 254 tests；`git diff --check` 通过；`docs-linhay/scripts/qmd-sync.sh` 成功。`docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。
- 三端网络接管新增文件态 proxy session：
  - `start --execute-runner --output <dir>` 成功时写 `<dir>/session-state.json`，schema 为 `triton.proxy.session.v1`，记录 platform、target、captureMode、proxyEndpoint、configured、visibility、limitations、artifacts、restoreSnapshotPath 与 sourceCommands；同时写 `<dir>/requests.ndjson` 作为 `triton.network.v1` 占位 capture artifact。
  - `device proxy status --platform <platform> --device <selector> --session <dir> --json` 与 `sim proxy status --simulator <udid|booted> --session <dir> --json` 可跨 CLI 调用读取 session-state 并校验 platform / target。
  - `device proxy export --platform <platform> --device <selector> --session <dir> --output <path.ndjson> --json` 与 `sim proxy export --simulator <udid|booted> --session <dir> --output <path.ndjson> --json` 可从 session artifact 导出 capture 文件；当前 `requests.ndjson` 仍是占位，不代表真实代理 listener 已写入业务请求事件。
  - 文档同步：`docs-linhay/spaces/20260609-three-platform-network-takeover/README.md`、对应 plan、`docs-linhay/dev/ai-cli-readable-control.md`、public `tritonkit-emulator-cli-takeover` skill、internal `tritonkit-host-simulator-takeover` skill。
  - 验证记录：`swift test --package-path CLI --scratch-path /tmp/triton-cli-test-session-6F89318E-90E3-4262-81E3-36EFFFD4649A --filter DeviceCrossPlatformTests` 通过 43 tests；同 scratch 下 `--filter SchemaFactSource` 通过 107 tests；`--filter SimulatorAdvancedControlsTests` 通过 16 tests；完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-test-session-6F89318E-90E3-4262-81E3-36EFFFD4649A` 通过 255 tests。
  - 收尾验证：`git diff --check` 通过；`docs-linhay/scripts/qmd-sync.sh` 成功并完成 embed。`docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。
- 三端网络接管新增本地 capture proxy 服务：
  - 新增 `triton device proxy serve --listen 127.0.0.1:19431 --output <dir> --jsonl`，作为 iOS Simulator、Android Emulator 和后续验证后的 Harmony proxy adapter 共用的 host-side proxy endpoint。
  - `serve` 写 `<dir>/requests.ndjson`，事件 schema 为 `triton.proxy.capture.v1`，JSONL 输出 `proxy.serve.ready`、`proxy.serve.request`、`proxy.serve.connection-failed` 和最终 `proxy.serve.summary`；HTTP proxy request 与 HTTPS `CONNECT` tunnel 都会记录 metadata-only event。
  - 当前 redaction 策略为 `headers-names-only`：保存 method、url、host、port、path、tunnel 和 header names，不保存 header values / body，不做 TLS 解密；`proxy.serve.request` 只能证明 capture proxy 看到流量，不证明 HTTPS body 可见。
  - schema 同步：`device` command 暴露 `proxy serve --listen <host:port> --output <dir> --jsonl` usage、`--listen` / `--jsonl` option、example 和 `host.device-proxy-serve` output contract。
  - 验证记录：新增 parser 与本地 socket smoke 测试；`swift test --package-path CLI --scratch-path /tmp/triton-cli-test-session-6F89318E-90E3-4262-81E3-36EFFFD4649A --filter DeviceCrossPlatformTests` 通过 45 tests；同 scratch 下 `--filter SchemaFactSource` 通过 107 tests。
  - 收尾验证：完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-test-session-6F89318E-90E3-4262-81E3-36EFFFD4649A` 通过 257 tests；`git diff --check` 通过；`docs-linhay/scripts/qmd-sync.sh` 成功并完成 embed。`docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。
- 三端网络接管新增 session capture 的 metadata-only HAR 导出：
  - `exportNetworkProxyCaptureArtifact` 现在根据输出后缀分流：`.ndjson` 原样复制 session capture artifact，`.har` 将 `requests.ndjson` 中的 `proxy.serve.request` events 转为 HAR 1.2 skeleton。
  - HAR skeleton 只写 method、URL、query、CONNECT tunnel metadata、header names 的 `<redacted>` placeholder 和空 response placeholder；不写 header values、request/response bodies、TLS decrypted content 或真实 response payload。
  - `device proxy export --platform <platform> --device <selector> --session <dir> --output <path.har> --json` 与 `sim proxy export --simulator <udid|booted> --session <dir> --output <path.har> --json` 共享该转换路径。
  - 验证记录：新增 `device proxy export converts capture events into metadata-only HAR`、`proxy session export writes HAR when requested by output extension` 与 `device proxy export CLI writes HAR from a persisted session`；`swift test --package-path CLI --scratch-path /tmp/triton-cli-test-session-6F89318E-90E3-4262-81E3-36EFFFD4649A --filter DeviceCrossPlatformTests` 通过 48 tests。
  - 文档同步：`docs-linhay/spaces/20260609-three-platform-network-takeover/README.md`、对应 plan、`docs-linhay/dev/ai-cli-readable-control.md`、public `tritonkit-emulator-cli-takeover` skill、internal `tritonkit-host-simulator-takeover` skill。
- 三端网络接管新增共享 capture proxy 的 block policy：
  - `triton device proxy serve --listen <host:port> --output <dir> --mode record|block --jsonl` 现在暴露模式参数；`record` 保持 metadata capture + upstream forwarding，`block` 记录同样的 request metadata 后直接返回 `502 TritonKit Proxy Blocked`，不连接 upstream。
  - `proxy.serve.request` event 新增 `captureMode` 与 `policyAction`；block mode 写 `policyAction=blocked`，summary 写 `captureMode=block`，output contract 同步 `captureMode` / `policyAction` 字段。
  - 该能力是 iOS Simulator、Android Emulator 和未来验证后的 Harmony adapter 共用 host-side proxy policy；它不表示 Harmony 平台代理 mutation 已验证，Harmony 仍保持 HDC probe-only。
  - 验证记录：先补红灯测试，失败于缺少 `NetworkProxyServeConfig.mode` 与 `NetworkProxyServeSummary.captureMode`；实现后 `swift test --package-path CLI --scratch-path /tmp/triton-cli-test-session-6F89318E-90E3-4262-81E3-36EFFFD4649A --filter DeviceCrossPlatformTests` 通过 49 tests。
  - 文档同步：三端 network takeover space、对应 plan、`docs-linhay/dev/ai-cli-readable-control.md`、public `tritonkit-emulator-cli-takeover` skill、internal `tritonkit-host-simulator-takeover` skill。
- 三端网络接管新增共享 capture proxy 的 mock policy，并补强 HAR 策略响应证据：
  - `triton device proxy serve --listen <host:port> --output <dir> --mode record|mock|block --jsonl` 现在支持 `mock`；mock mode 记录 metadata-only request event，写 `captureMode=mock`、`policyAction=mocked`，并直接返回固定 JSON mock response，不连接 upstream。
  - `device` schema、CLI help、output contract、三端 space、plan、`docs-linhay/dev/ai-cli-readable-control.md`、public `tritonkit-emulator-cli-takeover` skill、internal `tritonkit-host-simulator-takeover` skill 已同步 `record|mock|block`。
  - HAR skeleton 仍不写 header values、request/response bodies、TLS decrypted content 或真实 upstream response payload；但现在会为 policyAction 写策略响应状态：forwarded request 仍是 `status=0/not captured`，mock request 是 `200 TritonKit Proxy Mock`，block request 是 `502 TritonKit Proxy Blocked`。
  - 该能力属于三端共用 host-side proxy policy，不需要 App 内 SDK；Harmony 仍只复用 shared proxy endpoint / policy，在真实 DevEco / Harmony 代理设置命令验证前不执行平台代理 mutation。
  - 验证记录：`swift test --package-path CLI --scratch-path /tmp/triton-cli-test-session-6F89318E-90E3-4262-81E3-36EFFFD4649A --filter DeviceCrossPlatformTests` 通过 50 tests；`--filter SchemaFactSource` 通过 107 tests；完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-test-session-6F89318E-90E3-4262-81E3-36EFFFD4649A` 通过 262 tests。
  - 收尾验证：`git diff --check` 通过；`docs-linhay/scripts/qmd-sync.sh` 成功并完成 embed；`docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。
- 三端网络接管新增共享 capture proxy 的 throttle policy：
  - `triton device proxy serve --listen <host:port> --output <dir> --mode record|mock|block|throttle --jsonl` 现在支持 `throttle`；throttle mode 记录 metadata-only request event，写 `captureMode=throttle`、`policyAction=throttled`，并直接返回 `429 TritonKit Proxy Throttled` 与 `Retry-After: 1`，不连接 upstream。
  - HAR skeleton 会为 throttle request 写入策略响应状态 `429 TritonKit Proxy Throttled`；仍不写 header values、request/response bodies、TLS decrypted content 或真实 upstream response payload。
  - 该能力属于三端共用 host-side synthetic rate-limit policy，不需要 App 内 SDK；当前不是带宽整形或真实延迟注入，也不表示 Harmony 平台代理 mutation 已验证。
  - `device` schema、CLI help、output contract、三端 space、plan、`docs-linhay/dev/ai-cli-readable-control.md`、public `tritonkit-emulator-cli-takeover` skill、internal `tritonkit-host-simulator-takeover` skill 已同步 `record|mock|block|throttle`。
  - 验证记录：`swift test --package-path CLI --scratch-path /tmp/triton-cli-test-session-6F89318E-90E3-4262-81E3-36EFFFD4649A --filter DeviceCrossPlatformTests` 通过 51 tests；`--filter SchemaFactSource` 通过 107 tests；完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-test-session-6F89318E-90E3-4262-81E3-36EFFFD4649A` 通过 263 tests。
  - 收尾验证：`git diff --check` 通过；`docs-linhay/scripts/qmd-sync.sh` 成功并完成 embed；`docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。
- 三端网络接管补强 `device proxy serve --jsonl` 机器可读事件契约：
  - `NetworkProxyServeSummary` 新增 `event=proxy.serve.summary` 与 `schemaVersion=triton.proxy.capture.v1`，使 ready、request、connection-failed 和 summary 都能按同一 JSONL event 模型被 agent 消费。
  - `device` schema 新增 `jsonlEvents[] = proxy.serve.ready / proxy.serve.request / proxy.serve.connection-failed / proxy.serve.summary`，并声明 `finalEventKind=proxy.serve.summary`；output contract 同步把 `event` 与 `schemaVersion` 设为稳定字段。
  - 旧的失败事件 key 从 `proxy.serve.connection_failed` 收敛为 schema 规则允许的 lower-kebab `proxy.serve.connection-failed`；该事件只证明当前 proxy 请求解析/连接/转发失败，不代表业务请求成功。
  - 该切片只增强三端共享 host-side serve 证据与 schema，不新增真实平台代理 mutation；Harmony 仍保持 probe-only。
  - 验证记录：先补红灯测试，失败于缺少 `NetworkProxyServeSummary.event` / `schemaVersion`；实现后 `swift test --package-path CLI --scratch-path /tmp/triton-cli-test-session-6F89318E-90E3-4262-81E3-36EFFFD4649A --filter DeviceCrossPlatformTests` 通过 51 tests，`--filter SchemaFactSource` 通过 107 tests，完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-test-session-6F89318E-90E3-4262-81E3-36EFFFD4649A` 通过 263 tests。
  - 收尾验证：`git diff --check` 通过；`docs-linhay/scripts/qmd-sync.sh` 成功并完成 embed；`docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。
- 三端网络接管补齐 `device proxy export --session` 导出摘要与 artifact 失败 envelope：
  - `NetworkProxySession` 新增导出摘要字段 `requestCount`、`redaction`、`truncation`；HAR / NDJSON 导出成功后直接在 JSON envelope 中返回 capture 质量摘要，agent 不需要反读 artifact 才能判断是否真的观察到流量。
  - 占位 capture 返回 `requestCount=0`、`redaction=default`、`truncation=none`；真实 `proxy.serve.request` metadata capture 会统计请求数并返回 `redaction=headers-names-only`。
  - capture artifact 缺失、不可读或导出写入失败时返回稳定 `proxy_artifact_write_failed`，`error.nextAction.category=archive`，并保留目标输出 artifact path。
  - 同步更新 `host.device-proxy` output contract、`device` failure codes、三端 network takeover space / plan、`docs-linhay/dev/ai-cli-readable-control.md`、public `tritonkit-emulator-cli-takeover` skill、internal `tritonkit-host-simulator-takeover` skill。
  - 验证记录：`swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 53 tests；`swift test --package-path CLI --filter SchemaFactSource` 通过 107 tests。
- 三端网络接管补上 `device proxy serve --jsonl` command-level smoke：
  - 新增测试直接运行 `DeviceProxyServe.run()`，捕获 stdout JSONL，验证真实命令输出路径会按顺序产生 `proxy.serve.ready`、`proxy.serve.connection-failed`、`proxy.serve.request` 与最终 `proxy.serve.summary`。
  - 测试同时确认 mock mode 会返回固定 `200 TritonKit Proxy Mock` response，stdout summary 包含 `schemaVersion=triton.proxy.capture.v1`、`captureMode=mock`、`requestCount=1`，而 `<dir>/requests.ndjson` 只保留 capture artifact 事件，不写 final summary。
  - 为兼容 export summary WIP，`NetworkProxySession` 新增显式 initializer，保留旧调用默认值并允许 `proxy export --session` 返回 `redaction/requestCount/truncation`；`host.device-proxy` output contract 已声明这些字段。
  - 该切片继续只验证本地 host-side proxy endpoint 和 CLI JSONL 输出，不执行真实 iOS / Android / Harmony 代理 mutation；Harmony 仍保持 probe-only。
  - 验证记录：先补红灯测试，失败于 command stdout 缺 `proxy.serve.request`（原因是 readiness probe 消耗了一个连接）以及 `NetworkProxySession` export summary 初始化不兼容；改为 `--max-connections 2` 并补显式 initializer 后，`swift test --package-path CLI --scratch-path /tmp/triton-cli-test-session-6F89318E-90E3-4262-81E3-36EFFFD4649A --filter DeviceCrossPlatformTests` 通过 53 tests。
