# 2026-06-11

## 三端 proxy restore 失败归档 artifact

- 为 iOS / Android break-glass restore snapshot 失败路径补齐机器可读残留状态归档：`stop --restore-snapshot <path> --execute-runner` 的 runner 返回 `proxy_restore_failed` 时，会在 snapshot 同目录写出 `restore-failure.json`。
- 新 artifact schema 为 `triton.proxy.restore-failure.v1`，记录 `platform`、`target`、`action`、`auditRecord`、`restoreSnapshotPath`、`restoreSourceCommands[]`、`errorCode`、`errorSummary` 和 `capturedAt`；失败 envelope 同步返回 `artifacts[].kind=proxy-restore` 与 `proxy_restore_failure_artifact_written` limitation。
- 该切片只增强已有 iOS / Android restore snapshot 的 archive/evidence recovery；不运行真实 `networksetup` / ADB，不改变本机或 emulator 代理；Harmony 仍保持 `proxy_unverified_platform_proxy` / probe-only，不编造 DevEco / Harmony mutation 命令。
- 验证记录：
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-restore-failure-artifact-20260611 --filter DeviceCrossPlatformTests` 通过 63 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-restore-failure-artifact-schema-20260611 --filter SchemaFactSource` 通过 107 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-restore-failure-artifact-full-20260611` 通过 284 tests。
  - 全量测试中发现既有 stdout 捕获用例会被并发输出干扰，本切片将对应 CLI argv 断言改为 ArgumentParser parse + typed runtime session 断言，避免依赖全局 stdout。

## 三端 proxy serve NDJSON 策略响应状态

- 为 `device proxy serve` 的 `proxy.serve.request` 事件补齐本地策略响应字段：mock 写 `responseStatus=200` / `responseStatusText="TritonKit Proxy Mock"`，block 写 `502` / `TritonKit Proxy Blocked`，throttle 写 `429` / `TritonKit Proxy Throttled`。
- 该字段只表达 TritonKit host-side proxy 本地合成响应，不代表真实 upstream response，不代表 TLS 解密，也不执行任何 iOS / Android / Harmony 代理 mutation；record / forwarded 请求继续不写响应状态。
- schema 同步在 `host.device-proxy-serve` output contract 暴露 `responseStatus` 与 `responseStatusText`，让 agent 可直接读取 NDJSON 判断 mock / block / throttle 策略效果，不必先导出 HAR。
- 文档同步：三端 network takeover space / plan、`docs-linhay/dev/ai-cli-readable-control.md`、public `tritonkit-emulator-cli-takeover` skill、internal `tritonkit-host-simulator-takeover` skill。
- 验证记录：
  - 先补红灯测试，`device proxy serve mock mode records metadata and returns a fixed mock response` 失败于 NDJSON 缺少 `responseStatus` / `responseStatusText`。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-response-evidence-20260611 --filter DeviceCrossPlatformTests/deviceProxyServeMockModeRecordsMetadataAndReturnsFixedMockResponse` 通过。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-response-evidence-20260611 --filter DeviceCrossPlatformTests` 通过 56 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-response-evidence-20260611 --filter SchemaFactSource` 通过 107 tests。
  - 完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-response-evidence-20260611` 通过 274 tests。
  - `docs-linhay/scripts/qmd-sync.sh` 成功，embed 完成；底层仍打印一行 `node-llama-cpp` Metal 编译提示。
  - `git diff --check` 通过。
  - `docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## 三端网络接管验证收尾

- 继续推进三端模拟器/仿真器网络接管，范围仍是本机 CLI + 本机 iOS Simulator、Android Emulator、Harmony / DevEco Emulator；不做真机、远端 agent、设备云、Web / Wails UI。
- 收尾验证上一日新增的 `device proxy serve --jsonl` command-level smoke：真实命令路径会输出 `proxy.serve.ready`、`proxy.serve.connection-failed`、`proxy.serve.request` 与 `proxy.serve.summary`，`requests.ndjson` 只保留 capture artifact 事件，不写 final summary。
- 本切片只增强三端共用 host-side proxy endpoint、JSONL 事件契约与导出摘要，不执行真实 iOS / Android / Harmony 代理 mutation；iOS / Android 真实 mutation 仍必须显式 `--confirm --audit-record <id> --execute-runner`，Harmony 仍保持 probe-only，不能伪造未验证 DevEco / Harmony 代理设置命令。
- 验证记录：
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-test-session-schema-20260610 --filter SchemaFactSource` 通过 107 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-test-session-schema-20260610` 通过 265 tests。
  - `git diff --check` 通过。
  - `docs-linhay/scripts/qmd-sync.sh` 最终退出码为 0，embed 成功完成；底层仍打印一行 `node-llama-cpp` Metal 编译提示。
  - `docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## 三端网络接管补齐保守证书状态

- 为 `device proxy doctor/status` 补齐 `NetworkProxySession.cert` 的保守机器可读状态：`installed=false`、`trusted=false`，iOS scope 为 `simulator`，Android / Harmony scope 为 `emulator`。
- 该字段只作为 HTTPS 可见性限制证据，不安装证书、不修改 trust store、不声明 TLS 解密可用；`device proxy serve` 的 CONNECT metadata 也仍不能当作 HTTPS body 可见证明。
- 先补红灯测试：`device proxy doctor returns a stable host-side envelope for all three platforms` 与新增 `device proxy status returns conservative certificate state before a session exists` 均失败于 `session.cert == nil`。
- 最小实现：新增 `networkProxyConservativeCertificate(platform:)`，让 doctor/status 未运行 session 时返回保守 cert 状态。
- 文档同步：三端 network takeover space / plan、`docs-linhay/dev/ai-cli-readable-control.md`、public `tritonkit-emulator-cli-takeover` skill、internal `tritonkit-host-simulator-takeover` skill。
- 验证记录：
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-cert-status-20260611 --filter DeviceCrossPlatformTests` 通过 54 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-cert-status-20260611 --filter SchemaFactSource` 通过 107 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-cert-status-20260611` 通过 267 tests。
  - `git diff --check` 通过。
  - `docs-linhay/scripts/qmd-sync.sh` 成功，embed 完成；底层仍打印一行 `node-llama-cpp` Metal 编译提示。
  - `docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## Harmony HDC target list 兼容性

- 发现真实 DevEco / Harmony 环境中 `hdc list targets -v` 与 stdout/stderr 捕获方式存在差异：有时 target 行出现在 stderr，有时 verbose 输出不可解析但 plain `hdc list targets` 可返回单列 target。
- 修正 `triton device list --platform harmony --json` / `device resolve` 的 target discovery：解析 HDC 输出时合并 stdout + stderr，过滤 `Connect server failed` 这类 prose error；verbose 列表没有可解析 target 时 fallback 到 plain `hdc list targets`；单列 target 如 `127.0.0.1:5555` 视为 `Connected` 并按 emulator scope 分类。
- 新增 `TKHarmonyHDCCommand.listTargetsPlain()` 和独立 `HarmonyDeviceTargetListTests`，覆盖 stderr target、plain fallback 与 sourceCommand ledger。
- 验证记录：
  - `swift test --filter TKHostAdapterModelsTests` 通过 33 tests。
  - `swift test --package-path CLI --filter HarmonyDeviceTargetListTests` 通过 2 tests。
  - `swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 61 tests。
  - `swift build --package-path CLI --product triton` 通过。
  - `CLI/.build/debug/triton device list --platform harmony --json` 真实输出包含 `harmony:127.0.0.1:5555`、`ready=true`、`scope=emulator`、`transport=TCP`。

### iOS sim proxy alias selector 对齐

- 修正 iOS `sim proxy --simulator <selector>` 的 CLI 路径：`start/status/export/stop` 现在复用 `makeNetworkProxyPlanTarget(platform: .ios, device:)`，与三端 `device proxy --platform ios --device <selector>` 使用同一套 workspace alias / `current` 解析。
- `--simulator iphone15`、`--simulator current` 会从 `.triton/host-targets.json` 解析为真实 Simulator target，避免把 friendly alias 写进 `host.device-proxy` target 字段或 `networksetup` ledger。
- `--simulator` 指向 `scope=real`、`kind=real-device` 或 `ios-real:` alias 时，保持 `proxy_real_device_not_supported`，不生成 host proxy mutation ledger。
- 该切片只补齐 iOS alias selector 语义，不执行真实 `networksetup`，不改变本机代理；Harmony 仍保持 probe-only。
- 验证记录：
  - 新增 CLI argv 级测试 `sim proxy alias CLI resolves workspace aliases like device proxy`，覆盖 `start/status/export/stop` 的 alias/current 解析和 real-device alias 拒绝。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-sim-proxy-alias-20260611 --filter DeviceCrossPlatformTests` 通过 61 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-sim-proxy-alias-20260611 --filter SchemaFactSource` 通过 107 tests。
  - 完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-sim-proxy-alias-20260611` 通过 281 tests。

## 三端 proxy session evidence 导入补强

- 为 `network.proxy-session` evidence 导入补上三端 fixture 覆盖：iOS `booted`、Android `emulator-5554`、Harmony `127.0.0.1:5555` 都通过同一个 `captureEvidenceBundle(... proxySessionPath:)` 路径导入。
- 测试确认 evidence manifest 中 `network.proxy-session` 与 `network-capture` artifacts 均保留对应 `platform` / `target`，并且相对路径 `requests.ndjson` 会按 session 目录解析后复制到 `artifacts/network/requests.ndjson`。
- 继续补强真实 CLI argv smoke：直接运行 `Evidence.parse(... --proxy-session ...)` 与 `Capture.parse(... --proxy-session ...)`，解码 stdout JSON manifest，确认 agent 实际调用路径会复制 `session-state.json` / `requests.ndjson` 并保留 platform / target。
- 该切片只强化 `.tritonevidence` 归档可验证性，不执行任何新的 iOS / Android / Harmony 代理 mutation；Harmony 仍保持 probe-only。
- 文档同步：三端 network takeover space / plan、`docs-linhay/dev/ai-cli-readable-control.md`。
- 验证记录：
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-evidence-argv-20260611 --filter EvidenceBundleTests` 通过 11 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-evidence-argv-20260611 --filter SchemaFactSource` 通过 107 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-evidence-argv-20260611` 通过 270 tests。
  - `git diff --check` 通过。
  - `docs-linhay/scripts/qmd-sync.sh` 成功，embed 完成；底层仍打印一行 `node-llama-cpp` Metal 编译提示。
  - `docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## 三端 network-proxy task plan

- 新增 `triton plan network-proxy` task plan：参数覆盖 `--platform ios|android|harmony`、`--device <selector>`、`--proxy <host:port>`、`--mode record|mock|block|throttle`、`--output <proxy-session-dir>`、`--evidence <dir.tritonevidence>`。
- 该 plan 是 host-only dry-run / plan-only 链路，即使 embedded server 不可达也返回 `mode=task`、`nextStep=proxy-probe-plan`，并保留 `serverReachable=false`；步骤依次为 target resolve、proxy doctor、`device proxy probe --plan-only`、`device proxy serve --jsonl`、`device proxy start --plan-only`、`device proxy export --plan-only`、`evidence --include network.proxy-session`、`device proxy stop --restore --plan-only`。
- 该切片不执行真实 iOS / Android / Harmony 代理 mutation；Harmony 仍保持 probe-only，不声明 verified DevEco / Harmony 代理设置命令。
- 验证记录：
  - 先补红灯测试，失败于 `WorkflowPlanRequest` 缺少 `platform/proxy/mode/output` 字段。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-network-proxy-plan-20260611 --filter SchemaFactSource` 通过 107 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-network-proxy-plan-20260611` 通过 271 tests。
  - `git diff --check` 通过。
  - `docs-linhay/scripts/qmd-sync.sh` 成功，embed 完成；底层仍打印一行 `node-llama-cpp` Metal 编译提示。
  - `docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## 三端 `.tritonplan` proxy session evidence step

- 为 `.tritonplan` 的 `evidence` step 补上 `proxySession` 字段：当 step 声明 `include: "network.proxy-session"` 与 `proxySession: "<dir>"` 时，`plan inspect` 会在 `steps[].argv` 中保留 `--proxy-session <dir>`，并把 `network.proxy-session` / `network-capture` 放进 `expectedArtifacts[]`。
- `triton replay <file.tritonplan> --dry-run --var platform=android --json` 会对 `proxySession` 做变量替换，并在 dry-run step argv 中输出真实执行参数；真实 replay evidence step 会把该路径传给 `captureEvidenceBundle(... proxySessionPath:)`，从而复用已有 `.tritonevidence` 导入逻辑。
- 该能力只归档已有 host-side proxy session，不启动 `device proxy serve`，不执行 iOS / Android / Harmony 代理 mutation；Harmony 仍保持 probe-only。
- 验证记录：
  - 先补红灯测试：`network proxy evidence step keeps explicit session in replay plan metadata` 固定 `proxySession` 解码、inspect argv、expected artifacts 与变量替换。
  - 新增 CLI dry-run smoke：`dry-run replay preserves network proxy session evidence argv`，直接运行 `triton replay --dry-run --var platform=android --json`。
  - `swift test --filter TKReplayPlanModelsTests` 通过 24 tests。
  - `swift test --package-path CLI --filter ReplayCommandTests` 通过 10 tests。
  - `swift test --package-path CLI --filter SchemaFactSourceTests/taskWorkflowPlansExposeExecutableCommandSequences` 通过 1 test。
  - 沙盒内首次 `swift test --package-path CLI --filter TKReplayPlanModelsTests` 编译成功但 `.xctest` 加载被 macOS system policy 拦截，非沙盒重跑后确认过滤器实际属于根 package，需要改用根 `swift test --filter TKReplayPlanModelsTests`。

## 三端 `.tritonplan` proxy lifecycle dry-run 审计

- 继续扩展 `.tritonplan` proxy lifecycle action：在既有 `proxy-serve`、`proxy-start`、`proxy-export`、`proxy-stop` 前新增 `proxy-probe`，`plan inspect` 与 `replay --dry-run` 会生成 `triton device proxy probe --plan-only`、`serve --jsonl`、`start --plan-only`、`export --plan-only`、`stop --restore --plan-only` argv，并暴露 `network-capture`、`host-device-proxy`、`proxy-restore` 等 expected artifacts。
- 新增 step 字段 `platform`、`device`、`proxy`、`mode`、`restore`，支持 `${platform}` / `${device}` 变量替换；`proxy-start` / `proxy-stop` 永远生成 plan-only argv，不带 `--execute-runner`。
- 真实 replay 遇到 proxy lifecycle step 会返回 `unsupported_capability` / dry-run-only 错误，要求 agent 先审阅 argv 再显式执行单条命令；该切片不启动 proxy listener，不执行 iOS / Android / Harmony 代理 mutation，Harmony 仍保持 probe-only。
- 顺手修复 CLI test target 编译阻塞：`DeviceCrossPlatformTests` 的 Harmony fallback 用例改用本地测试 helper 复用 HDC parser，不执行真实 `hdc`。
- 验证记录：
  - 先补红灯测试，失败于 `TKReplayAction` 无法 decode `proxy-serve`。
  - `swift test --filter TKReplayPlanModelsTests/networkProxyLifecycleStepsExposeReplayDryRunArgv` 通过。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-replay-lifecycle-20260611 --filter ReplayCommandTests/dryRunReplayPreservesNetworkProxyLifecycleArgv` 通过。
  - `swift test --filter TKReplayPlanModelsTests` 通过 25 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-replay-lifecycle-20260611 --filter ReplayCommandTests` 通过 11 tests。
  - 本轮 `proxy-probe` 增量验证：`swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-probe-replay-20260611 --filter ReplayCommandTests` 通过 11 tests；`swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-probe-replay-full-20260611` 通过 283 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-replay-lifecycle-20260611 --filter DeviceCrossPlatformTests` 通过 55 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-replay-lifecycle-20260611 --filter SchemaFactSource` 通过 107 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-replay-lifecycle-20260611` 通过 273 tests。
  - `git diff --check` 通过。
  - `docs-linhay/scripts/qmd-sync.sh` 成功，embed 完成；底层仍打印一行 `node-llama-cpp` Metal 编译提示。
  - `docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## 三端网络接管归档 proxy session 到 evidence

- 为 `.tritonevidence` 补齐 host-side proxy session 显式导入路径：`triton evidence --include network.proxy-session --proxy-session <dir> --output <dir.tritonevidence> --json` 与 `triton capture --include network.proxy-session --proxy-session <dir> --output <dir.tritonevidence> --json`。
- 该路径读取并校验 `<dir>/session-state.json` 的 `triton.proxy.session.v1`，导入为 `network.proxy-session` artifact；同时复制 session 声明的 `network-capture` artifact 到 `artifacts/network/requests.ndjson`。
- `network.proxy-session` 与 `network-capture` 都标记 `redactionStatus=sensitive`，`network-capture` 进入 evidence primary artifact 排序；agent 仍必须检查 `proxy.serve.request` events，不能把占位 `requests.ndjson` 当作真实流量证明。
- schema 同步暴露 `--proxy-session`、`network.proxy-session` include 示例和 `network-capture` artifact taxonomy；public `tritonkit-emulator-cli-takeover` 与 internal `tritonkit-host-simulator-takeover` skill 已写回同一工作流。
- 验证记录：先补红灯测试，失败于 `captureEvidenceBundle` 缺少 `proxySessionPath`；实现后 `swift test --package-path CLI --filter EvidenceBundleTests` 通过 9 tests，`swift test --package-path CLI --filter SchemaFactSource` 通过 107 tests。
- 收口验证：`swift test --package-path CLI` 通过 267 tests；`git diff --check` 通过；`docs-linhay/scripts/qmd-sync.sh` 成功；`docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## 三端网络接管 evidence 导入缺失 capture 的降级

- 修正 proxy session evidence 导入的 archive fallback：当 `<dir>/session-state.json` 可读、但 declared `network-capture` artifact 缺失或不可读时，manifest 仍保留 `network.proxy-session` artifact，只把 `network-capture` 写入 `skipped[]`。
- 该行为适用于 iOS Simulator、Android Emulator、Harmony / DevEco Emulator 三端 session 归档；agent 可以保留代理配置、platform / target、sourceCommands 和 limitations，再单独处理缺失 capture artifact。
- 红灯测试：`evidence proxy session import preserves session metadata when capture is missing` 先失败于 skipped kind 错误为 `network.proxy-session`；实现后 `swift test --package-path CLI --filter EvidenceBundleTests` 通过 10 tests，`swift test --package-path CLI --filter SchemaFactSource` 通过 107 tests。
- 收口验证：修复 `device proxy serve command emits ready request and summary JSONL events` 的并发 stdout 噪声过滤后，`swift test --package-path CLI --filter DeviceCrossPlatformTests` 通过 54 tests，`swift test --package-path CLI` 通过 270 tests，`git diff --check` 通过，`docs-linhay/scripts/qmd-sync.sh` 成功；`docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## 三端 proxy certificate plan-only 契约

- 新增 `device proxy cert doctor --platform ios|android|harmony --json` 与 `device proxy cert plan --platform ios|android|harmony --device <selector> --certificate <path.cer> --json`。
- `cert plan` 返回 `NetworkProxySession.action=proxy.cert.plan`、`configured=false`、保守 `cert.installed=false/trusted=false`、`artifacts[].kind=proxy-certificate` 与 `proxy_cert_untrusted` limitation；该路径不执行证书安装、信任配置或 TLS 解密。
- iOS ledger 只声明 `xcrun simctl keychain <target> add-root-cert <path.cer>`；Android ledger 只声明 `adb push` 与 `android.credentials.INSTALL` 用户安装 intent；Harmony 继续 `proxy_cert_harmony_probe_only`，不编造未验证 DevEco / Harmony 证书信任命令。
- schema / capability 同步新增 `proxy cert doctor/plan` usage、`--certificate` option、`network-certificate-plan` capability，以及 `proxy_cert_untrusted` 的 diagnose / plan recovery 分类。
- 验证记录：
  - 先补红灯测试，失败于缺少 `makeNetworkProxyCertificatePlanSession`。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-cert-20260611 --filter DeviceCrossPlatformTests/deviceProxyCertPlanExposesConservativeThreePlatformTrustSetupLedgers` 通过。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-cert-20260611 --filter DeviceCrossPlatformTests` 通过 56 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-cert-20260611 --filter SchemaFactSource` 通过 107 tests。
  - 完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-cert-20260611` 通过 274 tests。
  - `git diff --check` 通过。
  - `docs-linhay/scripts/qmd-sync.sh` 成功，embed 完成；底层仍打印一行 `node-llama-cpp` Metal 编译提示。
  - `docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## 三端网络接管 real-device target 拒绝

- 将 `device proxy` 网络接管边界固化为 simulator/emulator-only：`HostDeviceTarget.scope=real` 或 `kind=real-device` 时，`start/stop/status/export/cert plan` 与 break-glass executed helper 统一返回 `proxy_real_device_not_supported`。
- 拒绝 envelope 保持 `configured=false`、空 `sourceCommands[]` / `artifacts[]`，并把 `error.nextAction` 指向 `device proxy doctor --platform <platform> --json`，避免从真实设备 target 生成 `networksetup`、ADB 或 HDC 代理 mutation ledger。
- schema 同步新增 `proxy_real_device_not_supported` failure code；recovery 分类为 diagnose / prepare-target / plan。
- 验证记录：
  - 先补红灯测试：`device proxy rejects real-device targets because takeover is emulator scoped` 失败于 plan-only 路径返回 `ok=true` 且生成 iOS / Android / Harmony proxy ledger。
  - focused test 实现后通过，并加厚覆盖 break-glass executed helper、status/export session helper，确认 runner 未被调用。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-emulator-scope-20260611 --filter DeviceCrossPlatformTests` 通过 57 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-emulator-scope-20260611 --filter SchemaFactSource` 通过 107 tests。
  - 完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-emulator-scope-20260611` 通过 275 tests。
  - `git diff --check` 通过。
  - `docs-linhay/scripts/qmd-sync.sh` 成功，embed 完成；底层仍打印一行 `node-llama-cpp` Metal 编译提示。
  - `docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

### selector 前缀误分类修正

- 发现 `makeNetworkProxyPlanTarget` 会把 `ios-real:` / `android-real:` / `harmony-real:` selector 字符串直接伪造成 simulator / emulator target，导致 plan-only 路径生成 `networksetup`、ADB 或 HDC proxy ledger。
- 新增红灯测试 `device proxy plan target preserves real-device selector prefixes for rejection`，先失败于 target.scope/kind 变成 simulator/emulator 且 `session.ok=true`。
- 修正后 CLI planner 会把这些前缀保留为 `scope=real`、`kind=real-device`，再由统一 `proxy_real_device_not_supported` envelope 拒绝，确保 sourceCommands / artifacts 为空。
- 验证记录：
  - focused test 通过。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-real-selector-20260611 --filter DeviceCrossPlatformTests` 通过 58 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-real-selector-20260611 --filter SchemaFactSource` 通过 107 tests。
  - 完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-real-selector-20260611` 通过 276 tests。
  - `git diff --check` 通过。
  - `docs-linhay/scripts/qmd-sync.sh` 成功，embed 完成；底层仍打印一行 `node-llama-cpp` Metal 编译提示。
  - `docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

### 三端 proxy workspace selector 解析

- 修正 `device proxy --device <selector>` 的 plan target 解析：现在会读取当前工作目录 `.triton/host-targets.json`，支持 `device use` / `target use` 写入的 `current`，以及 `iphone15`、`android-a`、`harmony-a` 等 alias。
- alias target 中的 `sim:`、`android:`、`harmony:` 前缀会在进入 proxy planner 前归一化为平台真实 target，避免把友好别名或 store 前缀写入 `networksetup`、ADB 或 HDC probe ledger。
- 若 alias 指向 `scope=real`、`kind=real-device` 或 `android-real:` 等真实设备 selector，仍保留 `proxy_real_device_not_supported` 拒绝 envelope，不生成任何平台代理 mutation ledger。
- 修正 `device proxy status --device <selector> --json` 无 `--session` 时忽略 selector 的 CLI 缺口：现在 status 也会解析 alias / current；emulator alias 会出现在 `target` 字段，real-device alias 会返回 `proxy_real_device_not_supported` 并以非 0 退出。
- 本切片只改变计划/选择器解析，不执行真实 iOS / Android / Harmony 代理 mutation；Harmony 仍保持 probe-only。
- 验证记录：
  - 先补红灯测试 `device proxy plan target resolves workspace aliases and current selectors`，失败于 `iphone15`、`android-a`、`harmony-a`、`current` 均被原样当作 target，real-device alias 也被伪造成 emulator。
  - 修正后 focused test 通过。
  - 加厚 CLI argv 级测试 `device proxy start CLI plan-only resolves workspace aliases in emitted ledgers`：直接运行 `DeviceProxyStart.parse(... --plan-only --json)`，确认 iOS target 字段解析为 `SIM-1`，Android `current` ledger 写 `emulator-5554` / `10.0.2.2:19431`，Harmony ledger 写真实 HDC target。
  - 新增红灯测试 `device proxy status CLI resolves aliases and rejects real-device aliases without a session`，先失败于 status JSON 缺 `target`；实现后确认 emulator alias 与 real-device alias 在 CLI 层都走同一 target-aware contract。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-cli-alias-20260611 --filter DeviceCrossPlatformTests` 通过 61 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-cli-alias-20260611 --filter SchemaFactSource` 通过 107 tests。
  - 完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-cli-alias-20260611` 通过 279 tests。
  - `git diff --check` 通过。
  - `docs-linhay/scripts/qmd-sync.sh` 成功，embed 完成；底层仍打印一行 `node-llama-cpp` Metal 编译提示。
  - `docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## 三端 proxy 只读能力 probe

- 新增 `triton device proxy probe --platform ios|android|harmony --device <selector> --json`，返回 `NetworkProxySession.action=proxy.probe`、`configured=false`、`sourceCommands[]` 和 `probeResults[]`。
- Harmony probe 只执行 HDC readiness、shell probe 与 `param ls -r proxy|http` 只读命令；命中 proxy-looking 参数时只标记 `proxy_harmony_candidate_parameters_found:manual_verification_required`，不把候选参数升级为 verified mutation command。
- 该切片继续保持 host-side simulator/emulator proxy 边界：不执行真实 iOS / Android / Harmony 代理 mutation；iOS / Android break-glass runner 仍必须显式 `--confirm --audit-record <id> --execute-runner`，Harmony 仍保持 `proxy_unverified_platform_proxy`。
- 验证记录：
  - `swift test --filter TKHostAdapterModelsTests/harmonyHDCCommandBuilderArgv` 通过。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-probe-20260611 --filter DeviceCrossPlatformTests` 通过 63 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-probe-20260611-schema --filter SchemaFactSource` 通过 107 tests。
  - 完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-probe-20260611` 通过 283 tests。
  - `git diff --check` 通过。
  - `docs-linhay/scripts/qmd-sync.sh` 成功，最终状态为 all content hashes already have embeddings。
  - `docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## 三端 network-proxy plan 前置 probe

- 调整 `triton plan network-proxy --platform ios|android|harmony ... --json`：任务计划现在在 `proxy doctor` 后增加 `proxy-probe-plan`，命令为 `triton device proxy probe --platform <platform> --device <selector> --plan-only --json`。
- `network-proxy` 的 `nextStep` 从 `proxy-serve` 改为 `proxy-probe-plan`，让 agent 先读取只读平台代理能力证据，再启动本地 proxy endpoint 或审阅 `start --plan-only` ledger。
- 该切片不启动 `device proxy serve`，不执行真实 iOS / Android / Harmony 代理 mutation；Harmony 仍保持未验证 mutation command 的 probe-only 边界。
- 验证记录：`swift test --package-path CLI --scratch-path /tmp/triton-cli-network-proxy-probe-plan-20260611 --filter SchemaFactSource` 通过，107 tests；`swift test --package-path CLI --scratch-path /tmp/triton-cli-network-proxy-probe-plan-full-20260611` 通过，283 tests；`git diff --check` 通过。

## 三端 `.tritonplan` proxy lifecycle 静态校验

- 补齐 P5 replay dry-run 静态检查：`TKReplayPlanSummary.steps[].validationErrors[]` 现在会标出 proxy lifecycle 的缺字段与顺序问题。
- 覆盖范围：`proxy-probe/start/export/stop` 缺 `platform` 或 `device`，`proxy-serve/start` 缺 `proxy` endpoint，`proxy-serve/start/export` 缺 `output`，`proxy-stop` 缺 `restore=true`，以及 `proxy-export` 出现在同一 plan 的首个 `proxy-start` 之前。
- 该切片只增强 inspect / dry-run 审计信号，不启动 proxy listener，不执行 iOS / Android / Harmony 代理 mutation；Harmony 仍保持 probe-only。
- 验证记录：`swift test --scratch-path /tmp/triton-root-proxy-lifecycle-validation-20260611 --filter TKReplayPlanModelsTests` 通过 26 tests；`swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-lifecycle-validation-replay-20260611 --filter ReplayCommandTests` 通过 11 tests；`swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-lifecycle-validation-schema-20260611 --filter SchemaFactSource` 通过 107 tests；完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-lifecycle-validation-full-20260611` 通过 283 tests。

## 三端 replay 失败后 proxy state 归档

- 真实 `triton replay` 仍在首个失败 step 停止后续业务 action / assertion，但现在会查找失败 step 之后显式声明 `include: "network.proxy-session"` 与 `proxySession` 的 evidence step，并只执行 proxy-only archive。
- 该后置归档只复用已有 `<proxy-session-dir>/session-state.json` 与 declared `requests.ndjson`，不会启动 `device proxy serve`，不会执行 iOS / Android / Harmony 代理 mutation；Harmony 仍保持 probe-only。
- 新增 `ReplayCommandTests.replayArchivesProxySessionEvidenceAfterFailedStep`，用 Android session fixture 验证失败后可跳过中间业务 `tap`，按原 evidence step index 归档 `network.proxy-session` 与 `network-capture`，并把 argv 收敛成 `--include network.proxy-session`。
- 验证记录：`swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-failure-archive-20260611 --filter ReplayCommandTests` 通过 12 tests；`swift test --scratch-path /tmp/triton-root-proxy-failure-archive-models-20260611 --filter TKReplayPlanModelsTests` 通过 26 tests；`swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-failure-archive-schema-20260611 --filter SchemaFactSource` 通过 107 tests；完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-failure-archive-full-20260611` 通过 284 tests。
- `git diff --check` 通过；`docs-linhay/scripts/qmd-sync.sh` 成功，仍打印一行 `node-llama-cpp` Metal 编译提示；`docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## 三端 proxy restore failure 证据归档

- 扩展 `network.proxy-session` evidence 导入：当 `<proxy-session-dir>/restore-failure.json`、session artifacts 中的 `proxy-restore`，或 `restoreSnapshotPath` 同目录的 restore failure 文件存在时，会复制到 `.tritonevidence/artifacts/network/restore-failure.json`。
- manifest 中新增归档 artifact `kind=proxy-restore`、`redactionStatus=sensitive`、`policy=proxy-restore-failure-recovery`，并保留 restore failure payload 中的 `platform` / `target`；`TKEvidenceArtifactSummary` primary 排序也将 `proxy-restore` 放在 `network-capture` 之后、`network.proxy-session` 之前。
- 当 declared `network-capture` 缺失或不可读时，导入仍保留 `network.proxy-session` 与可用的 `proxy-restore`，只把 `network-capture` 写入 `skipped[]`；这让恢复失败现场能进入 evidence archive recovery，不代表 restore 成功。
- schema 同步把 `proxy-restore` 暴露到 `evidence` / `capture` 命令 artifacts 和 schema artifact taxonomy；public `tritonkit-emulator-cli-takeover` 与 internal `tritonkit-host-simulator-takeover` skill 已写回同一归档口径。
- 验证记录：
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-restore-evidence-20260611 --filter EvidenceBundleTests` 通过 12 tests。
  - 首次 `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-restore-evidence-schema-20260611 --filter SchemaFactSource` 红灯于 schema artifact taxonomy 未包含 `proxy-restore`；补齐后复跑通过 107 tests。
  - 完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-restore-evidence-full-20260611` 通过 285 tests。
  - `git diff --check` 通过。
  - `docs-linhay/scripts/qmd-sync.sh` 成功，仍打印一行 `node-llama-cpp` Metal 编译提示。
  - `docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## 三端 proxy status restore failure 可见性

- 扩展 `device proxy status --session <dir>` / iOS `sim proxy status --session <dir>` 的只读 session 装配逻辑：当 session 目录或 `restoreSnapshotPath` 同目录存在 `restore-failure.json`，status 响应会在 `artifacts[]` 中追加 `NetworkProxyArtifact(kind: "proxy-restore", path: ..., bytes: ...)`。
- 该逻辑不改写 `session-state.json`，只做后续诊断可见性增强；`export --session` 仍只导出 `network-capture`，不把 restore failure 当成网络流量 artifact。
- 新增/扩展 `DeviceCrossPlatformTests.proxySessionStateReportsStatusAndExportsCaptureArtifacts`，用 Android session fixture 写入 restore failure payload，验证 status 可见 `proxy-restore`，同时 export 仍返回 `network-capture`。
- 验证记录：
  - 首次 `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-status-restore-evidence-20260611 --filter DeviceCrossPlatformTests` 红灯于 `#expect` 右侧直接使用 `try`；修正为提前计算 byte count 后复跑通过 63 tests。
  - 完整 `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-status-restore-evidence-full-20260611` 通过 285 tests。
  - `git diff --check` 通过。
  - `docs-linhay/scripts/qmd-sync.sh` 成功，仍打印一行 `node-llama-cpp` Metal 编译提示。
  - `docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## 三端 network-proxy plan 接入证书准备

- 扩展 `WorkflowPlanRequest` 与 `triton plan network-proxy`：新增 `--certificate <path.cer>`，默认占位为 `<path.cer>`。
- `network-proxy` task plan 现在在 `proxy-probe-plan` 后、`proxy-serve` / `proxy-start-plan` 前插入 `proxy-cert-plan`，argv 为 `triton device proxy cert plan --platform <platform> --device <selector> --certificate <path.cer> --json`。
- `proxy-cert-plan.expectedArtifacts` 包含 `proxy-certificate`，用于把 TLS 可见性准备审计纳入三端主计划；该步骤仍是 plan-only，不安装 CA、不配置 trust、不声明 TLS 解密。Harmony 继续只表达 probe-only / unverified certificate trust boundary。
- 同步更新 schema example / option、workflow fixture、三端 proxy space、dev 文档、public emulator takeover skill 与 iOS host simulator takeover skill。
- 验证记录：
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-network-proxy-cert-plan-20260611 --filter SchemaFactSourceWorkflowTests` 通过 14 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-network-proxy-cert-plan-schema-20260611 --filter SchemaFactSource` 通过 107 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-network-proxy-cert-plan-full-20260611` 通过 285 tests。
  - `git diff --check` 通过。
  - `docs-linhay/scripts/qmd-sync.sh` 通过，仍打印 `node-llama-cpp` Metal 编译提示。
  - `docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## 三端 proxy restore snapshot plan-only 审计

- 扩展 `device proxy stop --restore-snapshot <path> --plan-only` 与 iOS alias `sim proxy stop --restore-snapshot <path> --plan-only`：plan-only 路径会读取 start 写出的 `triton.proxy.restore.v1` snapshot，返回原值 `restoreCommands` 作为 `sourceCommands[]`，并保持 `configured=false` / `restore.restored=false`。
- `triton plan network-proxy` 的最终 `proxy-stop-plan` 步骤从泛化 `--restore --plan-only` 改为 `--restore-snapshot ... --plan-only`；有具体 `--output` 时使用 `<output>/restore-state.json`，无具体 output 时使用完整 argv 占位 `<restore-state-json>`，避免把半截占位拼进路径 token。
- 该切片不运行真实 `--execute-runner`，不修改本机或模拟器/仿真器代理；Harmony 仍保持 probe-only，不能伪造未验证的 DevEco / Harmony proxy mutation。
- 验证记录：
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-stop-snapshot-plan-20260611 --filter DeviceCrossPlatformTests` 通过 64 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-stop-snapshot-sim-20260611 --filter SimulatorAdvancedControlsTests` 通过 16 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-stop-snapshot-schema-20260611 --filter SchemaFactSource` 初次发现默认 plan 拼出 `<proxy-session-dir>/restore-state.json` 这种半截占位 token；修正为无具体 output 时使用 `<restore-state-json>` 后通过 107 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-stop-snapshot-plan-20260611 --filter SchemaFactSourceWorkflowTests` 通过 14 tests。
  - `swift test --package-path CLI --scratch-path /tmp/triton-cli-proxy-stop-snapshot-full-20260611` 通过 286 tests。
  - `git diff --check` 通过。
  - `docs-linhay/scripts/qmd-sync.sh` 通过，仍打印 `node-llama-cpp` Metal 编译提示。
  - `docs-linhay/scripts/check-docs.sh` 仍因既有无关 `docs-linhay/spaces/20260608-issue-34-prefs-data` 缺 `README.md` 失败。

## Web mock UI

- 新建 Web mock space：`docs-linhay/spaces/20260611-web-mock-ui/`，明确本期只做可运行 mock，不改变 CLI / HTTP 机器可读控制边界。
- 新建可追踪 Web 工程：`Web/`，技术栈为 React 19 + TypeScript + Vite 7 + lucide-react；Vite dev server 固定 `127.0.0.1:34127` 且 `strictPort=true`，preview 固定 `127.0.0.1:34128` 且 `strictPort=true`。
- Mock 页面首屏为本机三端模拟器控制台：左侧 target list，中间设备镜像，右侧 Target Inspector，底部 Actions / Network / Logs；覆盖 iOS Simulator、Android Emulator、Harmony / DevEco Emulator 三类 mock DTO。
- 验证记录：`npm install` 通过且 0 vulnerabilities；`npm run build` 通过；浏览器打开 `http://127.0.0.1:34127/`，console error 为 0，1200px 视口无横向溢出，Android / Harmony target 切换会同步更新设备镜像、指标、网络事件和日志。
- 验收截图：`docs-linhay/spaces/20260611-web-mock-ui/screenshots/20260611/20260611-web-mock-dashboard-after-v01.png`。

### Web mock UI Device Hub 风格迭代

- 根据用户补充的 Xcode Device Hub 标注图，将 Web mock 从通用浅色 dashboard 改为深色 macOS / Xcode 工具窗视觉：顶部 traffic lights 与工具胶囊、左侧 Devices navigator、中间横屏 device canvas、右侧 Info / Apps / Profiles inspector、底部 device controls + Network / Logs evidence。
- 本切片继续保持 Web 只消费 `Web/src/data/mockData.ts` 的 mock DTO，不新增真实 create / update / delete / execute 控制闭环；CLI / HTTP 仍是业务控制事实入口。
- 参考截图已归档：`docs-linhay/spaces/20260611-web-mock-ui/screenshots/20260611/20260611-web-device-hub-reference-baseline-v01.png`。
- 验证记录：`npm run build` 通过；浏览器打开 `http://127.0.0.1:34127/`，console error 为 0，1200px 视口无横向溢出；iOS / Android / Harmony target 切换会同步更新 app、bundle、device、proxy 和 evidence 字段；`git diff --check` 通过。
- 验收截图：`docs-linhay/spaces/20260611-web-mock-ui/screenshots/20260611/20260611-web-device-hub-after-v04.png`。

### Web mock UI 真实 iOS Simulator 只读接入

- 在 Vite dev server 内新增开发期只读 bridge：`/web/ios-simulator/targets` 调用 `CLI/.build/debug/triton sim list --json` 并映射成 Web DTO；`/web/ios-simulator/screenshot?simulator=<udid>` 对已 Booted Simulator 调用 `triton sim screenshot --json` 并返回 PNG data URL。
- 前端优先展示真实 iOS Simulator 列表，并保留 Android / Harmony mock 作为非本期真实接入占位；Web 不触发 `sim boot`、shutdown、install、launch 或输入事件。
- 当前机器真实状态：`triton sim list --json` 返回 20 台 iOS Simulator，runtime 为 `iOS 26.5`，当前均为 `Shutdown`；页面显示真实设备名、UDID、runtime、state 与 `triton sim list --json` 来源。
- 验证记录：`npm run test` 通过 2 tests；`npm run build` 通过；`git diff --check` 通过；`curl http://127.0.0.1:34127/web/ios-simulator/targets` 返回真实列表；浏览器 1200px 视口无横向溢出，console error 为 0。
- 验收截图：`docs-linhay/spaces/20260611-web-mock-ui/screenshots/20260611/20260611-web-real-ios-simulator-after-v02.png`。

## 会话整理沉淀

- 按“整理”流程新增内部 skill：`.agents/skills/tritonkit-web-mock-ui/SKILL.md`，沉淀 Web mock 的 space-first、React/Vite、固定端口、浏览器验收和只读 DTO 边界。
- 更新 `.agents/skills/tritonkit-ops-governance/SKILL.md` 与 `AGENTS.md`：`Web/` 现在是可运行 mock 原型，但不是正式产品 UI 或 Wails 复活；正式 Web/Wails 仍需重新定义 space / BDD / 控制边界。
- 新增整理文档：`docs-linhay/dev/20260611-session-distillation.md`，记录本轮沉淀范围、Open Design 本机 CLI 验证结论和不纳入长期规则的临时内容。
- Open Design 个人级 skill 已在用户目录创建并通过 `od plugin validate/install/info/doctor` 验证；该文件位于 `~/.nolon/skills/open-design`，不属于 TritonKit 仓库提交范围。

### 自动触发规则补强

- 按用户要求将“整理 / 沉淀 / 整理，沉淀，提交”等收尾指令写入 `AGENTS.md` 的自动触发规则：后续不再追问是否沉淀，直接进入 `tritonkit-session-skill-distill` + `tritonkit-ops-governance` 流程。
- 固化执行顺序：先隔离 worktree 状态，再抽取可复用模式，优先更新项目级 skills，再写 docs / memory，只有 repo-wide 长期规则更新 `AGENTS.md`，并运行 qmd / diff / docs 结构检查。
- 若用户指令包含“提交”，整理完成后自动只 stage 本次整理相关文件并创建整理提交。

## 单设备 Web 详情页三端收口

- 按用户要求补齐 Baguette-like 单设备详情页三端真实 smoke：iOS Simulator、Android Emulator、Harmony / DevEco Emulator 都能通过 `/simulators/<id>` 直接选中目标，并在 Web 手机壳中渲染真实 framebuffer。
- 最新截图证据已归档到 `docs-linhay/spaces/20260608-single-device-web-preview/screenshots/`：
  - `20260609-web-ios-detail-after-v04.png`：`host:ios:60667794-96F8-40E6-8664-85538EC4663E`，显示 `丁香园`，framebuffer natural size `1206x2622`。
  - `20260609-web-android-detail-after-v02.png`：`host:android:emulator-5556`，显示 `sdk_gphone64_arm64`，framebuffer natural size `1080x2400`。
  - `20260609-web-harmony-detail-after-v02.png`：`host:harmony:127.0.0.1:5555`，framebuffer natural size `1308x2880`。
- 操作 smoke 全部返回 `ok:true`：iOS host id 的 tap 通过同 UDID embedded runtime fallback 执行，并在 server 日志中显示 `via triton:ios-simulator:60667794-96F8-40E6-8664-85538EC4663E`；Android Home 通过 `adb input keyevent KEYCODE_HOME`；Harmony Back 通过 `hdc uitest`。
- 验证记录：`swift test --package-path CLI --filter SingleDeviceWebPageTests` 通过 14 tests；`swift build --package-path CLI` 通过；`/web/targets` 可同时列出 iOS / Android / Harmony host targets；Playwright 截图人工复核三端均不是空白方块。
