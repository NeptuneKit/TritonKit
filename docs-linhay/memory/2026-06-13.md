# 2026-06-13

## Web 三角验收闭环

- 建立 `docs-linhay/spaces/20260613-web-triad-qa/README.md`，明确三角色 subagent 分工、BDD 场景、循环规则与验收标准，用“修改者 / 质疑者 / 法官”持续推进 `Web/` mock 体验收敛。
- 本轮代码收敛点放在 Web readonly 边界与三端切换可见性：`Web/src/App.tsx` 改为真实 host target 优先、缺失平台用内置 QA mock 补齐；同平台 iOS host target 切换时，App tile、target、network evidence、logs 都跟着 target 变化。
- `Web/src/data/iosSimulatorClient.ts` 对未知 host app / bundle 改为诚实 fallback：展示 `前台 App 未暴露 · <target.name>` 与 `Target <selector>`，避免 UI 靠静态 mock 名称伪造真实 app 身份；host targets 维持 `readonly: true`。
- `Web/dev/iosSimulatorBridge.mjs` 移除残留 host input 执行链路，只保留 `/web/host-input` 的 405 readonly 语义；同时导出 `mapTritonHostCapturesToWebTargets(...)` 供 bridge 与测试共用三端 target mapping。
- `Web/dev/iosSimulatorBridge.test.mjs` 新增三端 host captures 聚合测试与 readonly 405 route 测试；`cd Web && npm test` 现为 5 项全绿，`cd Web && npm run build` 通过。
- 今天补跑最终烟测时，先发现 `127.0.0.1:34127` dev server 已退出；重启后继续验收。in-app Browser 对本地 URL 命中 policy 拒绝访问，因此退回 Chrome DevTools 面板完成浏览器验证，不做规避。
- 浏览器最终验证：`1200 x 820` 与 `960 x 820` 下 `documentElement.scrollWidth === clientWidth`；console 无 error；切换 Android 时显示 `Overloaded / overloaded.cn.debug / /api/catalog / ADB target ready: emulator-5556`，切换 Harmony 时显示 `Triton Smoke / com.tritonkit.demo / /capabilities / HDC target discovered from plain list fallback`。
- 截图归档：`docs-linhay/spaces/20260613-web-triad-qa/screenshots/20260613/20260613-web-triad-switching-after-v04.png`。

## Web 三角验收第二轮

- 继续按“修改者 / 质疑者 / 法官”循环推进上一轮留下的建议，把“host bridge 请求成功但 `targets=[]` 时页面静默退回 QA mock”收敛成新的最小闭环。
- 新增 `Web/src/data/hostBridgePresentation.ts` 纯 helper，把 host bridge 状态抽成机器可测试的页面语义：`loading`、`error`、`success-but-empty-targets`、`readonly-host-targets`。
- `Web/src/App.tsx` 接入该 helper：空列表成功态下 toolbar 副标题切为 `QA mock fallback`，并在 `hub-body` 顶部渲染只读提示条，明确写出“当前没有可用 host target，正在展示 QA mock fallback”；`Web/src/styles.css` 增加 warning / error 两种 notice 样式。
- 自动化测试补到 `Web/dev/iosSimulatorBridge.test.mjs`：新增“请求成功但空 targets”与“请求失败”两条 fallback 测试。`cd Web && npm test` 现在是 7 项全绿，`cd Web && npm run build` 通过。
- 主控用浏览器 reload init script 强制 `/web/host-targets` 返回 `ok=true` 且 `targets=[]`，现场验证提示条真能出现，不只是 helper 单测过关；`1200 x 820` 与 `960 x 820` 下 `scrollWidth === clientWidth`，console 仍只有 React DevTools info。
- 恢复真实 host 场景后，Android/Harmony 的 app、bundle、network evidence、logs 仍正常切换，说明第二轮提示改动没有把第一轮三端切换闭环打坏。
- 新截图归档：`docs-linhay/spaces/20260613-web-triad-qa/screenshots/20260613/20260613-web-triad-empty-host-fallback-after-v05.png`。

## Web 三角验收第三轮

- 继续顺着第二轮质疑者建议往前推进，把 fallback notice 从 pure helper 覆盖再推到共享 markup 层：新增 `Web/src/components/HostBridgeNotice.ts`，用无 JSX 的 `createElement(...)` 组件同时服务真实页面和 Node smoke。
- `Web/src/App.tsx` 改为直接复用共享 `HostBridgeNotice` 组件，避免测试里 render 的 notice 和页面里真实 notice 分家。
- `Web/dev/iosSimulatorBridge.test.mjs` 新增 `react-dom/server` smoke：直接断言 `class="bridge-notice is-warning|is-error"`、`role="status"`、`aria-label="Host bridge 状态"`、`<strong>` 标题和 `<span>` detail 文案被渲染出来。
- 本地主控复跑 `cd Web && npm test`，现在是 9 项全绿；`cd Web && npm run build` 通过；`git diff --check -- Web/src Web/dev/iosSimulatorBridge.test.mjs` 通过。
- 浏览器回归只做最关键一刀：再次强制 `/web/host-targets` 返回 `ok=true, targets=[]`，确认共享组件接线后页面仍显示 `QA mock fallback` 与完整 notice 文案，`960 x 820` 下依旧 `scrollWidth === clientWidth`。
- 顺手清理了 space README 里残留的“待记录”占位，让这轮 triad 文档不再留空壳条目。
- 按第三轮质疑者建议补拍了一张第三轮专属截图：`docs-linhay/spaces/20260613-web-triad-qa/screenshots/20260613/20260613-web-triad-empty-host-fallback-after-v06.png`，对应共享 `HostBridgeNotice` 组件接线后的真实浏览器 fallback 画面。

## Web 三角验收第四轮

- 第四轮继续把 fallback 覆盖往前推：不满足于 pure helper 和 server-render markup，而是加一条 mounted DOM smoke，直接测 `App` 挂载后异步 fetch 完成、页面真的出现 `QA mock fallback` notice。
- 采用最小依赖路线：只在 `Web/` devDependencies 增加 `happy-dom`，没有引入 `@testing-library/react` / `jsdom` 整套栈。
- 新增 `Web/dev/appFallbackDom.test.mjs`：测试里用 `happy-dom` 起最小浏览器环境，用 Vite `createServer(...middlewareMode)` + `ssrLoadModule("/src/App.tsx")` 拉起真实 `App` 模块，stub `fetch("/web/host-targets") => ok=true + targets=[]`，再断言挂载后的 DOM 里真的出现 toolbar 副标题 `QA mock fallback` 和 fallback notice 标题 / detail 文案。
- 本地主控复跑 `cd Web && npm test`，现在是 10 项全绿；`cd Web && npm run build` 通过；`git diff --check -- Web` 通过。
- 第四轮收尾时补记了 `docs-linhay/scripts/check-docs.sh` 的当轮证据，返回 `docs-linhay structure ok`，避免法官把“功能已过但当轮 docs gate 证据没显式写回”判成流程性 FAIL。
- 这轮的残余风险也很清楚：mounted DOM 覆盖已经到 `App` 挂载和异步更新这一层，但错误分支还只有 helper / markup smoke，没有同等级 mounted DOM 覆盖。

## Web 三角验收第五轮

- 第五轮只补一刀：把 `host bridge request failed` 也拉到 mounted DOM 同级覆盖，避免第四轮只测“成功但空 `targets`”。
- `Web/dev/appFallbackDom.test.mjs` 新增错误分支 mounted DOM smoke：继续用 `happy-dom + Vite ssrLoadModule + react-dom/client`，stub `/web/host-targets` 返回 `502`，然后断言挂载后的 DOM 中出现 `QA mock fallback` 副标题、错误态 notice 标题 `Host bridge 请求失败，正在展示 QA mock fallback`，以及错误明细 `Host targets request failed: 502`。
- 本地主控复跑 `cd Web && npm test`，现在是 11 项全绿；`cd Web && npm run build` 通过；`git diff --check -- Web` 通过；`docs-linhay/scripts/check-docs.sh` 通过。
- 这一轮新增了一个新的非阻塞候选点：`npm test` 期间 Vite 会打印 `WebSocket server error: listen EPERM 0.0.0.0:24678`，但测试、构建和 docs gate 都没红，当前先记为环境噪音，不影响功能闭环。

## Web 三角验收第六轮

- 第六轮不再扩功能面，只收敛测试环境噪音：在 `Web/dev/appFallbackDom.test.mjs` 里给 Vite middleware server 显式补上 `server.ws: false`，并保留 `hmr: false`，目标是去掉 `npm test` 期间那条 `WebSocket server error: listen EPERM 0.0.0.0:24678`。
- 本地主控复跑 `cd Web && npm test`，11 项仍全绿，而且测试输出已经不再出现那条 EPERM WebSocket 告警；`cd Web && npm run build` 也通过。
- `git diff --check -- Web` 通过，`docs-linhay/scripts/check-docs.sh` 通过。
- 这轮的剩余风险也很收敛：测试夹具现在显式关掉了 Vite WS/HMR 通道，因此它不会帮未来的 HMR 行为兜底；但对当前 SSR / mounted DOM smoke 来说，这正是我们想要的更干净环境。

## Web 三角验收第七轮

- 第七轮先补了一个可重复触发真实浏览器错误态 fallback 的 dev-only 入口：`Web/src/data/iosSimulatorClient.ts` 识别 `__tritonkit_mock_host_targets=request-failed`，`Web/dev/iosSimulatorBridge.mjs` 与 `Web/dev/iosSimulatorBridge.test.mjs` 则保留只读 502 route smoke；同时补上 `Web/src/vite-env.d.ts`，让 `import.meta.env.DEV` 的读取能稳定通过构建。
- `cd Web && npm test` 升到 12 项全绿，新增 `forces /web/host-targets failure for dev browser fallback smoke`；`cd Web && npm run build`、`git diff --check -- Web docs-linhay/spaces/20260613-web-triad-qa/README.md`、`docs-linhay/scripts/check-docs.sh` 也都通过。
- 但第一次真实浏览器验收暴露了 blocker：直接让 bridge route 返回 502 时，页面虽然会显示 `QA mock fallback` 和 `Host targets request failed: 502`，Chrome console 也会多出两条 `Failed to load resource: 502`。这和 space 里“console 没有 error”的尺子撞上了，所以第七轮只能算发现了问题，不能算收口。

## Web 三角验收第八轮

- 第八轮把 dev-only 错误态入口从“真的发一个失败请求”改成“客户端短路抛错”：`Web/src/data/iosSimulatorClient.ts` 在开发态 URL 参数命中时直接抛 `Host targets request failed: 502`，让真实浏览器还能进入同一错误态 fallback，但不再制造新的网络级 console error。
- `Web/dev/appFallbackDom.test.mjs` 跟着收紧：错误态 mounted DOM smoke 现在断言 dev-only URL 参数场景下不会调用 `fetch("/web/host-targets")`；bridge 的 502 route 语义继续由 `Web/dev/iosSimulatorBridge.test.mjs` 单独兜底。
- 本地主控再次复跑 `cd Web && npm test`、`cd Web && npm run build`、`git diff --check -- Web docs-linhay/spaces/20260613-web-triad-qa/README.md`、`docs-linhay/scripts/check-docs.sh`，全部通过。
- 真实浏览器重新打开 `http://127.0.0.1:34127/?__tritonkit_mock_host_targets=request-failed` 后，`1200 x 820` 与 `960 x 820` 下都满足 `scrollWidth === clientWidth`，页面仍显示 `QA mock fallback`、`Host bridge 请求失败，正在展示 QA mock fallback` 和 `Host targets request failed: 502`，但 console 只剩 React DevTools info，network 面板也不再出现 `/web/host-targets?...request-failed` 的失败请求。
- 错误态浏览器截图已归档到 `docs-linhay/spaces/20260613-web-triad-qa/screenshots/20260613/20260613-web-request-failed-fallback-after-v07.png`。

## Web 三角验收第九轮

- 第九轮不再改运行时代码，只把“错误态 fallback 下三端切换仍成立”补成直接证据：`Web/dev/appFallbackDom.test.mjs` 新增 mounted DOM smoke，在 `?__tritonkit_mock_host_targets=request-failed` 下真实挂载 `App`，切换 Android `Pixel API 35` 与 Harmony `DevEco Local`，断言 `QA mock fallback` notice 始终保留，且 app / bundle / network / logs 随切换变化。
- 本地主控复跑 `cd Web && npm test`，现在是 `13/13` 全绿；`cd Web && npm run build`、`git diff --check -- Web docs-linhay/spaces/20260613-web-triad-qa/README.md`、`docs-linhay/scripts/check-docs.sh` 也都通过。
- 真实浏览器在同一个 `http://127.0.0.1:34127/?__tritonkit_mock_host_targets=request-failed` 会话里补完了 Android / Harmony 终验：Android `1200 x 820` 下 `scrollWidth === clientWidth`，页面显示 `Overloaded`、`overloaded.cn.debug`、`/api/catalog`、`ADB target ready: emulator-5556`，同时错误态 notice 仍在；Harmony `960 x 820` 下 `scrollWidth === clientWidth`，页面显示 `Triton Smoke`、`com.tritonkit.demo`、`/capabilities`、`HDC target discovered from plain list fallback`，错误态 notice 仍在。
- 这一轮浏览器 console 仍只有 React DevTools info，没有新的 error；错误态切到 Harmony 后的截图已归档到 `docs-linhay/spaces/20260613-web-triad-qa/screenshots/20260613/20260613-web-request-failed-switching-after-v08.png`。

## Web 三角验收第十轮

- 第十轮继续只补证据，不改 `Web/src/` 运行时代码：`Web/dev/appFallbackDom.test.mjs` 新增 mounted DOM smoke `restores iOS DTO after round-tripping Android and Harmony targets in request-failed fallback`，在同一个 `?__tritonkit_mock_host_targets=request-failed` 会话里按 `iOS -> Android -> Harmony -> iOS` 往返点击，断言 `QA mock fallback` notice 全程保留，且回到 iOS 后恢复 `丁香园`、`cn.dxy.iDxyer`、`/v1/home/feed` 与 `Selected host iOS target and paired embedded runtime`。
- 本地主控复跑 `cd Web && npm test`，现在是 `14/14` 全绿；`cd Web && npm run build`、`git diff --check -- Web/dev/appFallbackDom.test.mjs docs-linhay/spaces/20260613-web-triad-qa/README.md docs-linhay/memory/2026-06-13.md`、`docs-linhay/scripts/check-docs.sh` 也都通过。
- 真实浏览器在同一个 `http://127.0.0.1:34127/?__tritonkit_mock_host_targets=request-failed` 会话里完成 `Android -> Harmony -> iOS` 往返：切回 `DXY iPhone 15` 后，`1200 x 820` 下 `scrollWidth === clientWidth === 1200`，页面仍显示 `QA mock fallback` 与错误态 notice，当前应用恢复为 `丁香园`，bundle 恢复为 `cn.dxy.iDxyer`，network evidence 恢复为 `/v1/home/feed`、`/v1/search/query`、`/assets/config.json`，logs 恢复为 `Selected host iOS target and paired embedded runtime` 与 `Fetched framebuffer through simctl in 42 ms`。
- 同一往返会话缩到 `960 x 820` 后，`scrollWidth === clientWidth === 960`，当前 target 仍为 `DXY iPhone 15`，当前应用与 bundle 仍保持 iOS 文案；浏览器 console 全程只有 React DevTools info，没有新的 error。第十轮专属截图已归档到 `docs-linhay/spaces/20260613-web-triad-qa/screenshots/20260613/20260613-web-request-failed-roundtrip-ios-after-v09.png`。

## Web 三角验收第十一轮

- 第十一轮把 blocker 从“DTO 回正”推进到“停在 `视图树` 面板里也能切三端”：`Web/src/App.tsx` 把 `selected / targets / onSelect` 继续传给 `ViewTreePanel`，在树面板里新增 `视图树 target 切换` 区块；`Web/src/styles.css` 增加 `view-tree-target-list / chip` 样式，保证左侧树面板里能直接点 `DXY iPhone 15 / Pixel API 35 / DevEco Local`，而不是被迫切回“设备”面板。
- `Web/dev/appFallbackDom.test.mjs` 新增 mounted DOM smoke `keeps target switching available inside view-tree panel during request-failed fallback round-trip`，先切到 `视图树`，再在树面板内完成 `iOS -> Android -> Harmony -> iOS` 往返，直接断言 target 入口仍可见、`QA mock fallback` notice 不消失、树标题/节点切换、以及 app / bundle / network / logs 同步变化。
- 本地主控复跑 `cd Web && npm test`，现在是 `15/15` 全绿；`cd Web && npm run build`、`git diff --check -- Web/src/App.tsx Web/src/styles.css Web/dev/appFallbackDom.test.mjs docs-linhay/spaces/20260613-web-triad-qa/README.md docs-linhay/memory/2026-06-13.md`、`docs-linhay/scripts/check-docs.sh` 也都通过。
- 真实浏览器在同一个 `http://127.0.0.1:34127/?__tritonkit_mock_host_targets=request-failed` 会话里切到 `视图树` 后，`1200 x 820` 下 `scrollWidth === clientWidth === 1200`，左侧树面板内能看到 3 个 target chip；继续在该面板内切到 Android 时，树标题/节点变成 `Overloaded / DecorView / AndroidComposeView / settingsList`，DTO 同步变成 `Overloaded / overloaded.cn.debug / /api/catalog / ADB target ready: emulator-5556`；切到 Harmony 时，树标题/节点变成 `Triton Smoke / UIAbilityWindow / Column / settingsContent`，DTO 同步变成 `Triton Smoke / com.tritonkit.demo / /capabilities / HDC target discovered from plain list fallback`；再回 iOS 后，树标题/节点回到 `丁香园 / UIWindowScene / UIStackView / questionList`，DTO 也回正到 iOS 文案。
- 同一 `视图树` 错误态会话缩到 `960 x 820` 后，`scrollWidth === clientWidth === 960`，3 个 target chip 仍可见，当前 active tab 仍为 `视图树`，错误态 notice 仍保留；浏览器 console 仅有 Vite debug 与 React DevTools info，没有新的 error。第十一轮专属截图已归档到 `docs-linhay/spaces/20260613-web-triad-qa/screenshots/20260613/20260613-web-request-failed-view-tree-switching-after-v10.png`。

## Web 三角验收第十二轮

- 第十二轮把 blocker 换成“搜索框必须真实过滤 target，而不是只摆个输入框”：`Web/src/App.tsx` 接入 `targetSearch` 与 `filterTargetsBySearch`，让同一个搜索框同时过滤 `设备` 面板列表和 `视图树` target chip。
- 这一轮明确保留一个交互约束：搜索只过滤左侧入口，不自动切换当前选中 target。过滤结果变化和 target 真正切换依旧拆成两个动作，避免把只读浏览和 target 选中状态混在一起。
- `Web/dev/appFallbackDom.test.mjs` 新增 mounted DOM smoke `filters targets through the shared search box across devices and view-tree panels`，覆盖 `设备` 与 `视图树` 两个面板下的过滤、清空恢复，以及搜索期间已有 target DTO 恢复链路不被打坏。
- 本地主控复跑 `cd Web && npm test`，现在是 `16/16` 全绿；`cd Web && npm run build` 通过；文档写回后已补跑 `git diff --check`、`docs-linhay/scripts/check-docs.sh` 与 `docs-linhay/scripts/check-docs.sh`。
- 真实浏览器在 `http://127.0.0.1:34127/?__tritonkit_mock_host_targets=request-failed` 下补完搜索终验：`1200 x 820` 先搜 `Pixel`，`设备` 面板只剩 `Pixel API 35`；切到 `视图树` 后搜 `DevEco`，chip 只剩 `DevEco Local`；再搜 `DXY`，chip 只剩 `DXY iPhone 15`；清空后 `视图树` 和 `设备` 都恢复 `DXY iPhone 15 / Pixel API 35 / DevEco Local` 三端 target。
- 同一轮缩到 `960 x 820` 后再次验证 `Pixel` 过滤与清空恢复，`scrollWidth === width === 960`，没有横向溢出。第十二轮截图归档为 `docs-linhay/spaces/20260613-web-triad-qa/screenshots/20260613/20260613-web-request-failed-search-filtered-after-v11.png` 和 `docs-linhay/spaces/20260613-web-triad-qa/screenshots/20260613/20260613-web-request-failed-search-restored-after-v12.png`。
