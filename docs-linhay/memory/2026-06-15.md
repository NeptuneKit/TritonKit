# 2026-06-15

## Web 三角验收第十三轮

- 第十三轮继续沿用 `docs-linhay/spaces/20260613-web-triad-qa/` 的三角色闭环，目标从“搜索能过滤 target”推进到“搜索无结果时空态不能误导三端开发者”。
- 先补 mounted DOM smoke `shows search empty state across devices and view-tree panels without implying no running targets`，用 `zzzz-no-target` 验证 `设备` 与 `视图树` 两个面板的搜索空结果。红灯阶段旧实现会在新 smoke 上超时，因为 `设备` 面板仍把搜索空结果说成 `暂无运行中的仿真器`。
- `Web/src/App.tsx` 最小接入 `isSearching`：搜索态过滤为空时 `设备` 面板显示 `未找到匹配 target`，非搜索态真的无 target 时仍保留 `暂无运行中的仿真器`；`视图树` 面板继续使用同一搜索空态文案。
- `Web/dev/appFallbackDom.test.mjs` 的新 smoke 覆盖：无匹配搜索下 `设备` 面板不出现 `暂无运行中的仿真器`，切到 `视图树` 后仍显示 `未找到匹配 target`，清空搜索后恢复 `DXY iPhone 15 / Pixel API 35 / DevEco Local`，且 `QA mock fallback` notice、当前 iOS app / bundle 不被搜索污染。
- 本地主控复跑 `cd Web && npm test`，现在是 `17/17` 全绿；`cd Web && npm run build` 通过；`git diff --check`、`docs-linhay/scripts/check-docs.sh` 与 `docs-linhay/scripts/qmd-sync.sh` 均已通过。
- 真实浏览器打开 `http://127.0.0.1:34127/?__tritonkit_mock_host_targets=request-failed`：`1200 x 820` 下搜 `zzzz-no-target`，`设备` 面板和 `视图树` 面板都显示 `未找到匹配 target`，旧文案 `暂无运行中的仿真器` 不出现；当前应用仍为 `丁香园`，bundle 仍为 `cn.dxy.iDxyer`，错误态 fallback notice 仍保留。
- 同一浏览器会话清空搜索后，`视图树` target chip 和 `设备` row 都恢复三端 target；缩到 `960 x 820` 后重复搜索空态和清空恢复，`scrollWidth === width === 960`，没有横向溢出，console error 为空，仅有 Vite debug 与 React DevTools info。
- 第十三轮截图归档到 `docs-linhay/spaces/20260613-web-triad-qa/screenshots/20260615/`：`20260615-web-request-failed-search-empty-after-v13.png` 与 `20260615-web-request-failed-search-empty-restored-after-v13.png`。

## Web 三角验收第十四轮

- 第十四轮回应浏览器评论里点中的 `实时 1 fps`：该状态不能只是只读展示，三端开发者需要能把 Web mock 的实时预览刷新率调高，避免观察画面时过卡。
- 先补 mounted DOM smoke `lets users tune live preview fps without changing selected host target state`，红灯阶段旧实现缺少 `调整实时预览帧率` / `提高实时预览帧率` 控件，无法把 `1 fps` 调到 `15 fps`。
- `Web/src/App.tsx` 新增 per-target `previewFpsById`，把当前 target 的 fps 注入 `selectedWithScreenshot`，让右上角 badge、滑杆和右侧 `帧率` Metric 读同一份值；live preview loop 使用 `fpsToRefreshIntervalMs(selectedPreviewFps)`，不再固定 `900ms`。
- `Web/src/styles.css` 给右上角 live preview 区域增加滑杆和升/降帧按钮；这只改变 Web mock 本地预览刷新率，不新增 CLI / HTTP host action，不改写 app、bundle、network、logs DTO。
- `cd Web && npm test` 通过 18 项；`cd Web && npm run build` 通过；`git diff --check -- Web/src/App.tsx Web/src/styles.css Web/dev/appFallbackDom.test.mjs` 通过。
- 真实浏览器打开 `http://127.0.0.1:34127/`，`1257 x 963` 下真实点击 `提高实时预览帧率` 14 次后，从 `1 fps` 调到 `15 fps`；badge 显示 `实时 15 fps`，range value 为 `15`，右侧 `帧率` Metric 为 `15`，当前 app / bundle 仍为 iOS host target 的 `前台 App 未暴露 · iPhone 17` / `Target 60667794-96F8-40E6-8664-85538EC4663E`。
- 同一浏览器会话切到 Harmony 后 iOS 的 `15 fps` 不污染 Harmony；切回 iOS 后仍恢复 `15 fps`。`960 x 820` 下 `scrollWidth === width === 960`，无横向溢出，console error 为空，仅有 Vite debug 与 React DevTools info。
- 第十四轮截图归档到 `docs-linhay/spaces/20260613-web-triad-qa/screenshots/20260615/`：`20260615-web-live-fps-adjusted-after-v14.png` 与 `20260615-web-live-fps-adjusted-narrow-after-v14.png`。

## Web 三角验收第十五轮

- 回应浏览器评论“这个拖动条为什么不是点击才显示”：上一轮为了保证可发现性把滑杆常驻在画布右上角，但这会抢占设备画面注意力；更合理的交互是默认只保留 `实时 N fps` 状态胶囊，点击后才展开调节条。
- `Web/src/App.tsx` 在 `DeviceCanvas` 内新增 `isPreviewFpsOpen` 与 `previewControlRef`，默认不渲染 `调整实时预览帧率` input；点击 `实时 N fps` 胶囊展开，点外部或切换 target 后收起。
- `Web/src/styles.css` 把默认命中区域收窄为胶囊本身，仅展开时把 live preview control 宽度扩到 228px，避免透明区域截获画布点击。
- `Web/dev/appFallbackDom.test.mjs` 更新 fps smoke：先断言默认状态没有调节 input，再点击 `展开实时预览帧率控制`，随后用升帧按钮调到 `15 fps` 并继续确认 badge、slider、Metric 同步。
- `cd Web && npm test` 通过 18 项；`cd Web && npm run build` 通过；`git diff --check -- Web/src/App.tsx Web/src/styles.css Web/dev/appFallbackDom.test.mjs` 通过。
- 应用内浏览器后台 tab 在本轮验证时崩溃，但 dev server `127.0.0.1:34127` 正常返回 `200 OK`；改用独立浏览器自动化验证同一 URL。`1257 x 963` 下默认只有 `展开实时预览帧率控制` 胶囊，没有滑杆；点击后出现滑杆和按钮，调到 `15 fps` 后 badge、range、右侧 Metric 同步为 `15`。
- 独立浏览器 `960 x 820` 下 `scrollWidth === width === 960`，没有横向溢出，console error 为 0。
- 第十五轮截图归档到 `docs-linhay/spaces/20260613-web-triad-qa/screenshots/20260615/`：`20260615-web-live-fps-collapsed-after-v15.png` 与 `20260615-web-live-fps-expanded-after-v15.png`。

## Web 运行日志接入第一版

- 回应浏览器评论“这个能对接 app 的日志吗？”：先落一版 iOS host-side bounded 日志接入，不直接宣称已接 embedded app runtime 日志。
- 更新 `docs-linhay/spaces/20260611-web-mock-ui/README.md`，补充 `2026-06-15 iOS host-side bounded 日志接入` 范围说明、BDD 场景和验收标准，明确当前只读 `/web/host-logs` 仅支持 iOS running target，Android / Harmony 继续沿用占位日志。
- `Web/dev/iosSimulatorBridge.mjs` 新增 `/web/host-logs?platform=ios&target=<udid>`：通过 `triton sim logs --simulator <udid> --output <tmp.ndjson> --duration 2 --style ndjson --json` 采集一小段 bounded OSLog，解析 ndjson 行为 `{time, level, message}`，并把临时 artifact 路径脱敏为 `<tmp.ndjson>`。
- `Web/src/data/iosSimulatorClient.ts` / `Web/src/types.ts` 新增 host logs DTO；`Web/src/App.tsx` 增加 `hostLogsById` 缓存，选中 readonly iOS host target 时自动拉取 `/web/host-logs`，成功后让 `运行日志` 面板优先展示真实 host-side 日志，失败则继续回退 `selected from readonly host discovery` / `network/app runtime evidence not exposed by CLI DTO` 占位日志。
- 测试先行：`Web/dev/iosSimulatorBridge.test.mjs` 新增 `/web/host-logs` 成功与非 iOS 501 场景；`Web/dev/appFallbackDom.test.mjs` 新增 mounted DOM smoke，验证页面确实渲染 `App launched` / `Network timeout` 两条 iOS host logs，而不是只存在 route。
- 本地验证：`cd Web && npm run test` 通过 21 项；`cd Web && npm run build` 通过；`git diff --check` 通过。
- 实际浏览器验证：打开 `http://127.0.0.1:34127/` 后，`运行日志` 面板已显示真实 iOS host-side 日志，不再只是占位提示；当前可见条目包括 `WebContent...invalidated...`、`LookinServer - frame in type=200...`、`Resume Connection To Game Controller Daemon` 等真实日志。浏览器 console error 为 0。
