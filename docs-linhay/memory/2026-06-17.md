# 2026-06-17

## 编译 triton 并启动 Web 脚本

- 新增 `docs-linhay/scripts/start-web-with-triton.sh`，用于本地先构建 `triton` CLI，再启动 `Web` Vite dev server。
- 默认使用 Debug 构建和独立 scratch path `.build/web-dev-cli`，避免覆盖 release 验证产物；需要发布形态时可传 `--release`。
- 脚本启动 Web 时显式注入 `TRITONKIT_TRITON_BIN=<built-triton>`，确保 dev bridge 使用本轮构建产物。

## Web 三角验收第十六轮

- 本轮 blocker 是 Web host input 只读边界回归：红灯时 `Web/src/App.tsx` 存在 `??` / `||` 混用的 SSR transform 语法错误，且 `/web/host-input` POST 实际返回 `502`，没有固定为只读 `405`。
- 已删除前端 `dispatchHostInput` 调用，点击 / 拖动只记录本地 readonly warning 与 interaction log；`Web/dev/iosSimulatorBridge.mjs` 的 `/web/host-input` 固定返回 `405 web_host_input_readonly`，并移除 `triton serve` 转发 helper、body 解析和 runtime 探测链路。
- 已删除 `Web/src/data/iosSimulatorClient.ts` 的 `dispatchHostInput` 与 `Web/src/types.ts` 的 `HostInputResponse`，避免 Web mock 重新暴露业务控制入口。
- 验证：`cd Web && npm test` 通过 21 项，`cd Web && npm run build` 通过，相关文件 `git diff --check` 通过；dev server 下 POST `/web/host-input` 返回 `405` 与 `web_host_input_readonly`。
- 浏览器验收：`1200 x 820` 下页面首屏正常、live fps 控件仍默认收起；`960 x 820` 下无横向溢出，console error 为空。截图归档到 `docs-linhay/spaces/20260613-web-triad-qa/screenshots/20260617/20260617-web-host-input-readonly-after-v16.png`。

## v0.1.19 发布准备

- 用户要求提交、推送并发布当前状态。当前最新 tag 为 `v0.1.18`，本轮按补丁版本推进到 `v0.1.19`。
- 发布前聚焦门禁：`cd Web && npm test` 通过 21 项，`cd Web && npm run build` 通过。
- `docs-linhay/scripts/qmd-sync.sh` 中 `qmd update` 已完成；`qmd embed` 在本机触发 `node-llama-cpp` / `ggml-metal-device` assert，最终 `Abort trap: 6`，属于已知本机 embedding 后端问题。本轮不把它作为 Web mock 或 release asset 代码 blocker，正式发布仍以 release script preflight 与 GitHub CI 为准。
- 已提交 `a4f232a feat: harden web mock triad qa`，并随本地既有 `4b7e98c Add web startup script with triton build` 一起推送到 `main`。
- 已运行 `docs-linhay/scripts/release.sh v0.1.19 --yes`。本地 preflight 通过：SwiftPM dependency boundary、iOS DEBUG isolation、Swift tests 185 项、release CLI build / smoke、Harmony host smoke、iOS runtime observe smoke、iOS Simulator build、docs structure、git diff whitespace check。
- `v0.1.19` tag 已推送并发布完成：GitHub Release 为 `https://github.com/NeptuneKit/TritonKit/releases/tag/v0.1.19`，最终资产包含 `triton-macos-arm64.tar.gz`、`triton-macos-x86_64.tar.gz`、`tritonkit-skills.tar.gz`、`tritonkit_checksums.txt`；Homebrew 路径为 `brew install NeptuneKit/tap/triton`。Release run `27668011230` 全部 job 成功，包括 x86_64 backfill 与 Homebrew tap 二次更新。
