# 2026-06-18

## v0.1.21 Homebrew 本机验证与 x86 后补修复

- 本机 arm64 Homebrew 已从 `neptunekit/tap/triton 0.1.20` 升级到 `0.1.21`，`/Users/linhey/.local/bin/triton -> /opt/homebrew/bin/triton -> ../Cellar/triton/0.1.21/bin/triton` 额外 symlink 链路下，`triton web --print-command --json` 返回 `mode=packaged`，`bundledWebRoot=/opt/homebrew/share/triton/web`。
- 本机实际启动 `triton web --host 127.0.0.1 --port 34131` 后，`/` 与 SPA fallback 返回 `200 text/html`，`/web/host-input` 返回 `405 web_host_input_readonly`，确认 Issue #64 的 Homebrew Web root 修复对 brew 安装可用。
- 观察 `v0.1.21` tag workflow 时发现 x86_64 后补 job 在 `Merge release checksums` 失败：`tar -xOf ... README.txt | grep -Fq ...` 命中后 grep 提前关闭 pipe，GNU tar 报 stdout write error。
- 已把 arm64 与 x86_64 release README 校验改为先提取到临时文件再 grep，并在 `verify-release-automation.sh` 增加契约检查，禁止恢复为 `tar | grep -q` 写法。
- 已发布 `v0.1.22`，GitHub Actions run `27706566656` 全部成功：validate、arm64 package、Homebrew arm64 更新、x86_64 asset 后补和 Homebrew x86_64 backfill 均为 success；Release 资产包含 `triton-macos-arm64.tar.gz`、`triton-macos-x86_64.tar.gz`、`tritonkit-skills.tar.gz` 与 `tritonkit_checksums.txt`。
- 本机 Homebrew 已升级 `neptunekit/tap/triton 0.1.21 -> 0.1.22`；`triton version --json` 返回 `version=0.1.22`，`triton web --print-command --json` 返回 `mode=packaged` 与 `/opt/homebrew/share/triton/web`。
- 本机 `v0.1.22` 实际服务 smoke：`triton web --host 127.0.0.1 --port 34132` 启动成功，`/` 和 `/device/host` 返回 `200 text/html`，`/web/host-input` 返回 `405 web_host_input_readonly`；额外 PATH symlink `/Users/linhey/.local/bin/triton` 调用也能解析到 `/opt/homebrew/Cellar/triton/0.1.22/share/triton/web`。

## 整体发布版本一致性

- 用户明确整体发布应覆盖各端内置包入口，不能只完成 CLI/Homebrew/Web bundle。
- 新增 `docs-linhay/scripts/verify-release-package-versions.sh <version>`，校验 `TritonKit.podspec`、`TritonKitShared.podspec`、`Web/package.json`、`Web/package-lock.json` 均等于 release tag 版本；`docs-linhay/scripts/release.sh` 在打 tag 前强制执行该检查。
- 将 CocoaPods podspec 与 Web package manifest 同步到 `0.1.23`，准备通过新 patch tag `v0.1.23` 作为整体发布版本；已发布的 `v0.1.22` 不移动 tag。
- 沉淀内部 skill：新增 `.agents/skills/tritonkit-release-package-governance/SKILL.md`，用于后续整体发布、各端内置包版本同步、Homebrew/Web/SwiftPM/CocoaPods/public skill 包一致性发版。
- `v0.1.23` 已推送 tag；用户进一步要求对外 `TritonKit.skills/` 包也提供更新 skill。按不移动已发布 tag 规则，继续推进 `v0.1.24`。
- 新增 public skill `TritonKit.skills/tritonkit-update`，指导外部用户更新 Homebrew CLI、替换 `TritonKit.skills/` bundle、清理旧顶层 skill 目录，并对齐 SwiftPM/CocoaPods App 依赖到同一 release tag。
- 将整体发布 manifest 版本推进到 `0.1.24`，准备发布包含 public update skill 的完整包。
- `v0.1.24` tag CI 的 arm64 release asset packaging 失败：workflow 中 release asset set 仍硬编码三个 public skills，未把 `tritonkit-update` 纳入 tarball 内容和 `BUILD_INFO.json` expected list。修复后继续按不移动 tag 规则推进 `v0.1.25`。
- `v0.1.25` tag CI 的 x86_64 后补发布失败：`set -o pipefail` 下 `tar -tf ... | grep -Eq ...` 命中后 grep 提前退出，GNU tar 收到 SIGPIPE 并返回 `tar: stdout: write error`。
- 已将 arm64 与 x86_64 tar 内容校验改为 `tar -tf > contents-file` 后再 grep，并扩展 `verify-release-automation.sh` 禁止恢复 `tar -tf | grep -q` 类写法；整体 manifest 版本推进到 `0.1.26`，准备重新发布。
- 发布观察中确认 tag CI 仍被完整 validate 队列阻塞：`v0.1.26` 必须等待 Swift tests 与两个 podspec lint 后才开始 arm64 release asset 打包。已取消仍在跑的 `v0.1.26` tag run，且不移动旧 tag；已优化 `ci-validate-mode.sh`：`v*` tag 固定走 `contracts` 快检，发布链路依赖 release script 本地门禁与 main/PR validate，不再重复等待远端完整 validate；整体 manifest 版本推进到 `0.1.27`。
- `v0.1.27` tag validate 快路径成功，但 release jobs 因 `github.ref` 条件未命中而全部 skipped，未创建 Release 资产。已将 release job 条件统一改为 `github.ref_type == 'tag' && startsWith(github.ref_name, 'v')`，并让 `release.sh` 在 tag run 成功但未生成 arm64 资产时快速失败；整体 manifest 版本推进到 `0.1.28`。
- `v0.1.28` tag 仍显示 release jobs skipped，说明 `github.ref_type` 在当前 job 条件里不可靠。已改为基于触发集合的保守判断：release build 在 `workflow_dispatch` 或 `push && github.ref_name != 'main'` 时运行，release 上传只在 `push && github.ref_name != 'main'` 时运行；当前 workflow 只监听 `main` branch 和 `v*` tags，因此该条件等价于 tag push。整体 manifest 版本推进到 `0.1.29`。
- `v0.1.29` tag 仍显示 release jobs skipped，说明 job-level GitHub ref 判断仍不可靠。已改为由实际运行的 `classify-validate` bash step 输出 `release=true`，后续 release jobs 依赖 `needs.classify-validate.outputs.release == 'true'`；整体 manifest 版本推进到 `0.1.30`。
- `v0.1.30` classify log 已确认 `release tag: true`，但 release jobs 仍 skipped；已将所有依赖 `needs.classify-validate.outputs.release` 的 job-level `if` 改为显式 GitHub expression：`${{ ... }}`。整体 manifest 版本推进到 `0.1.31`。
- `v0.1.31` release jobs 仍 skipped；将 classify output 从 `release` 改为 `is_release_tag`，避免 output 名与 GitHub release 语义冲突或表达式歧义。整体 manifest 版本推进到 `0.1.32`。
- `v0.1.32` release jobs 仍 skipped。停止继续在 CI workflow 内调 job-level tag 条件，新增独立 `Release` workflow，只监听 `v*` tags 并直接运行 release jobs；`release.sh` 默认观察 `Release` workflow。整体 manifest 版本推进到 `0.1.33`。
- `v0.1.33` 独立 `Release` workflow 已让 `CI` tag run 走 8 秒左右的 contracts 快路径，arm64 CLI、skills、checksum 与 Homebrew tap 可先发布；长尾耗时集中在 `macos-15-intel` 的 x86_64 Swift release build。
- 本机验证 `swift build --package-path CLI --scratch-path /tmp/tritonkit-cli-x86-cross -c release --product triton --triple x86_64-apple-macosx13.0` 可在 arm64 Mac 产出 `Mach-O 64-bit executable x86_64`。据此将独立 `Release` workflow 的 x86_64 job 改为 `macos-15` 上交叉编译，并在上传前用 `file` 校验架构；整体 manifest 版本推进到 `0.1.34`。
- `v0.1.34` 继续显示 Swift release build 是主要耗时，因此为独立 `Release` workflow 的 arm64 与 x86_64 job 增加 SwiftPM dependency/build cache，分别使用 `release-cli-arm64` 与 `release-cli-x86_64` cache key，并将整体 manifest 版本推进到 `0.1.35`。
- `v0.1.35` Release workflow run `27736403675` 已全部成功：CI tag run 只跑 contracts 快路径，独立 Release 完成 arm64 CLI、x86_64 CLI、skills bundle、checksum manifest、Homebrew arm64 更新与 x86_64 backfill。GitHub Release 资产包含 `triton-macos-arm64.tar.gz`、`triton-macos-x86_64.tar.gz`、`tritonkit-skills.tar.gz`、`tritonkit_checksums.txt`。
- 已验证 `tritonkit-skills.tar.gz` 解包入口为整个 `TritonKit.skills/` 文件夹，`BUILD_INFO.json` 版本为 `0.1.35`，包含 `tritonkit-dev-feedback`、`tritonkit-emulator-cli-takeover`、`tritonkit-real-project-regression`、`tritonkit-update`。
- 本机 Homebrew 从 `neptunekit/tap/triton 0.1.22` 升级到 `0.1.35`；`triton version --json` 返回 `0.1.35`。从 `/tmp` 验证 `triton web --print-command --json` 返回 `mode=packaged` 与 `/opt/homebrew/Cellar/triton/0.1.35/share/triton/web`，实际启动 `triton web --host 127.0.0.1 --port 34136` 后 `/` 与 `/device/host` 返回 `200 text/html`，`/web/host-input` 返回 `405 web_host_input_readonly`。

## 整理沉淀：Release CI 耗时治理

- 本轮可复用模式是 release 耗时治理：普通 `CI` 保持 validate-only，`v*` tag 走 contracts 快路径；独立 `Release` workflow 负责产物构建；arm64 先发布，x86_64 通过 arm64 runner SwiftPM cross build 后补；Release build job 使用架构分离的 SwiftPM cache。
- 已同步当前规则入口：`AGENTS.md`、`.agents/skills/tritonkit-ops-governance`、`.agents/skills/tritonkit-release-package-governance`、`docs-linhay/dev/20260519-github-ci-release-artifacts.md`、`docs-linhay/dev/20260519-homebrew-binary-release.md` 与 `docs-linhay/dev/20260525-session-distillation.md`。
- 本次不纳入：v0.1.24-v0.1.34 的中间失败细节不升级为新 AGENTS 规则；历史 session 文档中作为当时事实存在的旧 runner 描述不全量重写，只修正仍作为当前 runbook 的入口。

## Web Device Hub 本机启动失败治理

- 本机排查确认 `127.0.0.1:34127` 被 PID 61415 的旧 `triton web` 占用，进程仍指向 `/opt/homebrew/Cellar/triton/0.1.22/share/triton/web`；该路径缺少 `index.html`，所以新 `triton web` dev 模式触发 Vite `Port 34127 is already in use`，浏览器访问 `/` 则命中旧服务的 `web_static_asset_failed` JSON。
- 修复 `triton web` 启动韧性：启动前探测目标 host/port，已占用时返回稳定错误 `web_port_in_use`，hint 给出 `lsof -nP -iTCP:<port> -sTCP:LISTEN` 与 `triton web --port <port>` 恢复路径，避免低层 npm/Vite 报错成为第一层输出。
- 修复 packaged static 缺失体验：packaged server 启动前重新校验 bundled web root；运行期若静态资源缺失，浏览器文档路径返回可读 HTML 诊断，保留 `web_static_asset_failed`、缺失路径和恢复命令，API 路径继续保持 JSON envelope。
- 已更新 `docs-linhay/spaces/20260611-web-mock-ui/README.md` 的 BDD 场景、验收标准与实现记录。验证：`swift test --package-path CLI --filter WebCommandTests` 通过 14 tests；`CLI/.build/debug/triton web --json` 在端口占用状态下返回单个 `web_port_in_use` JSON envelope。随后停止 stale PID 61415，短暂启动 `CLI/.build/debug/triton web --no-install`，`/` 返回 `200 text/html`；验证后停止 Vite 子进程，34127 当前无 listener。

## Web Device Hub 诊断入口

- 用户要求按主流 CLI 方式开独立 space 处理端口占用 / stale server / packaged static 缺失的诊断层；已新建 `docs-linhay/spaces/20260618-web-launch-diagnostics/README.md`，限定为 CLI 只读诊断，不扩展 Web/Wails 控制面。
- 新增 `triton web status --json`：返回 `web.status`、host/port/url、`portListening`、可选 HTTP probe 与恢复建议；端口占用时建议 `lsof -nP -iTCP:<port> -sTCP:LISTEN` 与 `triton web --port <port>`，空闲时建议 `triton web`。
- 新增 `triton web doctor --json`：在 status 基础上输出 `checks[]` 与 `healthy`；当 probe 识别 `web_static_asset_failed` 时，`web-static-assets` check 为 failed，并建议重新安装 packaged release 或使用 checkout `--root`。
- 同步 `web` schema：新增 `status` / `doctor` subcommands 与 `web.status` / `web.doctor` output contracts。验证：`swift test --package-path CLI --filter WebCommandTests` 通过 17 tests；`CLI/.build/debug/triton web status --json` 与 `CLI/.build/debug/triton web doctor --json` 在空闲端口状态下返回合法 JSON，`doctor.healthy=true`。

## Web Device Hub 真机 App runtime mirror

- 用户在 Web mock 设备列表处标注“需要支持真机，我目前连接了真机”。本轮将 `/web/host-targets` 的 Vite dev bridge 与 packaged Swift bridge 同步扩展为读取 `triton device list --platform ios|android|harmony --scope real --json`。
- Web 前端新增 `scope/kind/blockedReasons/sensitive` 字段映射；`ready=true` 且 `scope=real` 或 `kind=real-device` 的 target 在 Devices navigator 与 canvas 中显示为“真机”。根据用户反馈“不在线的不用显示”，`ready=false`、离线、未信任或 DDI 缺失的真机不进入左侧设备列表，诊断仍保留在 CLI / HTTP 契约中。
- 用户继续反馈 Android / Harmony 未启动、iPhone 未接线仍显示。定位到两类原因：host tool 的 `ready=true` 可能来自 iOS wireless / localNetwork 配对或 HDC/ADB 可见状态；同时 Web 在 host targets 非空时仍按缺失平台补 QA mock。已收敛为：真机必须 `ready=true` 且 `transport=wired|usb` 才进入左侧；host targets 非空时不补齐 QA mock Pixel / DevEco。
- 重启 34127 后验证 `/web/host-targets` 当前只返回 2 个目标：1 个 Booted iOS Simulator 与 1 个 `transport=wired` iOS 真机；Android / Harmony 未再返回，`transport=localNetwork` 的 iPhone 也被过滤。
- 用户追问真机为什么一直显示“正在获取实时画面”，随后明确可接受底线是“App 启动 + App 实时画面 + 支持模拟手势”，并允许参考 Lookin。调研 LookinServer README 后确认其核心模式是 Debug App 集成 iOS Framework，由 App 内 server 暴露视图调试能力；本轮采用同类 App runtime mirror 路线，不依赖 Xcode/devicectl 整机截图。
- Web iOS 真机不再显示“真机画面未接入”作为最终态；ready+wired 真机进入 canvas 后会请求 `/web/host-screenshot?scope=real&kind=real-device&source=runtime`，Vite bridge / packaged bridge 转为 `triton screenshot --output <tmp> --json`，读取业务 App embedded runtime screenshot。当前本机未启动 `triton serve`，endpoint 按预期返回 `app_runtime_unavailable`，提示启动 `triton serve --host 127.0.0.1 --port 19421` 并启动接入 TritonKit 的 Debug App。
- 有 App runtime screenshot 后，Web canvas 的 tap / swipe 通过 `/web/host-input?scope=real&kind=real-device&source=runtime` 转发为 `triton input --json`；坐标沿用 App runtime screenshot 的 UIKit point 坐标，不走系统级 HID。App 启动仍以 `triton app launch --platform ios --scope real --device <selector> --bundle-id <id> --json` 为事实入口，Web 不猜测 bundle id。
- 验证：`npm test` 通过 25 tests；`npm run build` 通过 TypeScript 与 Vite build；`swift build --package-path CLI --scratch-path .build/cli --product triton` 通过；`swift test --package-path CLI --scratch-path .build/cli --filter Web` 通过 49 个 Web 相关测试；`bash -n docs-linhay/scripts/start-web-with-triton.sh` 通过，并修复该脚本空 `extra_vite_args` 在 `set -u` 下导致无法启动的问题。重启 34127 后 `/web/host-targets` 当前只返回 Booted iOS Simulator 与 `transport=wired` 的 iOS 真机，Android/Harmony 未启动或非 USB 目标未进入列表；真机 runtime screenshot endpoint 在 App runtime 未连接时返回稳定 `app_runtime_unavailable`。
- 用户继续要求安装测试 App 到真机并顺便测试 Triton 编译/安装能力。Triton-first 验证 `triton xcode discover/settings` 可识别 `Examples/TritonKitDemo`；真实 iOS device build 发现 `triton xcode build` 缺少自动签名授权参数，本轮新增 `--allow-provisioning-updates` 并验证 schema、CLI fake `xcodebuild` smoke 与真实 build invocation 都带 `-allowProvisioningUpdates`。
- 真机安装未执行到成功路径，原因是本机 Xcode 账户/profile 前置条件不满足：没有可用开发者账户凭证与 `com.neptunekit.tritonkit.demo` Development provisioning profile，现有未过期 Development profile 也不包含当前可用开发证书。该 blocker 不写入 team id、完整设备标识或证书细节；恢复条件是登录 Xcode Accounts 或提供匹配 bundle id / 设备 / 本机私钥证书的 Development profile。
- `TritonKitDemo` 新增 `TRITONKIT_DEFAULT_HOST` Info.plist build setting，允许真机构建时注入 Mac 可达 IP；未设置时继续回落 `127.0.0.1`，保证模拟器默认体验。验证 `triton xcode build --sdk iphonesimulator` 对 demo 构建成功，覆盖 App.swift 与 Info.plist 改动。
- 用户补齐 signing 后继续验证：不指定 `DEVELOPMENT_TEAM`、仅通过 xcconfig 注入 `CODE_SIGN_STYLE=Automatic` 与 `TRITONKIT_DEFAULT_HOST=192.168.228.128`，`triton xcode build --sdk iphoneos --allow-provisioning-updates` 成功签出 `TritonKitDemo.app`；`triton app install --platform ios --scope real --device ios-real:73f725dfa795` 成功安装，`triton app launch --bundle-id com.neptunekit.tritonkit.demo` 成功启动。
- 真机 runtime mirror 闭环已跑通：`triton serve --host 0.0.0.0 --port 19421` 后，`triton status --json` 返回 `connected=true`、`runtime=embedded`、`targetCount=1`；`triton screenshot --output /tmp/tritonkit-real-runtime.png --json` 产出 402x874 PNG；`triton input --json --summary --strict` tap 命中 `UIButton` 且 `failedCount=0`；重启 34127 到最新 bridge 后，`POST /web/host-input?...source=runtime` 返回 `ok=true`，Web bridge screenshot 也返回 `ok=true`。
- 用户在 Web canvas 标注“滑动条不支持拖动”。定位到 Web 已把拖拽转发为 runtime `swipe`，但 iOS embedded runtime 的 `performSwipe` 只支持 `UIScrollView`，命中 `UISlider` 时返回 “Hit view is not inside a UIScrollView”。本轮补 `UISlider` 专用 `slider-drag` 策略：按拖拽 end point 映射 `minimumValue...maximumValue`、发送 `.valueChanged`，并新增 `TKRuntimeInputActionsTests` 覆盖。真机重装 Demo 后，`POST /web/host-input?...source=runtime` 返回 `ok=true`、`strategy=slider-drag`，截图 `/tmp/tritonkit-slider-after-drag.png` 显示滑块进度更新到 93%。
- 用户继续指出左侧 iOS 真机仍显示“前台 App 未暴露”。定位到设备列表只消费 host `device list` DTO，devicectl 真机发现未提供 foreground app；而画布来自 embedded runtime screenshot/input，是另一条证据链。`triton runtime manifest --json` 与 `triton state app --json` 当前默认 target 都因 `triton list --json` 为空返回 `target_not_found`，不能可靠 join 真实 App identity。本轮前端改为：iOS 真机缺 `appName` 时初始显示“App runtime 镜像”；当 `/web/host-screenshot?...source=runtime` 成功且 `runtimeScope=app-runtime` 后，将对应 target 文案同步为“App runtime 已连接”，不再显示“未暴露”。
- 用户要求设备列表“不显示图标，在右上角显示平台角标”。本轮移除 `.device-row-icon` 渲染，将平台信息改为 `.device-platform-badge` 放在设备行右上，版本号保留在右侧下方；DOM 测试覆盖不再渲染图标且存在 `iOS` 平台角标。
- 用户指出底部“主屏幕”按钮需要在设备支持时才显示。该按钮属于系统级设备控制，当前 iOS 真机 App runtime mirror 只提供 App 内 screenshot/input，不支持系统 Home；本轮将 `DeviceControls` 改为按能力渲染，`screenshotSource=runtime` 的 target 不再显示“主屏幕”，DOM 测试覆盖该按钮缺失。
- 用户继续指出点击画布不能输入文字、Backspace 无效、设备列表第二行不应显示 runtime 状态。本轮统一键盘输入链路：Web 画布聚焦后把字符键、粘贴、Backspace/Delete 转发到 `/web/host-input`；共享输入契约新增 `deleteBackward`；embedded runtime 对 UIKit `UIKeyInput` 与 WKWebView DOM 输入框分别执行插入/单字符删除。`App runtime 已连接` 不再写入 `appName`，设备行第二行只显示 App 名称或 `前台 App 未识别`，平台保留为右上角角标。
- 验证：`swift test` 通过 189 tests；`cd Web && npm test && npm run build` 通过 25 tests 与 Vite build。
- 用户在 iOS Simulator Web canvas 标注“为什么我点不掉这个 alert”。根因是前端只给 iOS real-device runtime mirror 打开 `canInput`，iOS Simulator 虽有真实 screenshot 但点击不会发 `/web/host-input`；同时 Vite dev bridge 对 host simulator input 仍返回 real-device-only unsupported。
- 本轮修复 ready iOS Simulator canvas 的 tap/swipe 输入：前端为 simulator target 开启 `canInput`，请求携带 `scope=simulator&kind=simulator&source=host`；Vite bridge 将 host simulator/emulator input 代理到 `triton serve /web/input?target=host:<platform>:<target>`，必要时自动拉起 `triton serve --host 127.0.0.1 --port 19421`。
- 验证：`npm --prefix Web test` 通过 27 tests；`npm --prefix Web run build` 通过；`git diff --check` 通过。in-app Browser 刷新验证被 Browser URL policy 拦截，未做浏览器点击实测。
- 用户继续指出点击 App 输入框后 Web 端也需要提供输入框进行键盘事件联动。本轮新增 canvas keyboard relay：真实 screenshot 短 tap 后在 tap 附近显示并聚焦 `设备键盘输入` input，新增文本转发为 `type`，清空/删除已有文本转发为连续 `deleteBackward`，粘贴转发为 `paste`，Escape 收起。该 relay 不解析 screenshot 或猜测 App 控件身份，仍以先发 tap 与 Triton input 结果为事实。
- 验证：`npm --prefix Web test` 通过 27 tests；`npm --prefix Web run build` 通过。
- 用户指出底部 `网络证据` strip 需要支持关闭，并进一步指出控制入口应放在画布左下设备控制面板。本轮设备控制胶囊新增 `隐藏网络` / `显示网络` 按钮，和已有日志按钮一起控制下方面板；`NetworkStrip` 自身保留 `隐藏网络证据` 就近入口。网络和日志面板可独立显示/隐藏；只关闭网络时日志占满底部，只关闭日志时网络占满底部；两者都关闭时底部证据区域直接折叠，不再显示 `证据面板已隐藏` 状态条，恢复入口只保留在画布控制胶囊。
- 验证：`npm --prefix Web test` 通过 28 tests；`npm --prefix Web run build` 通过。
- 会话沉淀：将 screenshot canvas 输入 relay 与 evidence panel hide/restore 两个可复用 UI 模式写入 `.agents/skills/tritonkit-web-mock-ui`。本轮不升级 AGENTS，因为规则限定在 Web mock UI 交互实现，不是 repo-wide 长期治理规则。
