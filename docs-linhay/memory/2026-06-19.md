# 2026-06-19

## iOS 内置 SDK 能力门控 P1 内部拆分

- Space：`docs-linhay/spaces/20260618-ios-runtime-capability-gating/`
- 新增计划：`plans/20260619-p1-internal-modularization.md`
- 先补 `TKPlatformFallbackTests` 红灯用例，覆盖 request message 到 runtime capability/action 的映射，以及 input / WebView 禁用响应的模型级 JSON 形状。
- 新增 `Sources/TritonKit/TKRuntimeCapabilityGate.swift`，集中承载 capability 映射、action 名称和 `capability_disabled` 响应构造。
- 将 `TritonKit.Configuration.isRuntimeCapabilityEnabled(_:)` 从 `TritonKit.swift` 移入 capability gate 文件，保持 public `TritonKit` product 和 App 接入 API 不变。
- `TritonKitRequestHandler` 只在路由入口调用 gate，后续 Observe / Input / WebView / Semantic handler 继续拆分时可复用同一能力边界。

验证：

- `swift test --filter TKPlatformFallbackTests/capabilityGateMapsRequestMessages`
- `swift test --filter TKPlatformFallbackTests/capabilityGateBuildsDisabledResponses`
- `swift test --filter TKPlatformFallbackTests`
- `swift test`
- `docs-linhay/scripts/check-docs.sh`
- `git diff --check`

## iOS 内置 SDK 能力门控 P2 Handler Domain Split

- Space：`docs-linhay/spaces/20260618-ios-runtime-capability-gating/`
- 在 P1 计划文件追加 P2：`plans/20260619-p1-internal-modularization.md`
- 新增 `TKRuntimeRequestDomain`，将 request type 显式映射到 `control`、`observation`、`input`、`webView`、`semantic`、`ledger`、`legacyInspection` 和 `unsupported` 域。
- `TritonKitRequestHandler.swift` 收缩为 runtime guard、capability gate、domain routing、ledger recording 和共享错误消息。
- 新增域扩展文件：`TritonKitRequestHandler+Observation.swift`、`+Input.swift`、`+WebView.swift`、`+Semantic.swift`、`+LegacyInspection.swift`，保持 public product 和 wire payload 不变。
- 新增 `TKPlatformFallbackTests.requestTypesMapToHandlerDomains`，先红灯确认 domain 边界不存在，再实现转绿。

验证：

- `swift test --filter TKPlatformFallbackTests/requestTypesMapToHandlerDomains`
- `swift test --filter TKPlatformFallbackTests`
- `swift test`

## Web Device controls 探测按钮反馈

- 用户在 `127.0.0.1:34127` 的设备控制胶囊标注“探测”为何点击无反应。定位到 `Web/src/App.tsx` 中 `DeviceControls` 的 `应用 / 点选 / 探测` 只有渲染，没有任何 `onClick`、`aria-pressed` 或 active 态。
- 修正：新增本地 `DeviceCanvasTool` 状态，默认 `point`；`应用 / 点选 / 探测` 现在可切换 `.is-active` 与 `aria-pressed`，并把 `.device-screen` 的 aria-label 更新为 `设备画面，当前工具 ...`。同时根据工具切换 cursor：`点选=crosshair`、`探测=zoom-in`、`应用=default`。
- 边界保持不变：`探测` 目前只提供本地 UI 反馈，不伪造新的 probe/inspect 后端契约；实际 host input 仍沿用现有 `点选` 交互链。

- 用户随后追问设备控制面板上是否“很多都没用”。复查确认 `应用`、`主屏幕`、`竖屏/横屏` 在当前 Web mock 中都没有真实事件链路或契约支撑，只是历史视觉占位。
- 修正：设备控制胶囊收敛为只展示真正可用的按钮：`点选`、`探测`、网络显隐、日志显隐。删除 `应用`、`主屏幕`、`竖屏/横屏` 这些伪可点击控件，避免误导用户以为它们可操作。
- 同步把工具态缩为 `point | probe`；`探测` 继续保留本地 inspect 模式反馈，`点选` 保持 host input 行为。

验证：

- `npm --prefix Web test`
- `npm --prefix Web run build`

## Web host hierarchy endpoint

- 在 Web dev bridge 增加只读 `/web/host-hierarchy` endpoint，按 `platform` 与 `target` 返回 `HostHierarchyResponse.scene`，scene 结构与 Web `HierarchyScene` DTO 对齐。
- `探测` 模式优先消费 host hierarchy endpoint；endpoint 失败时回退内置 iOS / Android / Harmony mock scene，避免 dev bridge 缺失导致原型不可用。
- CLI 侧新增 shared `TKHierarchyScene` / `TKHostHierarchyResponse` DTO，并扩展 `triton hierarchy --platform ios|android|harmony --json`：iOS 来自 embedded runtime Lookin-style `displayItems[]` / UIView-CALayer tree，Android 来自 UIAutomator XML，Harmony 来自 `uitest dumpLayout`。
- Web dev bridge 已从内置 mock 改为调用 `triton hierarchy --platform <platform> --target <target> --json`；命令失败返回 `web_host_hierarchy_failed`，未知平台才返回 `web_host_hierarchy_platform_not_supported`。
- `triton schema --command hierarchy --json` 和 `triton capabilities --json` 已暴露 `hierarchy-scene`、`android-hierarchy`、`harmony-hierarchy`。当前仍未实现 per-node screenshot / surface slice，不能声明完整 Lookin parity。
- 现场补验：保存 Triton-first `status/doctor/capabilities/schema` 证据到 `docs-linhay/spaces/20260619-lookin-hierarchy-viewer/evidence/20260619/`；iOS Simulator `F4E55B8E-0141-4C46-9965-263CCE782B5F` 可返回 `TKHostHierarchyResponse.scene`，另一台 Booted simulator 因未连接 embedded runtime 返回 `target_not_found`。
- Android 当前没有已连接 emulator；修复 `hierarchy --platform android --json` 默认 selector 被 `triton:local` 污染的问题，并保证无 target 时返回 JSON error envelope。Harmony 当前无 DevEco Emulator target，只发现 sensitive real-device，未作为本机 emulator 验收替代。
- 继续推进三端真实现场：Android `device doctor --platform android --emulator ...` 显示 adb/emulator 可用，`emulator -list-avds` 有 `Dxyer_API_34` / `Neptune_API_34`；但多次 raw fallback 启动后 Triton/ADB 仍无法发现 emulator。低内存 UI 模式日志记录 `Failed to create DisplaySurfaceGl`，headless swiftshader 能进入 GRPC/display 初始化但随后进程退出。
- Harmony `device doctor --platform harmony --emulator ...` 显示 hdc/DevEco Emulator 可用，`Emulator -list -details` 有 `Codex Test Phone` / `Pura 90 Pro Max`；`Codex Test Phone` 启动日志显示 guest OS boot completed 和 bridge socket `10100`，但 `hdc tconn 127.0.0.1:10100` 返回 `[Fail]Connect failed`，最终 Triton `device list --platform harmony --scope emulator` 仍为空。
- 结论：iOS scene 真实链路已通过；Android/Harmony scene 契约和 Web fallback 已可用，但当前宿主 emulator 启动/连接无法完成真实 hierarchy 验收。下一切片应补 Triton-first `device start --platform android|harmony` 或等价 plan/diagnose 契约，把 raw 启动、PID、ADB/HDC connect、wait-ready 与失败 envelope 纳入机器可读链路。

验证：

- `npm --prefix Web test` 通过 33 tests，新增 bridge 用例覆盖三端 hierarchy 响应与 unsupported platform envelope。
- `swift test --filter TKHierarchySceneModelsTests`
- `swift test --package-path CLI --filter HierarchySceneRuntimeTests`
- `swift test --package-path CLI --filter SchemaFactSourceTests`
- `triton hierarchy --platform ios --target F4E55B8E-0141-4C46-9965-263CCE782B5F --json`
- `triton hierarchy --platform android --json` 返回机器可读 `target_not_found`
- `triton device doctor --platform android --emulator /Users/linhey/Library/Android/sdk/emulator/emulator --json`
- `triton device doctor --platform harmony --emulator /Applications/DevEco-Studio.app/Contents/tools/emulator/Emulator --json`
- `git diff --check`

## Android/Harmony device start 契约与真实 hierarchy 闭环

- Space：`docs-linhay/spaces/20260619-lookin-hierarchy-viewer/`
- 新增 `triton device start --platform android|harmony`，支持 `--plan-only --json` 输出启动 ledger，并在真实执行时 detached 启动 emulator，返回 `started`、`pid`、`sourceCommands`、`executedSourceCommands`、stdout/stderr log path 与 `device wait-ready` nextAction。
- Android start plan 覆盖 AVD、headless、gpu、memory、no-snapshot-load、no-audio、no-boot-anim 和预期 adb serial；Harmony start plan 覆盖 HVD、deployed path、DevEco Emulator、hdc、hdcPort、bootmode 和 `hdc tconn` follow-up。
- `triton schema --command device --json` 与 `triton capabilities --json` 已暴露 `android-device-start` / `harmony-device-start`。
- Android 现场通过：`device-start-android-dxyer-execute.json` 返回 detached pid，`device-list-android-after-start.json` 发现 `emulator-5554` 且 ready，`device-wait-ready-android-emulator-5554-after-start.json` 通过，`hierarchy-android-emulator-5554-after-start.json` 返回 33 个 host-layout scene 节点。
- Harmony 现场通过：`device-start-harmony-codex-execute.json` 返回 detached pid，按 start sourceCommand 执行 `hdc tconn 127.0.0.1:10100` 后返回 Target is connected，`device-list-harmony-after-device-start.json` 发现 emulator 且 ready，`hierarchy-harmony-10100-after-start.json` 返回 93 个 host-layout scene 节点。
- 至此 Lookin-style hierarchy viewer 的真实三端 scene 数据链路已闭环：iOS 来自 embedded runtime，Android 来自 UIAutomator host-layout，Harmony 来自 uitest host-layout。后续 exact node slice 切片进一步接入了 iOS per-node screenshot / surface slice；剩余非本轮范围是完整 Lookin 属性面板 parity 和正式 Web/Wails 产品化。
- 收尾门禁发现 `verify-harmony-host-smoke.sh` 默认复用 `.build/harmony-host-smoke` 时会被 CLI artifact 覆盖保护拒绝；脚本已改为每次先清理自己的输出目录，并用连续两次运行验证幂等。

验证：

- `swift test --package-path CLI --filter DeviceCrossPlatformTests/androidEmulatorStartPlanBuildsDetachedHeadlessEmulatorCommand --filter DeviceCrossPlatformTests/harmonyEmulatorStartPlanBuildsDevEcoStartAndHDCConnectCommands`
- `swift test --package-path CLI --filter DeviceCrossPlatformTests/deviceSchemaExposesCrossPlatformSurface --filter DeviceCrossPlatformTests/deviceProxyCapabilitiesExposeThreePlatformMetadata`
- `triton device start --platform android --avd Dxyer_API_34 --plan-only --json`
- `triton device start --platform android --avd Dxyer_API_34 --json`
- `triton device wait-ready --platform android --device emulator-5554 --json`
- `triton hierarchy --platform android --target emulator-5554 --json`
- `triton device start --platform harmony --hvd 'Codex Test Phone' --path ~/.Huawei/Emulator/deployed --plan-only --json`
- `triton device start --platform harmony --hvd 'Codex Test Phone' --path ~/.Huawei/Emulator/deployed --json`
- `triton device wait-ready --platform harmony --device 127.0.0.1:10100 --json`
- `triton hierarchy --platform harmony --target 127.0.0.1:10100 --json`
- `TRITON_BIN=.build/cli/release/triton docs-linhay/scripts/verify-harmony-host-smoke.sh` 连续两次
- `docs-linhay/scripts/verify.sh --local`

## iOS Lookin 一致性真实采集入口

- Space：`docs-linhay/spaces/20260619-lookin-hierarchy-viewer/`
- 用户要求 iOS 侧 3D 视图必须像 Lookin 一样还原，而不是线稿，并明确扩大到真实采集控制入口。
- Web dev bridge 新增显式采集语义：`POST /web/host-hierarchy?platform=<platform>&target=<target>` 触发 Triton hierarchy 采集，响应包含 `control.action=hierarchy.capture`、`entrypoint=web-dev-bridge`、`method=POST`、`readonly=true`、`mutatesApp=false`。Web 仍不直接调用平台工具，采集事实源保持 Triton CLI 契约。
- iOS fallback 修正：当 `triton hierarchy --platform ios ...` 不支持 `--platform` 或对 simulator UDID 返回 `target_not_found` 时，bridge 回退旧 runtime 命令 `triton hierarchy --target <target> --json`，并把 Lookin-style `displayItems[]` 转为 Web `HierarchyScene`，避免现场 iOS runtime 数据退回 mock 线稿。
- Web `探测` viewer 增加 `重新采集` 按钮和采集状态输出；自动 GET 成功显示 `现场采集`，手动 POST 成功显示 `手动采集`，失败时明确显示 fallback 状态。
- 视觉还原推进：有真实截图时，WebGL 3D viewer 按 node frame 从整屏截图裁剪实际节点切片并贴到 3D plane；结构节点和缺截图场景才降级到样式 texture / 淡描边。该策略比单纯 class/name 线框更接近 Lookin，但仍不是完整 parity。
- 完整 Lookin parity 仍依赖后续真实 per-node exact `slice`、UIKit/CALayer `style`、raw platform payload 和 render hints；本轮没有伪造这些字段，只消费现有 hierarchy + screenshot 事实。
- Shared / CLI 契约推进：`TKHierarchyLayerNode` 新增 `style`、`slice`、`raw`、`renderHints`；`makeHierarchyScene` 会输出标准化 style、slice metadata、raw source 信息和渲染优先级，`triton schema --command hierarchy --json` 新增 `hierarchy.scene` output contract。Web `HierarchyLayerNode` 类型同步 `raw` 字段，后续 exact `slice.dataUrl` 可直接接入现有 viewer。
- 顺手修复 CLI test target 编译 blocker：补回 `xcodeDerivedDataCacheState` 和 `XcodeDerivedDataCacheState`，让 `XcodeDiagnosticsTests/derivedDataCacheStateReportsWarmAndMissingPaths` 可编译通过。
- 浏览器现场验证受限：重启 Vite dev server 在 sandbox 内被 `listen EPERM` 拦截，经授权外部启动后已恢复监听；Browser 插件随后拒绝重新访问本地 URL（Browser Use URL policy）。未绕过策略，采用自动化 DOM/bridge 测试和 build 作为可重复验收。

验证：

- `npm --prefix Web test`：38/38 通过。
- `npm --prefix Web run build`：通过，保留既有 Three.js chunk > 500KB 警告。
- `swift test --package-path CLI --filter HierarchySceneRuntimeTests`：通过。
- `swift test --package-path CLI --filter SchemaFactSourceSurfaceContractTests`：通过。

## Xcode workflow 真实设备 `--device` 契约修复

- Overloaded 真实 iPhone 联调时发现 `triton schema --command xcode --json` 已承诺 `xcode settings/build/test/run --device`，但实际 ArgumentParser 未暴露该参数，导致 `triton xcode settings --device ...` 直接报 `Unknown option '--device'`。
- Xcode workflow 四个子命令补齐 `--device`，`ResolvedXcodeInvocation` 与 `TKXcodeActionSummary` 保留 device 字段；`--device` 默认解析为 `sdk=iphoneos` 与 `destination=generic/platform=iOS`，避免把 `ios-real:*` Triton selector 误拼成 xcodebuild 原生 `id=`。
- `triton xcode run --device` 现在走真实设备分支：Xcode 负责 build/解析 `.app` 与 bundle id，install/launch 复用 Host App 的 devicectl plan，最终仍提醒用 runtime status/wait/assert/screenshot/evidence 验证业务就绪。
- `triton build ios --device` 改为复用同一套 Xcode invocation 解析规则，保持和 `triton xcode build --device` 一致。

验证：

- `swift test --package-path CLI --filter XcodeCommandTests`：3/3 通过。
- `swift test --package-path CLI --filter 'XcodeDiagnosticsTests|SchemaFactSourceTests/build schemas|BuildRuntimeTests'`：11/11 通过。
- `swift run --package-path CLI triton xcode settings --help | rg -- '--device|USAGE|settings'`：确认 settings 暴露 `--device`。
- `swift run --package-path CLI triton xcode build --help | rg -- '--device|--allow-provisioning-updates|USAGE'`：确认 build 暴露 `--device` 与签名 flag。
- `swift run --package-path CLI triton xcode run --help | rg -- '--device|USAGE|real device'`：确认 run 暴露真实设备入口。
- `swift test --package-path CLI --filter SchemaFactSourceTests`：因既有 `hierarchy:hierarchy.scene:hierarchy-scene` taxonomy 失败未全绿；同轮中 `build schemas expose real-device build contracts` 通过。
- `git diff --check -- CLI/Tests/TritonKitCLITests/XcodeCommandTests.swift Sources/TritonKitCLI/CLIXcodeCommands.swift Sources/TritonKitCLI/CLIXcodeRuntime.swift Sources/TritonKitCLI/CLIXcodeModels.swift Sources/TritonKitCLI/CLIBuildCommands.swift Sources/TritonKitShared/TKXcodeWorkflowModels.swift`：通过。

## Web 真机实时画面恢复

- 用户反馈 Web 拿不到实时画面。Triton-first 检查发现 19421 control server 未运行时，Web iOS 真机 target 一直显示“正在获取实时画面”，错误只在画布外层展示，不够可操作。
- 启动 19421 后进一步发现第二个根因：当 server 同时存在真机 runtime 与 iOS Simulator runtime 时，Web `/web/host-screenshot` 请求 App runtime screenshot 未指定 target，触发 `ambiguous_target`。
- Web UI 修复：真机 runtime mirror 失败时，设备画面占位区直接显示 `App runtime 未连接`、runtime 依赖说明和 `triton serve --host 127.0.0.1 --port 19421`，不再无限转圈。
- Web bridge 修复：iOS real-device runtime mirror 会先读取 `/targets`，选择 connected 的非 simulator runtime target（例如 `triton:connection:2`），再带 target 请求 screenshot，避免 simulator + 真机双 runtime 时 ambiguous。
- 当前本机已用 detached `screen` 常驻：
  - `triton-serve-19421`：`triton serve --host 0.0.0.0 --port 19421`
  - `triton-web-34127`：`TRITONKIT_TRITON_BIN=.../CLI/.build/debug/triton npm --prefix Web run dev -- --host 127.0.0.1 --port 34127`

验证：

- `triton status --json`：`ok=true`、`connected=true`、`runtime=embedded`。
- `curl http://127.0.0.1:34127/web/host-screenshot?...source=runtime`：`ok=true`、`runtimeScope=app-runtime`、`pixelWidth=402`、`pixelHeight=874`、`dataUrlLength=93410`。
- Playwright 浏览器验证：页面 `.real-screenshot` 已加载，natural size `1206 x 2622`，live badge 为 `实时 1 fps`，未出现 `App runtime 未连接`。
- 截图证据：`docs-linhay/spaces/20260619-lookin-hierarchy-viewer/screenshots/20260619/20260619-web-real-device-live-screen-after-v01.png`。
- `swift test --package-path CLI --filter SingleDeviceWebPageTests`：17/17 通过。
- `npm --prefix Web test -- appFallbackDom.test.mjs`：54/54 通过。
- `npm --prefix Web run build`：通过，保留既有 Three.js chunk > 500KB 警告。
- `git diff --check -- Web/src/App.tsx Web/src/styles.css Web/dev/appFallbackDom.test.mjs CLI/Tests/TritonKitCLITests/SingleDeviceWebPageTests.swift Sources/TritonKitCLI/CLIWebDeviceRuntime.swift Sources/TritonKitCLI/CLIWebRuntime.swift`：通过。

## Runtime layer metadata 与节点 Inspector

- embedded iOS runtime 的 `TKHierarchyBuilder` 已开始采集 CALayer metadata：bounds、position、anchorPoint、zPosition、transform、sublayerTransform、masksToBounds、cornerRadius、opacity、hidden、contentsScale、contentsGravity、contentsRect、borderWidth、shadowOpacity、shadowRadius、shadowOffset。
- 这些字段经 `TKDisplayItem` -> `TKHierarchyLayerNode.layer` 进入 shared / CLI / Web contract；旧截图来源统一标记为 `visualSources[{kind:"subtreeSnapshot"}]`，仍然只作为 evidence，不能作为默认 node material。
- Web hierarchy viewer 新增选中节点 Inspector，展示 Node / Layer / Visual Sources / Default Material，直接解释当前节点来源和 Lookin parity 限制。
- 本轮验证通过：`npm --prefix Web test` 45/45、`npm --prefix Web run build`、`swift test --package-path CLI --scratch-path /private/tmp/triton-cli-test-layer-metadata --filter HierarchySceneRuntimeTests`、`swift test --package-path CLI --scratch-path /private/tmp/triton-cli-test-layer-metadata --filter SchemaFactSourceSurfaceContractTests`、`git diff --check`。
- `swift test --package-path CLI --filter XcodeDiagnosticsTests/derivedDataCacheStateReportsWarmAndMissingPaths`：通过。
- `swift build --package-path CLI --product triton`：通过。

## Lookin-style 三端 3D hierarchy viewer P0

- 用户要求设备画布具备 Lookin 对应的视图层级分层三维可旋转形态，并要求开始执行、落实到三端可用。
- 新建 space：`docs-linhay/spaces/20260619-lookin-hierarchy-viewer/README.md`，P0 明确为 Web mock 只读 3D hierarchy viewer，不完整复刻 Lookin，不在 Web 里新增真实采集或业务控制入口。
- Web 新增 `HierarchyScene` / `HierarchyLayerNode` DTO，三端 mock target 均具备 viewport、node frame、depth、visible、interactive 和 color。左侧 `视图树` 改为从同一 DTO 派生，避免树和 3D 画布使用两套数据。
- `探测` 模式现在把设备屏幕切换为 Lookin-style 3D hierarchy viewer：Three.js 渲染分层平面，DOM fallback 暴露 layer 列表和 rotation 状态；拖动 viewer 会更新 rotationX / rotationY，并停止事件传播，避免误发设备 tap/swipe。
- 用户指出三维层级模式不需要设备外框；已将 `探测` 模式拆出 `.device-frame` / `.device-screen`，改由独立 `.hierarchy-stage` 承载 3D viewer，普通 `点选` 镜像模式继续保留设备壳。浏览器 DOM 验收确认 `frameCount=0`、`screenCount=0`、`hierarchyStageCount=1`、`viewerCount=1`。
- 用户继续反馈三维模式仍像渲染了一个框，且切片不应双轴旋转；已移除 `.hierarchy-stage` / `.hierarchy-scene-viewer` 的圆角背景、阴影和容器背景，并把交互收敛为固定 `-18deg` 俯仰 + 水平 yaw 单轴旋转，纵向拖动不再改变旋转状态。
- 用户给出 LookInside 参考后，探测态视觉收敛为左侧视图树、中央真实截图主切片、背后低透明描边层级、右侧属性检查器的方向；WebGL 成功时隐藏 DOM fallback，避免双重渲染造成发糊和拥挤，点击 `探测` 会自动把左侧切到 `视图树`。
- 用户指出 LookInside 会在独立节点切片上还原视图样式；WebGL hierarchy viewer 已为每个可见节点生成 canvas texture，并按 type/name/frame 近似绘制导航栏、按钮、文本、输入框、列表、卡片、tabbar 等样式，再贴到对应 3D 切片。当前仍非完整 parity，后续需要 runtime / host DTO 提供真实 backgroundColor、cornerRadius、font、alpha、shadow、border、CALayer / Compose / ArkUI 样式字段。
- 用户追问真实还原缺少哪些数据；新增 `docs-linhay/spaces/20260619-lookin-hierarchy-viewer/plans/20260619-node-slice-style-contract.md`，定义 `style`、`slice`、`raw`、`renderHints` 四类补齐字段和 iOS / Android / Harmony 采集路径。过渡实现已移除探测态主截图平面，结构节点仅淡描边，右下缩放控件在探测态贴底。
- 用户指出需要旋转到约 168 度才看到子节点切片在前；定位为 WebGL z 轴方向与透明绘制顺序反了。已改为 depth 越大 z 越大、更靠近相机，且 renderOrder 随 depth 递增，子节点默认在父节点前方。
- 三端验证：QA fallback 下点击 `探测` 可显示 iOS `UIStackView/questionList`，切换 Android target 后显示 `AndroidComposeView/settingsList`，切换 Harmony target 后显示 `ArkUIRoot/settingsContent`；DOM 测试覆盖拖动旋转状态变化。
- 新增 Web 依赖：`three` 与 `@types/three`。`npm run build` 通过，但 Vite 提示独立 `three.module` chunk 超过 500KB；当前是 mock P0 可接受，后续正式化时应继续做按需加载或 chunk 配置。

验证：

- `npm --prefix Web test`
- `npm --prefix Web run build`

## Web 设备画布 longPress / pinch 手势

- 用户在 `127.0.0.1:34127` 设备画布标注“没支持长按和双指捏合等手势吗”。确认当前 Web 只识别 tap / swipe，Shared input model 和 CLI schema 也没有 `longPress` / `pinch`，packaged bridge 会拒绝这类 payload。
- 修正：Shared `TKInputType` / `TKInputRequest` 新增 `longPress` 与 `pinch`，CLI input schema 同步暴露两个 action。capability gate 归类为 `longPress -> inputTap`、`pinch -> inputSwipe`。
- Web 画布新增长按阈值识别和双 pointer 捏合识别，分别发送 `longPress` 和 `pinch` DTO；视觉反馈新增长按环与捏合缩放环。
- 平台边界：Web 只采集/转发 DTO；Android host `longPress` 通过 `adb input swipe x y x y duration` 支持，iOS Simulator host、Harmony host、embedded runtime 的 `longPress` / `pinch` 当前返回明确 unsupported，后续底层多点注入需独立补 adapter。
- 浏览器烟测：Playwright 打开 `http://127.0.0.1:34127/`，合成长按与双指捏合，拦截到 `longPress` 和 `pinch` payload；console error 为 0。截图归档：`docs-linhay/spaces/20260611-web-mock-ui/screenshots/20260619/20260619-web-device-canvas-gestures-after-v01.png`。

验证：

- `npm --prefix Web test -- --test-name-pattern 'long press|pinch'`
- `npm --prefix Web test`
- `npm --prefix Web run build`
- `swift test --filter TKInputModelsTests/longPressRequestWireShape --filter TKInputModelsTests/pinchRequestWireShape`
- `swift test --filter TKPlatformFallbackTests/capabilityGateMapsRequestMessages`
- `swift test --package-path CLI --filter SingleDeviceWebPageTests`

## Web mock 窄屏 toolbar 设备切换

- 用户在窄屏标注 toolbar 标题 `iPhone 17 iOS 26.5`，要求点击此处弹出菜单切换设备；此前窄屏隐藏左侧设备列表后没有 target 切换入口。
- Web `DeviceHubToolbar` 标题改为可访问按钮，打开只读 `切换设备` listbox；菜单项来自 `pageTargets`，不受侧栏搜索过滤影响。选择 target 只更新本地 `selectedId` 并关闭菜单，不引入 Web 业务控制闭环。
- 新增 DOM 回归测试 `switches devices from the narrow toolbar title target menu`：QA fallback 下从 `DXY iPhone 15` 切到 `Pixel API 35`，验证 toolbar 标题、应用卡片和菜单关闭状态同步。

验证：

- `npm --prefix Web test -- --test-name-pattern "switches devices from the narrow toolbar title target menu"`：新增用例通过；同一测试进程中的既有 `proxies iOS Simulator Web input through triton serve host target route` 在沙箱内因 `listen EPERM 127.0.0.1` 失败。
- `npm --prefix Web test`：先在沙箱内复现同一端口监听失败，再经授权非沙箱重跑通过 35/35。
- `npm --prefix Web run build`：通过，仍有既有 Three.js chunk > 500KB 警告。
- 浏览器验收：打开 `127.0.0.1:34127`，点击 `切换设备` 标题菜单后选择 Android target，标题切到 `Pixel API 35`、应用卡片切到 `Overloaded`、菜单关闭。

## iOS Lookin exact node slice 闭环

- Space：`docs-linhay/spaces/20260619-lookin-hierarchy-viewer/`
- 用户继续要求“还原成和 Lookin 一致”，并授权修改项目任意代码直到达成需求。
- iOS `triton hierarchy --platform ios --json` scene 事实源已从 AX-only `runtimeSnapshot` 调整为优先使用 embedded runtime legacy `displayItems[]`，保留 Lookin-style UIView/CALayer class chain、frame/depth、hidden/alpha、backgroundColor、customDisplayTitle 和 screenshotRef；Android/Harmony 仍保持 host layout scene。
- embedded runtime `TKHierarchyBuilder` 在有 `/data` uploader 时采集受预算限制的可见节点 PNG，并上传为 `TKDisplayItem.screenshotRef`；无 uploader 时不把图片 Data 内联进 JSON。
- Shared `TKHierarchyNodeSlice` 新增 `dataRef`，CLI scene 输出稳定 runtime data 引用，Web dev bridge 负责把 `dataRef` 或 legacy `screenshotRef` 水合成 `slice.dataUrl`。
- WebGL hierarchy viewer 新增独立 `slice.dataUrl` 图片加载；即使没有整屏 screenshot，也能把真实节点截图贴到 3D plane。整屏 screenshot frame crop 变为次级 fallback，最后才使用样式化 texture/淡描边。
- 当前已解决“iOS 侧不是线稿图、要真实节点切片还原”的主诉；未覆盖完整 Lookin 客户端的属性编辑、transform/mask/shadow/font/border 等高级属性面板能力。

验证：

- `npm --prefix Web test`：40/40 通过。
- `npm --prefix Web run build`：通过，保留既有 Three.js chunk > 500KB 警告。
- `swift test --package-path CLI --filter HierarchySceneRuntimeTests`：通过。
- `swift test --package-path CLI --filter SchemaFactSourceSurfaceContractTests`：通过。
- `swift build --package-path CLI --product triton`：通过。
## iOS Lookin Demo + Simulator 验证与防闪动修复

- 用户要求用 Demo + 模拟器验证 iOS Lookin-style 3D hierarchy，并追问 3D 视图一直闪动的原因。
- Triton-first 实测链路：`triton sim list --json` 发现 booted `Overloaded-v2 Dedicated iPhone 16 Pro`（UDID `1B360513-22E7-46DB-A942-198EE522C6DC`）；`triton xcode discover` / `triton xcode schemes` 找到 `Examples/TritonKitDemo` 与 `TritonKitDemo` scheme；`triton xcode run ... --jsonl` build/install/launch Demo 成功。
- 现场发现同一模拟器同时挂着 `丁香园` 与 `TritonKitDemo` 两个 embedded runtime 连接且 target id 相同，导致 `triton:local` ambiguous、指定 simulator id 也可能采错 App。通过 `triton app terminate --bundle-id cn.dxy.iDxyer` 隔离旧 App 后，`triton list --json` 只剩 Demo target。
- Demo hierarchy 真实采集通过：`triton hierarchy --platform ios --target triton:ios-simulator:1B360513-22E7-46DB-A942-198EE522C6DC --max-nodes 120 --json` 返回 `ok=true`、`nodeCount=120`、`sliceCount=32`；Web dev bridge 可把 `slice.dataRef` 水合为 `slice.dataUrl=data:image/png;base64,...`。
- 修复现场暴露的问题：CLI legacy iOS scene node id 从字面量 `ios:runtime:(oid)` 修正为真实 `ios:runtime:<oid>`；runtime 节点截图从可能阻塞的 `drawHierarchy` 调整为受预算保护的 `CALayer.render(in:)`；CLI decode 兼容旧 appInfo 缺 `platform` 字段。
- 闪动根因：Web 端 `captureHierarchy` 依赖整个 target，live preview 1fps screenshot 刷新会重新生成 target 对象并反复触发 hierarchy capture；viewer 在已有 node slice 时仍订阅 live screenshot fallback，且 slice 逐张加载/透明深度测试会造成 WebGL 重建和 z-fighting。
- 防闪动修复：层级采集只依赖稳定 target selector；有 node `slice.dataUrl` 时不再订阅 1fps 全屏 screenshot；slice 图片批量加载后一次性替换；透明 Mesh/Line 关闭 `depthTest/depthWrite` 并依赖 `renderOrder` 稳定绘制。

验证：

- `npm --prefix Web test`：40/40 通过，新增覆盖 live screenshot refresh 不会重复触发 hierarchy capture。
- `npm --prefix Web run build`：通过，保留既有 Three.js chunk > 500KB 警告。
- `swift test --package-path CLI --filter HierarchySceneRuntimeTests`：通过。
- `swift test --package-path CLI --filter SchemaFactSourceSurfaceContractTests`：通过。
- `git diff --check`：通过。
- `docs-linhay/scripts/check-docs.sh`：失败于既有空 space `docs-linhay/spaces/20260619-issue-68-harmony-app-target-failure` 缺 README，非本轮新增。

## Hierarchy viewer 捏合缩放

- 用户在 iOS 3D hierarchy viewer 标注需要支持捏合手势缩放。
- Web viewer 新增独立 `zoom` 状态：单指横向拖动继续控制 yaw，双指 pointer 距离变化控制缩放，范围 45% - 240%；`ctrl/meta + wheel` 也映射到同一缩放状态，便于触控板/桌面浏览器验证。
- WebGL 用 `three.group.scale.setScalar(zoom)` 缩放整体层级 group，DOM fallback 通过 `--hierarchy-zoom` 同步缩放；状态输出显示 `水平旋转 xx° · 缩放 yy%`。

验证：

- `npm --prefix Web test`：40/40 通过。
- `npm --prefix Web run build`：通过，保留既有 Three.js chunk > 500KB 警告。
- `git diff --check`：通过。

## Hierarchy viewer 快照模式停止定时刷新

- 用户指出三维视图模式是快照，不用定时刷新。
- Web 父组件新增 hierarchy snapshot mode：进入 `探测` 时停止 live screenshot 轮询，退出到 `点选` 或切换 target 后恢复设备镜像刷新。探测态不再显示 `实时 1fps` 控件。
- hierarchy viewer 只在进入探测时 GET 采集一次，或用户点击 `重新采集` 时 POST 手动刷新；不会再由 1fps screenshot timer 驱动刷新。
- DOM 回归测试断言进入 probe 后等待超过 1 秒，`/web/host-screenshot` 请求数不增加，`/web/host-hierarchy` 仍只触发一次，且 live preview badge 不存在。

验证：

- `npm --prefix Web test`：40/40 通过。
- `npm --prefix Web run build`：通过，保留既有 Three.js chunk > 500KB 警告。
- `git diff --check`：通过。

## Hierarchy capture evidence 与状态文案

- 用户把裁决整理为工程边界/验收清单，明确 Web 不能只看 scene 是否存在，而要用机器可读 evidence 判断是否是真实节点切片。
- Web `HostHierarchyResponse` 新增 `captureEvidence`，包含 `captureId`、`capturedAt`、target、source kind、`nodeSlice=real|styled|none`、`screenshotSlice=real|none`、hydration `dataUrlCount/nodeCount/failedNodeCount`。
- Web dev bridge 在 `/web/host-hierarchy` 响应中生成 evidence；`dataRef -> dataUrl` 水合成功时标记 `nodeSlice=real`，静态/样式化 scene 标记 `nodeSlice=styled`。
- UI 文案改为 evidence-first：真实节点切片才显示 `现场采集 · 真实截图切片`；样式化 scene 显示 `样式化快照 · 非真实节点切片`；失败态显示 `采集失败 · 已显示 fallback scene`。

验证：

- `npm --prefix Web test`：40/40 通过。
- `npm --prefix Web run build`：通过，保留既有 Three.js chunk > 500KB 警告。
- `git diff --check`：通过。

## Hierarchy 叶子节点贴图，容器只保留结构

- 用户指出 3D hierarchy viewer 每层都在重复渲染，和 Lookin 心智不一致。
- 根因是 iOS runtime 的 `CALayer.render(in:)` 容器截图会包含子 layer；如果父容器、子容器、叶子节点都渲染真实纹理，同一 UI 会在多个深度层重复出现。
- WebGL viewer 改为只对没有可见子节点的叶子节点使用真实 screenshot slice；有可见子节点的节点即使带 `slice.dataUrl`，也只渲染为结构/描边层。DOM fallback 同步标记 `data-render-mode=slice|structure`。
- 后续裁决已废弃 leaf-only 默认贴图模型：默认 scene 不再渲染任何普通 `node.slice.dataUrl` texture plane。

验证：

- `npm --prefix Web test`：41/41 通过。
- `npm --prefix Web run build`：通过，保留既有 Three.js chunk > 500KB 警告。
- `git diff --check`：通过。

## Hierarchy 默认模型改为 main snapshot + structure

- 用户明确裁决：旧 bug 不是“贴图太多”，而是 subtree snapshot 被误用成 layer-own texture；默认模型必须改为 `main-snapshot-with-structure`。
- Web viewer 现在默认只使用一张完整 App screenshot 作为主 surface；所有节点默认为结构 plane / outline。真实 `node.slice.dataUrl` 保留为 evidence，但只渲染一个选中/聚焦节点的 `selected-slice`，不再按节点批量创建 texture plane。
- DOM/test hooks 已落地：主 surface 标记 `data-render-role="main-snapshot-surface" data-render-mode="main-snapshot"`，默认节点标记 `data-render-mode="structure"`，选中证据标记 `data-render-mode="selected-slice"`；后续 `data-texture-source` 改为输出 `VisualSource.kind`。
- 状态文案区分主 screenshot 与 node slice evidence：真实切片文案只表示可用于选中节点证据；没有真实 node slice 时不再暗示 Lookin parity。
- 新增测试 `renders one main snapshot surface and keeps node slices selected-only evidence`，断言默认 scene 中没有 `node-slice` / `slice` 批量贴图，structure 节点存在，selected-slice 只有一个。

验证：

- `npm --prefix Web test`：41/41 通过。
- `npm --prefix Web run build`：通过，保留既有 Three.js chunk > 500KB 警告。
- `git diff --check`：通过。

## Layer Metadata Contract + Material Source Guard

- 用户要求把“subtreeSnapshot is not layer contents. It must never be used as default node material.”固化成工程边界，避免 Web 继续被“看起来不像 Lookin”拖着调参。
- Web / Swift shared hierarchy node contract 新增 `view/layer/visualSources`。当前 slice 仍保留兼容，但正式来源为 `visualSources`：
  - `layerOwnContents`：默认 3D material eligible。
  - `subtreeSnapshot`：evidence only。
  - `mainScreenshotCrop`：fallback evidence。
  - `styledFallback`：结构降级并带 reason。
- Web 新增 `hierarchyMaterialPolicy` 纯函数：`resolveDefaultMaterialSource` 只允许 `layerOwnContents`，`resolveEvidenceSources` 返回全部 evidence，`getMaterialExplanation` 输出节点来源解释，`computeParityClaim` 禁止在 subtree snapshot / 缺 layer-own contents 时宣称 Lookin parity。
- Web viewer 改为通过 policy 选 evidence source，`data-texture-source` 输出 contract kind；新增 `Snapshot Evidence Mode · Lookin parity unavailable` 和当前节点视觉来源文案。
- Web dev bridge / CLI legacy iOS scene 将旧 `slice` 映射为 `visualSources[{kind:"subtreeSnapshot"}]`；host observe scene 输出 `styledFallback`，schema contract 同步暴露 `view/layer/visualSources`。

验证：

- `npm --prefix Web test`：45/45 通过。
- `npm --prefix Web run build`：通过，保留既有 Three.js chunk > 500KB 警告。
- `swift test --package-path CLI --filter HierarchySceneRuntimeTests`：通过。
- `swift test --package-path CLI --filter SchemaFactSourceSurfaceContractTests`：通过。

## Device Hub URL Route Sync

- 用户要求“切换设备，视图选中节点，要改变 url 路由”。
- Web Device Hub 新增 query route 同步：`target` 跟随当前设备，`panel=view-tree` 跟随视图树面板，`node` 跟随选中的 hierarchy node。
- 初始 URL 可恢复到指定 target / panel / node；切换到不包含旧 node 的设备时清空 `node`，避免跨平台误选。写回 URL 时保留 `__tritonkit_mock_host_targets` 等调试参数。
- `ViewTreeRow` 增加 `data-node-id` 作为测试和机器可读定位 hook；新增 DOM 回归测试覆盖 URL 恢复、设备切换后 route 更新、节点选择后 route 更新。

验证：

- `npm --prefix Web test`：55/55 通过。
- `npm --prefix Web run build`：通过，保留既有 Three.js chunk > 500KB 警告。

## DevTools Top Evidence Tabs

- 用户要求“底部面板都放右侧，参考 chrome devtools 布局”。
- 用户继续指出“上面不是有 tab 列表吗”，明确应复用右侧顶部既有 tab 列表，而不是在过滤栏下方新增证据 tabs。
- Web Device Hub 移除底部 `hub-bottom` evidence 区，并把 `网络` / `日志` 合并到右侧顶部 `信息` / `应用` / `配置` 同一组 tab 中；点击 `网络` / `日志` 时在同一右侧内容区切换 pane。
- 隐藏/恢复语义保持不变：网络和日志仍是独立 Web UI pane，不停止采集，不修改 CLI / HTTP evidence contract；恢复按钮继续在设备控制胶囊中。
- 修正网络 tab 下单条数据居中：原因是 `.network-rows` 作为 CSS grid 缺少 `align-content: start`，单条 `.network-row` 会被拉伸到剩余高度；现在网络与日志列表共享顶部对齐规则。
- DOM 测试补充：顶部 tab labels 必须为 `信息 / 应用 / 配置 / 网络 / 日志`，不应存在额外 `.evidence-tabs` 或 `.hub-bottom`。浏览器验证截图已保存到 `docs-linhay/spaces/20260619-lookin-hierarchy-viewer/screenshots/20260619/20260619-web-devtools-tabs-after-v02.png`。

验证：

- `npm --prefix Web test`：55/55 通过。
- `npm --prefix Web run build`：通过，保留既有 Three.js chunk > 500KB 警告。
- in-app browser 验证：顶部 tab 为 `信息/应用/配置/网络/日志`，无 `.evidence-tabs`、无 `.hub-bottom`，点击 `网络` / `日志` 后分别只显示对应 pane。
- in-app browser 复验网络单行位置：`.network-rows` computed `align-content=start`，首条 `.network-row` 相对容器偏移为 `0`，横向溢出为 `0`。

## Remove Web 3D Probe Mode

- 用户明确要求“移除 3d 模式，我们做不好不如没有”。
- Web Device Hub 已删除中央画布的 `探测` 工具入口、`HierarchySceneViewer`、Three.js 动态渲染路径和 `.hierarchy-*` 样式；`Web/package.json` / lockfile 同步移除 `three` 与 `@types/three`。
- 设备控制胶囊只保留稳定的 `点选`、网络/日志显隐与缩放能力；普通设备镜像、输入 relay、实时预览和右侧 DevTools tabs 继续可用。
- 左侧 `视图树`、设备/节点 URL 路由以及 CLI / HTTP `hierarchy` 机器可读契约保留；后续层级能力以树形事实和接口契约推进，不再用 Web 3D mock 暗示 Lookin parity。
- DOM 测试改为负向断言：`探测` 按钮、`.hierarchy-stage`、`.hierarchy-scene-viewer`、`.hierarchy-three-canvas` 均不应存在；视图树仍能跨 iOS / Android / Harmony target 展示节点。

验证：

- `npm --prefix Web test`：54/54 通过。
- `npm --prefix Web run build`：通过，产物不再包含 Three chunk。
- `git diff --check`：通过。
- in-app browser 复验：`探测` 按钮数量为 `0`，3D stage/viewer/canvas 均为 `0`，普通 device frame/screen 均为 `1`，横向溢出为 `0`。

## Remove Simulator Canvas Zoom Controls

- 用户追问模拟器区域右下角按钮组是否已经无用；确认该组只控制普通设备镜像的 CSS scale，在 3D 探测模式移除后已经不是核心工作流。
- Web Device Hub 已删除 `CanvasZoomControls`、`canvasZoom` 状态、`.canvas-zoom-controls` 样式和 `--canvas-zoom` transform。设备镜像继续由 `.device-frame` / `.device-screen` 的自适应尺寸承载。
- 网络/日志入口由右侧顶部 DevTools tab 承接，不再依赖画布角落的浮动按钮。
- DOM 回归测试新增负向断言：`[aria-label="画布缩放控制"]` 与 `.canvas-zoom-controls` 均不应存在。

验证：

- `npm --prefix Web test`：54/54 通过。
- `npm --prefix Web run build`：通过。
- `git diff --check`：通过。
- in-app browser 复验：`zoomControls=0`，普通 `.device-frame`、`.device-screen` 均为 `1`，横向溢出为 `0`。

## Remove Simulator Device Controls

- 用户继续追问左下角按钮组；确认该组也已经失去主要职责：`点选` 是唯一模式，网络/日志已经并入右侧顶部 DevTools tab。
- Web Device Hub 已删除 `DeviceControls` 组件、`.device-controls` / `.control-pill` / `.strip-action` 相关样式，以及网络/日志隐藏/恢复状态。画布不再显示左下角浮动工具胶囊。
- `网络` / `日志` tab 改为常驻可选，不再禁用，也不通过画布按钮隐藏或恢复；pane 内也不再显示隐藏按钮，避免 Chrome DevTools 风格 tab 与浮动控制重复。
- 设备画面仍保持 `tool-point` 与 `aria-label="设备画面，当前工具 点选"`，输入 relay / tap / long press / pinch 语义不变。

验证：

- `npm --prefix Web test`：55/55 通过。
- `npm --prefix Web run build`：通过。
- `git diff --check`：通过。
- in-app browser 复验：`.device-controls=0`、右下缩放控件 `0`、`点选/探测` 按钮均为 `0`，右侧 `信息/应用/配置/网络/日志` tab 全部可用，横向溢出为 `0`。

## Remove Right Toolbar Placeholder Buttons

- 用户继续指出导航区右侧两组按钮缺少明确用途；确认 `键盘`、`屏幕布局`、`展开`、`更多`、`调整`、`文档`、`信息` 都没有绑定动作，只是历史占位。
- Web Device Hub 已删除右侧 `.toolbar-center` 两组占位按钮，以及检查器工具组里的 `调整` / `文档` / `信息`。右侧工具区只保留有实际行为的 `刷新全局数据`。
- 顶部导航仍保留当前有效入口：左侧 target 辅助按钮、侧栏收起/展开、标题设备切换和右侧刷新；不新增 Web 侧业务控制闭环。

验证：

- `npm --prefix Web test`：56/56 通过。
- `npm --prefix Web run build`：通过。
- `git diff --check`：通过。
- in-app browser 复验：右侧检查器工具仅有 `刷新全局数据`，`键盘/屏幕布局/展开/更多/调整/文档/信息` 均不存在，`.toolbar-center=0`，横向溢出为 `0`。

## Keep Only Sidebar Toggle On Left Toolbar

- 用户要求“左侧只留一个展开收起侧边栏的按钮”。
- Web Device Hub 已删除顶部左侧红黄绿窗口装饰灯，以及 `添加目标` / `筛选与排序` 这组无绑定占位按钮；左侧只保留 `收起侧边栏` / `展开侧边栏`。
- 顶部布局从五列收敛为三列：侧边栏按钮、设备标题切换、右侧刷新，不改变设备标题切换或右侧刷新行为。

验证：

- `npm --prefix Web run build`：通过。
- in-app browser 复验：`trafficLights=0`、`添加目标=0`、`筛选与排序=0`、侧边栏按钮 `1`，toolbar buttons 为 `收起侧边栏 / 切换设备 / 刷新全局数据`，横向溢出为 `0`。
- `npm --prefix Web test`：仍失败于既有旧用例期望 `QA mock fallback` / 静态 mock targets；当前产品代码与新用例已改为不挂静态 mock targets，非本轮导航清理引入。

## Remove Info And App DevTools Tabs

- 用户要求移除右侧 DevTools 顶部 `信息` / `应用` 两个 tab。
- Web Device Hub 已将 `DevtoolsPanel` 收敛为 `config | network | logs`，默认选中 `配置`；右侧 tab 列表只渲染 `配置` / `网络` / `日志`。
- 原 `信息` / `应用` 不再作为 tab 出现；当前检查器内容继续由 `配置` pane 承载，网络与日志切换语义不变。

验证：

- `npm --prefix Web run build`：通过。
- `git diff --check`：通过。
- in-app browser 复验：tabs 为 `配置 / 网络 / 日志`，`信息=false`、`应用=false`，默认选中 `配置`，横向溢出为 `0`。
- `npm --prefix Web test`：仍失败于既有旧用例期望 `QA mock fallback` / 静态 mock targets；当前产品代码与新用例已改为不挂静态 mock targets，非本轮 tab 清理引入。

## Localized Human-Readable Logs

- 用户要求 `日志` tab 的显示需要转换为人类可读格式并本地化。
- Web Device Hub 新增日志展示层 formatter：把 `LogEntry.message` 转成中文可读消息，并补齐 `时间 / 级别 / 来源 / 消息` 四列展示。常见来源包括 `iOS`、`Android`、`Harmony`、`应用`、`网络`、`CLI`。
- 已覆盖常见机器短语：host target selection、CLI DTO evidence missing、`triton ... exit=...`、`simctl framebuffer`、`ADB target ready`、`HDC target discovered`、`App launched`、`Network timeout` 等。
- 原始英文 message 不直接作为正文展示，但保留在 `.log-row[title]` 中，方便排查时对照原始机器证据。

验证：

- `node --test --test-name-pattern='renders bounded iOS host logs|keeps network and logs' dev/appFallbackDom.test.mjs`：日志本地化核心用例通过；另一个既有右侧 tab 用例仍失败于旧 `QA mock fallback` 等待语义。
- `npm --prefix Web run build`：通过。
- `git diff --check`：通过。
- in-app browser 复验：`日志` tab 行内容为中文本地化消息，`Selected host` / `ADB target ready` / `HDC target discovered` / `App launched` / `Network timeout` 等英文已不作为可见正文出现；原文保留在 `title`，横向溢出为 `0`。

## Web Language Preference Settings

- 用户要求新增设置页面来配置语言偏好。
- Web Device Hub 右侧 DevTools tab 新增 `设置`，作为本机展示偏好页，不新增 CLI / HTTP 控制契约。
- 语言偏好支持 `简体中文` 与 `English`，使用 `localStorage` 持久化；切换后右侧 tab、设置页、网络模式标签与日志的级别 / 来源 / 正文立即更新。
- 默认语言保持 `简体中文`；刷新页面后沿用上次选择。
- 验证：`npm --prefix Web run build` 通过；目标 DOM 测试 `renders bounded iOS host logs|keeps network and logs` 2/2 通过；Playwright 隔离浏览器验证默认中文、切 English、刷新持久化和横向溢出 `0` 均通过。完整 `npm --prefix Web test` 为 48/57 通过，剩余失败仍是旧 request-failed fallback / 静态 mock target 断言；`check-docs.sh` 失败于既有缺 README space。

## Config Tab Selected Node Hot Edit Preview

- 用户指出配置 tab 没有显示当前 view-tree 选中节点信息，并要求支持类似 Lookin 的热修改。
- 根因：`Inspector` 只接收 target / network / bridge，没有接收 `selectedHierarchyNode` 或 `HierarchyScene`，所以配置 tab 无法跟随视图树选中状态。
- Web Device Hub 已在配置 tab 增加 `选中视图节点` 面板，显示节点 type/name/id/frame/depth/state，并展示本机热修改预览控件。
- 热修改当前为 Web 本地 draft，不写回 App runtime：支持修改 `x/y/width/height/opacity/cornerRadius/backgroundColor/hidden`，即时影响画布 `.view-node-highlight` 位置与样式，并支持重置到 runtime DTO。
- 验证：`npm --prefix Web run build` 通过；目标 DOM 测试 `shows selected view-tree node details` 通过；Playwright 隔离浏览器在当前 `panel=view-tree&node=ios:runtime:6` URL 验证配置 tab 显示节点信息，修改 X 后高亮 left 从 `0%` 变为 `4.97512%`，横向溢出 `0`。

## Device Mirror Snapshot Inspect Mode

- 用户要求模拟器区域新增快照模式：不实时刷新，只能手动刷新；点击画布不发送设备事件，而是选中视图节点。
- Web Device Hub 增加 per-target `实时 / 快照` 模式。默认仍为实时模式；快照模式下 live screenshot 轮询 effect 直接退出，实时 fps 控件隐藏，显示 `手动刷新快照`。
- 手动刷新会重新拉取 screenshot；如果当前处于 view-tree 或已有 hierarchy scene，会同步刷新 hierarchy scene。
- 快照模式下 `.device-screen` pointer down 不再走 `sendHostInput`，而是按 hierarchy viewport frame 做 hit-test，选择命中的最深可见节点，并同步 view-tree / URL node。
- 验证：`npm --prefix Web run build` 通过；目标 DOM 测试 `snapshot mode stops live refresh` 通过；Playwright 隔离浏览器在当前 URL 验证进入快照后 screenshot 请求数 1.3 秒内不变，点击只更新选中节点且 `/web/host-input=0`，手动刷新后 screenshot 请求 +1，横向溢出 `0`。

## View Tree Panel IA Cleanup

- 用户指出 `panel=view-tree` 模式下不应显示切换 target；切换 target 是“设备”tab 的事情。
- Web Device Hub 已移除 view-tree 面板内的 target chip/list，仅在设备 tab 保留 target 列表与搜索过滤。URL 仍支持 `panel=view-tree` 和 `node` 恢复；需要切 target 时先回到设备 tab，再切回 view-tree。
- 用户继续指出 view-tree 面板里的 `视图层级 / 前台 App 未暴露 · ...` header 也不用显示。已移除 `.view-tree-title`，view-tree 面板只显示树本身；当前 target/app 信息保留在顶部 target 菜单和设备 tab。
- 浏览器 DOM 验证：`titleExists=false`、`targetChipCount=0`、`targetListExists=false`，view-tree 面板文本中不含 `视图层级` 或 `前台 App 未暴露`。截图归档：`docs-linhay/spaces/20260619-lookin-hierarchy-viewer/screenshots/20260619/20260619-view-tree-no-target-switch-no-header-after-v01.png`。
- 用户要求点击 view-tree 节点后相关视图区域需要高亮和描边。已把 `selectedHierarchyNode` 传入设备画布，基于 `HierarchyLayerNode.frame` 和 `HierarchyScene.viewport` 计算百分比 overlay，渲染 `.view-node-highlight[data-node-id]`；高亮包含半透明填充、2px 描边、外圈光晕，`pointer-events: none`，不改变设备输入链路。
- 浏览器验证 `?panel=view-tree&node=back`：左侧选中节点与 overlay 都为 `back`，overlay 类型 `UIButton`，位置 `left=3.58974% top=6.51659% width=8.71795% height=4.02844%`，computed border `2px`，颜色来自节点 color。截图归档：`docs-linhay/spaces/20260619-lookin-hierarchy-viewer/screenshots/20260619/20260619-view-tree-node-highlight-after-v01.png`。
- 用户追问 `UITabBar` 下为什么只有三个 `UIButton`。根因是当前 Web 页展示的是 iOS QA fallback hierarchy，不是真机 runtime 层级；`Web/src/data/mockData.ts` 中 `tabbar` 只硬编码了三个 mock tab。已将 fallback 数据改为四个 Overloaded 底部入口：`serverTab`、`photosTab`、`musicTab`、`settingsTab`，并补测试锁住 `tabbar` 的四个直系 `UIButton` 子节点。
- 用户继续要求“不要显示 mock”。Web Device Hub 已移除真实联调路径里的 mock fallback：`pageTargets` 只使用 `/web/host-targets` 返回的 host targets；host bridge 失败或 targets 为空时只显示真实错误/空态，不再显示 QA mock 设备。
- View tree 改为只消费 `/web/host-hierarchy` 的真实 `HierarchyScene`。层级请求失败时显示“未拿到实时视图层级”，不再按平台读取 `mockData.hierarchyScenes`；设备画布节点描边也只在真实 scene 存在时显示。
- 浏览器验证当前 `?target=sim:...&panel=view-tree&node=music-tab`：请求 `/web/host-hierarchy?platform=ios&target=1B360513-22E7-46DB-A942-198EE522C6DC...` 返回 200，view-tree 为 433 个 `ios:runtime:*` 节点；页面不含 `QA mock fallback`，也不含静态 `serverTab/photosTab/musicTab/settingsTab`。截图归档：`docs-linhay/spaces/20260619-lookin-hierarchy-viewer/screenshots/20260619/20260619-view-tree-no-mock-runtime-after-v01.png`。
- 用户反馈 view-tree 层级太深且名称被截断。树行改为 disclosure + wrapping copy block，类型和名称都允许 `overflow-wrap:anywhere`，不再单行 ellipsis；深层缩进改为 `clamp(0px, depth * 14px, 112px)`，避免十几层后只剩很窄的文本区。
- 为 Swift runtime mangled 类型名增加只读显示短名：例如 `_TtC10Overloaded...18LocalPhotoGridCell#598` 展示为 `LocalPhotoGridCell#598`，完整原始 `type/name` 保留在 `.tree-node-copy[title]`，不改变 hierarchy DTO 或节点选择逻辑。
- 用户随后在 `?target=ios-real:7a9d976cc4d4&panel=view-tree` 看到“未拿到实时视图层级”。Triton-first 事实显示 `triton:connection:30` 在线，手动 `/web/host-hierarchy?...&scope=real&kind=real-device` 成功，但裸 `target=ios-real:*` 请求失败为 `target_not_found`。
- 修复 Web dev bridge：`isIOSRuntimeMirror` 与 `isIOSRealHostTarget` 现在仅凭 `target` 以 `ios-real:` 开头即可走 App runtime mirror，并把 target 透传进 screenshot / input / hierarchy error context；即使前端 URL 或旧页面状态丢了 `scope/kind/source`，也会解析到当前 `triton:connection:*`。

验证：

- `node --test --test-name-pattern='target switching only|syncs selected device|filters targets|search empty state' dev/appFallbackDom.test.mjs`：4/4 通过。
- `node --test --test-name-pattern='highlights selected view-tree node area|syncs selected device' dev/appFallbackDom.test.mjs`：2/2 通过。
- `node --test --test-name-pattern='four Overloaded bottom tabs|highlights selected view-tree node area|syncs selected device' dev/appFallbackDom.test.mjs`：3/3 通过。
- `node --test --test-name-pattern='does not mount mock targets|does not render static mock hierarchy' dev/appFallbackDom.test.mjs`：3/3 通过。
- `npm run build`：通过。
- Playwright DOM 复验当前 `?target=sim:...&panel=view-tree&node=back`：`tabbar` 存在，直系 tab rows 为 `server-tab`、`photos-tab`、`music-tab`、`settings-tab`；`targetChips=0`、`headerExists=false`、`highlightNode=back`。
- in-app browser 复验 simulator view-tree：`rowCount=433`、`hasHorizontalOverflow=false`；长 Swift cell 行显示为 `LocalPhotoGridCell#598`，完整 mangled 名保留在 title，行高从约 `170px` 降到约 `61.5px`。
- `curl /web/host-hierarchy?platform=ios&target=ios-real%3A7a9d976cc4d4`：修复后返回 `ok=true`、source command 为 `triton hierarchy --platform ios --target triton:connection:30 --json`、`nodeCount=115`。
- in-app browser 复验真机 view-tree：URL 保持 `target=ios-real:7a9d976cc4d4&panel=view-tree`，`rowCount=115`，不再显示“未拿到实时视图层级”。
- `npm test`：55 条中 53 通过；剩余 2 条为既有设备控制/网络日志隐藏按钮测试与当前 App 控件实现不一致，非本次节点高亮引入。
- `git diff --check -- Web/src/App.tsx Web/src/styles.css Web/dev/appFallbackDom.test.mjs`：通过。

## Overloaded 真机 Web 实时画面与输入联调

- 用户要求联调 Overloaded 真机画面，并指出 Web 拿不到实时画面、随后 runtime input 报 `app_runtime_unavailable` / `ambiguous_target`。
- Triton-first 事实：`triton status --json` / `triton list --json` 显示 simulator 与真机 runtime 同时在线时，裸 `triton screenshot` / `triton input` 会解析成 `triton:local` 并触发 ambiguous；真机 host target 为 `ios-real:7a9d976cc4d4`，真机 runtime target 为 `triton:connection:4`。
- 修复 Web dev bridge：`/web/host-screenshot` 和 `/web/host-input` 在 iOS real-device App runtime mirror 模式下先调用 `triton list --json`，把 `ios-real:*` 映射到无 `simulatorUDID` 的 runtime target，再分别调用 `triton screenshot --target <runtime-id>` 与 `triton input --target <runtime-id> --json --summary`。
- 修复 packaged Swift Web bridge：截图路径和 input fallback 同样按 host target 选择非 simulator runtime，避免 packaged `triton web` 仍走 ambiguous target。
- 修复 Web 错误态：真机 runtime 不可用时在画布上显示明确 runtime guidance，不再静默空白。
- Overloaded 真机接入问题：Debug app 打包出的 `TritonKitDefaultHost` 曾为空字符串，bootstrap 回退到 `127.0.0.1`，真机上会连向手机本机而不是 Mac。已在 Overloaded Debug build settings 写入 `TRITONKIT_DEFAULT_HOST = 192.168.10.179`；Release 未改。
- 真机验证：`triton xcode run --project ... --scheme Overloaded --device ios-real:7a9d976cc4d4 --sdk iphoneos` 增量 build/install/launch 成功；`triton list --json` 显示 `triton:connection:4` 与 simulator runtime 同时在线。
- Web 验证：`/web/host-screenshot?...target=ios-real:7a9d976cc4d4...` 返回 `ok=true`，source command 为 `triton screenshot --target triton:connection:4 ...`，PNG 尺寸 `428x926`；浏览器切换到 `?target=ios-real%3A7a9d976cc4d4` 后 DOM 中 `img.real-screenshot` 为 `iPhone 截图`、`data:image/png`、无 `App runtime 未连接` / `ambiguous_target`。
- 输入验证：`/web/host-input?...target=ios-real:7a9d976cc4d4...` 不再返回 ambiguous；测试请求已进入 runtime，因点击左上角空白 `UIView` 返回业务级 `ok=false` / `Hit view does not expose a public UIControl tap action`，说明路由层已恢复。
- 后续真机输入问题：用户点击底部 tab / cell 时分别遇到 `Hit view does not expose a public UIControl tap action`、`runtime_ui_interrupted` 408 和疑似闪退。排查后确认不能用 `UIGestureRecognizer.value(forKey: "_targets")` 之类私有 KVC 触发任意 gesture；Objective-C exception 不能被 Swift `catch` 稳定兜住，可能直接杀业务 app。
- Runtime input 修复方向：只保留公开 UIKit/AX 路径。`UIControl` 优先处理 text/toggle/segment/slider/stepper/tabbar，其次 `accessibilityActivate()`，再走公开 `allTargets/actions`；table/collection cell 走 `selectRow` / `selectItem` 后异步调用 delegate `didSelect...`，避免业务回调阻塞 runtime 响应；普通 tap gesture 仅尝试 view 的 `accessibilityActivate()`，否则明确返回“不支持公开触发 gesture target”。
- Cell 命中补强：如果 `hitTest` 命中的是 collection/table 内部私有 wrapper 或容器而不是 cell superview 链，runtime 会按输入坐标扫描可交互 `UITableView` / `UICollectionView`，用 `indexPathForRow(at:)` / `indexPathForItem(at:)` 反查 cell，再执行同一套 cell selection。
- 用户反馈点击“照片”tab 时“设置”tab 响应。现场 hierarchy 发现 iOS 26 浮动 `UITabBar` 会暴露两组重叠 `_UITabButton`：一组在 `SelectedContentView`，一组在 `ContentView`。旧 runtime 按全部 `UIControl` 扁平数组排序后直接映射 `tabBar.items[index]`，导致视觉第二槽位可能落到扁平第四个按钮，从而选择“设置”。
- 修复：`performTabBarControlTap` 不再用扁平按钮下标，而是按屏幕 `midX` 把重复 `_UITabButton` 聚合成视觉槽位 `tabBarButtonSlots`，再由命中 control 或输入坐标映射到对应 `UITabBarItem`。新增 `UITabBar duplicate private button layers map by visual slot` 回归测试，锁住重复私有按钮层只算一个视觉槽位。
- Web dev bridge 同步修复 `/web/host-hierarchy` 真机 runtime mirror：`ios-real:*` 会按 `scope=real&kind=real-device&source=runtime` 解析到当前 `triton:connection:*` 后再调用 `triton hierarchy --platform ios --target ... --json`，避免 view-tree 真机层级请求把 host target 直接传给 runtime CLI 导致 `target_not_found`。
- 真机连接稳定性补强：real device runtime WebSocket 在 app 仍运行时会短时间从 `/targets` 消失；将 embedded runtime keepalive 从 15 秒首次 timer 改为连接后立即 `sendPing`，并每 3 秒续 ping。Overloaded SwiftPM checkout 里按旧版 `TritonKit.swift` 做了同样最小 patch，避免整文件覆盖引入主仓新 semantic 类型导致旧 checkout 编译失败。
- Overloaded 本地 SwiftPM checkout 已同步 `tabBarButtonSlots` patch 并重新 `xcodebuild` 真机 Debug 成功；修复版 `.app` 已通过 `triton app install --platform ios --scope real --device ios-real:7a9d976cc4d4 ...` 安装到真机。
- 用户解锁真机后，`triton app launch ... --bundle-id overloaded.cn.debug --json` 成功，App runtime 重新注册为 `triton:connection:29`。
- Web endpoint 回归：`/web/host-input?...ios-real...` 点设置中心 `x=350,y=883` 后 `_UITabSelectionView.x=298.5`，点照片中心 `x=168,y=883` 后 `_UITabSelectionView.x=116.166...`，确认 Web 点击照片不会再串到设置。
- 当前照片页 hierarchy 未暴露标准 `UITableViewCell` / `UICollectionViewCell` 类型，主要是图片/自定义容器；未对内容区做盲点测试，避免影响用户当前真机状态。cell selection 专用路径仍需结合具体命中 view 继续追。

验证：

- `node --test Web/dev/iosSimulatorBridge.test.mjs`：23/23 通过，覆盖 iOS real-device Web input / screenshot / hierarchy bridge。
- `npm --prefix Web run build`：通过。
- `xcodebuild -project /Users/linhey/Desktop/FlowUp-Libs/Overloaded-v2/iOS/Overloaded/Overloaded.xcodeproj -scheme Overloaded -configuration Debug -sdk iphoneos -destination 'id=00008140-000474401E3B001C' -derivedDataPath ... build`：通过。
- `triton app install --platform ios --scope real --device ios-real:7a9d976cc4d4 --app .../Overloaded.app --json`：通过。
- `triton app launch --platform ios --scope real --device ios-real:7a9d976cc4d4 --bundle-id overloaded.cn.debug --json`：解锁后通过。
- `curl /web/host-hierarchy?...target=ios-real...`：返回 `ok=true`、source command 为 `triton hierarchy --platform ios --target triton:connection:29 --json`、viewport `428x926`。
- `curl /web/host-screenshot?...target=ios-real...`：返回 `ok=true`、source command 为 `triton screenshot --target triton:connection:29 ...`、PNG `428x926`。
- `curl -X POST /web/host-input?...target=ios-real...`：设置与照片 tab 都返回 `ok=true` / `strategy=tab-bar-selection`，选中条分别落在第四/第二视觉槽位。
- `npm --prefix Web test`：55/55 通过。
- `npm --prefix Web run build`：通过，保留既有 Three.js chunk > 500KB 警告。
- `swift test --package-path CLI --filter SingleDeviceWebPageTests`：17/17 通过。
- `swift build`：通过。
- Overloaded SwiftPM checkout `swift build`：通过。
- `swift test --filter TKRuntimeInputActionsTests`：被既有 `Tests/TritonKitSharedTests/TKXcodeWorkflowModelsTests.swift` 编译错误阻塞，错误为缺少 `TKXcodeDerivedDataCacheState` 与 `TKXcodeProgressEvent` / `TKXcodeActionSummary` 初始化参数不匹配。

## BuilderIO skills 工作流吸收

- Space：`docs-linhay/spaces/20260619-agent-workflow-skill-governance/`
- 用户确认由主控负责吸收 `BuilderIO/skills` 中适合 TritonKit 的模式；本轮只吸收工作流，不直接复制外部 skill 包，不引入 Agent-Native hosted Plan 作为默认依赖。
- 新增内部 skill：`.agents/skills/tritonkit-plan-arbiter/SKILL.md`，用于多 agent / 多方案 / 多 PR 策略裁决，输出固定为 `Adopt` / `Hybrid` / `Revise first`，并要求列出验证门禁和拒绝项。
- 更新 `.agents/skills/tritonkit-subagent-supervision/SKILL.md`：加入 `watch only`、`audit`、`audit and fix`、`compare` 四种监督模式，明确审计 evidence、问题分类和报告结构；subagent 结果只作为证据线索，主控必须复核关键文件、diff、命令输出、截图或 CI 状态。
- 更新 `.agents/skills/tritonkit-autonomous-cruise/SKILL.md`：吸收 plow-ahead / efficient-frontier 思路，补充自治决策规则、真实停止条件、wave 推进、subagent handoff 和 decision log。
- 更新 `.agents/skills/tritonkit-ops-governance/SKILL.md` 与 `.agents/skills/README.md`：记录 plan arbiter 触发入口、本地 visual recap 落位、docs-first 规则，以及内部 / public skill 边界。
- 新增研发文档：`docs-linhay/dev/20260619-agent-workflow-skill-governance.md`，沉淀取舍：吸收 agent-watchdog、plan-arbiter、plow-ahead、efficient-frontier、visual-recap、read-the-damn-docs 的可迁移模式；不吸收模型品牌绑定、hosted Plan 强依赖和 emoji 状态 footer。

## 内部 skill 瘦身 P1

- Space：`docs-linhay/spaces/20260619-agent-workflow-skill-governance/`
- 将两个历史设备 subagent 编排入口合并为 `.agents/skills/tritonkit-device-subagent-orchestration/SKILL.md`：
  - 原 `tritonkit-android-subagent-orchestration` 作为 Android Emulator track。
  - 原 `tritonkit-real-device-subagent-orchestration` 作为 cross-platform real-device track。
- 删除旧的 `.agents/skills/tritonkit-android-subagent-orchestration/SKILL.md` 与 `.agents/skills/tritonkit-real-device-subagent-orchestration/SKILL.md`，减少项目级 skill 入口膨胀；相关 source-of-truth 仍保留在各自 `space/plans/`。
- 移除 `.agents/skills/apple-docs` symlink。它指向外部 `linhey.wiki` Apple 文档 skill，不属于 TritonKit 仓库内部治理 skill；外部真实目录仍保留，后续 Apple 文档查询继续走用户级 / 外部 skill 来源。
- 更新 `.agents/skills/README.md`、Android Emulator execution breakdown、real-device P0-P3 plan、`docs-linhay/dev/20260619-agent-workflow-skill-governance.md` 和本 space README，全部指向新的统一入口。

## 内部 skill 瘦身 P2

- Space：`docs-linhay/spaces/20260619-agent-workflow-skill-governance/`
- 将 `tritonkit-session-skill-distill` 合并进 `.agents/skills/tritonkit-ops-governance/SKILL.md`：ops governance 的 description 现在显式覆盖“整理 / 沉淀 / 收尾整理”触发词，正文保留会话沉淀顺序、收尾隔离、只 stage 本次整理相关文件、SwiftPM / CLI 修复沉淀和 AGENTS 升级边界。
- 删除 `.agents/skills/tritonkit-session-skill-distill/SKILL.md`，减少独立治理分流入口。
- 更新 `AGENTS.md`：整理 / 沉淀类指令统一使用 `tritonkit-ops-governance` 执行，不再要求同时触发 session distill skill。
- 更新 `tritonkit-autonomous-cruise` 的 skill 关系说明，将会话整理和模式沉淀指向 `tritonkit-ops-governance`。

## Issue #65 Xcode Wait Idle Host Timeout

- 在独立 worktree ../TritonKit-worktrees/20260619-issue-65-xcode-wait-idle-timeout/ 处理 GitHub Issue #65，分支 feat/20260619-issue-65-xcode-wait-idle-timeout。
- 修复 waitForXcodeIdle：单次 HostCommandRunError.timeout 视为 transient poll failure，继续等待调用者指定的总 timeout；若后续观察到 active xcodebuild，最终返回 xcode_not_idle 而不是 host_command_timeout。
- 新增测试覆盖 pgrep 首次超时、后续仍能观察到 active build 的场景。
- 验证：swift test --package-path CLI --scratch-path .build/cli-issue65 --filter XcodeDiagnosticsTests；git diff --check；docs-linhay/scripts/check-docs.sh。

## issue #66：Xcode DerivedData 增量缓存语义

- 需求：让 `triton xcode build` 的默认 `.triton/DerivedData` 增量缓存行为对 agent 可见，避免误以为每次都是全量构建或默认清理缓存。
- 实现：新增 `TKXcodeDerivedDataCacheInfo`，在 Xcode invocation、action summary、status / use 输出和 schema 中暴露 `path`、`exists`、`cacheState`、`incrementalExpected`、`cleanupPolicy`、`guidance`。
- 决策：不新增自动清理；`cleanupPolicy=preserve-by-default` 明确 Triton 默认不删除 DerivedData，若要重置缓存应由后续显式命令承担。
- 验证：见 `docs-linhay/spaces/20260619-issue-66-xcode-incremental-cache-ux/README.md`。

## issue #67：host screenshot artifact metadata

- 需求：真实 UI audit 中，agent 需要对 host-side screenshot 做非空、尺寸和帧差比对，当前 JSON 只有 artifact / format，需要额外 shell 检查。
- 本轮切片：为 `HostDeviceArtifactOutput` 增加 `bytes`、`width`、`height`、`sha256`、`capturedAt`，并在 screenshot schema 中暴露 `host.artifact` 契约。
- 边界：#67 其余建议继续保留为后续切片，不在本提交混入 burst/video、overwrite、Xcode status 日志或 DerivedData cleanup。

## issue #68：Harmony HDC 语义失败不能返回 ok:true

- 需求：真实 Harmony target 的 screenshot 可用，但 app launch/open-url 遇到 HDC stdout `[Fail]Not match target founded` 时仍返回成功，导致 agent 误判动作已提交。
- 实现：`runHostCommand` 与 `runHostCommandWritingStdoutArtifact` 对 HDC stdout/stderr 中的 `[Fail]` 做语义失败识别，即便 exit code 为 0 也走 `HostCommandRunError.nonZeroExit`。
- 验证：补充 app lifecycle planner 对 Harmony real-device raw target 的 launch/open-url 断言；补充 fake hdc stdout `[Fail]` 测试。
- 边界：未调用真实 HDC；不 push、不关闭 issue。

## Issue #69 iOS Real Device App Selector

- 在独立 worktree ../TritonKit-worktrees/20260619-issue-69-ios-real-device-app-selector/ 处理 GitHub Issue #69，分支 feat/20260619-issue-69-ios-real-device-app-selector，避免污染主仓已有 WIP。
- 问题根因：iOS host-side app 命令在显式 --device ios-real:<hash> 但未带 --scope real 时仍只发现 simulator；同时 devicectl parser 未把 UDID / unique device id / serial 作为本地 selector alias。
- 修复：显式 iOS selector 默认 discovery scope 扩展为 .all，booted 保持 simulator；TKDevicectlDeviceTarget.alternateIdentifiers 与 HostDeviceTarget.rawTargetAliases 支持 CoreDevice identifier / UDID alias 匹配，敏感 alias 不进入 Encodable JSON。
- 验证通过：
  - swift test --package-path CLI --scratch-path .build/cli-issue69-green --filter 'DeviceCrossPlatformTests|TKHostAdapterModelsTests'：DeviceCrossPlatformTests 80 tests passed。
  - swift test --scratch-path .build/issue69-shared --filter TKHostAdapterModelsTests：36 tests passed。
- 剩余风险：未连接真实 iPhone 跑 live smoke；triton app list 对 iOS real device 仍返回 host action / artifact 摘要，不在本轮扩展解析完整 app list。
