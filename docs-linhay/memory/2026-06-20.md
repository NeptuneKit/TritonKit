# 2026-06-20

## Hierarchy viewer 显示 UIViewController 节点

- 用户要求 view-tree 中也显示 `UIViewController`。
- 根因：iOS runtime 已在 `TKDisplayItem.hostViewControllerObject` 暴露控制器对象，但 CLI legacy iOS scene 与 Web dev bridge fallback 之前只把它作为 class fallback，控制器不会成为独立树节点。
- 已在 CLI 转换层和 Web dev bridge 转换层插入 `ios:controller:<oid>` 语义节点，字段包含 `source=runtime-controller`、`raw.role=UIViewController`、`style.display=controller`、`renderHints.preferredMode=structure`。
- 实际 view/layer 节点挂到对应 controller 下；同一 controller 子树内不重复插入控制器节点。只有 runtime DTO 提供 `hostViewControllerObject` 时才显示，不在 Web 端伪造。
- 验证：`node --test --test-name-pattern='falls back to legacy iOS runtime hierarchy and converts it to a Web scene' dev/iosSimulatorBridge.test.mjs` 通过；`npm --prefix Web run build` 通过；`swift test --package-path CLI --filter HierarchySceneRuntimeTests` 3 个用例通过。
- 浏览器复验：重启 `34127` Vite dev server 后，当前 simulator view-tree 出现 `MainTabBarController#12`、`AppNavigationController#585`、`PhotosViewController#592`、`UITrackingElementWindowController#442`、`UIEditingOverlayViewController#447` 五个 `ios:controller:*` 节点；节点数 `433 -> 438`，横向溢出 `0`。

## 模拟器壳标注当前 UIViewController

- 用户确认采用更好的 runtime 事实优先方案，不从 screenshot 或 wrapper view 猜当前 controller。
- `TKHierarchyScene` 增加可选 `controllerContext`，字段包含 active controller id/name/class、controller stack 和 source。
- iOS embedded runtime 的 hierarchy 响应现在复用 UIKit route 解析逻辑，按 root / presented / tab selected / navigation top / split / page / custom visible child 生成 `runtime-route` controller stack。
- CLI legacy iOS scene 和 Web dev bridge 都支持旧 runtime fallback：如果 payload 没有 `controllerContext`，则从 `ios:controller:*` / `runtime-controller` 节点生成 `scene-controller-node-fallback`。
- Web 设备壳新增 `controller-shell-badge`：优先显示当前选中节点所属 controller，否则显示 runtime active controller；fallback 会显示 `fallback` 标记。
- 验证：`swift test --package-path CLI --filter HierarchySceneRuntimeTests` 通过；Web bridge / DOM 目标测试 2/2 通过；`npm --prefix Web run build` 通过；in-app browser 当前 simulator 页面显示 `UIViewController · PhotosViewController fallback`，横向溢出 `0`。

## 整理沉淀：runtime 语义标签必须事实源优先

- 会话沉淀识别出的复用模式：Web mock 上的可见语义标签如果依赖 runtime state，必须先由 CLI / HTTP / runtime DTO 提供机器可读字段，Web 只读消费。
- 已将该模式写入 `.agents/skills/tritonkit-web-mock-ui/SKILL.md`：UIKit route / controller ownership / hierarchy owner 等标签优先使用 `HierarchyScene.controllerContext` 这类一等事实字段；旧 runtime 才允许从 `ios:controller:*` 父链或几何信息 fallback，并必须显式标记 fallback。
- 新增研发说明 `docs-linhay/dev/20260620-hierarchy-controller-context.md`，记录 controller context 的契约原则、实现落点、验证门禁和后续复用规则。
- 未升级 `AGENTS.md`：这条规则仍属于 Web mock / hierarchy viewer 领域内的实现模式，不是全仓长期行为规范。

## 右侧 DevTools 面板支持收起

- 用户要求当前右侧 DevTools 面板也支持收起，和左侧 sidebar 一样释放中央模拟器区域空间。
- 已在 Web mock 中新增 `isDevtoolsVisible` 布局状态，并在顶部检查器工具区增加 `PanelRight` 图标按钮；按钮文案在 `收起右侧面板` / `展开右侧面板` 之间切换。
- `.hub-body` 新增 `is-devtools-hidden` 组合 class，覆盖桌面、窄屏、移动端网格；左栏和右栏可独立收起，也可同时收起。
- 右侧面板隐藏只是前端布局状态，不停止 hierarchy、screenshot、network、logs 读取，也不改变 CLI / HTTP 契约；再次展开时保留用户之前选中的右侧 tab。

## 模拟器壳 controller 类名不截断

- 用户指出模拟器壳上的 `UIViewController` 类名会被截断，当前原因是 `.controller-shell-badge strong` 使用单行省略号。
- 处理边界限定在 Web mock 设备壳：类名标注移到模拟器壳上方，只显示可读的自定义 controller 类名，支持文本选择 / 复制，占满可用宽度并允许换行；Swift 私有 mangled 类名会提炼为可读后缀类名，`title` 继续保留完整 controller stack。
- 不改变 runtime fact source、`HierarchyScene.controllerContext`、view-tree 数据或 CLI / HTTP 契约。

## 实时模式刷新 controller 标签

- 用户切换 App 页面后，模拟器壳上方 controller 标签仍停在旧类名。
- 根因是 Web 只在首次打开 `view-tree` 时读取 hierarchy cache，实时预览轮询只更新 screenshot；controller 标签依赖 `HierarchyScene.controllerContext`，不会随截图自动变化。
- 已在非快照实时模式下为 `view-tree` 增加低频 hierarchy 刷新，继续通过只读 `/web/host-hierarchy` DTO 更新 controller 标签和视图树；快照模式保持手动刷新，不实时刷。

## VLM test runner primitive fact gate

- 用户确认 Revyl / Maestro / VLM 测试能力方向需要先收敛到底层事实门禁，避免直接实现 runner 后被迫补第二套 runtime。
- 已在 `docs-linhay/spaces/20260620-vlm-test-runner/plans/primitive-stability-matrix.md` 建立开工门禁表，限定 verdict 只能是 `pass`、`pass-with-gap`、`blocker`、`unknown`。
- 真实 iOS Simulator smoke 使用 `Examples/TritonKitDemo` 和 booted iOS 26.5 simulator，覆盖 `triton xcode run`、status/list、geometry、hierarchy、AX、screenshot、text tap、point tap、focused type、assert pass/failure、`.tritonevidence` pass/failure、evidence inspect/summary、record template 和 replay dry-run。
- 当前总裁决为 `pass-with-gap`：现有 primitive 足以支撑下一步 P0B `triton test validate` 离线 normalized plan，但完整 `triton test run`、remote VLM、App Map、selector healing 和 HTML report 必须后置。
- 关键缺口：Demo 不是 Login/Home/Settings/Delayed/List/Error/Modal fixture；AX exact text 对部分可见 SwiftUI title 不稳定；runtime screenshot 输出为 point-sized PNG，而 geometry 仍记录 `scale=3`，VLM 坐标转换必须显式建模；runner 还需要把 action/assert/screenshot 写入 `.tritonevidence/run/events.jsonl`。

## VLM test runner P0B validate-only

- 用户裁决：P0A 只能打开 P0B validate-only，不能打开 runner execution；P0B 不碰 device、simulator、VLM、App Map、replay 或 run events。
- 已新增 `triton test validate <path.tritontest.yaml> --json`、`--emit-normalized-plan` 与 `triton test normalize <path> --json`，只做 YAML parse / validate / normalize。
- P0B 仅支持 `launch`、`takeScreenshot`、`tap.point`、`assertVisible.text`；`tap.point.coordinateSpace` 固定为 `runtime-point`，`assertVisible` 固定为 exact AX text。
- validation error 输出 `{ ok:false, error:{ type:"validation_error", message, path, code, allowed? } }`，覆盖 `unsupported_step`、`invalid_point`、`duplicate_step_id`、`invalid_app_bundle_id` 等 contract tests。
- 新增样本：`samples/pass-contract.tritontest.yaml` 与 `samples/invalid-contract-swipe.tritontest.yaml`；invalid swipe smoke 确认 exit code 为 1、`error.path=$.steps[0].swipe`。
- 验证：`swift test --package-path CLI --filter TestValidationTests` 7/7 通过；`swift test --package-path CLI --filter SchemaFactSourceTests` 108/108 通过；真实 CLI sample validate pass/fail 已跑通。
- 下一步只应进入 P0C Run Event Writer + Fixture App；完整 runner execution 仍后置到 P0D。

## VLM test runner P0C run events + fixture

- 用户裁决：P0C 只能补可审计执行地基，不实现 `triton test run`、step executor、selector healing、App Map、remote VLM、AI assertion、replay evidence 或 HTML report。
- 已新增 `Examples/TritonKitTestFixture`，bundle id 为 `com.neptunekit.tritonkit.testfixture`，包含 Login/Home/Settings/Delayed Loading/Dynamic List/Error State/Modal，关键元素有稳定 visible text 与 `accessibilityIdentifier`。
- 已新增 `TKTestRunEventWriter`、`TKTestRunEventLogParser` 与 dot-style event contract，事件覆盖 `run.started`、`step.started`、`command.executed`、`artifact.created`、`assertion.result`、`step.finished`、`run.finished`、`failure.recorded`。
- `.tritonevidence` 兼容增加 `run/run.json` 与 `run/events.jsonl`；`evidence summary` 现在能读回 run event count、run id 和 verdict。
- 真实 iOS Simulator fixture smoke 已生成 pass/failure bundle：`20260620-p0c-fixture-pass.tritonevidence` 有 13 个 run events，`20260620-p0c-fixture-failure.tritonevidence` 有 10 个 run events；缺失文本断言 `Definitely Not Existing` 返回 exit code 1 且 failure evidence 未断。
- 验证：`RunEventWriterTests` 3/3 通过；`EvidenceBundleTests/summaryExposesP0CRunEventOverview` 通过；`TestValidationTests` 7/7 通过；`git diff --check` 通过。
- 未通过项均为 unrelated：root `swift test --filter TKTestRunEventModelsTests` 被既有 `Tests/TritonKitTests/TKDisplayItemTests.swift` 编译错误阻塞；`docs-linhay/scripts/check-docs.sh` 被既有 `docs-linhay/spaces/20260619-issue-68-harmony-app-target-failure` 缺 README 阻塞。
- 当前裁决为 `pass-with-gap`：只允许进入 P0D minimal runner execution，且仅执行 P0B 支持的 `launch`、`takeScreenshot`、`tap(point)`、`assertVisible(text)`。

## VLM test runner P0D minimal runner execution

- 用户要求进入 P0D，只实现 `triton test run <path.tritontest.yaml> --json --evidence-dir <dir>`；执行前必须复用 P0B validate/normalize，执行范围只限 `launch`、`takeScreenshot`、`tap(point/runtime-point)`、`assertVisible(text/source=ax/match=exact)`。
- 已新增 `CLITestRunRuntime` 和 `TestRun` CLI subcommand；runner 在 validation 通过后才创建 evidence 和调用 primitive executor。
- P0D `launch` 只绑定并验证已连接 TritonKit runtime target 与 `app.bundleId` 对齐，不在 runner 内新增 host install / launch / reset 逻辑；真实 smoke 通过 `triton app terminate/launch` 在 runner 外部重置 fixture。
- 执行过程写入 `.tritonevidence/run/events.jsonl`、`run/run.json`、`normalized-plan.json`、`manifest.json`，并把 screenshot / failure debug artifacts 纳入 `evidence summary` 可读的 run manifest。
- failure run 记录 `failure.type=assert_visible_failed`、selector、artifactRefs、失败截图、AX、hierarchy 和 assert-result；CLI exit code 为 1。
- unsupported `swipe` 仍是 P0B `validation_error/unsupported_step`，CLI exit code 为 1，目标 `.tritonevidence` 目录不会创建；focused fake executor 测试确认 primitive operation count 为 0。
- `triton schema --command test --json` 和 `triton capabilities --json` 已增加 `test-run-minimal` 机器可读事实；validate/normalize 保持离线，schema 明确 run 需要 runtime target，实际 readiness 仍由 `status/doctor` 表达。
- 真实 fixture smoke artifacts：`20260620-p0d-fixture-pass.tritonevidence`、`20260620-p0d-fixture-failure.tritonevidence`；plan 文件为 `p0d-fixture-pass.tritontest.yaml`、`p0d-fixture-failure.tritontest.yaml`、`p0d-unsupported.tritontest.yaml`。
- 验证：`swift test --package-path CLI --filter TestRunExecutionTests` 3/3 通过；`swift test --package-path CLI --filter TestValidationTests` 7/7 通过；真实 pass smoke exit code 0；真实 failure/unsupported smoke 均按预期 exit code 1。
- 当前裁决为 `pass-with-gap`：P0D 只允许最小 deterministic flow；完整 runner execution、VLM、App Map、selector healing、replay、HTML/JUnit、`tap(text)`、`input`、`swipe` 仍不得进入。

## VLM test runner P0E screen workspace readiness

- 用户修正裁决：P0E 不叫 Hardening Gate，而是 Screen Workspace Readiness；下一步价值不是继续证明 runner 能跑，而是让 runner evidence 足够派生 screen graph。
- 已在 P0D runner events 中新增 `observation.captured`，覆盖 `takeScreenshot.after`、`tap.before/after` 和失败 `assertVisible.after`。
- observation event 固定引用 screenshot、AX、hierarchy artifact，并写入 `screenCandidate`：`screenshotSha256`、`axTextHash`、`hierarchySha256`、`visibleTexts`；tap after 会在 fingerprint 变化时记录 `changed=true`。
- `.tritonevidence` 新增 `coordinate-contract.json`，明确 `canonicalTapSpace=runtime-point`，runtime screenshot space 为 point-sized image，runtime geometry 仍记录 `scale=3`，VLM image space / host framebuffer space 在 P0E 明确 unsupported。
- `manifest.run.observationCount` 与 `triton evidence summary` 已能读回 observation 数量。
- 真实 fixture pass smoke 生成 `20260620-p0e-fixture-pass.tritonevidence`：`observationCount=4`，包含 Login observation、tap before/after、Home observation，tap after `changed=true`。
- 真实 fixture failure smoke 生成 `20260620-p0e-fixture-failure.tritonevidence`：exit code 1，失败 step observation 存在，failure artifactRefs 覆盖 assert result、failure screenshot、AX、hierarchy。
- unsupported `swipe` 仍是 P0B `validation_error/unsupported_step`，exit code 1，`20260620-p0e-unsupported.tritonevidence` 未创建。
- 新增文档 `docs-linhay/spaces/20260620-vlm-test-runner/plans/p0e-screen-workspace-readiness.md`；README 分期计划已改为 P0E 后打开 P1 Screen Workspace Evidence projection，VLM 后置。
- 验证：`swift test --package-path CLI --filter TestRunExecutionTests`、`TestValidationTests`、`EvidenceBundleTests`、`SchemaFactSourceTests` 均通过，其中 `SchemaFactSourceTests` 为 108/108 通过。
- 未通过项为 unrelated：根包 `swift test --filter TKTestRunEventModelsTests` 会编译整个 root test target，被既有 `TKXcodeWorkflowModelsTests.swift` / `TKDisplayItemTests.swift` 编译错误阻塞。

## VLM test runner P1 screen workspace evidence projection

- 用户裁决：P1 不是 runner execution，而是 evidence projection；只读取 `.tritonevidence/run/events.jsonl`，消费 `observation.captured`，生成 `screens.json` 与 `transitions.json`。
- 已新增 `triton evidence project-screens <dir.tritonevidence> --json`，纯离线执行，不连接 runtime、simulator、VLM、App Map 或 replay。
- `screens.json` 按 strict fingerprint 分组：`screenshotSha256 + axTextHash + hierarchySha256`，生成 run-local `screen-000` 等 id，不引入 `screenTemplateId` 或 cross-run merge。
- `transitions.json` 只从 `tap.before + tap.after + after.changed=true` 派生 transition；非 tap、缺 before/after、未变化或同屏只记录 warning / 跳过。
- `manifest.json` 与 `triton evidence summary` 已增加 `screenWorkspace`，包含 `screensPath`、`transitionsPath`、`screenCount`、`transitionCount`、`warningCount`；manifest artifacts 增加 `screen-workspace.screens` 与 `screen-workspace.transitions`。
- `triton schema --command evidence --json` 与 `triton capabilities --json` 已暴露 `evidence-project-screens`，nextAction 为 `evidence project-screens <dir.tritonevidence> --json`。
- 新增文档 `docs-linhay/spaces/20260620-vlm-test-runner/plans/p1-screen-workspace-evidence-projection.md`；README P1 状态已更新。
- 当前验证：`swift test --package-path CLI --filter ScreenWorkspaceProjectionTests` 3/3 通过；schema/capability focused tests 108/108 通过。
- P1 仍不打开 `.tritonmap`、VLM、AI assert、selector healing、HTML/JUnit、path/suite 或完整 runner execution。

## VLM test runner P1-P2 app map test path graph

- 用户进一步裁决：不要继续按 P1、P1B、P2 细抠，P0A-P0E 已经打够地基，本轮直接进入 P1-P2 App Map Test Path Graph；仍然不扩 runner step、不做 VLM、不做 AI、不做 App exploration。
- 已新增 `triton evidence project-workspace <dir.tritonevidence> --json`，作为 `project-screens` 的更准确入口，继续只离线生成 `screens.json` 与 `transitions.json`。
- 已新增 `triton map` 命令组：`merge`、`inspect`、`paths`、`export-flow`；schema/capabilities/recovery taxonomy 均已暴露机器可读契约。
- `map merge` 会在 evidence 缺少 P1 workspace projection 时自动补齐，然后生成 `.tritonmap/`：`app-map.json`、`screens/`、`transitions/`、`paths/`、`suites/smoke.json`、`runs/`。
- map-level screen id 来自 strict fingerprint hash；transition 只接收 `tap.before` / `tap.after` 且 `after.changed=true` 的 replayable runtime-point action；path 只从 confirmed replayable transition chain 派生。
- pass fixture smoke 从 `20260620-p0e-fixture-pass.tritonevidence` 生成 `screenCount=2`、`transitionCount=1`、`pathCount=1`、`suiteCount=1`，path id 为 `path-fixture-login-home`。
- `map export-flow` 已导出 `generated-fixture-login-home.tritontest.yaml`，并通过 `triton test validate`；normalized steps 为 `launch`、`takeScreenshot`、`assertVisible("Fixture Login")`、`tap(point/runtime-point)`、`assertVisible("Fixture Home")`。
- failure fixture smoke 从 `20260620-p0e-fixture-failure.tritonevidence` 生成 `screenCount=1`、`transitionCount=0`、`pathCount=0`，inspect health 为 `failCount=1`，不会生成成功路径。
- 当前 runtime server 未运行：`triton status --json` 返回 `server_unavailable`，所以本轮没有真实执行导出的 flow；这不是 export-flow / validate 的契约失败。
- 新增文档 `docs-linhay/spaces/20260620-vlm-test-runner/plans/p1-p2-app-map-test-path-graph.md`；README 分期计划已更新为 P1-P2 后再进入 mock VLM。
- 当前验证：`swift test --package-path CLI --filter AppMapPathGraphTests` 3/3 通过；真实离线 smoke 命令链通过。

## VLM test runner P2B/P3 exported flow rerun gate + map inspection

- 用户裁决：P1-P2 为 `pass-with-gap`，产品主链成立；唯一 gap 是 exported flow 没有真实 re-run。下一步不接 VLM，先补 P2B Exported Flow Re-run Gate，并推进 P3 App Map Inspect + Path Operations。
- 已补 path-level health 回灌：`TKAppMapPath` 新增 `sourceRuns`，同一路径 merge 新成功 run 时 path `observedRuns/passCount` 增加，同 run id 重新 merge 不重复计数。
- failure evidence 若没有成功 transition，只更新 map-level run health，不生成 duplicate path，也不污染成功 path health。
- 已新增 P3 只读命令：`triton map screens`、`map transitions`、`map path show`、`map health`、`map suite inspect`。
- P3 命令均为 offline App Map inspection，只读取 `.tritonmap`，用于回答 screens、transitions、paths、suite、health 和 coverage gap；不执行 device operation，不做 VLM/AI/selector healing/HTML/JUnit。
- 新增文档 `docs-linhay/spaces/20260620-vlm-test-runner/plans/p2b-p3-exported-flow-rerun-and-map-inspection.md`；README 路线更新为 P2B/P3 之后再进入 mock VLM。
- 用户要求本轮改为“大步推进，最后再集体测试”，因此后续不再每个小改动后单独跑测试，改为统一跑 AppMap、ScreenWorkspace、EvidenceBundle、TestRunExecution、SchemaFactSource 和真实 fixture smoke。

## Overloaded 本地联调：iOS Simulator runtime input

- 用户在 Web Device Hub 联调 Overloaded 时发现 `longPress` 没有及时触发，并返回 `runtime input longPress iOS Simulator host-side longPress is not exposed in the Web device surface yet`。
- 根因拆成两层：
  - Web canvas 只在 `pointerup` 后按按住时长判断 `longPress`，导致长按不会在阈值到达时及时提交。
  - iOS Simulator target 的输入请求默认沿用 `screenshotSource=host`，`longPress` / `pinch` 会落到 host-side adapter；该 adapter 对这些手势没有公开实现，应该改走 embedded runtime。
- 修复：
  - Web `SingleGestureState` 增加 `longPressDispatched`，在 520ms 阈值到达且指针未移动超过 tap threshold 时立即发送 `longPress`；`pointerup` 只清理预览，不再重复发送或退化成 tap。
  - Web `sendHostInput` 对 iOS Simulator 的 `longPress` / `pinch` 强制设置 `source=runtime`。
  - Swift packaged Web bridge 的 `/web/host-input` 解析 `target`，并通过 `/targets` 将 `sim:<UDID>` 映射到具体 `triton:ios-simulator:<UDID>`，避免多 runtime target 时 ambiguous。
  - embedded runtime 新增公开 UIKit 边界内的 `performLongPress`：命中 `UIControl` 时发送 `.touchDown`、等待 `duration`、再发送 `.touchUpInside`；命中仅有 `UILongPressGestureRecognizer` 的视图时返回公开 API 边界错误，不做私有 target-action 反射。
  - embedded runtime 新增 `performPinch`：命中 zoomable `UIScrollView` 时调整 `zoomScale`，否则返回 `zoomable-scroll-view-required`。
- 现场验证：
  - `node --test --test-name-pattern "dispatches canvas long press and pinch gestures" Web/dev/appFallbackDom.test.mjs` 通过，证明长按在 `pointerup` 前已发送且 `source=runtime`。
  - `node --test --test-name-pattern "dispatches iOS Simulator runtime long press" Web/dev/iosSimulatorBridge.test.mjs` 通过，证明 dev bridge 能把 Simulator UDID 映射到 runtime target。
  - `npm run build`、`swift build`、`swift build --package-path CLI --scratch-path .build/cli -c debug --product triton`、`git diff --check` 均通过。
  - 通过 `triton xcode run` 将 Overloaded 重新 build/install/launch 到 simulator `1B360513-22E7-46DB-A942-198EE522C6DC`，launch PID `29174`。
  - 实测 `curl /web/host-input?...source=runtime` 长按 `_UITabButton` 返回 `ok=true`、`message=Submitted UIControl long press touch events`、`strategy=control-long-press-touch-events`。
- 已知阻塞：`swift test --filter TKRuntimeInputActionsTests` 会编译整个 root test target，当前被既有 `Tests/TritonKitTests/TKDisplayItemTests.swift` 与 `Tests/TritonKitSharedTests/TKXcodeWorkflowModelsTests.swift` 编译错误阻塞；该阻塞不是本轮 input 修复引入。
- 模式沉淀：Web canvas 可以采集人类手势，但手势路由必须根据 host/runtime 能力边界选择事实入口；host-side 已明确 unsupported 的手势不应在 Web 层继续伪装成 host 能力，应该转交 embedded runtime 并保留公开 API 边界错误。
