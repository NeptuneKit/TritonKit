# 2026-06-21

## VLM test runner P2B/P3 exported flow rerun gate + map inspection

- P1-P2 App Map Test Path Graph 当前裁决为 `pass-with-gap`：产品主链成立，唯一 gap 是 exported flow 尚需真实 re-run smoke。
- 本轮按用户要求大步推进并集中测试：先补 P2B path health 回灌，再补 P3 App Map inspection 命令，最后统一跑自动化与真实 fixture smoke。
- P2B 代码 gate：`TKAppMapPath` 新增 `sourceRuns`，同一路径 merge 新成功 run 会增加 path `observedRuns/passCount`，同 run id 不重复计数；failure evidence 不生成成功 path，也不污染已有成功 path health。
- P3 新增 offline/read-only 命令：`triton map screens`、`map transitions`、`map path show`、`map health`、`map suite inspect`。
- P3 目标是让 agent/CLI 结构化读取 screen、transition、path、suite、health 和 coverage gap，不新增 UI、VLM、AI、selector healing、HTML/JUnit 或 runner primitives。
- 新增计划文档：`docs-linhay/spaces/20260620-vlm-test-runner/plans/p2b-p3-exported-flow-rerun-and-map-inspection.md`。
- 真实 P2B smoke 已补齐：启动 `triton serve`，用 `triton xcode run` 启动 `TritonKitTestFixture` 到 simulator `0333546D-2AC6-4C22-AF01-293E2F4BA5BC`，执行导出的 `generated-p2b-fixture-login-home.tritontest.yaml`，生成 `evidence/20260621-p2b-rerun.tritonevidence`，project-workspace 后 merge 回 `.tritonmap-p2b`。
- P2B smoke 结果：同一 path 未重复，`map inspect` 为 `observedRuns=2/passCount=2`，`map paths` 中 `path-fixture-login-home` 的 path health 也为 `observedRuns=2/passCount=2`；再 merge failure evidence 后 map `failCount=1`，成功 path health 保持 `failCount=0`。

## VLM test runner P4 mock VLM grounding contract

- 按用户要求继续大步推进，新增离线 `triton vlm ground --provider mock`，不接远端模型、不接 API key、不触发设备动作、不改 runner execution。
- 新增共享输出模型 `TKVLMGroundResponse`，包含 image metadata、coordinate contract reference、normalized point、runtime-point、coordinate transform 和 overlay/request/response artifacts。
- 新增 CLI runtime：mock provider 只返回 `normalized_0_1000`；CLI 读取 P0E `coordinate-contract.json`，fail-closed 转换到 canonical `runtime-point`，越界点和不支持坐标空间返回机器可读 `vlm_*` 错误。
- 新增 artifacts：`vlm-request.redacted.json`、`vlm-response.json`、`vlm-overlay.png`；overlay 标出 target、normalized/runtime point、image sha prefix、crosshair 和 circle。
- schema/capability 已补齐：`triton schema` 暴露 `vlm` command，`triton capabilities` 暴露 `vlm-ground-mock`，并同步 artifact/output/recovery taxonomy。
- 真实 evidence smoke 使用 `docs-linhay/spaces/20260620-vlm-test-runner/evidence/20260621-p2b-rerun.tritonevidence/debug/step-003-before.png` 与 `coordinate-contract.json`，输出到 `docs-linhay/spaces/20260620-vlm-test-runner/evidence/20260621-vlm-mock-grounding/`，结果 `runtimePoint=(201, 289.5)`。
- 新增计划文档：`docs-linhay/spaces/20260620-vlm-test-runner/plans/p4-mock-vlm-grounding-contract.md`。
- 验证通过：`VLMGroundingTests` 4 个、`SchemaFactSourceTests` 108 个、`AppMapPathGraphTests` 5 个、`ScreenWorkspaceProjectionTests` 3 个、`TestRunExecutionTests` 3 个，以及真实 `triton vlm ground --provider mock` CLI smoke。

## VLM test runner P5 OpenAI-compatible grounding

- 继续推进到 P5：`triton vlm ground --provider openai-compatible` 已实现，但仍不接 runner execution、不触发 device operation。
- 新增 CLI flags：`--base-url`、`--model`、`--api-key-env`、`--allow-remote-vlm`。默认只允许 localhost / loopback provider，远端 provider 未显式授权时返回 `vlm_remote_provider_requires_approval`。
- OpenAI-compatible provider 调用 `/chat/completions`；请求向 provider 发送 target prompt 与 screenshot data URL，但 redacted request artifact 只保存 image metadata、target、model、baseURL、redaction/network，不保存 base64 screenshot。
- strict parser 支持 `(x,y)` 与 JSON point：顶层 `x/y`、`point`、`normalized`、`normalizedPoint`，统一转换为 `normalized_0_1000 -> runtime-point`。
- 新增 failure codes：`vlm_openai_base_url_required`、`vlm_openai_base_url_invalid`、`vlm_remote_provider_requires_approval`、`vlm_api_key_missing`、`vlm_provider_request_invalid`、`vlm_provider_request_failed`、`vlm_provider_response_invalid`、`vlm_provider_point_parse_failed`。
- 真实 CLI smoke 使用本机临时 fixture server `127.0.0.1:19455` 返回 Chat Completions-compatible JSON，生成 `docs-linhay/spaces/20260620-vlm-test-runner/evidence/20260621-vlm-openai-compatible-grounding/`，结果 `runtimePoint=(201, 289.5)`。
- 新增计划文档：`docs-linhay/spaces/20260620-vlm-test-runner/plans/p5-openai-compatible-grounding.md`。
- 验证通过：`VLMGroundingTests` 8 个、`SchemaFactSourceTests` 108 个，以及 localhost fixture server CLI smoke。

## VLM test runner P6 deterministic runner primitives

- 按用户“大步推进，最后集体测试”的节奏，先扩展 `triton test run` deterministic primitive 范围，暂不进入 VLM runner execution、suite runner 或报告生成。
- validate/normalize 新增支持：`stop`、`input.text`、`press.button`、`swipe.from/to`、`assertNotVisible.text`、`scrollUntilVisible.text`，并继续支持既有 `launch`、`takeScreenshot`、`tap.point`、`assertVisible.text`。
- runner executor 新增 deterministic action 执行：`input`、`press`、`swipe` 复用 runtime input primitive，并像 `tap` 一样写 before/after `observation.captured`；`scrollUntilVisible` 采用 bounded deterministic swipes。
- `assertNotVisible` 使用 AX exact text observation 的负向判断；unsupported step 仍在 validation 阶段失败，不创建 evidence、不触发 primitive executor。
- `stop` 进入 normalize 合同，但当前 live embedded-runtime executor 返回 `stop_not_supported`，不伪装成 host-side app terminate；后续应在 runner-owned launch/readiness 阶段接入 host app terminate target selection。
- schema/capability 同步：保留 `test-run-minimal` 兼容 capability，新增 `test-run-deterministic`；normalized plan contract 增加 `endPoint`、`text`、`button`、`direction`、`maxScrolls`。
- 新增计划文档：`docs-linhay/spaces/20260620-vlm-test-runner/plans/p6-deterministic-runner-primitives.md`。
- 已跑 targeted checks：`swift test --package-path CLI --filter TestValidationTests --filter TestRunExecutionTests` 通过；全量/集体验证按当前大切片结束后统一执行。

## VLM test runner P7 VLM-assisted tap(target)

- 继续按“大步推进，最后集体测试”的节奏推进到 runner/VLM 接合点，但保持边界克制：只实现 gated `tap(target)`，不做 AI assertion、VLM action provider、selector healing、suite runner、HTML/JUnit 或 autonomous loop。
- validate/normalize 新增 `tap.target + grounding: vlm + provider: mock/openai-compatible`；`tap.text`、id/index/relationship selector 仍按 unsupported/validation error 处理。
- `triton test run` 新增 `--allow-vlm`、`--allow-remote-vlm`、`--vlm-base-url`、`--vlm-model`、`--vlm-api-key-env`；缺少 `--allow-vlm` 时 live executor 在 runtime resolution 前返回 `vlm_step_not_allowed`，不触发 device operation。
- runner VLM step 复用 P0B validate/normalize 与 P4/P5 `groundVLMTarget`，执行链路为 before observation -> grounding -> runtime-point tap -> after observation -> `vlm.grounding` event。
- `.tritonevidence/run/events.jsonl` 新增 `vlm.grounding` event，引用 `vlm-overlay.png`、`vlm-request.redacted.json`、`vlm-response.json`；event parser 对 provider/target 做非空校验。
- `.tritonmap` path 新增 `source` 字段；包含 `vlm.grounding` 的 evidence merge 后同一路径标记为 `vlm-assisted`，deterministic path 保持 `deterministic`，重跑不生成 duplicate path。
- 新增计划文档：`docs-linhay/spaces/20260620-vlm-test-runner/plans/p7-vlm-assisted-tap-target.md`。
- 已跑 targeted checks：`swift test --package-path CLI --filter TestValidationTests --filter TestRunExecutionTests --filter RunEventWriterTests --filter ScreenWorkspaceProjectionTests --filter AppMapPathGraphTests` 通过，27 tests。

## VLM test runner P8 path confirmation / suite semantics

- 继续推进到 P8：App Map path 从“run 自动生成的观察资产”升级为“需要确认后才可进入 suite 的测试计划资产”。
- `triton map merge ...` 默认只生成 candidate path；`--confirm` 仍保留为快速闭环入口，会确认 path 并加入 smoke suite。
- 新增 CLI：`triton map path confirm/unconfirm`，以及 `triton map suite add-path/remove-path`。
- `suite add-path` 要求 path 已 confirmed 且 replayable；unconfirmed path 返回 `unconfirmed_path`，non-replayable path 返回 `non_replayable_path`。
- `path unconfirm` 会把该 path 从所有 suites 移除，保证 unconfirmed path 不参与 suite export/run。
- `.tritonmap/suites/*.json` path 列表保持 sorted + unique，后续 suite runner 可直接消费。
- schema/capability 同步：新增 `app-map-path-confirm`、`app-map-suite-edit`，并补 recovery taxonomy。
- 新增计划文档：`docs-linhay/spaces/20260620-vlm-test-runner/plans/p8-path-confirmation-suite-semantics.md`。
- 已跑 targeted checks：`swift test --package-path CLI --filter AppMapPathGraphTests --filter SchemaFactSourceTests --filter SchemaFactSourceCapabilityTests --filter SchemaFactSourceWorkflowTests` 通过，115 tests。

## VLM test runner P10 suite runner

- 继续按“大步推进，最后集体测试”的节奏推进到 P10，但仍不扩 runner primitive、不做 HTTP wrapper、不做 reports。
- 新增 `triton map suite run <dir.tritonmap> --suite <suite-id> --target <target> --evidence-root <dir> --json`，消费 P8 confirmed + replayable suite path。
- suite runner 链路为：export-flow -> triton test run -> new .tritonevidence -> project-workspace -> merge back into same .tritonmap -> health backfill。
- 新增 `TKAppMapSuiteRunResponse` / `TKAppMapSuiteRunPathResult`，输出 per-path flow、evidenceDir、runId、status、failure 与 suite pass/fail 计数。
- suite policy `stopOnFailure=true` 时首个失败 path 后停止；unconfirmed / non-replayable path 仍按 P8 gate 拒绝，不绕过确认语义。
- schema/capability 已同步 `app-map-suite-run` capability 与 `app-map.suite-run` output contract。
- 新增计划文档：`docs-linhay/spaces/20260620-vlm-test-runner/plans/p10-suite-runner.md`。
- 已跑 targeted checks：`swift test --package-path CLI --filter AppMapPathGraphTests` 通过，`swift test --package-path CLI --filter SchemaFactSourceTests --filter SchemaFactSourceCapabilityTests --filter SchemaFactSourceTaxonomies --filter SchemaFactSourceWorkflowTests` 通过，108 tests。
- 集体验证通过：`swift test --package-path CLI` 通过 387 tests，schema/capability CLI smoke 可见 `app-map-suite-run`，`git diff --check` 通过，`docs-linhay/scripts/check-docs.sh` 通过。
- 真实 iOS Simulator P10 pass smoke 已生成 `docs-linhay/spaces/20260620-vlm-test-runner/evidence/20260621-p10-suite-run.tritonevidence-root/001-path-fixture-login-home.tritonevidence`，run id 为 `run-1f45eb68-cfb9-4a0b-b204-58e481bb8049`。
- P10 pass smoke merge 后 `.tritonmap` health 为 `observedRuns=2/passCount=2/failCount=0`，同一 path 没有 duplicate；path health 同为 `observedRuns=2/passCount=2`。
- failure smoke 使用 Home 起点重复执行 suite，返回 exit code 1、`ok=false`、`failedCount=1`、`stoppedOnFailure=true`、`failure.type=assert_visible_failed`；该临时 failure run 不保留到长期 `.tritonmap`，避免 `/tmp` evidence 引用污染持久地图。

## VLM test runner P11 HTTP API thin wrapper

- 新增现有 `triton serve` 下的本机 HTTP App Map route：`/v1/app-map/inspect`、`/paths`、`/screens`、`/transitions`、`/path`、`/health`、`/suite`、`/suite/run`。
- HTTP route 是 CLI/core thin wrapper：read-only route 直接返回现有 App Map response model，suite run route 复用 `runTritonAppMapSuite`。
- POST body 映射 P10 suite runner 参数：`map`、`suite`、`evidenceRoot`、`target`、`host`、`port`、VLM passthrough flags。
- invalid query/body 返回 HTTP 400 `invalid_payload`；App Map runtime failure 返回 HTTP 409；suite run failure 返回 `triton.app-map.suite-run-result` 且 `ok=false`。
- 新增计划文档：`docs-linhay/spaces/20260620-vlm-test-runner/plans/p11-http-api-thin-wrapper.md`。
- 已跑 live HTTP smoke：`GET /v1/app-map/inspect`、`paths`、`suite` 均返回正确 JSON；缺少 `map` 返回 HTTP 400；空 POST suite run 返回 HTTP 400。

## VLM test runner P12 tap(text)

- 新增 deterministic runner primitive：`tap.text` with `source=ax` and `match=exact`。
- validate / normalize 将 text tap 写入 normalized plan selector；point tap 和 VLM tap 语义保持不变。
- live runner 只使用 AX exact text target resolution，不做 hierarchy fallback、selector healing、OCR 或 VLM fallback。
- text target 找不到时返回 `text_not_found` 并携带 selector；input 失败返回 `tap_failed`。
- schema test command 文案已更新，`tap(text/source=ax/match=exact)` 进入 deterministic run contract。
- 新增计划文档：`docs-linhay/spaces/20260620-vlm-test-runner/plans/p12-more-runner-primitives.md`。
- 已跑 targeted checks：`swift test --package-path CLI --filter TestValidationTests --filter TestRunExecutionTests` 通过 17 tests。

## VLM test runner P13 JSON test report

- 新增 `triton test report <dir.tritonevidence> --json`，作为纯离线 evidence projection，不触发 simulator、runtime 或 App Map mutation。
- report 读取 `manifest.json`、`run/run.json` 与 `run/events.jsonl`，输出 `triton.test.report`。
- report 聚合 per-step command/status/assertion/failure/artifact refs/observation/screenCandidate/VLM grounding，并统计 event、step、assertion、artifact、observation、failure、screenshot、overlay 数量。
- 失败 evidence 也返回 `ok=true` 的报告，测试结果由 `summary.status` 与 `failure` 表达，方便 CI/agent 检查失败上下文。
- schema/capability 同步新增 `test.report` output contract 与 `test-report` capability。
- 新增计划文档：`docs-linhay/spaces/20260620-vlm-test-runner/plans/p13-json-test-report.md`。
- 已跑 targeted checks：`swift test --package-path CLI --filter TestRunExecutionTests`、`swift test --package-path CLI --filter SchemaFactSourceWorkflowTests` 通过。
- 已跑真实 evidence smoke：`CLI/.build/debug/triton test report docs-linhay/spaces/20260620-vlm-test-runner/evidence/20260621-p10-suite-run.tritonevidence-root/001-path-fixture-login-home.tritonevidence --json`，输出 `status=passed`、`stepCount=5`、`observationCount=3`、`screenshotCount=3`。

## VLM test runner P14 mock AI assertions

- 新增 mock AI evidence contract：`assertWithAI`、`assertNoDefectsWithAI`、`extractTextWithAI`、`assertScreenshot`。
- validate / normalize 新增 AI prompt/provider 与 screenshot baseline/threshold/cropOn 字段；P14 provider 只允许 `mock`，不触发远端模型调用。
- AI steps 默认 `optional=true`，`assertScreenshot` 默认 blocking；runner 现在尊重 optional step failure，记录 `failure.recorded` 但继续后续 required steps。
- mock AI runner step 捕获 observation 并写 `ai.assertion` / `ai.extraction` artifact；`extractTextWithAI` 基于 captured AX visibleTexts 输出提取内容。
- `assertScreenshot` 当前执行严格 SHA256 baseline 对比，写 `screenshot.diff` JSON artifact；像素 threshold diff 仍未实现。
- schema/capability 同步新增 `test-run-ai-mock` capability，并扩展 normalized plan output contract。
- 新增计划文档：`docs-linhay/spaces/20260620-vlm-test-runner/plans/p14-mock-ai-assertions.md`。
- 已跑 targeted checks：`swift test --package-path CLI --filter TestValidationTests --filter TestRunExecutionTests --filter SchemaFactSourceWorkflowTests` 通过 34 tests。

## VLM test runner P15 action provider parser

- 新增 `triton action parse --provider <ui-tars|agentcpm-gui> --input <raw> --json`，离线解析外部 GUI agent 单步 action，不执行设备动作。
- UI-TARS parser 支持 `click`、`swipe`、`type`、`press`、`wait`、`finished/status/done`，输出 bounded primitive preview。
- AgentCPM-GUI JSON parser 支持 `POINT/CLICK/TAP`、`SWIPE`、`TYPE`、`PRESS`、`WAIT`、`STATUS/DONE`。
- 坐标动作统一记录 `coordinateSystem=normalized_0_1000`，当前只输出 `commandPreview`，不做截图坐标 transform。
- schema/capability 同步新增 root command `action`、output selector `action.provider.parse`、capability `action-provider-parse`。
- 新增计划文档：`docs-linhay/spaces/20260620-vlm-test-runner/plans/p15-action-provider-parser.md`。
- 已跑 targeted checks：`swift test --package-path CLI --filter ActionProviderParserTests --filter SchemaFactSourceWorkflowTests` 通过 19 tests。
- 已跑 CLI smoke：UI-TARS click 与 AgentCPM-GUI TYPE 均返回 `triton.action-provider.parse-result`。

## VLM test runner P16 static App Map viewer

- 新增 `triton map viewer <dir.tritonmap> --output <file.html> --json`，离线导出静态 HTML，不启动 Web/Wails/HTTP server，不执行测试，不修改 `.tritonmap`。
- 新增 `triton test create --from-session <dir.tritonevidence> --output <path.tritontest.yaml> --json`，从现有 runner evidence 的 `normalized-plan.json` 导出可编辑 `.tritontest.yaml` 草稿。
- viewer 消费现有 App Map runtime projections：inspect、screens、transitions、paths。
- session-to-test 消费现有 P0D/P10 runner evidence：`manifest.json` 与 `normalized-plan.json`，导出后立即复用 P0B validate 自检。
- HTML 包含 counts / pass-fail summary、paths、screens、transitions tables。
- schema/capability 同步新增 `app-map.viewer` / `test.create` output contracts 与 `app-map-viewer` / `test-create-from-session` capabilities。
- 新增计划文档：`docs-linhay/spaces/20260620-vlm-test-runner/plans/p16-static-app-map-viewer.md`。
- 已跑 targeted checks：`swift test --package-path CLI --filter AppMapPathGraphTests --filter SchemaFactSourceWorkflowTests` 通过 23 tests。
- 已跑 targeted checks：`swift test --package-path CLI --filter TestCreateFromSessionTests --filter TestValidationTests --filter SchemaFactSourceWorkflowTests` 通过 27 tests。
- 已跑 CLI smoke：`CLI/.build/debug/triton map viewer docs-linhay/spaces/20260620-vlm-test-runner/.tritonmap --output /tmp/triton-app-map-viewer.html --json` 返回 `screenCount=4/transitionCount=2/pathCount=1`。
- 已跑 CLI smoke：`CLI/.build/debug/triton test create --from-session docs-linhay/spaces/20260620-vlm-test-runner/evidence/20260621-p10-suite-run.tritonevidence-root/001-path-fixture-login-home.tritonevidence --output /tmp/generated-from-session.tritontest.yaml --json` 返回 `ok=true/stepCount=5/source=normalized-plan.json`。
- 已跑 CLI validate smoke：`CLI/.build/debug/triton test validate /tmp/generated-from-session.tritontest.yaml --json` 返回 `ok=true`。
- 已生成 smoke artifact：`docs-linhay/spaces/20260620-vlm-test-runner/app-map-viewer.html`。
- 已生成 smoke artifact：`docs-linhay/spaces/20260620-vlm-test-runner/generated-from-session.tritontest.yaml`。
- 最后集体验证已跑：`swift test --package-path CLI` 通过 400 tests；`triton schema --command test` smoke 能发现 `test-create-from-session/test.create`；`triton capabilities` smoke 能发现 `test-create-from-session/app-map-viewer/action-provider-parse`；`git diff --check` 与 `docs-linhay/scripts/check-docs.sh` 均通过。
- 集体验证期间修正 schema taxonomy / capability governance：新增 action root recovery category、`test-create-result` / `test-report` / `app-map-viewer-result` / `action-provider-parse-result` kinds、`app-map-viewer-html` 与 `tritontest-yaml` evidence/artifact taxonomy、`ai_` failure recovery mapping。

## VLM test runner final hardening gate

- 功能面已冻结，新增最终交付报告：`docs-linhay/spaces/20260620-vlm-test-runner/plans/release-gate-report.md`。
- 工作区审计分类为 shared models、CLI commands/runtimes、tests、docs/plans/memory、samples/generated artifacts、evidence bundles。
- Artifact 策略：小型 `app-map-viewer.html`、`generated-from-session.tritontest.yaml`、`.tritonmap` 可作为样本；大型/截图型 evidence 不得混入 core runtime commits；临时 final gate 输出仅保留在 `/tmp`。
- 最终 runtime smoke 使用 fixture target `triton:ios-simulator:0333546D-2AC6-4C22-AF01-293E2F4BA5BC`，输出根目录 `/tmp/triton-final-gate.JgcLsJ`。
- Final smoke 链路通过：validate -> test run -> evidence project-workspace -> map merge -> map inspect -> export-flow -> validate exported flow -> map viewer -> test create --from-session -> action parse -> exported flow re-run -> project-workspace -> merge back -> inspect。
- Final smoke 回灌结果：同一 map path `observedRuns=2`、`passCount=2`、`failCount=0`。
- 已知 gap：真实外部 remote VLM 未跑；suite runner 真实 smoke 仍是单 path；HTML viewer readonly；action provider parse-only；launch 不保证 screen reset，re-run 前需要显式复位 fixture。
