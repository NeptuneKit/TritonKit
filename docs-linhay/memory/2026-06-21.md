# 2026-06-21

## issue #67：agent UX evidence 追加小切片

- 本轮在 `feat/20260619-issue-67-agent-ux-evidence-xcode` 上追加两个低风险 UX 输出增强：`xcode status --json` 从最近 `/tmp/triton-xcode-artifacts` 目录暴露 `stdoutLogPath`、`stderrLogPath`、`lastOutputAt`、`stdoutBytes`、`stderrBytes`；host app action JSON 顶层新增 `suggestedCommands[]`，与 `hostAction.nextAction` 保持一致。
- 边界：继续不做 `record-video` / `--burst`，因为它们会引入长进程与视频采集；`triton device/sim screenshot --auto-suffix` / `--overwrite` 安全策略、DerivedData cleanup 仍留给后续独立切片。
- 验证：先补红灯测试确认缺字段，再实现；通过 focused `XcodeDiagnosticsTests|AppOpenURLFlowTests|SchemaFactSourceSurfaceContractTests`。
