# 2026-06-22

## Xcode 线上 issue #82 / #83

- 处理 GitHub issue #82：`triton xcode status` 不再把无关 SwiftPM `swift-build --package-path ...` provider 构建计入 Xcode workflow active；保留 `SwiftBuildService` / `XCBBuildService` / `xcodebuild` / `xctest` 作为 Xcode lane blocker。既有 `wait-idle` transient probe timeout 语义通过 focused test 保持。
- 处理 GitHub issue #83：`triton xcode settings/build/test/run --json` 在 stderr 输出 progress JSONL，stdout 仍保留最终 JSON envelope；timeout summary 包含 stdout/stderr artifact path，避免 agent wrapper 长构建期间看到空输出并误判 143。
- 验证：`swift test --package-path CLI --scratch-path .build/cli-issue82-focused --filter XcodeDiagnosticsTests`、`swift test --package-path CLI --scratch-path .build/cli-issue83-focused --filter XcodeCommandTests` 均通过。
- 文档同步：更新 `docs-linhay/spaces/20260520-xcode-workflow-takeover/README.md` 与 `.agents/skills/tritonkit-xcode-workflow-takeover/SKILL.md`，新增 issue space `20260622-issue-82-xcode-wait-idle` / `20260622-issue-83-xcode-build-timeout-output`。

## 0.2.1 release preflight

- 版本收口：将 `TritonKit.podspec`、`TritonKitShared.podspec`、`Web/package.json`、`Web/package-lock.json` 从 `0.2.0` 提升到 `0.2.1`，用于承载 issue #82 / #83 修复。
- Release 契约验证：`docs-linhay/scripts/verify-release-package-versions.sh 0.2.1`、`docs-linhay/scripts/verify-release-automation.sh`、`git diff --check`、`docs-linhay/scripts/check-docs.sh` 均通过。
- Web / Pod 验证：`npm --prefix Web ci`、`npm --prefix Web run build`、`pod lib lint TritonKitShared.podspec --allow-warnings --skip-tests`、`pod lib lint TritonKit.podspec --include-podspecs=TritonKitShared.podspec --allow-warnings --skip-tests` 均通过；pod lint 仅有 Xcode build setting note / search path warning。

## Issue #85：Harmony DevEco device/build workflow 缺口

- 线上 issue #85 仍为 OPEN，问题集中在 Harmony / DevEco workflow：首次协议阻塞被报告为 started=true、Harmony stop 失败 hint 落到 iOS/simctl、build harmony 不支持真实 DevEco 项目验证过的 node hvigor.js assembleApp --no-daemon -p product=default -p buildMode=debug。
- 新增 space：docs-linhay/spaces/20260622-issue-85-harmony-deveco-workflows/README.md，记录 BDD 场景、范围与验收标准；本轮只处理本机 Harmony Emulator / DevEco CLI 合同，不扩展真机、远端 agent、设备云或 Web/Wails UI。
- triton build harmony 新增 --node、--java-home、--deveco-sdk-home、--product、--task、--no-daemon；默认旧命令仍保持 hvigorw|hvigor --mode <module>@<mode> assembleHap，显式 DevEco 参数时切到 node <hvigor.js> assembleApp --no-daemon -p product=<name> -p buildMode=<mode> 并向 build process 注入 JAVA_HOME、DEVECO_SDK_HOME 与 PATH=<JAVA_HOME>/bin:$PATH。
- triton device start --platform harmony 对显式 stdout/stderr log 进行短窗口检测，识别 DevEco 首次协议确认提示后返回 emulator_license_agreement_required，避免继续输出可执行的 started=true。
- triton device stop --platform harmony 的 launchctl / DevEco Emulator stop failure 映射为 harmony_emulator_stop_failed，恢复提示改为 Harmony HVD、DevEco Emulator、launchd label/domain 与 --skip-launchd 排查路径。
- triton schema --command build --json 已暴露新增 Harmony build 选项与 DevEco assembleApp 示例；triton schema --command device --json 已暴露 emulator_license_agreement_required 与 harmony_emulator_stop_failed，保持 agent-facing schema 可发现。
- 验证：swift test --package-path CLI --scratch-path .build/issue-85-tests --filter BuildRunnerTests 通过；swift test --package-path CLI --scratch-path .build/issue-85-tests --filter DeviceCrossPlatformTests 通过；swift test --package-path CLI --scratch-path .build/issue-85-tests --filter BuildRuntimeTests 通过；swift test --package-path CLI --scratch-path .build/issue-85-tests --filter SchemaFactSourceTests 通过；.build/issue-85-tests/arm64-apple-macosx/debug/triton schema --command build --json 与 schema --command device --json smoke 通过；swift build --package-path CLI --scratch-path .build/issue-85-tests --product triton 通过。

## Issue #86：Xcode Swift macro malformed response 诊断

- 线上 issue #86 仍为 OPEN，问题集中在真实 iOS 项目 triton xcode run 的 Swift macro target 构建失败：NavigatorMacros 在 repo-local .triton/DerivedData 下 produced malformed response，同项目通过既有 Xcode build path 可成功，成功 .app 经 Triton install/launch 可通过。
- 新增 space：docs-linhay/spaces/20260622-issue-86-xcode-macro-deriveddata/README.md，记录 BDD 场景、范围与验收标准；本轮只处理 Triton Xcode build diagnostics 与 agent-facing schema 指引，不扩展 Xcode IDE bridge、真机、远端 agent 或 Web/Wails UI。
- 扩展 XcodeBuildOutputDiagnosticsParser：识别 external macro implementation type ... could not be found for macro ... 与 produced malformed response，输出 swift-macro-plugin-malformed-response、macro executable path samples、原始错误行、recovery 与 triton xcode build --derived-data-path <fresh-derived-data-path> --jsonl next action。
- 更新 triton schema --command xcode --json：--derived-data-path 描述、xcode.final.xcodeDiagnostics 描述、failureShape 与 failureCodes 均暴露 Swift macro malformed-response 恢复语义，保持 agent 可机器发现。
- 普通 xcodebuild 失败仍保持 xcodebuild_failed；Swift macro malformed response 输出专用 failureCode=swift_macro_plugin_malformed_response；既有 stale DerivedData outside-root 诊断不变。Triton 不自动删除 .triton/DerivedData，避免破坏增量缓存策略。

## triton update CLI 自更新命令

- 新增 space：`docs-linhay/spaces/20260622-cli-update-command/README.md`，记录 `triton update` 的 BDD 场景、范围、非目标和验收标准。
- 新增 `triton update` root subcommand，支持 `--check`、`--dry-run`、`--yes`、`--version`、`--include-skills`、`--skills-dir`、`--json`，并通过 `triton schema --command update --json` 暴露机器可读契约。
- 安装来源边界：Homebrew 来源只走 `brew update` / `brew upgrade neptunekit/tap/triton`，不直接覆盖 Cellar binary；手动 tarball 来源下载 release asset 与 `tritonkit_checksums.txt`，校验 SHA-256 后替换当前 binary；source checkout build 返回 unsupported 并提示本地 SwiftPM build。
- Public update skill 已同步优先使用 `triton update --check/--dry-run/--yes --json`，skills bundle 可通过 `triton update --include-skills --skills-dir <dir> --yes --json` 同步。
- 验证：`swift test --package-path CLI --scratch-path .build/cli --filter UpdateCommandTests` 通过；真实 CLI smoke `triton update --check --version v0.1.1 --current-executable /usr/local/bin/triton --json` 与 `triton schema --command update --json` 通过。

## Overloaded iOS real-device runtime 联调

- 用户说明当前启动项目为 `/Users/linhey/Desktop/FlowUp-Libs/Overloaded-v2/iOS/Overloaded`；排查确认 Overloaded 已有 `TritonKitDebugBootstrap.start()` 且在 `AppDelegate.didFinishLaunching` Debug 路径调用。
- 根因链路：Mac host 能发现真机 `ios-real:7a9d976cc4d4`，但 Web 初始只能看到 simulator runtime；真机页面返回 `app_runtime_unavailable` 是正确保护，不能 fallback 到 simulator runtime。
- 服务端修复：Web dev bridge 管理的 `triton serve` 默认绑定从 localhost-only 改为 `0.0.0.0`，但浏览器侧仍使用 `127.0.0.1:19421`；这是 iOS 真机 runtime 能连接 Mac 的必要条件。
- App 侧联调：Overloaded Debug build setting 更新为当前可达 `TRITONKIT_DEFAULT_HOST = 192.168.228.128`，Release 未改；本轮实际通过 `devicectl device process launch --environment-variables '{"TRITON_HOST":"192.168.228.128","TRITON_PORT":"19421"}' overloaded.cn.debug` 绕过重编并注入 runtime host。
- 排障过程：旧 IP `192.168.10.179` 随网络变化失效，`curl http://192.168.10.179:19421/status` timeout；重新用 `ifconfig` / `route get default` 找到当前候选 IP，并用 `curl http://192.168.228.128:19421/status` 证明可达后，真机 runtime 才连入。
- Xcode 构建阻塞：`triton xcode run` 在外部工程 SwiftPM 解析阶段失败过一次，错误为 Sentry 9.18.0 binary artifact cache `already exists in file system`；已把冲突 artifact 移到 `~/Library/Caches/org.swift.swiftpm/artifacts-backup-20260622023313/`。后续优先用 devicectl 启动已安装 App 验证 runtime，而不是等待完整重编。
- 当前验证结果：`triton list --json` 出现真机 runtime `triton:connection:2`（无 `simulatorUDID`）；`/web/host-screenshot?...target=ios-real%3A7a9d976cc4d4...` 返回 200，来源命令为 `triton screenshot --target triton:connection:2 ... --json`，尺寸 `428x926`；`/web/host-hierarchy?...target=ios-real%3A7a9d976cc4d4...` 返回 200，root `ios:runtime:2`，节点数 118。
- 兼容修复：当前 CLI 的 hierarchy 能力在 `triton debug hierarchy` 下，Web bridge 已从顶层 `triton hierarchy` 切到 `triton debug hierarchy`；`npm run test` 70 pass / 4 skipped，`npm run build` 通过，`git diff --check` 与 `docs-linhay/scripts/check-docs.sh` 通过。

## Issue #82：xcode wait-idle 与 SwiftPM provider build 误判

- 新增 space：`docs-linhay/spaces/20260622-issue-82-xcode-wait-idle/README.md`，记录线上 issue #82 的 BDD 场景与验收标准。
- `triton xcode status` 的进程分类收窄：直接 SwiftPM provider 构建（如 `swift-build --package-path Tools/TritonMLXProvider -c release --product triton-mlx-provider`）不再计入 Xcode build-service active；`SwiftBuildService` / `XCBBuildService` 仍保留为 Xcode build-service 观测。
- `wait-idle` 既有 transient host probe timeout 回归测试保留，确保单次内部进程探测 timeout 不直接覆盖整体等待语义。
- 验证：swift test --package-path CLI --scratch-path .build/issue-xcode-tests --filter XcodeDiagnosticsTests 通过 12 tests。
- 实际 CLI smoke：.build/issue-xcode-tests/arm64-apple-macosx/debug/triton xcode status --workspace /Users/linhey/Desktop/linhay-open-sources/TritonKit --json 返回 active=false、processes=[]、summary.buildServiceCount=0，证明当前仓库 lane 不被其它项目 xcodebuild 污染。
- 线上状态核对：GitHub issue #82 当前为 CLOSED；origin/main 已包含 Merge feat/20260622-issue-82-xcode-wait-idle，v0.2.1 release 已发布。

## Issue #83：xcode build timeout / 143 空输出

- 新增 space：docs-linhay/spaces/20260622-issue-83-xcode-build-timeout-output/README.md，记录线上 issue #83 的 BDD 场景与验收标准。
- runXcodeBuild 现在把用户传入的 --timeout 传递到 host command，并在 xcodebuild timeout 时返回 TKXcodeActionSummary，避免 HostCommandRunError.timeout 穿透成空输出；summary 固定包含 failureCode=xcodebuild_timeout、failureKind=timeout、timeoutSeconds、timeoutSource、stdout/stderr artifact 路径与恢复提示。
- runXcodeBuild 对 exit 143 / SIGTERM 风格终止补充稳定 summary 字段：failureKind=terminated、terminationSignal=sigterm，避免 agent wrapper 侧只观察到 {exit_code:143, output:""}。
- triton schema --command xcode --json 的 xcode final output contract 已暴露 failureKind、timeoutSeconds、timeoutSource、terminationSignal，并将 xcodebuild_timeout 纳入 xcode build failure codes，保持 agent-facing 契约可发现。
- 验证：swift test --package-path CLI --scratch-path .build/issue-xcode-tests --filter XcodeCommandTests 通过 8 tests；swift test --package-path CLI --scratch-path .build/issue-xcode-tests --filter XcodeDiagnosticsTests 通过 12 tests；swift test --package-path CLI --scratch-path .build/issue-xcode-tests --filter SchemaFactSourceTests 通过 113 tests；.build/issue-xcode-tests/arm64-apple-macosx/debug/triton schema --command xcode --json 通过，实际输出包含 xcodebuild_timeout 与新增 summary 字段。
- 显式 CLI build smoke：swift build --package-path CLI --scratch-path .build/issue-xcode-tests --product triton 通过；schema 抽检确认 command/subcommand 均包含 xcodebuild_timeout，xcode.final 包含 failureKind、timeoutSeconds、timeoutSource、terminationSignal。
- 线上状态核对：GitHub issue #83 当前为 CLOSED；origin/main 已包含 Merge feat/20260622-issue-83-xcode-build-timeout-output 与 Prepare release 0.2.1；v0.2.1 release 已发布，资产包含 triton-macos-arm64.tar.gz、triton-macos-x86_64.tar.gz、tritonkit-skills.tar.gz 与 tritonkit_checksums.txt。

## Issue #85：Harmony DevEco device/build workflow 缺口

- 线上 issue #85 仍为 OPEN，问题集中在 Harmony / DevEco workflow：首次协议阻塞被报告为 `started=true`、Harmony stop 失败 hint 落到 iOS/simctl、`build harmony` 不支持真实 DevEco 项目验证过的 `node hvigor.js assembleApp --no-daemon -p product=default -p buildMode=debug`。
- 新增 space：`docs-linhay/spaces/20260622-issue-85-harmony-deveco-workflows/README.md`，记录 BDD 场景、范围与验收标准；本轮只处理本机 Harmony Emulator / DevEco CLI 合同，不扩展真机、远端 agent、设备云或 Web/Wails UI。
- `triton build harmony` 新增 `--node`、`--java-home`、`--deveco-sdk-home`、`--product`、`--task`、`--no-daemon`；默认旧命令仍保持 `hvigorw|hvigor --mode <module>@<mode> assembleHap`，显式 DevEco 参数时切到 `node <hvigor.js> assembleApp --no-daemon -p product=<name> -p buildMode=<mode>` 并向 build process 注入 `JAVA_HOME`、`DEVECO_SDK_HOME` 与 `PATH=<JAVA_HOME>/bin:$PATH`。
- `triton device start --platform harmony` 对显式 stdout/stderr log 进行短窗口检测，识别 DevEco 首次协议确认提示后返回 `emulator_license_agreement_required`，避免继续输出可执行的 `started=true`。
- `triton device stop --platform harmony` 的 launchctl / DevEco Emulator stop failure 映射为 `harmony_emulator_stop_failed`，恢复提示改为 Harmony HVD、DevEco Emulator、launchd label/domain 与 `--skip-launchd` 排查路径。
- `triton schema --command build --json` 已暴露新增 Harmony build 选项与 DevEco assembleApp 示例；`triton schema --command device --json` 已暴露 `emulator_license_agreement_required` 与 `harmony_emulator_stop_failed`，保持 agent-facing schema 可发现。
- 验证：`swift test --package-path CLI --scratch-path .build/issue-85-tests --filter BuildRunnerTests` 通过 6 tests；`swift test --package-path CLI --scratch-path .build/issue-85-tests --filter DeviceCrossPlatformTests` 通过 84 tests；`swift test --package-path CLI --scratch-path .build/issue-85-tests --filter BuildRuntimeTests` 通过 4 tests；`swift test --package-path CLI --scratch-path .build/issue-85-tests --filter SchemaFactSourceTests` 通过 113 tests；`.build/issue-85-tests/arm64-apple-macosx/debug/triton schema --command build --json` 与 `schema --command device --json` smoke 通过；`swift build --package-path CLI --scratch-path .build/issue-85-tests --product triton` 通过。

## Test Recorder Replay P0 合同面

- replay dry-run 与 replay-result 已新增 contractRef：记录 compiled-contract.json 的 path、byteCount、digestAlgorithm=fnv1a64 与 deterministic digest；本机 CLI smoke 已验证 dry-run、replay-result 和 evidence bundle 内 run/replay-result.json 的 contractRef 完全一致。
- replay evidence events 已同步 contractRef：run/events.jsonl 中 started / page / network / step / finished 每条事件都写入同一 contractRef；本机 CLI smoke 已验证事件数等于 evidenceSummary.expectedEventCount，且每条事件的 contractRef 都等于 replay-result.contractRef。
- evidence manifest artifacts 已同步 contractRef metadata：manifest.json 中 replay-result / events / run 三个 artifact 都带同一 contractRef；本机 CLI smoke 已验证 manifest artifacts、run/events.jsonl 和 replay-result 的 contractRef 完全一致。
- testrec command schema 已补 artifacts：tritontestcase、contract-capabilities、compiled-contract、action-map、page-map、network-map、network-fixture、compile-proposals、evidence-bundle 均可从 schema 发现；schema smoke 已验证 command 和 start / compile / replay 子命令 artifact metadata。
- replay dry-run 已补 executorProfiles[] 合同：local-simulated=available，local-device=unsupported，真实设备回放缺失的 live-target-device / device-action-execution / evidence-artifact-capture / network-policy-application 被作为 requirement 暴露；已用 focused tests、schema tests 和真实 CLI smoke 验证 executorProfiles[]=local-simulated,local-device。
- testrec schema output contract 已固定 executor requirement status taxonomy：satisfied / missing / optional / not-required / simulated / not-present / not-requested；dry-run executorProfiles 与 replay-result execution.executorRequirements 共用同一组状态词，focused tests 已覆盖字段描述枚举。
- testrec schema artifact taxonomy 已同步补齐 tritontestcase、contract-capabilities、compiled-contract、action-map、page-map、network-map、network-fixture、compile-proposals 与 evidence-bundle；`swift test --package-path CLI --filter SchemaFactSourceTests` 通过 113 tests，`swift build --package-path CLI --product triton` 通过。
- 新建并收敛 `docs-linhay/spaces/20260622-test-recorder-replay/`：产品方向冻结为“一端录制合同，多端证据化回放”，但不提前抽 Agent Runtime；Action Map / Page Map / Network Map 暂作为 Test Recorder 内部合同能力。
- P0.5 决策：首版 `.tritontestcase` 必须包含 `contract-capabilities.json`，用 action / page / network 三类能力表达未来多端 capability skip 和 unsupported 不算 pass 的合同基础。
- 已实现独立 CLI surface：`triton testrec start/event/stop/inspect/compile/proposals/replay`。当前最小合同要求 `manifest.json` 与 `contract-capabilities.json`；actions / assertions / network / page artifacts 先输出 presence report，不在 inspect 阶段强制存在。
- P1 源端录制 MVP 已落地为显式事件闭环：`testrec start` 初始化 `.tritontestcase` 与本机 session，`testrec event` 将 action / network / page event 写入对应 JSONL，`testrec stop` 汇总 artifacts；当前不做全局系统输入监听、不连接设备。
- 已补本机 HTTP 管理 API 显式事件与 path-based case route：`POST /v1/test-recorder/sessions`、`POST /v1/test-recorder/sessions/{sessionId}/events`、`POST /v1/test-recorder/sessions/{sessionId}/stop`、`POST /v1/test-recorder/cases/inspect`、`POST /v1/test-recorder/cases/compile`、`POST /v1/test-recorder/cases/proposals`、`POST /v1/test-recorder/cases/match-page`、`POST /v1/test-recorder/cases/replay-dry-run`，HTTP handler 与 CLI 共用同一 session/case/compile/page-match/replay dry-run 逻辑；当前只接收调用方显式提交的 action / network / page JSON、target-side fingerprint 或本机 `.tritontestcase` path，不做真实 listener、case registry 或 run registry。
- 原始 action 流兼容性补强：deterministic compiler 现在同时接受 action event 的 `kind` / `type` / `action` 字段，避免 Web/HTTP recorder 常见 `type:"tap"` 被编译为 unknown action。
- 工程治理：`CLITestRecorderModels.swift` 拆为 DTO/error envelope，新增 `CLITestRecorderRuntime.swift` 承载 session、HTTP handler、inspect、compile、proposals 与 replay dry-run runtime，避免新 testrec surface 继续堆成超过 1500 行的巨型文件。
- `compile` 当前是离线 deterministic compiler：复用 inspect 校验，统计 `actions.jsonl`、`network/capture.ndjson`、`pages/route-events.jsonl`、`pages/fingerprints.jsonl`，输出 `compiled` / `needs-input` / `needs-review` 状态与 warning codes；当输入完整时写入 `compiled-contract.json`，合同内记录 semantic actions、network requests、page routes、page fingerprints 和 `llmUsed=false` / `vlmUsed=false`，不调用模型、不执行设备。
- deterministic Action Map 已落地：compile 在存在 `actions.jsonl` 时写出 `actions/action-map.json`，为每个 source action 生成 semantic target、strategy、review / redaction flags 与 evidence；type action 的输入文本只进入 evidence，不覆盖目标 label，避免跨端 replay 误用输入值作为 selector。
- deterministic Network Map 已落地：compile 在存在 `network/capture.ndjson` 时写出 `network/map-rules.json`；普通业务请求生成 `mock-candidate` + `redactionRequired=true`，偶发/analytics 请求生成 `passthrough` + `nonBlocking=true`。当业务请求带 response body 时，会写出 `network/fixtures/<id>.json` 脱敏 fixture，并通过 rule.fixturePath 引用；`compiled-contract.json` 不编码原始 response body，真实 replay 仍不会执行 network policy。
- deterministic Page Map 已落地：compile 在存在 page route 或 fingerprint 时写出 `pages/page-map.json`，按 route / pageId 合并页面身份，记录 route source、fingerprint source、fingerprint hash 与 evidence 列表；该 artifact 供只读 Inspector / agent / replay plan 消费，不引入 Web 控制面。
- Page Fingerprint match policy 已进入 compiled contract：`pages.matchPolicy` 固定声明 `deterministic-fingerprint-matcher-v1`、`matched=0.82` / `assistedMatched=0.70` / `needsReview=0.55` 阈值、required element gate、冲突策略和 `llmDecisionAuthority=false`。LLM/VLM 仍只能产出结构化感知或 proposal，不能直接判 pass。
- `testrec match-page --json` 已落地：从 `compiled-contract.json` 查找 source page fingerprint，对 target-side candidate fingerprint 计算 deterministic `score`、`decision`、component evidence，并固定输出 `llmUsed=false` / `llmDecisionAuthority=false`；HTTP route `POST /v1/test-recorder/cases/match-page` 复用同一 runtime。当前只产出 page-level replay evidence，不调用真实 VLM/LLM，不执行设备回放。
- compile quality pass 已落地：deterministic 检测 `privacy_candidate`、`transient_network_request`、`weak_selector`、`fixed_wait`，同时输出 warnings 和 `compiledContract.qualityFindings[]`；`proposalKind` 预留给后续 LLM / VLM proposals，当前不调用模型、不自动改合同。
- `compile-proposals.jsonl` 已落地：当 quality findings 非空时，compile 写出 JSONL proposals，当前覆盖 `contract.redaction` / `contract.network` / `contract.selector` / `contract.wait`，统一 `status=proposed`；proposal 是审查输入，不自动修改 `compiled-contract.json`。
- `testrec proposals --json` 已落地：只读读取 `compile-proposals.jsonl`，返回 `proposalCount`、proposal 列表和 inspect 建议命令；当前没有 apply / approve / reject，避免候选建议绕过 schema / review gate 直接改写合同。
- `testrec inspect --json` 已新增 lifecycle summary：`raw -> needs-compile`、`compiled -> ready`、`proposed -> review-proposals`，用于 agent 判断 case 当前处于 raw / compiled / proposed 哪个阶段。
- `replay --dry-run` 当前是离线计划生成：要求显式 `--platform` 与 `--dry-run`，只从 `compiled-contract.json` 生成 `pageChecks[]` 与 `plannedSteps[]`；`pageChecks[]` 计划调用 `testrec match-page` 并要求后续 executor 提供 `<target-fingerprint-json>`，`plannedSteps[]` 生成 planned Triton action argv、workflow categories、expected artifacts、stop conditions 与 blockers；缺少 compiled contract 返回 `missing_compiled_contract`，非 dry-run 返回 `dry_run_required`，避免伪装真实设备执行或绕过 compiler。
- `replay --executor local-simulated --target-fingerprints-json <json> --evidence-dir <dir.tritonevidence>` 已落地为离线本机模拟 executor：复用 dry-run plan，输出 `triton.testrec.replay-result`、`execution`、`pageResults[]`、`networkResults[]` 与 `steps[]`；`execution` 固定声明 `mode=offline-simulated`、`requiresDevice=false`、`deviceCommandsExecuted=false`、`llmUsed=false`、`vlmUsed=false`、`networkPolicyMode=simulated-projection` / `not-present`，以及 step status taxonomy：`executed / failed / skipped / blocked / not-run / simulated-passed`，作为后续真实 executor 的 side-effect 边界合同。target-side fingerprints 支持单对象、数组或 `{pages:[...]}`，并复用 deterministic matcher 生成 `matched / assisted-matched / needs-review / not-matched`、`matchScore` 与 evidence；`not-matched` / `needs-review` 会阻断 action steps。Network Map 会被投影为 `simulated-mock-candidate` / `simulated-passthrough` 等网络结果，并在 `run/events.jsonl` 中写入 `testrec.replay.network` 事件；结果保留 strategy、`redactionRequired`、`nonBlocking` 与 source path evidence，保留 fixturePath；但当前不执行 fixture body 或真实 network policy。每个 step result 和 `testrec.replay.step` event 现在都带 `deviceCommandExecuted` / `artifactRefs` / `failure`，local-simulated 固定为 `false` / `[]` / `null`；后续真实 executor 必须用同一字段记录每步实际设备命令是否执行、实际 evidence artifact refs，以及 `failure{code,message,path,artifactRefs,recoveryCommands,retryable}`。传入 `--evidence-dir` 时写出 `manifest.json`、`run/replay-result.json`、`run/events.jsonl` 和 `run/run.json`，先固定 replay result / evidence bundle 合同。该 executor 不连接真实设备、不调用动作命令。
- replay evidence JSONL 已统一基础 event envelope：`testrec.replay.started/page/network/step/finished` 均输出 `schemaVersion`、`event`、`runID`、`timestamp`、`category`、`subjectID`、`status`、`artifactRefs` / `failureCode`（按事件类型可空）与 `evidence`；`category` 当前限定为 `run / page / network / step`。这让后续真实 executor 可以流式写入同一 `run/events.jsonl`，不需要为 page / network / step 另起事件结构。
- local-simulated replay 已将 Network Map 的 fixturePath 透出到 `networkResults[].fixturePath`、`networkResults[].artifactRefs` 和 `testrec.replay.network.artifactRefs`；当前仍只作为证据引用，不执行真实 network policy。
- 验证补充：真实 CLI smoke 已覆盖 `testrec compile -> replay --executor local-simulated --evidence-dir`，确认 `networkResults[0].fixturePath == network/fixtures/n1.json` 且 `run/events.jsonl` 的 `testrec.replay.network.artifactRefs` 指向同一 fixture。
- replay result 已新增 `evidenceSummary`：声明 `expectedEventCount`、page / network / step event count、`blockerCount` 与 `statusConsistent`；写入 evidence bundle 时，`manifest.json.run.eventCount` 与 `run/events.jsonl` 行数必须和 `expectedEventCount` 对齐，避免后续真实 executor 产生 “result passed 但 events / manifest blocked 或缺事件” 的矛盾证据。
- replay result 的 `execution.executorRequirements[]` 已拆出 executor capability requirements：当前 `local-simulated` 明确 `compiled-contract=satisfied`、`live-target-device=not-required`、`device-action-execution=not-required`、`network-policy-application=simulated`、`evidence-artifact-capture=satisfied/not-requested`；请求未实现的 `local-device` executor 会返回 `unsupported_replay_executor`，hint 中保留 `live-target-device`、`device-action-execution`、`evidence-artifact-capture` 和 `network-policy-application`，让 agent 知道真实设备回放还缺哪些能力。
- 已接入 schema / capabilities 事实源：`triton schema --command testrec --json` 暴露 `testrec.session-start`、`testrec.event`、`testrec.session-stop`、`testrec.inspect`、`testrec.compile`、`testrec.proposals`、`testrec.page-match`、`testrec.replay-dry-run` 与 `testrec.replay-result` output contract，capabilities matrix 暴露 `testrec-session-start` / `testrec-event-ingest` / `testrec-session-stop` / `testrec-inspect` / `testrec-compile` / `testrec-proposals-inspect` / `testrec-page-match` / `testrec-replay-dry-run` / `testrec-replay-local-simulated`，并归入 test workflow。
- 质量约束：`start/event/stop` 的 `suggestedCommands` 只指向当前已实现的 event / stop / inspect / compile；`inspect` 的 `suggestedCommands` 只指向当前已实现的 `triton schema --command testrec --json`；`compile` 的 `suggestedCommands` 只指向已实现的 `testrec inspect`；`proposals` 的 `suggestedCommands` 只指向已实现的 `testrec inspect`；`replay dry-run` 的 `suggestedCommands` 只指向已实现的 `testrec compile`。完整合同 compiler、LLM / VLM proposals、network map 生成与真实设备 `testrec replay` 未实现前不能作为机器可读建议命令暴露给 agent。
- 验证：`swift test --package-path CLI --filter TestRecorderContractTests` 通过，`swift test --package-path CLI --filter SchemaFactSourceTests` 通过，`testrec start/event/stop/inspect/compile` 真实 CLI smoke 通过，`testrec compile -> match-page` 真实 CLI smoke 通过，`testrec compile -> replay --dry-run` 真实 CLI smoke 验证 `pageChecks[]` 通过，`testrec compile -> replay --executor local-simulated --target-fingerprints-json <json> --evidence-dir <dir.tritonevidence>` 真实 CLI smoke 验证 target page `matched` / `matchScore=1` / `triton.testrec.replay-result` / `execution.mode=offline-simulated` / `execution.deviceCommandsExecuted=false` / `execution.networkPolicyMode=simulated-projection` / `execution.executorRequirements[]` / `execution.stepStatusTaxonomy=executed,failed,skipped,blocked,not-run,simulated-passed` / `evidenceSummary.expectedEventCount == manifest.run.eventCount == events.jsonl line count` / `steps[].deviceCommandExecuted=false` / `steps[].artifactRefs=[]` / `steps[].failure=null` / replay event envelope `timestamp/category/subjectID` / `networkResults[].strategy=mock-candidate` / `networkResults[].redactionRequired=true` / `testrec.replay.network` / `simulated-passed` / `no-device-command-executed` / unsupported `local-device` 返回 `unsupported_replay_executor` 且 hint 包含 `live-target-device` / `manifest.json` / `run/replay-result.json` / `run/events.jsonl` 通过，`testrec compile` 真实 CLI smoke 验证 `actions/action-map.json` 与 `pages/page-map.json` 通过，`/v1/test-recorder/cases/match-page` 真实 HTTP smoke 通过，`/v1/test-recorder/sessions -> events -> stop -> cases/inspect -> cases/compile -> cases/proposals -> cases/replay-dry-run` 真实 HTTP smoke 通过且 replay plan 为 `ready`，Network Map HTTP smoke 验证 `network/map-rules.json` 写出且策略为 `mock-candidate` / `passthrough`，早前 `testrec proposals` / `testrec replay --dry-run` smoke 通过，`docs-linhay/scripts/check-docs.sh` 与 `git diff --check` 通过。全量 `swift test --package-path CLI` 在当前机器上仍有既有并发敏感失败（DeviceCrossPlatform proxy serve、BuildRuntime fake runner、SimulatorAdvancedControls host command timeout），相关 suite 单独复测中 BuildRuntime 与 DeviceCrossPlatform 已通过，失败与 testrec 改动无直接交集。
