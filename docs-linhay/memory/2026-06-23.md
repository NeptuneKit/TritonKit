# 2026-06-23

## 线上 issue #89/#90/#91 处理

- 处理 GitHub open issues #89、#90、#91，执行工作区为 `../TritonKit-worktrees/20260623-open-issues-89-91/`，避免污染主仓库既有未提交改动。
- #89：当 runtime server 不可达且 agent 请求具体任务 plan 时，`plan --json` 保持 `mode=bootstrap` 与 `primaryNextAction=serve`，同时新增 `afterRecoverySteps[]` 保存恢复后要执行的原始任务步骤，避免 agent 丢失 open-url/wait/assert/evidence 目标。
- #90：补充 Harmony 回归测试，确认 `device list` 发出的 `harmony:<hdc-target>` id 可直接用于 `app install --device`，并解析为原始 HDC target 执行 `hdc -t <target> install -r <hap>`。
- #91：当前公开 `xcrun simctl io` 不提供稳定 tap primitive；因此默认 schema/capabilities/help 不再暴露 iOS simulator host tap，显式残留命令保持 unsupported 语义，不伪造可执行能力。
- 验证：已通过相关 Swift 测试集合 `SchemaFactSourceTests|SimulatorAdvancedControlsTests|DeviceCrossPlatformTests`，覆盖 216 个测试。
## Triton Web Auto Discovery space

- 新建 docs-linhay/spaces/20260623-triton-web-auto-discovery/，用于后续换会话实现 triton web 默认自动发现。
- 方向结论：triton web 本身应成为零配置入口；不应要求用户传 --real-device auto、手动配置 TRITON_HOST 或理解 --host 0.0.0.0。
- 真机默认 transport 优先级：USB tunnel > LAN / Bonjour > manual fallback。
- Web 只读消费 triton serve 的统一 target registry；真机 target 不允许 fallback 到 Simulator screenshot。
- 后续实现需先补 BDD / schema / model tests，再分切片实现 USB runtime tunnel、Bonjour fallback、registry merge 和 Web 四态展示。

## Triton Web Auto Discovery Phase 0/1

- 完成 contract-first 最小切片：新增 Shared target registry DTO，覆盖 host/runtime/mirror/diagnosis/nextAction 与真机自动发现错误码。
- triton web launch plan 新增 discovery 字段，默认 simulator=auto、realDevice=auto、transportPriority=usb/bonjour/manual、registry=serve-owned；triton web --json 输出机器可读 plan。
- triton web 新增 --simulator-only、--no-usb、--no-lan 作为 escape hatch。
- triton serve 新增 GET /web/target-registry，只读合并已有 host targets 与 runtime targets；iOS real-device runtime 缺失时返回 runtime_not_found + start_debug_app，不 fallback simulator。
- 已验证：TKWebTargetRegistryModelsTests、WebCommandTests、SingleDeviceWebPageTests；默认 CLI .build 曾因新增 Shared 源缓存陈旧失败，swift package --package-path CLI clean 后重编译仍耗时/卡住，最终有效验证使用 .build/cli-autodiscovery scratch path。

## Triton Web Auto Discovery 真机验证

- 源码版 .build/cli-autodiscovery/debug/triton 的 schema/web --json 已包含 discovery 与 targetRegistryEndpoint；PATH 上 /Users/linhey/.local/bin/triton 仍是旧版，不用于本次源码验证。
- triton device list --platform ios --scope all --json 发现两台 ready wired 真机：ios-real:7a9d976cc4d4、ios-real:73f725dfa795。
- 本地 triton serve 的 GET /web/target-registry 返回两台真机 mirror.state=runtime_not_found、nextAction.code=start_debug_app，同时 booted simulator 保持 ready；证明真机 host target 已自动进入 registry，且没有 fallback 到 simulator runtime。
- 当前阻塞真机截图的是 real-device Debug App runtime 未连接；后续应实现 USB tunnel/runtime listener，再验证 runtime.transport=usb 与 screenshot/hierarchy capability。
- 继续修复多真机防误绑：当前真实环境有两台 ready wired 真机，registry merge 已改为在多 ready real hosts 且 runtime 无法唯一关联时返回 ambiguous_runtime_target + select_runtime_target，不把一个 real runtime 自动绑定到多台 host。

## Triton Web Auto Discovery Phase 2

- Web 数据层已优先消费 /web/target-registry，并在 endpoint 不存在或不可用时回退 /web/host-targets；Vite dev bridge 同步增加 /web/target-registry 代理。
- registry host DTO 补充 target/name/runtime/scope/kind，Web 可直接从 registry 显示 target 名称、系统版本和 target selector。
- Web registry 映射覆盖 ready、host_offline、runtime_not_found、mirror_unavailable；runtime_not_found 真机可见但 canScreenshot=false，避免 fallback 到 simulator screenshot。
- wired iOS real-device registry entry 增加 transportDiagnostics；本机无 iproxy 时返回 ios_usb_tunnel_unavailable，主状态仍保留 runtime_not_found + start_debug_app。
- 已验证：node --test dev/targetRegistryClient.test.mjs、npm --prefix Web run build、npm --prefix Web test、TKWebTargetRegistryModelsTests、SingleDeviceWebPageTests、WebCommandTests。

## Triton Web Auto Discovery Phase 2 真机复验

- 使用源码版 triton 在 127.0.0.1:19431 临时启动 serve 复验 /web/target-registry；19421 当前为旧服务，返回 404，不用于本次新 endpoint 验证。
- 复验确认两台 booted simulator 为 mirror.state=ready；两台 wired 真机 ios-real:7a9d976cc4d4、ios-real:73f725dfa795 为 runtime_not_found + start_debug_app，且 host.name/runtime/target 已进入 registry。
- 本机未发现 iproxy；复验确认两台 wired 真机都带 transportDiagnostics[0].code=ios_usb_tunnel_unavailable。
- 复验时发现并修复 ready simulator 被错误标记为 runtime_not_found 的缺口；新增 SingleDeviceWebPageTests 覆盖“ready simulator independent from app runtime”。
- 临时 19431 serve 已停止。

## Triton Web Auto Discovery manual fallback

- embedded runtime 的 `TritonKitStartPayload.environment()` 增加 Info.plist / build setting fallback：`TritonKitDefaultHost`、`TritonKitDefaultPort`。
- 优先级现在是 `TRITON_HOST` / `TRITON_PORT` > bundle setting > Bonjour `_tritonkit-server._tcp.local` > `127.0.0.1:19421`。
- `triton serve` 发布 `_tritonkit-server._tcp.local`；`dns-sd -B _tritonkit-server._tcp local` 已在临时 19431 serve 上发现 `TritonKit` 服务。
- 这覆盖 USB 不可用时的 Bonjour fallback，但不替代后续 USB tunnel backend。
- 已验证：`swift test --filter TKPlatformFallbackTests/startPayloadReadsEnvironment`、`swift test --package-path CLI --scratch-path .build/cli-autodiscovery --filter WebCommandTests`、Bonjour `dns-sd` smoke。

## Triton Web Auto Discovery real-device run follow-up

- Demo App 已改为启动时使用 `TritonKitStartPayload.environment()` 自动解析 endpoint，并补 `NSBonjourServices`、`NSLocalNetworkUsageDescription`、`NSAppTransportSecurity.NSAllowsLocalNetworking`，避免真机明文本地 `ws://<Mac LAN IP>:<port>` 被 ATS 拦截。
- `triton xcode run --device` 已支持 iOS real-device launch env/args；实现用 `DEVICECTL_CHILD_*` 传给 `devicectl`，sourceCommand 输出 `<redacted>`，避免泄露 `TRITON_HOST/TRITON_PORT` 等值。
- 真机 smoke：源码版 `triton serve --host 0.0.0.0 --port 19431` 发布 Bonjour；`triton xcode run --device ios-real:73f725dfa795 --env TRITON_HOST=<Mac LAN IP> --env TRITON_PORT=19431 --jsonl` 成功 build/install/launch，launch sourceCommand 脱敏。
- 当前未达 runtime ready：`GET /status` 仍为 `connected=false`，registry 对 ready 真机保持 `runtime_not_found + start_debug_app`，没有 fallback 到 simulator；随后设备变为 `offline + ddi-missing`，后续 `triton xcode run` 正确返回 `target_not_found`。
- 已验证新增契约：`swift test --filter TKHostAdapterModelsTests`、`swift test --package-path CLI --scratch-path .build/cli-autodiscovery --filter AppOpenURLFlowTests --filter XcodeCommandTests`、`swift test --filter TKPlatformFallbackTests/startPayloadReadsEnvironment`、`swift test --filter TKWebTargetRegistryModelsTests`、`swift test --package-path CLI --scratch-path .build/cli-autodiscovery --filter WebCommandTests`、`swift test --package-path CLI --scratch-path .build/cli-autodiscovery --filter SingleDeviceWebPageTests`、`node --test dev/targetRegistryClient.test.mjs`、`npm run build`、`npm test`、`git diff --check`、`docs-linhay/scripts/check-docs.sh`。

## Triton Web Auto Discovery continuation

- 修复 embedded runtime endpoint 解析边界：`TritonKitStartPayload.environment()` 现在会忽略未展开的 Info.plist build setting 占位符，例如 `$(TRITONKIT_DEFAULT_HOST)` / `$(TRITONKIT_DEFAULT_PORT)`，并继续走 Bonjour fallback，避免 Debug App 误连占位符 host。
- 当前真机事实：`.build/cli-autodiscovery/debug/triton device list --platform ios --scope real --json` 显示所有 iOS real-device 均为 `offline + ddi-missing`，ready 真机数为 0；本轮无法继续验证 runtime ready、USB/Bonjour runtime transport、真机 screenshot/hierarchy。
- 临时源码版 `triton serve --host 0.0.0.0 --port 19431` 复验 `/status` 与 `/web/target-registry`，endpoint 返回合法 `web.target-registry` envelope，targets 为空与当前 host facts 一致；复验后已停止临时 serve。
- 复验 `triton web --json` 默认 discovery plan：simulator auto、realDevice auto、transportPriority usb/bonjour/manual、serve-owned registry endpoint。
- 已验证：先补失败测试后修复；`swift test --filter TKPlatformFallbackTests/startPayloadReadsEnvironment`、`swift test --filter TKWebTargetRegistryModelsTests`、`swift test --package-path CLI --scratch-path .build/cli-autodiscovery --filter WebCommandTests --filter SingleDeviceWebPageTests --filter XcodeCommandTests --filter AppOpenURLFlowTests`、`node --test dev/targetRegistryClient.test.mjs`（在 Web 目录）、`npm run build`、`npm test` 均通过。

## Triton Web Auto Discovery managed serve contract

- Web dev bridge 已有 managed `triton serve` 自动启动逻辑，默认绑定 `0.0.0.0`；本轮把该行为提升为 CLI 机器可读契约。
- `triton web --json` 的 `discovery.managedServeHost` 现在返回 `0.0.0.0`，launch `environment` 同步注入 `TRITONKIT_WEB_MANAGED_SERVE_HOST=0.0.0.0`，避免 agent 只看到 Web 绑定 `127.0.0.1` 而误判真机不可达。
- `web.launch-plan` output contract 与 web command schema successShape 已同步新增 `managedServeHost`。
- 已验证：先补 WebCommandTests 红灯；`swift test --package-path CLI --scratch-path .build/cli-autodiscovery --filter WebCommandTests` 通过；源码版 `.build/cli-autodiscovery/debug/triton web --json` 输出新字段和环境变量。

## Triton Web Auto Discovery target registry managed serve

- 修复 Web dev bridge `/web/target-registry` 路由：proxy registry 前先调用现有 `ensureTritonServe`，与 host input 共用 managed serve 机制。
- 影响：`triton web` 打开的 Web 页面读取 registry 时会先复用或自动启动绑定 `0.0.0.0` 的 `triton serve`，避免 serve 未起时直接 502 并退回旧 `/web/host-targets`。
- 已验证：新增 bridge route 测试覆盖 `/health` readiness probe 后再 proxy `/web/target-registry`；`node --test dev/iosSimulatorBridge.test.mjs` 通过。

## Triton Web Auto Discovery packaged registry endpoint

- packaged `triton web` server 新增 `GET /web/target-registry`，直接返回统一 `web.target-registry` DTO。
- packaged registry bridge 复用 `makeWebTargetRegistry(...)` 合并 host targets 与可用 runtime targets，减少发布版 Web 退回旧 `/web/host-targets` 的概率。
- 已验证：新增 WebCommandTests 覆盖 packaged registry bridge；`swift test --package-path CLI --scratch-path .build/cli-autodiscovery --filter WebCommandTests` 通过。

## Triton Web Auto Discovery real-device locked smoke

- 真机 `ios-real:73f725dfa795` 恢复 ready，transport 为 `localNetwork`；Mac LAN IP 为 `192.168.228.128`。
- 源码版 `triton serve --host 0.0.0.0 --port 19431` 可通过 `http://192.168.228.128:19431/status` 访问，并发布 Bonjour。
- `triton xcode run --device ios-real:73f725dfa795 --env TRITON_HOST=192.168.228.128 --env TRITON_PORT=19431 --jsonl` 完成 build/install，launch sourceCommand 脱敏 `DEVICECTL_CHILD_TRITON_HOST` / `DEVICECTL_CHILD_TRITON_PORT`。
- launch 被设备锁屏拒绝，CLI 返回机器可读 `device_locked`，原因是 `Unable to launch ... because the device was not, or could not be, unlocked`。
- registry 复验：`ios-real:73f725dfa795` 为 `host.ready=true`、`transport=localNetwork`、`mirror.state=runtime_not_found`、`nextAction=start_debug_app`，没有 fallback simulator；临时 19431 serve 已停止。

## Release 0.2.2 preparation

- 准备下一版 tag `v0.2.2`：同步 `TritonKit.podspec`、`TritonKitShared.podspec`、`Web/package.json`、`Web/package-lock.json` 到 `0.2.2`。
- release preflight 首次暴露 Demo App 顶层 `import TritonKit` 未包 `#if DEBUG`；已最小修复为 DEBUG-only import，并让 Release 下 endpoint helper 不引用 TritonKit 符号。
- 已验证：`docs-linhay/scripts/verify-release-package-versions.sh 0.2.2`、`docs-linhay/scripts/verify-release-automation.sh`、`git diff --check`、`docs-linhay/scripts/check-docs.sh`、`npm --prefix Web run build`、两个 `pod lib lint`、`TRITON_VERIFY_XCODE=0 docs-linhay/scripts/verify.sh --local` 均通过。

## triton update Homebrew latest fallback

- 修复 `triton update` 在 Homebrew 安装来源下仍先请求 GitHub `/releases/latest` 的顺序问题；当未显式传 `--version` 且不更新 skills 时，Homebrew 分支现在直接规划 `brew update` / `brew upgrade neptunekit/tap/triton`，不被 GitHub API rate limit 阻断。
- 保留手动 tarball / unknown 来源对 GitHub latest 的解析；`--include-skills` 仍需要 release tag，因此未指定版本时仍需解析 GitHub release。
- 已验证：`swift test --package-path CLI --scratch-path .build/cli --filter UpdateCommandTests`；源码版 `.build/cli/arm64-apple-macosx/debug/triton update --check --current-executable /opt/homebrew/Cellar/triton/0.2.2/bin/triton --json` 在 GitHub API 限流时仍输出 Homebrew plan。
