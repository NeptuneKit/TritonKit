# 2026-06-24

## GitHub open issues #95/#96/#97

- 新建 space：docs-linhay/spaces/20260624-open-issues-95-97/，统一处理 GitHub open issues #95、#96、#97，并准备下一 patch release v0.2.4。
- #95：修正 triton update 的 Homebrew check 语义。未解析到明确 targetVersion 时，updateAvailable 不再因存在 brew update/upgrade actions 而默认为 true；真实 Homebrew mutating update 仍允许执行 brew update / brew upgrade。
- #96：迁入 CocoaPods single-pod 安装方案。TritonKit.podspec 直接打包 Sources/TritonKit 与 Sources/TritonKitShared，并通过 TRITONKIT_COCOAPODS_SINGLE_POD 避免业务 App 显式添加 TritonKitShared；SwiftPM module 边界保持不变。
- #97：调整 swift_macro_plugin_malformed_response 的恢复建议。保留 failureCode，nextAction 改为稳定 project category 的 triton xcode status --json，recovery 文案明确 repeated fresh DerivedData 后应转向 macro plugin / Xcode plugin 诊断、保留 stdout/stderr artifacts，并可用已构建 .app 走 app install/launch fallback。
- 发版版本对齐：TritonKit.podspec、Web/package.json、Web/package-lock.json bump 到 0.2.4；CLI 版本仍由 tag release CI 注入，不手改 CLIBuildInfo。

## Test Recorder replay evidence contract

- 需求：Triton Test Recorder P0 先打通 `.tritontestcase`、`inspect`、`compile`、`replay` 的机器可读合同；继续保持 Runtime / Agent 抽象、真实设备执行和模型调用在 P0 外。
- 实施：`testrec compile` 为业务网络请求写出脱敏 `network/fixtures/<id>.json`，`compiled-contract.json` 不编码原始 `responseBody`；local-simulated replay 把 fixture 透出到 `networkResults[].fixturePath`、`networkResults[].artifactRefs` 和 `testrec.replay.network.artifactRefs`，请求 `evidenceDir` 时复制 fixture 到 evidence bundle 并在 manifest 中以 `testrec.network.fixture` 索引。
- 实施：local-simulated replay 在提供 target-side page fingerprints 且请求 `evidenceDir` 时写出 `pages/target-fingerprints.json`，manifest 以 `testrec.page.target-fingerprints` 索引；artifact 明确 `modelCallsExecuted=false`、`llmUsed=false`、`vlmUsed=false`，区分“消费调用方提供的 VLM-style 指纹”和“回放中现场调用模型”。
- 实施：`pageResults[].artifactRefs`、`networkResults[].artifactRefs`、`testrec.replay.page.artifactRefs` 与 `testrec.replay.network.artifactRefs` 已形成 result / events / manifest / artifact file 的证据链；HTTP `POST /v1/test-recorder/cases/replay` 与 CLI local-simulated replay 覆盖同一 evidence 行为。
- 实施：`evidenceSummary` 增加 `expectedEventCount`、page / network / step event count、`artifactRefCount`、page / network / step artifact ref count、`blockerCount` 与 `statusConsistent`，用于审计 replay result、events.jsonl 与 manifest 是否存在数量或状态矛盾。
- 实施：`testrec inspect` 的 artifacts[] 对已存在文件输出 `byteCount`、`digestAlgorithm=fnv1a64` 与 `digest`，不存在文件保持 identity 字段为空；agent 可以在 inspect 阶段判断合同文件、map 和 proposal 是否变化。
- 实施：`.tritonevidence/manifest.json.run.summary` 写入通用 evidence run 摘要，`runID` 与 `run/run.json` 对齐，`verdict` 映射为 `success / blocked`，`stepCount` 与 replay steps 对齐，`frictionCount` 与 blockers 对齐；CLI 与 HTTP replay 测试均校验该 parity。
- 实施：`testrec inspect` 的 artifacts 增加 present 文件身份信息：`byteCount`、`digestAlgorithm=fnv1a64` 与 `digest`；缺失 artifact 保持身份字段为空，便于 agent 判断合同文件是否真实存在且可追踪。
- 验证：inspect P0 coverage 继续补齐无效 `manifest.json` 的 `invalid_json` validation envelope，以及手写 `actions.jsonl` / `assertions.json` / `network/map-rules.json` / `pages/page-map.json` 的 artifact presence 与 identity；不新增 runtime 解析逻辑，避免绕过 compile/replay 合同边界。
- 实施：`manifest.json` 增加 `tritonKitVersion`、`capabilitiesRef=contract-capabilities.json`、`redactionStatus=pending` 与 `truncationStatus=not-truncated` 默认字段；decoder 兼容旧手写最小 manifest，session start 新建 case 会显式写出当前 CLI 版本、能力引用和这两个 P0 状态。
- 实施：compile 阶段对敏感 `type` / `paste` 输入只保留 review finding，不把原始输入写进 `compiled-contract.json.actions[].inputText`，dry-run replay argv 也不再携带原文。
- 实施：replay dry-run 对未处理的 `contract.redaction` finding 返回 `redaction_review_required` blocker，保持计划可审计但状态为 blocked，避免隐私 review 未完成的合同被当成 ready。
- 治理：将 compile quality / warning helper 从 `CLITestRecorderRuntime.swift` 拆到 `CLITestRecorderCompileQualityRuntime.swift`；将 test recorder proposals schema output contract 拆到 `CLISchemaTestRecorderOutputContracts.swift`，保持 `CLITestRecorderRuntime.swift` 与 `CLISchemaOutputContracts.swift` 均低于 1500 行治理阈值。
- 验证：Test Recorder patch 在隔离 worktree `/private/tmp/triton-testrec-origin-check` 内执行；重放证据合同切片通过 `swift test --package-path CLI --scratch-path /private/tmp/triton-testrec-origin-check-build2 --filter TestRecorderContractTests`、`swift test --package-path CLI --scratch-path /private/tmp/triton-testrec-origin-check-build2 --filter SchemaFactSourceTests`、`swift build --package-path CLI --scratch-path /private/tmp/triton-testrec-origin-check-build2 --product triton`、`git diff --check` 和 `docs-linhay/scripts/check-docs.sh`。
