# 2026-06-25

## Local verification isolation

- 修复 `docs-linhay/scripts/verify.sh --local` / `--ci-validate` 的 Swift tests 步骤：运行 `swift test` 时固定 `TRITON_HOST=127.0.0.1` / `TRITON_PORT=19421`，避免本机真机联调环境或 Bonjour 发现污染默认 endpoint 测试。
- `verify.sh` 现在导出 repo-local `CLANG_MODULE_CACHE_PATH=.build/clang-module-cache`，避免 sandbox / 受限机器上 Swift 写入 `~/.cache/clang/ModuleCache` 失败。
- `verify-spm-dependency-boundary.sh` 对只读 `swift package describe` 使用 `--disable-sandbox`，避免 SwiftPM manifest sandbox 在受限本地环境下失败。
- `verify.sh` 的 Swift tests 使用 `swift test --disable-sandbox`，保持测试逻辑不变，规避 Codex / sandboxed 本机环境中的 SwiftPM manifest sandbox 限制。

## Test Recorder matrix fan-out slice

- 在 `codex/20260625-testrec-matrix` / `../TritonKit-worktrees/20260625-testrec-matrix` 上补了 `triton testrec matrix` 离线多端矩阵合同。
- 范围保持收敛：默认 dry-run fan-out；`--executor local-simulated` 只复用现有离线 executor；不连接真实设备、不写 evidence bundle、不调用 LLM / VLM。
- 验证：`swift test --package-path CLI --scratch-path /private/tmp/triton-testrec-matrix-build --filter TestRecorderContractTests`、`swift test --package-path CLI --scratch-path /private/tmp/triton-testrec-matrix-build --filter SchemaFactSource` 均通过。

## Test Recorder HTTP matrix parity

- 在 `codex/20260625-testrec-http-matrix` / `../TritonKit-worktrees/20260625-testrec-http-matrix` 上补齐 `POST /v1/test-recorder/cases/matrix`，复用 CLI `matrixTritonTestCase`，保持 HTTP 管理 API 与 `triton testrec matrix` 同一离线 fan-out 合同。
- 范围保持 P0/P1 收敛：只接收 path、targets、可选 executor / targetFingerprints；不连接真实设备、不写 matrix evidence bundle、不调用 LLM / VLM。
- 验证：`git diff --check`、`docs-linhay/scripts/check-docs.sh`、`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-http-matrix-clang-cache4 swift test --package-path CLI --scratch-path /private/tmp/triton-testrec-http-matrix-build4 --filter TestRecorderContractTests` 通过。

## Test Recorder matrix per-target evidence

- 在 `codex/20260625-testrec-matrix-evidence` / `../TritonKit-worktrees/20260625-testrec-matrix-evidence` 上给 `triton testrec matrix --executor local-simulated` 增加可选 `--evidence-root`，复用现有单端 replay evidence writer，为每个 target 写独立 evidence bundle。
- 范围保持收敛：只增加 per-target evidenceDir，不新增聚合 manifest，不连接真实设备，不调用 LLM / VLM。
- 验证：`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-matrix-evidence-clang swift test --package-path CLI --scratch-path /private/tmp/triton-testrec-matrix-evidence-build --filter TestRecorderContractTests` 通过，35/35。

## Test Recorder evidence summary parity

- 在 `codex/20260625-testrec-evidence-summary` / `../TritonKit-worktrees/20260625-testrec-evidence-summary` 上补回归覆盖：`testrec replay --executor local-simulated --evidence-dir` 生成的 `.tritonevidence` 可被通用 `summarizeEvidenceBundle` / `triton evidence summary` 读取。
- 范围保持收敛：只证明现有通用 evidence summary 可消费 testrec evidence bundle；不新增 testrec 专用 summary 命令，不改 evidence schema，不引入 LLM / VLM。
- 验证：`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-evidence-summary-clang swift test --disable-sandbox --package-path CLI --scratch-path /private/tmp/triton-testrec-evidence-summary-build --filter TestRecorderContractTests` 通过，35/35。

## Test Recorder matrix evidence summary commands

- 在 `codex/20260625-testrec-matrix-summary-commands` / `../TritonKit-worktrees/20260625-testrec-matrix-summary-commands` 上补齐 matrix evidence 的 agent follow-up：当 `testrec matrix --executor local-simulated --evidence-root` 写出 per-target evidence bundle 时，`suggestedCommands[]` 会包含每个 evidenceDir 的 `triton evidence summary ... --json`。
- 范围保持收敛：不新增聚合 manifest，不新增 testrec 专用 summary 命令，只复用通用 evidence summary。
- 验证：`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-evidence-summary-clang swift test --disable-sandbox --disable-automatic-resolution --package-path CLI --scratch-path /private/tmp/triton-testrec-evidence-summary-build --filter TestRecorderContractTests` 通过，35/35。

## Test Recorder matrix per-target replay commands

- 在 `codex/20260625-testrec-matrix-target-command` / `../TritonKit-worktrees/20260625-testrec-matrix-target-command` 上补齐 matrix per-target `suggestedCommand`：dry-run target 会带 `--device`，local-simulated target 会带 `--device` 与 `--evidence-dir`，路径和 device selector 统一 shell quote。
- 范围保持收敛：只修复 agent follow-up 命令，不新增 executor、聚合 manifest 或 LLM / VLM 调用。
- 验证：`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-evidence-summary-clang swift test --disable-sandbox --disable-automatic-resolution --package-path CLI --scratch-path /private/tmp/triton-testrec-evidence-summary-build --filter TestRecorderContractTests` 通过，35/35。

## Test Recorder matrix redaction gate coverage

- 在 `codex/20260625-testrec-matrix-redaction-gate` / `../TritonKit-worktrees/20260625-testrec-matrix-redaction-gate` 上补回归覆盖：含 `contract.redaction` finding 的合同会在 `testrec matrix` dry-run 与 local-simulated matrix 中传播 `redaction_review_required` blocker，matrix 总状态保持 `blocked`。
- 范围保持收敛：只补测试和文档，不新增 proposal apply、不新增 LLM / VLM 调用。
- 验证：`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-evidence-summary-clang swift test --disable-sandbox --disable-automatic-resolution --package-path CLI --scratch-path /private/tmp/triton-testrec-evidence-summary-build --filter TestRecorderContractTests` 通过，35/35。
## Test Recorder space baseline refresh

- 按最新 AGENTS / ops governance 规则重新校准 `20260622-test-recorder-replay` space，确认主仓当前脏且 `main` 落后 `origin/main`，因此只在干净 worktree `codex/20260625-testrec-match-suggestions` 上整理文档。
- 新增 `plans/20260625-p0-contract-current-baseline-v02.md`，把当前 `origin/main@5761b754` 的 Test Recorder P0 合同面冻结为最新基线：`.tritontestcase`、inspect、compile、proposals、match-page、replay dry-run、local-simulated replay、matrix、per-target evidence、HTTP parity 和 redaction gate。
- 结论保持收敛：发现 Agent Runtime，但暂不实现 Runtime；Web 只读；LLM / VLM 只做未来 proposal / fingerprint 输入，不拥有 replay pass/fail 决策权。
## Test Recorder match-page review suggestions

- 在 `codex/20260625-testrec-match-suggestions` / `../TritonKit-worktrees/20260625-testrec-match-suggestions` 上补齐 match-page follow-up：page fingerprint match response 的 `suggestedCommands[]` 现在指向 inspect / proposals / compile，并 shell quote case path。
- 范围保持收敛：只改善 agent 下一步审查命令，不新增 LLM / VLM 调用，不让模型判定 pass / fail。
- 验证：`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-evidence-summary-clang swift test --disable-sandbox --disable-automatic-resolution --package-path CLI --scratch-path /private/tmp/triton-testrec-evidence-summary-build --filter TestRecorderContractTests` 通过，35/35；`git diff --check`、`docs-linhay/scripts/check-docs.sh` 通过。
## Test Recorder matrix unsupported capability gate

- 在 `codex/20260625-testrec-unsupported-matrix-gate` / `../TritonKit-worktrees/20260625-testrec-unsupported-matrix-gate` 上补 matrix 层回归：当 `contract-capabilities.json` 声明 unsupported action（如 `drag`）时，`testrec matrix` dry-run fan-out 保持 `blocked`，`readyCount=0`、`passedCount=0`，每个 target 都带 `unsupported_capability` blocker。
- 范围保持收敛：现有 runtime 已经通过 compile warning -> replay blocker 传播该状态，本切片只补测试和文档，不改执行逻辑。
- 验证：`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-evidence-summary-clang swift test --disable-sandbox --disable-automatic-resolution --package-path CLI --scratch-path /private/tmp/triton-testrec-evidence-summary-build --filter TestRecorderContractTests` 通过，36/36；`git diff --check`、`docs-linhay/scripts/check-docs.sh` 通过。
## Test Recorder HTTP match-page suggestions parity

- 在 `codex/20260625-testrec-http-match-suggestions` / `../TritonKit-worktrees/20260625-testrec-http-match-suggestions` 上补 HTTP parity 回归：`POST /v1/test-recorder/cases/match-page` 返回的 `suggestedCommands[]` 与 CLI match-page 保持一致，指向 inspect / proposals / compile。
- 范围保持收敛：handler 已复用同一 matcher response，本切片只补测试，不改 HTTP/runtime 逻辑。
- 验证：`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-evidence-summary-clang swift test --disable-sandbox --disable-automatic-resolution --package-path CLI --scratch-path /private/tmp/triton-testrec-evidence-summary-build --filter TestRecorderContractTests` 通过，36/36；`git diff --check`、`docs-linhay/scripts/check-docs.sh` 通过。
## Test Recorder HTTP matrix evidence-root parity

- 在 `codex/20260625-testrec-http-matrix-evidence` / `../TritonKit-worktrees/20260625-testrec-http-matrix-evidence` 上补 HTTP matrix parity 回归：`POST /v1/test-recorder/cases/matrix` 支持 `executor=local-simulated`、`evidenceRoot` 与 target fingerprints，并为每个 target 写出独立 evidence bundle。
- 范围保持收敛：handler 已复用 `matrixTritonTestCase`，本切片只补测试，不新增 aggregate manifest、不改 runtime 逻辑。
- 验证：`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-evidence-summary-clang swift test --disable-sandbox --disable-automatic-resolution --package-path CLI --scratch-path /private/tmp/triton-testrec-evidence-summary-build --filter TestRecorderContractTests` 通过，37/37；`git diff --check`、`docs-linhay/scripts/check-docs.sh` 通过。

## Test Recorder real executor design gate

- 在 `codex/20260625-testrec-real-executor-plan` / `../TritonKit-worktrees/20260625-testrec-real-executor-plan` 上新增 `plans/20260625-p1-real-executor-design-v01.md`，为未来 `testrec replay --executor local-device` 固定 target discovery、device action execution、evidence capture、network boundary 和 destructive policy。
- 范围保持收敛：只写设计门，不实现真实设备 executor，不抽 Runtime，不新增 Web/LLM/VLM 控制面。
- 验证：`git diff --check`、`docs-linhay/scripts/check-docs.sh` 通过；本次为 docs-only 设计计划，未运行 Swift 测试。

## Test Recorder local-device profile coverage

- 在 `codex/20260625-testrec-local-device-profile` / `../TritonKit-worktrees/20260625-testrec-local-device-profile` 上补回归覆盖：`testrec replay --dry-run` 返回的 `local-device` executor profile 必须完整声明 mode、nextCommand、required requirements、missing statuses 与 not-wired evidence。
- 范围保持收敛：现有 model 已经声明这些 fail-closed capability fields，本切片只锁定合同，不实现真实设备 executor、不新增 Runtime / Web / LLM / VLM。
- 验证：focused `replayDryRunCreatesPlanWithoutExecutingDeviceActions` 通过；`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-evidence-summary-clang swift test --disable-sandbox --disable-automatic-resolution --package-path CLI --scratch-path /private/tmp/triton-testrec-evidence-summary-build --filter TestRecorderContractTests` 通过，37/37。

## Test Recorder local-device target gate

- 在 `codex/20260625-testrec-local-device-target-gate` / `../TritonKit-worktrees/20260625-testrec-local-device-target-gate` 上补 P1 real executor 第二个切片：`local-device` 非 dry-run 先 fail-closed 返回 `target_not_found`，并证明不执行 device action。
- 范围保持收敛：不接真实 target discovery、不写 evidence bundle、不执行 `triton act`、不调用 LLM / VLM；matrix 仍只允许 `local-simulated`。
- 验证：先补测试得到红灯；focused `replayLocalDeviceBlocksWhenTargetIsNotResolved` 通过；`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-evidence-summary-clang swift test --disable-sandbox --disable-automatic-resolution --package-path CLI --scratch-path /private/tmp/triton-testrec-evidence-summary-build --filter TestRecorderContractTests` 通过，38/38。

## Test Recorder local-device blocked evidence

- 在 `codex/20260625-testrec-local-device-evidence` / `../TritonKit-worktrees/20260625-testrec-local-device-evidence` 上补 P1 real executor 第三个切片：`local-device` blocked replay 在传入 `evidenceDirectory` 时复用现有 `.tritonevidence` writer 写 manifest、run metadata、replay-result 和 events。
- 范围保持收敛：只写 blocked evidence bundle；不接真实 target discovery、不执行 device action、不新增 Runtime / Web / LLM / VLM。
- 验证：先补测试得到红灯；focused `replayLocalDeviceBlocksWhenTargetIsNotResolved` 通过；`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-evidence-summary-clang swift test --disable-sandbox --disable-automatic-resolution --package-path CLI --scratch-path /private/tmp/triton-testrec-evidence-summary-build --filter TestRecorderContractTests` 通过，38/38。
