# 2026-06-25

## Local verification isolation

- 修复 `docs-linhay/scripts/verify.sh --local` / `--ci-validate` 的 Swift tests 步骤：运行 `swift test` 时固定 `TRITON_HOST=127.0.0.1` / `TRITON_PORT=19421`，避免本机真机联调环境或 Bonjour 发现污染默认 endpoint 测试。
- `verify.sh` 现在导出 repo-local `CLANG_MODULE_CACHE_PATH=.build/clang-module-cache`，避免 sandbox / 受限机器上 Swift 写入 `~/.cache/clang/ModuleCache` 失败。
- `verify-spm-dependency-boundary.sh` 对只读 `swift package describe` 使用 `--disable-sandbox`，避免 SwiftPM manifest sandbox 在受限本地环境下失败。
- `verify.sh` 的 Swift tests 使用 `swift test --disable-sandbox`，保持测试逻辑不变，规避 Codex / sandboxed 本机环境中的 SwiftPM manifest sandbox 限制。

## Test Recorder matrix fan-out slice

- 在 `codex/20260625-testrec-matrix` / `../TritonKit-worktrees/20260625-testrec-matrix` 上补了 `triton testrec matrix` 离线多端矩阵合同。
- 范围保持收敛：默认 dry-run fan-out；`--executor local-simulated` 只复用现有离线 executor；不连接真实设备、不写 evidence bundle、不调用 LLM / VLM。
- 验证：`swift test --package-path CLI --scratch-path /private/tmp/triton-testrec-matrix-build --filter TestRecorderContractTests`、`swift test --package-path CLI --scratch-path /private/tmp/triton-testrec-matrix-build --filter SchemaFactSource` 均通过。

## Test Recorder HTTP matrix parity

- 在 `codex/20260625-testrec-http-matrix` / `../TritonKit-worktrees/20260625-testrec-http-matrix` 上补齐 `POST /v1/test-recorder/cases/matrix`，复用 CLI `matrixTritonTestCase`，保持 HTTP 管理 API 与 `triton testrec matrix` 同一离线 fan-out 合同。
- 范围保持 P0/P1 收敛：只接收 path、targets、可选 executor / targetFingerprints；不连接真实设备、不写 matrix evidence bundle、不调用 LLM / VLM。
- 验证：`git diff --check`、`docs-linhay/scripts/check-docs.sh`、`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-http-matrix-clang-cache4 swift test --package-path CLI --scratch-path /private/tmp/triton-testrec-http-matrix-build4 --filter TestRecorderContractTests` 通过。

## Test Recorder matrix per-target evidence

- 在 `codex/20260625-testrec-matrix-evidence` / `../TritonKit-worktrees/20260625-testrec-matrix-evidence` 上给 `triton testrec matrix --executor local-simulated` 增加可选 `--evidence-root`，复用现有单端 replay evidence writer，为每个 target 写独立 evidence bundle。
- 范围保持收敛：只增加 per-target evidenceDir，不新增聚合 manifest，不连接真实设备，不调用 LLM / VLM。
- 验证：`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-matrix-evidence-clang swift test --package-path CLI --scratch-path /private/tmp/triton-testrec-matrix-evidence-build --filter TestRecorderContractTests` 通过，35/35。

## Test Recorder evidence summary parity

- 在 `codex/20260625-testrec-evidence-summary` / `../TritonKit-worktrees/20260625-testrec-evidence-summary` 上补回归覆盖：`testrec replay --executor local-simulated --evidence-dir` 生成的 `.tritonevidence` 可被通用 `summarizeEvidenceBundle` / `triton evidence summary` 读取。
- 范围保持收敛：只证明现有通用 evidence summary 可消费 testrec evidence bundle；不新增 testrec 专用 summary 命令，不改 evidence schema，不引入 LLM / VLM。
- 验证：`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-evidence-summary-clang swift test --disable-sandbox --package-path CLI --scratch-path /private/tmp/triton-testrec-evidence-summary-build --filter TestRecorderContractTests` 通过，35/35。

## Test Recorder matrix evidence summary commands

- 在 `codex/20260625-testrec-matrix-summary-commands` / `../TritonKit-worktrees/20260625-testrec-matrix-summary-commands` 上补齐 matrix evidence 的 agent follow-up：当 `testrec matrix --executor local-simulated --evidence-root` 写出 per-target evidence bundle 时，`suggestedCommands[]` 会包含每个 evidenceDir 的 `triton evidence summary ... --json`。
- 范围保持收敛：不新增聚合 manifest，不新增 testrec 专用 summary 命令，只复用通用 evidence summary。
- 验证：`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-evidence-summary-clang swift test --disable-sandbox --disable-automatic-resolution --package-path CLI --scratch-path /private/tmp/triton-testrec-evidence-summary-build --filter TestRecorderContractTests` 通过，35/35。

## Test Recorder matrix per-target replay commands

- 在 `codex/20260625-testrec-matrix-target-command` / `../TritonKit-worktrees/20260625-testrec-matrix-target-command` 上补齐 matrix per-target `suggestedCommand`：dry-run target 会带 `--device`，local-simulated target 会带 `--device` 与 `--evidence-dir`，路径和 device selector 统一 shell quote。
- 范围保持收敛：只修复 agent follow-up 命令，不新增 executor、聚合 manifest 或 LLM / VLM 调用。
- 验证：`CLANG_MODULE_CACHE_PATH=/private/tmp/triton-testrec-evidence-summary-clang swift test --disable-sandbox --disable-automatic-resolution --package-path CLI --scratch-path /private/tmp/triton-testrec-evidence-summary-build --filter TestRecorderContractTests` 通过，35/35。
