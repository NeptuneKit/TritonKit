# 2026-06-26

## Web Inspect 节点命中与回归修复

- 修复 DeviceCanvas 在已选中父/祖先节点后再次点击同一区域无法下钻到子节点的问题：命中栈循环现在优先选择当前节点下最深的命中后代；点击不同分支时仍回到该点最深节点。
- 视图树缩进改为按 parentId 树结构重新计算，避免 runtime DTO 的 depth 异常导致树层级显示错位。
- 保持 live tap 边界：真实设备/模拟器 live 模式下，点击当前选中节点覆盖区域时优先切换层级选中；点击其它区域仍按 tap relay 发送到 host input。
- 同步修复 Web mock 回归锚点：目标切换改为轻量本地菜单，实时预览 fps 控件在首帧前也可调整，日志行恢复 .log-row 与原始 title，截图错误状态暴露 .real-screenshot-pending.is-error。
- 验证：
  - npm --prefix Web test
  - npm --prefix Web run build
  - git diff --check
  - antd usage Web/src --format json
  - antd lint Web/src --format json

## serve-sim 参考项目归档

- 按用户要求将 EvanBacon/serve-sim 浅克隆到 `docs-linhay/references/serve-sim/`，本地 HEAD 为 `f94d57c`；该目录受 `.gitignore` 的 `docs-linhay/references` 规则忽略，保持为本地参考源码快照。
- 新增本地参考说明 `docs-linhay/references/serve-sim.md`，记录可借鉴的 host-side Simulator streaming、normalized input、permission、camera injection、AX/log/evidence 和 agent skill 设计。
- 已把 serve-sim 作为下期候选需求补入 `docs-linhay/spaces/20260520-xcrun-host-adapter-research/plans/20260520-host-adapter-next-steps.md`：优先吸收 CLI/schema/evidence 契约，不复制 Web preview、`/.sim/exec`、远端 tunnel 或 npm runtime 依赖；camera injection 仅作为后续独立候选。
