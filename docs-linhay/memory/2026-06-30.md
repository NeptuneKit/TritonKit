# 2026-06-30

## Issue #120 update include-skills

- 处理 GitHub issue #120：`triton update --version vX.Y.Z --include-skills --skills-dir <dir> --yes --json` 在 CLI 已经是目标版本时不应被 `updateAvailable=false` 短路。
- 修复 `runCLIUpdate`：CLI binary 更新和 public skills bundle 安装分开执行、分开上报；仅 skills 安装成功时返回 `updated=false`、`skillsUpdated=true`。
- `--include-skills` 现在作为独立 destructive action 参与确认判断，未传 `--yes` 时 `requiresConfirmation=true`。
- 同步 `docs-linhay/dev/20260622-cli-update-command.md` 与 public `tritonkit-update` skill，明确 skills bundle 可独立于 CLI binary 更新。
- 验证通过：`swift test --package-path CLI --filter UpdateCommandTests --disable-sandbox --disable-automatic-resolution`；`git diff --check`；`docs-linhay/scripts/check-docs.sh`；`docs-linhay/scripts/verify.sh --local`。

## Web redesign space

- 新建 `docs-linhay/spaces/20260630-web-redesign/`，用于从头重设 TritonKit Web mock 页面。
- 已归档首张草图到 `screenshots/20260630/20260630-web-layout-sketch-baseline-v01.png`。
- 当前方案修正为单栏第一屏：实时/快照是默认主体；左侧视图树/运行动作、右侧属性/日志/网络通过两侧展开面板进入。
- 边界保持不变：Web 是 mock / observation surface，不直接成为业务控制入口；动作必须受 Triton CLI / HTTP contract 约束。
- 已重写根目录 `design.excalidraw` 为规范版：使用 Excalidraw diagram skill palette、clean line、默认单栏主视图与左右 dashed 展开入口，并归档 after 预览截图到 space screenshots。
- 修复 `.agents/skills/excalidraw-diagram-skill/references/render_template.html`：将 `https://esm.sh/@excalidraw/excalidraw?bundle` 固定为 `@0.18.0`，避开 `0.18.1` 拉取不存在的 `@braintree/sanitize-url@6.0.2/es2022/dist/constants.mjs`；原始 `render_excalidraw.py design.excalidraw` 已恢复可用。
