# 2026-07-01

## GitHub issues 120-122

- #120 已由现有 `UpdateCommandTests/includeSkillsInstallsBundleEvenWhenCLIIsAlreadyTargetVersion` 覆盖：CLI 已在当前版本相同时仍执行 skills bundle 安装，回归测试通过。
- #121 修复 embedded iOS tap 在 `TKRuntimeInputActions.targetActions(for:event:)` 中读取 `UIControl.allTargets` 的 Swift `Set` 强桥接风险：改为通过 Objective-C `perform(allTargets)` 取原始集合，并只归一化 `NSSet` / `NSArray` 与 `NSNull`。
- #122 为 Harmony `triton app install --platform harmony --app <signed.app>` 增加 signed `.app` archive 支持：HAP 继续走 `hdc install -r`，signed `.app` 走 `hdc install <path>`，同步 CLI help/schema 示例。
- 验证通过：`swift test --package-path CLI --filter DeviceCrossPlatformTests/hostAppInstallAcceptsHarmonySignedAppArchives --disable-sandbox --disable-automatic-resolution`；`swift test --package-path CLI --filter DeviceCrossPlatformTests/appAndSmokeSchemasExposeUnifiedDeviceSelector --disable-sandbox --disable-automatic-resolution`；`swift test --package-path CLI --filter 'DeviceCrossPlatformTests|FailureDiagnosticsTests|UpdateCommandTests' --disable-sandbox --disable-automatic-resolution`；`xcodebuild -scheme TritonKit -destination 'generic/platform=iOS Simulator' -derivedDataPath .build/xcode-ios-sim build`。

## v0.2.7 release prep

- 准备 `v0.2.7` patch release，发布面同步 `TritonKit.podspec`、`Web/package.json`、`Web/package-lock.json`。
- 发版前红灯确认：`docs-linhay/scripts/verify-release-package-versions.sh 0.2.7` 在 0.2.6 manifests 上失败。
- 已通过：`docs-linhay/scripts/verify-release-package-versions.sh 0.2.7`；`docs-linhay/scripts/verify-release-automation.sh`；`git diff --check`；`docs-linhay/scripts/check-docs.sh`；`npm --prefix Web run build`；`pod lib lint TritonKit.podspec --allow-warnings --skip-tests`。
