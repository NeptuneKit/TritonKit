# 2026-07-05

## Web mock AX 审查收口验证与测试基线修复

- 当前工作区继续推进 `20260703-host-simulator-ax-takeover` 的 Web mock
  验收补充：iOS hierarchy 请求使用 `source=runtime`，单 iOS runtime target
  且缺少 `simulatorUDID` 时允许 bridge 解析到该 runtime；`InspectorCard` 增加
  节点详情 modal；`StreamCard` / `InspectorCard` 共用有效可见性过滤，并支持已选
  overlay 再次点击下钻到更小后代节点。
- 补回已跟踪 Web 测试直接引用但当前 checkout 缺失的纯 helper：
  `hierarchyMaterialPolicy.ts`、`inspectorWorkspaceModel.ts`、
  `hostBridgePresentation.ts`、`HostBridgeNotice.ts`、`iosSimulatorClient.ts`、
  `mockData.ts`。这些文件只恢复测试契约和只读 DTO 处理，不新增 Web 业务控制闭环。
- 额外修复 `App` named export 与 `localStorage` capability guard，使 SSR / happy-dom
  测试环境不会因不完整 storage 对象崩溃；补齐旧 CSS contract 测试锁定的
  view-tree 和 real screenshot frame 规则。
- 已通过：`node --test dev/hierarchyMaterialPolicy.test.mjs
  dev/inspectorWorkspaceModel.test.mjs dev/targetRegistryClient.test.mjs
  dev/iosRuntimeMirrorBridge.test.mjs dev/hierarchyVisibility.test.mjs`、
  `npm exec -- tsc --noEmit`、`npm run build`、`antd usage Web/src --format json`、
  `antd lint Web/src --format json`、`docs-linhay/scripts/check-docs.sh`、
  `git diff --check`。
- `npm test` 已运行但仍未通过：剩余失败集中在 `dev/appFallbackDom.test.mjs`
  等旧整页 DOM 规格，等待 `.bridge-notice` / 旧 Inspect Session 页面结构出现；
  当前 `Web/` 已是 split-card mock，这类失败不能用本轮 AX 插槽最小改动修绿，
  后续需要单独决定是迁移旧 DOM 测试到当前界面，还是恢复对应旧入口。

## Web mock test script 收敛到当前界面

- 将 `Web/package.json` 的 `npm test` 从 `dev/*.test.mjs` glob 改为当前有效测试清单，
  排除已过期的旧整页 DOM 规格 `dev/appFallbackDom.test.mjs`；该旧规格保留为
  `npm run test:legacy-dom`，用于后续单独迁移或诊断。
- 新增 `dev/currentWebContracts.test.mjs` 承接旧 DOM 文件里仍有价值的纯契约断言：
  iOS fallback tabbar DTO、view-tree 长文本 CSS contract、portrait real screenshot
  frame 高度 contract。
- 验证：`npm test` 通过 51 项当前 Web mock 测试；`npm run build`、
  `antd usage Web/src --format json`、`antd lint Web/src --format json`、
  `docs-linhay/scripts/check-docs.sh`、`git diff --check` 均通过。

## Node resolve root workflow 与 schema 元数据修正

- 修复 schema 推荐 `triton node resolve ...` 但 CLI root 仍把 `node` 当退休命令拦截的问题：
  root command 重新注册 `node -> resolve` workflow，`debug node --oid` 继续保留为低层
  raw inspection 入口。
- 修复 `CLIHelpTests` 的二进制解析顺序：优先使用 `TRITON_CLI_PATH`、当前测试 bundle
  同目录和本轮 root scratch 产物，避免误拿 `CLI/.build/debug/triton` 历史缓存导致假红灯。
- 修正 `schema --command node --json` 的 P23 surface metadata：`node` 现在是
  `surfaceLayer=workflow`、`deprecatedForMainPath=false`，不再提示旧的 `act find`
  replacement；同时将 host capability 的 `schema` nextAction 纳入 capability taxonomy。
- 验证：`swift test --disable-sandbox --package-path CLI --scratch-path .build/cli-test --filter CLIHelpTests`、
  `swift test --disable-sandbox --package-path CLI --scratch-path .build/cli-test --filter SchemaFactSourceTests/schemaExamplesDoNotRecommendRetiredRootCommands`、
  `swift test --disable-sandbox --package-path CLI --scratch-path .build/cli-test --filter SchemaFactSourceTests/p23CommandSchemasExposeProductSurfaceMetadata`、
  `swift test --disable-sandbox --package-path CLI --scratch-path .build/cli-test --filter capabilityGroupStaysAlignedWithNextActionRootCommands`、
  `swift build --package-path CLI --scratch-path .build/cli --product triton` 均通过；
  smoke 确认 `.build/cli/arm64-apple-macosx/debug/triton node --help` 可用，且
  `schema --command node --json` 输出 `workflow / false`。
