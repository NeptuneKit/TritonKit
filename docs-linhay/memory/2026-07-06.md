# 2026-07-06

## Web Inspect Session / Slot 联动需求

- 用户要求不要只做最小联动修复，而是整理“插槽设计 + 界面与 AX 审查跟随刷新 + cell 命中”整体方案。已新建 `docs-linhay/spaces/20260706-web-inspect-session-slots/`。
- 需求边界明确为 Web mock 内部架构治理：Slot Shell 只管布局，Slot Binding 管 follow/pin，InspectTarget 管设备身份与 source，InspectSession 管 hierarchy / overlay / selected node / refresh state，derive / hit-test 管 View tree、AX tree、overlay nodes 与 cell 命中。
- 后续实现不应让 `StreamCard` 和 `InspectorCard` 各自拥有 hierarchy 事实；所有刷新必须走 `refreshInspectSession(targetKey, reason)`，iOS simulator/real-device 的 `source=host|runtime` 从 target descriptor 传递，不再由组件猜。
- 本轮已落地 Web inspect session 基础架构：新增 `Web/src/inspect/target.ts`、`sessionStore.ts`、`hierarchyQuery.ts`、`hierarchyDerive.ts`、`hitTest.ts`；`StreamCard` 接入 target/session/overlay/hit-test，`InspectorCard` 绑定 session 派生树和详情，View / AX tab 切换会同步 Stream overlay。
- 新增 `Web/dev/inspectModel.test.mjs` 覆盖 target source、requestSeq 防竞态、per-target selected node、pinned binding、cell grouping 与 AX hit-test。验证通过 `cd Web && npm test`、`cd Web && npm run build`、`swift build --package-path CLI --scratch-path .build/cli --product triton`、`docs-linhay/scripts/check-docs.sh && git diff --check`。
- Browser smoke 使用 `TRITONKIT_TRITON_BIN=.build/cli/debug/triton` 启动 34127，确认页面 LIVE / Inspector 就绪，Inspector 顶部显示“跟随”绑定控制，点击 `AX 树` tab 后 Stream overlay radio 同步为 `AX`，点击设备画面 overlay 后 Inspector 显示选中节点详情，新操作 console 无 error/warn。截图为 `docs-linhay/spaces/20260706-web-inspect-session-slots/screenshots/20260706-web-inspect-session-slots-browser-smoke-after-v01.png`（本地验收产物，仍被仓库级 `screenshots/` ignore）。

## Web / CLI 手势差异抹平

- 用户要求 Web / CLI 侧抹平三端手势差异。本轮先把已具备稳定 host adapter 路径的 `longPress` 收敛成三端一致：Web 对 iOS / Android / Harmony 都发送 `/web/host-input?...source=host`，不再把 iOS `longPress` 打到 runtime fallback。
- CLI iOS Web host adapter 现在支持 `TKInputRequest.longPress`：复用 Baguette host HID `swipe`，start/end 坐标相同，duration 默认 `0.65s`，继续走现有 framebuffer 坐标归一化。
- Android / Harmony 仍使用已有同点 swipe hold 路径。`pinch` / `rotate` / 多指路径不在本轮伪实现；Web target registry 新增统一 `inputCapabilities[]`，当前将 `tap` / `swipe` / `longPress` 标为 host supported，将 `pinch` / `rotate` / `multiTouchPath` 标为 host unsupported，Web 前端映射到 `DeviceTarget.inputCapabilities` 与 `canInput`。
- 验证通过：`cd Web && node --test dev/streamGestureModel.test.mjs`、`cd Web && node --test dev/targetRegistryClient.test.mjs`、`swift test --disable-sandbox --package-path CLI --scratch-path .build/cli-test --filter iOSWebHostLongPressUsesSamePointHostHIDSwipe`、`swift test --disable-sandbox --package-path CLI --scratch-path .build/cli-test --filter webTargetRegistryExposesUnifiedHostInputCapabilities`。
- Web 浏览器 smoke 补测时发现 dev bridge 若未指定 `TRITONKIT_TRITON_BIN` 会使用旧 `CLI/.build/debug/triton`，导致 registry 缺少本轮新字段；本轮用 `.build/cli/debug/triton` 重启 Vite 后确认 `/web/target-registry` 带 `inputCapabilities[]`。
- 同次浏览器 smoke 修复 Web hierarchy source 泄漏：`AppContext.fetchHierarchy` 不再对所有 iOS 强制 `source=runtime`，iOS simulator 默认 `source=host`，real-device 默认 `source=runtime`。新 tab 验证 `LIVE`、截图加载、审查状态就绪、实际点击显示 `tap 603,1313`，新 console 无 error/warn。截图归档在 `docs-linhay/spaces/20260706-web-stream-gesture-mapping/screenshots/20260706-web-stream-gesture-browser-smoke-after-v02.png`（本地验收产物，当前被仓库级 `screenshots/` ignore 规则忽略）。

## Web host target 可用性过滤

- 用户指出 Web 显示 `[IOS] TritonKit Dedicated iPhone 17`，但 Simulator.app 窗口并未打开；排查确认 CoreSimulator 后台设备 `0333546D-2AC6-4C22-AF01-293E2F4BA5BC` 处于 `Booted`，同时 `triton status --json` 为 `connected=false`、`targetCount=0`。
- 本轮收紧 `/web/host-targets` iOS simulator 暴露条件：单纯 `Booted` 不再算 Web 可用，只有存在连接中的 iOS App runtime 且能按 `simulatorUDID` 匹配，或唯一无 `simulatorUDID` runtime fallback 时，才把 iOS simulator 放进 Web target 列表。
- 当前验证 `/web/host-targets` 已不再返回 `TritonKit Dedicated iPhone 17`；Android / Harmony 仍按原 ready 规则显示。
- 验证通过：`node --test dev/iosSimulatorBridge.test.mjs dev/iosRuntimeMirrorBridge.test.mjs`、`npm run build`。

## Web bridge 文件拆分

- 用户追问 `Web/dev/iosSimulatorBridge.mjs` 是否需要拆分。本轮按低风险边界拆到 `Web/dev/ios-bridge/`：`hostTargets.mjs`、`process.mjs`、`hostLogs.mjs`、`runtimeMirror.mjs`、`hierarchy.mjs`、`index.mjs`；最终删除旧 `Web/dev/iosSimulatorBridge.mjs` 兼容入口。
- Vite 与测试导入路径已直接指向 `Web/dev/ios-bridge/index.mjs`。
- 验证通过：`npm test`、`npm run build`。

## `sim ax` 机器可读失败契约

- M3 iOS Host Adapter Boundary 继续推进：`triton sim ax --json` 不再只用
  `CleanExit.message` 输出人类文本失败，而是复用 `TKCLIErrorResponse`，并设置
  `surface=sim.ax`。
- 新增稳定错误码：`ios_host_ax_unavailable`、`ios_host_ax_frontmost_unavailable`、
  `ios_host_ax_platform_element_unavailable`、`ios_host_ax_tree_unavailable`、
  `ios_host_ax_unsupported_platform`，并保留缺失目标的 `simulator_not_found`。
- `schema --command sim --json` 已暴露 `ax` subcommand、`host.simulator-ax`
  output contract 和对应 failure codes；fake UDID smoke 返回
  `code=simulator_not_found`、`nextAction=triton sim list --json`，可作为无状态
  availability probe。
- 验证：`swift test --disable-sandbox --package-path CLI --scratch-path .build/cli-test --filter SimulatorAdvancedControlsTests`、
  `swift test --disable-sandbox --package-path CLI --scratch-path .build/cli-test --filter subcommandOutputSelectorsStayCoveredByParentOutputContracts`、
  `swift build --package-path CLI --scratch-path .build/cli --product triton` 均通过；
  `.build/cli/arm64-apple-macosx/debug/triton schema --command sim --json` 和
  `.build/cli/arm64-apple-macosx/debug/triton sim ax --device 00000000-0000-0000-0000-000000000000 --json`
  smoke 通过预期契约。

## iOS host AX observe/node 路由

- M3 继续收敛：`observe current|tree --platform ios --device <selector>
  --json` 现在会识别 iOS Simulator host target，并优先走 host-side
  private-framework AX tree，输出仍保持 `observe.surface`，其中
  `primarySource.name=host-layout`。
- `node resolve --platform ios --device <selector> --text <text> --json`
  复用同一 host AX observe tree 做 readonly 解析；未带 `--device` 或显式
  `--runtime-base-url` 的 iOS 路径仍保持 embedded runtime 语义。
- 新增 `observe-ios-host-ax` capability，并同步 schema/capabilities
  matrix、failure recovery family、output contract taxonomy、`node` 根 workflow
  固定清单和 `sim ax --device` schema option。
- 解析阶段的 fake simulator selector 也已机器可读：
  `.build/cli/arm64-apple-macosx/debug/triton observe tree --platform ios --device sim:00000000-0000-0000-0000-000000000000 --json`
  返回 JSON `ok=false` / `code=target_not_found`，不再泄漏 raw text error。
- 验证通过：`swift test --disable-sandbox --package-path CLI --scratch-path
  .build/cli-test --filter ObservationOutputTests`、`FailureDiagnosticsTests`、
  `SchemaFactSourceTests`，以及 `swift build --package-path CLI --scratch-path
  .build/cli --product triton`。实际 CLI schema/capabilities smoke 也确认
  `observe-ios-host-ax` 暴露成功。
- 完整本地门禁 `docs-linhay/scripts/verify.sh --local` 也已通过，覆盖
  SwiftPM dependency boundary、Swift tests、release CLI build、Harmony host
  smoke、iOS runtime observe smoke、iOS Simulator build、docs structure 和
  diff whitespace check。
- 当时剩余边界是 booted Simulator 的真实 host AX observe/node smoke；同日后续
  M3 已补真实 smoke 与 tap 动作证据。

## iOS host AX 真实 smoke 与 tap 动作

- booted Simulator 真实 smoke 已完成，目标为 `TritonKit Dedicated iPhone 17`
  / `0333546D-2AC6-4C22-AF01-293E2F4BA5BC` / iOS 26.5。
- Triton-first 链路：`sim boot --wait --jsonl` 返回 `ready=true`；
  `sim ax`、`observe tree --platform ios --device <udid>` 和
  `node resolve --platform ios --device <udid> --text "设置"` 均返回
  host-layout / private AX 证据。
- 已实现 `act tap --platform ios --device <selector> --text <text> --json`，
  输出 `HostIOSTapOutput`，capability 名为 `ios-simulator-host-tap`，schema
  暴露 `host.ios-tap` output contract 和
  `ios_host_ax_action_unavailable` failure code。
- 首次真实 tap 后 private framework 回调触发
  `accessibilityTranslationRootParentWithToken:` 未实现异常；已在
  `TokenDispatcher` 补回调，复测 `act tap --platform ios --text "通用"` 干净
  返回 `ok=true`，后置 observe 看到 `通用` / `关于本机`，证明从 Settings 根页
  进入 General。
- 验证通过：`SelectorFlagTests`、`SchemaFactSourceTests`、
  `FailureDiagnosticsTests`、`swift build --package-path CLI --scratch-path
  .build/cli --product triton`。证据写入
  `docs-linhay/spaces/20260702-strong-emulator-control/evidence/20260706-ios-host-ax-tap-smoke-v01.md`。

## M4 outline 与 `@N` alias 契约

- M4 Alias And Outline 已完成代码/契约切片：`observe tree --outline --json`
  会基于统一 `ObserveNodeOutput` 生成 deterministic `outline[]`，并在当前
  workspace 写入 `.triton/node-aliases.json`；候选过滤为非 hidden、非
  candidateOnly，且有文本、identifier 或 capability 的节点。
- `node resolve @N --platform <platform> ... --json` 现在读取 repo-local
  alias cache。平台/target 不匹配时返回 `stale_node_alias`，并带
  `nextAction=triton observe tree --platform <platform> --device <target>
  --outline --json`。
- 新增 schema/capability：`observe-outline`、`node-alias-resolve`；
  `observe.surface` output contract 记录 `outline?` 与 `aliasCache?`；
  `node.resolve` 支持 positional `<query>`，`--text` 保留兼容。
- 真实 CLI smoke：在临时目录跑
  `.build/cli/debug/triton observe tree --platform ios --device booted
  --outline --json` 生成 14 个 alias，随后
  `.build/cli/debug/triton node resolve @1 --platform ios --device booted
  --json` 返回 `ok=true`；故意使用不同 `--target` 返回
  `code=stale_node_alias`。
- Android 真实 exit gate 已补跑：先用 Triton `device start --platform
  android --avd Dxyer_API_34 --headless --gpu swiftshader_indirect --json`
  启动 `emulator-5554`，`device wait-ready` 返回 `ready=true`；随后
  `device bridge forward --confirm --audit-record m4-outline-smoke
  --execute-runner` 恢复本机 `127.0.0.1:19422 -> tcp:8080` 转发，
  `device bridge probe` 返回 `probeReachable=true`。
- M4 Android gate 在临时 workspace 通过：
  `observe tree --platform android --device emulator-5554 --outline --json`
  返回 `primarySource.name=android-bridge`、`outline_count=33`；
  `node resolve @1 --platform android --device emulator-5554 --json` 返回
  `ok=true`、`node.source=android-bridge`。证据写入
  `docs-linhay/spaces/20260702-strong-emulator-control/evidence/20260706-m4-android-outline-smoke-v01.md`。
- 清理缺口已补：`triton device stop --platform android --device emulator-5554
  --confirm --json` 现在解析统一 selector、只接受 Android emulator scope，并执行
  `adb -s emulator-5554 emu kill`。真实回归重新启动 `Dxyer_API_34`，
  `wait-ready` 证明 `kind=emulator` 后 stop 返回 `ok=true`，后续
  `device list --platform android --json` 无 emulator 残留；同机连接的 Android
  真机仍以 `scope=real` 单独保留，未被 stop 影响。
- 验证通过：`DeviceCrossPlatformTests`、`SchemaFactSourceCapabilityTests`、
  `SchemaFactSourceSurfaceContractTests`、`ObservationOutputTests`、`SelectorFlagTests`、
  `SchemaFactSourceTests`、`swift build --package-path CLI --scratch-path
  .build/cli --product triton`、`docs-linhay/scripts/check-docs.sh`、
  `git diff --check`，以及完整 `docs-linhay/scripts/verify.sh --local`。
- M5 reviewable commit series 已准备在
  `docs-linhay/spaces/20260702-strong-emulator-control/plans/20260702-milestones-subagent-map-v01.md`：
  建议拆为 iOS host AX、observe outline/node alias、Android emulator stop、
  docs/evidence，以及可选的 Web mock UI、Demo camera harness 独立 review
  （含 `App.swift` 与 camera permission `Info.plist`）。当前未
  自动 stage/commit；iOS host AX outline、Android bridge outline 与 stale alias
  smoke 都已在临时 workspace 通过，未污染仓库 `.triton/`。

## Demo camera harness

- `Examples/TritonKitDemo` 已新增 `Camera` 场景，用 `AVCaptureDevice.default(for: .video)`、
  `AVCaptureVideoDataOutput` delegate 和 `AVCaptureVideoPreviewLayer` 验证
  Simulator camera hook。UI 暴露 `CameraHarnessStatus`、
  `CameraHarnessFrameCount`、`CameraHarnessPreview`，方便后续 AX / UI 自动化读取。
- Demo `Info.plist` 已补 `NSCameraUsageDescription`，避免首次访问相机时缺少权限文案。
- 已通过 Triton-first Xcode build smoke：
  `triton xcode build --project Examples/TritonKitDemo/TritonKitDemo.xcodeproj --scheme TritonKitDemo --configuration Debug --sdk iphonesimulator --destination 'generic/platform=iOS Simulator' --derived-data-path /tmp/triton-demo-derived-data --timeout 300 --jsonl`。

## 整理提交与后续边界

- 本轮提交已按领域落在 `main`：`fb278bd6 feat(cli): strengthen emulator control workflows`、
  `877e1692 feat(web): refine host bridge mock inspector`、
  `1d2bbcf7 feat(demo): add camera smoke harness`、
  `5fc2d49d fix(cli): expose simulator camera privacy service`。提交后工作区干净，
  `main` 相对 `origin/main` ahead 4。
- 里程碑文档已从“准备提交”更新为提交后的 handoff：核心 CLI/schema/test/evidence、
  Web mock、Demo camera 和 camera privacy service 是四个实际 review 边界。CLI 部分因为
  schema/test 文件高度重叠，采用单个领域提交，避免脆弱 hunk staging。
- 后续可并行但不应默认继续扩面的区域：iOS host HID beyond tap、Android bridge endpoint
  parity、Web mock 展示 polish、Demo camera runtime smoke。没有具体 workflow 前，不新增
  daemon alias cache、真机控制、远端 agent 或正式 Web/Wails 业务入口。
- 本次不更新 `AGENTS.md`，也不新建内部 skill：Triton-first、host-side adapter、
  emulator-only 边界和 subagent 监督规则已经由现有 ops / emulator takeover skill 覆盖；
  本次新增的是该 space 的提交事实和 handoff 状态。

## Demo camera live smoke follow-up

- `Camera` 场景补成双路径验证：保留 `AVCaptureVideoPreviewLayer(session:)` 创建，另外用
  `AVCaptureVideoDataOutput` 的 sample buffer 更新 `CameraHarnessPreviewImage`，让截图可以直接证明
  frame stream 已渲染，不依赖私有 preview layer 内部连接。
- live smoke 使用 `TritonKit Dedicated iPhone 17`
  (`0333546D-2AC6-4C22-AF01-293E2F4BA5BC`) 验证通过：`triton app launch`
  输出 `SIMCTL_CHILD_DYLD_INSERT_LIBRARIES=<redacted>` 和
  `SIMCTL_CHILD_TRITON_SIM_CAMERA_SOCKET=<redacted>`；`act tap` 命中 `Camera`；
  `observe tree` 读到 `CameraHarnessStatus = camera=Triton Simulator Back Camera` 与
  `CameraHarnessFrameCount = frames=55 size=1280x720`；截图
  `/tmp/triton-demo-camera-final2.png` 显示彩色测试帧。
- live smoke 暴露并修复 hook 崩溃：fake camera session 下不能调用原始
  `AVCaptureVideoPreviewLayer initWithSession:`，否则 AVFoundation 会读取 fake
  input 的真实 `ports`；现在 fake session 使用普通 layer init，再挂 Triton overlay。
  `setFrame:` / `setBounds:` 同时补了 overlay nil guard。
- 本轮临时执行过 `triton camera on --bundle-id com.neptunekit.tritonkit.demo --json`，
  smoke 后已用 `triton camera off --bundle-id com.neptunekit.tritonkit.demo --json`
  清空启用 bundle，避免后续默认注入。
- 已补充 `.agents/skills/tritonkit-host-simulator-takeover/SKILL.md` 的 camera hook
  smoke 顺序；不更新 `AGENTS.md`，因为这是 host simulator 领域内流程，不是 repo-wide
  长期硬规则。

## Web iOS bridge 文件治理

- `Web/dev/iosSimulatorBridge.mjs` 兼容入口已删除，`Web/vite.config.ts`、
  `Web/dev/iosRuntimeMirrorBridge.test.mjs` 和 `Web/dev/iosSimulatorBridge.test.mjs`
  直接 import `Web/dev/ios-bridge/index.mjs`。
- host target 映射测试从 `Web/dev/iosSimulatorBridge.test.mjs` 拆到
  `Web/dev/ios-bridge/hostTargets.test.mjs`，并加入 `Web/package.json`
  的 `npm test` 门禁。
- `Web/dev/ios-bridge/index.mjs` 已收缩为 middleware / route 分发入口，约 255 行；iOS
  App runtime mirror 匹配拆到 `runtimeMirror.mjs`，host hierarchy 抓取、legacy iOS hierarchy
  转换和 dataRef hydration 拆到 `hierarchy.mjs`。
- route 周边实现继续归类：托管 `triton serve` 到 `tritonServe.mjs`，JSON/body helper 到
  `http.mjs`，图片解析到 `image.mjs`，host screenshot 到 `hostScreenshot.mjs`，host input 到
  `hostInput.mjs`，iOS/Android/Harmony stream proxy 到 `streamRoutes.mjs`，host logs capture
  到 `hostLogs.mjs`，host target capture/mapping 到 `hostTargets.mjs`。
- `Web/dev/iosSimulatorBridge.test.mjs` 继续拆分：hierarchy route 用例迁到
  `Web/dev/ios-bridge/hierarchyRoute.test.mjs`，fake triton / fake host input server /
  runtime data server / middleware invoke 迁到 `Web/dev/ios-bridge/testSupport.mjs`。主
  `iosSimulatorBridge.test.mjs` 降到约 389 行，只保留 input、screenshot、target registry、
  host logs 等 route smoke。
- 旧版 Web UI 的 mounted DOM 规格已完整移除：删除 `Web/dev/appFallbackDom.test.mjs`、
  `Web/dev/appFallbackDomHarness.mjs`、`Web/src/components/HostBridgeNotice.ts`、
  `Web/src/data/hostBridgePresentation.ts` 和 `npm run test:legacy-dom`；`happy-dom` 依赖从
  `Web/package.json` / `Web/package-lock.json` 移除。
- `Web/src/App.tsx` 从约 452 行拆成瘦入口：布局树类型、localStorage 恢复和 split/update/remove
  操作进入 `Web/src/layoutModel.ts`；pane toolbar、卡片注册和递归渲染进入
  `Web/src/components/AppCanvas.tsx`。`AppContext.tsx` 与 `InspectorCard.tsx` 改为从
  `layoutModel.ts` 引用 `LayoutNode`，避免继续从 App 入口拿类型。
- 验证：bridge 相关 `node --test` 通过 30 项；`cd Web && npm test`、`cd Web && npm run build`、
  `docs-linhay/scripts/check-docs.sh` 和 `git diff --check` 通过。

## Web hierarchy source tabs

- 新建 `docs-linhay/spaces/20260706-web-hierarchy-source-tabs/`，收敛 Android / Harmony
  “视图树”和“AX 树”显示一致的问题：这两端当前没有独立 view/AX 双源，Web 不应展示两份等价 tab。
- `Web/src/inspectorTreeTabs.ts` 负责按 platform/source 决定审查 tab：iOS 保留“视图树 / AX 树”，
  Android bridge 显示单个“辅助功能树”，Android UIAutomator 与 Harmony HDC dumpLayout 显示单个“布局树”。

## Web stream gesture mapping space

- 新建 `docs-linhay/spaces/20260706-web-stream-gesture-mapping/`，定义“设备实时画面流支持手势映射响应”的一期边界：Web mock 只把 pointer 事件映射为 `TKInputRequest` DTO，再走现有 `/web/host-input` / `triton serve /web/input`，不新增 React 侧低层设备控制。
- 参考项目结论已写入 space：Baguette 浏览器侧可用归一化坐标但 wire 使用设备 points + width/height；serve-sim 强调 tap 必须单次派发，避免 begin/end 分离造成长按；sim-use Android bridge 以 `/tap`、`/swipe`、`/gesture` 通过 `AccessibilityService.dispatchGesture()` 执行；Loupe 保持 host CLI / runner 执行动作，runtime observation 与 Web 不应合成低层输入。
- TritonKit 当前可复用能力：`TKInputRequest` 已覆盖 `tap`、`longPress`、`swipe`；Web bridge 已有 `/web/host-input`；host target `/web/input` 对 iOS Web surface 当前开放 `tap`/`swipe`，Android 开放 `tap`/`longPress`/`swipe`，Harmony 开放 `tap`/`swipe`。
- 一期方案建议新增纯前端 `streamGestureModel`：负责 viewport letterbox 过滤、图像坐标到设备坐标映射、tap/swipe/longPress 判定和重复派发抑制；`StreamCard` 只在 overlay 为“无”时启用设备输入，`view` / `ax` 模式继续用于节点审查。
- 已按完整一期落地：`Web/src/streamGestureModel.ts` 新增纯模型；`StreamCard` 在无 overlay 时接入 pointer 手势并 POST `/web/host-input`；iOS longPress 路由到 runtime fallback，Android / Harmony longPress 走 host；成功后刷新 hierarchy，失败显示错误。
- 验证通过：`cd Web && node --test dev/streamGestureModel.test.mjs`、`cd Web && npm test`、`cd Web && npm run build`。
- 用户要求 Harmony longPress 也纳入本期后，已补 host 执行层：`/web/input` 的 Harmony `longPress` 复用同点 `uitest uiInput swipe` hold，Web 手势模型放行 Harmony `longPress` 到 `source=host`。
- 真实 Harmony emulator smoke 已补跑：启动 `Codex Test Phone` 到 `127.0.0.1:10100`，Triton `device wait-ready` 返回 `ready=true`；通过 `triton serve` 的 `/web/input?target=host:harmony:127.0.0.1:10100` POST `longPress`，返回 `ok=true/action=longPress/message=Harmony longPress was submitted through uitest same-point swipe hold.`；before/after 截图写入 `docs-linhay/spaces/20260706-web-stream-gesture-mapping/screenshots/20260706-web-harmony-longpress-before-v01.jpeg` 与 `...after-v01.jpeg`。smoke 后关闭 19421 server，并用 `device stop --skip-launchd --confirm` 停止 emulator，最终 list 显示 `127.0.0.1:10100` 为 `Offline`。
- `InspectorCard.tsx` 复用现有 tree 构建和选择逻辑，只改变 tab 展示与来源说明；不新增后端契约。
- 验证：`cd Web && node --test dev/inspectorTreeTabs.test.mjs` 通过 4 项；`cd Web && npm test`
  通过 51 项；`cd Web && npm run build` 通过。

## Web Inspector property sheet

- 用户要求选中节点详情不再占用 Inspector 底部空间，并希望 modal sheet 接近 Lookin 的属性查看方式，还能反向修改界面属性。
- 已将 `InspectorCard` 的选中节点详情改为 modal property sheet：顶部只保留轻量 `选中:` chip；modal 左侧显示节点摘要，右侧按 `Geometry` / `View` / `Layer` / `AX` / `Raw` 分区展示属性。
- 新增 `Web/src/inspect/nodePropertyDraft.ts` 与 `Web/dev/nodePropertyDraft.test.mjs`，把可编辑字段收敛为 draft，并支持导出只包含变更字段的 patch payload。
- 已补 CLI/HTTP/runtime 节点属性写入契约：`TKNodePropertyPatchRequest/Response` 进入 shared DTO，`triton debug patch-node` 和 `triton serve /web/node-property` 都通过 `modifyAttribute` 下发到 embedded runtime。
- iOS runtime 写入白名单覆盖 frame、UIView hidden/alpha/userInteraction/AX identifier/label、CALayer hidden/masksToBounds/opacity/cornerRadius/zPosition、常见文本控件 text 与前景/背景色；无对象、无 diff 或不支持字段返回 `ok=false` 或 `skipped`，不再假成功。
- Web property sheet 的 `应用到设备` 改为真实调用 `/web/node-property`：iOS host/simulator target 通过 bridge 解析到匹配 App runtime target 后转发，成功后刷新 Inspect Session；Android / Harmony 该 endpoint 明确返回不支持。
- 浏览器 smoke：使用 `http://127.0.0.1:34127/` 左右分屏 Stream + Inspector，选中 iOS runtime tree 的 `iDxyer.MTLFormCell` 后打开详情，确认 modal、Geometry 输入值和禁用应用按钮正常，console 无 error/warn；截图为 `docs-linhay/spaces/20260706-web-inspect-session-slots/screenshots/20260706/20260706-web-inspector-property-sheet-after-01.png`。
- 真实 Demo 节点属性下发 smoke 已补跑：`TritonKit Dedicated iPhone 17` / `com.neptunekit.tritonkit.demo` 对 `ComplexHarnessStatus` / `ios-runtime:130` 写入 `view.accessibilityLabel`；`triton serve /web/node-property`、`triton debug patch-node` 和 Vite bridge `127.0.0.1:34127/web/node-property` 均返回 `ok=true` 与 `applied:["view.accessibilityLabel"]`，`triton observe tree` 回读命中 smoke 文本，最后恢复为 `Complex harness: 0`。
- smoke 暴露并修复同一 Simulator 上多 App runtime 的 target id 冲突：Demo 与 iDxyer 现在分别暴露为 `triton:ios-simulator:<udid>/app:<bundle>`；旧 UDID / unscoped selector 在多 runtime 时返回 `ambiguous_target`，Vite bridge 对 ambiguous simulator runtime mirror 返回错误，不再静默选第一个 App。
- 验证：`swift test --filter TKCLITransportModelsTests/nodePropertyPatchRequestResolvesOid`、`swift test --filter TKCLITransportModelsTests/commandRequestTypeMapping`、`swift test --filter TKNodePropertyPatchTests`（macOS 上 UIKit case 被跳过）、`swift test --package-path CLI --filter SchemaFactSourceTests/observeAndNodeProvidedCapabilitiesStaySchemaMatrixAligned`、`cd Web && node --test dev/ios-bridge/nodePropertyRoute.test.mjs dev/nodePropertyDraft.test.mjs`、`cd Web && npm test`、`cd Web && npm run build`、`docs-linhay/scripts/check-docs.sh`、`git diff --check` 均通过。

## GitHub issue 135 Harmony install failure code

- #135 要求 Harmony `hdc install` 即使进程 exit 0，只要 stdout/stderr 出现 `msg:error:`、`failed to install bundle` 等失败文本，也必须返回机器可读失败。
- `runHostCommand` 已有统一语义失败识别；本轮补齐 CLI failure envelope，把 HDC install 的 `HostCommandRunError.nonZeroExit` 从泛化 `app_install_failed` 收窄为 `harmony_app_install_failed`，并将回归 fixture 改为 issue 原文 `code:9568320 error: no signature file`。
- 验证：补丁级 `git diff --check -- CLI/Tests/TritonKitCLITests/DeviceCrossPlatformTests.swift Sources/TritonKitCLI/CLIHostProcessRuntime.swift` 通过；重新 `swift test --filter DeviceCrossPlatformTests/harmonyAppInstallTreatsHDCStdoutFailureAsCommandFailure` 被当前工作区已有 `PatchNode` 未完成改动阻塞，错误为 `CLIDebugCommands.swift:19:13: cannot find 'PatchNode' in scope`，需先收口该并行改动后复跑。

## Web workbench 插槽产品形态收敛

- 用户明确当前产品形态不是十宫格控制台；Web mock 工作台插槽收敛为 `stream` 与 `inspector` 两类，只保留设备实时画面流和界面 AX 审查。
- `Web/src/layoutModel.ts` 将 `CardType` 收窄为 `stream | inspector`，并在读取旧 `localStorage` 布局时把废弃卡片降级为空插槽，避免旧浏览器状态继续显示 VLM / Xcode / doctor 等卡片。
- `Web/src/components/AppCanvas.tsx` 移除其他插槽选择项和渲染分支，并删除不再引用的八个卡片组件文件。
- 回归：`Web/dev/currentWebContracts.test.mjs` 新增插槽白名单与旧布局清理测试；`cd Web && npm test`、`cd Web && npm run build`、`git diff --check`、`docs-linhay/scripts/check-docs.sh` 均通过。

## Agent mobile runtime platform space

- 用户要求新开 space，并把新插槽产品方向对齐 Revyl 同类产品能力。
- 新建 `docs-linhay/spaces/20260706-agent-mobile-runtime-platform/`：定义 Agent Mobile Runtime Platform，一期只包装本机 iOS Simulator / Android Emulator / HarmonyOS DevEco Emulator 的 CLI/HTTP 机器可读控制、证据包和 workflow seed，不复制 Revyl 专有实现，也不直接引入云设备池、多租户、计费或远端 agent。
- Web 新插槽定位为人类可视化与调试入口；业务控制事实仍以 TritonKit CLI/HTTP 契约为准。
- 用户补充该产品是本地单机产品，完整 Atlas map 与 VLM 自主探索 loop 是必做项；space 已调整为：Atlas map 必须基于本机 session/evidence 生成 screen/state/transition/coverage 图谱，VLM loop 必须通过 Triton CLI/HTTP 执行动作并写入可审计 evidence。
- 用户进一步明确不限制真机 / 模拟器 / 仿真器；space 已调整为本机 mobile target 产品模型，产品入口按 `platform + scope + capabilities` 抹平差异，不按设备类型分叉。现有 Web `DeviceTarget/InspectTarget` 与 CLI app scope 已部分具备该抽象，后续缺口按 capability/unsupported 补齐。
- 用户指出已接入相关基础能力；space 已补“已接入基础能力”表，明确复用 target/capability discovery、Web target registry / InspectTarget、host app lifecycle、observe/hierarchy/WebView、`triton act`、evidence capture、`.tritonplan` replay、local MLX VLM grounding、semantic provider、Web stream/inspector/property sheet 等基础，只补 runtime session、Atlas graph 和 VLM bounded loop 缺口。
- 用户要求结合 serve-sim 与 Maestro 给完整落地方案；新增 `plans/20260706-serve-sim-maestro-landing-v01.md`。方案裁定：serve-sim 只吸收本机 streaming/input/permission/camera/helper lifecycle 经验，不引入 Node/npm 或 Web preview 作为产品控制面；Maestro 只吸收 YAML flow、selector、smart wait、Studio authoring、artifact/report 和 AI optional 边界。落地顺序为 P0 flow validate、P1 deterministic runner、P2 Atlas build、P3 VLM grounding、P4 bounded explore loop、P5 serve-sim host primitives。
- 继续补充该落地方案：新增 Maestro/serve-sim 到 Triton 命令映射、Flow Validate / Flow Run / Atlas Build / Explore Run DTO 草案、本地 `.triton/` 文件布局、测试矩阵、首批 Issue A-F 切片、风险约束和不做清单。核心约束保持：Flow 先编译 `.tritonplan`，Atlas 与 VLM 共用 evidence，VLM bounded-run 默认显式开启，serve-sim 经验只进入 Triton host adapter 契约。
- 再次补充该落地方案：新增 Selector Resolution v1、Smart Wait / Scroll v1、Atlas Signature v1、VLM Decision Protocol v1、Report Export v1、`.triton/test.yaml` Config v1 和 P0 DoD。后续第一刀应先做 `triton flow validate --json`，只声明 `flow-validate` 能力，不误报真实设备 flow runner 已完成。
- 用户明确要求默认开启 LLM 和 VLM，以最大化场景理解和执行；space、serve-sim/Maestro 落地方案与 `tritonkit-local-mlx-vlm-provider` skill 已调整为 workspace run 默认 `llm.enabled=true`、`vlm.enabled=true`。模型核心职责收敛为两件事：帮助流程在各种初始场景中稳定启动，以及帮助偏航流程回到正轨；动作仍经 Triton capability / policy / evidence gate。本地 replay / 稳定回归也不关闭模型，而是用 `actionPolicy=planFirst` 让模型全程参与启动、观察、验证、drift 诊断和修复建议。本 space 不把 CI 作为当前产品主语。
- 用户要求在 space 内设计需求方案和技术方案；新增 `plans/20260706-run-requirements-technical-plan-v01.md`，把产品本质收敛为本地 App 运行理解器，核心对象为 `Run = goal + local target + observation + LLM/VLM bootstrap/recovery + policy-gated action + evidence + atlas delta + replay seed`。方案明确 `events.jsonl` 是唯一事实源，Atlas / flow / report / Workbench 都从 runId 和事件流派生；第一刀应先做 workspace run 目录与事件协议，而不是先做完整 Atlas、DSL 或 UI。
- 用户进一步明确 LLM/VLM 的核心价值是帮助流程回到正轨、帮助流程在各种场景中稳定出发；Run 方案已补 Flow Bootstrap / Flow Recovery 需求和技术协议，新增 `flow.bootstrap.*` 与 `flow.recovery.*` 事件、Bootstrap Check DTO、Recovery Proposal DTO、BDD 场景和第一刀实现验收。
