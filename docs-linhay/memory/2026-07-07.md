# 2026-07-07

## Workspace Run 第一刀

- `20260706-agent-mobile-runtime-platform` 的 Run 方案开始落地到代码：新增 `triton workspace run|inspect|stop|export-flow`，并把 `workspace` 加入 `triton schema --command workspace --json`。
- CLI runtime 在 provider missing 时只做本地 dry skeleton，不伪装真实设备或模型已执行；配置 `mock` LLM/VLM provider 后会进入 deterministic model loop。
- 默认 LLM/VLM 开启已进入 run DTO：`llmEnabled=true`、`vlmEnabled=true`；provider 未接入时显式 `providersReady=false`，并给 `configure_ai_provider` nextAction。
- Shared run event parser 补齐 `target.resolved`、`provider.checked`、`app.ready`、`run.stopped` 和 `stopped` 状态；此前已补的 `flow.bootstrap.*` / `flow.recovery.*` 继续作为 LLM/VLM bootstrap/recovery 事实流。
- HTTP 同构入口已补 handler：`POST /workspace/run`、`GET /workspace/runs/:runId`、`POST /workspace/runs/:runId/stop`、`POST /workspace/runs/:runId/export-flow`，复用同一套 workspace runtime。
- 该切片结束时尚未完成：真实 target discovery、真实 LLM/VLM provider request/response、真实模型服务输出的 decision / recovery proposal、完整 Atlas graph 和 Web Workbench；截至本日后续切片，OpenAI-compatible LLM decision provider 已接入，OpenAI-compatible VLM 配置 / preflight / evidence metadata 已接入，workspace action path 已能在有本地 screenshot artifact 时做 VLM grounding，已支持 bounded multi-step recovery 的 step-indexed evidence / Atlas delta，并已把 run-local Atlas 投影到 `atlas/app-map/`；仍未完成跨 run Atlas state merge / coverage path、`mlx-swift-lm` workspace helper 和 Web Workbench。
- 聚焦验证已通过：`swift test --package-path CLI --filter WorkspaceRunTests`、`swift test --package-path CLI --filter SchemaFactSourceTests/schemaCommandInventoryRemainsStableForAgentDiscovery`、`swift test --package-path CLI --filter SchemaFactSourceTests/schemaCommandFilteringCoversTheFullCommandInventory`。
- 第二刀协议夹具已开始：`--dry-model-fixture` / HTTP `dryModelFixture=true` 会显式追加 `model.decided`、`policy.checked`、`action.executed`、`verify.checked`、`flow.recovery.*`、`atlas.updated`、`flow.updated` 事件和对应 placeholder artifact。mock provider ready 后，默认 workspace run 也复用同一事件形状，但 artifact `mode` 标为 `mock-provider`。

## Workspace Run Provider Preflight

- `workspace run` 已从硬编码 provider missing 推进到显式 LLM/VLM provider preflight：CLI 支持 `--llm-provider mock --vlm-provider mock`，HTTP `POST /workspace/run` 支持 `llmProvider: "mock"`、`vlmProvider: "mock"`。
- `mock` 作为本地 deterministic LLM/VLM provider 会记录 `providersReady=true`、`providerStatus=ready`，`provider.checked` phase 为 `ready`，`flow.bootstrap.checked` phase 为 `provider_ready`，并触发一轮默认 model loop；这仍不代表真实模型 request/response 已接入。
- `mock` 作为本地 deterministic VLM provider 会记录 `vlmProviderStatus=ready`；因为 LLM provider 尚未配置，整体仍返回 `providersReady=false`、`providerStatus=partial`、`llmProviderStatus=missing`，并给 `configure_llm_provider` nextAction。
- VLM-only preflight 的 provider evidence 和事件流会写入对应事实：`evidence/model/provider-check.json` 记录 provider 状态，`provider.checked` phase 为 `vlm_ready_llm_missing`，`flow.bootstrap.checked` phase 为 `llm_missing`。
- 该切片时 `mlx-swift-lm` / `openai-compatible` 在 workspace preflight 中暂不伪装 ready：缺模型或 base URL 时只返回配置 nextAction；后续已接入 OpenAI-compatible LLM decision provider、OpenAI-compatible VLM 配置 / preflight / evidence metadata、workspace action VLM grounding 和 bounded recovery loop；`mlx-swift-lm` workspace helper 仍待后续切片。

## Workspace Run Atlas Seed

- `workspace run` 支持显式 target metadata：CLI `--platform/--scope` 和 HTTP `platform/scope` 会写入 `target.platform`、`target.scope` 与 `evidence/model/target.json`；未传时仍保留 `unknown/current`，不伪装自动设备发现。
- `workspace run` 的 `atlas/atlas.json` 已从空壳推进到 run-local 初始 Atlas seed：包含 `kind=triton.workspace.atlas`、`runId`、`screen_0000`、`state_0000`、coverage 计数和 `events.jsonl#observation.captured` / screenshot / hierarchy evidence backlink。
- `--dry-model-fixture` 现在会写回 Atlas transition：`atlas/deltas.jsonl` 和 `atlas/atlas.json` 都包含 `transition_0000`，状态为 `candidate_failed`，从 `screen_0000` 回到 `screen_0000`，并引用 model / policy / action / verify evidence。
- `workspace export-flow` 会从 `action.executed` 事件派生最小 action step；dry fixture 当前可导出 observation-derived tap 候选，并保留 model / policy / verify evidence backlink。
- `workspace inspect` 现在返回 Atlas summary：`atlasRef`、`deltaRef`、`coverageStatus`、`screenCount`、`stateCount`、`transitionCount`，HTTP inspect 复用同一响应。
- 当前 Atlas / flow 已能从 run-local 多步 transition 投影出 `atlas/app-map/` candidate path；仍未做跨 run state variant 合并或路径 coverage 聚合。下一步应把更多真实 action 前后 observation 和 `atlas.updated` 事件合并成跨 run 地图，而不止单个 run 的投影。

## Workspace Run Runner Bounds

- `workspace run` 现在把自主探索 loop 的边界写成机器可读 `runner` 契约：`actionPolicy`、`maxSteps`、`allowedActions`、`stopConditions` 会进入 `run.json`、`report.json` 和 `config.yaml`。
- 默认 runner 边界是 `maxSteps=20`、`allowedActions=[tap,swipe,type,wait,verify,stop]`、`stopConditions=[max_steps_reached,provider_missing,unsupported_capability,policy_rejected,model_unparseable]`；这为后续真实 LLM/VLM loop 提供停止条件，避免无界探索。
- CLI 支持 `--max-steps`、可重复 `--allowed-action`、可重复 `--stop-condition`；HTTP `POST /workspace/run` 支持 `maxSteps`、`allowedActions`、`stopConditions`，两者复用同一 runtime 校验。
- 非法 `maxSteps <= 0` 会抛出 `RuntimeError`，不会创建 run；schema 已同步暴露 runner options。
- 已验证：先跑红灯确认 `runner` 字段不存在，再实现后通过 `swift test --package-path CLI --filter WorkspaceRunTests`。

## Workspace Run Policy Gate Dry Path

- `--dry-model-fixture` 现在会把 observation-derived `tap <visibleText>` 候选先交给 runner allowlist；当 `allowedActions` 不包含 `tap` 时，`policy.checked` 写 `failed`，`evidence/model/policy-000.json` 写 `stopReason=policy_rejected`。
- policy 拒绝时不会写 `action.executed`、`verify.checked`、`atlas.updated`、`atlas/deltas.jsonl` 或 `evidence/actions/action-000.json`；Atlas coverage 保持初始 seed，避免把被拒动作误记成 transition。
- 拒绝路径仍会写 `model.decided`、`policy.checked`、`flow.recovery.detected(policy_rejected)` 和 `flow.recovery.proposed(stop)`，让后续 LLM/VLM recovery 设计有可追溯事实流。
- 已验证：先跑红灯确认固定 allowed=true 的旧路径会错误执行 action，再实现后通过 `swift test --package-path CLI --filter WorkspaceRunTests`。

## Workspace Run Paused Lifecycle

- Shared event protocol 新增 `TKTestRunStatus.paused` 与 `run.paused`，parser summary 现在把 `run.finished`、`run.paused`、`run.stopped` 都视为 terminal status source。
- `workspace run` 在 provider missing / LLM missing 时返回 `status=paused` 并以 `run.paused` 收尾；policy reject dry path 也以 `run.paused phase=policy_rejected` 收尾，不再伪装成正常 stopped。
- `workspace stop` 仍负责显式把 run 转成 stopped：如果最后事件不是 `run.stopped`，会追加 `run.stopped` 并把 `run.json` status 写成 `stopped`。
- schema 已同步暴露 workspace run 的 final event 为 `run.paused|run.stopped`，output contract 说明 `summary.status` 可来自 paused terminal event。
- 已验证：`swift test --package-path CLI --filter RunEventWriterTests`、`swift test --package-path CLI --filter WorkspaceRunTests`。

## Workspace Inspect Pause Reason

- `workspace inspect` / HTTP inspect 现在返回 `latestPause`，直接暴露最后一个 `run.paused` 事件；provider missing 会显示 `phase=provider_missing`，policy reject 会显示 `phase=policy_rejected`。
- policy reject dry path 的 `run.nextActions` 现在包含 `review_policy_rejection`，指向调整 runner allowlist、修正 goal 或检查 `evidence/model/policy-000.json`；provider setup blocker 仍优先返回 provider 配置 nextAction。
- 已验证：`swift test --package-path CLI --filter WorkspaceRunTests`。

## Workspace Bootstrap Proposal

- `--dry-model-fixture` 现在会写 `flow.bootstrap.proposed` 事件和 `evidence/model/bootstrap-proposal-000.json`，用单步 observation-derived `triton act tap <visibleText> --json` 表达稳定启动候选。
- `workspace inspect` / HTTP inspect 返回 `latestBootstrapProposal`，agent 不需要扫 `events.jsonl` 就能读取最近一次 bootstrap proposal。
- 该 proposal 仍只是候选动作：后续是否执行继续由 runner allowlist 和 policy gate 决定。
- 已验证：`swift test --package-path CLI --filter WorkspaceRunTests`。

## Workspace Model Evidence Artifacts

- dry fixture 和 mock provider loop 的模型参与步骤现在写入 request / response artifact：bootstrap proposal 对应 `evidence/model/bootstrap-proposal-000-request.redacted.json` 与 `evidence/model/bootstrap-proposal-000-response.raw.txt`，decision 对应 `evidence/model/decision-000-request.redacted.json` 与 `evidence/model/decision-000-response.raw.txt`。
- `bootstrap-proposal-000.json` 和 `decision-000.json` 会通过 `artifacts` 字段反链这些文件，补齐“每个模型参与步骤都有 request、response、confidence、artifact、policy decision”的测试协议证据面。
- 该切片能力仍是 deterministic fixture / mock provider，不代表真实 LLM/VLM provider request/response 已接入；后续 OpenAI-compatible LLM 已进入同一 request / response evidence 契约，VLM screenshot / hierarchy / model raw output grounding 仍待接入。
- 已验证：`swift test --package-path CLI --filter WorkspaceRunTests`。

## Workspace Mock Provider Default Loop

- `workspace run` 在 `--llm-provider mock --vlm-provider mock` / HTTP `llmProvider=mock, vlmProvider=mock` 时不再只停在 provider preflight；默认追加 `flow.bootstrap.proposed -> model.decided -> policy.checked -> action.executed -> verify.checked -> flow.recovery.detected/proposed/rejected -> atlas.updated -> flow.updated`。
- mock provider loop 复用 runner allowlist：observation-derived `tap <visibleText>` 候选被允许时写入 `atlas/deltas.jsonl`、`transition_0000` 和 `export-flow` action step；候选动作不在 `allowedActions` 时以 `run.paused phase=policy_rejected` 收尾，并返回 `review_policy_rejection`，不写 action / verify / atlas transition。
- CLI 与 HTTP inspect 的 provider-ready 路径现在都有 17 个事件；provider missing / VLM-only partial 路径仍保持暂停，不进入默认 autonomous action。
- 已验证：先用 `swift test --package-path CLI --filter WorkspaceRunTests` 跑出 provider-ready 仍只有 skeleton 的红灯，再实现后同命令通过。

## Workspace Observation Fixture Seed

- `workspace run` 新增 `--observation-fixture <json>`，HTTP `POST /workspace/run` 新增 `observationFixture` 字段；schema 已同步暴露该参数。
- fixture 复用 test-run observation 的 `artifacts` 与 `screenCandidate` 结构，可写入 screenshot / ax / hierarchy refs、hash、visibleTexts、sourceCommands 和 changed；运行时把原始 fixture 保存为 `evidence/observations/0000.json`。
- `--observation-fixture` 现在也接受 `triton observe current/tree --json` 输出形态：从 nodes 推导 visibleTexts，从 primarySource / artifacts 推导 hierarchy、screenshot、AX 引用，并用原始 observe 输出、可见文本和 node fingerprint 生成 screenCandidate hashes。
- `observation.captured` 事件和 `atlas/atlas.json` 的 `screen_0000.signature`、`dominantTexts`、`evidenceRefs` 现在可从 fixture 生成，不再只能是固定 placeholder；未传 fixture 时旧 placeholder 行为保持。
- 该能力仍不是 live `triton observe` 自动采集，但已把 raw observe JSON 与 workspace seed 统一到同一入口；下一步应由 `workspace run` 直接调用 `observe current/tree` 并保存同样证据。
- 已验证：先补红灯确认 `observationFixture` 参数缺失，再实现后通过 `swift test --package-path CLI --filter WorkspaceRunTests`；CLI 与 HTTP run 测试都覆盖 fixture 输入。

## Workspace Live Observe Seed

- `workspace run` 新增显式 live observe：CLI `--observe-live --observe-kind current|tree --platform <platform> --target <selector>`，HTTP `POST /workspace/run` 新增 `observeLive`、`observeKind`、`observeMaxNodes`、`observeOutput`、`observeRuntimeBaseURL`、`observeHost`、`observePort`、`hdc` 字段。
- live observe 复用现有 `observeIOS/observeAndroid/observeHarmony` runtime，不通过 CLI 子进程绕回自身；运行时保存 raw `ObserveOutput` 到 `evidence/observations/0000.json`，并从 nodes / primarySource / artifacts 派生 `observation.captured` 与 Atlas `screen_0000`。
- observation seed 解析已拆到 `CLIWorkspaceObservationRuntime.swift`，`CLIWorkspaceRuntime.swift` 降到 1500 行治理线以下；无独立 screenshot/AX artifact 时，live observe 使用 placeholder screenshot 和 hierarchy/raw observe 回退，保持 `TKTestRunEventLogParser` 可解析。
- 当前仍不是完整默认链路：`workspace run` 只有显式 `--observe-live` 才采集 live observation；app launch 已可通过显式 `--app-mode launch` 进入 run，候选动作执行也只在显式 `--execute-actions` 时触发；动作前后 observation transition 已由后续切片补齐，见本日 `Workspace post-action observation atlas transition`。
- 已验证：先补红灯 `workspace run captures live observe seed when enabled`，实现后通过 `swift test --package-path CLI --filter WorkspaceRunTests`。

## Workspace App Lifecycle Evidence

- `workspace run` 新增显式 App lifecycle mode：CLI `--app-mode dry|attach|launch` 与 HTTP `appMode` 默认 `dry`；`launch` 会在 initial observation 前调用现有 host app launch adapter，支持 iOS `bundleID`、Android `packageName/activity`、Harmony `bundle/ability`。
- 新增 `CLIWorkspaceAppLifecycleRuntime.swift`，把 app lifecycle request/evidence/provider 从 `CLIWorkspaceRuntime.swift` 拆出，避免继续扩大主 runtime 文件；测试可注入 fake provider，不触碰本机模拟器状态。
- `evidence/actions/app-ready.json` 现在写 `kind=triton.workspace.app-lifecycle`、`mode`、`phase`、`submitted`、`sourceCommands`、`businessReady=false`；`events.jsonl#app.ready` phase 由 lifecycle evidence 驱动，launch 路径为 `launch_submitted`。
- `launch_submitted` 只证明 host app launch 命令提交，不证明业务 ready；后续仍需要 `--observe-live`、wait/assert、action/verify 或 evidence capture 证明流程进入正轨。
- schema、README、`docs-linhay/dev/ai-cli-readable-control.md`、space 技术方案和对外 `tritonkit-emulator-cli-takeover` skill 已同步新参数与边界。
- 已验证：先补红灯 `workspace run records app launch evidence when enabled` / `workspace HTTP run records app launch evidence when enabled`，实现后通过对应 focused tests 与 `workspace schema exposes bounded runner options`。

## Workspace Launch Observed Readiness

- `workspace run --app-mode launch --observe-live` 现在把 app lifecycle 和首帧 observation 合并到同一个 app-ready evidence：launch-only 仍是 `phase=launch_submitted`，live observe 成功后升级为 `phase=launch_observed`、`ready=true`、`observedAfterLifecycle=true`。
- `evidence/actions/app-ready.json` 在 `launch_observed` 时写 `observationRef=events.jsonl#observation.captured`，并附带首帧 observation 摘要：screenshot、AX、hierarchy 和 `visibleTextCount`；`businessReady` 仍保持 `false`，等待后续 wait/assert/action/verify 证明业务状态。
- `app.ready` 事件 phase 与 artifact phase 使用同一个 derived app-ready snapshot，避免 timeline 和 artifact 对同一 run 阶段说法不一致。
- 已验证：先补红灯 `workspace run records launch observed readiness when live observe follows launch`，实现后通过该 focused test 和完整 `swift test --package-path CLI --filter WorkspaceRunTests`。

## Workspace Business Ready Checkpoint

- `workspace run` 新增首个业务完成 checkpoint：CLI `--business-ready-text <text>` 与 HTTP `businessReadyText` 会用 initial observation 的 visibleTexts 做 exact match。
- 命中时写 `evidence/business/ready.json`，事件流追加 `business.ready` 和 passed `verify.checked`，`run.json` / `report.json` 写 `business.ready=true`，并以 `run.finished status=passed` 收尾；未命中时返回 `business_checkpoint_missing` nextAction。
- 该 checkpoint 是业务 ready 的证据，不改变 `evidence/actions/app-ready.json` 的边界：`launch_submitted` / `launch_observed` 仍只证明 host launch / 首帧可观察，`businessReady=false` 保持在 app lifecycle artifact 内。
- provider ready 且 checkpoint 已通过时，workspace run 不再继续写 deterministic mock action 失败事件，避免同一 run 同时声称业务通过和 mock action drift。
- 已验证：先补红灯 `workspace run completes when business ready text is observed`，实现后通过该 focused test 和完整 `swift test --package-path CLI --filter WorkspaceRunTests`。

## Workspace business live wait checkpoint

- `workspace run` 的业务 checkpoint 现在支持显式 runtime wait：CLI `--business-ready-live-wait --business-ready-text <text>`，HTTP 字段 `businessReadyLiveWait=true`，可配 `businessReadyTimeout` / `businessReadyInterval`。
- live wait 通过已有 `performWait(WaitRequest(condition: .text))` 调用本机 embedded runtime；证据仍写入 `evidence/business/ready.json`，但 `check=runtime_wait`、`source=runtime.wait`、`phase=wait_matched|wait_timeout|wait_unmatched`，并嵌套 wait 结果摘要。
- 初始 observation exact text checkpoint 保持默认；只有显式开启 live wait 时才会等待 runtime 文本，避免把首帧 fixture 行为悄悄改成网络/runtime 依赖。
- 已验证：先补红灯 `workspace run completes from live business wait`，实现后通过 focused test 与完整 `swift test --package-path CLI --filter WorkspaceRunTests`。

## Workspace explicit action execution checkpoint

- `workspace run` 新增显式候选动作执行开关：CLI `--execute-actions` 与 HTTP `executeActions=true` 会把 observation-derived 当前步骤 tap 候选交给 runtime action provider。
- 执行证据写入 `evidence/actions/action-000.json`，其中 `source=workspace.action-provider`、`proofSource=runtime.input`，并嵌套 runtime `inputResult` / `tapResolution`；`action.executed` 事件引用该证据。
- Atlas transition 状态在真实执行后写为 `executed_unverified`，表示动作已经提交，但业务 ready 仍必须由后续 wait/verify 或二次 observation 证明；若业务 checkpoint 已通过，则不会再执行候选动作。
- 新增 `CLIWorkspaceActionExecutionRuntime.swift`，把 action execution request/result/artifact/Atlas transition helper 从主 workspace runtime 拆出，`CLIWorkspaceRuntime.swift` 保持在 1500 行治理线以下。
- 已验证：先补红灯 `WorkspaceActionExecutionTests`，实现后通过 `swift test --package-path CLI --filter WorkspaceActionExecutionTests`、`swift test --package-path CLI --filter WorkspaceRunTests` 与 `swift test --package-path CLI --filter WorkspaceBusinessReadinessTests`。

## Workspace post-action business verification checkpoint

- `workspace run --execute-actions --business-ready-text <text> --business-ready-live-wait` 现在会先用 initial visible text checkpoint 判断是否已经 ready；未 ready 时执行模型候选动作，动作成功后再调用 runtime wait(text)。
- action 后 wait 通过时，`evidence/business/ready.json` 写 `stage=post_action`、`phase=post_action_wait_matched`，并把 `events.jsonl#action.executed` / `evidence/actions/action-000.json` 纳入 evidenceRefs。
- 未启用 `observeLive` 时事件顺序为 `action.executed -> business.ready -> verify.checked -> atlas.updated -> flow.updated -> run.finished`；`run.status=passed`，`evidence/model/verify-000.json` 写 `status=passed` 和 `businessRef=evidence/business/ready.json`。启用 `observeLive` 后的 post-action observation 顺序见本日后续切片。
- Atlas transition 状态在 action 后业务验证通过时从 `executed_unverified` 升级为 `verified`；未验证的 execute-only 路径仍保持 `executed_unverified`。
- 新增 `CLIWorkspaceEventsRuntime.swift`，把 workspace skeleton / model decision event 构造从 `CLIWorkspaceRuntime.swift` 拆出，主 runtime 保持在 1500 行治理线以下。
- 已验证：先补红灯 `workspace run verifies business readiness after explicit action execution`，实现后通过 `swift test --package-path CLI --filter WorkspaceActionExecutionTests`、`swift test --package-path CLI --filter WorkspaceRunTests` 与 `swift test --package-path CLI --filter WorkspaceBusinessReadinessTests`。

## Xcode interrupted build lifecycle diagnostics

- GitHub issue #137 修复了 `triton xcode build/test/run` 对 `** BUILD INTERRUPTED **` / exit code 15 的泛化失败分类：无 `BUILD FAILED` / `TEST FAILED` 标记时返回 `xcodebuild_interrupted`，若 post-action status 仍看到匹配 workspace/project 的 `xcodebuild`，返回 `orphaned_xcodebuild`。
- `TKXcodeActionSummary` 新增 `postActionProcessStatus` 和 `nextActions`，在 orphaned 场景中带出仍活跃 PID、workspace/project/scheme/destination/DerivedData，以及 `triton xcode status --json` / `triton xcode wait-idle --workspace <workspace> --timeout 120 --json` 恢复动作。
- schema、Xcode takeover skill、README、`docs-linhay/dev/ai-cli-readable-control.md` 与 space 技术设计已同步；后续 agent 遇到 interrupted/orphaned build 时应先按 Triton status/wait-idle 处理，不直接 fallback 裸 `xcodebuild`。
- 已验证：先补红灯 `XcodeDiagnosticsTests`，实现后通过 `swift test --package-path CLI --filter XcodeDiagnosticsTests`；当前主工作区另有未提交 workspace-run 改动会影响更广测试，#137 需在隔离干净 worktree 验证 schema 和 build。

## Workspace post-action observation atlas transition

- `workspace run --observe-live --execute-actions` 现在会在模型候选动作成功后再次调用 live observe：initial raw `ObserveOutput` 仍写 `evidence/observations/0000.json`，post-action raw `ObserveOutput` 写 `evidence/observations/0001.json`。
- 事件流新增 action 后的 `observation.captured phase=post_action`，顺序为 `action.executed -> observation.captured(post_action) -> business.ready -> verify.checked`，让 business verification 和 Atlas transition 都有动作后屏幕证据。
- Atlas 从自环推进到真实前后帧 transition：action 后 observation 生成 `screen_0001/state_0001`，transition 从 `screen_0000` 指向 `screen_0001`；搭配 post-action runtime wait 通过时 transition 状态保持 `verified`，未验证路径仍可保持 `executed_unverified`。
- schema、`docs-linhay/dev/ai-cli-readable-control.md`、`20260706-agent-mobile-runtime-platform` space / 技术方案和对外 `tritonkit-emulator-cli-takeover` skill 已同步，后续 agent 应把 `0001.json` 和 `phase=post_action` 当成 action 后状态变化证据。
- 已验证：先补红灯 `workspace run captures post-action observation and atlas transition`，旧实现只 observe 一次且缺少 `0001.json`；实现后通过 `swift test --package-path CLI --filter WorkspaceActionExecutionTests`、`swift test --package-path CLI --filter WorkspaceRunTests`、`swift test --package-path CLI --filter WorkspaceBusinessReadinessTests`。

## Workspace observation-derived action candidate

- workspace model loop 的当前步骤候选不再固定为 `tap Continue`：runtime 会从当前 observation visibleTexts 派生 tap query，优先 `Continue` / `Start` / `Get Started` / `Next` / `Login` / `Log In` / `Sign In`，其次首个 visible text，缺失时 fallback 为 `Continue`。
- 派生出的 candidate 会贯穿 action request、`evidence/model/*request/decision/bootstrap*`、`action.executed`、Atlas transition selector 和 `export-flow` action step；model artifacts 写 `candidateSource=observation.visibleTexts` 或 fallback source，便于后续替换成真实 LLM/VLM provider 输出。
- 该切片仍是 deterministic visible-text heuristic，不是完整模型服务决策；后续 OpenAI-compatible LLM 已可驱动当前步骤 candidate，workspace VLM grounding 已可参与 tap 定位，bounded multi-step recovery 已能在 post-action checkpoint 失败后继续；完整 Atlas state merge / coverage path 仍是后续切片。
- 已验证：先补红灯 `workspace run derives explicit action candidate from observation`，旧实现执行 `Continue`，实现后通过 focused `swift test --package-path CLI --filter WorkspaceActionExecutionTests/workspaceRunDerivesExplicitActionCandidateFromObservation`、完整 `WorkspaceActionExecutionTests` 和 `WorkspaceRunTests`。

## Workspace model decision provider boundary

- `runWorkspaceRunAsync` 新增可注入 `TKWorkspaceModelDecisionProvider`：provider request 包含 mode、goal、app、actionPolicy、allowedActions、stopConditions、providerStatus、LLM/VLM provider 和当前 observation visibleTexts。
- provider 返回的 `TKWorkspaceModelDecision` 现在会驱动 candidate action/query/confidence/candidateSource，并贯穿 runtime action request、`decision-000*.json`、raw response、`action.executed`、Atlas transition 和 `flow.tritonflow.yaml`；默认 provider 仍使用 deterministic visible-text heuristic。
- model request artifact 固定写入 visibleTexts 和 runner/provider context，即使 provider 自己只返回额外 request metadata，也不会丢掉模型 grounding 输入。
- 主 runtime 把 model decision artifact writer 拆到 `CLIWorkspaceModelDecisionRuntime.swift`，`CLIWorkspaceRuntime.swift` 降到 1381 行，继续低于 1500 行治理线。
- 这一步仍不是 CLI 可配置的真实 openai-compatible / mlx-swift-lm 调用；下一步应把真实 provider invocation 接入这个 boundary，并保留同一 request/response evidence 契约。
- 已验证：先补红灯 `workspace model provider output drives explicit action candidate`，旧实现没有 provider 注入点；实现后 focused test 通过。

## Workspace OpenAI-compatible LLM decision provider

- `workspace run` 新增真实本地 LLM decision provider 配置：CLI `--llm-provider openai-compatible --llm-base-url <local /v1> --llm-model <model> [--llm-api-key-env <ENV>] [--allow-remote-llm]`，HTTP 字段 `llmProvider/llmBaseURL/llmModel/llmAPIKeyEnv/allowRemoteLLM`。
- 默认 provider 在 async workspace runtime 中会调用 OpenAI-compatible Chat Completions endpoint，prompt 输入包含 goal、app、actionPolicy、allowedActions、stopConditions、providerStatus、observationRef 和当前 visibleTexts；返回值必须是当前步骤 JSON action candidate，由 Triton 继续负责 policy/action/evidence/Atlas。
- provider preflight 现在区分 `missing_base_url`、`missing_model`、`invalid_base_url`、`remote_approval_required`、`missing_api_key` 和 `ready`；远端 LLM base URL 默认拒绝，必须显式 `--allow-remote-llm`。
- model request / provider-check artifact 会记录 redacted `llmBaseURL`、`llmModel`、`llmAPIKeyEnv` 名称和 `allowRemoteLLM`，不写 secret value。
- 这一步把 LLM 从可注入测试边界推进到 CLI/HTTP 可配置真实 provider；截至本日后续切片，VLM workspace grounding 和 bounded multi-step recovery loop 已接入，仍未完成 `mlx-swift-lm` workspace helper 和更丰富的 recovery proposal 分类。
- 已验证：先补红灯 `WorkspaceModelDecisionProviderTests` 和 `workspace run marks local openai-compatible LLM ready`，实现后通过 `swift test --package-path CLI --filter WorkspaceModelDecisionProviderTests`、`swift test --package-path CLI --filter WorkspaceRunTests`、`swift test --package-path CLI --filter WorkspaceActionExecutionTests`。

## Workspace OpenAI-compatible VLM preflight and evidence metadata

- `workspace run` 新增 OpenAI-compatible VLM provider 配置面：CLI `--vlm-provider openai-compatible --vlm-base-url <local /v1> --vlm-model <model> [--vlm-api-key-env <ENV>] [--allow-remote-vlm]`，HTTP 字段 `vlmProvider/vlmBaseURL/vlmModel/vlmAPIKeyEnv/allowRemoteVLM`。
- provider preflight 已从 `CLIWorkspaceRuntime.swift` 拆到 `CLIWorkspaceProviderPreflightRuntime.swift`，主 runtime 从 1468 行降到 1280 行，保持在 1500 行治理线以下。
- VLM preflight 现在和 LLM 一样区分 `missing_base_url`、`missing_model`、`invalid_base_url`、`remote_approval_required`、`missing_api_key` 和 `ready`；远端 VLM 默认拒绝，必须显式 `--allow-remote-vlm`，因为 screenshots / visual evidence 可能离开本机。
- provider-check 和 model request artifact 会记录 redacted `vlmBaseURL`、`vlmModel`、`vlmAPIKeyEnv` 名称和 `allowRemoteVLM`；LLM prompt 也会看到 VLM provider metadata，但当前仍不伪装为已完成 screenshot grounding。
- 该切片结束时仍未完成：workspace action path 还没有生成 coordinate contract、调用 `triton vlm ground`、写 overlay/transform/parsed point 并把 VLM point 接到 runtime tap；本日后续切片已补 workspace action VLM grounding 与 bounded multi-step recovery，`mlx-swift-lm` workspace helper 接入仍未完成。
- 已验证：先补红灯 `workspace run marks local openai-compatible VLM ready`、`workspace run requires explicit approval for remote openai-compatible VLM`、`workspace HTTP run maps openai-compatible VLM provider options`，实现后通过 `swift test --package-path CLI --filter 'WorkspaceRunTests|WorkspaceModelDecisionProviderTests'`。

## Workspace VLM-grounded action execution

- `workspace run --execute-actions` 现在在 VLM provider ready 且当前 observation 有可读本地 screenshot artifact 时，会先生成 run-local coordinate contract，调用 `TKWorkspaceVLMGroundingProvider`，按步写 `evidence/actions/vlm-000/vlm-grounding.json` / `vlm-001/vlm-grounding.json` 和 overlay / request / response，再把 grounding runtime-point 注入 runtime tap。
- action evidence `evidence/actions/action-000.json` 在该路径写 `usedVLMGrounding=true`、`proofSource=vlm.grounding+runtime.input` 和 `vlmGrounding` refs；没有可读 screenshot 时保持 bounded runtime selector path，`usedVLMGrounding=false`，不伪装 VLM 已定位。
- `runWorkspaceRunAsync` 新增 `vlmGroundingProvider` 注入点，HTTP async 入口同步透传；默认 provider 复用现有 `groundVLMTarget`，因此 OpenAI-compatible VLM 可通过同一 `--vlm-provider/--vlm-base-url/--vlm-model/--allow-remote-vlm` policy gate 参与每步 grounding。
- 新增 `CLIWorkspaceVLMGroundingRuntime.swift`，`CLIWorkspaceRuntime.swift` 仍保持 1500 行治理线以下；runID 会在 async runtime 开始时固定，确保 action 前写入的 VLM artifacts 落到最终 run 目录。
- 截至本日后续切片，bounded multi-step loop 已接入；仍未完成：VLM grounding failure 的细分 recovery proposal、Atlas state variant / coverage merge、`mlx-swift-lm` workspace helper。
- 已验证：先补红灯 `workspace run grounds tap candidate with VLM before action execution`，实现后通过 `swift test --package-path CLI --filter WorkspaceActionExecutionTests/workspaceRunGroundsTapCandidateWithVLMBeforeActionExecution`，随后通过 `swift test --package-path CLI --filter 'WorkspaceActionExecutionTests|WorkspaceRunTests|WorkspaceModelDecisionProviderTests|VLMGroundingTests'`。

## Workspace bounded recovery loop

- `workspace run --execute-actions --observe-live --business-ready-text <text> --business-ready-live-wait --max-steps N` 现在能在 post-action business checkpoint 失败后继续 bounded loop：下一轮 model decision request 使用最新 post-action observation visibleTexts，而不是停在初始 observation。
- 多步证据按 step-indexed artifact 写入：第一步 `decision-000/action-000/verify-000`，第二步 `decision-001/action-001/verify-001`；post-action observations 连续写 `evidence/observations/0001.json`、`0002.json`。
- Atlas 已能表达连续 transition 链，例如 `screen_0000 -> screen_0001 -> screen_0002`，`transition_0000` 可标记 `verification_failed`，`transition_0001` 可标记 `verified`，coverage 的 screen/state/transition count 随多步 loop 更新。
- VLM grounding artifact path 也改为按步编号：保留单步兼容的 `coordinate-contract.json` / `evidence/actions/vlm-000/*`，第二步起使用 `coordinate-contract-001.json` / `evidence/actions/vlm-001/*`，避免多步 grounding 证据覆盖。
- 这一步仍不执行模型生成的批量多步计划；每轮只允许当前步骤 action candidate，实际动作继续由 runner allowlist、Triton runtime action provider、business wait 和 evidence gate 控制。
- 仍未完成：跨 run Atlas state variant 合并、coverage path 聚合、`mlx-swift-lm` workspace helper，以及更丰富的 recovery proposal 分类。
- 已验证：先补红灯 `workspace run continues bounded loop after failed business checkpoint`，旧实现只执行一步并停在 `run.paused`；实现后该测试转绿，并通过 `swift test --package-path CLI --filter 'WorkspaceActionExecutionTests|WorkspaceRunTests|WorkspaceModelDecisionProviderTests|WorkspaceBusinessReadinessTests|VLMGroundingTests'`。

## Workspace run-local app-map projection

- `workspace run` 现在会把 run-local `atlas/atlas.json` 投影到 `atlas/app-map/`，生成 app-map `app-map.json`、screen、transition、run record、candidate path 和 workspace suite 文件。
- `workspace inspect` 响应新增可选 `appMap` 摘要，包含 `mapRef=atlas/app-map/app-map.json`、screen / transition / path count 和 `pathIds`；text 输出也显示 appMap 计数。
- bounded recovery loop 的多步链路已经可以形成完整 path，例如 `Login -> Dashboard` 会写 `paths/path-login-dashboard.json`，并保留 `sourceRuns` 指向当前 workspace run。
- 该投影是单 run map，不等同于跨 run Atlas state variant 合并；跨 run coverage path 聚合、更丰富 state / transition 级 app-map health、以及 `mlx-swift-lm` workspace helper 仍是后续切片。
- 已验证：先补红灯 `workspace run continues bounded loop after failed business checkpoint` 中的 app-map 断言，旧实现缺少 `inspect.appMap`；实现后该 focused test 通过。

## Workspace app-map merge

- `workspace merge-map <run-id> --map-dir <dir.tritonmap> [--confirm] --json` 现在可把 run-local `atlas/app-map/` 合并进长期本地 app-map。
- 合并按稳定 screen / transition / path / suite id 去重，保留并累积 path `sourceRuns` 与 `health.observedRuns/passCount/failCount`；同一个 run 重复 merge 不重复增加 path health。
- schema 已新增 `workspace.merge-map` output contract，响应模型为 `TKWorkspaceMergeMapResponse`，便于 agent 从 `triton schema --command workspace --json` 发现入口。
- 已验证：先补红灯 `workspace app map merge accumulates repeated runs`，旧实现缺少 `mergeWorkspaceRunAppMap`；实现后 focused test 通过。
