# 2026-07-08

## Agent mobile runtime

- 继续推进 `20260706-agent-mobile-runtime-platform`：补齐 iOS Demo 的真实 LLM/VLM + Atlas map 全链 smoke，而不只停留在手动 action 和 run-local pathCount=0 的 baseline。
- `workspace run` 支持 initial observation fixture 与 post-action live observe 同时存在。fixture 用于首轮模型和 VLM grounding，`observeLive=true` 仍会在 action 后采集 live observation，写入 `post_action` event、Atlas transition 和 app-map。
- 修复 iOS simulator workspace runtime target：target resolve 给出的 raw UDID 只适合 host app lifecycle；runtime observe/action/assert 必须归一化为 `triton:ios-simulator:<udid>/app:<bundle-id>`。
- 长期 app-map 已保留 workspace VLM provenance。VLM-grounded action 会进入 transition/path `vlmHealth`，path 标记 `source=vlm-assisted`、`requiresVLM=true`，`map paths/path show/export-flow` suggested command 会带 `--allow-vlm`。
- app-map export-flow 支持混合 replay selector：有 runtime point 时导出 `tap.point`，普通 selector 导出 `tap.text`，只有 transition 自身带 VLM provenance 时才导出 `tap.target + grounding: vlm + provider`，因此导出的 `.tritontest.yaml` 能通过 `triton test validate`。
- 真实 smoke 通过：`TRITON_BIN=CLI/.build/debug/triton TRITON_IOS_DEMO_SIMULATOR=83407554-53AB-45B4-A0C1-D59F34E26A67 TRITON_VERIFY_OUT_DIR=.build/ios-demo-e2e-smoke-map-current9 docs-linhay/scripts/verify-ios-demo-e2e-smoke.sh`。覆盖 `xcode run`、runtime ready、manual action/evidence、workspace OpenAI-compatible LLM mock、VLM grounding、bounded recovery loop、post-action assert、run-local app-map、`workspace merge-map`、`map paths`、`map path show`、`map export-flow` 和 `test validate`。

