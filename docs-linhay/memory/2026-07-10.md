# 2026-07-10

## GitHub Issues #144/#145 Action Guidance

- 处理 open issues #144 与 #145：收敛 agent-facing action guidance，明确 retired root action commands 不再作为产品入口，示例与 recovery hint 使用 `triton act ...`。
- CLI/schema 更新：
  - `triton tap --help` 等 retired root action 现在返回恢复提示，例如 `triton act tap ...`。
  - `triton act swipe --help` 说明 iOS 使用 embedded runtime target，target 来自 `triton list --json`，不要把 host selector `sim:<udid>` 当作 runtime gesture target。
  - `triton schema --command act swipe --json` 暴露 iOS runtime target 说明，`act` usageForms 的可复制样例使用完整 `triton act ...`。
  - runtime transport error hint 从 retired `triton schema --command tap --json` 改为 `triton schema --command act tap --json`。
- 文档/skill 同步：
  - README、`docs-linhay/dev/ai-cli-readable-control.md`、`docs-linhay/dev/device-control-from-baguette.md`、internal host-simulator skill、public emulator/dev-feedback/real-project skills 都已同步 iOS runtime target 与 Harmony lifecycle 参数形态。
  - Harmony app lifecycle 示例显式使用 `--platform harmony --bundle <bundle> --ability <ability>`，并说明不要使用 iOS `--bundle-id` 形态。
- 新增 space：`docs-linhay/spaces/20260710-issues-144-145-action-docs/README.md`，记录 BDD 场景与验收边界。`docs-linhay/scripts/create-space.sh` 在当前仓库不存在，因此按 AGENTS 路径规范手动创建目录与 README。
- 验证：
  - 通过：`swift test --package-path CLI --filter CLIHelpTests`。
  - 通过：`swift test --package-path CLI --filter actSchemaUsageFormsAreFullyQualifiedForAgentReuse`。
  - 通过：`swift test --package-path CLI --filter actSchemaDocumentsIOSRuntimeTargetForSwipe`。
  - 通过：`swift test --package-path CLI --filter schemaLookupSupportsNestedCommandSelectors`。
  - 通过：`swift test --package-path CLI --filter appSchemaExposesHostAppSubcommandsUsedByPlans`。
  - 通过：`swift build --package-path CLI --scratch-path .build/cli --product triton`。
  - 通过：`git diff --check`。
  - 通过：`docs-linhay/scripts/check-docs.sh`。
  - 已运行但未全绿：`swift test --package-path CLI --filter SchemaFactSourceWorkflowTests` 与 `swift test --package-path CLI --filter SchemaFactSourceContractTests`。失败集中在既有 `device proxy` / `device bridge` / `device start|stop` 子命令 schema-backed 校验、`device` output contract 覆盖和 subcommand 参数引用缺口；本轮新增 action/help/schema 测试均已通过。
