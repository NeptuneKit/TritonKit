# 2026-07-11

## Spaces 索引

- 新增 `docs-linhay/spaces/README.md` 作为 space 与独立 worktree 状态的固定总入口；两个待集成 worktree 当日稍后已合入，主仓现有 105 个 space。
- 索引状态统一为：进行中、待集成、明确待办、规划冻结、待收口、已归档。
- 当前重点未完成项包括 Agent Mobile Runtime Platform、iOS Embedded SDK Expansion、Test Recorder Replay 和 Simulator Target Simplification。
- `20260701-host-framebuffer-stream` 和 `20260702-cross-platform-framebuffer-stream` 已有对应实现，但原计划勾选与现状不一致，因此标记为“待收口”而不是“未实现”。
- 新增 `docs-linhay/memory/MEMORY.md`，记住 spaces 总索引的固定路径和同步规则。
- 同步更新 `AGENTS.md` 与 `.agents/skills/tritonkit-ops-governance/SKILL.md`，将索引维护提升为后续新建、状态变化、worktree 集成和 space 收口时的固定治理动作。
- `docs-linhay/scripts/check-docs.sh` 新增索引门禁：总索引必须存在，且每个主仓 space 必须各有且只有一个标准 Markdown 链接；当前覆盖 105 个 space。

## 2026-07-07 worktree 集成

- 审查 `codex/20260707-issue-140-redaction-preflight`：public issue preflight、占位符规则和 skill package 门禁在 `main` 中没有等价实现，决定保留；原提交 `ab6f1263` 已 cherry-pick 为 `e0c4cc4e`。
- 审查 `codex/20260707-github-issue-batch`：CombineCocoa-backed `UIControl` tap 安全过滤、Harmony/Android platform-focused doctor/plan 和 WebView static guard 在 `main` 中没有等价实现，决定保留；原提交 `9c0b63b0` 已 cherry-pick 为 `e95c9f95`。
- 两次集成都只在 `docs-linhay/memory/2026-07-07.md` 产生追加冲突；解决方式为保留后续主线 memory，并追加对应 issue 记录。
- 两个 space 已从总索引“待集成”移入“历史归档”；主仓 space 数量更新为 105。
- 集成后定向测试、WebView static guard、skill package 校验和 `docs-linhay/scripts/verify.sh --local` 全部通过；完整门禁覆盖 225 项 Swift 测试、Release CLI、Harmony/iOS smoke 与 iOS Simulator 构建。
- 两个已完成 worktree 已删除；对应 `codex/20260707-*` 分支继续保留，作为原始提交和审计入口。

## Spaces 路线裁决

- 对原“当前队列”8 个 space 重新按产品边界和真实证据裁决，不沿用陈旧勾选状态。
- 执行：`20260521-ios-embedded-sdk-expansion` 只补 WebView-aware tap 真实 harness 动态闭环；`20260525-simulator-target-simplification` 只补多开 iOS + Harmony alias smoke。
- 待定：`20260622-test-recorder-replay` 暂停新增实现，先裁决是否并入 `workspace run`、`.tritontest`、replay/evidence 和 Atlas 契约。
- 废弃：`20260527-revyl-cli-agent-entrypoint-research` 不再按原 M1-M6 独立实施，已有价值已被后续 agent-facing 能力吸收。
- 已归档：Agent Mobile Runtime Platform 一期、host framebuffer 原型、cross-platform framebuffer 原型和 Issue 61 Android adapter。
- 验证事实：`WebViewRouteTests` 18 项通过；`DeviceCrossPlatformTests` 93 项通过，确认 Issue 61 的旧编译 blocker 已消失。

## Spaces 执行收口

- `20260521-ios-embedded-sdk-expansion` 已归档：真实 iOS Simulator WebView harness 先得到 `DOM click dispatched` 但 `submitted=true` 未出现的红灯，再为 Demo `#submit` 增加业务状态变化；最终输出 `status=passed`、`trusted=false`、`verification.textMatched=true`，且 edge/navigation 全链回归通过。
- `verify-ios-webview-harness.sh` 已迁移到当前 `triton act find/tap` 命令，并把 `submitted=true` 点击状态加入静态与动态门禁。
- `20260525-simulator-target-simplification` 已归档：真实环境存在两个 Booted iOS Simulator 和一个 Connected Harmony Emulator；无 selector 返回 `ambiguous_target`，iOS/Harmony alias 分别命中指定 UDID 与 HDC target，截图和 iOS App launch 成功。
- Harmony `observe tree --device <alias>` smoke 暴露缺省 platform 被强制解释为 iOS；已改为 platform 可选推断，并用 `SelectorFlagTests` 覆盖“alias 推断平台”和“显式 platform 保留过滤器”，真实 Harmony tree 返回 93 个节点。
- 临时 alias 已清理，`.triton/host-targets.json` 恢复为空；`--bundle` 反向过滤明确不实现，批量 fan-out 或新 selector 能力必须另建 space。
