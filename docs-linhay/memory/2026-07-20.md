# 2026-07-20

## GitHub Issue #146：Simulator 录屏不能只信 container duration

- `triton sim record --duration 3` 可等待约三秒并由 `simctl` 正常退出，但 artifact 仍可能只有一帧、可播放时长约 `0.066667s`；host command 退出码不是视频证据有效性的充分条件。
- AVFoundation 的 `AVAsset.duration` 与 `AVAssetTrack.timeRange.duration` 会反映被拉长的 timeline，本例均约三秒，仍会掩盖一帧 artifact。录屏验收必须使用 `AVSampleCursor` 遍历 encoded samples，以首尾 PTS + sample duration 计算实际时长。
- 成功契约新增 requested/actual/minimum/container/track duration、file bytes 与 `durationValidation=passed`；actual 低于 requested 的 75% 返回 `sim_record_truncated`，媒体 sample 不可读取返回 `sim_record_invalid_artifact`。
- 真实 smoke 证明旧路径的假成功已被改为单个失败 JSON 和非零退出码；`ffprobe` 仅作为 artifact 独立核对，不作为 Triton 产品入口。
- #146 的正式本地门禁、docs 与 public skill package 校验通过。扩展 CLI suite 仍有既存的 `device proxy/alias/bridge` schema 与暂停 `testrec` 断言，不属于录屏切片；本次新增 schema failure 已全部清零。

## GitHub Issue #148：Xcode simulator selector 必须驱动 destination

- `ResolvedXcodeInvocation.simulatorUDID` 正确不代表 `xcodebuild` 使用了同一 target；workspace 保存的旧 destination 若先于显式 `--simulator` 解析，会造成 summary 与 source command 身份分裂。
- destination 优先级固定为显式 `--destination` > real-device selector > 显式 `--simulator` > workspace default destination；只有继承默认 simulator 且没有显式 selector 时，才保留 workspace destination。
- simulator selector 不能一律写入 `id=`：UUID 与 `sim:<UUID>` 使用 `id=<UUID>`，人类可读设备名使用 `name=<name>`。`xcode use` 和 settings/build/test/run 必须共用同一合成函数。
- 聚焦 Xcode 命令测试按 BDD/TDD 从缺少 helper 的编译红灯转绿，10 个测试覆盖旧 destination 覆盖、UUID、前缀、名称与显式 destination 优先级。

## GitHub Issue #151：Evidence partial capture 必须是单一失败契约

- 聚合型 CLI 不能调用会自行打印 JSON 并抛 `ExitCode` 的叶子 resolver；evidence artifact 采集改用静默 target resolver，只在最终 entrypoint 输出一次 manifest。
- `TKEvidenceManifest` 新增向后兼容的 `partial` 与顶层 `error`，`TKEvidenceSkippedArtifact` 新增可选结构化 `error`。旧 manifest 未含 `partial` 时按 `skipped` 是否为空推导。
- 预期 unsupported artifact 仍是 `ok:true, partial:true`；请求或写入失败是 `ok:false, partial:true`、`error.code=evidence_capture_partial`，打印 manifest 后以非零退出。agent 不得因 status/list/version 等局部 artifact 已存在就宣称证据完整。
- 断线测试模拟 list 成功后五个 runtime artifact 全部返回 `target_not_found`，单次 JSON 解码成功，stdout/persisted manifest 一致，且每个 `skipped[].error` 保留原始稳定错误码。

## GitHub Issue #147：Harmony wait 的 transfer timeout 必须服从总 deadline

- host-side poll 的总 timeout 不能只包住循环；其内部 dump/recv 子进程也必须使用剩余预算。Harmony 每次 layout capture 上限固定为 `min(5s, remaining wait budget)`，dump 与 recv 再共享同一个绝对截止时间。
- `hdc file recv` 的瞬时 timeout 是可重试观察失败，不应立即映射为泛化 `request_failed`。`HostHarmonyWaitOutput` 通过 `transientFailureCount`、`lastTransientError` 与 `sourceCommands` 保留错误码、remote path、底层命令及日志路径；持续失败最终返回标准 `timedOut=true` envelope。
- `--gone` 不能把“本轮布局没采到”解释成“文本已消失”；只有成功解析 layout 后才能作 absence 判断。
- CLI wait 与 smoke wait 原先存在两套循环，本轮收敛为共享 helper，避免 timeout、diagnostics 与 condition 语义继续漂移。
- Triton-first 事实显示本机唯一 DevEco emulator 为 Offline，因此仅完成可重复的 Harmony host smoke，没有绕过 Triton 运行裸 HDC 或宣称真实 emulator 验收。

## GitHub Issue #149：iOS Simulator wait 与 host AX 事实源对齐

- 同一命令含 embedded 与 host mode 时，schema 的 `requiresServer` 不能按 embedded 路径一刀切为 true；只要 `--platform ios|android|harmony` 可离线执行，command-level 契约就应标为 false，再由 runtimeScope 和 mode-specific error 表达 embedded 依赖。
- `wait --platform ios` 必须复用 `observeIOSHostAX`，使 `observe tree -> wait -> host input fallback` 读取同一 host-layout 事实源；不能在 CLI help 接受 iOS 后仍把 selector 交给 embedded `/targets`。
- iOS host wait 首期严格限定 ready Simulator 与 text/exists/gone，真机、idle、hierarchy-change、predicate 不隐式降级。`--gone` 只在成功取得 AX 树后判定 absence。
- `host.ios-wait` 直接携带 resolved `HostDeviceTarget`、optional role、matched node、poll/elapsed/timeout 和 sourceCommands；capability 使用独立 `ios-simulator-host-wait`，确保 server/runtime disconnected 时仍可发现并执行。
- 真实 booted iOS 26.5 Simulator 在 server 关闭时完成 match、timeout、gone 三条 smoke，证明修复不依赖 embedded runtime。

## GitHub Issue #150：Xcode discovery 返回的 Package.swift 必须可执行

- `xcode discover` 把 `Package.swift` 作为推荐 container 返回后，`xcode use/schemes/settings/build/test/run` 必须能用 `--package` 直接消费；否则 discovery 只是不可执行提示，会迫使 agent 回退裸 `xcodebuild`。
- Xcode 对 standalone Swift package 没有 `-packagePath` 参数；正确 host 适配是让 `xcodebuild` 从 package 目录启动。因此 `TKHostCommand` 新增可审计 `workingDirectory`，短命与 JSONL streaming 两套 process runner 必须共用同一配置函数。
- 只修通用 host runner 不够：真实 smoke 证明 `schemes` 已识别临时 package，而 streaming `build` 仍会误用调用 worktree。新增 cwd runner 测试后，standalone package 的 generic iOS Simulator 构建同时产出 arm64/x86_64 并返回 `ok=true`。
- repo-local Xcode defaults 增加互斥 `package` container；显式 workspace/project/package 会整体覆盖旧 container defaults，避免显式 package 与历史 project default 被拼成假冲突。
- `schema --command xcode.build --json` 与既有 `xcode build` 形式统一收窄父 schema，build contract 暴露 `--package` 与 `package` summary 字段。

## GitHub Issue #152：真机 launch env 必须使用 devicectl 显式 JSON

- host source command 中出现 `DEVICECTL_CHILD_KEY=<redacted>` 只证明 caller environment 被设置，不证明 CoreDevice 会把原始 key 注入 App；iOS 26.5.2 真实回归已证明该路径可静默失效。
- 当前 `devicectl device process launch` 提供 `--environment-variables <JSON dictionary>`，且明确其优先于 `DEVICECTL_CHILD_*`。Triton 真机 launch 改用该显式 flag，Simulator 继续使用 `SIMCTL_CHILD_*`。
- secret 不只会出现在 process environment，也可能进入 argv。`TKHostCommand` 因此新增 argv index 脱敏集合；实际执行保留 JSON，`sourceCommand` 仅保留 flag 并把整个 JSON 参数显示为 `<redacted>`。
- connected 真机取证必须区分 target readiness 与 App 内动态断言。本机没有可读 `ProcessInfo.environment` 的 Triton/Demo/Bench sentinel App，因此完成 non-mutating target/schema/command contract 验证，不以启动无关 App 冒充端到端成功。

## GitHub Issue #153：真机 container artifact 必须通过 pull 契约取回

- Simulator `app container` 返回 host filesystem path，iOS 真机 container 没有可打印的 host path；两者不能用同一语义伪装。真机使用独立 `triton app pull`，底层映射 `devicectl device copy from`。
- `appDataContainer` 的 domain identifier 是 bundle id，`appGroupDataContainer` 是 group id；CLI 分别要求 `--bundle-id` 与 `--group-id`，避免让 agent 猜测两类身份可互换。
- host artifact overwrite 采用同目录 staging + 校验后替换。命令失败、目录未授权、超过 `--max-bytes` 或含 symlink/special file 时只清理 staging，不先破坏已有 final destination。
- 真机 readiness、DDI readiness 与设备 unlock 是三层不同事实。本轮仓库 TestFixture 已通过 Triton build/install，devicectl 启用了 DDI services，但 launch 因设备锁定返回 `device_locked`；随后 pull 返回 `ddi_missing`。因此静态成功契约由 fake runner 覆盖，真实动态 sentinel 不伪造通过。
- `ios-real-app-pull` 必须同时进入 capability matrix、group/requiredBy/nextAction/evidence metadata 与 app schema；只注册 subcommand 会造成 agent 无法从 bootstrap 事实源发现。新增 artifact/failure 名称也必须复用或扩展 schema taxonomy。
- #153 本地 `verify.sh --local` 全绿；完整 CLI suite 的 26 个 issue 均落在既存 schema/testrec/Harmony timing/xcode-use 基线，新增 `HostAppPullTests` 9 项、CLI help 6 项和 shared builder 38 项均通过。

## GitHub Issue #154：Public skill 命令必须在打包前对齐 CLI schema

- v0.2.8 官方 `tritonkit-skills.tar.gz`、对应 tag 与当前 Git 历史都不包含 `tritonkit-runtime`；该名称来自官方 bundle 之外，不能据此新增或伪造 release skill 成员。
- 当前四个 public skills 的 action/AX 示例大多已迁移到 `triton act` / `triton debug`，但扫描发现 `tritonkit-real-project-regression` 仍残留一个顶层 `triton geometry`，已改为 `triton debug geometry`。
- `package-public-skills.py` 现在会在 tarball 创建前扫描全部 public Markdown。root 必须存在于 `public-skill-command-schema.json`，`act` / `debug` 的 literal 子命令还要精确存在；unknown root 会基于 schema 子命令反查并建议当前层级。
- CLI test 将 command snapshot 与 `commandSchemas()` 全量比对，package test 注入 `triton find` / `triton ax` 并要求非零退出及 `triton act find` / `triton debug ax` 建议，形成 schema 变更、skill 文档、release packaging 三方闭环。
- #154 本地 `verify.sh --local` 全绿；完整 CLI suite 的新增 snapshot/help suite 均通过，余下 21 个 issue 仍是既存 schema fact、暂停 testrec 与旧 xcode-use 断言基线。

## v0.2.12 Release Prep

- `v0.2.12` 汇总 GitHub issues #146–#154；发布范围保持为 macOS arm64/x86_64 `triton` CLI、checksum manifest、四个 public skills 合并包，以及同步版本的 SwiftPM/CocoaPods/Web 入口。
- BDD 验收：tag 前版本入口一致、本地门禁与 `main` validate 全绿；tag 后双架构资产、checksum、Homebrew、CLI/skill 版本与仓库外 packaged Web HTTP smoke 全部通过，才判定完整发布。
- TDD 红灯：`docs-linhay/scripts/verify-release-package-versions.sh 0.2.12` 因 `TritonKit.podspec` 仍为 `0.2.11` 按预期失败。
- 版本入口已同步到 `0.2.12`：`TritonKit.podspec`、`Web/package.json`、`Web/package-lock.json`。CLI build info 与 public skill metadata 继续由 tag workflow 注入，不手工改写。
- CocoaPods 本轮只做 podspec 版本对齐与 lint，不自动推送 trunk；若需 trunk 发布，必须作为单独外部写操作执行并记录。
- tag 前本地门禁：
  - 通过：`docs-linhay/scripts/verify-release-package-versions.sh 0.2.12`、`verify-release-automation.sh`、`verify-version-stamping.sh`、`verify-skill-package.sh`。
  - 通过：`npm --prefix Web run build`；Vite 保留既有 chunk size warning。
  - 通过：`pod lib lint TritonKit.podspec --allow-warnings --skip-tests`；CocoaPods 仅输出 Xcode/Metal toolchain note。
  - 通过：`TRITON_VERIFY_XCODE=0 docs-linhay/scripts/verify.sh --local`；根包 226 项测试、release CLI build/smoke、Harmony host smoke、iOS runtime observe smoke、文档与 whitespace 门禁全绿。Xcode build 按发布治理命令显式跳过，本轮 issue 交付阶段已有对应 Xcode/Simulator 回归证据。
- 发布结果：
  - 版本准备提交 `c7e59dd4 chore(release): prepare v0.2.12` 已推送 `main`，远端 CI run `29752373168` 的 contracts、podspec、Swift tests 与聚合 Validate 全绿。
  - 首次 `release.sh` 二次 preflight 在 `Release CLI smoke` 后非零退出，确认本地/远端均未创建 tag；逐项复核 version、九个 schema、两条 validation failure 与 fake-xcodebuild smoke 全绿后，从同一正式入口重跑成功。未绕过门禁，未移动已发布 tag。
  - GitHub Release `https://github.com/NeptuneKit/TritonKit/releases/tag/v0.2.12` 已发布；Release run `29753221099` 的 contracts、arm64、x86_64 交叉编译、asset publish 与两次 Homebrew tap 更新全部成功。
  - 下载四个 release assets 后，`shasum -a 256 -c tritonkit_checksums.txt` 对 arm64 CLI、x86_64 CLI、skill bundle 全部通过；`file` 分别证明 Mach-O arm64/x86_64。
  - release arm64 CLI `version --json` 为 `0.2.12`；public skill `BUILD_INFO.json` 为 `v0.2.12` / commit `c7e59dd4`，四个 `SKILL.md` metadata 均为 `0.2.12`。
  - Homebrew 已从 `0.2.9` 升级到 `0.2.12`，`brew test NeptuneKit/tap/triton` 与安装后 `triton version --json` 通过。
  - 仓库外 `/opt/homebrew/bin/triton web --print-command --json` 返回 `mode=packaged`；实际 packaged Web 在 `127.0.0.1:34127` 启动，`web status/doctor` 健康，根页面 HTTP 200 且引用打包 JS/CSS，随后正常停止。
  - CocoaPods 只完成 `TritonKit.podspec` 版本对齐与 lint，没有推送 CocoaPods trunk。

## GitHub Issue #155：Simulator process console 必须与 unified log 分源

- `simctl spawn <udid> log stream` 只能证明 unified logging artifact；Swift runtime 等直接写到 App process stdout/stderr 的诊断必须走独立进程控制台契约，不能靠 OSLog 成功推断已覆盖。
- `triton sim app-console` 先做 `appinfo` preflight，再以 `launch --console-pty --terminate-running-process` 重启 App；合并 PTY 流明确标记 `sourcesCaptured=[process-stdout,process-stderr]`、`streamLayout=merged-pty`。`triton sim logs` 则固定标记 `sourcesCaptured=[unified-log]`。
- 进程控制台是敏感 artifact：JSON 不内联正文，env value 必须在 source command 脱敏；capture 同时受 duration 与 max bytes 约束，达到字节上限仍需持续 drain 子进程 pipe，避免死锁。
- 极短 duration 可能在 `simctl` 验证 bundle 前就发 interrupt，造成缺失 bundle 的空 artifact 假成功。App metadata preflight 是必要的成功前置事实，失败返回 `sim_app_console_failed` 并删除 partial output。
- 真实 TestFixture 通过 opt-in sentinel 同时写 stdout/stderr，Triton artifact 捕获两行且约两秒 bounded 结束；独立 `sim logs` 只报告 unified-log source。该回归不需要裸 `simctl` 动作，raw help 仅在已发布 schema 明确缺失能力后用于确认 host tool contract。

## v0.2.13 Release Prep

- `v0.2.13` 仅增量发布 #155：iOS Simulator App process stdout/stderr bounded capture、unified-log source 明确化，以及配套 CLI schema/capability/public skill 文档。
- 旧 `v0.2.12` tag 保持不可变；新版本入口同步为 `TritonKit.podspec`、`Web/package.json` 与 `Web/package-lock.json` 的 `0.2.13`，CLI build info 与 public skill `BUILD_INFO.json` 继续由 tag workflow 注入。
- BDD/TDD release 红灯：版本同步前运行 `docs-linhay/scripts/verify-release-package-versions.sh 0.2.13`，按预期因 podspec 仍为 `0.2.12` 返回非零。
- CocoaPods 本轮继续只做 podspec lint，不自动推送 trunk；完整发布必须等待 arm64/x86_64 CLI、skill bundle、checksum manifest、两次 tap 更新与安装后 smoke 全部通过。
- tag 前版本、release automation、version stamping、public skill package 与 docs/whitespace 门禁通过；`npm --prefix Web run build` 通过并仅保留既有 chunk-size warning，`pod lib lint TritonKit.podspec --allow-warnings --skip-tests` 通过并仅保留 Xcode/Metal toolchain note。
