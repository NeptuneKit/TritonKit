# 2026-07-21

## v0.2.13 Release Completion

- `v0.2.13` annotated tag 保持指向版本准备提交 `4be243ee7e3b9ce61a2770d61c459bda590a8cff`，没有移动 `v0.2.12`。
- tag 前 main CI run `29789060365` 的 contracts、Swift tests、CLI release build、podspec 与聚合 Validate 全绿；正式 Release workflow run `29789559363` 全绿。
- GitHub Release `https://github.com/NeptuneKit/TritonKit/releases/tag/v0.2.13` 已包含 `triton-macos-arm64.tar.gz`、`triton-macos-x86_64.tar.gz`、`tritonkit-skills.tar.gz` 与 `tritonkit_checksums.txt`。
- 通过资产 API 完整下载后，arm64/x86_64/skill 三个 SHA-256 均与 manifest 一致；`file` 分别证明 Mach-O arm64/x86_64。首次 `gh release download` 校验发生在两个大文件仍传输中，partial download 的 checksum 失败不属于线上资产损坏，等待完整下载后已排除。
- arm64 release CLI `version --json` 为 `0.2.13`；`schema --command sim --json` 包含 `app-console`、`host.simulator-logs` 与 `host.simulator-app-process-console`。
- public skill bundle 的 `BUILD_INFO.json` 为 `v0.2.13` / commit `4be243ee`，四个 `SKILL.md` metadata 均为 `0.2.13`。
- Homebrew 已从 `0.2.12` 升级到 `0.2.13`，`brew test NeptuneKit/tap/triton` 和安装后 `version --json` 通过。
- 仓库外 `/opt/homebrew/bin/triton web` 以 `mode=packaged` 在 `127.0.0.1:34127` 启动；`web status/doctor` healthy，根页面 HTTP 200 并引用打包 JS/CSS，随后正常停止且端口关闭。
- CocoaPods 只完成 podspec 版本对齐与 lint，没有推送 trunk。
- 发布完成后 GitHub 新增 #156 与 #157；它们不回写或移动 `v0.2.13`，按独立 space/下一 patch 继续处理。

## GitHub Issue #156：UIButton Primary-Action Menu

- iOS 17.4+ embedded runtime 已通过 public `UIButton.performPrimaryAction()` 打开只配置 `menu` / `showsMenuAsPrimaryAction` 的按钮，稳定 strategy 为 `button-primary-menu-action`。
- 菜单打开必须再以可见 action title 验证；TestFixture 中 `Fixture Menu Action` 由 `verify text-exists` 证明存在。
- public UIKit 没有安全接口直接执行已呈现的 `UIAction`，因此菜单项标题点击返回 `unsupported_capability` / `button-primary-menu-item-unsupported`，需要 item selection 时使用 host HID 或 App 自有 semantic DEBUG action。
- 修复 CLI input failure 双输出：已经打印的失败 `TKInputResult` 直接以 `ExitCode.failure` 结束，不再生成第二个泛化 `request_failed`。
- merge commit `c44273e5` 已推送 `main`，线上 CI run `29797634616` 全绿后关闭 GitHub #156；版本交付进入下一 patch，不移动 `v0.2.13`。

## GitHub Issue #157：UITableViewCell Selection Callback

- table cell ancestor helper 已由异步 submitted 改为同步 `willSelectRowAt → selectRow → selection state check → didSelectRowAt`，并尊重 redirect/deny。
- 成功稳定返回 `ancestor-table-cell-selection` 与 `Selected UITableViewCell ancestor and invoked delegate callback`；真实 TestFixture 的 label/ancestor 与 coordinate 两条入口均由 `Fixture Table Selection Complete` 后置文本证明 callback 已执行。
- UIKit focused 4 项先红后绿；根包 226 项与 CLI schema 18 项通过。测试过程同时修复 `TKObjectRegistry` stale ObjectIdentifier mapping，避免已释放对象地址复用后新 window/view 获得失效 OID。
- `triton xcode test` 全量执行中本次 4 项仍通过；同时如实保留 35 个既有 key-window/concurrency 与 Package cwd 基线失败，证据为 `/private/tmp/triton-issue157-full.xcresult`，不纳入 #157 的修复范围。
- feature commit `99fae7a5` 通过 merge commit `3981467f` 进入 `main`；线上 CI run `29799367673` 全绿，完成 issue 评论留证并关闭 #157，下一步发布 `v0.2.14`。

## GitHub Issue #158：iOS Real-Device Screenshot Scope

- 0.2.13 会把 ready `ios-real:*` screenshot selector 送进 simctl，返回 `host_action_failed` / `Invalid device` 与 Simulator-only hint；Triton-first 基线保存在 `/private/tmp/triton-issue-158-baseline/`。
- 当前 Xcode devicectl 没有 screenshot/capture command，因此 host screenshot 的正确边界是 iOS Simulator-only。新实现会在任何平台工具前为真机返回 `unsupported_scope` 与 schema nextAction，并在 doctor/capabilities/help/schema 中暴露同一边界。
- focused 行为/schema/doctor/capability 测试已由红转绿；新 CLI 对真实 ready iOS selector 返回预期失败，同时 booted Simulator screenshot 仍成功，证据在 `/private/tmp/triton-issue-158-green/`。
- release CLI 复验 real selector 为 `unsupported_scope` / diagnose schema nextAction，Simulator PNG 非空；本地标准门禁根包 226 项、release build/smoke、Harmony/iOS runtime smoke 与 public skill package 全部通过。非标准 scratch 的 CLI 全量仍保留 43 个既有路径/schema/Harmony timing 基线 issue，本次相关测试均通过。
