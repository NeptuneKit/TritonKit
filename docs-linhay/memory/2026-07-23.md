# 2026-07-23

## GitHub Issue #164 / #165 交付审计续跑

- #164 的 evidence 双截图实现已按代码、schema、redaction 与测试逐层复审：iOS Simulator 的 host-composited framebuffer 保持 `screenshot.png` 主视觉事实，embedded runtime 图像独立保存为 `screenshot.runtime`；host 失败返回 `failed-partial`、`host_screenshot_unavailable` 与 Triton-first 恢复动作，非 iOS target 不猜测 Simulator identity。
- #165 的 timeout 恢复契约补齐直接测试：workspace、project、package 三种 container 均在 `xcode_schemes_timeout` 的 `nextAction` 中保留；timeout 至少提升至 300 秒，并继续使用 `triton xcode schemes --disable-automatic-package-resolution --json`。
- `XcodeDiscover.parse([])` 新增 CLI 层默认 `maxDepth == 8` 断言，避免只有 shared API 测试而 CLI 默认漂移。
- 复审后的 `XcodeCommandTests` 与 `FailureDiagnosticsTests` 合并运行 30 项全部通过；`git diff --check` 通过。
- 后续权限恢复后，`gh` 登录与 GitHub 网络均可用，线上确认只剩 #164/#165；项目内部 host-simulator / Xcode workflow skills 已完成写回。
- 原样 `docs-linhay/scripts/verify.sh --local` 完整通过：根 Swift 包 231/231、release CLI build/smoke、Harmony host smoke、iOS runtime observe smoke、iOS Simulator build、docs 与 diff 门禁均为绿灯。
- #164 真实 evidence smoke 已连接到带 `simulatorUDID` 的 embedded runtime，证明 host framebuffer 以 `scope=host-simulator`、`source=simctl-framebuffer`、`fidelity=full-screen` 成为主 artifact；连接 App 的旧 JPEG runtime 辅助图被 PNG 契约拒绝为 failed-partial，未覆盖 host 主视觉事实。
- #165 真实 release smoke 已完成根仓 discovery 与 Swift package schemes listing；300 秒 timeout 和 `-disableAutomaticPackageResolution` 均进入实际 `xcodebuild` source command，并成功返回三个 package schemes。
- #164 由 `e489dcfd`、#165 由 `b3cdd40c` 分别提交并推送到 `main`；GitHub CI `29973762696` 的 Swift tests、CLI release build、Podspec 与 release contracts 全绿。
- CI 通过后已关闭 GitHub #164 与 #165；对应 space 与总索引同步归档，线上 open issue 将以最终查询结果为准。

## v0.2.15 发布准备

- 最新已发布 tag 为 `v0.2.14`；本轮不移动历史 tag，按下一 patch `v0.2.15` 发布 `v0.2.14..main` 中 #159–#165 的修复。
- 发布版本红灯已确认：`verify-release-package-versions.sh 0.2.15` 首次因 `TritonKit.podspec` 仍为 `0.2.14` 失败；随后将 podspec、`Web/package.json` 与 `Web/package-lock.json` 统一更新为 `0.2.15`。
- 发布面保持 macOS 双架构 CLI、bundled Web、checksum、四个 public skills、Homebrew、SwiftPM tag、CocoaPods podspec 与 Web manifest 一致；CocoaPods 本轮只 lint，不自动推送 trunk。
- 发版前 focused 门禁已通过：版本一致性、release automation、Web production build、public skill package、docs、diff 与 `pod lib lint TritonKit.podspec --allow-warnings --skip-tests`；`TRITON_VERIFY_XCODE=0 verify.sh --local` 进一步通过 231 项 Swift tests、release CLI build/smoke、Harmony host smoke 与 iOS runtime observe smoke。

## v0.2.15 发布完成

- 版本准备提交 `29798f0d` 的 main CI `29974377146` 全绿后，创建 annotated tag `v0.2.15`；tag 固定指向该提交，未移动任何历史 tag。
- Release workflow `29974713055` 全绿：arm64/x86_64 CLI、arm64 首发、x86_64 backfill、checksum/public skill bundle与两次 Homebrew tap 更新全部成功。
- 重新下载的 `triton-macos-arm64.tar.gz`、`triton-macos-x86_64.tar.gz`、`tritonkit-skills.tar.gz` 与 `tritonkit_checksums.txt` 全部匹配；`file` 分别证明 CLI 为 arm64/x86_64 Mach-O，两份 `version --json` 均为 `0.2.15`。
- public skill bundle 顶层为 `TritonKit.skills/`，`BUILD_INFO.json` 为 `v0.2.15` / `29798f0d`，四个必需 skill 的 `metadata.version` 均为 `0.2.15`。
- 远端 formula 同时包含 arm64/x86_64 URL 与 checksum；Homebrew 已从 `0.2.14` 升级到 `0.2.15`，`brew test NeptuneKit/tap/triton` 通过。
- 仓库外安装版 `triton web --print-command --json` 返回 `mode=packaged`；实际 Web server 在 `127.0.0.1:34127` 返回 HTTP 200 与 `Triton Inspector` 页面。CocoaPods 本轮仅 lint，未推送 trunk。

## 会话沉淀

- “处理线上全部 issue”不是一次批量关闭动作，而是可重复的队列闭环：固定 open 快照、逐 issue 完成 BDD/TDD 与独立审计、等待 main CI、关闭并归档，再重新查询；最终必须同时满足 open 查询为空与最后一次归档 CI 通过。该模式已写入内部 `tritonkit-ops-governance`。
- Release 公开资产校验必须等待 `gh release download` 进程完整退出后再执行 checksum。本轮曾在下载仍进行时读取到较小的截断 tarball 并产生假 mismatch；等待进程结束后，文件 size、checksum 与 `gzip -t` 全部通过。该恢复判定已写入内部 release skill 与 CI release 技术文档。
- 两条规则都属于 GitHub issue / release 领域流程，优先更新既有内部 skills；没有形成新的 repo-wide 通用约束，因此本轮不修改 `AGENTS.md`，也不新增 skill 入口。

## Space 编号索引治理

- 新增 `docs-linhay/spaces/INDEX.md`，为现存 125 个 space 完成 `SP-001-<topic>` 至 `SP-125-<topic>` 的唯一编号登记，并逐条链接到兼容目录 README。
- 本轮仅完成索引与规范同步：旧日期 slug 目录保留以防断开历史 README、证据和 Git 链接，物理迁移进度为 0/125；后续必须按独立批次迁移并同步 `INDEX.md`、路线总览和引用。
- `AGENTS.md`、项目记忆与内部治理 / 巡航 / subagent skill 已统一新建 space、branch、worktree 使用 `SP-<三位序号>-<topic>`。

## AGENTS 精简与并行默认

- 将 repo-wide `AGENTS.md` 从 197 行收敛为 85 行，只保留路径、space、验证、安全、产品边界、发布和 DoD 等长期硬约束；具体流程继续下沉到对应 skill 或 `docs-linhay/dev/`。
- subagent 策略调整为：任务可安全拆分且并行有明显收益时，主控可主动调度；简单、紧耦合或共享副作用任务保持单 agent / 串行。主控仍承担集成、验证和最终 DoD，远端写入仍须用户明确授权。
