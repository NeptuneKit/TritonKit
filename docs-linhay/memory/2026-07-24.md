# 2026-07-24

## 项目开发进度整理

- 已核验公开基线：`v0.2.15` 已于 2026-07-23 发布；双架构 CLI、checksum、public skills、Homebrew 和 packaged Web smoke 有发布/CI 证据。CocoaPods 本轮只 lint，未发布 trunk。
- GitHub 当前有 3 个 open issue、无 open PR：#166（真机 JPEG runtime screenshot 被拒绝并丢失字节）、#168（iOS 真机 terminate 缺 PID）、#167（Xcode device alias 在构建后才失败）。建议恢复顺序为 #166 → #168 → #167；开始实现前为每项建立新的 SP 编号 space。
- 路线总览没有“执行”中的 space；Test Recorder Replay 仍为待定，必须先裁决独立 `testrec` 产品面是否并入现有 workspace/test/replay，暂停新增实现。
- 审计发现两项既有边界债务：`triton serve` 默认 bind 与 loopback 治理口径不一致；Web 声称只读但已有 POST 写入口。只记录为后续裁决项，本轮未改产品代码。
- 主工作区干净但 `main` 比 `origin/main` ahead 2，均为已提交未推送的文档/治理记录。本轮没有推送。
- 保留 `../TritonKit-worktrees/20260722-issue-164-evidence-simulator-screenshot-fidelity`：其中有 7 个未提交 Swift/测试文件修改，且分支与当前 `main` 分叉，意图未确认；不得直接合并或删除。另有 `codex/20260707-github-issue-batch` 和 `codex/20260707-issue-140-redaction-preflight` 的本地独有提交，后续单独审计。
- 已生成项目级回顾 `docs-linhay/dev/20260724-development-progress-recap.md`；同步修正 Test Recorder 旧停止点的误导性表述。本轮为文档整理，未重跑产品功能门禁。
