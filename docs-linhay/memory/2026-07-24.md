# 2026-07-24

## SP-126 Testrec Convergence 接手

- 用户指定 Test Recorder Replay 为唯一待裁决方向，并要求建立 space、由 Codex 接手评估和指定后续方案。
- 新建规范 space：`SP-126-testrec-convergence`；branch：`feat/SP-126-testrec-convergence`；worktree：`../TritonKit-worktrees/SP-126-testrec-convergence/`；基线为 `main@931645ed`。
- 代码审计结论：`testrec` 的 `.tritontestcase`、确定性编译、quality/redaction、page matcher 有保留价值，但 `local-simulated` 不是真实执行；`test run` 已有 live primitive executor、前后 observation 和 evidence；workspace 已有 target/lifecycle/Atlas 与 `.tritontest.yaml` 导出方向。
- 裁决为 Hybrid：`testrec` 收敛为录制/编译/导入兼容层，`test` 成为唯一真实执行与 verdict 入口，workspace 后续复用同一合同，replay 保持低层 smoke。
- 首个实施切片限定为 `.tritontestcase -> triton test import -> test validate` 的 fail-closed seam；可信基线通过后，先做 iOS Simulator / Debug embedded runtime 的单动作真实证据闭环，Android 必须等显式 host test primitive adapter。暂停 `testrec local-device`、矩阵、live network policy、系统级监听和删除迁移。
- 本次只建立方案与文档，不提交、不合入、不删除历史代码或兼容 surface。

## 产品级 12 个月路线裁决

- 用户将长期完整规划委托给 Codex；复用 SP-126 作为当前执行锚点，并在 `docs-linhay/plans/20260724-agent-native-local-execution-roadmap-v01.md` 写入项目级路线。
- 唯一产品主线定为“本机移动 App 的 agent-safe execution-and-proof substrate”：能力发现、受控动作、业务验证、failure/recovery、evidence 与 replay 是一个闭环；不把项目扩张成设备云、通用自治平台、Web/Wails 控制台或第二套 testrec executor。
- 代码审计纠正了此前的首平台假设：当前 `test run` live executor 与 workspace 实际动作路径以 iOS embedded runtime 为可信起点；Android/Harmony 现有 host/discovery/observation 不等于 test runtime。因此 SP-126 的第一个真实纵切改为 iOS Simulator，Android 被后置为显式 host test primitive adapter。
- 阶段门槛采用 VRCR、ECR、FER、ORR、ZTTC；不要求 evidence 文件字节完全相同，避免 timestamp/run ID/截图编码造成伪 flaky。
- 当前顺序：先定责 #164 遗留 worktree，串行处理 #166/#168/#167 和 serve/Web 边界债，再进入 iOS canonical test/evidence、testrec importer、真实项目证明、Android adapter。未经 owner 确认不合并或删除 #164 WIP。

## 交接 Luna 执行

- 用户要求整理并提交，将后续执行移交 Luna。本次以 `feat/SP-126-testrec-convergence` 的提交作为路线检查点；具体 hash 以该分支最新提交为准。
- 已写入可直接执行的交接：先对 #164 dirty worktree 做只读定责，再以独立 space/worktree 串行处理可靠性与事实债；在 iOS canonical 证据门槛通过前，不启动 `test import` 实现或任何第二执行器。
- 本次只提交 SP-126 路线、space 索引、历史入口、长期计划与交接文档；不推送、不合入、不触碰主工作区未提交文档，也不改动 #164 worktree。
