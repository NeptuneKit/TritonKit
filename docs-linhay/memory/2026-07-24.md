# 2026-07-24

## SP-126 Testrec Convergence 接手

- 用户指定 Test Recorder Replay 为唯一待裁决方向，并要求建立 space、由 Codex 接手评估和指定后续方案。
- 新建规范 space：`SP-126-testrec-convergence`；branch：`feat/SP-126-testrec-convergence`；worktree：`../TritonKit-worktrees/SP-126-testrec-convergence/`；基线为 `main@931645ed`。
- 代码审计结论：`testrec` 的 `.tritontestcase`、确定性编译、quality/redaction、page matcher 有保留价值，但 `local-simulated` 不是真实执行；`test run` 已有 live primitive executor、前后 observation 和 evidence；workspace 已有 target/lifecycle/Atlas 与 `.tritontest.yaml` 导出方向。
- 裁决为 Hybrid：`testrec` 收敛为录制/编译/导入兼容层，`test` 成为唯一真实执行与 verdict 入口，workspace 后续复用同一合同，replay 保持低层 smoke。
- 首个实施切片限定为 `.tritontestcase -> triton test import -> test validate` 的 fail-closed seam；可信基线通过后，先做 iOS Simulator / Debug embedded runtime 的单动作真实证据闭环，Android 必须等显式 host test primitive adapter。暂停 `testrec local-device`、矩阵、live network policy、系统级监听和删除迁移。
- 本次只建立方案与文档，不提交、不合入、不删除历史代码或兼容 surface。

## 产品级 12 个月路线裁决

- 用户将长期完整规划委托给 Codex；复用 SP-126 作为当前执行锚点，并在 `docs-linhay/plans/20260724-agent-native-local-execution-roadmap-v01.md` 写入项目级路线。
- 唯一产品主线定为“本机移动 App 的 agent-safe execution-and-proof substrate”：能力发现、受控动作、业务验证、failure/recovery、evidence 与 replay 是一个闭环；不把项目扩张成设备云、通用自治平台、Web/Wails 控制台或第二套 testrec executor。
- 代码审计纠正了此前的首平台假设：当前 `test run` live executor 与 workspace 实际动作路径以 iOS embedded runtime 为可信起点；Android/Harmony 现有 host/discovery/observation 不等于 test runtime。因此 SP-126 的第一个真实纵切改为 iOS Simulator，Android 被后置为显式 host test primitive adapter。
- 阶段门槛采用 VRCR、ECR、FER、ORR、ZTTC；不要求 evidence 文件字节完全相同，避免 timestamp/run ID/截图编码造成伪 flaky。
- 当前顺序：先定责 #164 遗留 worktree，串行处理 #166/#168/#167 和 serve/Web 边界债，再进入 iOS canonical test/evidence、testrec importer、真实项目证明、Android adapter。未经 owner 确认不合并或删除 #164 WIP。

## 交接 Luna 执行

- 用户要求整理并提交，将后续执行移交 Luna。本次以 `feat/SP-126-testrec-convergence` 的提交作为路线检查点；具体 hash 以该分支最新提交为准。
- 已写入可直接执行的交接：先对 #164 dirty worktree 做只读定责，再以独立 space/worktree 串行处理可靠性与事实债；在 iOS canonical 证据门槛通过前，不启动 `test import` 实现或任何第二执行器。
- 本次只提交 SP-126 路线、space 索引、历史入口、长期计划与交接文档；不推送、不合入、不触碰主工作区未提交文档，也不改动 #164 worktree。

## SP-127 Issue #168 安全 fallback

- 主控审核通过 #164 首轮只读报告，确认 `e489dcfd` 是 main 的 canonical screenshot evidence contract；#164 独立 dirty worktree 继续隔离，不清理、不借用。
- 新建规范 space：`SP-127-issue-168-ios-real-device-terminate-pid`；branch：`feat/SP-127-issue-168-ios-real-device-terminate-pid`；worktree：`../TritonKit-worktrees/SP-127-issue-168-ios-real-device-terminate-pid/`；基线为 `feat/SP-126-testrec-convergence@5f6c2f6f`。
- #168 当前 real-device terminate 直接将 bundle ID 传给 `devicectl device process terminate`，而 Xcode 26.6 help 要求 `--pid`。官方本地 CoreDevice 模型审计没有证明 `bundleIdentifier -> App executable -> running-process executable -> PID` 的一一关系：`AppInfo` 有 bundleIdentifier 与可选 URL 但没有 executable；`ProcessInformation` 有可选 executable URL 与 processIdentifier 但没有 bundleIdentifier；没有官方 join/路径正常化规则。`device info apps` 与 `device info processes` 的 help 也没有提供可表达该链的字段关系。
- 可定位证据：`/Library/Developer/PrivateFrameworks/CoreDevice.framework/Versions/A/Frameworks/CoreDeviceClientJSONSupport.framework/Versions/A/CoreDeviceClientJSONSupport` 的本地 Swift 符号；CoreDevice `devicectl` binary `/Library/Developer/PrivateFrameworks/CoreDevice.framework/Versions/A/Resources/bin/devicectl` 的 `strings -a -t x` 偏移 `0x128150`（`Processes.swift`）、`0x128220`（running processes 命令）、`0x12d1f0`（versioned/stable JSON file interface）；以及 `xcrun devicectl device info apps/processes --help`。结论是 **no evidence**，不实现 `info processes` parser/resolver，不用 URL、basename、path、进程名或宿主 `ps` 猜 PID。
- 已保存 Triton-first status/doctor/capabilities/plan：server `127.0.0.1:19421` 不可达，runtime/real device 未连接；后续使用 fake/devicectl adapter tests，真实设备 smoke 如不可用只记 blocker。
- 控制流为 `HostAppTerminate.run -> planHostAppTerminate -> TKDevicectlCommand.terminateApp`；因此采用安全 fallback：shared `terminateApp` 只接受明确 PID 并生成 `--pid <pid>`；iOS real plan 在 builder 前抛出稳定 `app_terminate_pid_resolution_unavailable`，不调用 `info processes`、不产生 terminate command；Simulator/Android/Harmony 继续使用既有 builder。
- recovery detail 保持单一 JSON envelope，使用 `selection.target.id` 这样的 public selector 生成 `app launch --device <selector> --scope real --platform ios --bundle-id <id> --json`。hint/message 明确这是用户可选择的冷重启替代操作，**不表示 terminate 成功**；raw target 不进入 message、hint 或 nextAction。
- app root 与 `terminate` 子命令 schema 均登记 `app_terminate_pid_resolution_unavailable`，子命令 summary 明确 iOS real 无 verified PID 时 fail closed。SP-127 不扩展 evidence、testrec/importer、Android、Web/Wails 或第二执行器。
- TDD/red 事实已保留：旧 bundleID-only / process-JSON 假设先产生编译红态，随后改为显式 PID builder、real plan fail-closed、recovery/schema focused tests；本轮 production 与 docs 已更新，等待未提交 checkpoint 审核。
- focused 验证：`TKHostAdapterModelsTests` 38 项通过；`HostAppTerminatePIDTests` 4 项通过（含 simulator/Android/Harmony plan regression）；`FailureDiagnosticsTests` 12 项通过。`SchemaFactSourceTests` 全 suite 仍有 12 项 baseline schema contract failures（proxy/target alias/selector/coverage 等，与本 slice 无关），不在本轮修复。
- 未运行 server、真实 Xcode/设备动作；live target 不可用，真实 smoke 保留 blocker。当前 SP-127 worktree 保持未 stage、未 commit，等待主控 checkpoint 审核。
## SP-128 #167 Xcode 真机 alias preflight

- 主控审核通过后建立规范 space：`SP-128-issue-167-xcode-device-alias-preflight`；branch：`feat/SP-128-issue-167-xcode-device-alias-preflight`；独立 worktree；基线为 `feat/SP-126-testrec-convergence@5f6c2f6f`。
- 只读审计确认 `runXcodeRealDeviceBuildInstallLaunch` 当前在 `runXcodeBuild` 和 build-settings product resolution 之后才调用 `resolveHostDeviceSelection`。因此子目录 alias store 缺失时会先执行昂贵 build/tool invocation，再返回 target failure。
- 本 slice 的实现边界是最小的 selection preflight -> build -> product -> install/launch 重排，并以窄 closure seam 做 focused test；不改变 alias store、discovery、install/launch 或其他 issue 的行为。
- BDD 重点：不可解析 alias 返回单一 machine-readable `target_not_found` 与 recovery，且 build closure 不调用；可解析 alias/显式 selector 保持既有 xcodebuild SDK、destination 和 argv。实际 selection resolver 可达 `target_not_found`、`ambiguous_target`（`booted` 多候选）和 `target_platform_mismatch`（alias 平台不符），三者已纳入 Xcode schema/recovery；`parameter_conflict` 不由该固定 selection request 产生，未预占。
- XcodeBuildMCP 参考文档在当前 checkout 缺失，保留为环境文档风险；不在本 slice 修文档、不跑真实 xcodebuild/设备。测试使用本 worktree 独立 scratch；不 push、PR、merge、tag、release 或清理 worktree/branch。
- 第二轮窄 TDD 将 Xcode root/run 的 target failure recovery 收窄为 `triton target resolve <selector> --platform ios --scope real --ready --json`，其它 schema（含 `target`）继续使用 generic `triton target resolve <selector> --json`；同时移除 `target_platform_mismatch` hint 中对 Xcode 不存在的 `--platform` 操作建议。
- 第二轮 red 先由缺失 detail helper 的 focused assertion 暴露；补实现后 `XcodeCommandTests` 21/21、`FailureDiagnosticsTests` 13/13 通过，未启动真实 Xcode、设备或 server。`SchemaFactSourceTests` 119 tests 中的 12 个既有 failures 集中在 device proxy、sim app-console、device 参数/selector 等无关 contract，未在本 slice 扩修。
## SP-129 Serve Loopback Default

- 新建规范 space：`SP-129-serve-loopback-default`；branch：`feat/SP-129-serve-loopback-default`；worktree：`../TritonKit-worktrees/SP-129-serve-loopback-default/`；基线为 `feat/SP-126-testrec-convergence@5f6c2f6f`。
- 可信基线审计确认 `Serve.host`、中文 help 和 bootstrap schema 曾分别声明 `0.0.0.0`，而 recovery/plan 默认建议已是 `127.0.0.1:19421`；本切片只收敛 serve 无 `--host` 的默认值，显式非 loopback host 保持兼容。
- BDD/TDD focused tests 覆盖 parser 默认、显式 `--host 0.0.0.0`、中文 help、serve schema、默认 recovery/action 与显式 host 传递；测试不调用 `Serve.run()`，不启动 server、Bonjour 或设备。
- 验证结果：`ServeCommandTests` 6/6 通过；纯 recovery helper `ReplayCommandTests/replayRecoveryCommandsIncludeFailureErrorNextAction` 1/1 通过；release `triton` 在独立 scratch 编译通过，直接读取 serve schema 也确认 host default 为 `127.0.0.1`。既有 `SchemaFactSourceTests` 12 个失败集中在 network-proxy/device proxy、sim app-console 和设备 selector/output contract，与本 diff 无关；完整 Replay suite 的 2 个失败是测试寻找 `CLI/.build` 产物与独立 scratch 约束冲突。
- `git diff --check` 通过；`check-docs.sh` / `verify.sh --ci-docs` 因基线缺少并发保留的 SP-127、SP-128 而在连续编号检查停止，未改 checker、未写占位 space，主控集成后需重跑。
- Bonjour 风险保留：`Serve.run()` 仍发布 `_tritonkit-server._tcp.local`；loopback bind 不等于无 LAN 广播，本切片不改变 discovery 语义，也不宣称已消除广播。
- 本切片禁止触碰 CLIWebRuntime/Web/evidence/testrec/Android/#164/#166/#167/#168，禁止远端操作；只保留本地 checkpoint commit，待主控集成。
