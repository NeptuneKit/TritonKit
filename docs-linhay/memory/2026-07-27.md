# 2026-07-27

## SP-137 #171 safe UICollectionViewCell tap（完成，待受控集成）

- 在独立 `feat/SP-137-issue-171-safe-collection-tap` / `SP-137-issue-171-safe-collection-tap` worktree 完成 #171：embedded runtime 不可再把异步 collection `selectItem` / delegate 投递当作已完成成功；cell fallback 固定返回单一 `TKInputResult` 的 `unsupported_capability`，不调用 selection/delegate。
- BDD 覆盖 text smart、ancestor、coordinate collection hit 的 `unsupported_capability` / 无 selection / 无 delegate，以及 cell 内更近 UIControl、可 accessibility 激活 gesture 的真实成功；审查修复后 gesture 搜索以当前 cell 为硬边界，cell 外 container/window activation 不会越界触发。table 同步选择与 host iOS AX 语义保持。
- `input.result` schema 已公开 `error` / `error.code` / `error.message`，`act input` failure codes 覆盖 `unsupported_capability`；CLI output 4/4、route 18/18 和目标 schema contract 通过，离线 schema CLI 已实证。
- `git diff --check`、`check-docs.sh` 与 `verify.sh --ci-docs` 已通过。
- macOS 根 SwiftPM 编译了相关 source/test target 但条件排除 UIKit test（0 个执行）；iOS UIKit 场景保留为测试资产。全量 `SchemaFactSourceTests` 的 12 条 device/proxy schema failure 为既有无关缺口，本 slice 未越界处理。
- 范围限定 runtime input、input-result/tap schema、CLI output/schema tests 与 space 文档；未启动 server、Simulator、Xcode workflow、设备或 `test run`，未读取/修改 #164 WIP，未做远端写入。

## SP-126～136 本地候选栈收口（集成前历史快照）

- 已将 2026-07-24 的开发进度审计保留为历史快照，并新增 follow-up，记录后续 SP-126～136 的本地 checkpoint；本轮不合并、不推送、不发布，也不删除任何 worktree 或 branch。
- 产品方向已裁决为 Hybrid：`testrec` 是录制/编译/兼容层，`test` 是唯一真实执行、evidence 与 verdict 入口，`workspace` 后续复用同一合同，`replay` 保持低层 smoke。`testrec local-simulated`、dry-run 与 matrix 均已标为 offline diagnostic，不能作为真实 verdict 或 reliability sample。
- 最新本地候选为 `feat/SP-136-ios-simulator-reliability-collection-preflight@16e5ec81`。该 first-parent 链包含真机 PID fail-closed、Xcode target preflight、loopback 默认、JPEG->PNG、canonical/imported iOS Simulator proof、offline reliability gate、compatibility boundary 与 collection preflight。
- 已验证的真实纵切限于 dedicated iOS Simulator fixture；真实 3 flow × 20 采样尚未执行。SP-136 的 `ready_to_collect` 只证明 private collection 的离线合同完整，不代表 server、Simulator、reset、evidence 或 reliability gate 已执行/通过。
- 所有 feature worktree 在审计时干净；`main@931645ed` 仍未含 SP-126～136，且保留其既有文档 WIP。#164 dirty evidence worktree 持续隔离，未读取、修改、合并或清理。
- 当前安全下一步是：在用户授权后先受控集成候选栈并重跑匹配门禁；之后另建 live-harness space，显式拥有 Simulator / loopback server / reset receipt / private evidence。环境所有权不明或隐私处置不足时停止，不自动采样。

## SP-126～136 受控集成（本地）

- 已从 `main@76f5f0ac` 在独立 `codex/integration-sp126-136` worktree 合并本地候选 `feat/SP-136-ios-simulator-reliability-collection-preflight@16e5ec81`；冲突仅限历史 memory / testrec 说明，按 Hybrid 裁决保留历史边界并未改写 #164 evidence WIP。
- 合并前后的 focused suites、release CLI schema / omitted-argument envelope、`check-docs.sh`、`verify.sh --ci-docs` 与 `TRITON_VERIFY_XCODE=0 verify.sh --local` 均已通过；总门禁包含根 SwiftPM 231/231、release CLI build/smoke、Harmony host smoke、iOS runtime observe smoke 和 docs/whitespace。Xcode Simulator build 因显式 `TRITON_VERIFY_XCODE=0` 跳过，不将其表述为已验证。
- 本轮仅创建本地整合提交；未 push、PR、tag、release、关闭 issue 或清理 worktree / branch。真实 3 flow × 20 采样仍未开始，须在后继 live harness 的显式环境所有权与 reset/evidence gate 下执行。

## SP-131 iOS Simulator canonical proof

- 在 SP-130 JPEG normalizer checkpoint `a7404033` 上新建独立 worktree `SP-131-ios-simulator-canonical-proof`；不触碰 main 的用户未提交文档，也不读取、修改或整合 #164 dirty evidence WIP。
- 路线裁决保持不变：下一条有价值的主线是先重新证明手写 `.tritontest.yaml` 在 iOS Simulator + Debug embedded runtime 上能产生真实 `test run` verdict/evidence，再进入 SP-126 的 `.tritontestcase -> test import -> test validate`；不提前扩 Android、Web/Wails、真机或第二个 recorder executor。
- Triton-first preflight：`doctor --json` 如实显示 loopback runtime server 尚未运行；`sim list --json` 发现可用 booted Simulator，也存在专用的 TritonKit Simulator。后续只能使用专用 fixture target，并在启动 server/fixture 前后保存 machine-readable facts。
- canonical flow 固定为仓内 `TritonKitTestFixture` 的 `Fixture Login -> Go Home -> Fixture Home`，复用已验证的 runtime-point 和 P0D 计划结构；本轮 evidence 必须是当前运行产生的结果，历史 P0D evidence 仅作对照。
- 真实 proof 已一次通过：受控 `triton xcode run` 在 dedicated iPhone 17 / iOS 26.5 Simulator 构建、安装、启动 Debug fixture；只在 `triton list --json` 的 bundle ID + dedicated Simulator 双重匹配后显式传入 runtime target。`test validate` 返回 6-step normalized plan；`test run` 返回 `passed`（6 steps、2 AX assertions、4 observations、43 events、0 failures、1822 ms），`test report`、`evidence inspect/summary` 同时验证 target、Login -> Home 状态变化、PNG、AX 与 hierarchy artifacts。
- evidence 只保留为本机临时 `.tritonevidence`，未加入 Git；它是 `ios-private` profile 并含 12 个 sensitive artifacts。人工核对了 Login 与 Home 两张主截图；该 proof 只说明 runtime App-layer capture/AX/hierarchy，不能替代 #164 的 host-composited fidelity 结论。
- proof 后已停止 loopback server，并通过 `triton sim shutdown` 把专用 Simulator 恢复为 Shutdown；没有使用或改变其他已启动 Simulator / 用户 App。fixture 安装仅留在专用 Simulator；#164 仍未读取、修改或整合。
- focused regression：独立 scratch 的首轮 `TestValidationTests` 有 2 个 CLI contract case 因测试 helper 未找到默认 `.build` 可执行文件而失败；用同一独立 CLI 通过 `TRITON_CLI_PATH` 显式注入后，`TestValidationTests` 12/12、`TestRunExecutionTests` 9/9、`EvidenceBundleTests` 25/25 通过。该 helper 路径假设是既有测试基础设施限制，不属于本 space 的生产改动。
- SP-131 已完成有限 canonical proof；下一实现 slice 回到 SP-126 Hybrid 主线的 `.tritontestcase -> test import -> test validate`，不提前扩 Android、Web/Wails、真机或第二 executor。

## SP-132 testrec import seam

- 已从 SP-131 checkpoint `6f700b13` 新建独立 worktree `SP-132-testrec-import-seam`。P0 仅实现 offline importer，不启动 imported plan 的 runtime/device run；SP-131 的单条真实 iOS proof 继续作为未来 P1 的唯一候选，不伪称已完成可靠性矩阵。
- 初始代码审计确认 v1 `.tritontestcase` manifest 只有 sourcePlatform、没有 bundle identity；`compiled-contract.json` 只可靠提供 action、source path、targetText/inputText、page/network quality 等编译信息。因此 importer 必须要求 `--bundle-id`，且只映射现有 test primitive 可保真的 `tap(text)`、`assert(text)`、`screenshot`；`type/paste` 缺 target/focus 合同，不可猜测。
- P0 关键裁决：`redactionStatus=pending` 不是 blocker，因为现有 `.tritontestcase` 没有 review 状态流；实际 `qualityFindings`（尤其 `contract.redaction`/privacy）、截断和敏感/不可映射数据才阻断。source `ios` 也不能静默改写为 Simulator，调用者必须明确 `--device-platform ios-simulator`。
- SP-132 已完成本地 P0 importer：`triton test import <case> --output <plan> --bundle-id <bundle-id> --device-platform ios-simulator --json` 只读取 one-shot `compiled-contract.json` bytes，先内存 validate 再以 no-clobber publish 写 YAML。source manifest/capabilities/compiled artifacts canonical containment，output 必须位于 source package 外；symlink escape、既有/并发 output 都 fail closed。只映射 tap/assert/screenshot 加 launch，无法保真的 action 一律 `unmapped_contract_feature`；FNV-1a 仅为稳定身份，不是签名。
- typed provenance 已从 import YAML 贯通 `test validate` 和既有 `test run` 的 `normalized-plan.json`，只包含 importer/source kind/platform/package-relative contract ref，不包含绝对路径或敏感原文。success response 只发 source/output filename 与 command template；error 不插入 untrusted action/sourcePath 或底层错误。P0 只用 fake executor 做保留回归，不启动 imported plan、服务或设备。
- focused 验证通过：`TestImportTests` 11/11、`TestValidationTests` 13/13、`TestCreateFromSessionTests` 2/2、`TestRunExecutionTests` 9/9、`FailureDiagnosticsTests` 13/13、`SchemaFactSourceCapabilityTests` 22/22。`TestRecorderContractTests` 有 1 条既有 local-device recovery code 环境差异（38/39），`SchemaFactSourceContractTests` 有 6 条既有 device/app-console schema failure；均非 importer 路径，P0 不扩修。
- 最终 `TRITON_VERIFY_XCODE=0 docs-linhay/scripts/verify.sh --local` 已通过：根 SwiftPM 231/231、release CLI build/smoke、Harmony host smoke、iOS runtime observe smoke、docs 与 whitespace 均通过；遵守纯 offline P0 边界，显式跳过 Xcode Simulator build。

## SP-133 imported iOS Simulator proof

- 已从 SP-132 checkpoint `b065b3f0` 建立独立 worktree `SP-133-imported-ios-simulator-proof`，只审计一条 imported plan 的真实 iOS Simulator evidence/provenance 纵切；在 compatibility、共享服务与 dedicated target 空闲事实确认前，不启动 server、fixture 或 Simulator。
- 只读审计确认：仓内没有提交的 `.tritontestcase` fixture，但可用既有 `testrec start/event/stop/compile` 纯离线生成一个最小 compiled source（screenshot、exact AX assert `Fixture Login`、tap `Go Home`、screenshot、assert `Fixture Home`，以及两条 route evidence）。它语义上等价于 SP-131 flow，但不保留 runtime-point 坐标；首次真实 AX-text tap 结果是本 P1 的唯一产品风险，失败只记录 blocker，不改 importer 或回退 simulated executor。
- provenance 权威在 live evidence 的 `normalized-plan.json`；`test report` / `evidence inspect` / `evidence summary` 只经 `run.planRef` 与 artifact 间接关联，不能替代逐字段比对。现有 `evidence redact` 仍可能保留 normalized plan、run events 和 accessibility 内容，故本轮 evidence 仅本地留存，公开交接只能写脱敏摘要。
- 离线 CLI 实证已完成：隔离 session store 下生成 5-action/2-route source，`testrec compile` 为 deterministic-offline、无 quality finding；`test import` 与 `test validate --emit-normalized-plan` 得到 6-step AX semantic plan 和一致 typed provenance。独立 scratch 的 `TestImportTests` 11/11、`TestValidationTests` 13/13、`TestRunExecutionTests` 9/9 通过。
- 真正 P1 runtime proof 本轮未启动：Triton-first status/doctor 显示 `127.0.0.1:19421` 已连接一个非本任务 runtime，候选 dedicated Simulator 则是 Shutdown 且没有本 space ownership marker。遵守共享状态串行规则，不复用/停止 server、不 boot/选择 Simulator；后续需要空闲 server lane 与明确 target 确认后，从新鲜临时根重做 preflight。

- 随后的授权 run 从新鲜临时根重新开始：preflight 确认 19421 不可达、Xcode lane 空闲且专用 Simulator 可用后，才启动自管 loopback server；server 初始 `targetCount=0`，随后 `triton xcode run` 启动 Debug fixture，`triton list` 只返回 bundle 与 Simulator identity 双重匹配的一个 target。
- 真实 imported-plan proof 通过：source 保持 5-action/2-route compiled contract，import/validate 生成 6-step plan；`test run` 返回 passed（6 steps、2 AX assertions、4 observations、43 events、0 failures）。`test report` 显示 exact AX `Fixture Login -> Go Home -> Fixture Home`，其中首次真实 AX-text `Go Home` tap 产生 Login 到 Home 的可观察变化；没有使用 `local-simulated` 或 fake executor。
- evidence 的 `normalized-plan.json` 与 import/validate 的 typed provenance 逐字段一致，`run.planRef` 链接它；`evidence inspect/summary` 确认 runtime target、PNG、AX、hierarchy 与 completed verdict。evidence 含 sensitive artifacts，redaction 仍不适合外发，故整个临时根已移入本机废纸篓作可恢复处置且未进入 Git。
- 收尾已验证：只中断本轮 server，随后 19421 不可达；只 shutdown 本轮 boot 的专用 Simulator，状态恢复 Shutdown；Xcode lane 无遗留进程。SP-133 完成 P1 有限 proof，不将这条 fixture 结果扩张为可靠性矩阵、workspace、Android、真机或外部项目承诺。

## SP-134 iOS Simulator reliability gate

- 路线裁决采用可靠性门槛先行：SP-133 仅是一条 imported iOS Simulator proof；根据已登记路线，workspace live 接入必须等三个冻结 flow 的 ECR/FER/ORR 有可复核样本后再独立立项。
- 已从 `feat/SP-133-imported-ios-simulator-proof@19e2c35f` 新建独立 `SP-134-ios-simulator-reliability-gate` worktree。首个代码切片仅实现只读、privacy-safe 的 `triton test reliability` 报告；不启动 server、Simulator、fixture、Xcode 或 second executor。
- 关键前提已记录：当前 `test run` 的 launch 只解析已连接 runtime、不重置 App；任何未来 3 flow × 20 样本必须有显式 initial-state/reset contract、专用 target 与新鲜 private evidence。没有 failure sample 时 FER 不能虚报通过；ORR 不比较 timestamp/run ID/截图字节，但必须检测 verdict、step/artifact/failure taxonomy、target 与初态漂移。
- 已完成离线 `triton test reliability --samples <private.json> --json`：sample manifest、privacy-safe DTO、bundle containment/regular-file 校验、`run.started -> 唯一末尾 run.finished` 序列、manifest count/summary/verdict、manifest-declared normalized-plan / runtime-target sidecar、canonical iOS Simulator runtime target / manifest target / plan bundle 绑定、逐 step command/finish 覆盖（只要求 live runner 实际会产生 runtime observation 的步骤），以及 ECR/FER/ORR/`stop_expansion` gate 都留在 CLI 内；不启动 server、App、Simulator、Xcode、testrec replay 或第二 executor。
- 审查后收紧为 fail-closed：同一 canonical evidence bundle、event run ID 或 reset identity 的全部关联样本均不完整；partial/skipped evidence、缺 event/observation count、空执行日志、未知或 kind 不一致的 plan step、无 recovery/ref 的失败也不计 ECR。runtime target 必须是已连接、带 Simulator UDID 且 ID 由 UDID / bundle canonical 推导的 iOS target，并与 manifest target ID / connected / bundle 和 plan bundle 交叉绑定；runtime sidecar 必须由 manifest 中 matching artifact 声明。每个 `artifact.created` 都按 kind 和 canonical resolved path 绑定到 manifest inventory，observation 与 terminal failure artifact ref 也必须由 manifest 声明，不能用 bundle 内孤立文件虚报 ECR / FER。重启、暂停或中途终止的 event 序列不作为终态样本。FER 的分母覆盖所有已识别 non-passed run，分子只计与终态失败步骤 `command.executed -> failure.recorded -> step.finished` 区间绑定的完整可解释记录；损坏、partial 或未知 failure 都留在分母，不能抬高 FER。ORR 比较绑定后的 evidence target，而不是自报 token；报告只输出每报告 `flow_001` 形式匿名编号、plan digest、枚举 taxonomy、计数和 blocker，绝不回传私有 evidence 路径、bundle、target、run ID、selector、可见文本、failure message、artifact ref 或截图字节。终态失败仅用 SP-134 本地、版本化 failure-type taxonomy 评估 recovery；其中 `assert_screenshot_baseline_missing`、`text_not_found` 与 `vlm_step_not_allowed` 已有明确定向，未知 type 继续 fail closed，不能误称为既有 failure 模型携带的字段。
- focused 验证：`TestReliabilityRuntimeTests` 25/25（含 duplicate/partial、完整失败 FER、canonical/connected target 与 manifest sidecar binding、event/observation/failure manifest binding、真实 `../debug/...` refs、baseline/text recovery、event count、terminal failure 归因、真实 launch 步骤语义、plan tamper、重启/暂停序列与 privacy 回归）、`TestImportTests` 11/11、`TestRunExecutionTests` 9/9、`TestValidationTests` 13/13、`FailureDiagnosticsTests` 13/13、`SchemaFactSourceCapabilityTests` 22/22 均通过；缺少 `--samples` 的 release CLI 返回唯一 JSON `missing_required_field` envelope、exit 1。最终 `verify.sh --ci-docs`、`check-docs.sh`、`git diff --check` 均通过。测试使用独立 scratch 与显式本地 CLI 路径；未运行会启动真实 runtime/设备的总门禁。
- 本地 checkpoint 已提交：`031be660 feat(test): add reliability gate`。仅写入独立 SP-134 worktree；未 push、PR、merge、tag、release 或触碰 #164 WIP。

## SP-135 testrec compatibility guidance

- 计划裁决采用 SP-135：先把 `testrec replay --dry-run`、`local-simulated` 与 matrix 的旧兼容 `ready/passed` 输出明确标记为 offline diagnostic，不可作为真实 test verdict 或 reliability sample；真实路径仍唯一是 `test import -> test validate -> test run`。
- 不把两条相邻审计混为一项：未来 collection-preflight 需要三条冻结 imported flow、canonical target binding 与 reset receipt，当前尚不具备，登记为后继候选而不是用合成 fixture 伪造采样能力。
- 已从 `feat/SP-134-ios-simulator-reliability-gate@21a57399` 新建独立 `SP-135-testrec-compatibility-guidance` worktree；本 slice 只做 offline DTO/schema/capability/doc contract，不启动 server、Simulator、Xcode、设备或 `test run`，且不读取/修改 #164 WIP。
- 实现已将 dry-run、local-simulated run、matrix root/target 和 executor profile 统一加上 optional `verdictBoundary`；新输出固定为 `offline-diagnostic`、非真实 verdict、不可进入 reliability gate，旧 `replay-result.json` 无字段仍可 decode。`local-device` profile 不再承诺未来 testrec executor，capability `testrec-replay-local-simulated` 直接给出占位符化 `test import` migration action。
- TDD red 首先证明 DTO 缺字段；focused green 通过新增 simulated boundary / legacy decode、HTTP handler boundary、testrec schema、`TestImportTests` 11/11、`SchemaFactSourceCapabilityTests` 22/22 和 `FailureDiagnosticsTests` 13/13。release scratch 编译成功，release `schema --command testrec --json` 与 CLI help 均确认 boundary/迁移文案；`git diff --check`、`check-docs.sh`、`verify.sh --ci-docs` 通过。
- 完整 `TestRecorderContractTests` 仍有一条既有 local-device schema expectation mismatch（实际 `target_not_found`、历史测试期望 `target_capability_missing`）；该 legacy route/runtime 不在 SP-135 范围，新增/相关 focused tests 均通过。只读终审 PASS，且已去除对后继 space 编号的预占。
- SP-135 本地 checkpoint 已提交：`37d8f9c7 fix(testrec): mark simulated replay diagnostic`。仅写入独立 worktree；未 push、PR、merge、tag、release，未触碰 #164 WIP。

## SP-136 iOS Simulator reliability collection preflight

- 已从 SP-135 checkpoint `107eac45` 建立独立 `SP-136-ios-simulator-reliability-collection-preflight` worktree。此 slice 只把真实 3 flow × 20 前的 private collection 合同固化为离线 preflight；不启动/查询 server、Simulator、Xcode、target、App 或 `test run`，也不读取或修改 #164 dirty evidence WIP。
- 预检必须验证三条 imported iOS Simulator plan、每条 20 个新鲜 direct evidence slot、显式 canonical `triton:ios-simulator:<UDID>/app:<bundle>` binding digest、initial-state/reset recipe identity、一个独立 negative control 与尚未创建的 private evidence root；成功 `ready_to_collect` 不等于采样、reset receipt、runtime verdict 或 reliability gate pass。
- stdout/schema 只允许匿名 flow alias、digest、计数与稳定状态；路径、UDID、bundle、selector、flow ID、visible text 与 reset identity 均不得回显。若需要写 evidence、自动 boot/target/server 或 fan-out `test run`，必须另建受控 live harness space。
- 实现已完成：新 CLI 只离线读取 collection/imported plans，输出固定 `ready_to_collect`、`writesEvidence=false`、`usesRuntime=false`、`eligibleForReliabilityGate=false`；negative control 必须有不复用 supported flow 的 normalized-plan digest，schema 明确区分 validation 与 reliability/preflight 的单一 JSON error envelope。focused green 为 collection 4/4、validation 14/14、reliability 25/25、import 11/11、capability 22/22、failure diagnostics 13/13；release build/schema/omitted-collection envelope、`git diff --check`、`check-docs.sh`、`verify.sh --ci-docs` 均通过。广义 schema contract/workflow suite 保留既有 device/sim/network-proxy registry failures，未越界扩修。
