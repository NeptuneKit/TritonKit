# 2026-07-28

## Xcode DerivedData 固定默认路径决策

- 用户确认 `.triton/DerivedData` 固定作为 `triton xcode settings/build/test/run` 与 `triton build ios` 的 repo-local 增量构建缓存，不限定为检测到构建冲突后才启用。
- 收敛历史设计偏差：TritonKit 不再宣称自动生成 `<project>-<hash>` / workspace-scoped 子目录；多项目隔离、并行构建或缓存恢复由调用方显式传入 `--derived-data-path`。
- 默认缓存继续保持 `preserve-by-default`，Triton 不自动清理；实现本身已符合固定 `.triton/DerivedData` 行为，本轮只同步 Xcode takeover space、技术调研、内部 skill 与 memory，无代码或 CLI schema 变更。

## v0.2.16 发布准备

- 最新已发布 tag 为 `v0.2.15`；不移动历史 tag，按下一 patch `v0.2.16` 发布当前 `main` 自 `v0.2.15` 以来的累计变更与 SP-141 packaged Web Simulator input 修复。
- 版本红灯已确认：`verify-release-package-versions.sh 0.2.16` 首次因 `TritonKit.podspec` 仍为 `0.2.15` 失败；随后将 podspec、`Web/package.json` 与 `Web/package-lock.json` 统一更新为 `0.2.16`。
- 发布面保持双架构 CLI、bundled Web、checksum、四个 public skills、Homebrew、SwiftPM tag、CocoaPods podspec 与 Web manifest 一致。CocoaPods 本轮只 lint，不自动推送 trunk；安装版验收必须包含真实 Simulator host input，而不只检查 Web 首页。
- 首次 `pod lib lint` 暴露 `TKRuntimeInputActions.swift` 中 `TKInputResult.unsupported` 的 Swift 参数顺序错误；将 `strategy` 恢复到 `matchedOID` 之前后，TritonKit 0.2.16 pod lint 通过。这是 `v0.2.15..main` 累计改动的真实 iOS 编译 blocker，未绕过。
- 发版前门禁已通过：版本一致性、release automation、public skill package、Web 82 项与 production build、CocoaPods lint、docs/diff，以及 `TRITON_VERIFY_XCODE=0 verify.sh --local` 的根 Swift 233 项、release CLI build/smoke、Harmony host 与 iOS runtime observe smoke。

## v0.2.16 发布完成

- 发布准备提交 `f986ba97` 的 main CI `30326561802` 全绿后创建 annotated tag `v0.2.16`；Release workflow `30326964768` 的 contracts、arm64/x86_64 构建、arm64 首发、x86_64 backfill 与两次 Homebrew tap 更新全部成功。
- 从新临时目录重新下载四个公开资产并完成 checksum、gzip、Mach-O 与版本校验：两份 CLI 分别为 arm64/x86_64 且均报告 `0.2.16`；public skill bundle 的 `BUILD_INFO.json` 为 `v0.2.16` / `f986ba97`，四个 public skills 版本一致。
- Homebrew 已从 `0.2.15` 升级到 `0.2.16`，`brew test NeptuneKit/tap/triton` 与仓库外 packaged mode 通过。安装版隔离端口对当前 iOS Simulator 的真实 tap 返回 `ok=true` 与 host-HID adapter 成功消息。
- 原会话 `019fa319-986f-7723-9994-8a49d85e11da` 仅替换 `34127` listener 为 `/opt/homebrew/bin/triton 0.2.16`：页面保持 `Overloaded Douyin Playback iPhone 17` / LIVE，点击显示 `tap 603,1311`，HTTP 200、console 0 error/0 warning；`19421`、Simulator 与 App 未重启。CocoaPods 本轮仅 lint，未推送 trunk。

## v0.2.16 发布会话沉淀

- 多个发布门禁放入同一 shell invocation 时必须显式 fail-fast 或逐条检查退出码；不能因最后一条命令成功而掩盖前序 `pod lib lint` 失败。CocoaPods 完成证据同时要求 exit 0 与 `TritonKit passed validation.`。
- Web input bridge 发布后的真实 smoke 必须使用与页面一致的完整手势 DTO；coordinate tap 要包含 `x/y/width/height`。缺少尺寸返回 409 只能证明校验生效，不能作为输入成功证据。
- 安装版先在仓库外隔离端口验证，再向服务所属会话交接 canonical 端口；交接只替换 Web listener，保留共享 `triton serve`、Simulator 与 App。最终证据联合检查机器可读 `ok=true`、页面 LIVE/手势状态与 console 0 error。
- 上述模式已写入内部 `tritonkit-release-package-governance`；它们属于发布领域流程，不扩大为新的 repo-wide `AGENTS.md` 规则。

## SP-141 packaged Web Simulator input 修复（已发布 v0.2.16）

- 复现 `triton web --simulator-only` 的 packaged bridge 契约矛盾：`/web/target-registry` 对就绪 iOS Simulator 宣告 host tap/swipe/longPress supported，但 Swift `/web/host-input` 仅接受 iOS real-device App runtime mirror，导致画布点击返回 real-device-only unsupported；Vite dev bridge 已支持 host target，缺口只在 packaged server。
- `CLIWebRuntime.swift` 新增 runtime mirror / host target / unsupported 三路分流：真机继续走 embedded runtime，iOS/Android/Harmony simulator/emulator 复用 `runWebHostDeviceInput`，并把 `ios-real:*` selector 纳入真机识别。没有扩大 pinch/rotate/multi-touch、坐标算法或底层 adapter 能力。
- TDD 与运行态证据：新测试先因缺少分流函数红灯；`SingleDeviceWebPageTests` 27 项、Web 82 项与 production build 通过。隔离端口 packaged server 对当前 iOS Simulator 的真实 tap 返回 `ok=true`，Playwright 点击后 UI 显示 `tap 603,1311` 且 console 0 error/warning；截图归档到 SP-141。确认旧监听为 Homebrew `triton 0.2.15` 后，仅替换 `34127` listener 为本地修复版，未触碰 `19421`、Simulator 或 App；原地址的 HTTP 与浏览器输入复验通过。
- CLI 全量以隔离 scratch 启动后暴露既有 `CLI/.build` 硬编码、schema/device-proxy/Harmony failures，并在约 3 分钟无新增输出后停止；这些不涉及 SP-141 文件，focused 与真实 packaged smoke 是本轮完成证据。修复随后随 Homebrew `triton 0.2.16` 发布，并由安装版 HTTP 与页面点击复验闭环。

## SP-140 iOS Simulator receipt-backed reliability harness（本地完成，未做真实采样）

- 在独立 `feat/SP-140-ios-simulator-reliability-live-harness` / `SP-140-ios-simulator-reliability-live-harness` worktree 建立 3 flow × 20 + 1 negative 的 receipt-backed harness：`test reliability-reserve` 只对 fresh private root 原子写 immutable receipt；`test reliability-sample` 只执行一个显式确认、reset-bound、receipt-frozen slot；`test reliability --collection-receipt` 只评估已存在的 61 个样本。
- 采样前后均 fail closed：collection/preflight/reserve 都要求 canonical uppercase Simulator UUID、exact app target 与 binding digest；sample 直接从 `/targets` 要求唯一 exact connected iOS/UDID/bundle，pin 给 runner；服务端对完整 canonical app target 禁止同 UDID fallback。slot/root、receipt、binding/reset sidecar 均 no-clobber，不自动 boot/select/reset/install/launch/shutdown App/Simulator，也不启动、停止或接管 server 生命周期、清理或 retry。
- 初始命令合同曾将任意 expected negative nonpassed 视为 `ok=true`；该宽松语义已由后续 SP-143 修正为只接受 terminal `.failed` 与 receipt 冻结 deterministic assertion failure type 的精确匹配。测试用 internal execute seam 实际走 command 参数校验与 `ExitCode` 透传，不依赖真实 HTTP/Simulator。
- focused 验证已通过：HarnessRuntime 3、Harness 11、ServerTargetSelection 2、CollectionRuntime 4、ReliabilityRuntime 28、Validation 16、FailureDiagnostics 13、SchemaFactSourceCapability 22、CreateFromSession 2、PublicSkillCommandSchema 1、shared `TKCLITransportModels` 28；并补齐了 public skill schema snapshot 中此前遗漏的 `test import` 与 reliability 命令。`SchemaFactSourceContractTests` 仅保留 6 个既有无关 device/sim app-console registry failure。未启动 `triton serve`、Simulator、Xcode、设备或真实 `test run` runtime；#164 dirty evidence worktree 未读取或修改。
- 项目 README、AI CLI control 文档与 public feedback/regression skills 已同步 receipt-bound 边界、隐私与结果/exit 语义。真实 3×20+1 仍需要用户明确授予 dedicated Simulator、server ownership、reset recipe、safe negative control 和 private evidence lifecycle；在授权前不自动执行。

## SP-142 Web readonly contract（本地完成）

- 在独立 `feat/SP-142-web-readonly-contract` / `SP-142-web-readonly-contract` worktree，将 browser 专用写入口收敛为稳定 405 JSON：`triton serve` 的 `/web/input`、`/web/node-property`，以及 packaged/dev bridge 的 `/web/host-input`、`/web/node-property` 均不再解析 body、调用 runtime/host adapter 或转发上游。通用 agent 控制面 `/input`、CLI `act` 与 `debug patch-node` 未改。
- React StreamCard 去除手势 POST，并删除已无调用方的 `streamGestureModel` / query 测试；Inspector 保留本地 patch 草案/复制但移除“应用到设备”，legacy single-device HTML 去除 tap/swipe/type/paste/clear/Home 控件。Vite bridge 删除未引用的 `hostInput.mjs`，并以静态 contract test 锁定不存在 browser write dispatch；`currentWebContracts` 的 Vite loader 关闭 config/dependency scan，避免单元测试触发 binary/server 前置条件。
- 验证：Swift `WebCommandTests|SingleDeviceWebPageTests` 49/49（Hummingbird `.router` 以畸形 JSON body 验证 5 条 write route 的 405 + endpoint/code，且不监听端口），Node readonly bridge/static tests 18/18，向独立 scratch `triton` 显式注入 `TRITONKIT_TRITON_BIN` 后全量 `npm test` 75/75，`npm run build` 与 release `swift build --product triton` 均通过。未注入 binary 时有 12 个既有 Vite SSR suite 会 fail fast；全量测试的 Vite close dependency-scan 噪声为非致命。AntD usage 完成；lint 只剩 5 条本来就存在的 static `message.*` context warning，未新增；release build 仅输出既有 Simulator 私有 Objective-C selector warning。未启动 `triton serve`、Vite dev server、Simulator、Xcode 或设备，#164 dirty evidence worktree 未读取或修改。
- 风险仍如实保留：读取 bridge 仍可能沿用既有 managed-serve 生命周期；本 checkpoint 不宣称“零宿主副作用”。当前 main 已有已发布 `SP-141-packaged-web-simulator-input`，本 branch 基线较旧；主控集成必须保留该历史并作为 SP-142 追加后重跑 docs gate，不能用跳号或复制目录伪造连续性。

### loopback hardening follow-up（本地完成，未集成）

- 复核 strict readonly candidate 时发现其 browser write route 已 405，但 Web bridge 自管 `triton serve` 仍默认为 `0.0.0.0`，并在 real-device mirror error hint 建议 LAN bind；这与固定 loopback default 和真机另立空间边界冲突。
- 先将 Swift launch-plan、Node bridge、runtime hint 和 static source contract 改成 red：`WebCommandTests` 报 2 个 `0.0.0.0 -> 127.0.0.1` 断言，Node 报 3 项 managed-host / LAN hint 失败。最小实现固定 `managedServeHost` / Node default 为 `127.0.0.1`，删除 env override，并将 real-device mirror hint 收敛为显式 approved CLI/HTTP workflow。
- green：独立 scratch `WebCommandTests` 24/24，组合 `WebCommandTests|SingleDeviceWebPageTests|ServeCommandTests` 55/55；Node `iosSimulatorBridge + iosRuntimeMirrorBridge + currentWebContracts` 19/19，`npm run build` 通过。release `triton web --print-command --json` 也证明 `readonly=true` 且 launch-plan / environment 均为 `127.0.0.1`；只打印计划，未启动 runtime。docs gate 仅因分支的 SP-142 连号空档失败，不伪造目录或放宽门禁；后续仍需在 integration worktree 与已发布 SP-141 的历史 Web host-input 行为一并重审。

## SP-143 reliability gate integrity（本地完成，纯离线）

- 审计 `main@d016979d` 后确认 Stage 1 的四项离线合同缺口：legacy `test reliability --samples` 可绕过 receipt authority 输出 passed；observation/terminal-failure artifact ref 只验存在与 manifest 声明；negative control 将任意 nonpassed 收为成功；不同 flow/slot 没有 collection-wide execution lease。
- 本 space 仅以合成临时 evidence 做 BDD/TDD：legacy input 降为固定 blocked 的诊断、显式 machine-readable authority/eligibility；receipt-backed path 才可产生 canonical gate；observation 必须满足同 step `command.executed -> artifact.created -> observation.captured`，终态 failure ref 同样按 terminal step 时序绑定；negative 只能命中冻结的 `assert_visible_failed` / `assert_not_visible_failed`，且所有前缀步骤都必须 non-optional；collection lease 在 target resolver 前 atomically claim，crash stale lease fail-closed 为 `reliability_collection_busy`。
- 已串行通过独立 scratch 的 `TestReliabilityRuntimeTests` 33、`TestReliabilityCollectionRuntimeTests` 4、`TestReliabilityHarnessTests` 16、`TestReliabilityHarnessRuntimeTests` 3、`TestRunExecutionTests` 9、`FailureDiagnosticsTests` 13、`SchemaFactSourceCapabilityTests` 22、`TestCreateFromSessionTests` 2、`PublicSkillCommandSchemaTests` 1 与 release `triton` build。`TestValidationTests` 中 10 项纯解析/schema 通过；另 6 项既有 subprocess contract test 因它们硬编码寻找 `CLI/.build/debug/triton` 而在独立 scratch 下报 “Missing triton executable”，未为通过而共享/污染默认 scratch。`SchemaFactSourceContractTests` 仍只报既有 device/sim app-console registry 6 项不一致。`git diff --check` 通过；docs check 如预期因 SP-141 / SP-142 尚未集成而在 SP-143 的连续编号处停止。未启动 `triton serve`、Simulator、Xcode、设备或真实 test runner，未读取或修改 #164 dirty evidence worktree。SP-141 / SP-142 仍为待授权集成本地 checkpoint；SP-143 的 docs 连号检查将在三者按序集成后统一复跑，不能用占位记录掩盖缺口。

## SP-144 reliability receipt anchor（本地完成，纯离线）

- 审计确认 SP-140/143 的 receipt/reset/binding/evidence 链只校验 root 内当前 bytes 的自洽；它不能检测整份有效 receipt 被替换。已裁决采用 operator/CI 在 receipt root 外保存的 SHA-256 expected anchor：reserve 输出 raw receipt SHA-256，receipt-backed sample/report 必须带回该值并在任何 reset/lease/target/runner/evidence 扫描前比对。
- 本切片明确不把 SHA-256 误称为签名、远端不可抵赖或 hostile-filesystem 防护；不涉及真实 reset/runtime/App build identity、Stage 1 分母、server/Simulator/Xcode/设备或 #164。
- reserve 兼容保留既有 FNV `receiptDigest` 作为 reset/binding linkage，并新增 raw receipt bytes 的 lowercase `receiptSha256` 作为 root 外 operator-owned anchor；sample/report 都在 decode/reset/lease/resolver/slot/runner/evidence 枚举前验证它。缺失、非法或不匹配只返回脱敏的 `missing_required_field`、`invalid_reliability_receipt_anchor` 或 `reliability_receipt_anchor_mismatch` envelope，schema recovery 仅为 `diagnose`。
- 已串行通过独立 scratch 的 `TestReliabilityHarnessRuntimeTests` 4、`TestReliabilityHarnessTests` 18、`TestReliabilityCollectionRuntimeTests` 4、`TestValidationTests` 18、`TestReliabilityRuntimeTests` 33、`FailureDiagnosticsTests` 13、`SchemaFactSourceCapabilityTests` 22、`PublicSkillCommandSchemaTests` 1 与 release `triton` build；`git diff --check` 通过。`SchemaFactSourceContractTests` 仍仅有既有 device/sim registry 的 6 项失败，不含本切片。docs 连号门禁仍会因 SP-141 / SP-142 未集成而停止，不能用占位条目掩盖。
- 后续已拆为独立队列：SP-145 的 identity-chain v2 只解决离线绑定/漂移检测；SP-146 再裁决并实现 Stage 1A 60 supported 与 Stage 1B 61 receipt/control 的公开指标口径。真实 3 × 20 + 1 仍需单独用户授权。

## SP-145 private identity-chain v2（本地完成，纯离线）

- 已从 SP-144 checkpoint `ab6cbf1e` 建独立 `feat/SP-145-private-identity-chain-v2` / `SP-145-private-identity-chain-v2` worktree。每个已完成 runner 的 receipt slot 以 no-clobber 写私有 `identity-chain-v2.json`，canonical body 绑定 root 外 `receiptSha256`、legacy linkage、flow/slot/frozen identity、terminal outcome 与 binding/reset/normalized plan/runtime target/run metadata/events 的 raw SHA-256；manifest 只增加一次 private declaration。
- receipt-backed report 新增安全 `identityChain` aggregate（state、expected/valid/missing/invalid slot counts、receiptAnchorVerified），不输出 hash、path、UDID、bundle、flow、run 或 reset identity。缺 chain 为 `identity_chain_incomplete`，body/frozen identity/terminal proof/组件/manifest 漂移为 `identity_chain_invalid`；终态不可解析明确为 `identity_chain_terminal_unavailable`，不会使用伪造 status。
- sample chain sealing 失败只返回 `reliability_identity_chain_write_failed` 并保留 claimed slot，不 backfill/cleanup/rerun；schema/recovery 仅允许 diagnose/archive。ECR/FER/ORR 分母和阈值未改变，legacy `--samples` 固定 `identityChain=not_applicable`。
- 已串行通过独立 scratch 的 IdentityChain 2、Harness 20、HarnessRuntime 4、CollectionRuntime 4、ReliabilityRuntime 33、Validation 18、FailureDiagnostics 13、SchemaFactSourceCapability 22 与 PublicSkillCommandSchema 1；release `triton` build 和动态 `test` schema 字段/错误码核验也通过。`SchemaFactSourceContractTests` 的 6 项失败仍只涉及既有 device/sim registry；`check-docs.sh` / `verify.sh --ci-docs` 仍因 SP-141 / SP-142 未集成的编号连续性停止。未启动 server、Simulator、Xcode、设备或真实 test runtime，未触碰 #164。该层只做报告时离线 byte consistency，绝不宣称真实 reset/App/runtime/history/hostile filesystem proof。

## SP-146 Stage 1 metric contract（本地完成，纯离线）

- 已从 SP-145 checkpoint `50c89bea` 建立独立 `feat/SP-146-stage1-metric-contract` / `SP-146-stage1-metric-contract` worktree。裁决为 additive report contract：既有顶层 ECR/FER/ORR 编码不变；receipt-backed report 新增嵌套 `stage1` 的 Stage 1A（60 supported ECR/ORR）和 Stage 1B（61 receipt/control integrity、FER）cohort summary，legacy `--samples` 的模型值固定为 `nil` 且 JSON 省略该字段。
- 本 slice 只做合成 evidence、report/schema/文档合同，不启动 server、Simulator、Xcode、设备或真实 test runtime，不改 SP-144 anchor、SP-145 chain、live harness、testrec/workspace/Android/Web，也不触碰 #164。真实 3 × 20 + 1 仍需用户单独授权。
- Stage 1A 已从单次 global duplicate/core analysis 取数，跨 flow 复用 reset/evidence/run identity 会扣减 60-slot completeness 与 per-flow 20 次 gate；Stage 1B 的 61-slot integrity 同时要求 receipt validation 与该 slot 的 core evidence/manifest completeness。harness 不再以 preflight target 添加 `runtime.target` 声明或覆盖 runner `manifest.target`；合成 runtime facts 只由测试 fixture 提供。
- 已通过 Harness 23、ReliabilityRuntime 33、HarnessRuntime 4、IdentityChain 2、Validation 18、FailureDiagnostics 13、SchemaFactSourceCapability 22、PublicSkillCommandSchema 1、TestRunExecution 9；独立 release `triton` 编译和动态 `test` schema 的 Stage 1 字段核验通过，终审 P1 清零，`git diff --check` 通过。`SchemaFactSourceContractTests` 仅复现既有 device/sim registry 6 个 assertion failure；docs checks 仍在 SP-143 因 SP-141/SP-142 未集成而停止，未规避门禁。

## SP-146 live authorization packet（待用户批准，文档-only）

- 为真实 3 × 20 + 1 的下一步建立 `plans/20260728-live-authorization-packet-v01.md`：逐项收集 dedicated Simulator、fixture App、`127.0.0.1:19421` server owner、reset recipe、safe negative control、private evidence / external anchor lifecycle 和执行窗口的精确授权。
- packet 明确不是 CLI 配置、receipt、sample 或 evidence，不能自动授权；在用户填明范围并明确批准前，不启动/探测/复用 server，不访问 Simulator、Xcode、设备或真实 test runtime，也不创建 live collection。
- 批准后仍先建独立 live-sampling space，执行一次 Triton-first preflight；目标、server、reset、anchor、identity-chain 或 negative 条件任一漂移即停止，不 retry、cleanup、换设备或把失败算作通过。

## Checkpoint integration arbitration（文档-only）

- 裁决为 `Revise first`：当前 `main` 的已发布 `SP-141-packaged-web-simulator-input` 是唯一合法 SP-141；旧同编号 schema checkpoint 只保留审计，schema 收口候选改用从当前 main 独立建立的 SP-147。
- 依据 AGENTS 的展示层边界，`triton web` / `/web/*` 采用 strict readonly；SP-142 的 405 / 无转发实现须在未来 integration worktree 重验，不能以旧 `/web/host-input` 发布行为视为例外。
- SP-143→146 与 SP-147→148 必须在用户明确允许本地集成后，从届时 main 的新 integration worktree 逐提交审查、处理 `CLISchemaTestCommands.swift` / docs registry 重叠，再统一执行 docs gate；本轮没有 merge、rebase、删除、编号伪造或 live action。

## Trusted baseline runtime proof packet（待用户逐项授权，文档-only）

- 把 Luna handoff 落地 1–5 的剩余环境 proof 统一列为 #166 JPEG evidence、#168 fail-closed terminate、#167 Xcode alias preflight、strict-readonly Web / loopback integration 与 Stage 1 3 × 20 + 1；每项都可独立批准，不能相互借权。
- 明确 #168 的 live proof 只允许验证预期 `app_terminate_pid_resolution_unavailable` envelope，不能执行 cold-restart nextAction；#167 只允许 target preflight，不能进入 `xcodebuild`；#166 只允许私有 screenshot/evidence 合同验证。
- 目标 4 需先明确允许 isolated integration worktree 的本地冲突处理和 local checkpoint，仍不 merge main / push；目标 5 继续复用专用 live authorization packet 的完整 8 项范围。

## SP-150 terminal failure recovery semantics（本地完成，纯离线）

- 在 SP-146 已验证离线链之上，静态审计发现 recovery 映射只看 `failure.type`，未与 terminal `step.type` 交叉验证；例如 `tap` 携带 assertion failure 可能错误计入 ECR/FER。
- 已建立独立 `feat/SP-150-reliability-failure-recovery-semantics` / `SP-150-reliability-failure-recovery-semantics` worktree。审计 runner taxonomy 后裁决：终态 step type 从已验证 normalized plan 按 stepIndex 取得；`launch_failed` 可落在任一已支持 step；`primitive_failed` 和未知/VLM 透传类型没有 contract-backed recovery，必须 `missing_failure_recovery`，不能计入 ECR/FER。
- 已先以合成 evidence 写出红灯（跨类型 assertion、`primitive_failed`、未知 VLM type 均被旧实现误计），随后补最小 pair allowlist、`terminal_failure_type_step_mismatch` completeness invalidator 及 regression；event step type 与 plan 故意对立时仍以 plan 为准，且 14 个 canonical step 的 `launch_failed` 兼容性已锁定。
- 验证：`TestReliabilityRuntimeTests` 35 项、关联 reliability/harness/collection/identity/diagnostics 81 项、独立 release `triton` build 与 `git diff --check` 均通过。未启动 server、Simulator、Xcode、设备或真实 runner，不触碰 #164；`check-docs.sh` / `verify.sh --ci-docs` 已在既有 `SP-143-reliability-gate-integrity` 连号门禁停止，待授权集成后统一重跑。
