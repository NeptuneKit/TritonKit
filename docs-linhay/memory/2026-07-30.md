# 2026-07-30

## SP-153 · GitHub #173 Xcode run target binding（本地完成）

- 根因是 `resolveXcodeInvocation` 允许显式 Simulator destination A 驱动 `xcodebuild`，却仍让 workspace default simulator B 留在 `simulatorUDID`，后续 simctl install/launch 因而可能转向 B。
- `xcode run` 现在会把显式 destination 中唯一的 Simulator `id` 固化为 invocation target；build、settings、install、launch 与 app-scoped readiness next action 复用同一 target。generic、name-only、多 id 或非 Simulator 显式 destination 在 build 前 fail closed 为 `xcode_run_target_unresolved`。
- Fake runner 的 red -> green 覆盖 stale B / explicit A 全链；Xcode focused regression 41/41、schema、失败 envelope 与 diff smoke 通过。未运行真实 Xcode/Simulator；隔离 scratch 全量仅暴露既有跨域基线失败并按任务边界停止。Docs gate 在该并行 worktree 中仅因主控预分配的 SP-153 前尚未集成 SP-142～152 而报编号不连续，须在整批 space 集成后重跑。
