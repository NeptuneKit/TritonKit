# 2026-08-09

## SP-157：GitHub 未关闭 issue 全量验证

- 固定初始线上未关闭队列为 #178–#195，共 18 条；用户随后授权本轮提交、合并、push 与按验证结果关闭 issue。
- 建立独立 worktree：`../TritonKit-worktrees/SP-157-open-issues-178-195-audit/`，branch `feat/SP-157-open-issues-178-195-audit`；#164 dirty worktree 未读取、未修改。
- 12 条 issue 已完成本地窄修复：Simulator host tap、Xcode 条件 build setting、Simulator data path、iOS proxy fail-closed、Harmony bm/open-url/wait/screenshot/start/stop/current 等。
- 3 条 issue 完成安全收口：gesture-only tap、stale hierarchy、UIAlertController modal boundary 均保持 fail-closed，并输出结构化恢复建议或错误。
- #180、#184 已有静态覆盖；#195 的可执行 DDI recovery 后续拆入 SP-159，真实设备 readiness 仍不能以 fixture 代替。
- 本机 HTTP `127.0.0.1:19421` server unavailable；无 Android、Harmony、物理 iOS target。根包 UIKit case 在 macOS 目标下没有可执行测试，iOS 编译目标被当前 SwiftPM sysroot 配置阻塞。
- focused 证据：DeviceCrossPlatform 100、SimulatorAdvancedControls 33、SingleDeviceWebPage 28、SchemaFactSourceContract 52、HarmonyWaitRuntime 4、EvidenceBundle 25、AppOpenURLFlow 7、Xcode working-directory 1 均通过。
- 提交 `ad1e55a9` 已进入本地 main；逐条关闭仍等待全轮主线门禁和远端 push。

## SP-158 / GitHub #196

- 在独立 `SP-158-issue-196-xcode-archive-export` worktree 实现 schema-backed `triton xcode archive` 与 `triton xcode export`。
- archive 固定 generic iOS destination，export 要求显式有效 `ExportOptions.plist`；两者保持重复 build settings、provisioning flags、argv 边界、bounded JSON/JSONL progress、artifact paths、final summary 和稳定 recovery contracts。
- `TKXcodeActionSummary` 增加 archive/export artifact fields；没有宣称真实签名、IPA 安装、App Store Connect 或私有 workspace 成功。
- focused archive/schema/export、BuildRunnerTests 与 full `XcodeCommandTests` 通过；parent 及 direct-child schema 均断言 `xcode-archive` / `xcode-export` `providedCapabilities`。release CLI build 通过。
- full `XcodeCommandTests` 46 项通过；流式 Xcode host runner 改用 `terminationHandler`，避免阻塞式 `waitUntilExit()` 占用全局队列，并恢复 `--destination` 既有 readiness 文案契约。
- 提交 `b3d0c27c` 已准备进入本地 main；真实签名/IPA 安装仍是风险边界。

## SP-159 / GitHub #195

- `ddi_missing` 保持稳定错误码；CLI 实际 `failHostCommand` 与可测试的 `hostCommandNonZeroExitErrorDetail` 统一输出 `app install --platform ios --scope real --device <selector> --app <app-path> --json` recovery。
- trust、Developer Mode、locked、offline、devicectl 缺失映射保持独立；offline 仍走 `device wait-ready`。
- `DeviceCrossPlatformTests` 98 项、`FailureDiagnosticsTests` 16 项和 CLI debug build 通过；`git diff --check` 通过。
- 仅使用脱敏/合成 fixture，未运行真实 iPhone/iPad DDI preparation；不宣称真实设备 readiness、签名或 tunnel 成功。

## SP-160 / GitHub #197

- 新 issue #197 在 #178–#196 push/CI 期间出现，另立 `SP-160-issue-197-harmony-wait-budget` worktree；不把它混入初始快照误关。
- `waitForHarmonyText` 新增 documented 0.4 秒 minimum layout capture budget；剩余预算低于 0.4 秒时提前返回标准 timeout envelope，不启动 `dumpLayout`/`recv`。
- TDD 首次红测捕获到 29 次近截止时间 layout capture；实现后 `HarmonyWaitRuntimeTests` 5/5 通过，保留正常预算下 dump/recv 共享绝对 deadline 的合同。
- 未执行真实 Harmony HDC/ArkUI smoke；该限制在 issue 验证评论和 space 记录中保留。

## 最终 GitHub issue 收口

- SP-157/158/159/160 已串行合入主线 `e77c72b7`；中间 push 触发的 CI `31299676013` 与最终主线 CI `31301092517` 均全绿，后者的 scope、contracts、podspec、Swift tests job 全部 success。
- #178–#197 共 20 条 issue 已逐条发布脱敏验证评论并关闭；最终 GitHub API open issue 查询返回 `[]`。#164 dirty worktree 持续未读取、未修改。
- 真实物理 iOS DDI/tunnel、私有 Xcode signing/IPA install、Harmony HDC/ArkUI live smoke 未执行，继续作为明确风险，而不是被本地 fixture、host smoke 或 CI 结果替代。

## Skill 沉淀

- 本轮将 GitHub 全量 issue 收口中的可复用流程沉淀到 `.agents/skills/tritonkit-ops-governance/SKILL.md`：初始/增量/最终三次 open 快照、增量 issue 独立建 space、最终 docs-only push 与对应 CI、最后一次空队列复查。
- 新增 GitHub DNS/API 降级规则：不改 remote、不把网络故障当完成；必要时使用 `dig` 解析临时地址、SSH `HostName` 与 API `curl --resolve`，token 不打印，所有远端事实仍以 SHA/CI/open 查询复核。
- 本次用户只要求整理 skills，未要求提交；因此仅保留未提交的 skill 与 memory 变更，不创建整理 commit。

## v0.2.17 Release

- 按 `tritonkit-release-package-governance` 选择下一 patch 版本 `v0.2.17`；先证明旧版本契约对目标版本失败，再将 `TritonKit.podspec`、`Web/package.json` 与 `Web/package-lock.json` 对齐到 `0.2.17`。未手动修改 `CLIBuildInfo.swift`，由 tag CI 写入 CLI 版本。
- 本地发布门禁通过：release package version、release automation、public skill package、Web production build、CocoaPods lint、`TRITON_VERIFY_XCODE=0 docs-linhay/scripts/verify.sh --local`；本轮本地门禁因环境变量跳过 Xcode build。
- 提交 `7fa61e05` 已推送 `main`，tag `v0.2.17` 不可移动。GitHub Release workflow `31302214544` 的 contracts、arm64/x86_64 构建、Release 资产、x86_64 checksum backfill 与两次 Homebrew tap 更新全部成功。
- 新临时目录重新下载四个公开资产，checksum/gzip、双架构 Mach-O、两个 CLI `version --json`、skills `BUILD_INFO.json` 与四个 public skill 均验证为 `0.2.17`；Homebrew 从 `0.2.16` 升级并通过 `brew test NeptuneKit/tap/triton`。
- 安装版在仓库外隔离端口 `34129` 启动 packaged Web，HTTP 返回 200。CocoaPods 仅 lint，未推送 trunk；Release 地址为 <https://github.com/NeptuneKit/TritonKit/releases/tag/v0.2.17>。
