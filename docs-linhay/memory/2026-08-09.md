# 2026-08-09

## SP-159 / GitHub #195

- `ddi_missing` 保持稳定错误码；CLI 实际 `failHostCommand` 与可测试的 `hostCommandNonZeroExitErrorDetail` 统一输出 `app install --platform ios --scope real --device <selector> --app <app-path> --json` recovery。
- trust、Developer Mode、locked、offline、devicectl 缺失映射保持独立；offline 仍走 `device wait-ready`。
- `DeviceCrossPlatformTests` 98 项、`FailureDiagnosticsTests` 16 项和 CLI debug build 通过；`git diff --check` 通过。
- 仅使用脱敏/合成 fixture，未运行真实 iPhone/iPad DDI preparation；不宣称真实设备 readiness、签名或 tunnel 成功。
