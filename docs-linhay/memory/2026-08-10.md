# 2026-08-10

## GitHub open issue 快照

- 初始快照：按 `gh issue list --state open --limit 100 --json number,title,state,url,updatedAt` 固定为 #198、#199 两条，分别是 Harmony `assembleApp` unsigned HAP 选择和 iOS `UICollectionViewCell` host-HID fallback。
- 增量快照：#198 本地集成后仍为 #198/#199；#199 本地集成后仍为 #198/#199。未执行远端评论、push、PR 或关闭。
- 最终快照：本轮本地收口后复查仍为 #198/#199；远端队列未因本地提交自动变化，等待用户明确授权远端写入。

## SP-161 / GitHub #198

- 独立 worktree `../TritonKit-worktrees/SP-161-issue-198-harmony-signed-hap/`、branch `feat/SP-161-issue-198-harmony-signed-hap`；实现提交 `ffc69a34`，通过 merge `574bbc9c` 进入本地 `main`。
- Harmony `assembleApp` 只接受文件名 token 含 `signed` 的 HAP；signed sibling 优先，无 signed 候选沿用 `hap_artifact_not_found` 单一失败 envelope；默认 `assembleHap` unsigned discovery 保持兼容。
- `BuildRunnerTests` 10/10、`BuildRuntimeTests` 4/4、release CLI build 通过；真实 Harmony 工程、签名资产和 HDC 安装未执行。

## SP-162 / GitHub #199

- 独立 worktree `../TritonKit-worktrees/SP-162-issue-199-ios-collection-cell-host-hid/`、branch `feat/SP-162-issue-199-ios-collection-cell-host-hid`；实现提交 `711db3f2`，通过 merge `38967558` 进入本地 `main`。
- `act tap` 新增显式 `--allow-host-hid-fallback`，只对已解析、有 finite/on-screen geometry 的 connected iOS Simulator UICollectionViewCell rejection 走现有 host-HID；默认仍 fail-closed。结果保留 source/strategy/geometry/fallbackFromStrategy/sourceCommands，并以 `verification.required=true,status=not-verified` 明确 host acknowledgement 不等于业务 postcondition。
- `CollectionCellHostHIDFallbackTests` 8/8、`InputOutputTests` 4/4、release CLI build、`git diff --check` 通过。
- `SchemaFactSourceTests` 仍有 5 项 `xcode-archive` / `xcode-export` capability/schema 失败；在未集成 SP-162 的基线 main 上复跑得到同样结果，确认是既有风险，另立修复，不混入 #199。
- 未运行真实 Simulator/Baguette、真机或私有 API smoke；不把 fixture/host acknowledgement 宣称为业务完成。

## 集成与治理

- `docs-linhay/spaces/INDEX.md`、`spaces/README.md` 已登记 SP-161/SP-162，覆盖范围更新为 162/162；两个 space 标记为“已完成（本地）”，远端 issue 仍待授权收口。
- 保留已有 #164 dirty worktree 不读取、不修改；本轮未清理其他独立 worktree。
- 下一步仅在用户明确授权后执行远端 push/PR/验证评论/关闭；此前不得把本地 merge 误报为 GitHub issue 已关闭。

## 本地门禁

- `docs-linhay/scripts/check-docs.sh` 通过（162 spaces registered）；`git diff --check` 通过。
- `TRITON_VERIFY_XCODE=0 docs-linhay/scripts/verify.sh --local` 在 root Swift tests 阶段因 `swiftpm-testing-helper` signal 11 中止；改用 `swift test --disable-sandbox --parallel --num-workers 1` 复跑仍为同一 signal 11。依赖边界与 iOS DEBUG isolation 已通过，未把总门禁误报为全绿。
- 因总门禁在 Swift tests 阶段提前停止，单独补跑 release CLI build（通过）、release CLI version/schema/validation smoke（通过）、Harmony host smoke（通过）和 iOS runtime observe smoke（通过）；Xcode Simulator build 因 `TRITON_VERIFY_XCODE=0` 跳过。
