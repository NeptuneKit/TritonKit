# 2026-08-14

## 发版 v0.2.19（issue #198–#206 收口后的下一 patch release）

- 发布范围：自 `v0.2.18` 之后合并进 `main` 的 21 个 commit，覆盖 GitHub issue #199（SP-162 recovery follow-up）、#200（SP-163 simulator 前台路由）、#201–#206（SP-164 Harmony act find、SP-165 sim tap point-space、SP-166 文本查询一致性、SP-167 iOS WebView 表单输入、SP-168 target lease、SP-169 app marketing version）。
- 版本对齐：`TritonKit.podspec`、`Web/package.json`、`Web/package-lock.json` 均更新为 `0.2.19`（SwiftPM `Package.swift` 无版本字段，不添加）。
- 发布门禁全部通过：
  - `verify-release-package-versions.sh 0.2.19`（先证明 0.2.18 不匹配，再通过）
  - `verify-release-automation.sh`、`verify-skill-package.sh`、`verify-public-skill-commands.py`（随 skill-package 内部执行）
  - `check-docs.sh`（169 spaces）、`git diff --check`
  - `npm --prefix Web run build`（✓ built）
  - `pod lib lint TritonKit.podspec --allow-warnings --skip-tests`（`TritonKit passed validation`）
  - `TRITON_VERIFY_XCODE=0 verify.sh --local`
- 发布方式：`docs-linhay/scripts/release.sh v0.2.19 --yes`（`TAP_GITHUB_TOKEN` 已在 repo 配置，Homebrew 由 Release workflow 自动推送 `NeptuneKit/homebrew-tap`）。
- 发布后验证项：GitHub Release 资产（arm64/x86_64 CLI tar、checksum manifest、`tritonkit-skills.tar.gz`）、CI/Release workflow（含 x86_64 backfill）、Homebrew `brew upgrade` + `brew test`、`triton version --json`、packaged `triton web --print-command --json` 与真实 packaged web HTTP smoke。

## 发布后验证（v0.2.19）

- GitHub Release `v0.2.19`（`https://github.com/NeptuneKit/TritonKit/releases/tag/v0.2.19`）已发布，含 4 个资产：`triton-macos-arm64.tar.gz`、`triton-macos-x86_64.tar.gz`、`tritonkit-skills.tar.gz`、`tritonkit_checksums.txt`。
- Release workflow `31793086587` 全绿（Release Contracts、Build CLI arm64/x86_64、Package arm64、Publish x86_64、Update Homebrew tap x2）。
- 资产校验：fresh download 后 `shasum -a 256 -c tritonkit_checksums.txt` 全 OK；`gzip -t` 三种 tar 全 OK；arm64/x86_64 CLI `file` 分别为 `Mach-O arm64`/`Mach-O x86_64`，`triton version --json` 均报 `version: 0.2.19`。
- skill 包：`TritonKit.skills/BUILD_INFO.json` = `{version:"0.2.19", releaseTag:"v0.2.19", commit:35d4112}`，含 tritonkit-dev-feedback/emulator-cli-takeover/real-project-regression/update 四 skills。
- packaged web：从独立 release archive（`/tmp`，非源码 checkout）跑 release `triton web --print-command --json` → `mode: packaged`、`bundledWebRoot` 指向包内 `web/`、`readonly: true`。
- Homebrew：`brew update` + `brew upgrade NeptuneKit/tap/triton`（0.2.18→0.2.19）+ `brew test` 全通过；`/opt/homebrew/Cellar/triton/0.2.19/bin/triton version --json` = `0.2.19`，PATH 上 `triton version` = `0.2.19`。tap formula 两 commits “Update triton to v0.2.19”（arm64 + x86_64 backfill）。
- x86_64 CLI 由 backfill job 交叉编译后随本 release 一并发布（资产已含 x86_64 tar），后续 Intel runner 端到端验证为已满足的 backfill gate。
