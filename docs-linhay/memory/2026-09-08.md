# 2026-09-08

## GitHub open issues #207～#212

- 原始集合6条，主仓/远端main=a56d7d6a；保留用户CLAUDE.md改动及旧worktree。
- 复用SP-170～173的前期成果，新增SP-174（label attrs）、SP-175（Xcode缓存证据）、SP-176（隔离集成）。没有把未验证并行工作写入main。
- 主控审计补修：Harmony显式page/callback/转发清理；longPress与tap duration不假成功；collection eligibility/verification；Xcode pipe完整性与compact parser；UIView oid/text runs；目录快照不冒充incremental。
- 真实UIKit验证已经运行：#211首轮3/4，修复动态trait fixture后4/4；合并46tests中19pass27fail，失败集中在旧window fixture无活动window（继续修复fixture，不放松runtime安全门禁）。
- CLI全量初轮972tests/34assertions失败，分类为真实schema缺口、旧capability/artifact注册缺漏、binary locator和并发stdout捕获；后续串行回归作为完成证据。
- 可复用规则：先检查子命令schema，不把父inventory当作全部子命令支持；UIKit条件测试必须报告实际iOS destination结果；task log headers不能冒充唯一任务/缓存命中。
- 所有远端写入状态与最终验证以SP-176验收记录为准。

- 最终验证：CLI 977/977、root 269/269、真实iOS26.5 UIKit46/46全过；新CLI真实Xcode公开package build也成功，目录存在但reuse unknown且observed无编译，未假称warm。专用Simulator已shutdown，未影响原有模拟器和server。最终本地/文档门禁及远端授权状态见SP-176。

## GitHub issue 收口授权与集成

- 用户明确同意提交、合入main、推送和关闭#207～#212。
- 6个issue实现已各自保留提交/merge历史；host tap duration与cache schema作为独立follow-up提交，已验证snapshot完整复制至main。
- CLAUDE.md保留原有用户修改；没有stash、没有覆盖用户文件。下一步为main本地门禁、push、实现CI、逐issue关闭及docs-only归档CI。
