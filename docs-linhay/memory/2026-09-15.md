# 2026-09-15

## SP-178 / GitHub #213 本地修复（旧分支编号 SP-177）

- 用户确认排查 GitHub open issue；初始队列仅 #213（真机 host launch 后 embedded runtime 不可达）。本轮授权不含提交、合并、推送、发布或关闭 issue。
- 主仓 `CLAUDE.md` 既有改动保持不动，复用 SP-177 独立 worktree / `feat/SP-177-issue-213-real-device-runtime`，基于既有 `4369552f` 补齐。
- 根因证据：live smoke 调用会打印错误信封的 `resolveRuntimeClient(...jsonError:true)`，连接失败后又被 smoke 转为 summary；stdout 出现额外 JSON，原始错误变为 `ExitCode(rawValue:1)`。新增 red 测试真实触发：5 tests 中 2 failed / 5 issues（1 个 resolver 断言、4 个 setup hint 断言）。
- 修复：smoke 使用不打印输出的 `resolveTarget`，由 smoke 唯一拥有错误 summary；真机 hint 提供 Debug bootstrap、可信 LAN server、App 进程 `--env` 注入和显式 runtime target 的诊断路径。不自动开放监听，不提供 USB tunnel 或 host/runtime 自动绑定。
- 文档同步 README、iOS integration guide、两个 public skill 的 iOS reference 和 SP-177；撤回旧 space 尚未实现的自动 endpoint 注入验收承诺，采用 issue 允许的 readiness failure + setup 路径。未新增 JSON 字段或参数，不改变现有 schema 结构。
- 验证：focused 5/5；完整 CLI `--no-parallel` 978 tests / 76 suites 通过，明确 `TRITON_CLI_PATH` 指向本次 debug 产物。日志位于 worktree `.build/issue-213-focused.log`、`.build/issue-213-cli-full.log`。
- 本地总门禁通过：`TRITON_VERIFY_XCODE=0 docs-linhay/scripts/verify.sh --local`，根包 269 tests / 34 suites、Release CLI build、CLI/Harmony/iOS runtime observe 脚本 smoke、docs/diff 全过；日志 `.build/issue-213-local-gate.log`。明确跳过 Simulator build，本轮不改 UIKit、不操作真机/Simulator；缺少本次签名 App、网络许可与目标 identity 证据，不能宣称端到端真机 smoke 已通过。
- 实现期间复查 open 队列仍仅 #213；未执行任何远端写入。锁文件与生成产物没有 tracked 变更，无需提交锁文件/生成文件。可复用的 App 进程 env 与 readiness 判定已沉淀到现有 public skill reference，不增加 AGENTS 规则。
- 尝试只读 subagent 审计，但运行未完成且无报告；关键源码与测试由主控自行核查，不计为审计通过。

## 授权发布 v0.2.21

- 用户随后明确授权「提交，发布，回复，关闭」，并单独授权直接提交已有 `CLAUDE.md` 改动，独立提交 `c79d9037`（MacOS Alias 文件，不混入修复提交）。
- 修复补齐提交 `240985db`；主线合并时发现 `SP-177-ios-simulator-resource-management` 已占用编号，保留主线编号，将 issue 文档迁至 `SP-178-issue-213-real-device-runtime` 并同步索引。历史 issue 分支/worktree 名保留以便追溯。
- 发布目标 v0.2.21，基于最新已发布 v0.2.20；发布资产、CI、Homebrew 成功后才回复关闭 #213，不移动旧 tag，不把真机端到端未测描述为已通过。
- 主线集成门禁通过：CLI 983 tests / 77 suites、root 269 tests / 34 suites；Web build、版本/发布契约/public skill 打包通过；CocoaPods 明确 `TritonKit passed validation.`（仅 lint，未推送 trunk）；`TRITON_VERIFY_XCODE=0 verify.sh --local` 通过。日志 `.build/release-0.2.21/`，真实 Simulator build 跳过。
- `TritonKit.podspec`、Web manifest/lock 的 package 版本对齐为 0.2.21；CLI build info 与 public skill 版本由 CI stamp，不手动写入 CLI 版本常量。

## v0.2.21 发布与 issue 收口

- 提交链：`c79d9037`（用户授权 CLAUDE alias）、`240985db`（修复补齐）、`4a266806`（集成/编号裁决）、`2a814fde`（版本对齐）。主线已推送；已发布 tag `v0.2.21` 指向 `2a814fde3ad780540997662635ae2ae32d619799`，不移动 tag。
- 主线 CI `34942529692`、tag CI `34943378446`、Release workflow `34943378497` 全部成功；Release 双架构构建、x86_64 backfill、两次 Homebrew tap 更新全过。
- 全新目录重新下载四个公开资产，三个 tarball 的 checksum/gzip 全过；CLI 为 arm64/x86_64 Mach-O 且两者均报告 0.2.21。四个 public skills 的 BUILD_INFO 为 0.2.21 / v0.2.21 / `2a814fde` / dirty=false。
- Homebrew 已从 0.2.19 升级到 0.2.21，formula test 与 PATH 上的 `triton version --json` 通过。设置 `HOMEBREW_NO_INSTALL_CLEANUP=1` 保留旧 keg，不重启共享服务。
- 安装版在仓库外 isolated `127.0.0.1:34129` 启动 packaged Web；HTML 和 bundled JS/CSS 均 HTTP 200，只证明包装/静态资源可用，不宣称设备业务交互通过。仅停止本次验收进程；未动共享 runtime/server 或设备。
- #213 回复：https://github.com/NeptuneKit/TritonKit/issues/213#issuecomment-5676856920；2026-09-15T08:04:02Z 已关闭，明确采用 readiness failure/setup 方案、真机端到端未复测。CocoaPods 仅 lint，未推送 trunk。
- 队列快照：初始仅 #213；发布期间复查仍仅 #213；关闭后 open=[]。归档完成后还需推送 docs-only commit、观察其 CI，再做最终 open 查询。
