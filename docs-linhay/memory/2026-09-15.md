# 2026-09-15

## SP-179 / GitHub #214

- 审计确认当前 `.triton/DerivedData` 仅有路径/存在性/构建观察，没有大小统计、dry-run 或清理入口。根目录与 iOS 子项目分别存在缓存可由不同工作目录/显式 `-derivedDataPath` 形成，不能直接判定重复或泄漏。
- 新增 `triton xcode derived-data inspect|cleanup`：默认 repo-local `.triton/DerivedData`，支持显式用户 Xcode DerivedData root；inspect 只读，cleanup 默认 dry-run，真实删除需 `--confirm`。
- 实现校验 canonical root、repo-local `.triton/DerivedData` 或用户全局 DerivedData、禁止 symlink/path escape，删除前重新检查路径；输出 totalBytes/fileCount/directoryCount/latestModification/entries/breakdown/symlinkSkipped/errors。
- 验证：默认 `swift test --package-path CLI --filter SmokeRuntimeTests` 通过（5 tests）；临时 fixture inspect 统计 8 bytes/2 files 正确，cleanup dry-run 不修改文件；docs structure 179 spaces 通过。专用 XcodeCommandTests 已补 CLI parse 契约。
- 未提交、未发布、未回复或关闭 #214；仍需完善 symlink/errors 细节、完整 CLI/根包门禁与文档 public skill，待用户授权后再提交发布关闭。
